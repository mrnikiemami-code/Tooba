# Runtime — TB-P10-T017

Host :5088 + FE :3000. Capture: `docs/evidence/TB-P10-T017/capture.mjs`. Report: `runtime-report.json` ok=true.

Canonical restore: tooba-blue / LightOnly / classic / Neutral / canonical Home.

Pre-existing: anonymous 401 `/api/auth/me`, SSR hydration warnings, occasional PDP 500 on first paint (page still rendered). No task-owned render loop.
