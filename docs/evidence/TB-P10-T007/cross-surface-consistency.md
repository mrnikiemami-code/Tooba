# TB-P10-T007 — Cross-surface consistency

Header (already tokenized), Home, PLP, PDP, Cart, Shipping, account/customer chrome resolve `--color-primary` from the same `StoreAppearancePaletteRegistry` / `STOREFRONT_PALETTES`. No page-local palette table. No per-card appearance fetch.
