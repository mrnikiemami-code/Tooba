# TB-P10-T007 — Shipping / checkout token completion

Shipping Shopeiva red brand chrome (CTA, selected method/date, hero, amounts) → primary tokens. `hover:bg-[#d32f2f]` → `hover:bg-primary-strong`.

Errors remain `text-red-600` / `bg-red-50`. Checkout/payment/confirmation leftover blue hex → primary. Cart conversion / commit semantics untouched.
