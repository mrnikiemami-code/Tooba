# Permissions

Reuse Admin panel access + existing `fulfillment.view` / `fulfillment.manage` / `order.handle` as projected by Order Operations.

View-only: queue inspect without mutation actions when permissions deny.
