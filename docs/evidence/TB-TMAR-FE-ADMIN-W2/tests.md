# Tests — TB-TMAR-FE-ADMIN-W2

```
npm run test:admin-reviews     # 6/6 PASS
npm run test:architecture      # 11/11 PASS
npm run test:discovered        # 925 executed; admin-reviews discovered; legacy fails unchanged
```

Host `TmarSourceSize` shrink-only baselines updated for admin-api/admin-screens; inventory evidence count drift pre-existing (not widened).

Pre-existing critical-storefront bg-white + typecheck/PAGE_SIZES + admin-api actor-header ESM binding debt unchanged (not in slice).
