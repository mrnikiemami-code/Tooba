# Order hub residuals (Order.Application foreign Application)
- Tooba.Promotion.Application — CHECKOUT_CRITICAL (quote path); next contract candidate after W3
- Cart.Application — REMOVED (now Cart.Contracts)
- Inventory.Application — already removed in W2
- No other App→App edges from Order.Application beyond Promotion (baseline)
