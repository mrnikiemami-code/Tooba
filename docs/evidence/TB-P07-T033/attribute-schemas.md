# Attribute schemas — TB-P07-T033

- Domain-scoped definitions: `demo_attr_{domain}_{suffix}` (41 definitions)
- 78 enum options
- 190 L3 category bindings with required/filter/variant/comparable flags
- 117 facet configurations on filterable L3 bindings
- Variant axis clamped when reused definition is not variant-capable
