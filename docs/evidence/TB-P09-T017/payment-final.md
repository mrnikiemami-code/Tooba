# Payment final — TB-P09-T017

Manual confirm/reject projected while waiting. Cancelled blocks confirm/reject (`CancelledBlockedCodes`). Successful-payment cancel keeps Order Cancelled and starts refund workflow (T016). History shows cancel + inventory release. No raw gateway JSON in normal UI. Error map covers `order.cancel.forbidden` / `order.cancelled.blocks_action`.
