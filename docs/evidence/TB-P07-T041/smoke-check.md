# Smoke check — TB-P07-T041

- No `LegacyAppDataGrid` references in src/frontend
- Server grids wired via `queryAdmin*Grid` + `postAdminGridQuery`
- Products grid unchanged (canonical reference)
- Saved view keys preserved per grid
