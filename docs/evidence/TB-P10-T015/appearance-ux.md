# Appearance UX — TB-P10-T015

Admin Settings → ظاهر فروشگاه adds «پس‌زمینه فروشگاه»:

- خنثی (default)
- رنگی ملایم

Integrated with existing dirty/save/cancel. No raw Neutral/PaletteTint labels in operator copy.

Preview canvas uses canonical `--color-page-background` from selected palette + style so Neutral stays gray paper and PaletteTint shows family tone; product card preview stays on white.
