# Article category tree picker

- `ContentArticleCategoryPicker` uses canonical `AppCategoryTree` (selection mode, no CRUD).
- L1 and L2 selectable; search; expand/collapse; path preview; clear.
- Language-scoped via article locale tree fetch.
