# Media file / video DAM

- Backend MIME allowlist extended: application/pdf, video/mp4, video/webm (default max 50MB).
- MediaLibraryDialog `assetKind` image|file|video.
- CKEditor damFile / damVideo insert safe HTML (`a.article-dam-file`, `video.article-dam-video`) via storefront media URLs only.
