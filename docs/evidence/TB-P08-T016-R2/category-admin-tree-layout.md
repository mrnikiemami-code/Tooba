# Category admin tree layout

- Tree pane widened: `lg:grid-cols-[minmax(280px,42%)_minmax(0,1fr)]` + resize-x.
- `AppCategoryTree` node titles expose `title={node.name}` full-name tooltip when truncated.
