# Landing pages grid

`/admin/landing-pages` uses `AppDataGrid` + `ORDERS_LIKE_CAPABILITIES` + `AppGridRowActionsCell` (icon Edit/Preview/Publish/Home) + Host saved views (`grid.admin.landing-pages`) + client GridQuery adapter over `GET /v1/admin/pages`.

No HTML `<table>` list.
