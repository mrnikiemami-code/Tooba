# Use-case foldering

Commands now live in `Commands/{CreateOffer,UpdateOffer,ActivateOffer,SuspendOffer,ArchiveOffer,SetReturnPolicy,SetOrderQuantityLimits}` and queries in `Queries/{GetOffer,ListSellerOffers}` with matching namespaces. The four broad dump files were removed.
