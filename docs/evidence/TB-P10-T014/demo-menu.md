# Demo menu — TB-P10-T014

منوی دموی فروشگاه (`store-demo-menu`):

- خانه (Home)
- خرید (Group) → دسته‌ها (Category) → کالای منتخب (Product) for L1–L3
- صفحهٔ دمو (Landing `landing-demo`)
- صفحهٔ کمپین (Landing `landing-campaign`) when the campaign page exists

No Article item (public href is slug-poor without Content join). Seed is idempotent and does not rewrite existing items. Header stays unset after runtime unless the operator assigns it.
