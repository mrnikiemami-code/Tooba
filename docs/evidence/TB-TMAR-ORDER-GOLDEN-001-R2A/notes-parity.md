# Notes parity
Note CRUD continues through the production MediatR/Order store path and `CanDelete` remains present. Actor display fields and typed delete outcomes are still missing, so note parity is incomplete.
