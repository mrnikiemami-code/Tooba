# Legacy helper audit
`AdminOrderCompletenessLegacyTestHelpers` still reproduces reduced Host-side helpers and does not validate the complete production projection. It was not removed because replacement production-path characterization was not completed. This remains an R2B residual.
