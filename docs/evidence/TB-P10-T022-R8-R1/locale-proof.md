# Locale Proof

- `locale=en-US` Store preview shows `Sample preview` badges (25 hits).
- Admin `template-preview-language` selector captured (`preview-language-selector-r8r1.png`).
- RTL fa-IR remains correct on Store/Sample captures.
