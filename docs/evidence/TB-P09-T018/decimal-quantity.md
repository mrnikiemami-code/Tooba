# Decimal Quantity

Queue shows 1.25 ordered; pack 0.50 remain 0.75. Formatter `formatQuantityDisplay` strips `.000000` and does not coerce to int. Item count (int) is labeled «قلم» separately from amount «مقدار».
