# Banner Isolation

Store Preview with zero banners uses Variant geometry + `/images/preview-placeholder/banner-*.svg` (and hero.svg for Hero).

Runtime: `store-banner-previewfake-isolated.png` — no `/images/fashion-template/` img srcs in banner section.
