# Source Boundary

Every fake item carries in-memory:

- `previewFake: true`
- `previewSource: "PreviewFake"` (`PREVIEW_FAKE_SOURCE`)

Not persisted. Not shown as developer jargon in UI (badge uses localized «نمونه نمایشی»).

Guards assert zero Template GUIDs / fashion-template paths / FASHION_IMAGES import in fake provider.
