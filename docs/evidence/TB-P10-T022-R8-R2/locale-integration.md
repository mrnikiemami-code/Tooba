# Locale Integration

Preview-Fake strings follow active preview locale via `previewFakeLocaleBucket` (fa/en/ar/default). Badge «نمونه نمایشی» / «Sample preview». No hardcoded single-locale strings in section components.
