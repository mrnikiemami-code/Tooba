# Offer-side contract (next task)

Offer may later expose IsReturnable / ReturnWindowDays / PolicyLabel override; checkout resolves platform default → offer override → non-returnable into OrderLine snapshot. No Offer editor in T005.
