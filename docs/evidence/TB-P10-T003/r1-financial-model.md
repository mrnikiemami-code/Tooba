# R1 financial model
Merchandise -> SellerOrder allocations (deterministic OrderBy SellerOrderId). Shipping -> StoreShipping bucket. Payable = merchandise + shipping. Single-store and marketplace share same model.
