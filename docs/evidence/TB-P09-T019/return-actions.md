# Return actions — TB-P09-T019

Capability-driven kebab via existing Admin order operations, scoped `returns-queue`.

Requested → approve_return / reject_return. RefundFailed → retry_refund. Confirm Dialog, no browser prompt. Exact quantity via `selections` → ReturnLineCommand. Stale approve mapped to `return.stale`.
