# 16 — Role members

- Selected role shows member count badge + filterable member list from `ListAssignments` grouped by `roleId`.
- Add member: UserPicker + `assignRole`.
- Remove member: `removeAssignment`.
- Click member / «دسترسی مؤثر» opens Users tab effective preview.

Proof: `data-testid=role-members` on Roles tab.
