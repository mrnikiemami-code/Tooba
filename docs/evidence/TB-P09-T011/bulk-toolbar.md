# Bulk toolbar recovery

Toolbar intersection now uses `lineLifecycleActions` + `intersectLifecycleCodes` + `isIncompatibleSelection`.

Single selected eligible line: shared codes from seller-level or line projection → Pack Selected visible; no mixed message.

Mixed Processing + Packed: empty intersection + `selectedCount > 1` → mixed FA, no invalid bulk button.
