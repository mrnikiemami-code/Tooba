# UI preservation

No Order Detail redesign. No ارسال و تحویل work. RTL, `min-w-[920px]`, overflow-x scroll, return-deadline column, seller grouping, shipment cards, T009/T010 corrective + sequence rules kept.

T010 exact-selection (`pack_selected` requires selections) and lifecycle (pack only after StartProcessing) unchanged.

USER_VISUAL_ACCEPTED=NO.
