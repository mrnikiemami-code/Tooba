# R3 decimal regression — TB-P09-T022-R3

`CommitForPaidOrder` / directory commit preserve `Quantity` unchanged. Focused test reserves and commits quantity `1.25` and asserts receipt + post-expiry find still report `1.25`.
