# Process Manager
Promotion not invoked directly by CheckoutProcessManager; evaluation remains inside CheckoutDirectory order-write path coordinated by PM/submit host. Ordering preserved (reserve → order write with promo eval → cart convert).
