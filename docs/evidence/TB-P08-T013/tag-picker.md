# TB-P08-T013 — Tag picker

- `ContentArticleTagsPanel`: searchable chips, add existing, inline create (content.edit), remove.
- Language-scoped search/create; remount/clear on pristine locale change via panel key.
- Endpoints: `/v1/admin/content/tags`, `/v1/admin/content/articles/{id}/tags`.
