# 14 — Navigation integration

| Shell | Change |
| --- | --- |
| `vendor-shell.tsx` | «ارسال» → `/vendor-panel/fulfillments` (`live: true`, Truck icon) |
| `admin-shell.tsx` | «ارسال / fulfillment» under عملیات → `/admin/fulfillments` |

Order preserved per Shopeiva vendor menu lock (inserted after orders).
