# 07 — Shared fulfillment UI components

File: `src/frontend/app/fulfillment/fulfillment-ui.tsx`

- `FulfillmentSummaryCard` — customer-facing card using existing gray/blue Shopeiva tokens
- `FulfillmentShipmentList` — multi-shipment list with tracking + timestamps
- `ShipmentStatusBadge` — status chip reuse via `fulfillmentStatusBadgeClass`

No new global CSS files. No motion/spacing redesign.
