# 13 — Multi-shipment UI readiness

`FulfillmentShipmentList` renders `shipments[]` from Host snapshot.

- Customer summary card lists all shipments with tracking references
- Seller detail lists shipments and per-shipment mutation controls
- Admin detail read-only shipment list

Partial quantities represented via shipment line counts and item shipped/ordered rows on seller detail.
