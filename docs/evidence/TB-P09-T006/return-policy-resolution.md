# Return Policy Resolution

Store options (Tooba:ReturnPolicy): DefaultReturnWindowDays=7, SellerCanOverride, Min/Max, AllowNonReturnable. Resolver: Default|Custom|NonReturnable; category hard-restriction wins when provided. Invalid override falls back to store default at checkout.

