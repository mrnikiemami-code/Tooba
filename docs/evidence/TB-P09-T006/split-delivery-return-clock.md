# Split Delivery Return Clock

Per-line LineDeliveredAt from delivered shipment allocations. Undelivered qty does not start clock. Admin UI uses min DeliveredAt among shipments containing that line. Eligibility window per-line.

