# UI Preservation

No Orders grid/detail redesign. Widths, filters, kebab, اقلام و ارسال, financial tabs, history, notes, shipment modal unchanged.

Added only:

- whole-order dedupe in scope filter
- `correct_tracking` uses existing tracking Dialog (title «اصلاح کد رهگیری»)
- confirm Dialog messages from API (`confirmMessageFa`) for restore/reject-shipment/restore-cancel
- error map FA/EN for restore/tracking codes
