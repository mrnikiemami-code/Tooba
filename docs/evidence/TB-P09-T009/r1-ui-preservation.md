# T009-R1 UI preservation

No Orders grid/detail/اقلام و ارسال redesign. Widths, filters, kebab, history, notes, shipment modal, corrective-action menu/dialog styling unchanged.

FE: consume projection (hide `restore_cancelled_order` when Host omits it). Added FA/EN map for `order.restore.seller_payout_completed` only.
