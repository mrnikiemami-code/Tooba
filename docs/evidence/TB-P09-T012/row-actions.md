# Row-Actions — TB-P09-T012

Kebab hidden when `lineLifecycleActions` empty.

ROW_CODES: `mark_processing`, `pack_selected`, `unpack`.

Composer projects per-line `mark_processing` labeled «شروع پردازش این قلم» on ReadyToFulfill.

Processing: kebab «بسته‌بندی این قلم». Packed unallocated: kebab unpack.

T011 seller-level fallback kept when line-scoped action is absent.
