# Return-Display — TB-P09-T012

T011 dedup kept. Backend `returnRemainingDisplay=""` + `returnDeadlineDisplay="7 روز پس از تحویل"`. FE `canonicalReturnDisplay` shows the policy once. Column مهلت مرجوعی unchanged.
