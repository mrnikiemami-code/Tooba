# Lifecycle — TB-P09-T012

T010 preserved: pack before `mark_processing` is 400 `order.operation.invalid`. After StartProcessing, `pack_selected` is allowed. Seller stays Processing until all lines packed.

Runtime B/C/D on `01a07f1b-41de-7000-83ff-b2484761140e`.
