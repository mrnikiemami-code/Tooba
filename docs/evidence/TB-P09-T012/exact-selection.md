# Exact-Selection — TB-P09-T012

`pack_selected` still requires `selections`. One-line pack does not auto-expand siblings. `AdminFulfillmentScopeSequenceTests` remain green.

Runtime C: pack L1 qty 1 of 2 only that line.
