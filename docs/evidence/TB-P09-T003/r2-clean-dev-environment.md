# R2 clean dev environment

LOCAL `tooba_alpha` @ `127.0.0.1:5432`.

No destructive wipe of historical checkouts (FK/events risk). Fresh Order created via storefront cart→checkout→manual payment APIs; list smoke focused on that checkoutId.

Master data (offers/sellers/payment config) preserved.
