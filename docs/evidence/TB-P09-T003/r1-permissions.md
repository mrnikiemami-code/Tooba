# R1 permissions

- Unauthorized ops still 403.
- Note delete requires `order.handle` + author/lock rules.
- View-only path unchanged (`order.view` for list/detail/notes read).
- No new permission families.
