# R1 operations menu

- `AdminOrderOperationsMenu` renders via `createPortal(document.body)` with fixed coords from trigger rect.
- Empty copy: «هیچ عملیاتی مجاز نیست».
- Still API-driven only; no hardcoded transitions.
- Button appearance unchanged (same classes).
