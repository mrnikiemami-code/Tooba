# Category Product picker — TB-P07-T032

## Membership-only
- Removed change-category actions/dialog from Category→Products.
- Additional: «حذف از این دسته»
- Primary: badge «دسته اصلی» + helper to open Product; no direct remove.

## Live state (G)
- After add/remove: `selectedCount` updates immediately; grids reload via tokens without tab switch.

## Bulk (H)
- Top action «افزودن موارد انتخاب‌شده (N)» — disabled at 0, busy, partial failure, refresh.
- Row-level «افزودن» kept.

## Wording (J)
- Product Workspace: «دسته اصلی» / «نمایش در دسته‌های دیگر» + locked helper.
- Panel role badge uses «نمایش دیگر» with title for full locked term.
