# Localization — TB-P07-T034

Every demo product gets fa-IR + en:

- `name` (CreateProduct)
- `short_description` (≤512)
- `full_description` (TipTap/DOMPurify-compatible HTML: `<p>`, `<ul>`, `<li>`, `<strong>`)

Names are category-realistic (`{Brand?} {L3Name} {Series|Model|…} {n}`), not T021/Demo Product/R3 codes.
