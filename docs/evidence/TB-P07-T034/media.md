# Media — TB-P07-T034

- Exactly **5** DAM images per product (1 Primary + 4 gallery).
- Domain pools (`demo-media-prod-{domain}-{i}.png`) with varied RGB — controlled category/domain reuse.
- Attach → SetPrimary → Reorder; meaningful Persian AltText.
- No placeholder GUIDs; no hotlinks.
- Catalog media readiness (≥1 + primary) satisfied; T034 stricter exactly-5 asserted in seeder path / tests.
