# Tax / Duty — TB-P09-T014

TaxAmount = مالیات (`TaxSnapshot` / line `TaxAmountSnapshot`).
DutyAmount = عوارض (`TotalDutyAmount` / line `DutyAmountSnapshot`).
TotalTaxAndDutyAmount = Tax + Duty.

Duty persisted 0 unless a real duty calculation exists. VatAmount is not used as Duty.
