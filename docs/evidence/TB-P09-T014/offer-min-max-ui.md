# Offer min/max UI — TB-P09-T014

Seller offer form: decimal Min/Max. Product unit shown (`offer-product-unit`). No unit selector.

FE rejects Min > Max. Domain `SetOrderQuantityLimits` already rejects Min > Max and non-positive limits.
