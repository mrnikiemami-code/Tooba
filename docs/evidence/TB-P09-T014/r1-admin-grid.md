# Admin grid — TB-P09-T014-R1

Orders list column `lines` header is `تعداد اقلام`.

Value is integer `LineCount` from `SellerOrder.TotalItemCount`.

`TotalQuantity` is omitted from the list (mixed units would be misleading).

Filter/sort of `lines` uses header `TotalItemCount`.
