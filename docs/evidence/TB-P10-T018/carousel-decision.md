# Carousel decision — TB-P10-T018

Installed FE carousel: **Swiper** only (`package.json` swiper ^11.2.10). No Embla dependency.

Evaluation: a11y module available; RTL used today; touch proven on Home; SSR/Next integrated; one bundle already paid; autoplay/loop/variable slides supported.

**Primary engine: Swiper.** Second engine only if a future requirement cannot be met cleanly (none demonstrated).
