# Cache
Existing landing page MemoryCache (scope+locale+slug, 2min). Invalidation on page/section write. Campaign membership changes reflected after cache TTL / invalidate.
Identity: Store + PromotionType + Locale + campaign window (extension point; no polling).
