# runtime — TB-P10-T022-R13-R1

| Service | Probe |
|---------|-------|
| Host `:5088` | HTTP 200 `/health` at task start |
| FE `:3000` | HTTP 200 `/fa` at task start |

Screenshots attempted via browser against Admin landing composer + storefront when auth allows; see `screenshots/` and `visual-evidence.md`.
