# TB-P10-T008-R1 — Admin Appearance regression

All 7 palettes remain selectable. ThemeMode persist unchanged. Save/Cancel/dirty unchanged. Cache still invalidates current Store only. Invalid theme/key still 400. Preview style now includes `--color-primary-on-dark` from the same registry. Status colors still independent. Host Admin tests 12/12.
