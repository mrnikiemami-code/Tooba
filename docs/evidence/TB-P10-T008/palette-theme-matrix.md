# TB-P10-T008 — Palette × theme matrix

All 7 palettes keep brand primary in both modes (inline CSS vars). Dark surfaces come from canonical `.dark` tokens. CTA contrast (primary vs on-primary) remains AA. Status tokens independent. wine-burgundy text-primary on dark paper is darker-on-dark; CTAs still use on-primary white.
