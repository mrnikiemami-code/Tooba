# TB-P10-T008 — UserChoice

Toggle `storefront-theme-toggle` in header + mobile drawer. Visible only when Store ThemeMode is UserChoice. One cookie+localStorage key. No appearance fetch/PUT. Desktop/mobile share the same document class via `tooba-storefront-color-scheme` event.
