# TB-P10-T008 — Admin ThemeMode

Existing `/admin/settings` Appearance tab now has ThemeMode cards plus palette cards. Dirty includes either field. Save PUTs `{ paletteKey, themeMode }` once. Preview adds `.dark` when draft is DarkOnly. Status chips stay success/warning/danger.
