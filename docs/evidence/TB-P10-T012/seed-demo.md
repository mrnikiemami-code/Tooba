# Seed demo

`LandingPageDevelopmentSeed` is idempotent and slug-guarded.
Creates `landing-demo` (Published) and `landing-demo-draft` (Draft) on Development Host startup / workspace bootstrap.
Does not rewrite existing pages. Not used in Production.
