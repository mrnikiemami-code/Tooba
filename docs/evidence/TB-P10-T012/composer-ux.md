# Composer UX

Metadata form + section cards + Add Section chooser + side editor.
Enable/disable, delete confirm, up/down reorder via canonical reorder API.
Persian labels only. Saved/unsaved text. Publish badge.
