# 06 — Frontend validation

```
npm run typecheck → exit 0
npm run lint → exit 0
npm run test → exit 0 (full suite incl. critical-storefront, customer, seller nav)
npm run build → exit 0 (clean .next rebuild; production routes include /customer-panel/notifications and /vendor-panel/notifications)
```

Logs: `frontend-test.log`, `frontend-build.log`
