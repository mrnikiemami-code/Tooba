# Client boundary — TB-TMAR-FE-F1

- `language-list.tsx` remains `"use client"` (grid + modal) — same as before
- Route `page.tsx` remains server composition (no use client)
- No new use client directives
- No storefront SSR changes
