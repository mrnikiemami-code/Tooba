# R18 Category override

Category workspace tab «سیاست رزرو» loads one focused GET and saves via PUT.

Each field: ارث‌بری از فروشگاه vs explicit override + backend effective/source.

R15 category scope is the line category id only. No hidden parent-tree walk.

Clearing all three fields deletes the override row.
