# R17 capabilities

- Confirm deposit still auto-reacquires (existing R8 copy).
- `canRetryReservation` and `canExtendTimer` are false on the audit view.
- No «تمدید رزرو», timer reset, or ExpiresAt editor.
- Max cycles: copy only; no hidden retry.
- Operations menu maps the new flags and does not invent reservation mutation actions.
