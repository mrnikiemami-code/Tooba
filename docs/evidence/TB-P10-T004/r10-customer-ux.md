# TB-P10-T004-R10 — Customer UX

`PaymentState` maps `PaymentStatus.Expired` → `PaymentExpired`. Copy: «مهلت پرداخت این سفارش به پایان رسیده است.» Retry button when `canRetryUnpaid`. No client expiry timer. Payment result page also surfaces Expired + retry.
