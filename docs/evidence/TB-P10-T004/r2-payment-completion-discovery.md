# Discovery

Task: TB-P10-T004-R2
Phase: P10 — Storefront Checkout Journey
USER_VISUAL_ACCEPTED=NO

Online/Sandbox: Initiate + FakePaymentGateway + /payment/sandbox + Verify existed (partial). Fake redirect omitted paymentId/attemptId (fixed). Manual gateway + Admin confirm existed; customer tracking/proof missing (added). Converted cart still returned lines (now empty). Media upload reused.
