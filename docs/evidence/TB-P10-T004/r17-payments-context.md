# R17 Payments context

Receipts grid: compact reservation column + retry/reacquire hint. Batched `GetProjectionsAsync`.

Order Detail payment card: cycle label, retry possible, needs reacquire. No full history there.

`AdminPaymentOpsView` carries the same concise fields.
