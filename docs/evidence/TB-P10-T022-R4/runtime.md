# Runtime — TB-P10-T022-R4

Host `:5088` + FE `:3000`. Capture: `capture.mjs` → `runtime-report.json` (`ok: true`).

Flow verified:
A–J Admin Fashion selector iframe Desktop/Tablet/Mobile viewport attrs 920/768/390 (no CSS zoom), summary panel, seed summary, disabled cleanup.

Screenshots under `docs/evidence/TB-P10-T022-R4/screenshots/`.
