# Language Configuration Audit

Admin GET /v1/admin/languages via loadAdminLanguages.
Preview toolbar select uses active registry rows (no hardcoded fa/en/ar list).
Default = registry isDefault row.

