# Responsive Media Proof

Device modes still resize iframe viewport (no CSS zoom).
Full-page uses browser viewport.
Same asset + focal metadata across contexts.
Screenshots folder reserved under screenshots/.

