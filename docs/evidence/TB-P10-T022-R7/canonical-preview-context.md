# Canonical Preview Context

TemplatePreviewContext in src/frontend/lib/storefront-composition/template-preview-context.ts (TemplateKey, SourceMode, Locale, DeviceMode).
Iframe /template-preview/fashion and full-page /template-preview/fashion/full both render FashionTemplatePreviewView with the same query (source,locale).
Admin builds URLs via ashionPreviewPath.

