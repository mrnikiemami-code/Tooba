# Featured + Gallery

- `ContentArticleMediaDirectory` + `article_media` table
- Endpoints under `/v1/admin/content/articles/{id}/media/*`
- Gallery reorder, alt/caption at article-use level, unassign != delete
