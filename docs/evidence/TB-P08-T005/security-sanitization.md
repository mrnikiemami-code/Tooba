# Security sanitization

- `ContentArticleBodyRules.EnsureNoEmbeddedBinary` on domain update
- `sanitizeArticleRichHtml` allowlist img src to storefront media URLs only
- No script/event handlers; product sanitizer unchanged (no img)
