# Order Endpoints foundation
`Tooba.Order.Endpoints.csproj`, `OrderEndpointModule`, and module-owned route declarations now exist. Host references the project and calls `AddOrderEndpointPresentation` / `MapOrderEndpoints`.
