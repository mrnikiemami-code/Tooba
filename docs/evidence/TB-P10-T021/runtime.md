# Runtime — TB-P10-T021

Host :5088 + FE :3000.

`runtime-report.json` → `ok=true` via `capture.mjs`.

Covered:

- Admin template picker polish
- Product/Banner wave-2 variant pickers
- Section settings + composer cards
- Fashion / AutoParts / InteriorDecor / Beauty / mixed Landing (desktop + mobile)
- Custom Home set + canonical restore

Screenshots under `docs/evidence/TB-P10-T021/screenshots/`.
