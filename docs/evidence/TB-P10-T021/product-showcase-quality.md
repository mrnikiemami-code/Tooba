# Product showcase quality — TB-P10-T021

- Reuses `StorefrontProductCardView` via `ProductRailSection` (no second card system).
- Wave-2 layouts: `tabbed`, `large-cards`, `minimal-list`, plus existing rail/grid/rows.
- Empty product sources render friendly dashed empty state (Admin hint + storefront graceful).
- Touch-safe headers/links (`min-h-11`), title truncation, SurfaceRole preserved on section shells.
