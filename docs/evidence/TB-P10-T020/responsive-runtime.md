# Responsive runtime — TB-P10-T020

Every implemented Variant has a `RESPONSIVE_CONTRACTS` entry. Mobile layout is system-owned; Admin only chooses size presets where supported (Compact/Medium/Large/ExtraLarge). Banner mosaics stack on small viewports via CSS grid classes in CompositionBannerGrid.
