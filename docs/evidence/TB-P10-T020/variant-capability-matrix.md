# Variant capability matrix — TB-P10-T020

Source of truth: `VARIANTS` + `RESPONSIVE_CONTRACTS` in `src/frontend/lib/storefront-composition/`.

Implemented wave exposes only Manual/Category/Brand/Newest/LatestArticles/ApprovedReviews. No BestSelling/Featured/MostViewed/Discounted/HotTrending admin sources. Size presets only where `sizePresetsSupported`. Autoplay only where flagged. Mobile columns/heights from responsive contracts (code-owned).
