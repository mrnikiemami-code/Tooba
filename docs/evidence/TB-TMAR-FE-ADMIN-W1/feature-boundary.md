# Feature boundary — TB-TMAR-FE-ADMIN-W1

Public: `features/admin-promotions/index.ts`

Guard updated: `frontend-feature-boundary.guard.test.ts` covers `admin-languages` + `admin-promotions` (no deep external imports into `api/`/`components/`).
