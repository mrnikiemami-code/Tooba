# Legacy failures — TB-TMAR-FE-ADMIN-W6

Known prior: critical-storefront bg-white, PAGE_SIZES, inventory count drift, legacy typecheck — unchanged.
admin-api actor ESM: fixed local import of admin-result constants (was re-export-only / unbound under Node ESM); actor denied characterization now PASS.
NEW_FAILURES = 0
