# TB-P10-T009 — Default visual equivalence

`CLASSIC_PRODUCT_CARD_CHROME` is the exact accepted T008-R1 article/media/action/badge class strings.

`classic` is default for missing/legacy keys. Restoring `tooba-blue` + `LightOnly` + `classic` returns the previous default chrome.
