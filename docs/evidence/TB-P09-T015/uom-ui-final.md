# UoM UI final — TB-P09-T015

Admin Catalog page `واحدهای اندازه‌گیری` uses AppDataGrid.

Translations are LanguageId rows (not fixed fa/en columns).

Add / Edit / Deactivate: referenced unit is not hard-deleted (`isReferenced`).

Inactive unit cannot be assigned to a new Product; existing Product still displays the unit.

ru-RU proof (T014 fixture language `01a0844c-829e-7000-ab25-64b381896053`): kg name `килограмм`, short `кг`.

Later new languages need no schema change.
