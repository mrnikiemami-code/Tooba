# Characterization — TB-TMAR-HOST-W3

Pre-existing `StoreAppearanceAdminTests` lock palette/theme/skin/background validation, persistence, cache isolation, and Admin endpoint auth reuse.

After migration: same suite wired through MediatR + Directory — **PASS**.
