# Runtime A–I

Host `:5088` recycled after T011 build. Script `t011-runtime.mjs`. Raw `runtime-raw.json` (`ok: true`, 9/9).

Slug `section-lab`. Hero + ProductCollection Newest + PromoBanner. Disable middle hides ProductCollection. Reorder keeps section IDs. Invalid type 400. Unpublish public 404. Temporary rows deleted.
