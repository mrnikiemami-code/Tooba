# TB-P10-T001 — Visual smoke

Compared against Shopeiva Header mini-cart (overlay + left drawer + line cards + تکمیل خرید) and existing Tooba blue cart page.

Preserved:

- Drawer width / overlay / close
- Header badge placement
- Cart hero + summary composition already on Tooba cart page
- Product card layout (ATC moved to real button under link, same blue CTA)

Not redesigned. Accent remains `#2563EB` (Tooba remapped Shopeiva red).

`USER_VISUAL_ACCEPTED=NO`.
