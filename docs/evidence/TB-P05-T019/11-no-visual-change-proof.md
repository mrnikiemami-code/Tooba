# 11 — No Visual Change Proof

TB-P05-T019 scope lock compliance:

- Home visual structure unchanged except non-visual wrapper `data-testid="home-hero"` around existing slider
- PDP visual structure unchanged except non-visual `data-testid` hooks (`storefront-pdp`, `pdp-sticky-tabs`, `pdp-tab-*`, `pdp-other-sellers`, `pdp-related`)
- No Mega Menu / Product domain / Customer/Seller/Admin UX changes
- No redesign, no section reorder, no card-family replacement

Diff intent: tests + docs + capture tooling + minimal test hooks only.
