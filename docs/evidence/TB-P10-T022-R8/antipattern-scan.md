# Anti-Pattern Scan — CLEAN

Rejected: generic placeholder Store path; Store→Template fallback; DB fakes; scattered magic counts; separate fake renderer; all-fake replacing real; published fakes; hardcoded single-language labels; third-party hotlinks; clone; other-industry; polling workarounds; TB-P10-T023.
