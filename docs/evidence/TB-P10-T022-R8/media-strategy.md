# Media Strategy

Fake banners/hero/products/categories use project fashion-template images (no third-party hotlinks). Same `CompositionBannerGrid` / `LandingHero` image path + focal `objectPosition` as real items.
