# Fake Preview Data

In-memory only (`preview-fake-*` ids). Local assets from `/images/fashion-template/*.jpg`. Localized via locale-prefix maps (fa/en/ar/default). Marked `previewFake: true` + badge «نمونه نمایشی». Never DB/Template Catalog.
