# 13 — Permission Matrix UI

Shared `PermissionMatrix` behavior inside `AccessControlCenter`:
- search, module filter, selected-only
- expand/collapse groups
- select all / clear group
- disabled-by-ceiling + platform-only badges
- Shopeiva density (rounded-2xl cards, blue accent `#2563EB`)

Path: `src/frontend/app/access-control/access-control-center.tsx` (`data-testid="permission-matrix"`).
