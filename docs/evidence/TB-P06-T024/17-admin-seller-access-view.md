# 17 — Admin Seller Access View

Route: `/admin/sellers/{sellerId}/access-control`
API: `/v1/admin/sellers/{sellerId}/access-control/*`
Same shared UI (`mode=admin-seller`) with ceiling editor.
Admin override/revoke audited via `AccessAuditEvent`.
