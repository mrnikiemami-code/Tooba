# 14 — Role Management UI

Role list with real `permissionCount` / `assignmentCount`.
Create / clone / archive custom roles; system roles protected.
Unsaved-changes guard before switching roles.

Mounts: `/admin/access-control`, `/vendor-panel/access-control`, `/admin/sellers/[sellerId]/access-control`.
