# 19 — Access Control Tests

`AccessControlFoundationTests`:
- static boundary + catalog + endpoint paths
- ceiling / platform escalation / Mobile ALLOW / Books DENY / system immutable / foreign seller

`AuthorizationFoundationTests` schema v3 + capability definition.
`SpiceDbIntegrationTests` after renaming object type to `capability` (keyword-safe).

Log: `access-control-tests.log`, `authz-retest.log`, `dotnet-test-full.log`.
