# Rebuild projection — T021-R1

Cancel Created package → Released membership; Cancelled package excluded from SelectPreferredCustomerPackage.
Create new package → becomes primary (new MP number + tracking).
Admin history may retain cancelled package events; customer primary tracking follows active package only.
