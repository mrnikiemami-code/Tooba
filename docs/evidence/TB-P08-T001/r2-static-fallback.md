# TB-P08-T001-R2 — Static Fallback

- `SUPPORTED_LOCALE_DEFINITIONS` documented as routing bootstrap fallback only.
- DB `localization.languages` remains authoritative for Admin and Content validation.
- Runtime locale discovery for new languages beyond fa/en deferred (future task).
