# Fonts toolbar (TB-P08-T016-R5)

- `shouldNotGroupWhenFull: true` so Font Family / Size stay visible.
- `fontFamily` / `fontSize` moved earlier in toolbar (after heading).
- Existing family list (Arial…Vazirmatn / B Nazanin) and px sizes (`12px`…`28px`) unchanged.
- EN still uses `translations: undefined` (not empty array).
