# Real preferences

- Module: `UserPreference` (`user_preference` schema).
- Field: Locale only (`fa` | `en`).
- Routes: customer + admin operator preference endpoints.
- Persistence via `IUserPreferenceDirectory` Upsert/Get own-only.
- Theme / timezone / notification prefs not invented.
