# Languages DB-driven

Article list tabs from loadAdminLanguages (active, sortOrder). Mount-once fetch + selection generation guard; en/fa normalization. English click stays on en-US without bounce to fa-IR.
