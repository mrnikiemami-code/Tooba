# Seed demo

Idempotent MenuKey `store-demo-menu`, title «منوی دموی فروشگاه».
L1 Home + Group, L2 Category, L3 Product when data exists, Landing `landing-demo` when published.
Does not assign HeaderMenuId so fallback remains until Admin assigns.
