# Nesting contract

MaxDepth = 3. Parent must belong to same menu. Cycle walk rejected. Sibling SortOrder rewritten on reorder; MenuItemId stable. Disabled parent hides descendants in public projection; editor keeps rows.
