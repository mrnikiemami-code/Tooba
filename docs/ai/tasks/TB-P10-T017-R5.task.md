PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_REPAIR_TASK

Task-ID:
TB-P10-T017-R5

Parent-Task:
TB-P10-T017-R4

Channel:
tooba-main

WorkerId:
tooba-worker-01

AgentType:
cursor

Status:
REPAIR

USER_VISUAL_ACCEPTED=NO

Title:
Surface Composition Repair — Section Context, Inherit-by-Default, Local Derived Surfaces, Card-Area Guard

Architect Review

TB-P10-T017-R4 improved component-level token compliance, but USER VISUAL REVIEW still finds a systemic composition defect.

Real PDP + Cart runtime proves:

PaletteTint is active.

Outer canvas/section colors change.

Inner composition still looks like many paper-white blocks on top of the tint.

Semantic tokenization is present, but too many structural children resolve to CardSurface/ElevatedSurface.

Therefore the remaining defect is COMPOSITION, not route coverage or token presence.

Do NOT add new user-configurable colors.
Do NOT add page/component-specific color settings.
Do NOT redesign Admin/Seller panels.
Do NOT start TB-P10-T018.

Locked User Color Model

The ONLY user-facing/global surface colors remain:

PageBackground / زمینه صفحه

SectionSurface / بخش اصلی

SectionAlternate / بخش جایگزین

SectionAccent / بخش برجسته

Everything below these is system-derived.

Canonical composition rule:

Default child visual surface = inherit / transparent.

A child gets an independent local surface only when it is truly:

Card

Elevated panel

Input/Form

Interactive control group

Media well

Overlay/Modal

Status/Feedback

Structural/content wrappers MUST NOT become cards by default.

Target Architecture

PageBackground
-> Section Context
-> normal structural child = inherit
-> true card = local-card
-> elevated = local-elevated
-> input = local-input
-> interactive = local-interactive
-> media = local-media
-> status = status semantic

Local surfaces derive from the ACTIVE SECTION CONTEXT.

Scope

IN SCOPE:

Storefront

Customer Panel

OUT OF SCOPE:

Admin Panel

Seller Panel / Seller Backoffice

Public seller pages visible to customers remain Storefront and are in scope.

Recovery

Phase: P10
Last Architect-accepted: TB-P10-T017-R4
Last Implementation: TB-P10-T017-R4
Current Repair: TB-P10-T017-R5
Expected previous HEAD: 1f111ebf4b327ec4c6bf3d94e9cc0bca5438642e
Preserve LOCK-SF-001…214.
USER_VISUAL_ACCEPTED=NO

1. Recovery Preflight

Record branch/HEAD/origin/main/status/SoT/Host :5088/FE :3000/current Appearance/unrelated user changes.
Unexpected tracked divergence => BLOCK.

Evidence:
docs/evidence/TB-P10-T017/r5-recovery-start.md

2. Section Context Contract

Create one canonical context model:

section

alternate

accent

PageBackground remains outer canvas.

Each context must expose centrally derived local semantics equivalent to:

local-background

local-card

local-elevated

local-input

local-interactive

local-border

local-media

Requirements:

centralized

Light/Dark aware

PaletteTint aware

Neutral backward compatible

no page-specific values

no persisted component settings

no new API/DB fields

Document:
docs/architecture/storefront-section-context-composition.md

3. Inherit-by-Default

Shared customer-facing primitives must make structural children transparent/inherit by default.

Required:

Section establishes context.

Ordinary child wrapper does not paint a new surface.

Card/Elevated/Input/Media are explicit semantic opt-ins.

Border-radius/padding/border does NOT imply Card.

Giant CardSurface as page/section substitute is forbidden.

4. Reclassify Components

Reclassify all R4 customer-facing visual components:

A. Structural/Content -> inherit
B. True Card -> local-card
C. Elevated -> local-elevated
D. Input/Form -> local-input
E. Interactive -> local-interactive
F. Media -> local-media
G. Status -> status
H. Decorative -> decorative

Unknown = 0.

Evidence:
docs/evidence/TB-P10-T017/r5-component-reclassification.md

5. PDP Composition — REQUIRED

Using the actual rendered PDP:

PDP main area = SectionSurface context

Gallery = local-media

ProductInfo = inherit

Buy/offer structural column = inherit

seller/offer sub-panels = local-card/elevated only when truly independent

variant group wrapper = inherit; chips interactive

tabs/details region = SectionAlternate or SectionSurface

tab body = inherit

benefit objects may be local-card; whole band is not a giant card

related products = SectionAlternate or SectionAccent

ProductCard = local-card derived from parent section

Hard requirement:
PDP must no longer visually read as a giant white card sitting on a tinted page.

Evidence:
docs/evidence/TB-P10-T017/r5-pdp-composition.md

6. Cart Composition — REQUIRED

Using actual rendered Cart:

outer = PageBackground

Hero = SectionAccent

main cart area = SectionSurface

cart list structural wrapper = inherit

each cart line may be local-card but must harmonize with its context

summary = local-elevated

coupon = local-card/input grouping

benefits = SectionAlternate; benefit items inherit or local-card only if visually justified

recommendations = SectionAlternate/Accent

ProductCards = local-card derived from parent section

Hard requirement:
PaletteTint viewport must not be dominated by paper-white cards.

Evidence:
docs/evidence/TB-P10-T017/r5-cart-composition.md

7. Home Composition

Map Home sections to section contexts.

Inside each:

headings/wrappers = inherit

rails/grids = inherit

true item cards = local-card

media wells = local-media

promo blocks = accent

avoid nested card-on-card composition

Evidence:
docs/evidence/TB-P10-T017/r5-home-composition.md

8. Customer Panel Composition

Same section-context model:

outer canvas = PageBackground

main content zones = SectionSurface/Alternate

page title/header = inherit

list/table structural wrappers = inherit unless truly independent cards

OrderCard/AddressCard/SummaryCard = local-card/elevated

forms = local-input

modal/drawer = overlay/elevated

sidebar/header keep shell semantics

no giant white main content wrapper

Evidence:
docs/evidence/TB-P10-T017/r5-customer-composition.md

9. Commerce/Auth/Content

Apply same composition rules to:

Login/OTP

Checkout/Shipping/Payment

Content/Blog/Landing

Structural wrappers inherit.
Only true independent objects paint local surfaces.
Long-form readability and checkout trust remain priorities.

10. Local Surface Derivation

Extend the central derived-surface model so local surfaces derive FROM current section context.

Conceptually:

SectionSurface -> local-card/elevated/input/interactive/media
SectionAlternate -> local-card/elevated/input/interactive/media
SectionAccent -> local-card/elevated/input/interactive/media

Do not expose these as settings.
Do not persist them.

11. Card-Area Ratio Guard — REQUIRED

Existing guards passed while composition still looked wrong.

Add generalized runtime heuristics for PaletteTint measuring visible area by semantic class:

PageBackground

Section context

Card/Elevated

Media

white-ish/neutral

Unknown

Flag suspicious composition when:

Card/Elevated occupies excessive proportion of a major section/viewport

a section context is almost completely covered by cards

a giant structural wrapper is classified as Card/Elevated

Do NOT use one brittle percentage for every page.
Use reasonable thresholds + semantic exceptions and report ratios.

Representative pages:

PDP

Cart

Home

Checkout

Customer Dashboard

Customer Order Detail

Landing

Evidence:
docs/evidence/TB-P10-T017/r5-surface-area-report.md

12. Runtime DOM/Pixel Verification

For each representative route record:

route | section context | child component | semantic role | bbox | computed bg | local token | section-area %

PASS requires:

section context visibly survives in rendered composition

true cards remain distinct

no giant cardified structural wrapper

Unknown = 0

13. PaletteTint Quality

Run:

tooba-blue / PaletteTint / Light

wine-burgundy or forest-green / PaletteTint / Light

representative DarkOnly

Verify:

section colors are visible INSIDE the page

children harmonize with parent section

no flat monochrome wash

no paper-white domination

14. Neutral Compatibility

Neutral must remain close to accepted UI.
Inherit-by-default must not destroy hierarchy.
True cards may remain white/paper in Neutral.

15. No New User Settings

HARD GATE.

Forbidden:

CardColor

PDPColor

CartColor

CustomerPanelColor

InputColor

SectionCardColor

ContextCardColor

Only the four global surface roles remain user-facing.

16. Runtime Crawl

Extend current crawler.

Representative routes:

Home

PDP

Cart

Checkout/Shipping

Login

Customer Dashboard

Customer Orders

Customer Order Detail

Landing

Content/blog

Record:

200

no 500

no retry loop

no appearance polling

section markers

local surface markers

area ratios

screenshots only for failures + final representative proof

17. Final Screenshots

Persist:

pdp-composition-r5.png

cart-composition-r5.png

home-composition-r5.png

checkout-composition-r5.png

customer-dashboard-composition-r5.png

customer-order-composition-r5.png

landing-composition-r5.png

dark-pdp-composition-r5.png

dark-customer-composition-r5.png

Store:
docs/evidence/TB-P10-T017/screenshots/r5/

18. Minimal Useful Tests

Run/reuse:

Appearance

surface-role

component-compliance

one section-context derivation contract test

one inherit-by-default guard

one card-area/giant-cardification guard

one PDP composition guard

one Customer Panel composition guard

critical-storefront

recovery-staleness

git diff --check

No per-component matrix.

19. Anti-Pattern Gate

Reject:

page/component color settings

raw color props

giant structural CardSurface wrappers

default-card structural children

route-specific palette logic

separate customer theme engine

screenshot-only CSS

arbitrary ignore lists

polling

per-component Appearance fetch

layout redesign disguised as theming

Evidence:
docs/evidence/TB-P10-T017/r5-antipattern-scan.md

20. Locks

Add non-duplicate locks after LOCK-SF-214 equivalent to:

Structural/content children inherit parent section context by default; independent surfaces are explicit opt-ins.

SectionSurface/SectionAlternate/SectionAccent establish local derived card/elevated/input/interactive/media relationships.

Card/Elevated semantics may not be used as generic wrappers for whole sections/page regions.

PaletteTint acceptance includes section-context visibility and surface-area composition, not token compliance alone.

New customer-facing components must declare inherit vs explicit local surface.

User-configurable colors remain the four global surface roles; all local surfaces are automatic.

21. Evidence

Create/update:
docs/evidence/TB-P10-T017/

Required R5:

r5-recovery-start.md

r5-component-reclassification.md

r5-pdp-composition.md

r5-cart-composition.md

r5-home-composition.md

r5-customer-composition.md

r5-surface-area-report.md

r5-focused-validation.md

r5-antipattern-scan.md

r5-recovery-sot.md

screenshots/r5/

Architecture:
docs/architecture/storefront-section-context-composition.md

22. Recovery / Pipeline

At completion:

Phase P10

Last Architect-accepted before review: TB-P10-T017-R4

Last Implementation: TB-P10-T017-R5

Current Issued/Repair: none

USER_VISUAL_ACCEPTED=NO

Worker IDLE

branch main

HEAD==origin/main

task-owned clean

unrelated user changes preserved

do NOT invent/start TB-P10-T018

Acceptance

PASS only if ALL:

only four global user-facing surface roles remain configurable

central section context exists

local surfaces are section-context aware

structural child default = inherit/transparent

component reclassification complete; Unknown=0

PDP no longer reads as giant white card on tint

Cart no longer reads as stacks of white paper over tint

Home/Checkout/Auth/Customer/Content follow same model

no giant structural Card/Elevated wrappers remain

card-area ratio guard passes representative routes

section context remains visibly present under PaletteTint

Neutral backward-compatible

Dark coherent

Admin untouched

Seller Panel untouched

no new user color settings

no appearance polling/fetch duplication

screenshots persist

anti-pattern CLEAN

locks/recovery/git exact

no TB-P10-T018

USER_VISUAL_ACCEPTED remains NO

Result Contract

Return ONLY canonical BRIDGE-WAKE-V1 Result with:

Summary
Applicable-Locks
Recovery-Start
Component-Reclassification
Section-Context-Contract
Local-Surface-Derivation
PDP-Composition
Cart-Composition
Home-Composition
Commerce-Auth-Content-Composition
Customer-Composition
Card-Area-Ratio-Guard
Rendered-Composition-Coverage
PaletteTint-Quality
Neutral-Compatibility
Focused-Validation
Visual-Evidence
AntiPattern-Scan
Lock-Registry
Recovery-SoT
Git
Architectural-Concerns
Visual-Concerns
Blockers

END_TOOBA_REPAIR_TASK