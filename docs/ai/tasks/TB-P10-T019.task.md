PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_TASK

Task-ID:
TB-P10-T019

Parent-Task:
TB-P10-T018

Channel:
tooba-main

WorkerId:
tooba-worker-01

AgentType:
cursor

Status:
ISSUED

USER_VISUAL_ACCEPTED=YES

Title:
Shared Composition Renderer + Existing Shopeiva Variants + Visual Admin Variant Picker

Architect Decision

TB-P10-T018 is Architect-ACCEPTED.

The shared Home+Landing composition foundation is sound:

one Section/Variant registry

controlled settings

code-owned responsive contracts

Swiper primary carousel engine

safe Home/Landing adapter strategy

10 industry template plans

no free-form CSS/HTML/JS

only four global user-editable surface roles

Now implement the first real production slice.

This task is NOT yet the full variant-expansion task.
It must first prove that:

existing good Shopeiva blocks can render through the shared engine,

current Landing content remains compatible,

Admin can choose Variants visually and understandably,

Home/Landing use one renderer path without visual regression.

Do NOT invent TB-P10-T020.
Do NOT mass-build every planned new Variant in this task.

Recovery State

Phase: P10
Last Architect-accepted: TB-P10-T018
Last Implementation: TB-P10-T018
Current Task: TB-P10-T019
Expected previous HEAD: 9ae63f852703ca7c1eb8a49b7392ceea9fdc7c76
Worker: tooba-worker-01
Appearance USER_VISUAL_ACCEPTED=YES
Builder USER_VISUAL_ACCEPTED=NO

Preserve LOCK-SF-001…228.

1. Recovery Preflight

Record:

branch

HEAD

origin/main

git status --short

Recovery/SoT

Host :5088

FE :3000

current Home route behavior

current Landing demo behavior

unrelated local user changes

Unexpected tracked divergence => RECOVERY_CONFLICT/BLOCK.

Evidence:
docs/evidence/TB-P10-T019/recovery-start.md

2. Implement Shared Renderer Core

Create/complete one canonical renderer that accepts shared composition sections.

Required:

SectionType

VariantKey

typed settings

data source config

responsive contract

semantic SurfaceRole

enabled/order

Renderer must serve:

Home composition

Landing composition

Landing selected as Home

No duplicate Home-only/Landing-only section renderer where a shared path is possible.

Legacy/current Landing sections may pass through deterministic adapters.

3. Reuse Existing Shopeiva Home Blocks as Registered Variants

Based on T018 audit, wire the strongest current Shopeiva blocks into the shared registry.

At minimum register and render working equivalents for existing live blocks such as:

Hero Carousel

Story Rail

Category image/cards rail

Product deal/newest carousel

Best Seller columns

Most Viewed compact rows

Banner grid

Brand rail

Reviews carousel

Articles rail

Use actual existing components where healthy.

Allowed strategies:

direct reuse

thin adapter

small extraction into shared renderer-compatible component

Avoid visual redesign.

Evidence:
docs/evidence/TB-P10-T019/shopeiva-reuse-map.md

4. Preserve Current Home Fidelity

The current canonical Shopeiva Home must remain visually/functionally equivalent when rendered through the shared composition layer.

Required:

same section order

same existing data behavior

same ProductCard behavior

same Appearance/Surface roles

same Header/Footer shell

no SEO regression

no loading regression

no accidental section disappearance

Current Home remains canonical fallback until explicitly switched to a stored Home composition.

5. Landing Compatibility

Existing published/draft Landing pages must continue to work.

Required:

current T011/T012 section configs remain readable

adapter maps legacy type -> shared SectionType/Variant deterministically

Draft preview works

Published public route works

selected Landing as Home works

no forced destructive migration

no JSON manual edits required

Evidence:
docs/evidence/TB-P10-T019/landing-compatibility.md

6. Visual Variant Picker in Admin

Upgrade existing Landing Composer section chooser/edit flow to understand Variants.

User-facing UX must include:

Add Section

visual SectionType catalog

Persian title

short description

icon/thumbnail

Choose Variant

After SectionType selection:

show only valid Variants

Persian human-readable name

thumbnail or meaningful miniature preview

one-line explanation

no raw VariantKey

Example:
Product Showcase:

«اسلایدر کارت محصول»

«ردیف‌های فشرده»

«ستون‌های دسته‌بندی»

Banner Showcase:

«تک بنر»

«دو بنر مساوی»

«چهارتایی»

Only implemented/available Variants should be selectable.
Do NOT expose NewRequiredLater variants as functional choices.

7. Variant Preview Contract

Each implemented Variant must expose admin preview metadata:

PersianName

Description

PreviewKind / PreviewAsset or deterministic mini-preview metadata

RecommendedUse optional

supported size presets

whether autoplay applies

supported data-source kinds

Do not require real Store data to understand the preview.

No empty chrome boxes.

8. Controlled Settings Form

Admin settings form must be generated/validated from the registry metadata where practical.

At minimum:

title

subtitle if supported

CTA if supported

itemCount if supported

data source

size/height preset where supported

autoplay if supported

surface role if currently allowed by product rules

enabled

Hide unsupported controls per Variant.

No JSON editor.
No CSS.
No breakpoint controls.

9. Size Presets — Runtime

Implement bounded size presets for the existing Hero/Banner/Carousel variants that need them.

Required:

Compact

Medium

Large

ExtraLarge where semantically valid

Responsive contract owns tablet/mobile behavior.

User sees preset names, not px values.

10. Swiper Standardization

Use Swiper as the primary carousel engine for registered carousel variants.

Required:

RTL

touch

keyboard/accessibility where supported

navigation/pagination only when variant requires

autoplay optional where supported

pause/interaction behavior sane

no nested duplicate Swiper initialization

no second carousel library added

Reuse existing Shopeiva Swiper setup where healthy.

11. Data Source Truth

Only expose sources that are genuinely supported for the implemented Variant.

Current known safe baseline:

Manual

Category

Brand

Newest

If existing Home has heuristic BestSelling/MostViewed logic:

it may remain on canonical legacy Home behavior if already accepted

do NOT falsely expose it as a truthful configurable Landing/Home data source unless backend truth exists

Admin must not show unavailable source choices.

12. Shared Appearance / Surface Contract

All implemented sections must inherit:

PaletteKey

ThemeMode

BackgroundStyle

four global surface roles

local section-context derived surfaces

No section-specific color editor.

No Variant-specific arbitrary color controls.

13. Responsive Contract Enforcement

For each implemented Variant:

Desktop behavior

Tablet behavior

Mobile behavior

must come from the registry.

Admin may choose size preset.
Admin may NOT choose mobile columns/breakpoints.

Runtime must use the registered responsive contract rather than ad-hoc local breakpoints where feasible.

14. Composer Reorder / Enable / Delete

Ensure existing composer operations remain stable with Variant-enabled sections:

reorder

enable/disable

delete

save

preview

publish

Stable section IDs must survive Variant edits.

15. Runtime Proof

Use Host :5088 + FE :3000.

Required runtime flow:

A. canonical Home renders with shared renderer/adapters
B. create/edit Draft Landing
C. add HeroCarousel with an implemented Variant
D. add StoryRail
E. add ProductShowcase using one implemented existing variant
F. add BannerShowcase using one implemented existing layout
G. add BrandShowcase
H. reorder sections
I. disable one section
J. preview Draft
K. publish
L. open public Landing
M. select published Landing as Home
N. verify shared renderer/Appearance
O. restore canonical Home

Persistent screenshots:

admin-add-section.png

admin-variant-picker-product.png

admin-variant-picker-banner.png

admin-controlled-settings.png

draft-composer.png

draft-preview.png

published-landing.png

landing-as-home.png

canonical-home-after-shared-renderer.png

Store:
docs/evidence/TB-P10-T019/screenshots/

16. Minimal Useful Tests

Required only:

shared renderer resolves registered SectionType+Variant

invalid Variant for SectionType rejected

legacy Landing adapter resolves deterministic Variant

only implemented Variants appear in Admin selectable registry

responsive contract present for every implemented Variant

unsupported setting rejected/hidden

current Landing publish/preview compatibility guard

current Home fallback/shared rendering guard

critical-storefront

recovery-staleness + git diff --check

Do NOT add screenshot-unit matrices or one test per variant.

17. Performance / SSR

Required:

no per-section appearance fetch

no duplicate data fetch caused solely by renderer abstraction

no client polling

no FOUC

no task-owned hydration mismatch

carousel JS loaded only where needed if architecture supports reasonable code splitting

stable SSR markup

18. Anti-Pattern Gate

Reject:

duplicate Home and Landing renderers

wrapper components that fork behavior by route without semantic reason

free-form CSS

user breakpoints

raw variant keys shown to users

empty admin previews

unsupported Variants shown as active choices

fake data sources

second carousel library

custom per-template renderer

hardcoded industry-specific behavior

Evidence:
docs/evidence/TB-P10-T019/antipattern-scan.md

19. Locks

Add non-duplicate locks after LOCK-SF-228 equivalent to:

Home and Landing runtime sections resolve through the shared Section/Variant renderer contract.

Admin may expose only implemented/valid Variants with human-readable previews; planned variants remain unavailable until implemented.

Variant settings are capability-driven and controlled; unsupported controls are hidden/rejected.

Responsive behavior comes from registered Variant contracts; mobile layout remains code-owned.

Existing accepted Shopeiva Home blocks are adapted/reused rather than redundantly redesigned.

Existing Landing configs remain readable through deterministic compatibility adapters until explicit migration.

One primary carousel engine (Swiper) serves composition variants unless a future proven gap justifies otherwise.

Use next canonical LOCK-SF numbers.
Do not renumber existing locks.

20. Evidence

Create:
docs/evidence/TB-P10-T019/

Required:

recovery-start.md

shopeiva-reuse-map.md

landing-compatibility.md

shared-renderer.md

admin-variant-ux.md

responsive-runtime.md

focused-validation.md

runtime.md

antipattern-scan.md

recovery-sot.md

screenshots/

21. Recovery / Pipeline

At completion:

Phase P10

Last Architect-accepted before review: TB-P10-T018

Last Implementation: TB-P10-T019

Current Issued/Repair: none

Appearance USER_VISUAL_ACCEPTED=YES

Builder USER_VISUAL_ACCEPTED=NO

Worker IDLE

branch main

HEAD==origin/main

task-owned clean

unrelated user changes preserved

do NOT invent/start TB-P10-T020

Acceptance

PASS only if ALL:

shared renderer serves Home/Landing composition

existing good Shopeiva blocks are reused/adapted

canonical Home fidelity preserved

existing Landing pages remain compatible

Admin visually selects Section + implemented Variant

Admin previews are meaningful

controlled settings only

size presets work where supported

mobile remains system-owned

Swiper is standardized as primary engine

unsupported ranking sources are not exposed

Appearance/surface inheritance preserved

Draft/Preview/Publish/Home-selection works

only essential tests added

no task-owned performance/hydration regressions

anti-pattern CLEAN

locks/recovery/git exact

Worker IDLE

no TB-P10-T020

Result Contract

Return ONLY canonical BRIDGE-WAKE-V1 Result with:

Summary
Applicable-Locks
Recovery-Start
Shopeiva-Reuse
Shared-Renderer
Home-Fidelity
Landing-Compatibility
Admin-Variant-Picker
Variant-Preview
Controlled-Settings
Size-Presets
Carousel-Standardization
Data-Source-Truth
Responsive-Contracts
Appearance-Inheritance
Composer-Operations
Focused-Validation
Runtime
Visual-Evidence
AntiPattern-Scan
Lock-Registry
Recovery-SoT
Git
Architectural-Concerns
Visual-Concerns
Blockers

END_TOOBA_TASK