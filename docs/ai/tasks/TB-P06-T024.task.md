PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

TASK-ID: TB-P06-T024
PROJECT: Tooba
PHASE: P06
CHANNEL: tooba-main
STATUS: ISSUED
TASK-TYPE: ACCESS CONTROL CENTER / ADMIN + SELLER
WORKER-POLICY: ONE WORKER = ONE ACTIVE TASK

Title

Professional Access Control Center — Shared Admin/Seller Role & Permission Management with SpiceDB Enforcement, Delegation Ceiling, Searchable UI and Resource-Scope Foundation

Architect Context

Architect ACCEPTED:

TB-P06-T023-R1 = ACCEPTED

TB-P06-T023 = CLOSED_BY_REPAIR

P06 = IN_PROGRESS

Current authorization foundation:

SpiceDB-based authorization is canonical.

Customer/Seller/Admin isolation is enforced at use-case boundaries.

UI hiding alone is never sufficient.

Backend/SpiceDB is source of truth.

New commercial requirement:

Platform Admin manages Admin users/roles/permissions.

Platform Admin can inspect/manage Access Control for each Seller.

Seller Owner manages roles/permissions for its own staff/users.

Seller delegation is bounded by Platform-allowed permission ceiling.

Seller cannot grant platform/admin permissions or access outside its own Seller scope.

Architecture must be ready for future resource/data scopes: Category, Product, Brand, Warehouse, Store, OrderSegment.

Representative target: a seller employee may handle Mobile-category orders but must not see Book-category orders.

Primary Commercial Outcome

Build a sellable Access Control Center usable by non-technical operators.

Required UX:

clear Role create/edit

searchable permissions

module grouping

expand/collapse

select all / clear group

clone role

user-role assignment

effective-access preview

permission ceiling visibility

resource-scope foundation

no cryptic SpiceDB tuples exposed

ABSOLUTE SHOPEIVA VISUAL LOCK

If Shopeiva has no dedicated Access Control page, build it only from closest real Shopeiva patterns.

LOCKED:

CSS

Tailwind

JavaScript behavior

animation

transition

hover/focus/active

spacing

typography

responsive/mobile

icons/badges

cards/tables/tabs

modals/drawers

search controls

component geometry

micro-interactions

Do not create a generic ACL screen that looks foreign.
Result must look as if Shopeiva originally shipped it.
Any appended/foreign UI = VISUAL REGRESSION.

A — Repository Recovery

Run:
git rev-parse --show-toplevel
git fetch origin
git branch --show-current
git rev-parse HEAD
git rev-parse origin/main
git status --short --branch

Expected predecessor:
8f44f195423fb2c7b95cf1c51b11998576515864

Require:

main

HEAD == origin/main

safe tracked tree

If conflict:
RECOVERY_CONFLICT

B — Start Runtimes Before Work

Start and keep running where technically possible:

PostgreSQL

Backend

Tooba Frontend

Original Shopeiva

Verify:

health/live 200

health/ready 200

/admin

/vendor-panel

current auth flows

Original Shopeiva relevant Vendor/Account/Admin-like screens

Evidence:
docs/evidence/TB-P06-T024/01-runtime-before-access-control.md

C — Audit Existing Authorization Architecture

Inspect:

current SpiceDB schema

authorization service/contracts

AdminPanelAccess

SellerPanel access

actor/party model

tenant boundary

hardcoded roles if any

current capability checks

existing membership relations

UI capability projection

Do not redesign SpiceDB from scratch.

Classify:
LIVE / PARTIAL / HARDCODED / MISSING / NEEDS_EXTENSION

Evidence:
02-authorization-audit.md

D — Permission Catalog

Create canonical stable semantic Permission Catalog.

Examples:
admin.dashboard.view
product.view
product.create
product.edit
product.publish
order.view
order.detail
order.fulfill
order.cancel
order.refund
order.export
seller.view
seller.approve
payment.view
payment.reconcile
promotion.view
promotion.manage
review.view
review.moderate
story.view
story.create
story.edit
story.submit
story.approve
story.reject
story.publish
content.view
content.create
content.edit
content.publish
pagecomposition.view
pagecomposition.manage
fulfillment.view
fulfillment.manage
return.view
return.manage
refund.view
refund.manage
settlement.view
settlement.manage
accesscontrol.view
accesscontrol.manage

Do not expose endpoint names as permissions.

Metadata per permission:

PermissionId

Module

DisplayNameKey

DescriptionKey

Delegable

ScopeKinds supported

dependency/risk metadata where useful

Evidence:
03-permission-catalog.md

E — Dynamic Role Model

Role:

Id

TenantId / Platform scope

OwnerScopeKind

OwnerScopeId

Name

Code

Description

IsSystem

IsMutable

CreatedAt

UpdatedAt

RolePermission:

RoleId

PermissionId

optional ScopeDefinition

Enabled

UserRoleAssignment:

Actor/User

Role

Owner scope

No hardcoded if role == ... authorization.

Evidence:
04-role-model.md

F — Admin Access Control

Platform Admin manages:

Admin roles

Admin user assignments

Admin permissions

Seller permission ceilings

Seller Access Control inspection/management

Routes consistent with repo conventions, conceptually:

/admin/access-control

/admin/access-control/roles

/admin/access-control/users

/admin/sellers/{sellerId}/access-control

Evidence:
05-admin-access-control-capabilities.md

G — Seller Access Control

Seller Owner can:

view own roles

create/edit/clone custom role

assign/remove roles to own staff

search permissions

see effective access

define allowed resource scope

delegate only within Platform ceiling

Seller cannot:

grant platform/admin permissions

exceed platform ceiling

inspect another Seller

assign foreign users

privilege-escalate itself

Evidence:
06-seller-access-control-capabilities.md

H — Delegation Ceiling

PlatformSellerCeiling
INTERSECT
SellerOwnerGrantedPermission

Backend must reject manipulated payload escalation.

Test:

ceiling disabled -> cannot assign

ceiling enabled -> can delegate

ceiling revoked -> effective deny

stale role cannot bypass ceiling

platform permission cannot be delegated

Evidence:
07-delegation-ceiling.md

I — SpiceDB Integration

SpiceDB remains enforcement authority.

Extend only as needed for:

role membership

capability relationship

seller membership

delegated ceiling

resource-scope foundation

If PostgreSQL stores UI/config projections, clearly document source-of-truth and synchronization strategy.
No parallel auth engine bypassing SpiceDB.

Evidence:
08-spicedb-integration.md

J — Typed Resource Scope Foundation

ScopeKind:

GlobalWithinOwner

Category

Product

Brand

Warehouse

Store

OrderSegment

Permission grant concept:
PermissionId + ScopeKind + ScopeResourceId / typed ScopeDescriptor

Example:
orders.handle + Category

No arbitrary SQL or free-form policy expressions.

Evidence:
09-resource-scope-foundation.md

K — Mobile-vs-Books Representative Policy

Seller A employee E:

order.view

order.handle

Category scope = Mobile

Expected:

Mobile order = ALLOW

Book order = DENY

Implement minimum correct authorization projection/gateway if required.
No cross-module SQL JOIN.

If full order-query runtime scope integration is too large, at minimum prove the authorization policy end-to-end with explicit deferred runtime integration point. Do not falsely claim full scoped filtering.

Evidence:
10-category-scoped-order-access-foundation.md

L — Shared Access Control UI

Do not build separate Admin and Seller implementations.

Shared component system should cover:

AccessControlCenter

RoleList

RoleEditor

PermissionMatrix

PermissionSearch

PermissionGroup

ScopeEditor

UserAssignments

EffectiveAccessPreview

RoleClone

UnsavedChangesGuard

Admin/Seller wrappers thin.

Difference only:
DATA SCOPE + AVAILABLE CAPABILITIES + DELEGATION CEILING

Evidence:
11-shared-access-control-ui.md

M — Shopeiva Source Mapping

Inspect actual Shopeiva closest patterns:

settings

vendor/account management

search

tabs

table/list

cards

checkbox/toggle

modal/drawer

badges

accordion

forms

user list

Map every new Access Control UI component to actual Shopeiva source components.

Evidence:
12-shopeiva-access-control-source-map.md

N — Professional Permission Matrix

Required:

permission search

module filter

module grouping

expand/collapse

selected-only filter

select all per group

clear group

descriptions

obvious enabled/disabled states

disabled-by-platform-ceiling state for Seller

desktop/mobile intentional design

No giant unstructured checkbox wall.

Evidence:
13-permission-matrix-ui.md

O — Role Management UI

Required:

list

create

edit

clone

delete/archive custom roles where allowed

protect system roles

description

real user count

real permission count

search

empty/loading/error

unsaved change warning

No fake counts.

Evidence:
14-role-management-ui.md

P — User Assignment UI

Required:

search real users

inspect roles

assign/remove role

multiple-role policy documented

effective permissions preview

own-scope only

Use existing Identity/Party users.
No fake users.

Evidence:
15-user-role-assignment-ui.md

Q — Effective Access Preview

Show in understandable terms:
WHAT can this user do?
WHERE can this user do it?

Display:

effective permissions

inherited via roles

denied by platform ceiling

scoped restrictions

dependencies/conflicts

Do not expose SpiceDB tuple syntax.

Evidence:
16-effective-access-preview.md

R — Admin Seller-Specific Access View

Platform Admin can open Seller and inspect/manage:

Seller users

Seller roles

permission ceiling

assignments

effective access

scopes

Use same shared UI.
Admin override/revoke audited.

Evidence:
17-admin-seller-access-view.md

S — UI Capability Projection

Navigation/actions react to effective permissions.

Examples:

no product.view -> Products nav hidden

order.view but no order.refund -> Orders visible, Refund action absent

accesscontrol.view without manage -> read-only Access Control

Backend still returns 403 for direct unauthorized APIs.

Evidence:
18-ui-capability-projection.md

T — Authorization Tests

Minimum:

Admin allow

Admin deny

direct API bypass denied

Seller role allow

Seller role deny

foreign Seller denied

Seller cannot grant platform permission

Seller cannot exceed ceiling

ceiling revoke removes effective permission

Seller cannot manage foreign staff

view-only cannot mutate

accesscontrol.manage required for edits

system role immutable

tenant isolation

Mobile category allow

Books category deny

UI hidden action matches backend deny

Evidence:
19-access-control-tests.md

U — Audit / Observability

Audit:

role create/edit/clone

permission changes

ceiling changes

assignment add/remove

scope changes

admin override/revoke

Record actor, target, seller scope, safe before/after summary, trace id.

Evidence:
20-access-control-audit.md

V — Browser Proof

Capture real runtime.

Admin:

Access Control home

Role list

Role editor

searchable Permission Matrix

Seller-specific Access Control

effective preview

Seller:

Access Control

Role editor

permission search

platform-disabled permission state

user assignment

scope selector

Mobile:

Role editor

Permission Matrix

assignments

Original Shopeiva:

side-by-side closest source patterns

Evidence:
21-browser-proof.md

W — Visual Fidelity

Prove:

no foreign admin template

no generic ACL aesthetic

same Shopeiva visual language

source-derived CSS/JS

animation/transition

hover/focus

badges/cards/forms/search/accordion

responsive

Unauthorized deviation = NONE.

Evidence:
22-visual-fidelity.md

X — Validation

Backend:
dotnet restore src/backend/Tooba.slnx
dotnet build src/backend/Tooba.slnx
dotnet test src/backend/Tooba.slnx

Require:
warnings = 0
errors = 0
failed = 0
skipped = 0

Frontend:
cd src/frontend
npm run typecheck
npm run lint
npm run test
npm run test
npm run test
npm run test
npm run build

Also run:

access-control

authorization isolation

admin

seller

i18n

navigation guards

Always:
git diff --check

Evidence:
23-final-validation.md

Y — Runtime

Keep running:

Backend

Tooba Frontend

Original Shopeiva

Verify:

health/live 200

health/ready 200

Admin Access Control

Seller Access Control

Admin Seller-specific Access Control

restricted-user navigation/action projection

Evidence:
24-final-runtime.md

Z — USER-PREVIEW

Return exact URLs:

Admin Access Control

Admin Roles

Admin Users

Admin Seller Access Control

Seller Access Control

Seller Roles

Seller Users

Shopeiva reference routes

Preview scenario:

Platform Admin sets Seller ceiling

Seller creates "Mobile Order Operator"

grants order.view/order.handle

scope Category=Mobile

assigns employee

prove effective access

Mobile order allowed

Book order denied

If full runtime order filtering is not safely integrated, clearly label that step as deferred and prove policy layer only.

AA — Source of Truth

Update:
P06 = IN_PROGRESS
TB-P06-T023-R1 = ACCEPTED
TB-P06-T024 = AWAITING_ARCHITECT_ACCEPT

SPICEDB_AUTHORIZATION = LIVE
ACCESS_CONTROL_CENTER = LIVE | PARTIAL
ADMIN_ROLE_MANAGEMENT = LIVE | PARTIAL
SELLER_ROLE_MANAGEMENT = LIVE | PARTIAL
SELLER_PERMISSION_CEILING = LIVE
USER_ROLE_ASSIGNMENT = LIVE | PARTIAL
UI_CAPABILITY_PROJECTION = LIVE | PARTIAL
RESOURCE_SCOPE_FOUNDATION = LIVE
CATEGORY_SCOPED_ORDER_POLICY = LIVE_FOUNDATION | LIVE
VISUAL_CONTRACT = SHOPEIVA_LOCKED

Update:

docs/PROJECT-STATE.md

docs/ROADMAP.md

docs/ai/TOOBA-RECOVERY-CONTEXT.md

docs/prompts/START-HERE-IF-CHATGPT-IS-LOST.md

Acceptance Criteria

PASS requires ALL:

Permission Catalog

dynamic Role model

user-role assignments

Admin Access Control

Seller Access Control

Admin can manage Seller Access Control

Seller delegation ceiling

no privilege escalation

SpiceDB enforcement

shared Admin/Seller component system

searchable grouped Permission Matrix

expand/collapse

select/clear

clone role

effective access preview

ceiling-disabled visual state

navigation/action projection

direct API enforcement

typed resource-scope foundation

Mobile-vs-Books policy proven at least at authorization layer

no cross-module SQL JOIN

no fake users/counts

Shopeiva-native visual language

browser proof

backend 0/0/0/0

frontend green

runtimes healthy

repo synchronized

Worker may report:
ACCESS_CONTROL_CENTER_LIVE
RESOURCE_SCOPED_AUTHORIZATION_FOUNDATION_LIVE

Worker must NOT report:
ALL_RESOURCE_SCOPES_FULLY_INTEGRATED
PRODUCT_FULLY_READY
USER_VISUAL_ACCEPTED
PRODUCTION_GO_LIVE_READY

Git

git add ...
git commit -m "feat add professional access control center [TB-P06-T024]"
git push origin main
git fetch origin
git rev-parse HEAD
git rev-parse origin/main
git status --short --branch

Require:
HEAD == origin/main
tracked tree clean

Result Contract

PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_WORKER_RESULT

Task-ID:
TB-P06-T024

Channel:
tooba-main

Status:
PASS | FAIL | BLOCKED | RECOVERY_CONFLICT

Summary:
...

Authorization-Audit:

SpiceDB:

Admin:

Seller:

existing roles:

hardcoded gaps:

Permission-Catalog:

count:

modules:

metadata:

delegable:

scope kinds:

Roles:

model:

dynamic:

system roles:

custom roles:

clone:

delete/archive:

Assignments:

Admin users:

Seller users:

multiple roles:

effective access:

Delegation-Ceiling:

Platform ceiling:

Seller delegation:

escalation attempt:

revoke behavior:

stale role behavior:

SpiceDB:

schema changes:

enforcement:

sync:

source of truth:

Resource-Scope:

model:

Category:

Product:

Brand:

Warehouse:

Store:

OrderSegment:

arbitrary expressions:

Mobile-vs-Books:

employee:

permission:

Mobile:

Books:

runtime integration:

deferred:

Admin-UI:

Access Control route:

Roles:

Users:

Seller-specific view:

ceiling editor:

effective preview:

Seller-UI:

Access Control route:

Roles:

Users:

permission search:

disabled-by-ceiling:

scope editor:

effective preview:

Shared-UI:

component paths:

Admin imports:

Seller imports:

duplicate implementation:

duplicate CSS/JS:

Professional-UX:

search:

filters:

grouping:

expand/collapse:

select all:

clear:

clone:

unsaved warning:

effective access:

Navigation:

permission projection:

hidden modules:

action projection:

direct API deny:

Visual-Fidelity:

Shopeiva source files:

CSS:

JS:

animation:

transition:

hover/focus:

responsive:

foreign UI:

unauthorized deviation:

Authorization-Tests:

admin allow/deny:

seller allow/deny:

foreign seller:

ceiling:

privilege escalation:

tenant:

mobile/books:

API bypass:

Validation:

backend restore:

backend build:

backend tests:

warnings:

errors:

failed:

skipped:

typecheck:

lint:

critical-storefront:

storefront:

customer:

seller:

access-control:

i18n:

frontend build:

git diff --check:

Runtime:

Backend:

Frontend:

Shopeiva:

health/live:

health/ready:

USER-PREVIEW:

Admin Access Control:

Admin Roles:

Admin Users:

Admin Seller Access:

Seller Access Control:

Seller Roles:

Seller Users:

Shopeiva references:

preview steps:

Source-of-Truth:

P06:

T023-R1:

T024:

SpiceDB:

Access Control Center:

Admin roles:

Seller roles:

ceiling:

assignments:

resource scope:

visual contract:

Git:

commit:

push:

final HEAD:

origin/main:

synchronized:

tracked tree:

Architectural-Concerns:
...

Visual-Concerns:
...

Blockers:
...

END_TOOBA_WORKER_RESULT

After Result:

return to IDLE

do not self-issue another Task

keep Backend + Tooba Frontend + Original Shopeiva running where technically possible

END_TASK
