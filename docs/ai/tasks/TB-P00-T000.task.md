# Tooba — TB-P00-T000 — Bootstrap, Pipeline & Durable Requirements Baseline

BEGIN_TOOBA_CURSOR_TASK_V1

Protocol-Version: 1
Task-ID: TB-P00-T000
Phase: P00 — Architecture / Discovery
Type: Bootstrap / Source-of-Truth Initialization
Repository: https://github.com/mrnikiemami-code/Tooba
Primary-Branch: main
Implementation-Agent: Cursor
Architect: ChatGPT
Execution-Mode: PIPELINE
Supersedes: previous unreadable transport copies of TB-P00-T000 only

## Objective

Bootstrap Tooba's durable repository governance before product implementation.

Deliver:
- safe repository recovery;
- canonical pipeline/recovery documents;
- durable recording of all currently confirmed Tooba requirements;
- initial P00 roadmap/state;
- commit + push to origin/main;
- complete RESULT for Architect review.

Do not implement product features.

## Repository Safety

Before editing, run:

```bash
git rev-parse --show-toplevel
git fetch origin
git branch --show-current
git rev-parse HEAD
git rev-parse origin/main
git status --short --branch
```

Normal execution requires:

```text
branch = main
HEAD == origin/main
```

Unknown pre-existing working-tree changes, unsafe divergence, wrong branch, or unsafe history state => stop and return:

```text
RECOVERY_CONFLICT
```

Never force-push, rewrite history, destructively reset unknown work, delete unknown changes, or silently stash unknown work.

## Required Canonical Files

Create/update:

```text
AGENTS.md
docs/PROJECT-STATE.md
docs/ROADMAP.md
docs/ai/TOOBA-PIPELINE-PROTOCOL.md
docs/ai/TOOBA-PIPELINE-CONTROLLER.md
docs/ai/TOOBA-RECOVERY-CONTEXT.md
docs/prompts/START-HERE-IF-CHATGPT-IS-LOST.md
```

## Governance Contract

Record:

```text
USER       = product/business authority
ChatGPT    = Chief/Senior Architect / Task Issuer / Reviewer / Pipeline Controller
Cursor     = implementation agent
Repository = durable Source of Truth
```

Cursor is not the architect.

Rules:
- Cursor PASS != Architect ACCEPT.
- No Envelope = No Execution.
- Cursor must not invent requirements, redesign locked architecture, broaden scope, or self-authorize the next task.
- Architectural concerns are reported, not silently implemented.
- Repository truth overrides chat memory.

Task markers:

```text
BEGIN_TOOBA_CURSOR_TASK_V1
...
END_TOOBA_CURSOR_TASK_V1
```

Gate markers:

```text
BEGIN_TOOBA_CURSOR_GATE_V1
...
END_TOOBA_CURSOR_GATE_V1
```

Result markers:

```text
BEGIN_TOOBA_CURSOR_RESULT_V1
...
END_TOOBA_CURSOR_RESULT_V1
```

Task filename convention:

```text
TB-PXX-TXXX.task.md
```

Gate filename convention:

```text
TB-PXX-GATE.gate.md
```

## Pipeline Protocol

Document the lifecycle:

```text
Architect issues one complete authorized Markdown task
→ Cursor validates repository + envelope
→ Cursor executes only that task
→ tests / validation / visual review if required
→ SoT sync
→ local commit
→ push origin main
→ git fetch origin
→ verify HEAD == origin/main
→ Cursor sends RESULT
→ Architect reviews
→ ACCEPT / REPAIR / BLOCKED
→ if ACCEPT and no real blocker, Architect automatically issues the next task/gate
```

After RESULT, Cursor enters:

```text
WAITING
```

Meaning:
- do not invent work;
- keep pipeline alive;
- await/check for the next Architect-authorized envelope.

## Pipeline Controller States

Document at least:

```text
RECOVERING
READY
TASK_ISSUED
EXECUTING
RESULT_SUBMITTED
WAITING_ARCHITECT_REVIEW
WAITING
BLOCKED
RECOVERY_CONFLICT
```

Only Architect authorization may advance to a new implementation objective.

## Confirmed Tooba Requirements

Persist these as confirmed requirements. Clearly distinguish confirmed requirements from unresolved P00 design details.

### Product / Quality
- Commercial multilingual e-commerce product.
- Must reach a sellable state quickly without sacrificing production quality.
- SEO is top-tier and non-negotiable.
- UI/UX is commercially critical, production-grade, mobile-intentional, accessible, multilingual, and must not degrade into developer-skeleton screens.
- Digikala/Amazon are references, not architecture truth and must not be copied.
- Purchased template is an adaptation/reference input, not architecture truth.

### Editions / Deployment
Marketplace edition:
```text
one dedicated marketplace publish/deployment
one marketplace database in the currently stated model
multi-seller marketplace behavior
```

Single-store commercial edition:
```text
one shared publish/deployment for all single-store customers
many domains
incoming domain -> store/tenant resolution -> correct database
one database per customer/store in the currently stated requirement
theme per store
not multi-vendor
```

Do not finalize tenant implementation in this task.

### Architecture
Confirmed:
```text
Modular Monolith
```

Mandatory microservice-readiness:
- module/domain data ownership;
- no direct cross-module DB joins;
- no cross-module table/repository access;
- collaboration through explicit contracts/interfaces/gateways/events;
- future in-process gateway replacement by remote integration without rewriting consuming business logic.

Exact bounded contexts remain pending P00.

### SEO
P00 must later design:
- indexation policy;
- canonical;
- hreflang/locale;
- structured data;
- product/category/brand/tag/content/landing/search/facet rules;
- thin/duplicate URL control;
- sitemap/robots ownership;
- Core Web Vitals implications.

### Content
Use broad:
```text
Content
```
not Blog-only.

Plan for:
- articles;
- buying guides;
- editorial content;
- FAQ;
- category/brand content;
- campaign/landing content;
- taxonomy/tags;
- multilingual content;
- SEO metadata;
- authoring/publishing lifecycle;
- scheduling/versioning where appropriate;
- approved AI/RAG knowledge;
- cross-module references through contracts only.

### Page Composition
Landing pages must be reusable/composable, not one-off hardcoded pages.

Keep conceptual separation where appropriate:
```text
Semantic Content
!=
Page Composition
```

### Identity / Authentication
Extensible login identifiers:
```text
username
phone
email
national ID
future identifiers
```

Authentication:
```text
password
OTP login
optional 2FA/MFA
future external identity providers
```

Must remain extensible to:
```text
Keycloak
```
without coupling core identity to Keycloak.

### Authorization
Confirmed direction:
```text
SpiceDB
```

P00 must design relationship-based authorization. Do not collapse authorization into fixed role columns.

### Future B2B Foundation
Full B2B is after the first sellable release, but P00 must preserve foundations for:
```text
Party / Organization
User Membership
Organization Relationships
Sales Channel
Buyer / Actor separation
Customer / Organization pricing context
Contracts / Agreements
Quantity / Contract Pricing
Credit / Payment Terms
Delegated Permissions
```

Do not implement full B2B now.

### Pricing / Market / Currency
Never model product price as one scalar.

P00 must support contextual pricing around:
```text
Product / Offer
Market
Sales Channel
Currency
Customer / Organization
Quantity
Contract
Promotion
Tax / Fee Context
Validity Period
```

Confirmed:
```text
Locale != Market != Currency
```

Different markets may have different authored commercial prices.
A product/offer may have multiple currencies.

Distinguish where applicable:
```text
Authored Price
FX-Derived / Display-Converted Price
Promotional Price
B2B / Contract Price
```

Future design must consider currency precision, rounding, FX provenance/source, FX timestamp, price history, and market availability.

### Catalog Product vs Seller Offer
P00 must explicitly analyze separation of:
```text
Catalog Product
```
from:
```text
Seller Offer / Listing
```

Do not prematurely merge them.

### Search
Initial:
```text
PostgreSQL Full Text Search
```

Future:
```text
Elasticsearch / OpenSearch
```

Domain logic must not couple to PostgreSQL search internals. Use search abstractions and index/feed documents.

### Caching
Initial hosting may be public/shared; later dedicated.
Caching must be abstracted so Redis can be added without redesign.

### Observability / Logging / Audit
Mandatory foundations:
```text
OpenTelemetry
Advanced Technical Logging
Metrics
Traces
Audit Logging
```

Technical logs and audit/business events must remain conceptually separate.

### First-Party Analytics
Besides third-party analytics, plan first-party tracking for:
```text
PageView
Visitor / Session
ProductView
Search
AddToCart
Checkout
Purchase / Conversion
```

Tracking should not unnecessarily block customer requests.

### Media
Plan for:
- high-quality original asset;
- requested transformed/resized variants;
- cached generated variants;
- no repeated expensive transformations;
- swappable storage/CDN/provider;
- responsive images / modern formats;
- crop/focal-point and SEO/accessibility metadata capability.

### AI
Customer-facing AI agent required.

Plan for:
```text
grounded answers
approved site/product/content knowledge
RAG / retrieval
authorization-aware retrieval
Content as approved knowledge source
catalog/product knowledge via contracts/projections
```

No unrestricted direct AI access to internal DBs.

### Multilingual
Product is multilingual.
Initial locale list is unresolved.
Do not bind Locale to Market or Currency.

## Initial P00 Roadmap

Create small focused PLANNED work packages for at least:

1. repository/template technical inventory;
2. product capability/domain map;
3. edition/deployment/tenant model;
4. bounded-context/data-ownership rules;
5. identity/authentication;
6. SpiceDB authorization;
7. Party/Organization/B2B foundation;
8. Catalog/Product/Variant/Offer/Seller;
9. Pricing/Market/Currency;
10. Inventory;
11. Cart/Checkout/Order;
12. Payment;
13. Content + Page Composition;
14. SEO architecture;
15. Search/indexing;
16. Media pipeline;
17. First-party analytics;
18. AI/RAG;
19. Observability/Audit;
20. Caching/infrastructure abstractions;
21. Frontend/template adaptation strategy;
22. P00 gate.

Use statuses:
```text
PLANNED
IN_PROGRESS
BLOCKED
COMPLETE
```

Do not falsely mark future work complete.

## PROJECT-STATE Requirements

`docs/PROJECT-STATE.md` must include:
- project;
- canonical repo;
- branch;
- current phase;
- pipeline mode;
- last Architect accepted task;
- current issued task;
- issued-but-not-accepted task;
- known blockers;
- architecture status;
- confirmed requirements;
- unresolved P00 decisions;
- repository state;
- exact resume rule.

For this task:
```text
TB-P00-T000 = ISSUED / AWAITING_ARCHITECT_ACCEPT
```

Cursor must not mark Architect ACCEPTED.

## Recovery Requirements

`docs/ai/TOOBA-RECOVERY-CONTEXT.md` and `docs/prompts/START-HERE-IF-CHATGPT-IS-LOST.md` must instruct a recovered Architect to run:

```bash
git rev-parse --show-toplevel
git fetch origin
git branch --show-current
git rev-parse HEAD
git rev-parse origin/main
git status --short --branch
```

Then read:
```text
AGENTS.md
docs/PROJECT-STATE.md
docs/ROADMAP.md
docs/ai/TOOBA-PIPELINE-PROTOCOL.md
docs/ai/TOOBA-PIPELINE-CONTROLLER.md
docs/ai/TOOBA-RECOVERY-CONTEXT.md
```

Then determine:
- current phase;
- last accepted task;
- issued-but-unaccepted task;
- blockers;
- locked requirements;
- unresolved decisions;
- exact resume rule.

Never invent the next task from memory.

## Purchased Template

Architect has received:
```text
shopeiva.zip
```

Do not assume it exists in the repository.
Do not copy/unzip/vendor/modify it in this task unless already clearly present.
If already present, record only high-level presence.

Template inventory is a later P00 task.

## Explicit Out of Scope

Do NOT:
- implement backend modules;
- implement frontend features;
- choose final bounded contexts;
- choose DB schemas;
- implement tenant routing;
- install SpiceDB;
- implement auth;
- integrate Keycloak;
- implement search or Elasticsearch/OpenSearch;
- implement Redis;
- implement catalog/offers/pricing/B2B;
- implement Content CMS or landing builder;
- implement analytics;
- implement AI;
- implement media transformation;
- redesign/integrate the purchased template;
- create speculative ADRs that falsely lock unresolved architecture.

## Validation

Before commit:

```bash
git status --short --branch
git diff --check
```

Manually inspect canonical files for:
- correct Tooba naming;
- no accidental TravelCore product/domain references;
- no broken task IDs;
- consistent phase/status;
- correct markers;
- correct recovery instructions.

Existing safe Markdown/lint tooling may be run.
Do not add tooling solely for this task.

## Git Commit / Push

After validation:

```bash
git diff --check
git status --short --branch
git add AGENTS.md docs
git commit -m "docs bootstrap Tooba architecture pipeline [TB-P00-T000]"
git push origin main
git fetch origin
git rev-parse HEAD
git rev-parse origin/main
git status --short --branch
```

Required:
```text
HEAD == origin/main
working tree clean
```

Never force-push.

## SoT Sync

Before RESULT, Source of Truth must reflect:
```text
Current Phase: P00 — Architecture / Discovery
Current Task: TB-P00-T000
Task State: AWAITING_ARCHITECT_ACCEPT
No next task authorized
```

Avoid recursive commit-SHA self-reference.

## Evidence Required in RESULT

Include exact:
- repo root;
- branch;
- starting HEAD;
- starting origin/main;
- starting status;
- files created/updated;
- `git diff --check`;
- other checks;
- manual consistency review;
- commit SHA;
- push result;
- final HEAD;
- final origin/main;
- final status;
- HEAD/origin equality;
- scope confirmation.

## Result Contract

Return exactly:

```text
BEGIN_TOOBA_CURSOR_RESULT_V1

Protocol-Version: 1
Task-ID: TB-P00-T000
Phase: P00 — Architecture / Discovery
Status: PASS | BLOCKED | RECOVERY_CONFLICT

Summary:
...

Repository-Recovery:
- Repo-Root:
- Branch:
- Starting-HEAD:
- Starting-Origin-Main:
- Starting-Status:

Changes:
- ...

Validation:
- git diff --check:
- other checks:
- manual consistency review:

Git:
- Commit:
- Push:
- Final-HEAD:
- Final-Origin-Main:
- Final-Status:
- Head-Matches-Origin: YES | NO

Source-of-Truth:
- Current-Phase:
- Task-State:
- Recovery-Ready: YES | NO

Scope-Confirmation:
- Product implementation performed: NO
- Unresolved architecture falsely locked: NO
- TravelCore product architecture copied: NO

Architectural-Concerns:
- ...

Blockers:
- ...

END_TOOBA_CURSOR_RESULT_V1
```

## Pipeline Continuity

After RESULT:
```text
WAITING
```

Do not invent or execute `TB-P00-T001`.

Architect reviews this RESULT. If ACCEPTED and no real blocker exists, Architect automatically issues the next task.

END_TOOBA_CURSOR_TASK_V1
