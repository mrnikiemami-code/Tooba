PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_REPAIR_TASK

Task-ID:
TB-P10-T004-R23

Parent-Task:
TB-P10-T004-R22-R1

Channel:
tooba-main

WorkerId:
tooba-worker-01

AgentType:
cursor

Status:
REPAIR

USER_VISUAL_ACCEPTED=YES

Title:
Max Open Unpaid Orders + Reservation Churn Anti-Abuse + Admin Settings

Architect Review

TB-P10-T004-R22-R1 is ACCEPTED.

Accepted:

canonical /fa/login and /en/login exist

customer auth is mobile + OTP on existing Identity/session/BFF stack

Development fixture 09111111111 / 123456 is environment-gated

default CheckoutIdentityPolicy = AuthenticatedOnly

Cart remains anonymous-capable

Shipping / Checkout / Payment are auth-gated

backend enforces authentication

anonymous Cart merge is server-authoritative

merge creates no Reservation / Order / Payment

R21 atomic checkout still works after auth/cart-merge changes

Admin identity setting works

runtime A–O PASS

anti-pattern scan CLEAN

HEAD==origin/main

Worker IDLE

New locks accepted:

LOCK-SF-106…111

This task implements the next planned protection layer:

Max open unpaid Orders per authenticated customer

Reservation/checkout churn anti-abuse window

Admin Settings for both

correct interaction with Cancel / Hide / Reservation Cycles

concurrency-safe enforcement BEFORE new reservation / Order creation

Do NOT implement unrelated fraud scoring.
Do NOT use IP as primary identity.
Do NOT redesign checkout.
Do NOT create TB-P10-T005.

Product Decisions
Open unpaid limit

Scope:
Store + CustomerId

Setting:
MaxOpenUnpaidOrdersPerCustomer

Open unpaid count is by ORDER, never by Payment Attempt.

Hidden pending cards STILL count if the Order is still open/unpaid.

Cancelled / Paid / terminal non-payable Orders do NOT count.

Reservation churn protection

Cancel must not allow:

Reserve -> Cancel -> Reserve -> Cancel -> ...

Settings:

ReservationCommitWindowMinutes

MaxCheckoutCommitsPerCustomerInWindow

Every checkout that successfully reaches a real initial Reservation Cycle #1 consumes one churn quota event.

Cancellation:

frees the open-unpaid slot

DOES NOT refund/remove the churn quota event

Payment failure/retry inside the SAME Order:

does NOT consume another checkout-commit churn event

Retry Reservation Cycle #2+:

does NOT count as a brand-new checkout commit for this policy

remains governed by MaxReservationCycles and retry reservation policy

All enforcement is backend-authoritative.

Recovery State

Phase: P10
Last Architect-accepted: TB-P10-T004-R22-R1
Last Implementation: TB-P10-T004-R22-R1
Current Repair: TB-P10-T004-R23
Worker: tooba-worker-01
Expected branch: main
Expected previous HEAD: c17bbf391a53c255508e5028c01bbe78985d5567
USER_VISUAL_ACCEPTED=YES

Applicable Locks

Read:

docs/architecture/TOOBA-LOCKS.md

docs/architecture/STOREFRONT-CHECKOUT-PLAN.md

Preserve LOCK-SF-001…111.

Especially:

atomic checkout LOCK-SF-103

hide presentation-only LOCK-SF-104

canonical customer cancel LOCK-SF-105

AuthenticatedOnly default / backend auth locks

reservation cycle semantics

same Order across retry

no timer reset

immutable audit

backend-authoritative capabilities/settings

1. Recovery Preflight

Record exactly:

Phase

Last Architect-accepted

Last Implementation

Current Repair

Worker

branch

HEAD

origin/main

HEAD==origin/main

tracked dirty state

task-owned untracked state

Unexpected tracked divergence => BLOCK.

Evidence:
docs/evidence/TB-P10-T004/r23-recovery-start.md

2. Mandatory Discovery

Inspect:

Order statuses / Payment statuses

customer pending-payment projection

customer cancel lifecycle

hidden pending-card persistence

checkout atomic transaction path

Reservation Cycle #1 creation point

current Settings persistence / Admin settings UI

current Store scoping

current concurrency/idempotency primitives

auth CustomerId source

Create:
docs/evidence/TB-P10-T004/r23-discovery.md

Required table:
Order/Payment state | Counts as open unpaid? | Why | Can customer pay? | Can customer cancel? | Hidden-card effect

Do not assume names; map current real states.

3. Define Open Unpaid Canonically

Create/reuse one canonical backend predicate/projection:

IsOpenUnpaidOrder

Equivalent meaning:
An Order that is:

not successfully paid/settled as terminal for payment

not cancelled

still commercially relevant as unpaid/pending/retryable/manual-review

Likely includes current real states equivalent to:

PendingPayment

failed but retryable

expired but retryable/reacquirable

Manual AwaitingEvidence

Manual AwaitingAdmin

Do NOT count:

Succeeded/Paid

Cancelled

terminal non-payable

fully resolved terminal states

Do not duplicate this predicate in multiple services/frontends.

4. Setting — Max Open Unpaid Orders

Add Store-level setting:

MaxOpenUnpaidOrdersPerCustomer

Recommended default:
2

Validation:

integer

minimum 1

reasonable maximum, e.g. 20 unless current config conventions define a better cap

no silent clamp

Admin label:
حداکثر سفارش‌های بازِ پرداخت‌نشده

Help:
هر مشتری همزمان حداکثر این تعداد سفارش قابل پرداخت یا در انتظار بررسی می‌تواند داشته باشد.

5. Enforce BEFORE Reservation / Order Creation

In the atomic checkout path, for AuthenticatedOnly customer:

Before:

inventory reservation

Cycle #1

Order creation

check:

CurrentOpenUnpaidCount < MaxOpenUnpaidOrdersPerCustomer

If limit reached:

do not reserve stock

do not create Order

do not create SellerOrder

do not create Reservation Cycle

do not convert Cart

do not initiate Payment

Stable error equivalent:
checkout.open_unpaid_limit_reached

FA:
شما به حداکثر تعداد سفارش‌های در انتظار پرداخت رسیده‌اید. ابتدا یکی از سفارش‌های قبلی را پرداخت یا لغو کنید.

Return useful metadata:

current count

max count

Frontend may link/scroll to pending-payment section.

6. Hidden Pending Card Rule

LOCKED:

Hide does NOT change open-unpaid counting.

Test:

Order open/unpaid

user hides its pending card

open unpaid count remains unchanged

new checkout remains blocked if limit reached

No query may count only visible cards.

7. Cancel Rule

Canonical customer cancel:

Order leaves open-unpaid count when cancellation is successful

active reservation released

pending card removed

one slot becomes available

BUT:

cancellation does NOT erase churn quota consumption

cancellation does NOT delete audit history

This distinction is mandatory.

8. Paid / Terminal Rule

On successful payment:

Order immediately no longer counts as open unpaid

On terminal cancellation/non-payable resolution:

no longer counts

On Failed but retryable:

still counts

On Manual AwaitingAdmin:

still counts

9. Reservation Churn Audit Model

Introduce/reuse an immutable audit concept for successful initial checkout reservation commits.

Suggested semantic event:
CheckoutReservationCommit

Fields equivalent to:

StoreId

CustomerId

OrderId

ReservationCycleNumber (must be 1)

OccurredAt

source/action

optional OfferIds or line references only if needed later

The event is created only when checkout atomically commits a real Cycle #1.

It must NOT be deleted when:

Order cancelled

payment failed

card hidden

This is anti-abuse audit history.

Do not infer churn count from current open Orders because cancelled Orders must still count in the time window.

10. Churn Settings

Add Store-level:

ReservationCommitWindowMinutes
Recommended default: 30

MaxCheckoutCommitsPerCustomerInWindow
Recommended default: 3

Validation:

window > 0

max > 0

integer semantics

reasonable upper bounds

no silent clamp

Admin labels:

بازه کنترل رزروهای پیاپی
حداکثر شروع رزرو در این بازه

Help:
لغو سفارش سهمیه استفاده‌شده در این بازه را برنمی‌گرداند.

11. Churn Enforcement Point

Before new checkout enters real inventory reservation / Cycle #1:

Count successful CheckoutReservationCommit events for:
StoreId + CustomerId + OccurredAt >= now - window

If count >= max:
BLOCK before inventory reservation.

Stable error equivalent:
checkout.reservation_commit_limit_reached

FA:
تعداد دفعات مجاز شروع رزرو در بازه زمانی اخیر به پایان رسیده است. کمی بعد دوباره تلاش کنید.

Return:

current count

max

retryAfter/nextAvailableAt if cleanly derivable from oldest event in window

Do not expose internal IDs.

12. Churn Semantics

These DO consume churn quota:

each brand-new successful checkout that creates Cycle #1

These DO NOT consume additional churn quota:

payment retry on same Order inside active Cycle #1

retry after Cycle #1 expiry creating Cycle #2+

manual review transition in same Order

Admin confirm/reacquire on same Order

Cancellation:

does not refund quota

Hide:

does not affect quota

Payment success:

does not remove historical quota event until it naturally exits window

13. Atomicity / Concurrency

Critical:

Two concurrent checkout submissions by same Customer when:

open-unpaid remaining slot = 1
or

churn remaining quota = 1

must NOT both pass.

Required:

concurrency-safe backend enforcement

no race where both read same count and both create Orders

integrate with R21 atomic transaction / DB constraint / serialization/advisory lock/current canonical mechanism

Do NOT solve with:

frontend disable only

sleep/retry

eventually correcting duplicate Orders

Document exact strategy.

Evidence:
docs/evidence/TB-P10-T004/r23-concurrency.md

14. Settings Change Semantics

Changing limit settings:

affects future checkout attempts immediately

does not mutate existing Orders

does not mutate Reservation Cycle snapshots

does not delete churn audit events

If Admin lowers max below current count:

existing Orders remain

customer is simply blocked from creating another until count falls

15. Admin Settings UX

Add to existing Store Settings, near checkout/reservation controls.

Section:
کنترل سفارش‌های پرداخت‌نشده و سوءاستفاده از رزرو

Fields:

حداکثر سفارش‌های بازِ پرداخت‌نشده

بازه کنترل رزروهای پیاپی

حداکثر شروع رزرو در این بازه

Show concise explanation distinguishing:

open-order capacity

churn quota

Important copy:
لغو سفارش ظرفیت سفارش باز را آزاد می‌کند، اما سهمیه رزرو استفاده‌شده در بازه زمانی را بازنمی‌گرداند.

No technical names in UI.

16. Customer UX

When open-unpaid limit reached:

Cart remains intact

show safe message

provide action:
مشاهده سفارش‌های در انتظار پرداخت

if pending cards are hidden, the customer still needs a way to manage relevant Orders through Customer Orders/history

When churn limit reached:

Cart remains intact

no reservation occurs

show safe message

if retryAfter available, humanize it

Do not imply stock is reserved.

17. Optional Offer-Level Flash-Sale Protection

DISCOVERY ONLY unless clearly necessary.

Consider future:
MaxReservationCommitsPerCustomerPerOfferInWindow

Do NOT implement by default in R23 unless:

existing campaign/Offer settings architecture makes it minimal AND

there is a clear business requirement not covered by Store-level churn limit

If not implemented, document as a future extension point.

Avoid scope creep.

18. Audit / Admin Observability

Admin Order/customer operational context should be able to distinguish:

Open unpaid limit block

Reservation churn limit block

Do not need a new dashboard.

At minimum:

settings audit

checkout block audit/event

churn commit event is inspectable in backend/evidence

No raw codes in normal UX.

19. Focused Tests

Open unpaid:

0/2 -> checkout allowed

1/2 -> checkout allowed

2/2 -> blocked before reservation

hidden open Order still counts

cancelled Order stops counting

paid Order stops counting

Failed retryable counts

Manual AwaitingAdmin counts

Payment Attempt retries do not change Order count

Churn:
10. first commit event created at successful Cycle #1
11. second/third allowed up to max
12. next blocked
13. cancel does not refund churn quota
14. hide does not affect churn quota
15. payment success does not erase event
16. Cycle #2+ retry does not add churn event
17. event naturally ages out of window

Atomic/concurrency:
18. limit failure -> no Reservation
19. limit failure -> no Order
20. limit failure -> Cart remains Active
21. concurrent last open slot -> one winner
22. concurrent last churn slot -> one winner
23. no duplicate Cycle #1
24. no duplicate quota event

Settings:
25. Admin save/load
26. validation
27. lower limit below current count does not mutate Orders
28. future attempts use new values

Regression:
29. R21 atomic checkout
30. R22 login/auth gate
31. cancel/hide semantics
32. reservation retry semantics
33. recovery guard
34. git diff --check

Do NOT run unrelated full suites.

20. Real Runtime Matrix — REQUIRED

Use Host :5088 + FE :3000, authenticated as development customer.

A. max open=2; create unpaid Order #1
B. create unpaid Order #2
C. third checkout blocked; Cart remains
D. hide one pending card; third still blocked
E. cancel one Order; open slot frees
F. immediately create replacement Order if churn quota allows
G. prove cancel did NOT refund churn quota
H. hit churn max and get churn-block message
I. payment retry same Order does not consume new churn quota
J. expired Cycle #1 -> Cycle #2 retry does not consume new checkout-commit quota
K. payment success frees open-order slot
L. concurrent final-slot proof
M. Admin Settings visual/runtime
N. AuthenticatedOnly still enforced

21. Anti-Pattern Gate

Scan for:

counting pending cards instead of Orders

hide freeing open-order capacity

cancel refunding churn quota

churn inferred from current Orders

Payment Attempts counted as Orders

Cycle #2 counted as new checkout commit

frontend-only limits

IP-based primary identity

race-prone count-then-insert without protection

sleep/retry hacks

cleanup-after-duplicate workaround

raw machine code UX

duplicated eligibility predicates

N+1

Any occurrence => REPAIR.

Evidence:
docs/evidence/TB-P10-T004/r23-antipattern-scan.md

22. Lock Registry

Add non-duplicate locks equivalent to:

Open unpaid Order limit is scoped by Store + CustomerId and counts Orders, not Payment Attempts.

Hidden pending cards remain counted while the underlying Order is open/unpaid.

Successful cancellation frees open-unpaid capacity but does not refund reservation-churn quota.

Every new successful checkout Cycle #1 consumes one immutable churn event; retries on the same Order/Cycle #2+ do not.

Open-unpaid and churn limits are enforced backend-side before reservation/Order creation and are concurrency-safe.

23. Planned NEXT Task — DO NOT IMPLEMENT

After R23 PASS:

TB-P10-T004-R24
Final Identity + Limits + Atomic Checkout Integration Gate

Must cover:

Login gate

development OTP

anonymous Cart merge

atomic checkout

MaxOpenUnpaidOrders

churn quota

Cancel frees open slot

Hide does not free slot

Cancel does not refund churn quota

Reservation Cycle retry behavior

Admin Settings

concurrency

real browser

no polling/N+1

deployment readiness

24. Evidence

Add under docs/evidence/TB-P10-T004/:

r23-recovery-start.md

r23-discovery.md

r23-open-unpaid-policy.md

r23-churn-policy.md

r23-churn-audit.md

r23-concurrency.md

r23-admin-settings.md

r23-customer-ux.md

r23-observability.md

r23-focused-validation.md

r23-runtime.md

r23-antipattern-scan.md

r23-recovery-sot.md

25. Recovery / Pipeline

At completion:

Phase P10

Last Architect-accepted before review: TB-P10-T004-R22-R1

Last Implementation: TB-P10-T004-R23

Current Issued/Repair: none

USER_VISUAL_ACCEPTED=YES

Worker: tooba-worker-01 — IDLE

branch main

ending HEAD recorded

HEAD==origin/main

task-owned worktree clean

no TB-P10-T005

Run recovery guard.

Acceptance

PASS only if ALL:

canonical open-unpaid predicate exists

MaxOpenUnpaidOrdersPerCustomer works

blocked checkout leaves Cart untouched

hidden open Orders still count

cancellation frees open slot

paid/terminal Orders stop counting

churn audit event created exactly once per successful new Cycle #1 checkout

cancel/hide/payment success do not erase churn event

retries on same Order do not consume new churn quota

churn window/max enforced before reservation

concurrency cannot oversubscribe final slot/quota

Admin Settings work

customer messages are business-safe

runtime A–N PASS

anti-pattern scan CLEAN

focused tests green

recovery exact

HEAD==origin/main

Worker IDLE

no TB-P10-T005

Result Contract

Return ONLY canonical BRIDGE-WAKE-V1 Result with:

Summary
Applicable-Locks
Recovery-Start
Discovery
Open-Unpaid-Policy
Churn-Policy
Churn-Audit
Concurrency
Admin-Settings
Customer-UX
Observability
Focused-Validation
Runtime
AntiPattern-Scan
Lock-Registry
Recovery-SoT
Git
Architectural-Concerns
Visual-Concerns
Blockers

END_TOOBA_REPAIR_TASK