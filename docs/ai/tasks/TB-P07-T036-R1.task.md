PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_TASK

Task-ID:
TB-P07-T036-R1
Parent-Task:
TB-P07-T036
Channel:
tooba-main

Title:
Category Semantics Acceptance Repair — Live Migration Proof, Full Validation, Recovery SoT Sync

ARCHITECT REVIEW

TB-P07-T036 is NOT fully accepted yet.

Acceptance gaps:

Result says destructive/live demo migration was skipped and covered only by fixtures. T036 explicitly required LIVE preview/cancel/confirm/restore verification.

Frontend production build and full relevant FE test completion are not explicitly reported.

Recovery/SoT was recently observed stale by many tasks; synchronize it and add a repo-local staleness guard.

Preserve all successful T036 semantics:

Category→Products display-only

Primary change only in Product Workspace wizard

canonical Attribute inheritance/override

viewed-category-owned PLP facets

Brand canonical

no Product/Variant Price/Stock

A — LIVE PRIMARY CATEGORY MIGRATION

Use non-production seeded demo data.

Required sequence:

capture original Product snapshot: Primary, Additional, attributes, variants/default, lifecycle, readiness

open migration wizard

choose meaningful L3 target

run non-mutating preview

verify no state mutation

record preserved/added/removed attributes, required missing, variant impact, readiness blockers

cancel and verify unchanged

repeat and confirm migration

verify Primary changed transactionally

verify compatible DefinitionId values preserved

incompatible values leave active schema

missing required values block readiness

variant behavior matches impact result

history is human-readable

restore exact seeded baseline afterward

If restoration is unsafe, use a deterministic temporary gate-smoke Product and remove it after verification.

B — LIVE DISPLAY MEMBERSHIP

Using Product whose Primary is elsewhere:

add from Category→Products

role must be display-only

Primary unchanged

UI row/count updates immediately

remove membership

Primary unchanged

restore baseline

C — LIVE ATTRIBUTE INHERITANCE / OVERRIDE

Using seeded Categories:

verify one inherited DefinitionId from L1 through L2/L3

create L2 override using same DefinitionId

verify L3 effective behavior follows nearest override

verify no duplicate effective attribute

reset override to parent

verify fallback

restore baseline

D — LIVE PLP FACETS

Choose Category with Primary + display members.
Verify:

visible facets are viewed Category facets + approved globals only

no source Primary-category facet union

matching canonical DefinitionId values contribute buckets

missing values create no bucket

Product remains visible with no selected facet

selected facet excludes Product lacking matching value

Brand facet canonical; brandless behavior correct

restore temporary membership

E — FULL VALIDATION

Backend:

restore

build

FULL tests

MigrationRunner/tests

Frontend:

typecheck

lint

FULL relevant Catalog/Product/Category/PLP tests

production build

Also:

git diff --check

Require:

0 failed

0 skipped

no new warnings

F — RECOVERY / PIPELINE SOT SYNC

Update the repository's EXISTING recovery/state documents, not a competing system.

Record:

Current phase: P07

Last Architect-accepted before this repair: TB-P07-T035

Implemented task under review: TB-P07-T036

Current repair: TB-P07-T036-R1

branch: main

final HEAD/origin

USER_VISUAL_ACCEPTED=NO

next state: Worker IDLE / waits for Bridge Task

no invented next task

G — RECOVERY STALENESS GUARD

Add a lightweight repo-local deterministic validation/test so a PASS cannot leave recovery many tasks behind.

Guard should validate that existing recovery SoT contains current implemented/current repair state and is sufficiently synchronized for a fresh Worker.

Do NOT depend on remote Bridge API during build/test.

H — RUNTIME / GIT

Keep alive:

Backend :5088

Frontend :3000

Shopeiva :3001

Verify current HEAD loaded.

Report:

git rev-parse HEAD

git rev-parse origin/main

git status --short

Require HEAD==origin/main and unrelated dirty changes preserved.

I — EVIDENCE

Update docs/evidence/TB-P07-T036/:

live-primary-migration-r1.md

live-display-membership-r1.md

live-inheritance-override-r1.md

live-plp-facets-r1.md

recovery-sot-r1.md

full-validation-r1.md

ACCEPTANCE

PASS only if:

live preview/cancel/confirm/restore is proven

live display-only add/remove is proven

live inheritance override/reset is proven

live PLP facet semantics are proven

full backend validation green

full FE relevant tests + production build green

git diff --check green

recovery docs synchronized

recovery staleness guard exists and passes

runtime current

HEAD==origin/main

no Product/Variant Price/Stock

USER_VISUAL_ACCEPTED=NO.

RESULT CONTRACT

Return ONLY canonical BRIDGE-WAKE-V1 result for TB-P07-T036-R1 with sections:
Summary
Live-Primary-Migration
Live-Display-Membership
Live-Inheritance
Live-PLP
Recovery-SoT
Validation
Runtime
Evidence
Git
Architectural-Concerns
Visual-Concerns
Blockers

END_TOOBA_TASK