PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_REPAIR_TASK

Task-ID:
TB-P09-T001-R2

Parent-Task:
TB-P09-T001-R1

Channel:
tooba-main

WorkerId:
tooba-worker-01

AgentType:
cursor

Status:
REPAIR

USER_VISUAL_ACCEPTED=NO

Title:
Order Operations Final Repair — Marketplace Settlement Event Path Proof

Why This Repair Is Required

TB-P09-T001-R1 fixed the cancel authority gap and proved the post-settlement return rule in SingleStore using direct application APIs.

One production-critical gap remains:

The Result states:
SingleStore does not register settlement payment/refund handlers; production Marketplace edition relies on integration events.

The post-settlement return/refund behavior must therefore also be verified through the REAL Marketplace edition event-driven path before TB-P09-T001 can be architect-accepted.

This repair is BACKEND/INTEGRATION ONLY.

NO UI changes.

Scope Lock

Do NOT:

redesign any UI

add any new Order action

add a new ledger

add a new workflow engine

bypass MassTransit

add RabbitMQ

mutate settlements directly

create cross-module SQL JOINs

create duplicate refund/settlement handlers

Preserve:

MassTransit

PostgreSQL SQL/Database Transport

existing Outbox

existing Returns/Payments/Settlement boundaries

current Order UI exactly as-is

1. Inspect Marketplace Settlement Wiring

Before changing code, inspect the real Marketplace registration/wiring for:

payment completion integration events

refund completion integration events

seller settlement accrual

refund adjustment/debit

outbox/message delivery

idempotency keys/source ids

edition gating

Document:

event name/type

producer module

consumer/handler

resulting settlement operation

idempotency mechanism

edition registration path

Do not duplicate existing handlers.

2. Marketplace Post-Settlement Return Scenario

Run the actual application in Marketplace edition/configuration using the existing development/test mechanism.

Create/use deterministic test data and exercise the REAL flow:

marketplace Order exists with seller offer/items

Order paid

Fulfillment reaches Delivered

Seller accrual/settlement becomes completed/payable through the normal Marketplace event path

Confirm ReturnEligibility remains eligible while inside return window

Create Return through existing Return application/API path

Approve/refund through existing Payment/Return path

Let the normal integration-event/outbox path create the Seller refund debit/adjustment

Verify:

completed prior settlement/accrual is unchanged

exactly one refund debit/adjustment exists

source/reference links to the refund

future seller payable/net reflects the debit

retry/redelivery does not create duplicate debit

No direct Settlement API call may be used to manufacture the adjustment in this Marketplace proof.

3. Outbox / Event Delivery Proof

Because Marketplace relies on integration events, verify the actual messaging path.

Required:

producer commits business change + outbox according to current architecture

consumer runs via existing MassTransit SQL/Database transport

message can be retried/redelivered safely

duplicate delivery remains idempotent

no RabbitMQ configuration is introduced

no synchronous cross-module shortcut is added

If tests use an in-memory harness for one layer, runtime proof must still use the real configured PostgreSQL transport path where practical.

4. Failure / Retry Safety

Add/confirm focused tests for:

duplicate refund event

duplicate settlement-adjustment handler invocation

consumer retry

adjustment already exists

refund completion event redelivery

Expected:
ONE financial debit/adjustment only.

Do not invent a distributed transaction.
Preserve existing outbox/eventual-consistency design.

5. Edition Separation

Verify explicitly:

SingleStore:

no accidental Marketplace settlement handler registration

no regression

Marketplace:

required settlement/refund handlers are registered

event-driven adjustment path active

Do not merge edition-specific behavior incorrectly.

6. No UI Regression

No UI code should change.

Only quick verification:

Orders grid loads

View remains

contextual operations menu remains

Order detail عملیات سفارش remains

If UI files change without a necessary backend-contract reason, treat that as a task defect.

7. Focused Validation

Required focused backend coverage:

Marketplace handler registration

payment/accrual event path

refund/adjustment event path

duplicate event idempotency

completed settlement immutability

future payable/net reflects adjustment

SingleStore edition separation

existing cancel guard remains green

existing ReturnEligibility remains settlement-independent

Also:

relevant build

recovery guard

git diff --check

No unrelated full repository suite.

8. Runtime Smoke — REQUIRED

Use a Marketplace-mode runtime with current project conventions.

Primary ports may remain:
Backend: http://127.0.0.1:5088
Frontend: http://127.0.0.1:3000

The evidence must include a real Marketplace scenario and exact before/after identifiers/amounts for:

order

seller

completed accrual/settlement

return

refund

seller debit/adjustment

payable/net before

payable/net after

Also prove duplicate/redelivery does not create a second debit.

If the Marketplace runtime cannot be started or the normal event path cannot be executed, return BLOCKED/PARTIAL with exact reason.
Do NOT claim PASS from SingleStore proof alone.

9. Recovery / Pipeline

Update:

Phase: P09

Last Architect-accepted baseline: TB-P08-T016-R5

TB-P09-T001 remains under repair

Current Repair: TB-P09-T001-R2

Last Implementation on completion: TB-P09-T001-R2

USER_VISUAL_ACCEPTED=NO

Worker Next State: IDLE

do NOT invent/start TB-P09-T002

Run recovery guard.

10. Evidence

Create/update:

docs/evidence/TB-P09-T001/

Required:

r2-marketplace-settlement-wiring.md

r2-marketplace-runtime.md

r2-outbox-event-proof.md

r2-idempotency.md

r2-edition-separation.md

r2-recovery-sot.md

11. Git

Required:

preserve user-authored changes

commit only necessary task changes

push origin/main

HEAD==origin/main

no task-owned uncommitted changes

Acceptance

PASS only if ALL:

real Marketplace settlement/refund event wiring inspected

Marketplace uses existing MassTransit PostgreSQL transport/outbox path

no RabbitMQ introduced

no synchronous duplicate finance shortcut added

real Marketplace post-settlement return scenario executed

Return remains eligible independent of settlement when policy allows

refund flows through existing Return/Payment path

seller refund debit is produced by normal Marketplace event path

completed settlement/accrual remains immutable

exactly one debit/adjustment exists

duplicate/redelivery remains idempotent

future payable/net reflects debit

SingleStore edition remains correctly separated

cancel guard regression green

no UI redesign/change

focused validation green

recovery guard passes

HEAD==origin/main

Worker returns IDLE

no TB-P09-T002 invented

USER_VISUAL_ACCEPTED=NO.

Result Contract

Return ONLY canonical BRIDGE-WAKE-V1 result with:

Summary
Marketplace-Wiring
Event-Producers
Event-Consumers
Outbox-Transport
Marketplace-Runtime
Settlement-Immutability
Refund-Adjustment
Idempotency
Edition-Separation
Cancel-Guard-Regression
UI-Regression
Focused-Validation
Runtime-Smoke
Recovery-SoT
Git
Architectural-Concerns
Visual-Concerns
Blockers

END_TOOBA_REPAIR_TASK