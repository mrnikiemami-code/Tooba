PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_TASK

Task-ID:
TB-P10-T022-R3

Parent-Task:
TB-P10-T022-R2

Channel:
tooba-main

WorkerId:
tooba-worker-01

AgentType:
cursor

Status:
ISSUED

Appearance-USER_VISUAL_ACCEPTED=YES
Builder-USER_VISUAL_ACCEPTED=NO

Title:
Template Selection Wireframe — Realistic Preview Workspace, Device Modes, Seed-Pack UX Skeleton

Architect Decision

TB-P10-T022-R2 remains technically accepted, but Builder visual acceptance is still pending.

This task is intentionally WIREFRAME-ONLY for the template-selection experience.

Do NOT implement full seed-data persistence yet.
Do NOT generate real template data yet.
Do NOT redesign unrelated Builder flows.
Do NOT invent TB-P10-T023.

The goal is to make the template-selection experience visually understandable enough for the user to approve the structure before full implementation.

Locked Product Decisions
Template-specific seed pack — future implementation contract

For EACH industry template, the future seed pack will contain:

Categories: exactly 8 seeded top-level category trees, with meaningful three-level hierarchy

Products: exactly 15 seeded products

Product images: industry-relevant

Banners: industry-relevant

Brands: industry-relevant

These must be unique/relevant to each template.

Articles are NOT template-specific for now.
Reviews and Stories are NOT template-specific for now.

Seed records must later be traceable for safe cleanup.
Do not implement cleanup/persistence in this task, but the wireframe must reserve UX for:

“همراه با داده نمونه”

“پاک‌سازی داده‌های نمونه”

“آماده‌سازی فروشگاه برای ورود اطلاعات”

Future seed tracking must support safe deletion without deleting user-created/claimed data.

Recovery State

Phase: P10
Last Architect-accepted: TB-P10-T022-R2
Last Implementation: TB-P10-T022-R2
Current Repair: TB-P10-T022-R3
Expected previous HEAD: 587c7e85fc0b488196817f9d63d6b0ac6630c754
Worker: tooba-worker-01

Preserve LOCK-SF-001…273.

1. Recovery Preflight

Record:

branch

HEAD

origin/main

git status --short

Host :5088

FE :3000

current /admin/landing-pages/new

current template picker implementation

unrelated local changes

Unexpected tracked divergence => RECOVERY_CONFLICT/BLOCK.

2. Wireframe-Only Scope

Implement only the UI structure and interaction shell for the template-selection workspace.

This task must stop before:

real template seed creation

real seed cleanup

real media seeding

real category/product/brand persistence

final visual polish

Use lightweight deterministic mock/wireframe preview content.

3. New Template Selection Workspace

Replace the current card-only template picker with a richer workspace.

Required structure:

A. Template Browser Area

Show industry templates as selectable cards with:

template name

short industry description

compact realistic miniature/wireframe

section count

key blocks summary

clear selected state

B. Selected Template Detail Panel

For selected template show:

large preview area

template name

short description

included-section summary

seed-data summary placeholder

primary CTA: «استفاده از این قالب»

secondary CTA: «شروع از صفحه خالی»

C. Preview Device Toolbar

Above preview include:

Desktop

Tablet

Mobile

Use icon buttons with tooltips.

Switching device mode must update preview frame dimensions/layout.

Do not merely scale the same canvas; each device mode should use representative responsive proportions.

4. Device Preview Modes

Required modes:

Desktop

wide preview frame

full composition overview

Tablet

medium-width framed preview

responsive composition approximation

Mobile

phone-width preview

vertical composition approximation

Toolbar state must be obvious.

No user-editable breakpoints.

5. Template Preview Wireframes Must Be Industry-Distinct

Wireframes must visually communicate different compositions.

At minimum:

Fashion: hero + story circles + category/product rail + campaign banner

AutoParts: dense category/brand/product/search-oriented composition

BuildingSupplies: practical category/brand/product blocks

ToolsHardware: compact dense product/category layout

TileCeramic: large visual collection sections

InteriorDecor: large imagery + calmer editorial composition

HomeAppliance: brand/product/promo emphasis

Shoes: campaign hero + story/category + products

Plants: calm visual/category/product rhythm

Beauty: story/brand/campaign/product emphasis

Do NOT reuse the exact same miniature skeleton for all templates.

6. Seed-Pack UX Skeleton

For selected template show a non-functional wireframe block:

«داده نمونه این قالب»

Display future seed counts:

۸ درخت دسته‌بندی سه‌سطحی

۱۵ محصول نمونه

تصاویر محصول مرتبط

بنرهای مرتبط

برندهای مرتبط

Also show shared demo-content note:

استوری‌های نمونه عمومی

نظرات نمونه عمومی

مقالات عمومی نمایشی

Important:
Articles are not template-specific.
Stories/reviews are shared/general demo content.

No real data creation in this task.

7. Seed Tracking UX Note

Add explanatory wireframe text that sample data will be traceable and removable later.

Example meaning:
«داده‌های نمونه با برچسب سیستمی ثبت می‌شوند تا بعداً بدون حذف اطلاعات واقعی فروشگاه پاک‌سازی شوند.»

Do not expose technical IDs or internal metadata keys.

8. Cleanup / Prepare UX Skeleton

Selected-template detail must reserve actions for future implementation:

«پاک‌سازی داده‌های نمونه»

«آماده‌سازی فروشگاه برای ورود اطلاعات»

These buttons may be disabled/non-functional in this wireframe task, but must look like intentional product actions and clearly indicate “در مرحله بعد فعال می‌شود” or equivalent non-misleading status.

No destructive behavior in this task.

9. Start Blank Option

Keep a clear “شروع از صفحه خالی” option.

It must remain visually secondary but obvious.

The user should be able to understand:

use template with sample/demo structure

or start blank

10. Language Placement

Language remains page-level.

For the wireframe flow:

language selection belongs before final page creation/confirmation

do not place language inside section/template preview controls

no per-section language

11. Admin Theme Isolation

Template-selection wireframe must use fixed Admin design tokens.

Storefront palette must not recolor:

preview toolbar

template cards

action buttons

selected state

admin shell

Only the storefront preview content area may intentionally simulate storefront theme later.

12. No Final Visual Styling Yet

This is wireframe approval stage.

Use:

neutral admin styling

clear hierarchy

realistic layout geometry

placeholder/mock images if needed

no final industry imagery pack

no final art direction

Goal:
user approves layout/interaction before production design.

13. Runtime Proof

Host :5088 + FE :3000.

Required flow:
A. open /admin/landing-pages/new
B. select Fashion
C. inspect Desktop preview
D. switch Tablet
E. switch Mobile
F. select AutoParts and verify visibly different composition
G. select InteriorDecor and verify visibly different composition
H. inspect seed-pack summary
I. inspect future cleanup actions
J. inspect Start Blank option

Required screenshots:

template-wireframe-desktop.png

template-wireframe-tablet.png

template-wireframe-mobile.png

template-wireframe-fashion.png

template-wireframe-autoparts.png

template-wireframe-interior.png

template-wireframe-seed-summary.png

template-wireframe-cleanup-actions.png

template-wireframe-start-blank.png

Store:
docs/evidence/TB-P10-T022-R3/screenshots/

14. Minimal Useful Tests

Required only:

template picker exposes Desktop/Tablet/Mobile modes

selected device mode changes preview frame contract

template previews are structurally distinct

seed summary shows 8 category trees + 15 products

articles/stories/reviews are not marked template-specific

cleanup actions are non-destructive in this wireframe task

no per-section language selector

Admin theme isolation

recovery-staleness

git diff --check

No screenshot pixel tests.
No seed persistence tests yet.

15. Anti-Pattern Gate

Reject:

generic identical template previews

final seed implementation in this task

real destructive cleanup

device mode implemented as naive CSS zoom only

per-section language

Storefront palette leak into Admin shell

hidden template selection state

giant feature expansion outside template-selection wireframe

Evidence:
docs/evidence/TB-P10-T022-R3/antipattern-scan.md

16. Locks

Add non-duplicate locks after LOCK-SF-273:

LOCK-SF-274 Template selection requires Desktop/Tablet/Mobile preview modes.
LOCK-SF-275 Template-specific future seed pack contains 8 three-level category trees, 15 products, related product images, related banners, and related brands.
LOCK-SF-276 Articles are not template-specific in current seed plan; Stories/Reviews use shared demo data.
LOCK-SF-277 Seed data must be traceable for safe cleanup without deleting user-owned data.
LOCK-SF-278 Template previews must be structurally distinct and representative before full visual design is implemented.
LOCK-SF-279 Seed cleanup/preparation actions remain non-destructive until dedicated seed-data implementation task.

Do not renumber existing locks.

17. Recovery / Pipeline

At completion:

Phase P10

Last Architect-accepted before review: TB-P10-T022-R2

Last Implementation: TB-P10-T022-R3

Current Issued/Repair: none

Appearance USER_VISUAL_ACCEPTED=YES

Builder USER_VISUAL_ACCEPTED=NO

Worker IDLE

branch main

HEAD==origin/main

task-owned clean

unrelated user changes preserved

do NOT invent/start TB-P10-T023

Acceptance

PASS only if ALL:

template-selection workspace is materially clearer

Desktop/Tablet/Mobile preview modes exist

previews differ structurally by industry

selected template detail panel is understandable

seed-pack summary reflects locked future counts

cleanup/preparation actions are represented but non-destructive

Start Blank remains clear

no final seed persistence implemented

no final design overreach

only essential tests added

anti-pattern CLEAN

locks/recovery/git exact

Worker IDLE

no TB-P10-T023

Result Contract

Return ONLY canonical BRIDGE-WAKE-V1 Result with:

Summary
Applicable-Locks
Recovery-Start
Template-Workspace
Device-Preview-Modes
Industry-Preview-Distinctness
Seed-Pack-Wireframe
Seed-Tracking-UX
Cleanup-Prepare-UX
Start-Blank
Language-Placement
Admin-Theme-Isolation
Focused-Validation
Runtime
Visual-Evidence
AntiPattern-Scan
Lock-Registry
Recovery-SoT
Git
Architectural-Concerns
Visual-Concerns
Blockers

END_TOOBA_TASK