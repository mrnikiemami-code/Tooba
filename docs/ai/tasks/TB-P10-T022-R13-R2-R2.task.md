PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_TASK

Task-ID:
TB-P10-T022-R13-R2-R2

Parent-Task:
TB-P10-T022-R13-R2-R1

Channel:
tooba-main

WorkerId:
tooba-worker-01

AgentType:
cursor

Status:
ISSUED

Appearance-USER_VISUAL_ACCEPTED=YES
Builder-USER_VISUAL_ACCEPTED=NO

Title:
Product Showcase Final Closure — Real Published Route Proof + Canonical Git Result Integrity

Architect Review

TB-P10-T022-R13-R2-R1 is NOT fully accepted yet.

The repair proved the five new Product Showcase variants inside the real Admin wizard and preserved existing variants/user work, which is good.

However two closure gaps remain:

Git section returned:
PLACEHOLDER_SHA
This is not an acceptable canonical Result.

Worker stated:
Storefront home may not always host the edited Landing page
and therefore storefront captures were based on route probes after save rather than an unambiguous published page route tied to the exact edited Page.

For final acceptance, we need one clean end-to-end proof:
Admin edit → save/publish exact Page → open exact public route for that Page → verify selected Product Showcase variant.

Do NOT redesign anything.
Do NOT add/remove variants.
Do NOT modify ProductCard.
Do NOT change Embla architecture unless a real route-binding bug is found.
Do NOT touch P11.
Do NOT touch Canonical Home reset.
Do NOT touch unrelated user work.
Do NOT invent TB-P10-T023.

Canonical Start State

Last Architect-accepted:
TB-P10-T022-R13-R1-R1

Last Implementation:
TB-P10-T022-R13-R2-R1

Known feature implementation commit ancestry must include:

18ca10c9 user-work preservation commit

R13-R2 implementation commit(s)

Locks:
LOCK-SF-001…380

Builder:
USER_VISUAL_ACCEPTED=NO

Worker Protocol

No heartbeat.
No polling.
No IDLE loop.

After canonical Result is delivered through Bridge:
STOP completely.

1. Recovery Preflight

Record:

branch

HEAD

origin/main

git status

git log --oneline --decorate -n 12

verify 18ca10c9 ancestor of HEAD

verify R13-R2 implementation present

verify R13-R2-R1 evidence present

unrelated stashes/.tmp unchanged

If HEAD/origin differ, resolve only if clearly due to this task’s legitimate commit flow.
Do not reset user work.

Evidence:
docs/evidence/TB-P10-T022-R13-R2-R2/recovery-start.md

2. Determine Exact Public Route of Edited Page

Use the actual Page being edited in Admin.

Determine whether the Page is:

Home → public /
or

Landing → public /landing/{slug}

Do NOT substitute another route.

Record:

page id

page type

page title

slug if Landing

current publication status

exact public route

Do not expose raw IDs in ordinary UI screenshots; IDs may exist in technical evidence.

Evidence:
docs/evidence/TB-P10-T022-R13-R2-R2/page-route-binding.md

3. Admin → Exact Public Route End-to-End Proof

Perform this exact flow for TWO new variants:

Flow A — Sunny

open actual Admin Store Page editor

edit existing Product Showcase section

select سانی

save section

save/publish Page if current workflow requires it

open THAT exact Page public route

verify Sunny rail is rendered there

refresh public route

verify persisted

Flow B — Cinematic

Repeat with:
سینمایی

Do not use:

evidence page

generic home if edited Page is Landing

unrelated Storefront route

direct component sandbox

Required screenshots:

admin-sunny-selected-exact-page.png

public-sunny-exact-route.png

admin-cinematic-selected-exact-page.png

public-cinematic-exact-route.png

4. Route Binding Bug Handling

If the exact public Page does NOT reflect the saved section:

Investigate root cause:

wrong Page resolver

draft/published mismatch

cache invalidation

wrong Store context

wrong locale

composition not published

stale route cache

section source mismatch

If it is a real product bug:

fix root cause only

no timeout/polling workaround

no force-refresh hack

preserve current architecture

If it is simply publication workflow semantics:

follow the legitimate publish flow

document it clearly

do not change product behavior unnecessarily

5. Variant Identity Proof

On exact public route, prove selected variant identity by DOM/runtime marker or stable renderer metadata.

At minimum:

sunny route render is actually product.sunny or canonical equivalent

cinematic route render is actually product.cinematic or canonical equivalent

Do not infer only from visual similarity.

6. ProductCard Regression on Exact Public Route

On one of the exact public route proofs verify:

product title

price

PDP link

wishlist/action controls if visible

add-to-cart/quick action behavior according to existing ProductCard contract

No business regression.

7. SSR Proof on Exact Public Route

Fetch/render exact public route before hydration and verify product content is in initial HTML:

section heading

product name

price

PDP link

Document:
docs/evidence/TB-P10-T022-R13-R2-R2/ssr-exact-route.md

8. Cache / Persistence Proof

After Admin save/publish:

exact route reflects new variant without manual data surgery

normal refresh retains it

no stale previous variant after proper publish/invalidation

If cache revalidation is involved, use current canonical invalidation path only.

No sleeps/polling.

9. Canonical Git Result Integrity — CRITICAL

At completion, Result MUST contain a real SHA.

Required:

final HEAD SHA

HEAD == origin/main

clean tracked working tree, except explicitly documented unrelated user changes if any

no PLACEHOLDER_SHA

no fake/example values

Capture:
docs/evidence/TB-P10-T022-R13-R2-R2/git-final.md

The Git section in Result must include:

branch

HEAD full SHA

origin/main full SHA

equality status

confirmation 18ca10c9 is ancestor

10. User Work Preservation

Reconfirm:

18ca10c9 remains ancestor

user bug-fix changes remain present

no user files reverted/overwritten

unrelated stashes/.tmp untouched

Evidence:
docs/evidence/TB-P10-T022-R13-R2-R2/user-work-preservation.md

11. Focused Validation

Required:

actual edited Page route identified

Sunny Admin→exact-public-route PASS

Cinematic Admin→exact-public-route PASS

persisted after refresh

actual variant identity proven

ProductCard behavior intact

SSR product content present

no evidence/demo page used

no wrong-route substitution

cache/publish semantics documented

18ca10c9 ancestor PASS

user work preserved

critical-storefront PASS

recovery-staleness PASS

git diff --check PASS

final Git SHA real and HEAD==origin/main

12. Anti-Pattern Gate

Reject:

PLACEHOLDER_SHA

example/fake Git values

using home route for a Landing Page

using evidence/demo page

forcing route with manual state injection

disabling cache to make screenshot pass

arbitrary sleep/retry/polling

redesign

ProductCard changes

unrelated cleanup

user-work overwrite

Evidence:
docs/evidence/TB-P10-T022-R13-R2-R2/antipattern-scan.md

13. Locks

No new locks expected.

LOCK-SF-375…380 are sufficient.

Only add a new lock if a genuinely new cross-cutting invariant is discovered.

14. Completion State

At completion:

Phase P10 / Builder Acceptance Repair

Last Architect-accepted before review: TB-P10-T022-R13-R1-R1

Last Implementation: TB-P10-T022-R13-R2-R2

TB-P11-T001 retained for later

Appearance YES

Builder NO

no TB-P11-T002

no TB-P10-T023

branch main

HEAD==origin/main

user work preserved

Do NOT write Worker IDLE.
After canonical Result through Bridge:
STOP completely.

Acceptance

PASS only if:

two real Admin→exact-public-route flows are proven,

selected variant identity is proven,

SSR intact,

ProductCard intact,

user work preserved,

canonical Git Result contains real final SHA,

HEAD==origin/main,

no placeholder values,

Result delivered through Bridge,

worker stops.

Result Contract

Return ONLY canonical BRIDGE-WAKE-V1 Result with:

Summary
Applicable-Locks
Recovery-Start
Page-Route-Binding
Sunny-EndToEnd
Cinematic-EndToEnd
Route-Binding-Fix
Variant-Identity
ProductCard-Regression
SSR-Exact-Route
Cache-Persistence
User-Work-Preservation
Focused-Validation
Visual-Evidence
AntiPattern-Scan
Lock-Registry
Recovery-SoT
Git
Architectural-Concerns
Visual-Concerns
Blockers

Git MUST contain a real full SHA, never PLACEHOLDER_SHA.

Do not include Worker-IDLE.
After sending Result through Bridge, STOP.

END_TOOBA_TASK
