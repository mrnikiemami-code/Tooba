# Tooba — TB-P00-T021 — Frontend, UX & Template Adaptation Architecture

BEGIN_TOOBA_CURSOR_TASK_V1

Protocol-Version: 1
Task-ID: TB-P00-T021
Phase: P00 — Architecture / Discovery
Type: Architecture Design / Frontend, UX & Template Adaptation
Repository: https://github.com/mrnikiemami-code/Tooba
Primary-Branch: main
Implementation-Agent: Cursor
Architect: ChatGPT
Execution-Mode: PIPELINE
Depends-On: TB-P00-T020
Architect-Decision-On-Dependency: ACCEPTED

## Objective

Design Tooba's frontend architecture and purchased-template adaptation strategy deeply enough to support:

- professional commercial-grade storefront UX;
- professional Admin panel;
- professional Seller panel;
- polished Customer dashboard;
- intentional mobile UX;
- RTL/LTR;
- accessibility;
- strong SEO and Core Web Vitals;
- runtime tenant themes;
- multilingual routes/content;
- cohesive cross-module workflows;
- Next.js App Router architecture;
- Server Component first where appropriate;
- Shopeiva reuse without inheriting its architecture blindly;
- component/design-system governance;
- visual acceptance and screenshot evidence;
- future maintainability without page-by-page UI chaos.

This task is architecture/documentation only.

Do not implement pages, components, CSS, template migration, routes, screenshots, visual redesign, or backend APIs.

---

## 1. Repository Recovery

Before edits:

```bash
git rev-parse --show-toplevel
git fetch origin
git branch --show-current
git rev-parse HEAD
git rev-parse origin/main
git status --short --branch
```

Require:

```text
branch = main
HEAD == origin/main
working tree clean
```

Unsafe state =>

```text
RECOVERY_CONFLICT
```

Never force push, rewrite history, destructively reset, silently stash, or overwrite unknown work.

Expected predecessor:

```text
44577d76c7838063e0921e8db7b8deac7958c83f
```

If synchronized main legitimately advanced, continue from current safe main.

---

## 2. Source-of-Truth Transition

Update:

```text
TB-P00-T020 = ACCEPTED
TB-P00-T021 = ISSUED / AWAITING_ARCHITECT_ACCEPT
```

Roadmap:

```text
P00 Caching / Infrastructure Abstractions = COMPLETE
P00 Frontend / Template Adaptation Strategy = IN_PROGRESS
```

Do not mark T021 accepted.

---

## 3. Create Architecture Document

Create:

```text
docs/architecture/20-frontend-ux-template-adaptation.md
```

It must contain all sections below.

---

# A. Product-Critical UI Principle

Hard rule:

```text
Weak UI/UX = Product Failure
```

Frontend quality is not a late cosmetic phase.

Architecture must support:

```text
storefront
Admin
Seller
Customer
mobile
RTL/LTR
accessibility
SEO
performance
visual consistency
```

as first-class product requirements.

---

# B. Backend Boundary != UI Boundary

Hard rule:

```text
Backend/module boundary != UI boundary
```

Do not mirror backend modules as disconnected menu CRUDs.

Example:

Backend may have:

```text
Catalog
Media
Pricing
Inventory
SEO
Content
Offer
```

but Admin UX may provide one cohesive:

```text
Product Workspace
```

with integrated sections/tabs/steps.

Define this as a locked UX architecture principle.

---

# C. Shopeiva Role

Purchased template:

```text
shopeiva.zip
```

is:

```text
UI/reference/reuse input
```

not:

```text
domain truth
architecture truth
SEO truth
security truth
tenant truth
authorization truth
```

Reuse selectively.

Do not preserve bad information architecture just because it exists in template.

Do not promote template-only features into requirements.

---

# D. Template Reuse Classification

Create a reuse matrix for template assets/components/routes.

Classify each candidate area as:

```text
REUSE
ADAPT
REBUILD
DROP
DEFER
```

Cover at least:

```text
storefront shell
header/navigation
home sections
product cards
product detail layout
category/listing
search UI
cart
checkout
auth
customer dashboard
seller dashboard
content/blog pages
shared components
icons
forms
tables
charts
theme utilities
demo data
fonts/assets
```

Do not implement.

---

# E. Frontend Stack Direction

Based on known template inventory, preserve:

```text
Next.js App Router
React
TypeScript
Tailwind CSS
```

if repository/product direction remains consistent.

Document what is recommended vs needs ADR.

Do not blindly keep every template dependency.

Dependencies must earn their place.

---

# F. Server Component First

For public storefront and SEO-critical pages, default direction:

```text
Server Component First
```

Use Client Components only when interaction requires them.

Avoid converting entire pages to client-rendered apps unnecessarily.

Benefits:

```text
SEO
performance
smaller JS
server data composition
security boundaries
```

Do not implement.

---

# G. Client Component Boundary

Client Components are appropriate for:

```text
interactive filters
cart interactions
modals/drawers
forms
autocomplete
drag/drop
rich editors
dashboard interactions
local optimistic UI
```

Do not make every reusable component `"use client"`.

Define clear client-island strategy.

---

# H. Data Fetching / BFF Composition

Frontend pages may require data from several modules.

Use:

```text
application composition
BFF/read model
server composition
```

not direct database access from frontend.

Public product page may compose:

```text
Catalog
Offer
Pricing
Inventory
Reviews
Media
Content
SEO
```

behind application/read contracts.

Do not encode backend module topology into page-level spaghetti requests.

---

# I. Storefront Information Architecture

Define high-level storefront IA.

At minimum account for:

```text
Home
Category
Brand
Search
Product
Cart
Checkout
Content
Landing/Campaign
Account
```

Marketplace may also include:

```text
Seller storefront
Other offers
```

IA must be customer-goal-oriented.

Do not design from database table names.

---

# J. Admin Information Architecture

Admin must be operational and workflow-oriented.

Candidate top-level domains may include:

```text
Commerce
Catalog
Orders
Sellers
Customers/Parties
Content
Promotions
Analytics
Operations
Configuration
Security
```

Exact IA may evolve.

Hard rule:

Do not create one menu item per backend entity/table.

---

# K. Product Workspace UX

This is mandatory.

Design conceptually a unified product-authoring workspace.

Possible areas:

```text
Overview
Core Information
Variants
Specifications
Categories / Brand
Media
Offers
Pricing
Inventory
SEO
Content / Editorial
Publishing
Audit / History
```

Backend modules remain separate.

UI orchestrates them coherently.

Avoid making admin user jump between disconnected screens for one product.

---

# L. Seller Panel UX

Seller portal must support professional B2B/marketplace workflows.

Potential areas:

```text
Dashboard
Offers
Products / Catalog Requests
Pricing
Inventory
Orders
Customers where permitted
Analytics
Reviews
Promotions where permitted
Support
Settings
Team / Access
```

Do not simply clone Admin.

Seller scope must be authorization-aware.

---

# M. Customer Dashboard UX

Customer experience should be polished.

Potential areas:

```text
Overview
Orders
Addresses
Profile
Security
Notifications
Support
Saved items / wishlist only if product requirement later confirms
```

Do not expose backend boundaries.

Mobile-first navigation matters.

---

# N. Design System

Define a first-class design system layer.

At minimum:

```text
Design Tokens
Typography
Spacing
Grid
Radius
Elevation
Color Roles
State Colors
Iconography
Motion
Component Variants
Breakpoints
Accessibility Constraints
```

Do not hardcode random per-page styles.

Theme overrides operate through approved tokens/variants.

---

# O. Component Layers

Define conceptual component hierarchy such as:

```text
Primitives
Design-system components
Commerce components
Feature composites
Page sections
Page layouts
```

Examples:

```text
Button
Input
Dialog
DataGrid
ProductCard
PriceDisplay
StockBadge
SellerOfferCard
ProductGallery
OrderTimeline
HeroSection
```

Avoid one giant shared-components folder with no ownership conventions.

---

# P. Runtime Theme Architecture

Single-Store supports runtime tenant theme.

Hard rules:

```text
one shared publish
runtime theme
no arbitrary executable tenant code
```

Theme may include:

```text
design tokens
brand assets
approved component variants
layout/composition settings
```

Theme must preserve:

```text
a11y
responsive behavior
SEO
CWV
```

---

# Q. Theme Safety

Do not allow tenant-configured theme values to:

```text
hide legal/critical content
break contrast
destroy focus states
inject JavaScript
inject unsafe CSS
change semantic page structure arbitrarily
```

Define validated token constraints.

---

# R. RTL / LTR

RTL/LTR must be architectural.

Need:

```text
logical CSS properties
direction-aware icons
layout mirroring
text alignment
mixed-language content
tables
forms
charts
drawers
breadcrumbs
pagination
```

Do not fix RTL with page-specific hacks.

---

# S. Mobile-First UX

Mobile is not scaled desktop.

Design intentional mobile patterns for:

```text
navigation
search
filters
product gallery
sticky purchase CTA
cart
checkout
forms
admin/seller dense tables
drawers/sheets
dashboards
```

For operational UIs, decide when responsive tables become cards/stacked views versus horizontal grids.

No implementation.

---

# T. Accessibility

Target strong accessibility.

At minimum preserve:

```text
semantic HTML
keyboard navigation
focus management
screen reader labels
heading hierarchy
contrast
form errors
aria only when needed
reduced motion
accessible dialogs
accessible tables/charts
alt text
```

Accessibility is part of acceptance, not optional cleanup.

---

# U. Loading States

Every major UI path needs intentional loading behavior.

Patterns may include:

```text
skeleton
streamed section
optimistic local action
progress
deferred panel
```

Do not show random full-screen spinners everywhere.

Loading must preserve layout stability.

---

# V. Empty States

Professional empty states must:

```text
explain state
offer next action
respect role/scope
avoid looking broken
```

Examples:

```text
no orders
no seller offers
no search results
no products in category
no analytics data
```

---

# W. Error States

Need intentional error UX for:

```text
validation
authorization denial
not found
dependency unavailable
partial data failure
payment failure
inventory change
network error
AI unavailable
```

Do not leak exception details.

Support retry/recovery where safe.

---

# X. Partial Degradation

Composite pages may partially degrade.

Example PDP:

```text
reviews unavailable
```

should not necessarily break:

```text
product + price + purchase
```

Design composition with critical vs optional sections.

---

# Y. Forms

Professional form architecture must support:

```text
server validation
client interaction validation
field-level errors
form-level errors
async checks
dirty state
unsaved changes
multi-step flows
autosave where justified
accessibility
RTL/LTR
```

Do not rely only on client validation.

---

# Z. Admin Data Grid

This is mandatory product UX.

Design requirements for reusable professional Data Grid:

```text
sorting
column-type filters
global search where appropriate
reorder columns
show/hide columns
resize where useful
saved views
pagination
selection
bulk actions
export
sticky columns/header where useful
empty/loading/error states
keyboard accessibility
RTL/LTR
responsive strategy
```

Different column types need appropriate filters:

```text
text
number
money
date
enum
boolean
entity
status
```

Do not build a one-size-fits-all text filter.

---

# AA. Saved Views

Admin/Seller grids should support future:

```text
personal saved views
shared/team views where authorized
default views
filter/sort/column state
```

Do not implement now.

Authorization applies to shared views.

---

# AB. Bulk Actions

Bulk actions require:

```text
selection
scope clarity
authorization
confirmation for risky actions
partial failure reporting
background execution for large operations
audit
```

Do not make every bulk action synchronous.

---

# AC. Navigation

Design separate navigation patterns for:

```text
Storefront
Admin
Seller
Customer
```

Do not reuse one navigation shell everywhere.

Preserve breadcrumbs/context where workflows are deep.

---

# AD. Search UX

Storefront search must be first-class.

Preserve:

```text
autocomplete
suggestions
recent/popular queries future
category/brand/product suggestions
mobile search
keyboard support
zero-result recovery
filters
chips
sorting
result count
```

Search backend boundaries remain invisible to user.

---

# AE. Category / PLP UX

Professional listing experience should consider:

```text
facets
sort
result count
selected chips
clear all
mobile filter sheet
pagination/infinite-scroll progressive enhancement
skeletons
empty results
SEO crawlable navigation
```

Do not sacrifice SEO for client-only interaction.

---

# AF. PDP UX

Product Detail Page must support:

```text
gallery
title/brand/category context
variants
price
availability
seller/offer selection
other sellers Marketplace
delivery/fulfillment
CTA
specifications
content
reviews
related products
SEO semantic content
```

Need clear visual hierarchy.

Do not create dense unstructured wall of modules.

---

# AG. Marketplace Offer UX

Multiple offers must be understandable.

Potential UX:

```text
selected/buy-box offer
other offers
seller reputation
price
availability
delivery promise
warranty/service
```

Do not confuse Product identity with seller Offer.

---

# AH. Cart UX

Cart must communicate:

```text
seller grouping where relevant
quantity
price changes
availability changes
promotion
shipping estimates
next action
```

Long-lived stale cart state should be visible/recoverable.

---

# AI. Checkout UX

Checkout should reduce friction.

Design principles:

```text
clear steps
progress
address
delivery
payment
review
validation
price/stock changes
recovery
mobile usability
guest checkout
```

Do not reflect internal module orchestration as user-visible complexity.

---

# AJ. Auth UX

Login must support extensible identifiers/methods.

UX must accommodate:

```text
username/email/phone
password
OTP
MFA
recovery
external IdP future
```

without redesign.

Do not assume one fixed email/password form forever.

---

# AK. Content Studio UX

Content/Admin must support:

```text
library
filters
locale status
draft/review/published
revision history
preview
schedule
SEO inputs
taxonomy
related entities
AI eligibility
```

Do not reduce to title/body CRUD.

---

# AL. Page Composition UX

Landing composer should support:

```text
approved section palette
reorder
configuration
media picker
product/category binding
responsive preview
locale preview
theme preview
schedule/publish
validation
```

No arbitrary code blocks.

---

# AM. Media UX

Integrated media flows should support:

```text
upload
progress
ordering
primary selection
crop/focal point
alt/caption
responsive preview
processing/failure states
```

Product/Content editors should not be forced into separate Media CRUD for normal work.

---

# AN. Analytics UX

Admin/Seller analytics should be KPI/decision-oriented.

Need:

```text
date range
comparison
filters
drill-down
freshness
accessible charts/table
export
mobile behavior
RTL/LTR
```

Do not expose raw event schema.

---

# AO. Operations UX

Admin operations should support cohesive diagnosis:

```text
order/payment correlation
audit history
search/index state
jobs/queues
media failures
AI degradation
tenant context
```

Not raw log CRUD.

---

# AP. SEO Rendering

Public critical content should be server-rendered/crawlable.

UI interactions may progressively enhance.

Preserve:

```text
semantic headings
canonical content
breadcrumbs
crawlable pagination
structured data consistency
```

Do not hide critical SEO content behind client-only fetch.

---

# AQ. Core Web Vitals

Frontend architecture must explicitly protect:

```text
LCP
INP
CLS
```

Consider:

```text
server rendering
image optimization
font strategy
JS budget
client component budget
code splitting
streaming
cache
layout dimensions
third-party scripts
```

No implementation.

---

# AR. Performance Budgets

Define that implementation phase must establish measurable budgets for:

```text
JS shipped
page weight
LCP asset
interaction latency
image weight
third-party scripts
API/query latency
```

Exact values later.

Do not optimize only for Lighthouse screenshots.

---

# AS. Third-Party Scripts

Marketing/analytics/chat scripts can harm UX.

Need controlled loading:

```text
consent/policy
defer/lazy load
performance budget
failure isolation
```

Do not let third-party scripts block purchase path.

---

# AT. Frontend State Management

Do not default to global client state.

Use:

```text
server data
URL state
local component state
form state
small shared client stores only when justified
```

Known template includes Zustand; do not keep it merely because template has it.

Each global store must have explicit need.

---

# AU. URL as State

For shareable/navigable states such as:

```text
search query
filters
sort
pagination
tab where semantically appropriate
```

prefer URL state where it improves navigation/SEO/shareability.

Do not place all UI state in URL.

---

# AV. Client Data Cache

Frontend client cache must not become duplicate business source.

Avoid complex client-side normalized cache unless product needs it.

Server-side composition/read models remain primary for public pages.

---

# AW. Optimistic UI

Use optimistic updates only for reversible/predictable actions.

Examples may include:

```text
cart quantity
wishlist future
simple preferences
```

Do not optimistically claim:

```text
payment success
inventory reservation success
order placed
```

without server authority.

---

# AX. Design Tokens and Theming

Define token categories:

```text
color
typography
spacing
radius
shadow
motion
breakpoints
component density
```

Theme changes happen through approved tokens/variants.

Avoid arbitrary per-tenant CSS injection.

---

# AY. Visual Consistency

Need one coherent visual language across:

```text
Storefront
Customer
Seller
Admin
```

They may have different density/navigation while sharing design-system foundations.

Do not copy unrelated template visual patterns blindly.

---

# AZ. Density Strategy

Admin/Seller interfaces often need higher information density than storefront.

Design system should support:

```text
comfortable
compact
```

or equivalent density modes.

Do not force storefront card spacing into data-heavy operations.

---

# BA. Accessibility + Density

Dense tables/forms still require:

```text
touch target minimums
keyboard focus
readable type
contrast
row/action clarity
```

Density must not destroy usability.

---

# BB. Motion

Motion should aid orientation, not decoration.

Need:

```text
reduced-motion support
bounded animations
no layout-shifting entrance effects
```

Do not inherit excessive template animations automatically.

---

# BC. Iconography

Use consistent icon system.

Known template uses Lucide React; classify whether to retain.

Avoid mixing many icon libraries.

Direction-aware icons must behave correctly in RTL.

---

# BD. Typography

Need multilingual typography strategy.

Consider:

```text
Persian readability
Latin readability
font loading
weights
fallbacks
numerals
line-height
CWV
```

Do not embed font files in repository decisions without license/asset review.

---

# BE. Localization UX

Translations should not break layout.

Design for:

```text
longer labels
different pluralization
RTL/LTR
localized numbers/dates/currency
```

Do not hardcode widths around Persian-only labels.

---

# BF. Date / Time UX

Use localized presentation while preserving domain time semantics.

Do not let frontend date-picker library define business time model.

Template dependencies such as Persian date packages are UI implementation details.

---

# BG. Currency UX

Price display must respect:

```text
currency
locale formatting
market
rounding/display policy
```

Do not infer commercial currency from language.

---

# BH. Security UX

Sensitive actions need clear UX:

```text
reauth/step-up
confirmation
authorization denied
session expired
MFA
```

Do not leak permission model internals.

---

# BI. Authorization-Aware Navigation

Navigation/menu visibility may use authorization checks for UX.

But:

```text
hidden menu != security
```

Server/use-case authorization remains mandatory.

Avoid loading hundreds of per-item permission checks; use bulk/scope-aware permission data.

---

# BJ. Error Boundaries

Frontend architecture should support:

```text
route-level error boundary
feature/section boundary
recoverable component error
not-found
unauthorized
```

Composite pages should degrade intentionally.

---

# BK. Telemetry / UI Diagnostics

Frontend must integrate later with OpenTelemetry/observability strategy for:

```text
JS errors
route failures
CWV
critical interaction latency
failed API calls
```

No sensitive form data in telemetry.

---

# BL. Testing Pyramid — Frontend

Future implementation should support:

```text
unit tests
component tests
integration tests
route/server tests
E2E critical journeys
accessibility tests
visual regression
responsive tests
RTL/LTR tests
```

Do not rely only on snapshots.

---

# BM. Visual Acceptance Protocol

This is mandatory.

Any future task implementing/modifying user-visible UI must provide visual evidence.

At minimum capture/review:

```text
Desktop
Mobile
Responsive intermediate width where meaningful
RTL
LTR where supported
Loading
Empty
Error
Long-content/edge state
```

Not every screen needs all evidence in every task, but the task must define applicable states.

Cursor PASS is insufficient without visual inspection.

Architect must separately ACCEPT visual result.

---

# BN. Screenshot Evidence

Future UI tasks should place durable evidence under:

```text
docs/evidence/
```

or the pipeline-defined evidence location.

Evidence should identify:

```text
Task-ID
Route/screen
Viewport
Locale/direction
State
Date/build/commit
```

Do not store meaningless screenshots with no context.

---

# BO. Visual Regression

Preserve future ability to add automated visual regression.

Do not require tool/provider now.

Use stable test fixtures/data.

---

# BP. Realistic Data

UI acceptance must use realistic content lengths and states.

Avoid accepting screens only with:

```text
"Test"
"$10"
"Product 1"
```

Need long Persian/English names, multi-line content, zero/large values, multiple statuses, seller variations.

---

# BQ. Skeleton Fidelity

Loading skeletons should approximately match final layout.

Avoid severe CLS when content loads.

---

# BR. Empty/Error Quality

Empty and error states are part of visual acceptance.

Do not leave them as raw exception boxes or blank white space.

---

# BS. Cross-Module Workspace Pattern

Document reusable UX orchestration principle.

Examples:

```text
Product Workspace
Order Workspace
Seller Workspace
Customer Workspace
Content Studio
Tenant Settings Workspace
```

Each may compose multiple module-owned data/contracts into one user-goal-oriented screen.

---

# BT. Order Workspace

Admin/Seller order view may compose:

```text
Order
Payment
Fulfillment
Inventory events
Customer/Party
Audit
Support
```

without direct DB joins.

UI should present one coherent case/workspace.

---

# BU. Seller Workspace

Admin seller management may compose:

```text
Party/Organization
Seller status
Offers
Orders
Inventory
Analytics
Authorization/team
Audit
Support
```

not eight unrelated screens.

---

# BV. Customer Workspace

Admin customer support may compose:

```text
Party
Identity-safe summary
Orders
Payments
Support
Audit
Authorization-safe actions
```

PII access must be permissioned.

---

# BW. Route / Feature Folder Strategy

Define recommended frontend organization around:

```text
app routes
feature modules
shared design system
server composition
client interaction islands
```

Do not create technical folder structure solely from backend module names.

Provide recommended high-level folder conventions, but no code.

---

# BX. Shared Component Governance

A component becomes shared only when genuinely reusable.

Avoid premature generic abstractions.

Document rules for:

```text
where shared components live
when feature-specific component stays local
how variants are modeled
how breaking visual changes are governed
```

---

# BY. Template Extraction Process

Define future Shopeiva adaptation workflow:

```text
inventory component
classify reuse
extract dependency
remove demo data
normalize styles
convert to design-system tokens
adapt RTL/LTR
adapt server/client boundary
replace hardcoded content
connect real read contracts
validate SEO/a11y/CWV
visual review
```

Do not copy whole template into production unchanged.

---

# BZ. Template Dependency Audit

Known dependencies should be classified later as:

```text
KEEP
REPLACE
REMOVE
DEFER
```

At minimum consider:

```text
axios
chart.js
framer-motion
fuse.js
lucide-react
next-themes
persian-date
persian-datepicker
react-chartjs-2
react-hook-form
react-loading-skeleton
react-otp-input
react-paginate
react-toastify
swiper
zod
zustand
```

Do not automatically accept them.

---

# CA. Client-Side Search Libraries

Template includes Fuse.js.

Do not use it as product Search architecture.

It may be useful for tiny local lists only.

Canonical storefront search remains server/search-engine based.

---

# CB. Axios vs Platform Fetch

Evaluate whether `axios` remains necessary in Next.js architecture.

Do not lock decision now unless justified.

Server-side data access may prefer platform/native fetch/contracts where appropriate.

Classify as later implementation detail.

---

# CC. Charts

Chart library use belongs to analytics/admin UI.

Charts must have:

```text
accessible fallback/table
RTL labels
responsive sizing
loading/empty states
```

Do not make one chart library architecture truth.

---

# CD. Toasts / Notifications

Toasts are transient UX, not replacement for inline status/error.

Use appropriately.

Do not hide critical payment/order failures only in disappearing toast.

---

# CE. Modals / Drawers

Use modal/drawer/sheet based on workflow and viewport.

Mobile often needs bottom sheet/full-screen patterns.

Accessibility/focus management mandatory.

---

# CF. Tables vs Cards

Operational responsive design must decide per use case.

Do not mechanically convert every table row into cards.

For dense desktop workflows, high-quality Data Grid is preferred.

Mobile may use:

```text
condensed list
priority columns
detail drawer
horizontal scroll only when justified
```

---

# CG. Internationalization Architecture

Frontend must consume translation system/contracts without hardcoded language assumptions.

Need:

```text
route locale
message locale
content locale
direction
number/date/currency formatter
```

Do not mix translation strings into random components without namespace/domain organization.

---

# CH. Feature Discovery / Progressive Disclosure

Complex Admin/Seller workflows should use progressive disclosure.

Do not show every advanced setting on first screen.

But advanced capabilities must remain discoverable.

---

# CI. Confirmation / Destructive Actions

Destructive/high-impact actions require:

```text
clear consequence
authorization
confirmation
reason where appropriate
audit
```

Avoid generic "Are you sure?" without context for critical operations.

---

# CJ. Keyboard Efficiency

Professional Admin/Seller users benefit from:

```text
logical tab order
keyboard grid navigation
command/search palette future
shortcuts where discoverable
bulk selection
```

Do not compromise accessibility.

---

# CK. Data Freshness

Composite dashboards/workspaces must communicate freshness where data is eventually consistent.

Examples:

```text
search index pending
analytics updated 5 minutes ago
AI knowledge refresh pending
```

Do not present stale projections as real-time truth.

---

# CL. Optimistic vs Confirmed States

UI must visually distinguish pending from confirmed actions.

Examples:

```text
saving...
payment pending
inventory reservation pending
content publishing scheduled
search reindex pending
```

Do not show success before authoritative confirmation.

---

# CM. UI Security Boundaries

Never render secrets.

Avoid exposing:

```text
connection refs/secrets
provider keys
internal error stack
private IDs unnecessarily
```

Admin capabilities should reveal only needed operational detail.

---

# CN. Frontend Build Boundaries

Storefront/Admin/Seller/Customer may share one Next.js application or composed route areas initially.

Do not prematurely split into many frontend deployments.

Recommend architecture that preserves future split if commercially needed.

Exact deployment split:

```text
NEEDS_LATER_P00_DETAIL
```

---

# CO. Edition Differences

Marketplace and Single-Store UI differ by policy/composition.

Do not scatter:

```text
if (marketplace)
```

through every component.

Use edition capabilities/configuration/context.

Single-Store may hide seller/other-offer UI while reusing core commerce components.

---

# CP. Feature Capability Model

Frontend can consume a normalized capability model such as:

```text
Marketplace enabled?
Seller portal enabled?
Multiple offers?
B2B enabled?
AI assistant enabled?
```

Exact mechanism later.

Do not use template route existence as capability truth.

---

# CQ. Page Composition Renderer

Frontend must render approved Page Composition sections through a registry.

Conceptually:

```text
SectionType
→ Approved Renderer
→ Validated Config
```

No arbitrary dynamic code import from database values.

---

# CR. Content Renderer Safety

Rich content must use safe rendering.

No unsanitized arbitrary HTML/script execution.

Heading semantics must remain controlled.

---

# CS. SEO / UI Coordination

SEO route policy and UI navigation must agree.

Examples:

```text
facet chips
pagination
canonical route
hreflang links
breadcrumbs
internal linking
```

Do not let interactive UI create unbounded crawlable URLs.

---

# CT. Performance / Design Trade-Off

Hard principle:

```text
Visual richness is allowed,
but uncontrolled JS, media, animation and layout instability are not.
```

A visually polished product must still be fast.

Do not solve performance by making UI plain/uncompetitive.

---

# CU. UI Acceptance Gate Criteria

Future implementation gates should reject UI when:

```text
build passes but layout is poor
mobile is broken
RTL is broken
loading/empty/error missing
accessibility is poor
screens are generic CRUD
visual hierarchy is weak
template artifacts/demo content remain
cross-module workflow is fragmented
performance is visibly degraded
```

This must be documented as an acceptance rule.

---

# CV. Data Ownership / UI Composition Matrix

Create matrix across:

```text
Storefront
Admin
Seller
Customer
Catalog
Offer
Pricing
Inventory
Content
Media
Search
Order
Payment
Analytics
Authorization
```

For representative screens:

```text
PDP
PLP
Product Workspace
Order Workspace
Seller Workspace
Customer Dashboard
Content Studio
Analytics Dashboard
```

Mark:

```text
UI OWNER
DATA OWNER
COMPOSED FROM
AUTHORIZATION SOURCE
```

---

# CW. Screen Quality Matrix

Define quality dimensions:

```text
Visual hierarchy
Information architecture
Interaction clarity
Mobile
RTL/LTR
Accessibility
Performance
Loading
Empty
Error
Data freshness
Authorization clarity
```

Future UI tasks must identify applicable dimensions.

---

# CX. Failure Matrix

Analyze:

```text
Partial read-model failure
Search unavailable
Price unavailable
Inventory stale
Image failed
Unauthorized action
Theme config invalid
Translation missing
Client JS error
Server render error
Slow third-party script
AI unavailable
```

For each classify:

```text
page still usable?
hide/degrade section?
retry?
fallback?
error boundary?
customer-visible direction?
```

---

# CY. Testing Strategy — Architecture Level

Future implementation must test:

```text
desktop/mobile
RTL/LTR
screen reader/keyboard
tenant theme isolation
locale switching
market/currency display
public/private routing
SEO SSR
client island boundaries
Data Grid behavior
Product Workspace composition
Seller scope
Customer scope
loading/empty/error
visual regression
CWV budgets
```

No tests now.

---

# CZ. Decision Summary

Classify:

```text
RECOMMENDED_FOR_ADR
NEEDS_LATER_P00_DETAIL
DEFERRED
```

At minimum recommend:

1. UI/UX quality is a product-critical architecture concern.
2. Backend module boundaries must not dictate UI information architecture.
3. Shopeiva is reuse/reference input, not architecture truth.
4. Template areas are explicitly classified REUSE/ADAPT/REBUILD/DROP/DEFER.
5. Next.js App Router + Server Component First remains recommended.
6. Client Components are interaction islands, not default page architecture.
7. Public pages compose server-side read models/contracts.
8. A first-class Design System with validated runtime theme tokens is required.
9. One shared Single-Store publish supports tenant runtime themes.
10. RTL/LTR and mobile UX are architectural, not patchwork.
11. Accessibility is a mandatory acceptance dimension.
12. Admin Product Workspace composes Catalog/Media/Offer/Pricing/Inventory/SEO/Content coherently.
13. Seller/Admin/Customer experiences are distinct workflow products.
14. Professional typed Data Grid is a shared operational UX capability.
15. Loading/Empty/Error states are mandatory product states.
16. SEO-critical content remains server-rendered/crawlable.
17. CWV/performance budgets coexist with rich commercial UI.
18. Global client state is minimized; template Zustand is not automatically retained.
19. Page Composition uses approved renderer registry, not arbitrary executable code.
20. User-visible UI implementation requires visual evidence and Architect visual acceptance.
21. Build/test PASS alone never constitutes UI acceptance.
22. Future UI tasks must validate Desktop/Mobile/RTL/LTR/a11y/responsive states as applicable.
23. Cross-module workspaces are a reusable UX architecture pattern.
24. Marketplace/Single-Store UI variation uses capabilities/policy, not scattered conditionals.

Do not create final ADR yet.

---

## 4. Consistency Updates

If required, minimally align:

```text
docs/architecture/00-technical-inventory.md
docs/architecture/02-edition-tenant-deployment.md
docs/architecture/07-catalog-product-offer.md
docs/architecture/12-content-page-composition.md
docs/architecture/13-seo-architecture.md
docs/architecture/14-search-indexing.md
docs/architecture/15-media-image-pipeline.md
docs/architecture/18-observability-logging-audit.md
docs/architecture/19-caching-infrastructure-abstractions.md
```

No unrelated redesign.

---

## 5. Update SoT

Update:

```text
docs/PROJECT-STATE.md
docs/ROADMAP.md
docs/ai/TOOBA-RECOVERY-CONTEXT.md
```

Expected:

```text
Last Architect Accepted Task: TB-P00-T020
Current Issued Task: TB-P00-T021
Task State: AWAITING_ARCHITECT_ACCEPT
Current Phase: P00 — Architecture / Discovery
Frontend / Template Adaptation Strategy: IN_PROGRESS
```

No next task authorized.

---

## 6. Save Complete Envelope Verbatim

Save exact task to:

```text
docs/ai/tasks/TB-P00-T021.task.md
```

Verify:

```text
BEGIN_TOOBA_CURSOR_TASK_V1
END_TOOBA_CURSOR_TASK_V1
```

Do not save a shortened pointer.

---

## 7. Explicit Out of Scope

Do NOT:

- copy Shopeiva into application;
- implement frontend;
- create Next.js routes;
- build components;
- create design tokens;
- create Data Grid;
- redesign pages;
- take screenshots;
- add visual regression tooling;
- change backend architecture;
- create final ADR.

---

## 8. Validation

Run:

```bash
git diff --check
git status --short --branch
```

Manual checks:

- T020 ACCEPTED;
- T021 awaiting Architect acceptance;
- UI/UX explicitly product-critical;
- Backend boundary != UI boundary;
- Shopeiva classified as input, not truth;
- template reuse matrix exists;
- Server Component First preserved;
- client island boundary represented;
- Design System/runtime theme represented;
- Admin Product Workspace represented;
- professional Seller/Customer/Admin UX represented;
- Data Grid requirements represented;
- Desktop/Mobile/RTL/LTR/a11y acceptance represented;
- Loading/Empty/Error states represented;
- SEO/CWV preserved;
- visual evidence/Architect visual acceptance required for future UI tasks;
- no application code.

---

## 9. Git Commit & Push

Commit:

```text
docs design Tooba frontend UX architecture [TB-P00-T021]
```

Then:

```bash
git push origin main
git fetch origin
git rev-parse HEAD
git rev-parse origin/main
git status --short --branch
```

Require:

```text
HEAD == origin/main
working tree clean
```

No force push.

---

## 10. Result Contract

Return exactly:

```text
BEGIN_TOOBA_CURSOR_RESULT_V1

Protocol-Version: 1
Task-ID: TB-P00-T021
Phase: P00 — Architecture / Discovery
Status: PASS | BLOCKED | RECOVERY_CONFLICT

Summary:
...

Repository-Recovery:
- Repo-Root:
- Branch:
- Starting-HEAD:
- Starting-Origin-Main:
- Starting-Status:

Changes:
- ...

Frontend-Boundary:
- ...

Template-Adaptation:
- ...

Server-Client-Composition:
- ...

DesignSystem-Theming:
- ...

Admin-Seller-Customer-UX:
- ...

DataGrid-Workspaces:
- ...

SEO-CWV-A11y:
- ...

Visual-Acceptance:
- ...

Decision-Summary:
- RECOMMENDED_FOR_ADR:
- NEEDS_LATER_P00_DETAIL:
- DEFERRED:

Validation:
- git diff --check:
- other checks:
- manual consistency review:

Git:
- Commit:
- Push:
- Final-HEAD:
- Final-Origin-Main:
- Final-Status:
- Head-Matches-Origin: YES | NO

Source-of-Truth:
- Last-Architect-Accepted-Task:
- Current-Issued-Task:
- Current-Phase:
- Task-State:
- Recovery-Ready: YES | NO

Architectural-Concerns:
- ...

Blockers:
- ...

END_TOOBA_CURSOR_RESULT_V1
```

---

## 11. Pipeline Continuity — MANDATORY

After RESULT:

```text
WAITING
```

Remain inside the Tooba Architect-controlled pipeline.

Do not leave PIPELINE mode.

Do not invent, infer, prepare, or execute unauthorized work.

Do not advance to `TB-P00-T022`, any gate, implementation task, template migration, UI work, or any other work without a new valid Architect envelope containing:

```text
BEGIN_TOOBA_CURSOR_TASK_V1
...
END_TOOBA_CURSOR_TASK_V1
```

Cursor PASS is not Architect ACCEPT.

After sending RESULT, stop execution and remain WAITING for Architect review.

---

## 12. UI / UX Protection — MANDATORY PROJECT RULE

Tooba is a commercial product.

Weak UI/UX is considered product failure.

This rule remains active for every future phase and task.

Hard rules:

```text
Backend/module boundary != UI boundary
Build PASS != UI ACCEPT
Functional PASS != Visual ACCEPT
Desktop PASS != Mobile PASS
LTR PASS != RTL PASS
```

Future user-visible implementation work must preserve:

```text
professional storefront
professional Admin
professional Seller portal
polished Customer dashboard
intentional mobile UX
RTL/LTR
accessibility
responsive design
loading/empty/error states
strong visual hierarchy
high-quality interactions
Core Web Vitals
SEO semantics
```

Every future user-visible implementation task must define applicable visual evidence.

Architect visual acceptance is mandatory before that UI task is considered accepted.

END_TOOBA_CURSOR_TASK_V1
