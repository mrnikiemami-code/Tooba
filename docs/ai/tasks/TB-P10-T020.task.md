PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_TASK

Task-ID:
TB-P10-T020

Parent-Task:
TB-P10-T019

Channel:
tooba-main

WorkerId:
tooba-worker-01

AgentType:
cursor

Status:
ISSUED

Appearance-USER_VISUAL_ACCEPTED=YES
Builder-USER_VISUAL_ACCEPTED=NO

Title:
Native Section Expansion + First Variant Wave + Template Start UX

Architect Decision

TB-P10-T019 is Architect-ACCEPTED.

The shared renderer and Admin variant picker are working, but T019 reports two temporary proxy mappings:

StoryRail -> Host CategoryGrid proxy

BannerShowcase -> Host PromoBanner proxy

These proxies are acceptable only as migration scaffolding and must NOT become the permanent section model.

This task removes that limitation and implements the first substantial wave of real reusable variants.

This is still a controlled builder:

no free-form CSS

no free-form HTML/JS

no user breakpoints

mobile remains system-owned

only four global surface colors remain user-editable

Do NOT invent TB-P10-T021.

Recovery State

Phase: P10
Last Architect-accepted: TB-P10-T019
Last Implementation: TB-P10-T019
Current Task: TB-P10-T020
Expected previous HEAD: a54bde5c38f93499e5b400ebd186ffe9d2b787fe
Worker: tooba-worker-01

Preserve LOCK-SF-001…235.

1. Recovery Preflight

Record branch, HEAD, origin/main, git status, Recovery/SoT, Host :5088, FE :3000, current registered SectionTypes/Variants, unrelated user changes.

Unexpected tracked divergence => RECOVERY_CONFLICT/BLOCK.

Evidence:
docs/evidence/TB-P10-T020/recovery-start.md

2. Remove Temporary Story/Banner Proxy Dependence

Expand canonical Host/section contracts so StoryRail and BannerShowcase are first-class shared SectionTypes.

Required:

no StoryRail-as-CategoryGrid semantic proxy

no BannerShowcase-as-PromoBanner semantic proxy

typed config

typed validation

shared renderer support

Admin Composer support

Draft/Published/Home compatibility

migration adapter for any temporary T019 config if needed

Evidence:
docs/evidence/TB-P10-T020/native-section-contracts.md

3. First Production Variant Wave

Implement a useful first set of real variants.

HeroCarousel

Implement:

FullWidth

Contained

Split

HeroWithSidePromos

Reuse Shopeiva Hero where possible.

StoryRail

Implement:

CircleStories

ImageCircles

RoundedCards

CategoryShowcase

Implement:

ImageCards

CompactTiles

HorizontalRail

ProductShowcase

Implement:

CardCarousel

ProductGrid

CompactRows

CategoryColumns

FeaturedPlusRail

ProductRankedList / HotProducts

Implement:

RankedHorizontal

RankedGrid

MultiColumnRank

Use only truthful data sources. If Hot/Ranked backend truth does not exist, the visual variant may exist but must only be selectable with a truthful supported source. Do NOT invent ranking data.

BannerShowcase

Implement:

Single

TwoEqual

TwoAsymmetric

FourGrid

OneLargeTwoSmall

OneLargeFourSmall

EightCompact

Mosaic2x2

BrandShowcase

Implement:

LogoRail

LogoGrid

FeaturedBrands

ReviewsShowcase

Implement:

CardCarousel

CompactQuotes

ArticleShowcase

Implement:

MagazineRail

ArticleGrid

FeaturedPlusList

Do NOT implement variants merely as aliases if they look the same.
Every selectable Variant must have a real visual/layout difference.

4. Existing Shopeiva Reuse

For each implemented Variant record:

reused current Shopeiva component

adapted current component

genuinely new implementation

Do not redesign healthy existing Home blocks without reason.

Evidence:
docs/evidence/TB-P10-T020/variant-reuse-map.md

5. Variant Capability Matrix

Every implemented Variant must declare:

allowed data sources

item-count bounds

supported size presets

autoplay support

CTA support

title/subtitle support

default surface role

responsive contract

Home allowed?

Landing allowed?

Admin controls must derive from this matrix.

Evidence:
docs/evidence/TB-P10-T020/variant-capability-matrix.md

6. Banner Settings UX

Banner configuration must be ordinary-user friendly.

For each banner item allow controlled fields only:

image/media

destination

optional title

optional short text if Variant supports it

optional CTA if Variant supports it

Layout is chosen by Variant.

Height/size:

Compact

Medium

Large

ExtraLarge where applicable

No arbitrary pixels.
No width fields.
No manual mobile layout.

For multi-banner variants, Admin should visually show required slot count, e.g.:

2 slots

4 slots

5 slots

8 slots

User should understand where each image goes.

7. Story UX

StoryRail must support simple understandable entries:

image

title

destination

enabled/order

No custom circle size or mobile count controls.

System owns:

desktop visible count

mobile horizontal behavior

gap

crop

touch behavior

8. Product Showcase UX

ProductShowcase Admin must make the layout difference visually clear.

Required:

meaningful thumbnail/mini-preview

Persian name

one-line usage explanation

Examples:

اسلایدر کارت محصول

شبکه محصولات

ردیف فشرده

ستون‌های دسته‌بندی

محصول ویژه + ردیف محصولات

No raw keys.

9. Responsive Contracts — Real Runtime

Implement registered Desktop/Tablet/Mobile behavior for all variants implemented in this task.

Hard rules:

Mobile is code-owned

ExtraLarge desktop Hero cannot become absurdly tall on mobile

multi-banner layouts collapse/stack/rail according to their contract

StoryRail always remains touch-friendly

ProductShowcase never creates unusable tiny cards

no horizontal body overflow

10. Carousel Runtime Quality

Swiper remains the one primary carousel engine.

Required:

RTL correct

touch drag

keyboard/navigation where relevant

autoplay pauses/behaves sanely after interaction

responsive slides-per-view from code-owned contract

no duplicate Swiper mount

no nested carousel conflict

no layout shift from unbounded media

11. Start From Template UX Foundation

Add Admin entry for creating a Home/Landing composition:

«شروع از صفحه خالی»

«شروع از قالب آماده»

Template picker must show:

Persian name

industry/category

short description

visual preview/miniature

number/summary of sections

Selecting a template creates a NORMAL editable Draft.

Do NOT create template-specific renderer logic.

12. Seed Initial Industry Templates

Create editable composition presets for the 10 planned industries:

پوشاک

لوازم یدکی خودرو

لوازم ساختمانی

ابزار و یراق

کاشی و سرامیک

دکوراسیون داخلی

لوازم خانگی

کفش

گل و گیاه

آرایشی بهداشتی

Each template:

uses only implemented variants

uses only truthful supported data sources

has different section order/composition appropriate to the industry

is editable after creation

has no industry-specific renderer/CSS

may use demo-safe seeded references only where valid

Do NOT pretend unsupported ranking sources exist.

Evidence:
docs/evidence/TB-P10-T020/template-seeds.md

13. Template Distinctness Gate

The 10 templates must not be the same page with different names.

Require meaningful differences in:

opening section

section order

chosen variants

density

banner usage

product presentation

brand/category emphasis

Examples of intent:

Fashion: visual/editorial + stories

AutoParts: category/brand/product density

TileCeramic: visual collections/banners

InteriorDecor: large imagery + curated products

Beauty: brand/story/product carousel

ToolsHardware: compact dense product/category composition

No custom industry engine.

14. Home Creation / Selection Contract

Admin must support:

create Draft from blank

create Draft from template

edit/reorder/remove/add

preview

publish

select published composition as Home

restore canonical Home fallback

No rebuild/redeploy.

15. Landing Creation Contract

Same Template picker may be used for Landing creation where template is Landing-compatible.

If an industry template is Home-oriented but all sections are Landing-compatible, it may also be used as Landing starting point.

No duplicated template definitions.

16. Appearance Integration

All templates and variants inherit:

PaletteKey

ThemeMode

BackgroundStyle

four global surface roles

section-context local surfaces

No per-template colors.

A template may suggest a palette in preview metadata only if useful, but MUST NOT silently override Store Appearance.

17. Admin UX Quality

Ordinary user must be able to understand the builder without technical knowledge.

Required:

Persian labels

visual previews

clear empty states

clear required slot counts for banners

no enum keys

no IDs

no JSON

no CSS

no breakpoint terms

no technical "responsive contract" wording

Use wording like:

«اندازه نمایش»

«مدل چیدمان»

«تعداد محصولات»

«انتخاب دسته‌بندی»

«انتخاب برند»

18. Runtime Proof

Host :5088 + FE :3000.

Required flow:

A. open composer
B. create blank composition
C. add Hero Split
D. add Story Circle
E. add Product CompactRows
F. add Banner FourGrid
G. add Brand LogoRail
H. preview desktop
I. inspect mobile viewport
J. publish
K. open public page
L. create composition from Fashion template
M. verify editable normal sections
N. create composition from AutoParts template
O. verify clearly different section composition
P. select one published composition as Home
Q. inspect desktop + mobile
R. restore canonical Home fallback

Persistent screenshots:

template-picker.png

template-fashion-preview.png

template-autoparts-preview.png

hero-variant-picker.png

story-variant-picker.png

product-variant-picker.png

banner-variant-picker.png

banner-slot-editor.png

composer-mixed-page.png

storefront-mixed-desktop.png

storefront-mixed-mobile.png

fashion-template-home.png

autoparts-template-home.png

Store:
docs/evidence/TB-P10-T020/screenshots/

19. Minimal Useful Tests

Required only:

native StoryRail contract validation

native BannerShowcase contract validation

every selectable implemented Variant has renderer + responsive contract

capability matrix hides/rejects unsupported controls

template uses only implemented Variants

template uses only truthful data sources

template distinctness structural guard

template creates normal editable Draft

no user mobile breakpoint/layout settings

critical-storefront

recovery-staleness + git diff --check

Do NOT create one test per variant.
Do NOT create screenshot snapshot matrices.

20. Performance / SSR

Required:

no extra Appearance fetch per section

no N+1 section metadata requests

no polling

no client-only blank first paint

no task-owned hydration mismatch

lazy/code split heavier variant UI where reasonable

Swiper only where carousel Variant is actually rendered

21. Anti-Pattern Gate

Reject:

StoryRail proxying CategoryGrid after this task

BannerShowcase proxying PromoBanner after this task

fake visual differences between variants

template-specific React pages

template-specific CSS

mobile controls exposed to user

arbitrary px settings

free-form CSS

unsupported ranking data

second carousel library

raw technical keys in Admin

Evidence:
docs/evidence/TB-P10-T020/antipattern-scan.md

22. Locks

Add non-duplicate locks after LOCK-SF-235 equivalent to:

StoryRail and BannerShowcase are native composition section contracts, not semantic proxies.

Every selectable Variant must have a real distinct renderer, responsive contract, and capability metadata.

Banner layouts use bounded Variant slot patterns and size presets; arbitrary pixel layout is forbidden.

Industry templates are distinct editable compositions built only from implemented Variants and truthful sources.

Template selection creates a normal Draft; templates do not own special renderers or CSS.

Mobile rendering remains system-owned for every Variant.

Admin exposes only plain-language controlled capabilities supported by the selected Variant.

23. Evidence

Create:
docs/evidence/TB-P10-T020/

Required:

recovery-start.md

native-section-contracts.md

variant-reuse-map.md

variant-capability-matrix.md

template-seeds.md

admin-ux.md

responsive-runtime.md

focused-validation.md

runtime.md

antipattern-scan.md

recovery-sot.md

screenshots/

24. Recovery / Pipeline

At completion:

Phase P10

Last Architect-accepted before review: TB-P10-T019

Last Implementation: TB-P10-T020

Current Issued/Repair: none

Appearance USER_VISUAL_ACCEPTED=YES

Builder USER_VISUAL_ACCEPTED=NO

Worker IDLE

branch main

HEAD==origin/main

task-owned clean

unrelated user changes preserved

do NOT invent/start TB-P10-T021

Acceptance

PASS only if ALL:

StoryRail native

BannerShowcase native

first substantial real Variant wave implemented

variants are visually distinct

existing Shopeiva components reused where sensible

Admin variant picker clear

banner slot editor understandable

mobile system-owned

10 distinct editable industry templates seeded

templates use same shared engine

no fake data sources

Home/Landing creation from blank/template works

publish/Home selection works

Appearance inheritance preserved

only essential tests added

no task-owned SSR/performance regression

anti-pattern CLEAN

locks/recovery/git exact

Worker IDLE

no TB-P10-T021

Result Contract

Return ONLY canonical BRIDGE-WAKE-V1 Result with:

Summary
Applicable-Locks
Recovery-Start
Native-Section-Contracts
Variant-Wave
Variant-Reuse
Capability-Matrix
Banner-UX
Story-UX
Product-UX
Responsive-Runtime
Carousel-Runtime
Template-Start-UX
Industry-Templates
Template-Distinctness
Home-Creation
Landing-Creation
Appearance-Inheritance
Admin-UX
Focused-Validation
Runtime
Visual-Evidence
AntiPattern-Scan
Lock-Registry
Recovery-SoT
Git
Architectural-Concerns
Visual-Concerns
Blockers

END_TOOBA_TASK