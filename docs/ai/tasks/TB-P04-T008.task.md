Tooba — TB-P04-T008 — Live Cart Integration + Shopeiva Source Reconciliation

Captured from Architect chat overlay text (not Download).

BEGIN_TOOBA_CURSOR_TASK_V1

Protocol-Version: 1
Task-ID: TB-P04-T008
Phase: P04 — Experience Foundation
Type: Presentation-First Live Commerce Slice
Repository: https://github.com/mrnikiemami-code/Tooba
Branch: main
Execution-Mode: PIPELINE

Architect Decision

TB-P04-T007 is ACCEPTED.

The repaired Storefront is visually close enough on Header, Mega Menu, Home, and PDP. Do not spend another full polish task on those surfaces unless a real blocker is discovered.

Locked commercial rule remains:

PRESERVE SHOPEIVA
→ MINIMUM CHANGE
→ CONNECT TOOBA BACKEND
→ SELL QUICKLY
→ POLISH LATER

This task connects the real Cart flow while preserving the Shopeiva visual experience.

Source-of-Truth Reconciliation — DO FIRST

There is a conflict in prior evidence.

TB-P04-T006 reported:

Next.js 16.2.6
React 19.2.4
Tailwind CSS 4

TB-P04-T007 Repair RESULT stated:

original Vue/Nuxt tree
Tailwind-ported into React/Next

These statements conflict.

Before Cart implementation:

Resolve the actual Shopeiva source directory.

Inspect package.json, lockfile, framework config, route structure, component extensions.

Record exact evidence.

Correct inaccurate prior documentation/SoT.

Continue immediately with Cart work.

Create:

docs/evidence/TB-P04-T008/shopeiva-source-reconciliation.md

Record actual framework/version/component model/direct reuse feasibility and which prior statement was inaccurate.

Do NOT block the entire task merely because prior wording was wrong.

Repository Recovery

Run:

git rev-parse --show-toplevel
git fetch origin
git branch --show-current
git rev-parse HEAD
git rev-parse origin/main
git status --short --branch

Expected predecessor:

feaa01230eb1c79adec249ffa11f569144b67b03

Require:

branch = main
HEAD == origin/main
safe/known working tree

No force push.
No destructive reset.
No silent stash.
No history rewrite.

Objective

Make the first real customer purchase interaction work:

Live Shopeiva-style PDP
→ selected/resolved Seller Offer
→ quantity
→ Add to Cart
→ real Tooba Cart backend
→ Shopeiva-style Cart page
→ update quantity
→ remove line
→ live totals

No fake/demo/local-only cart business truth.

UI Preservation Rule

Use Shopeiva Cart/PDP patterns from the runtime atlas.

Do NOT design a new Cart UI.

Preserve as much as practical:

PDP buy-box
quantity control
Add to Cart CTA
cart icon/badge
cart page layout
line item presentation
quantity actions
remove action
price summary
checkout CTA
empty-cart state
responsive behavior

Only adapt what is required for Tooba backend semantics.

Cart Business Truth

Use the existing Tooba Cart module/API.

Do not create a second cart model.

Cart line identity must preserve selected Offer/commercial identity.

Must preserve:

Product != Offer
Offer != Price

Do not infer price from Product.
Do not store fake frontend-only totals.

Add to Cart

The PDP button must become live.

Required:

resolved primary Offer
+
selected quantity
→ real Cart command/API

On success:

cart badge updates
clear success feedback
cart page reflects the line

No fake toast before backend success.

If anonymous cart is supported, use it.

If Cart requires identity, use the narrowest existing supported identity path and document it.

Do NOT build a new identity system.

Cart Page

Adapt actual Shopeiva Cart visual structure.

Required live fields:

product image/presentation seam
product title
variant seam if applicable
seller
unit price
quantity
line subtotal
availability
remove
cart subtotal
discount if real
tax/fee seam according to current truth
next-step/grand amount as supported

Do not display invented discount/tax values.

Quantity Update

Implement real backend-backed increase/decrease/update.

After mutation:

refresh from authoritative Cart response

Do not calculate durable Cart truth only in React.

Respect backend validation and inventory/cart rules.

Remove Line

Implement real remove-line mutation.

After removal:

cart refreshes
badge refreshes
empty state shown when appropriate

Use Shopeiva empty-cart presentation.

Cart Header Badge

Connect Shopeiva header/cart icon to live Cart summary.

At minimum:

line/item quantity indicator
link to Cart

Avoid unnecessary global state framework introduction.

Multi-Seller

Cart must safely preserve Seller Offer identity.

For current live seed, prove at least one:

two offers of same Product can be represented distinctly

OR:

Cart contract demonstrably uses OfferId/commercial identity
and PDP primary seller line is preserved correctly

Do not collapse Offer identity into ProductId.

Pricing

Cart display must use authoritative resolved/cart pricing.

No frontend guessing.
No Product.Price.

Document whether totals are cart-estimate or checkout-authoritative.

Promotion

Do not invent promotion values.

If no applicable promotion, show no fake discount.

Tax

Respect:

Pricing
→ Promotion
→ Tax
→ Order Snapshot

If Cart is tax-exclusive estimate and Tax is authoritative at Checkout, present accordingly.

Inventory / Errors

Rely on backend rules.

Surface clear real errors for:

insufficient availability
offer unavailable
invalid quantity
stale commercial state

Use Shopeiva visual feedback language where practical.

Cart Persistence / Session

Use existing Cart ownership/session mechanism.

Do not add localStorage as business truth.

Document anonymous/authenticated ownership, CartId handling, persistence boundary.

Checkout Boundary

This task stops at a real Cart with visible Checkout CTA.

CTA may navigate to the next Shopeiva-style checkout route/shell.

Do NOT fake successful checkout.

Next task will connect Checkout/Order.

Initial Sellable Scope

Keep:

Persian
RTL
current market/currency presentation
blue Shopeiva theme

Do not expand full multilingual/LTR/advanced currency/wallet/gift/referral here.

Public Storefront Regression

Ensure accepted routes still work:

/
/products
/products/{slug}

Do not visually redesign them.

SEO

Cart must be non-indexable.

Preserve Home/Listing/PDP SEO.

Visual Evidence — LIVE TOOBA ONLY

Store:

docs/evidence/TB-P04-T008/

Capture:

01-pdp-add-to-cart-ready-desktop.png
02-cart-with-line-desktop-rtl.png
03-cart-quantity-updated-desktop.png
04-cart-empty-desktop.png
05-cart-mobile-390x844.png
06-header-cart-badge.png
07-cart-live-error-state.png

Use real backend Cart mutations.

No fixture/demo cart data.

For error evidence, use a real invalid/over-quantity/unavailable backend response.

Live Flow Evidence

Create:

docs/evidence/TB-P04-T008/live-cart-flow.md

Record PDP product, OfferId, seller, quantity, CartId, add/update/remove responses, summary, and error response. Redact secrets.

Architecture Evidence

Create:

docs/evidence/TB-P04-T008/cart-architecture-map.md

Show:

Storefront UI
→ Host/Public Cart API
→ Cart application/module
→ Offer/Pricing/Inventory seams

Prove no cross-module SQL join, no Product.Price, no Product.Stock, no frontend domain-truth calculation.

Validation — FULL

Backend:

dotnet restore src/backend/Tooba.slnx
dotnet build src/backend/Tooba.slnx
dotnet test src/backend/Tooba.slnx

NO filters.

Require warnings=0, errors=0, failed=0, skipped=0.

Frontend:

cd src/frontend
npm ci
npm run typecheck
npm run lint
npm run test:grid
npm run test:workspace
npm run test:product-workspace

Run existing Storefront tests.
Add and run focused Cart tests.

Then:

npm run build
git diff --check
git status --short --branch

Acceptance Conditions

PASS requires:

real PDP add-to-cart works
real Cart page works
quantity update works
remove works
cart badge works
authoritative Cart backend truth
Offer identity preserved
Shopeiva Cart visual language preserved
desktop RTL works
mobile RTL works
real backend error visible
no fake totals
no Product.Price
no Product.Stock
no cross-module SQL join
full validation green

SoT

Update:

docs/PROJECT-STATE.md
docs/ROADMAP.md
docs/ai/TOOBA-RECOVERY-CONTEXT.md
docs/prompts/START-HERE-IF-CHATGPT-IS-LOST.md

Record:

TB-P04-T007 = ACCEPTED
TB-P04-T008 = AWAITING_ARCHITECT_ACCEPT
P04 = IN_PROGRESS

Also record corrected Shopeiva framework source of truth.

Keep locked:

Preserve Shopeiva
Minimal UI change
Connect Tooba backend
Sell fast
P06 core integration deadline

Save Envelope VERBATIM

Save exactly:

docs/ai/tasks/TB-P04-T008.task.md

Commit / Push

git diff --check
git status --short --branch
git add ...
git commit -m "feat connect live Shopeiva cart flow [TB-P04-T008]"
git push origin main
git fetch origin
git rev-parse HEAD
git rev-parse origin/main
git status --short --branch

Require HEAD == origin/main.

Result Contract

Return:

BEGIN_TOOBA_CURSOR_RESULT_V1

Protocol-Version: 1
Task-ID: TB-P04-T008
Phase: P04 — Experience Foundation
Status: PASS | BLOCKED | RECOVERY_CONFLICT

Summary:
...

Repository-Recovery:
- ...

Shopeiva-Source-Reconciliation:
- actual framework:
- version:
- component model:
- prior inaccurate statement:
- direct reuse feasibility:
- evidence:

Shopeiva-Cart-Reuse:
- PDP buy box:
- Cart page:
- quantity controls:
- empty state:
- header badge:
- deviations:

Live-Cart:
- CartId:
- ownership/session:
- add line:
- update line:
- remove line:
- badge:
- empty state:

Commercial-Identity:
- Product:
- Variant:
- Offer:
- Seller:
- Offer preserved in Cart:

Pricing:
- authoritative source:
- cart estimate/authoritative:
- frontend guessing:

Inventory:
- real validation:
- error tested:

Checkout-Boundary:
- CTA:
- next route:
- fake checkout:

Visual-Evidence:
- 01:
- 02:
- 03:
- 04:
- 05:
- 06:
- 07:
- Live flow evidence:
- Architecture map:

Validation:
- backend restore:
- backend build:
- warnings:
- errors:
- backend tests passed:
- failed:
- skipped:
- npm ci:
- typecheck:
- lint:
- grid tests:
- workspace tests:
- product-workspace tests:
- storefront tests:
- cart tests:
- frontend build:
- git diff --check:

Architecture:
- Product.Price:
- Product.Stock:
- cross-module SQL join:
- frontend domain guessing:

Git:
- commit:
- push:
- final HEAD:
- origin/main:
- synchronized:
- final status:

Source-of-Truth:
- TB-P04-T007: ACCEPTED
- TB-P04-T008: AWAITING_ARCHITECT_ACCEPT
- P04: IN_PROGRESS
- Shopeiva source conflict reconciled:
- Recovery-ready:

Architectural-Concerns:
- ...

Visual-Concerns:
- ...

Blockers:
- ...

END_TOOBA_CURSOR_RESULT_V1

After RESULT, surface live Cart screenshots in this SAME chat/session.

CRITICAL — RESULT DOES NOT END THIS AGENT SESSION

After RESULT:

PIPELINE_STATE: WAITING_FOR_ARCHITECT_IN_SAME_SESSION

Then:

DO NOT CLOSE OR LEAVE THIS SESSION.
WAIT HERE FOR THE NEXT AUTHORIZED ENVELOPE.

Do not finish.
Do not exit.
Do not leave PIPELINE.
Do not self-advance.
Do not invent TB-P04-T009.

RESULT = CONTROL RETURNED TO ARCHITECT.
RESULT != WORKFLOW FINISHED.

Cursor PASS is not Architect ACCEPT.

Leaving or ending the pipeline after RESULT is a protocol violation.

END_TOOBA_CURSOR_TASK_V1
