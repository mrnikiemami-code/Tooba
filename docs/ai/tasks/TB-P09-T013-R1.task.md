PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_REPAIR_TASK

Task-ID:
TB-P09-T013-R1

Parent-Task:
TB-P09-T013

Channel:
tooba-main

WorkerId:
tooba-worker-01

AgentType:
cursor

Status:
REPAIR

USER_VISUAL_ACCEPTED=NO

Title:
Quantity Foundation Migration Integrity Repair — EF History Alignment + Full Rounding Runtime Proof

Architect Review

TB-P09-T013 is functionally strong, but it is NOT yet architecturally safe to accept because the Worker reported:

Offer EF history on tooba_alpha still only InitialOffer; T013 offer row inserted manually so later full Migrate replay of T006 return-policy would collide if history is not backfilled.

This is a migration-integrity defect.

The issued T013 runtime contract also required proof that switching GlobalRoundingMode changes NEW operations between Floor and Ceiling while historical OrderLine quantity remains unchanged. The Result only explicitly proves Nearest in runtime smoke.

This repair is intentionally narrow.

Do NOT redesign Quantity.
Do NOT change UoM architecture.
Do NOT create new UI.
Do NOT alter Product/Offer ownership decisions.
Do NOT start T014.
Do NOT touch unrelated migrations.

Applicable Locked Decisions

Discovery first; no duplicate subsystem

Quantity product amounts = decimal

DB quantity storage = numeric(18,6)

Product owns Unit / DecimalPlaces / optional Step

Variant does not duplicate quantity policy in this phase

Offer owns MinimumOrderQuantity / MaximumOrderQuantity

UoM translations use dynamic DB-backed Language Registry; no fixed fa/en schema

one GlobalRoundingMode: Floor / Ceiling / Nearest

historical OrderLine quantity policy snapshots must not be reinterpreted

no module-specific rounding drift

no full redesign of Inventory/Fulfillment/Returns

1. Migration History Discovery

Inspect:

EF migration assemblies/contexts for Offer

current migration-history table in local tooba_alpha

migration sequence around T006 Offer return policy

T013 Offer min/max migration

whether any manual SQL/schema step bypassed EF migration history

fresh database migration path

existing database upgrade path

Document exact discrepancy.

Do NOT guess or simply insert arbitrary migration-history rows.

2. Repair Migration Integrity

Both paths must work:

A. Fresh database

A clean database applying migrations from zero reaches current HEAD without collision.

B. Existing tooba_alpha

Current local schema/history upgrades to HEAD without:

duplicate column/table errors

missing migration errors

destructive replay of already-applied schema

manual one-off schema dependency

Use the smallest safe fix consistent with existing repo migration conventions.

If history backfill is required:

prove schema state exactly matches the migration being marked applied

make backfill idempotent/safe

document why it is correct

No destructive reset of user/local data.

3. Offer Quantity Fields Verification

Verify Offer schema contains exactly one canonical set of:

MinimumOrderQuantity

MaximumOrderQuantity

No duplicate columns/model configuration/migration path.

Existing T006 Offer return-policy fields must remain intact.

4. Global Rounding Runtime Proof

Use weighted Product:

DecimalPlaces = 2

Step = 0.25

Prove NEW operations:

Floor:
1.37 -> 1.25

Ceiling:
1.37 -> 1.50

Nearest:
prove current canonical nearest result for at least one non-tie value.

Decimal arithmetic only.

5. Historical Snapshot Immutability Runtime Proof

Create an OrderLine under one effective quantity policy.

Then change:

GlobalRoundingMode
and/or

Product Step / DecimalPlaces where safe

Verify:

historical OrderLine Quantity does not change

historical Unit/DecimalPlaces/Step snapshot semantics remain unchanged

only new operations use the new policy

Do NOT rewrite historical OrderLine data.

6. Focused Validation — ONLY NECESSARY

Required:

fresh DB migrate-to-head proof/test

existing tooba_alpha migrate-to-head proof

Offer return-policy + min/max coexistence

Floor/Ceiling/Nearest focused tests

historical snapshot immutability test

recovery guard

git diff --check

Do NOT run unrelated full repository suites.

7. Evidence

Update:
docs/evidence/TB-P09-T013/

Add:

r1-migration-history.md

r1-fresh-db-migrate.md

r1-existing-db-migrate.md

r1-rounding-runtime.md

r1-snapshot-immutability.md

r1-recovery-sot.md

Keep concise.

8. Recovery / Pipeline

Phase: P09

Last Architect-accepted: TB-P09-T012

TB-P09-T013 under repair

Current Repair: TB-P09-T013-R1

Last Implementation on completion: TB-P09-T013-R1

USER_VISUAL_ACCEPTED=NO

Worker Next State: IDLE

do NOT invent/start TB-P09-T014

Run recovery guard.

9. Git

Required:

preserve user-authored changes

commit + push

HEAD==origin/main

no task-owned uncommitted changes

Acceptance

PASS only if ALL:

Offer migration history/schema is consistent

fresh DB migrates cleanly to HEAD

existing tooba_alpha migrates cleanly to HEAD

no destructive database reset

T006 Offer return policy and T013 Offer min/max coexist

Floor runtime proves 1.37 -> 1.25 with Step 0.25

Ceiling runtime proves 1.37 -> 1.50 with Step 0.25

Nearest has focused proof

historical OrderLine quantity/policy snapshot is not reinterpreted

no UI redesign

recovery guard passes

HEAD==origin/main

Worker IDLE

no TB-P09-T014 invented

USER_VISUAL_ACCEPTED=NO.

Result Contract

Return ONLY canonical BRIDGE-WAKE-V1 result with:

Summary
Migration-History-Discovery
Migration-Integrity-Repair
Fresh-DB-Migrate
Existing-DB-Migrate
Offer-Schema-Coexistence
Global-Rounding-Runtime
Historical-Snapshot-Immutability
Focused-Validation
Recovery-SoT
Git
Architectural-Concerns
Visual-Concerns
Blockers

END_TOOBA_REPAIR_TASK