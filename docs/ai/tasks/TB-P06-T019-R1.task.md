PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

TASK-ID: TB-P06-T019-R1
PROJECT: Tooba
PHASE: P06
CHANNEL: tooba-main
STATUS: ISSUED
TASK-TYPE: RECOVERY + SHARED STORY MANAGEMENT RESCOPE
PARENT-TASK: TB-P06-T019
WORKER-POLICY: ONE WORKER = ONE ACTIVE TASK

Title

Shared Story Management — Recover Backend WIP and Reuse One Admin/Seller Story Component System with Permission-Scoped Actions

Architect Decision

TB-P06-T019 was intentionally interrupted before Seller UI implementation.

Current repository state:

HEAD == origin/main

predecessor:
43585f8904bf31736ab15746aefcd43889c7a507

tracked tree contains uncommitted Backend-only Story review/ownership WIP

no Seller Story frontend implementation exists

storefront Shopeiva Story rail/viewer remains untouched

This replacement Task must recover the valid Backend WIP and implement the corrected UI architecture.

Corrected UI Architecture — LOCKED

DO NOT build two separate visual implementations.

Target:

Shared Story Management Component System

StoryList

StoryEditor

StoryItemEditor

StoryMediaEditor / Picker

StoryCtaEditor

StoryPreview

StoryStatus

StoryValidation

shared loading/empty/error states

Admin and Seller MUST reuse these same components as much as technically possible.

Difference is ONLY:

DATA SCOPE
+
AUTHORIZATION
+
AVAILABLE ACTIONS

Concept:

Admin mode:

all authorized Stories

Admin-origin + Seller-origin

create/edit where allowed

approve

reject

schedule

publish/activate

disable

review seller submissions

Seller mode:

own Stories only

create

edit

manage items

media

CTA

locale/allowed targeting

submit for review

see rejection reason

resubmit

Seller MUST NOT:

approve

reject

publish

activate

change seller ownership

see another seller's Stories

IMPORTANT:
UI mode is convenience only.
Backend authorization remains source of truth.

ABSOLUTE SHOPEIVA LOCK

The following are STRICTLY LOCKED and must not be changed unless required to undo an already-existing unauthorized deviation:

CSS

Tailwind behavior

JavaScript behavior

Carousel / Swiper

Autoplay

Story progress/timing

Animation

Transition

Hover / Focus / Active

Shadow / Elevation

Spacing

Typography

Responsive / Mobile

Icons / Badges

Overlay

Modal / Viewer

Component geometry

Micro-interactions

Storefront Story UI:
DO NOT TOUCH unless necessary for API compatibility.
No seller-specific storefront Story renderer.

Existing Shopeiva-derived Story UI remains canonical.

A — Recovery First

Run:

git rev-parse --show-toplevel
git fetch origin
git branch --show-current
git rev-parse HEAD
git rev-parse origin/main
git status --short --branch
git diff -- src/backend/Modules/Story src/backend/Host/Tooba.Host/Story src/backend/Host/Tooba.Host.Tests/StoryFoundationTests.cs

Expected:
HEAD == origin/main == 43585f8904bf31736ab15746aefcd43889c7a507

Review all WIP line-by-line.

Classify each change:

KEEP

REPAIR

DROP

Do NOT discard valid WIP blindly.
Do NOT commit until recovery classification is complete.

Create:
docs/evidence/TB-P06-T019-R1/01-wip-recovery-classification.md

B — Preserve Compatible Backend Direction

Retain/repair if architecturally correct:

Story Origin Admin|Seller

SellerPartyId ownership

submit-for-review

approve

reject

rejection reason

review actor/time

seller direct publish forbidden

public eligibility excludes Draft/Submitted/Rejected

seller own-data scoping

admin review operations

No request-supplied seller identity authority.

Create:
02-backend-wip-reconciliation.md

C — Story Review State Machine

Required lifecycle:

Draft
-> SubmittedForReview
-> Approved | Rejected

Rejected
-> editable
-> SubmittedForReview

Approved
-> Scheduled / Active
-> Expired / Disabled

Rules:

Seller edits Draft/Rejected only

Seller submit only

Seller cannot approve/reject/publish/activate

Admin approve/reject

reject requires reason

approval safe/idempotent

only public-eligible lifecycle reaches storefront

editing approved/active seller Story must follow safe policy; do not silently mutate live reviewed content

Document exact adopted transition policy.

Create:
03-story-review-state-machine.md

D — Authorization / Data Scope

Backend enforce:

Seller:

list own only

detail own only

mutate own only

cannot change SellerPartyId

cannot see another seller

cannot invoke admin review/publish operations

Admin:

authorized all/scope according to SpiceDB

review seller-origin Story

manage Admin-origin Story

Test direct API bypass attempts.

Create:
04-story-scope-authorization.md

E — Shared Frontend Component Architecture

Before coding UI, inspect:

current /admin/stories

current Story editor components

actual Shopeiva Vendor/Account patterns

Refactor/extract only where necessary into a shared component system.

Preferred conceptual structure, adapt to repo conventions:

story-management/
StoryManagementList
StoryEditor
StoryItemEditor
StoryMediaEditor
StoryCtaEditor
StoryPreview
StoryStatusBadge
StoryActions

Do NOT create duplicate AdminStoryEditor and SellerStoryEditor implementations if the same component can serve both.

Props/config may conceptually expose capabilities such as:

canCreate

canEdit

canSubmit

canReview

canPublish

canActivate

dataScope

But security MUST NOT depend on props.

Create:
05-shared-story-component-architecture.md

F — Reuse Existing Admin Story Management

The existing Admin Story control panel is the primary management UI baseline.

Do NOT redesign it.

Shared components should preserve the currently accepted:

list

editor

item editor

media

CTA

preview

status

spacing

form geometry

button language

responsive behavior

Admin gains seller-review capabilities inside the same coherent management surface.

Create:
06-admin-shared-story-management.md

G — Seller Story Route Using Same Components

Add Seller Story management route(s), e.g. consistent with repo routing:

/vendor-panel/stories
/vendor-panel/stories/{id}

Use SAME shared Story Management components.

Seller view differences:

data endpoint seller-scoped

only own Stories

no approve/reject

no publish/activate

Submit for Review visible where allowed

rejection reason visible

resubmit available

Do NOT fork CSS.
Do NOT copy/paste a second visual implementation.

Create:
07-seller-shared-story-management.md

H — Shared UI Capability Matrix

Create an explicit matrix:

Capability | Admin | Seller
List | yes | own only
Create | yes | yes
Edit Draft | yes | own
Edit Rejected | yes | own
Manage Items | yes | own
Media | yes | own
CTA | yes | own
Submit | optional/admin not needed | yes
Approve | yes | no
Reject | yes | no
Publish | yes | no
Schedule | yes | no final authority
Activate | yes | no
Disable | yes | own draft/archive policy only if allowed
View seller owner | yes | own implicit

Create:
08-story-capability-matrix.md

I — Seller Origin in Admin

Extend existing Admin Story list/detail, not a separate review application.

Admin should be able to:

filter origin

filter Pending Review

see seller reference/display identity

preview

approve

reject with reason

schedule/activate approved Story

No visually separate moderation shell.

Create:
09-admin-review-in-existing-panel.md

J — Storefront Story UI MUST REMAIN IDENTICAL

Public projection remains canonical.

Flow:

Admin Story OR approved Seller Story
-> same public Story API
-> same HomeStoriesSection
-> same Shopeiva viewer

There must be NO:

seller-specific viewer

seller badge unless source/product requirement explicitly calls for it

modified progress

modified timing

modified Swiper

modified animation

modified CSS

Create:
10-storefront-story-lock-proof.md

K — CTA / Media Safety

Seller Story:

own/authorized media only

safe media types

own product/offer CTA where seller ownership matters

safe approved internal target types

locale-preserving links

unsafe schemes rejected

no arbitrary HTML/JS

Admin retains broader authorized target capability only where existing policy permits.

Create:
11-story-media-cta-safety.md

L — Review Events / Audit

If review lifecycle events are in WIP, preserve them if correctly implemented through MassTransit Outbox.

Expected useful events:

story.submitted.v1

story.approved.v1

story.rejected.v1

Do not claim notification delivery.

Audit:

actor

seller

StoryId

state transition

reason when rejected

timestamp / trace context

Create:
12-story-review-events-audit.md

M — Legitimate E2E Scenario

Using APIs only, no direct DB mutation:

Seller:

create Draft

edit items/media/CTA

submit

cannot publish

cannot approve

cannot see Seller B Story

Admin:
7. sees Pending
8. rejects with reason

Seller:
9. sees rejection
10. edits
11. resubmits

Admin:
12. approves
13. schedules/activates

Storefront:
14. absent before public eligibility
15. appears after activation
16. opens in unchanged Shopeiva viewer

Create:
13-shared-story-e2e.md

N — Browser Proof

Required actual browser captures:

Admin:

Story list

seller-origin filter/pending

same Story editor

review action

Seller:

Story list

SAME editor visual structure

submit state

rejected reason

resubmit

Side-by-side Admin vs Seller:
prove shared component system rather than independent designs.

Storefront:
prove viewer unchanged.

Desktop + mobile.

Create:
14-shared-ui-browser-proof.md

O — Shared Component Reuse Proof

Provide exact file/component evidence showing reuse.

Required:

shared component paths

Admin imports

Seller imports

duplicated CSS/JS check

duplicated editor JSX check

Target:
single implementation for common Story editor behavior.

Any unavoidable role-specific wrapper must be thin.

Create:
15-component-reuse-proof.md

P — Visual Fidelity Proof

For shared management UI:

no new foreign design language

no CSS fork

no custom seller styling

same spacing/typography/radius/shadow/button/form/modal patterns

For storefront:

CSS unchanged

JS unchanged

Swiper unchanged

autoplay/timing unchanged

progress unchanged

animation/transition unchanged

responsive unchanged

Create:
16-visual-fidelity-proof.md

Q — Tests

Backend minimum:

seller creates own Draft

seller own list

foreign seller list/detail denied

seller cannot change owner

submit

seller cannot approve

seller cannot reject

seller cannot publish

admin sees pending

reject requires reason

seller sees reason

resubmit

approve

duplicate approve safe

pre-approval not public

active approved Story public

CTA ownership/safety

media ownership

locale/tenant isolation

review event/outbox if emitted

Frontend minimum:

Admin uses shared Story components

Seller uses same shared Story components

Seller publish action absent

Seller approve/reject absent

submit visible

rejection reason visible

Admin review visible

Storefront Story visual guard

RTL

LTR

Create:
17-story-shared-management-tests.md

R — Validation

Backend:

dotnet restore src/backend/Tooba.slnx
dotnet build src/backend/Tooba.slnx
dotnet test src/backend/Tooba.slnx

Require:
warnings = 0
errors = 0
failed = 0
skipped = 0

Frontend:

cd src/frontend
npm run typecheck
npm run lint
npm run test
npm run test
npm run test
npm run test
npm run build

Run:

admin Story tests

shared Story tests

i18n

composition

Story guards

All PASS.

Always:
git diff --check

Create:
18-final-validation.md

S — Final Runtime

Keep running:

Backend

Tooba Frontend

Original Shopeiva

Verify:

health/live 200

health/ready 200

Seller Stories

Admin Stories

Pending Review

/fa Story storefront

/en Story storefront

Create:
19-final-runtime.md

T — User Preview

Return exact URLs:

Seller Stories

Seller Story edit

Admin Stories

Admin Pending Review state

Persian Home

English Home

Original Shopeiva Home

Preview flow:
Seller create -> submit -> Admin reject -> Seller sees reason -> resubmit -> Admin approve -> activate -> Home.

U — Source of Truth

Update:

P06 = IN_PROGRESS
TB-P06-T018 = ACCEPTED
TB-P06-T019 = SUPERSEDED_BY_ARCHITECT_RESCOPE
TB-P06-T019-R1 = AWAITING_ARCHITECT_ACCEPT

STORY_MANAGEMENT_UI = SHARED_ADMIN_SELLER_COMPONENT_SYSTEM
STORY_SELLER_SCOPE = OWN_ONLY
STORY_SELLER_DIRECT_PUBLISH = FORBIDDEN
STORY_ADMIN_REVIEW = LIVE
STORY_STOREFRONT_UI = SHOPEIVA_EXACT

VISUAL_CONTRACT = SHOPEIVA_LOCKED
NEW_UI_RULE = SOURCE_DERIVED_NATIVE_FIT

Update:

docs/PROJECT-STATE.md

docs/ROADMAP.md

docs/ai/TOOBA-RECOVERY-CONTEXT.md

docs/prompts/START-HERE-IF-CHATGPT-IS-LOST.md

Acceptance Criteria

PASS requires ALL:

interrupted WIP recovered safely

valid backend Story review work preserved/repaired

one shared Story Management component system

Admin uses shared components

Seller uses shared components

Seller sees own data only

Seller can create/edit/items/media/CTA/submit

Seller cannot approve/reject/publish/activate

Admin reviews inside existing Admin Story management

rejection reason/resubmit works

public eligibility correct

no seller-specific storefront renderer

Storefront Story CSS/JS/Swiper/progress/timing/animation unchanged

source-derived native fit

no duplicate Seller editor design

direct API authorization tests pass

browser proof

backend 0/0/0/0

frontend all green

runtime healthy

repo synchronized

Worker may report:

SHARED_STORY_MANAGEMENT_LIVE

Worker must NOT report:

PRODUCT_FULLY_READY
USER_VISUAL_ACCEPTED

Git

After recovery + implementation:

git add ...
git commit -m "feat share Story management across admin and seller [TB-P06-T019-R1]"
git push origin main
git fetch origin
git rev-parse HEAD
git rev-parse origin/main
git status --short --branch

Require:
HEAD == origin/main
tracked tree clean

Result Contract

PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_WORKER_RESULT

Task-ID:
TB-P06-T019-R1

Channel:
tooba-main

Status:
PASS | FAIL | BLOCKED | RECOVERY_CONFLICT

Summary:
...

Recovery:

WIP reviewed:

kept:

repaired:

dropped:

predecessor:

Shared-Components:

paths:

Admin imports:

Seller imports:

duplicate editor implementation:

duplicate CSS/JS:

thin wrappers:

Capability-Matrix:

Admin:

Seller:

Backend:

origin:

seller ownership:

review lifecycle:

direct seller publish:

public eligibility:

rejection/resubmit:

review audit/events:

Seller:

list own:

create:

edit:

items:

media:

CTA:

submit:

rejection reason:

resubmit:

approve action:

publish action:

foreign data:

Admin:

same management UI:

seller-origin filter:

pending:

preview:

approve:

reject:

schedule/activate:

Storefront:

same public API:

same renderer:

CSS:

JS:

Swiper:

progress/timing:

animation:

responsive:

seller-specific renderer:

Authorization:

Seller own:

Seller foreign:

owner mutation:

seller approve:

seller publish:

Admin:

E2E:

create:

submit:

reject:

reason:

resubmit:

approve:

activate:

pre-public absent:

post-public visible:

direct DB mutation:

Visual-Fidelity:

shared Admin/Seller UI:

native fit:

CSS fork:

JS fork:

storefront deviation:

browser proof:

Validation:

backend restore:

backend build:

backend tests:

warnings:

errors:

failed:

skipped:

typecheck:

lint:

critical-storefront:

storefront:

customer:

seller:

admin/story:

shared story:

i18n:

composition:

frontend build:

git diff --check:

Runtime:

Backend:

Frontend:

Shopeiva:

health/live:

health/ready:

USER-PREVIEW:

Seller Stories:

Seller Story:

Admin Stories:

Admin Pending:

Persian Home:

English Home:

Original Shopeiva Home:

preview steps:

Source-of-Truth:

P06:

T018:

T019:

T019-R1:

Story Management UI:

Seller scope:

Seller direct publish:

Admin review:

Storefront Story:

visual contract:

Git:

commit:

push:

final HEAD:

origin/main:

synchronized:

tracked tree:

Architectural-Concerns:
...

Visual-Concerns:
...

Blockers:
...

END_TOOBA_WORKER_RESULT

After Result:

return to IDLE

do not self-issue another Task

keep Backend + Tooba Frontend + Original Shopeiva running where technically possible

END_TASK
