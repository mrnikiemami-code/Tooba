PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_TASK

Task-ID:
TB-P10-T013

Parent-Task:
TB-P10-T012

Channel:
tooba-main

WorkerId:
tooba-worker-01

AgentType:
cursor

Status:
ISSUED

USER_VISUAL_ACCEPTED=NO

Title:
Storefront Menu Management — Professional Admin UX, Menu/MenuItem Model, Home/Landing Integration

Architect Decision

TB-P10-T012 is Architect-ACCEPTED.

Accepted baseline now includes:

professional Admin Landing Page Composer

Page list/create/edit

typed section forms

controlled searchable pickers

reorder/enable-disable/delete

Draft preview

publish/unpublish lifecycle

Store HomePage selection

one canonical Storefront section renderer

demo seeded Landing pages

Palette/Theme/ProductCardSkin inheritance

LOCK-SF-001…185

This task implements the remaining Menu capability required by the locked Home/Landing architecture.

This task DOES implement:

canonical Menu / MenuItem model if not already present

professional Admin Menu management

nested menu items within a safe bounded depth

internal links to approved Storefront destinations

external links only through controlled URL validation

menu selection/reference from approved Page/Home sections where applicable

Storefront rendering through existing Header/Home/Landing integration points

clear seeded demo menu data for later user inspection

This task does NOT implement:

arbitrary HTML menus

mega-menu free-form layout builder

arbitrary scripts/styles

unlimited recursive nesting

menu-specific theme systems

TB-P10-T014

UX CONTRACT — CRITICAL

The user requires all Admin configuration UX to be:

professional

visually clear

Persian-friendly

understandable to ordinary Store Admins

free of technical IDs/enum keys

preview-oriented where useful

simple despite professional capability

A technically functional but confusing Menu editor is NOT complete.

Recovery State

Phase: P10
Last Architect-accepted: TB-P10-T012
Last Implementation: TB-P10-T012
Current Task: TB-P10-T013
Worker: tooba-worker-01
Expected branch: main
Expected previous HEAD: c6a24aa941582c0cfdf8f82bc7f8059c66ae5a8e
USER_VISUAL_ACCEPTED=NO

Preserve LOCK-SF-001…185.

1. Recovery Preflight

Record:

branch

HEAD

origin/main

git status --short

Recovery/SoT

Host :5088

FE :3000

existing menu/navigation infrastructure

unrelated local user-authored changes

Unexpected tracked divergence => RECOVERY_CONFLICT/BLOCK.

Evidence:
docs/evidence/TB-P10-T013/recovery-start.md

2. Mandatory Discovery — Existing Navigation Reality

Inspect only relevant code.

Determine:

existing Header/Mega Menu navigation structures

existing category navigation

any Menu/MenuItem entities or config

current Home navigation/menu blocks

current Admin navigation/content management patterns

link helpers/routes

locale handling

current L1/L2/L3 support

existing Shopeiva mega-menu behavior

Do NOT duplicate a canonical menu subsystem if one already exists.

Evidence:
docs/evidence/TB-P10-T013/menu-discovery.md

3. Canonical Menu Model

Create/reuse ONE Store-scoped Menu model.

Required concepts:

MenuId

Store scope

Title / human name

Stable MenuKey only if required internally

Locale if current navigation is locale-scoped

IsEnabled

CreatedAt / UpdatedAt per project conventions

MenuItem required concepts:

MenuItemId

MenuId

ParentMenuItemId nullable

Label

LinkType

target/reference

SortOrder

IsEnabled

No arbitrary HTML/CSS/JS.

4. Controlled Link Types

Support only safe, user-understandable destination types that current architecture can resolve.

Target link types:

Home

Landing Page

Product

Category

Brand if canonical public route exists

Article/content if canonical public route exists

External URL

no-link/group heading if needed for nested navigation

External URLs:

validate http/https only

no javascript/data schemes

Internal references:

Store-scoped

selected with searchable pickers

no manual IDs

Do not fake routes that do not exist.

5. Nested Menu Contract

Support professional hierarchy without unlimited complexity.

Required:

L1

L2

L3 where current Shopeiva/Header architecture supports it

Bound maximum depth explicitly.

Required:

stable MenuItemId across reorder

parent must belong to same Menu

no cycles

deterministic sibling SortOrder

disabled parent/child behavior defined safely

Do NOT implement unlimited recursion.

6. Admin Menu List

Add professional Menu workspace in canonical Store Admin area.

Required:

menu name

locale if applicable

enabled status

item count

last updated

actions

Actions:

create

edit

enable/disable

delete with safe confirmation when not referenced

show reference usage if deletion is blocked

No raw MenuId or technical keys.

7. Menu Editor UX

Menu editor must be understandable as a hierarchy.

Required:

clear tree/list

visible nesting

add top-level item

add child

edit item

enable/disable

delete

reorder

move up/down accessible fallback

drag/drop only if existing reliable pattern exists

Each item editor:

label

destination type in Persian

searchable picker when internal

external URL field only for External

preview/resolved destination summary

Do NOT show enum names or IDs.

8. Searchable Destination Pickers

Reuse current pickers where possible for:

Landing Page

Product

Category

Brand

Article

Required:

Store-scoped

search by human title/name

loading/empty/error states

no manual identifier input

Draft Landing Pages excluded from public-navigation selection unless clearly blocked from publishing

9. Landing/Home Section Integration

The locked architecture requires Pages/Sections to be able to reference approved menus.

Integrate only where current Section types logically need it.

At minimum:

allow a Menu/MenuGroup reference in the appropriate Home/Landing navigation/category-menu section if such section exists

if current registry lacks a dedicated Menu section, add one approved controlled MenuLinks / NavigationMenu SectionType only if needed and safe

Do NOT overload unrelated sections.

Menu reference must be:

same Store

enabled

locale-compatible where applicable

10. Header / Mega Menu Integration

If the Storefront Header currently has canonical hard-coded/category navigation:

preserve current fallback behavior

allow selected Store menu to drive the intended navigation slot only through a clear Store setting/reference

Do NOT replace working navigation blindly.

If a Store menu reference is unset:

existing accepted Header navigation remains unchanged

If selected:

render through one canonical menu renderer

preserve RTL

preserve L1/L2/L3 interaction quality

responsive desktop/mobile behavior

11. Canonical Menu Renderer

Use one canonical renderer/adapter for Menu tree projection.

Required:

no arbitrary component from DB

enabled items only

deterministic order

bounded nesting

safe URLs

current PaletteKey/ThemeMode inherited

no menu-local theme state

If Header and Landing need different presentation, share the same menu data/projection and use controlled presentation adapters, not duplicate menu business logic.

12. Seed / Demo Data — REQUIRED

Seed a professional demo Menu for the development Store.

It should include representative:

L1

L2

L3 if supported

Landing Page link

Category link

Product or content link where data exists

Seed via canonical development/demo seed mechanism.

Requirements:

idempotent

easy to identify

non-destructive

visible in Admin

usable in Storefront preview/runtime

This is for later user inspection.

13. Visual Quality

Admin Menu UX must be production-grade.

Required:

Persian human labels

hierarchy easy to scan

nested levels visually clear

actions not cluttered

selected destination readable

disabled state obvious

no raw technical keys

mobile/responsive reasonable

no empty placeholder boxes

Storefront:

menu visually coherent with accepted Shopeiva structure

RTL correct

desktop/mobile usable

no clipped submenu

no accidental hover traps

L3, if supported, visibly functional

USER_VISUAL_ACCEPTED remains NO.

14. Minimal Useful Tests Only

Keep tests risk-driven and small.

Essential tests only:

cross-Store menu/reference rejected

cycle/depth violation rejected

external unsafe URL rejected

reorder preserves MenuItem IDs and deterministic order

disabled item excluded from public projection

selected menu reference resolves in intended Storefront slot

unset menu reference preserves existing fallback

one nested L1→L2→L3 projection if L3 supported

recovery guard + git diff --check

Reuse existing auth/store/route tests.

Do NOT create exhaustive combinatorial MenuItem test matrices.

15. Runtime — REQUIRED

Use:

Host :5088

FE :3000

Run browser flow:

A. open Menu Admin
B. create/edit demo menu
C. add L1
D. add L2
E. add L3 if supported
F. choose Landing Page via picker
G. choose Category/Product via picker
H. reorder
I. disable one item
J. preview/inspect tree
K. assign Menu to supported Home/Landing/Header slot
L. verify Storefront desktop
M. verify Storefront mobile
N. verify RTL nesting
O. verify disabled item absent
P. unset reference -> fallback returns
Q. leave seeded demo menu available for later user inspection

Persist screenshots:

menu-list.png

menu-editor.png

menu-nested.png

menu-picker.png

menu-desktop.png

menu-mobile.png

menu-landing-section.png if applicable

Store under:
docs/evidence/TB-P10-T013/screenshots/

16. Performance / Safety

Required:

bounded depth

no recursive N+1

one menu tree projection

no polling

no repeated menu fetch per item

cache/invalidation consistent with current Store content architecture

menu writes invalidate only relevant Store/menu projection

Do not over-engineer.

17. Anti-Pattern Gate

Scan for:

raw IDs in normal Admin UI

arbitrary URL schemes

arbitrary HTML/CSS/JS

unlimited recursion

cycle-prone tree

duplicate menu business logic

page-local menu SoT

per-item API fetch

polling

hard-coded Store/menu IDs

replacing existing Header fallback when unset

free-form Mega Menu builder sneaking in

Any real occurrence => REPAIR.

Evidence:
docs/evidence/TB-P10-T013/antipattern-scan.md

18. Lock Registry

Add non-duplicate locks after LOCK-SF-185 equivalent to:

Menu/MenuItem is Store-scoped structured navigation; arbitrary HTML/CSS/JS is forbidden.

Menu destinations use approved typed link kinds and Store-scoped pickers; technical IDs are not normal-facing inputs.

Menu hierarchy has bounded canonical depth, cycle prevention, deterministic order, and stable MenuItem identity.

Disabled MenuItems are excluded from public projection without deleting editor state.

Page/Home/Header menu references must resolve only enabled same-Store compatible menus; unset references preserve accepted fallback behavior.

Header/Landing may use different controlled presentations but share one canonical Menu tree/projection and business rules.

External menu URLs allow only validated safe web schemes.

Use next canonical LOCK-SF numbers.
Do not renumber existing locks.

19. Evidence

Create:
docs/evidence/TB-P10-T013/

Required:

recovery-start.md

menu-discovery.md

menu-model.md

admin-menu-ux.md

destination-pickers.md

nesting-contract.md

storefront-integration.md

seed-demo.md

focused-validation.md

runtime.md

performance.md

antipattern-scan.md

recovery-sot.md

screenshots/

Keep evidence concise.

20. Recovery / Pipeline

At completion:

Phase P10

Last Architect-accepted before review: TB-P10-T012

Last Implementation: TB-P10-T013

Current Issued/Repair: none

USER_VISUAL_ACCEPTED=NO

Worker IDLE

branch main

ending HEAD

HEAD==origin/main

task-owned clean

unrelated local user changes preserved

do NOT invent/start TB-P10-T014

Run recovery staleness guard.

Acceptance

PASS only if ALL:

canonical Menu/MenuItem model exists/reused

professional Persian Admin Menu UX exists

no raw IDs/enum leakage

controlled safe destinations only

searchable Store-scoped pickers work

bounded nesting/cycle protection works

deterministic reorder preserves IDs

L3 works if current Header architecture supports it

disabled items excluded publicly

Menu can be referenced from intended Home/Landing/Header slot

unset reference preserves current fallback

one canonical menu tree/business projection

demo menu seeded for inspection

persistent screenshots exist

only essential tests added

no polling/N+1/unbounded recursion

anti-pattern scan CLEAN

Lock registry updated without duplicates

Recovery exact

HEAD==origin/main

Worker IDLE

no TB-P10-T014

USER_VISUAL_ACCEPTED remains NO

Result Contract

Return ONLY canonical BRIDGE-WAKE-V1 Result with:

Summary
Applicable-Locks
Recovery-Start
Menu-Discovery
Menu-Model
Admin-Menu-UX
Destination-Pickers
Nesting-Contract
Storefront-Integration
Seed-Demo
Focused-Validation
Runtime
Performance
AntiPattern-Scan
Lock-Registry
Recovery-SoT
Git
Architectural-Concerns
Visual-Concerns
Blockers

END_TOOBA_TASK