PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_TASK

Task-ID:
TB-P10-T005

Parent-Task:
TB-P10-T004-R24-R1-R4

Channel:
tooba-main

WorkerId:
tooba-worker-01

AgentType:
cursor

Status:
ISSUED

USER_VISUAL_ACCEPTED=NO

Title:
Storefront Appearance Foundation — Canonical Theme Contract, Semantic Tokens, Future Landing/Page Extension Points

Architect Decision

TB-P10-T004-R24-R1-R4 is Architect-ACCEPTED.

This task starts the next P10 capability slice.

Architecture MUST be designed for the larger commercial Tooba product:

Marketplace edition

Single-store commercial edition

one shared codebase

per-store appearance

future controlled custom themes

multiple Landing Pages

selectable Home Page

section-based Home/Landing composition

menu selection for Home sections

no per-customer rebuild/deploy for appearance/content changes

BUT this task implements ONLY the Appearance foundation and locks the agreed architecture.

Do NOT:

implement Landing/Page builder yet

implement arbitrary/custom free-form themes yet

execute HTML/CSS/JS from DB

redesign Shopeiva

start TB-P10-T006

Product Decisions — LOCK THESE
A. Theme customization

Current:

curated/preset palettes only

no arbitrary theme builder yet

no arbitrary CSS/HTML/JS from DB

Future MUST remain possible:

Store owner can later build a controlled custom theme

custom theme = validated declarative design tokens

no executable HTML/CSS/JS

no per-store frontend build/deploy

runtime performance must remain CSS-variable/cache friendly

B. Product Card appearance

Future:

one canonical Product Card behavior/component

multiple controlled visual skins

skins change presentation only

commerce behavior/pricing/actions/accessibility remain shared

Do NOT implement skins in this task.

C. Landing/Home architecture

Future MUST support:

multiple DB-backed Landing Pages per Store

one selectable Home Page

dynamic route by Store + Locale + Slug

no physical Next.js file per page

no rebuild/redeploy for adding a Page

Pages composed only from approved registered Sections

Store Admin adds/reorders/configures approved Sections

product/data sources selected through UI, never SQL/query authoring

Menu remains independent; Page/Section may reference approved Menu/MenuGroup

reserved system routes cannot be shadowed

future SEO slug history/redirects remain possible

Do NOT implement Page entities/routes/composer in this task.
Document extension contracts only.

Recovery State

Phase: P10
Last Architect-accepted: TB-P10-T004-R24-R1-R4
Last Implementation: TB-P10-T004-R24-R1-R4
Current Task: TB-P10-T005
Worker: tooba-worker-01
Expected branch: main
Expected previous HEAD: bda1041a25fa2a5cf607ed1e412a02f0a1ef2211
USER_VISUAL_ACCEPTED=NO

Preserve LOCK-SF-001…126.

1. Recovery Preflight

Record exact repo/pipeline state:

repo root

branch

HEAD

origin/main

git status --short

Recovery/SoT

Unexpected tracked divergence => RECOVERY_CONFLICT/BLOCK.

Evidence:
docs/evidence/TB-P10-T005/recovery-start.md

2. Mandatory Discovery — Existing Appearance Reality

Inspect only relevant Storefront / design-system / Store settings code.

Determine:

ThemeProvider

Tailwind dark-mode config

existing CSS variables/tokens

hard-coded brand colors

hard-coded surface/text colors

ThemeReference ownership/use

Store settings infrastructure

design-system token conventions

StorefrontProductCardView ownership/reuse

Shopeiva visual locks

any existing appearance editor/settings

deferred/abandoned theme infrastructure

Create:
docs/evidence/TB-P10-T005/appearance-discovery.md

Required table:
Area | Current source | Hard-coded? | Tokenized? | Store-configurable? | Future migration path

Do NOT duplicate an existing canonical subsystem.

3. Canonical Store Appearance Contract

Define ONE canonical Store appearance contract.

Preferred conceptual model:
StoreAppearanceSettings

Future-capable concepts:

StoreId

PaletteKey

ThemeMode

ProductCardSkin

AppearanceVersion if existing patterns require it

For THIS task:

persist only fields needed for first safe foundation

do not add speculative blobs

do not add arbitrary token JSON execution

do not create second Store settings subsystem

If existing Store settings is the correct owner, extend it.

Evidence:
docs/evidence/TB-P10-T005/appearance-architecture.md

4. Semantic Token Foundation

Establish/reuse semantic tokens equivalent to:

primary

primary-hover / primary-strong

on-primary

surface

surface-elevated

background

text

text-muted

border

System semantic colors:

danger

success

warning

must remain independent from Store brand palette.

Brand color must NOT redefine danger/success/warning semantics.

Use existing design-system conventions when possible.

5. Safe Token Migration Scope

Foundation migration only; no visual redesign.

Prioritize shared Storefront surfaces:

root/layout

Header/navigation

shared buttons/links

StorefrontProductCardView

Cart shell/shared controls

Checkout/Shipping shared chrome

account menu/shared account affordances

For Home/PDP and Shopeiva-locked surfaces:

preserve current visual output

migrate only where token substitution is visually equivalent

if unsafe, document deferment instead of changing appearance

No broad mechanical replacement.

6. Default Palette Must Preserve Current Visuals

Current/default Storefront must remain visually equivalent.

If canonical primary is currently #2563EB, default tokens must render the same effective color unless discovery proves another accepted canonical value.

Do NOT reintroduce Shopeiva red.
Do NOT recolor Storefront in this task.

7. Preset Palette Registry Foundation

Create/reuse typed registry abstraction for curated palettes.

This task may include:

canonical default palette

registry/type contract

unknown PaletteKey validation/fallback according to existing project conventions

Do NOT yet expose multiple palette choices in Admin unless an existing settings surface makes it truly trivial and scope-safe.

No arbitrary color picker.

8. Runtime Application Contract

Appearance must be applicable without:

frontend rebuild

new publish

app-pool restart

per-store CSS bundle

Preferred:
Domain/Store resolution
→ Store appearance projection
→ cached effective theme
→ SSR/root CSS variables
→ Storefront semantic tokens

Avoid:

DB query per component/card

client-side theme flash

repeated large inline styles

runtime Tailwind class construction from DB values

Document exact runtime strategy.

Evidence:
docs/evidence/TB-P10-T005/runtime-theme-strategy.md

Include:

source

cache key

invalidation

SSR application point

hydration behavior

9. SSR / Hydration / Performance

Required:

first HTML should know effective appearance where practical

no avoidable FOUC/theme flash

no layout shift due to theme resolution

appearance projection cacheable

no N+1

appearance changes do not alter commerce fetching

If current architecture blocks a safe part, document exact gap; do not hack around it.

10. Store/Tenant Isolation

Appearance is Store-scoped.

Prove:

Store A cannot apply/read Store B appearance

current domain/store resolution stays authoritative

Marketplace and Single-store use same contract

no global mutable theme state shared across stores

Focused isolation test required.

11. Future Custom Theme Contract

Document only; DO NOT implement.

Future custom theme MUST:

use validated declarative tokens

enforce contrast/readability

generate/resolve shades safely

separate brand from semantic status colors

support light/dark equivalents

remain SSR/cache compatible

need no rebuild/deploy

reject executable HTML/CSS/JS

Create:
docs/architecture/storefront-appearance-extension.md

12. Future Landing/Home/Page Contract

Document only.

Future model MUST support:

Page

Store ownership

Locale

Title

Slug

publish state

SEO metadata

TemplateKey

approved PageSection composition

SectionType registry

SortOrder

enabled/disabled

controlled config

product sources: Manual / Category / Brand / Newest / BestSelling / Featured / Discounted

independent Menu/MenuItem ownership

Page/Section reference to approved Menu/MenuGroup

Store.HomePageId or equivalent

dynamic route resolution by Store + Locale + Slug

reserved system routes

no per-page build/deploy

future slug history / 301 capability

cache/versioning extension point

Do NOT create Page tables/routes/UI.

Create:
docs/architecture/storefront-pages-extension.md

13. Future Admin Contract

Document intended future Admin surfaces, do not build placeholder UI.

Appearance:

Palette preset

Theme mode

Product card skin

Preview

Pages:

Landing Page list

create/edit

slug/SEO

section composer

preview

publish

choose Home Page

Menus:

Menu/MenuItem management

Page/Section menu reference selection

Permissions backend-authoritative.

14. Lock Registry — REQUIRED

Add non-duplicate locks, continuing after LOCK-SF-126, equivalent to:

Store appearance is declarative and Store-scoped; executable HTML/CSS/JS from DB forbidden.

Current release uses curated/preset appearance choices; future custom theme only via validated declarative tokens.

Brand tokens are separate from danger/success/warning semantics.

Appearance changes do not require per-store build/deploy.

Product Card remains one canonical behavior/component with future controlled visual skins.

Future Landing Pages are DB-backed Store+Locale pages composed only from approved registered Sections.

Adding/publishing a Landing Page does not require physical Next.js page or Storefront redeploy.

Store has one canonical selectable Home Page reference.

Page authors choose approved data sources/pickers; never SQL/arbitrary query authoring.

Menu remains independent; Pages/Sections may reference approved menus.

Dynamic Page routes cannot shadow reserved system/product/category/cart/checkout/account/admin/API routes.

Free-form page-builder HTML/CSS/JS execution is forbidden by this architecture.

Do not renumber existing locks.

15. Focused Validation

Required:

frontend typecheck

relevant lint

focused theme/token tests

default palette visual-equivalence assertions where supported

Store isolation test

invalid PaletteKey test

SSR/root theme application test if implemented

no hydration mismatch

T004-R24-R1-R4 account/cart/checkout regressions

recovery guard

git diff --check

Do NOT run unrelated full suites.

16. Lightweight Runtime Verification

Use:

Host :5088

FE :3000

Shopeiva comparison runtime if already required by current project runtime contract

Verify:

Home

PLP

PDP

Cart

Shipping

account/header

Required:

default appearance visually preserved

no theme flash

no broken colors

readable text

no danger/success regression

account/cart/checkout behavior from T004-R24-R1-R4 unchanged

USER_VISUAL_ACCEPTED remains NO.

17. Evidence

Create:
docs/evidence/TB-P10-T005/

Required:

recovery-start.md

appearance-discovery.md

appearance-architecture.md

semantic-token-mapping.md

default-visual-equivalence.md

palette-registry.md

runtime-theme-strategy.md

store-isolation.md

future-custom-theme.md

future-pages-home.md

focused-validation.md

runtime-smoke.md

antipattern-scan.md

recovery-sot.md

18. Anti-Pattern Gate

Scan for:

arbitrary CSS from DB

arbitrary HTML from DB

arbitrary JS from DB

per-store generated frontend build

per-store hard-coded theme branch

theme DB query per Product Card

runtime Tailwind class construction from DB values

brand token overriding danger/success/warning

duplicate theme providers/state

client-only theme resolution causing avoidable flash

global mutable Store appearance across tenants

nonfunctional placeholder Admin settings

Page Builder implementation sneaking into this task

Product Card Skin implementation sneaking into this task

Any introduced occurrence => REPAIR.

19. Recovery / Pipeline

At completion:

Phase P10

Last Architect-accepted before review: TB-P10-T004-R24-R1-R4

Last Implementation: TB-P10-T005

Current Issued/Repair: none

USER_VISUAL_ACCEPTED=NO

Worker IDLE

branch main

ending HEAD

HEAD==origin/main

task-owned clean

do NOT invent/start TB-P10-T006

Run recovery staleness guard.

Acceptance

PASS only if ALL:

appearance infrastructure truthfully discovered

one canonical Store appearance ownership/contract established

semantic token foundation established/reused

default Storefront remains visually equivalent

no broad visual redesign

brand tokens separated from system semantic colors

typed preset palette registry foundation exists

no arbitrary custom theme editor

per-store appearance works architecturally without rebuild/deploy

SSR/cache strategy documented/implemented appropriately

Store isolation proven

future controlled custom theme extension documented

future Landing/Home/Page/Section/Menu architecture documented

dynamic Landing requires no physical per-page frontend file/deploy by contract

Home selection extension point locked

controlled data-source selection locked

no free-form HTML/CSS/JS execution path

Lock registry updated without duplicates

T004-R24-R1-R4 behavior unchanged

focused validation green

runtime smoke green

anti-pattern scan CLEAN

recovery exact

HEAD==origin/main

Worker IDLE

no TB-P10-T006

USER_VISUAL_ACCEPTED remains NO

Result Contract

Return ONLY canonical BRIDGE-WAKE-V1 Result with:

Summary
Applicable-Locks
Recovery-Start
Appearance-Discovery
Appearance-Architecture
Semantic-Tokens
Default-Visual-Equivalence
Palette-Registry
Runtime-Theme-Strategy
Store-Isolation
Future-Custom-Theme
Future-Pages-Home
Focused-Validation
Runtime-Smoke
AntiPattern-Scan
Lock-Registry
Recovery-SoT
Git
Architectural-Concerns
Visual-Concerns
Blockers

END_TOOBA_TASK