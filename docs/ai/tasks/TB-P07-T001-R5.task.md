PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

TASK-ID: TB-P07-T001-R5
PROJECT: Tooba
PHASE: P07
CHANNEL: tooba-main
STATUS: ISSUED
TASK-TYPE: UI-UX ONLY RESCOPE
WORKER-POLICY: ONE WORKER = ONE ACTIVE TASK

Architect Decision

SUPERSEDED:

TB-P07-T001

TB-P07-T001-R1

TB-P07-T001-R2

TB-P07-T001-R3

TB-P07-T001-R4

Canonical active task:
TB-P07-T001-R5

ABSOLUTE SCOPE LOCK

UNTIL FURTHER ARCHITECT INSTRUCTION:

UI/UX ONLY.

DO NOT:

add new backend capability

add new domain model

add new database schema

add new workflow

add new commercial feature

expand Customer/Storefront/Content

redesign architecture

invent new admin/seller functionality

Only exception:
a minimal unavoidable fix required to correctly render or bind an already-existing UI capability.
If such a fix is needed, document it and keep it minimal.

Focus Order

Admin Panel — highest priority

Seller Panel — second priority

Visual Contract

Seller Panel:
EXACT SHOPEIVA VENDOR PANEL

Admin Panel:
Shopeiva-native composition from real Shopeiva components/patterns only.

No free-form redesign.

What Must Be Improved
Admin

full Persian localization

no raw English labels

no raw technical keys

professional menu/sidebar

visual hierarchy

spacing

typography

cards

forms

tabs

modals

drawers

tables/DataGrid

filters

column manager

saved-view UX

product list/detail visual quality

media/gallery presentation

variant/specification presentation

access-control usability

role/member/user-picker UX

loading/empty/error

responsive/mobile

CSS polish

JS interactions

animation

transition

hover/focus/active

micro-interactions

skeleton/loading

success/error feedback

Seller

exact Shopeiva vendor shell

exact dashboard visual structure

exact sidebar/navigation

exact cards/charts/lists/forms

exact motion/hover/transition

exact responsive behavior

Tooba data bindings only

Access Control visually integrated into Vendor language

no invented dashboard

Shopeiva Fidelity

Preserve:

DOM/component geometry where source exists

CSS/Tailwind

spacing

typography

iconography

border radius

elevation/shadow

motion timings

animation curves

hover/focus states

mobile breakpoints

chart layout

drawer/modal behavior

interaction density

DataGrid UX Repair

UI/UX only.

Required:

fully localized operator labels

typed visual controls

status dropdown populated from existing data source

applied-filter summary

clear filters

compact RTL drawer

professional column manager

drag reorder UX

resize UX

restore default

clear saved-view affordance

saved-view state visibly understandable

no giant whitespace

no primitive arrow-only reorder UI

better selection column width

clear product/order information hierarchy

Do not introduce new backend persistence mechanism in this task.
Use existing persistence if already available.
If existing persistence is broken, only fix the smallest necessary binding defect.

Access Control UX Repair

UI/UX only.

Normal UI must NOT show:

product.view

perm.*

raw UUID/GUID

raw scope IDs

raw SpiceDB tuples

Show:

localized human titles

human descriptions

user names

email/phone where already available

searchable picker using existing user data

role members

assigned roles

effective access

human-readable scope names

If backend does not already expose needed human-readable data, report blocker rather than designing a new backend subsystem in this task.

Admin Localization

Persian Admin must not show raw:

payout

fulfillment

refund

settlement

contains

greaterThanOrEqual

technical enum names

permission keys

route/module labels

English locale must remain correct.

Motion / Interaction Quality

Use meaningful, subtle Shopeiva-native polish:

sidebar open/collapse

drawer transitions

modal transitions

tabs

accordions

row hover/selection

button busy states

toasts/feedback

drag/drop feedback

skeleton loading

focus rings

reduced-motion compatibility if already supported

No decorative flashy animation.

Runtime

Start and KEEP RUNNING:

Backend :5088

Tooba Frontend :3000

Original Shopeiva :3001

If runtime is down at Result:
Status MUST NOT be PASS.

Screenshot Pack

MANDATORY.

Admin:
capture EVERY live menu route.

Seller:
capture EVERY live vendor route and matching Original Shopeiva route.

For important screens capture multiple states:

DataGrid filters

column manager

saved views

product visual states

access-control roles/users/members

mobile

Images must be accessible to Architect/user, not only referenced by local paths.

Validation

Frontend-focused validation:
cd src/frontend
npm run typecheck
npm run lint
npm run test
npm run test
npm run test
npm run test
npm run build

Run admin/datagrid/access-control/i18n suites if available.

Backend:
only run build/tests to prove no regression if backend touched or if standard gate requires it.

Always:
git diff --check

USER-PREVIEW

Return exact URLs for:

every live Admin menu

every live Seller menu

matching Original Shopeiva vendor routes

No placeholders.

Source of Truth

If successful:

P07 = IN_PROGRESS
TB-P07-T001-R5 = AWAITING_ARCHITECT_ACCEPT

CURRENT_FOCUS = UI_UX_ONLY
ADMIN_UI = PRIORITY_1
SELLER_UI = PRIORITY_2
BACKEND_FEATURE_EXPANSION = FROZEN
CUSTOMER_UI = FROZEN
STOREFRONT_UI = FROZEN
CONTENT_UI = FROZEN
SELLER_VISUAL_SOURCE = EXACT_SHOPEIVA_VENDOR_PANEL
ADMIN_VISUAL_SOURCE = SHOPEIVA_NATIVE_COMPOSITION
VISUAL_CONTRACT = SHOPEIVA_LOCKED

Acceptance Criteria

PASS requires:

UI/UX-only scope respected

no feature expansion

no new backend/domain architecture unless minimal unavoidable UI fix

Admin visually professional

Seller visually exact/native to Shopeiva Vendor

Persian localization clean

no raw technical labels

DataGrid UX visibly improved

Access Control human-readable

meaningful CSS/JS/animation polish

responsive/mobile

full screenshot pack

runtimes alive

frontend build green

repo synchronized

Worker must NOT claim:
PRODUCT_FEATURE_COMPLETE
BACKEND_FEATURE_COMPLETE
USER_VISUAL_ACCEPTED
PRODUCTION_GO_LIVE_READY

Git

git add ...
git commit -m "style polish admin seller ui ux [TB-P07-T001-R5]"
git push origin main
git fetch origin
git rev-parse HEAD
git rev-parse origin/main
git status --short --branch

Require HEAD == origin/main and tracked tree clean.

Result Contract

PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_WORKER_RESULT

Task-ID:
TB-P07-T001-R5
Channel:
tooba-main
Status:
PASS | FAIL | BLOCKED | RECOVERY_CONFLICT

Summary:
...

Scope:

UI/UX only:

backend changes:

backend justification if any:

Admin:

localization:

navigation:

dashboard:

products:

orders:

datagrid:

filters:

columns:

saved views:

access control:

product media presentation:

variants/specs presentation:

motion:

responsive:

Seller:

exact Shopeiva:

dashboard:

navigation:

products/offers:

orders:

analytics:

reviews:

tickets:

access control:

settings:

motion:

responsive:

unauthorized differences:

Visual:

CSS:

JS:

animation:

transition:

hover/focus:

skeleton/loading:

feedback:

foreign UI:

Screenshot-Pack:

delivery:

Admin routes:

Seller routes:

Original Shopeiva routes:

mobile:

multi-state captures:

Validation:

typecheck:

lint:

storefront:

customer:

seller:

admin:

datagrid:

access-control:

i18n:

frontend build:

backend regression:

git diff --check:

Runtime:

Backend:

Frontend:

Shopeiva:

health/live:

health/ready:

kept alive:

USER-PREVIEW:

Admin URLs:

Seller URLs:

Original Shopeiva URLs:

preview steps:

Git:

commit:

push:

final HEAD:

origin/main:

synchronized:

tracked tree:

Visual-Concerns:
...
Blockers:
...

END_TOOBA_WORKER_RESULT

After Result:

return to IDLE

DO NOT STOP Backend / Tooba Frontend / Original Shopeiva

do not self-issue another Task

END_TASK
