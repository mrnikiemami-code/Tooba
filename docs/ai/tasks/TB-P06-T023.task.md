PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

TASK-ID: TB-P06-T023
PROJECT: Tooba
PHASE: P06
CHANNEL: tooba-main
STATUS: ISSUED
TASK-TYPE: TRANSACTIONAL NOTIFICATIONS / COMMERCIAL OPERATIONS
WORKER-POLICY: ONE WORKER = ONE ACTIVE TASK

Title

Transactional Notifications — Real Customer/Seller Notification Inbox from Commerce Events with Exact Shopeiva UI and No Fake Realtime

Architect Context

Architect ACCEPTED:

TB-P06-T021 = ACCEPTED
TB-P06-T022 = ACCEPTED

Current state:

SELLABLE_DEMO = YES
PRODUCTION_PAYMENT_FOUNDATION_READY = YES
REAL_PSP_PROVIDER_CONFIGURATION_REQUIRED = EXTERNAL BLOCKER
PRODUCTION_GO_LIVE_READY = NO

Notifications were intentionally deferred in TB-P06-T020 because no real module existed.

This Task now closes that gap correctly.

Primary Objective

Implement a real persistent Notification capability driven by real business events.

Priority recipients:

Customer:

payment succeeded/failed where appropriate

order created/paid

fulfillment/shipment/tracking updates

return/refund lifecycle

Seller:

new paid order / actionable order event

fulfillment-relevant state changes

return/refund events affecting seller

Story submitted/reviewed where seller is recipient

other already-existing commerce events only if semantically justified

Admin notifications are NOT required unless current architecture naturally supports them.

No fake notifications.
No fake unread counts.
No fake push.
No fake realtime.

ABSOLUTE SHOPEIVA UI LOCK

If Shopeiva has notification UI, reuse it exactly.

LOCKED:

CSS

Tailwind

JavaScript behavior

animation

transition

hover/focus/active

spacing

typography

responsive/mobile

icons

badges

modal/drawer

list/card geometry

micro-interactions

DO NOT:

redesign notification cards

create a generic dashboard inbox if Shopeiva has one

change panel navigation styling

invent a new visual language

Backend binding only.

Any unauthorized visual drift:
VISUAL REGRESSION
=> Task cannot PASS.

A — Repository Recovery

Run:

git rev-parse --show-toplevel
git fetch origin
git branch --show-current
git rev-parse HEAD
git rev-parse origin/main
git status --short --branch

Expected predecessor:

852c35fcef809c911ffe3a1c4f290f0cda486fe7

Require:

main

HEAD == origin/main

safe tracked tree

If conflict:
RECOVERY_CONFLICT

B — Runtime Before Work

Start and keep running where technically possible:

PostgreSQL

Backend

Tooba Frontend

Original Shopeiva

Verify:

health/live 200

health/ready 200

Customer panel

Seller panel

Shopeiva Customer notification route if present

Shopeiva Vendor notification route if present

Evidence:

docs/evidence/TB-P06-T023/01-runtime-before-notifications.md

C — Shopeiva Source Discovery FIRST

Inspect actual Shopeiva source for:

customer notifications

vendor notifications

header notification dropdown/badge if present

unread/read visual states

notification cards/list

links/actions

empty/loading patterns

responsive/mobile behavior

animation/transition behavior

Record exact:

source files

components

CSS/Tailwind

JS

icons

badges

interaction states

Do NOT implement UI before this map exists.

Evidence:

02-shopeiva-notification-source-map.md

D — Audit Existing Events

Inspect existing integration/domain events that can legitimately create notifications.

At minimum audit:

Payment
Order
Fulfillment
Returns
Refunds
Story review
Promotion if relevant

Classify each event:

AVAILABLE
MISSING_BUT_SMALL
NOT_REQUIRED

Do not invent broad events merely to populate inbox.

Evidence:

03-commerce-event-audit.md

E — Notification Domain

Create the minimum correct Notification owner if none exists.

Minimum persistent model:

Notification

Id

TenantId

RecipientPartyId / RecipientActorId as canonical architecture requires

RecipientKind where needed

Type

Locale

TitleKey / resolved snapshot strategy

BodyKey / resolved snapshot strategy

safe structured payload

TargetRoute / safe target descriptor

IsRead

ReadAt

CreatedAt

SourceEventId / idempotency key

SourceType

severity/category only if useful

Rules:

Notification is NOT technical log

Notification is NOT business audit

Notification is NOT analytics

source event idempotency prevents duplicate notification rows

no arbitrary HTML/JS

safe route targets only

no cross-tenant leakage

Evidence:

04-notification-domain.md

F — Localization Strategy

Notifications must support fa/en without conflating Locale with Market.

Choose and document a robust strategy.

Preferred:

persist semantic notification type + safe data payload

resolve localized copy at read/render time

OR:

persist locale-specific snapshot if current architecture requires immutable historical wording

Do NOT fabricate English translations if translation catalog does not support them.

Do NOT store unsafe raw formatted HTML.

Evidence:

05-notification-localization.md

G — Event Consumption

Consume real commerce events through required messaging architecture:

MassTransit
PostgreSQL SQL / Database Transport
Outbox/inbox/idempotency

NO RabbitMQ.

At minimum implement available relevant consumers for:

payment.succeeded

payment.failed when customer-facing and meaningful

order state changes

fulfillment/shipment/tracking

return lifecycle

refund lifecycle

story.approved/rejected if event exists

Where an event does not exist but a notification is commercially important:
add the smallest correct integration event in the owning module after commit.

No direct cross-module table reads.

Evidence:

06-notification-event-consumers.md

H — Recipient Resolution

Resolve recipients correctly.

Customer notification:

buyer/customer own event only

Seller notification:

seller(s) owning affected Order lines/Offer(s)

no exposure of another seller's private data

Story:

seller owner only

Do not trust recipient ids from public request payloads.

Use canonical identity/party boundaries.

Evidence:

07-recipient-resolution.md

I — Customer Notification API

Implement:

GET /v1/customer/notifications
GET /v1/customer/notifications/unread-count
POST/PATCH mark one read
POST/PATCH mark all read if appropriate

Required:

own only

pagination

newest first

locale-safe response

safe target

idempotent mark read

No mutation of foreign notifications.

Evidence:

08-customer-notification-api.md

J — Seller Notification API

Implement:

GET /v1/seller/notifications
GET /v1/seller/notifications/unread-count
mark one read
mark all read if appropriate

Required:

current seller only

tenant/seller isolation

safe target

real unread count

Evidence:

09-seller-notification-api.md

K — Customer UI LIVE

If Shopeiva has exact Customer notification route/component:

Port/reuse it exactly.

Route consistent with existing panel architecture.

Required:

real rows

unread/read state

mark read

unread count

safe deep link

empty/loading/error

pagination/load-more if source pattern supports it

No fake rows.

If header badge exists and can be wired without UI drift:
wire real unread count.

If not:
do not invent one.

Evidence:

10-customer-notification-ui.md

L — Seller UI LIVE

Same rule:

Use exact Shopeiva Vendor notification UI if present.

Required:

own real rows

unread/read

safe deep links

mark read

real count

empty/loading/error

No fake notifications.

Evidence:

11-seller-notification-ui.md

M — Navigation

After Notification UI is genuinely live:

restore/add Customer and Seller nav entries using existing exact Shopeiva navigation pattern.

Before backend + UI are live:
do not expose.

No dead links.

Evidence:

12-navigation-integrity.md

N — Deep-Link Safety

Notification targets must be typed/safe.

Examples:

Customer:

own Order

own Return

payment result/order

shipment tracking

Seller:

own Order

own Return

own Story review

own product/offer where relevant

Reject:

javascript:

arbitrary HTML

unauthorized cross-seller/customer ids

unsafe external redirect

Locale-preserve public-facing links.

Evidence:

13-notification-target-safety.md

O — Read/Unread Semantics

Prove:

new notification unread

opening list does NOT automatically mark all read unless Shopeiva source explicitly does

mark one read is idempotent

mark all read is idempotent

unread count accurate

duplicate event does not create duplicate unread row

Evidence:

14-read-unread-semantics.md

P — Realtime Decision

Do NOT add fake realtime.

Audit existing SignalR/realtime infrastructure.

If a canonical, already-used realtime path exists and Notification can safely publish through it with small scope:
optional live refresh is allowed.

Otherwise:
use explicit refresh/poll-on-navigation only if existing UI requires.

Report:

REALTIME_NOTIFICATIONS = LIVE | DEFERRED

Realtime is NOT required for PASS.

Evidence:

15-realtime-decision.md

Q — Observability / Audit

Record safe operational telemetry:

Notification created

type

recipient kind

source event type

duplicate suppressed

read transition

Do not log:

private message content unnecessarily

secrets

raw sensitive payloads

Notification read is not a substitute for domain audit.

Evidence:

16-notification-observability.md

R — Legitimate E2E Scenarios

No direct DB mutation.

Scenario 1 — Customer:

perform existing real sandbox purchase

payment succeeds

Order becomes Paid

customer receives real notification

unread count increments

customer opens notification

safe Order target

mark read

unread decrements

Scenario 2 — Seller:

same paid Order

owning seller receives actionable Order notification

foreign seller receives none

seller follows safe link to own Order

mark read

Scenario 3 — Fulfillment:

Seller updates shipment/tracking

Customer receives shipment/status notification

Scenario 4 — Story:

Seller Story rejected or approved

seller receives review result notification if event wiring is available/implemented

Scenario 5 — Duplicate:

same integration event delivered twice

one notification only

Evidence:

17-notification-e2e.md

S — Browser Proof

Capture:

Customer:

notification list

unread state

read state

deep-link

mobile

Seller:

notification list

unread state

deep-link

mobile

If header badge wired:
capture before/after read.

Original Shopeiva:
side-by-side comparable notification route/component.

Evidence:

18-browser-proof.md

T — Visual Fidelity Audit

For touched Notification UI prove:

same source component geometry

CSS preserved

JS interaction preserved

animations preserved

transitions preserved

hover/focus preserved

badges preserved

icons preserved

responsive/mobile preserved

no foreign component design

Evidence:

19-visual-fidelity.md

U — Authorization / Isolation Tests

Minimum:

customer own notification list

customer foreign notification denied

customer cannot mark foreign read

seller own notification list

seller foreign notification denied

seller cannot mark foreign read

seller A order event does not notify seller B

tenant isolation

safe target ownership

admin access only if explicitly implemented

Evidence:

20-notification-authorization.md

V — Integration Tests

Minimum:

payment succeeded -> customer notification

paid Order -> seller notification

fulfillment update -> customer notification

duplicate event suppressed

unread count correct

mark read idempotent

mark all read idempotent if implemented

locale response

story review notification if wired

MassTransit consumer/outbox/inbox semantics

Evidence:

21-notification-tests.md

W — Validation

Backend:

dotnet restore src/backend/Tooba.slnx
dotnet build src/backend/Tooba.slnx
dotnet test src/backend/Tooba.slnx

Require:

warnings = 0
errors = 0
failed = 0
skipped = 0

Frontend:

cd src/frontend
npm run typecheck
npm run lint
npm run test
npm run test
npm run test
npm run test
npm run build

Also run:

notifications

order

payment

fulfillment

returns/refunds

story

i18n

navigation guards

Always:

git diff --check

Evidence:

22-final-validation.md

X — Runtime

Keep running:

Backend

Frontend

Original Shopeiva

Verify:

health/live 200

health/ready 200

Customer Notifications

Seller Notifications

Customer Order

Seller Order

Story review if included

Evidence:

23-final-runtime.md

Y — User Preview

Return exact URLs:

Customer Notifications

Seller Notifications

Customer Order target

Seller Order target

Seller Story target if wired

Persian Home

Original Shopeiva comparable notification route

Provide preview steps:

purchase
-> Customer/Seller notifications
-> read/deep-link
-> fulfillment
-> Customer notification
-> Story review notification if wired

Z — Readiness Recheck

Update honestly:

Storefront %

Customer %

Seller %

Admin %

Blog %

Story %

Product sale readiness %

Marketplace sale readiness %

Notifications readiness %

SELLABLE_DEMO

production blockers

remaining commercial gaps

Do not claim real push/realtime if not implemented.

Evidence:

24-commercial-readiness.md

AA — Source of Truth

Update:

P06 = IN_PROGRESS
TB-P06-T022 = ACCEPTED
TB-P06-T023 = AWAITING_ARCHITECT_ACCEPT

NOTIFICATION_BACKEND = LIVE
CUSTOMER_NOTIFICATION_UI = LIVE
SELLER_NOTIFICATION_UI = LIVE
NOTIFICATION_UNREAD = LIVE
NOTIFICATION_DEEP_LINKS = LIVE
REALTIME_NOTIFICATIONS = LIVE | DEFERRED
FAKE_NOTIFICATIONS = FORBIDDEN

PAYMENT_PRODUCTION_FOUNDATION = LIVE
REAL_PSP_PROVIDER_CONFIGURATION_REQUIRED = YES

VISUAL_CONTRACT = SHOPEIVA_LOCKED

Update:

docs/PROJECT-STATE.md

docs/ROADMAP.md

docs/ai/TOOBA-RECOVERY-CONTEXT.md

docs/prompts/START-HERE-IF-CHATGPT-IS-LOST.md

Acceptance Criteria

PASS requires ALL:

real persistent Notification module

no fake inbox/unread

real commerce events drive notifications

MassTransit PostgreSQL transport

duplicate suppression

correct customer recipient

correct seller recipient

cross-seller isolation

customer APIs

seller APIs

real unread/read

safe deep links

Customer UI uses Shopeiva exact pattern where present

Seller UI uses Shopeiva exact pattern where present

nav entries only after live

no dead links

no fake push/realtime

real E2E notification proof

storefront/panel visual lock preserved

browser proof

backend 0/0/0/0

frontend green

runtimes healthy

repo synchronized

Worker may report:

TRANSACTIONAL_NOTIFICATIONS_LIVE

Worker must NOT report:

REALTIME_NOTIFICATIONS_LIVE
unless actually proven.

Worker must NOT report:

PRODUCTION_GO_LIVE_READY
USER_VISUAL_ACCEPTED
PRODUCT_FULLY_READY

Git

git add ...
git commit -m "feat add transactional notifications [TB-P06-T023]"
git push origin main
git fetch origin
git rev-parse HEAD
git rev-parse origin/main
git status --short --branch

Require:

HEAD == origin/main
tracked tree clean

Result Contract

PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_WORKER_RESULT

Task-ID:
TB-P06-T023

Channel:
tooba-main

Status:
PASS | FAIL | BLOCKED | RECOVERY_CONFLICT

Summary:
...

Shopeiva-Source:

Customer:

Seller:

Header badge:

CSS/JS:

responsive:

Event-Audit:

Payment:

Order:

Fulfillment:

Returns:

Refunds:

Story:

Notification-Domain:

owner:

recipient:

locale:

payload:

target:

idempotency:

source event:

Messaging:

MassTransit:

PostgreSQL transport:

consumer:

outbox/inbox:

duplicate suppression:

RabbitMQ:

Customer:

API:

list:

unread count:

mark read:

mark all:

UI:

deep-link:

foreign access:

Seller:

API:

list:

unread count:

mark read:

mark all:

UI:

deep-link:

foreign access:

Realtime:

decision:

implementation:

fake realtime:

E2E:

purchase:

customer notification:

seller notification:

unread:

read:

order deep-link:

fulfillment:

customer shipment notification:

story review:

duplicate delivery:

direct DB mutation:

Navigation:

Customer:

Seller:

dead links:

Multilingual:

fa:

en:

RTL:

LTR:

locale/market separation:

Visual-Fidelity:

Customer CSS:

Seller CSS:

JS:

animation:

transition:

hover/focus:

badges/icons:

responsive:

Shopeiva comparison:

unauthorized deviation:

Authorization:

customer own:

customer foreign:

seller own:

seller foreign:

cross-seller event:

tenant:

deep-link ownership:

Validation:

backend restore:

backend build:

backend tests:

warnings:

errors:

failed:

skipped:

notification tests:

payment/order:

fulfillment:

story:

typecheck:

lint:

storefront:

customer:

seller:

i18n:

frontend build:

git diff --check:

Runtime:

Backend:

Frontend:

Shopeiva:

health/live:

health/ready:

USER-PREVIEW:

Customer Notifications:

Seller Notifications:

Customer Order:

Seller Order:

Story:

Original Shopeiva:

preview steps:

Readiness-After:

Storefront %:

Customer %:

Seller %:

Admin %:

Blog %:

Story %:

Product sale readiness %:

Marketplace sale readiness %:

Notifications %:

SELLABLE_DEMO:

production blockers:

remaining gaps:

Source-of-Truth:

P06:

T022:

T023:

Notification backend:

Customer UI:

Seller UI:

unread:

realtime:

fake notifications:

visual contract:

Git:

commit:

push:

final HEAD:

origin/main:

synchronized:

tracked tree:

Architectural-Concerns:
...

Visual-Concerns:
...

Blockers:
...

END_TOOBA_WORKER_RESULT

After Result:

return to IDLE

do not self-issue another Task

keep Backend + Tooba Frontend + Original Shopeiva running where technically possible

END_TASK
