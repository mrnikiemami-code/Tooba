Tooba — TB-P03-T003 — Pricing Foundation

BEGIN_TOOBA_CURSOR_TASK_V1

Protocol-Version: 1
Task-ID: TB-P03-T003
Phase: P03 — Commerce Core
Type: Implementation / Pricing Foundation
Repository: https://github.com/mrnikiemami-code/Tooba
Branch: main
Execution-Mode: PIPELINE
Depends-On: TB-P03-T002
Architect-Decision-On-Dependency: ACCEPTED

Objective

Implement the minimum production-grade Pricing foundation without collapsing commercial dimensions.

Hard rules:

Product != Price
Offer != Price
Price != Inventory
Locale != Market
Market != Currency
Currency != Tax Jurisdiction

Pricing must be capable of evolving toward:

Offer
Market
SalesChannel
Currency
Customer / Organization
Quantity
Contract
Promotion
Tax/Fee context
Validity

Do NOT implement final promotions, contracts, B2B pricing UI, tax calculation, cart, order, or payment.

Repository Recovery

Run:

git rev-parse --show-toplevel
git fetch origin
git branch --show-current
git rev-parse HEAD
git rev-parse origin/main
git status --short --branch

Expected predecessor:

12e207089dbdaba97affdc7f954a3864679f392f

Require synchronized safe main.

Unsafe/ambiguous state => RECOVERY_CONFLICT.

No force push, history rewrite, destructive reset, silent stash, or unrelated work.

SoT Transition

Set:

TB-P03-T002 = ACCEPTED
TB-P03-T003 = ISSUED / AWAITING_ARCHITECT_ACCEPT
P03 = IN_PROGRESS
Read First

Read:

AGENTS.md
docs/architecture/08-pricing-market-currency.md
docs/architecture/07-catalog-product-variant-offer-seller.md
docs/architecture/43-seller-offer-listing-foundation.md
docs/architecture/32-persian-code-documentation-standard.md
docs/architecture/36-module-composition-boundary-enforcement.md
docs/PROJECT-STATE.md
docs/ROADMAP.md
docs/ai/TOOBA-RECOVERY-CONTEXT.md
Pricing Module

Create dedicated:

Pricing

with repository module conventions:

Domain
Application
Infrastructure
Contracts

Pricing owns:

PricingDbContext
pricing schema
migrations
write model

No cross-module FK.
No OfferDbContext/CatalogDbContext access.
No foreign repositories.

Money

Use explicit Money/value representation.

At minimum:

Amount
Currency

Preserve existing IRR/Toman architectural decisions.

Do not use floating point for monetary amounts.

Use decimal/integer minor-unit strategy consistent with existing ADRs.

Document rounding semantics.

Do not silently mix IRR and Toman.

Price Record

Implement a pricing record/rule foundation linked by opaque commercial reference.

Primary target should be:

OfferId

unless repository SoT explicitly supports another pricing target abstraction.

At minimum preserve:

PriceId
OfferId
MarketId / MarketCode
SalesChannel
Currency
Amount
ValidFrom
ValidTo optional
Status
CreatedAt
UpdatedAt

Do NOT add price fields back onto Offer or Product.

Market

Model Market explicitly.

Market is not Locale.

Examples conceptually:

IR
UK

Do not hard-code only Iran into schema/domain.

Market may influence:

currency availability
tax jurisdiction selection
catalog availability
pricing rules

but this task only establishes pricing-side market identity/seam.

Currency

Currency must be explicit and validated.

Do not infer Currency from Locale.

Do not infer Currency solely from Market in a way that prevents multiple currencies later.

Support ISO-style code/value object.

Sales Channel

Reuse the canonical SalesChannel concept established by Offer architecture.

Do not create a divergent pricing-only string list.

Pricing must be able to differentiate:

Direct
Marketplace
Agency
Corporate
Affiliate
Api

without implementing B2B workflows.

Validity

Pricing must support effective dating:

ValidFrom
ValidTo optional

Use canonical UTC/NodaTime semantics.

No Jalali persistence.

Prevent invalid windows:

ValidTo <= ValidFrom

where applicable.

Price Selection Contract

Create stable query/evaluation contract.

Conceptually:

ResolvePriceAsync(PricingContext)

Context must be extensible for future dimensions:

OfferId
Market
SalesChannel
Currency
Customer/Party optional
Organization optional
Quantity optional
Contract optional
Promotion context optional
Tax context optional
At/Instant

Do not require all future dimensions in DB today.

Do not expose EF entities.

Current Selection Precedence

For this foundation, implement only a deterministic minimal selection rule.

Example acceptable rule:

active
matching Offer
matching Market
matching SalesChannel
matching Currency
valid at requested Instant

If more than one equally applicable base price exists, reject ambiguity or enforce DB uniqueness.

Do not invent promotion stacking.

Customer / Organization Pricing Seam

Do NOT implement full B2B pricing.

But architecture must not block future:

customer-specific price
organization-specific price
quantity tier
contract price

Do not encode a schema that can only ever have one global price per Offer.

Provide an extensible qualifier/price-book/rule seam if appropriate.

Keep current implementation bounded.

Authored vs FX-Derived Price

Hard distinction:

Authored Price != FX-derived display price

Store authored commercial prices as source truth.

Do not silently overwrite/store converted price as authored price.

FX service is out of scope.

Document future derived-price projection/evaluation seam.

Tax Boundary

Pricing base price is:

Tax Exclusive

per locked architecture.

Do not calculate VAT/tax in this task.

Do not include tax amount as part of stored base price.

Preserve seam for later tax calculation context.

Offer Validation

Pricing references Offer via stable contract/gateway.

Use an Offer lookup contract if validation is needed.

Do not query OfferDbContext.

No DB FK to Offer schema.

Overlap / Uniqueness

Prevent ambiguous active base prices for the same currently-supported selector dimensions.

Use persistence constraints or transaction-safe validation where practical.

Tests must cover overlapping/duplicate ambiguity.

Do not rely solely on in-memory list checks.

Publication / Availability

A resolved price alone does not imply purchasability.

Hard rule:

Price exists != Offer active != Inventory available != Catalog published

Do not implement final purchasability engine here.

Tenant / Edition

Marketplace:

Pricing data in marketplace database

SingleStore:

Pricing data in tenant database

Use existing tenant-aware persistence.

No Host parsing inside Pricing module.

Tenant A price must not resolve in Tenant B.

Authorization

No final pricing admin permission matrix.

Use existing authorization guard seam where needed.

No Authzed.Net types in Pricing Domain/Application.

Events

Use existing event/outbox foundation.

Potential events:

pricing.price_created.v1
pricing.price_changed.v1
pricing.price_activated.v1
pricing.price_expired.v1

Do not leak internal EF entities or MassTransit types.

Tests — Mandatory

Add meaningful tests covering at minimum:

Product has no Price field
Offer has no Price/Money field
Pricing owns Money/Amount
Currency explicit
Locale not used as Currency
Market distinct from Currency
SalesChannel reused canonically
invalid validity window rejected
price resolves only for matching Offer/Market/Channel/Currency/time
expired/future price does not resolve
ambiguous overlapping active base prices prevented/rejected
authored price remains distinct from FX-derived concept
base price is tax-exclusive
Tenant A price not visible/resolvable in Tenant B
Pricing owns DbContext/schema
no cross-module FK
no foreign DbContext dependency
no MassTransit/Authzed SDK in Domain/Application

Use PostgreSQL Testcontainers where persistence/constraints matter.

Persian Documentation — MANDATORY

Every new/changed Tooba-owned:

Class
Interface
Method
Function
Property
Constructor
Record
Struct
Enum
important internal member

must have strong meaningful Persian documentation.

Comments must explain:

Money semantics
IRR/Toman distinction
Market vs Locale/Currency
selection dimensions
validity
tax-exclusive base price
authored vs FX-derived price
future B2B qualifiers
tenant isolation

Weak/name-echo comments = acceptance failure.

CS1591 must remain green.

Documentation

Create:

docs/architecture/44-pricing-foundation.md

Document:

Money model
Offer pricing target
Market
Currency
SalesChannel
validity
selection contract
ambiguity/overlap policy
future customer/org/quantity/contract dimensions
authored vs FX-derived prices
tax-exclusive boundary
tenant/edition behavior

State explicitly:

Product.Price is forbidden
Offer.Price is forbidden
Locale != Market != Currency != Tax Jurisdiction
SoT Updates

Update:

docs/PROJECT-STATE.md
docs/ROADMAP.md
docs/ai/TOOBA-RECOVERY-CONTEXT.md

Expected:

Last Architect Accepted Task: TB-P03-T002
Current Issued Task: TB-P03-T003
Current Phase: P03 — Commerce Core
Task State: AWAITING_ARCHITECT_ACCEPT
Pricing Foundation: IN_PROGRESS

Preserve all deferred concerns and mandatory future UX sequence.

Save Envelope VERBATIM

Save exactly:

docs/ai/tasks/TB-P03-T003.task.md

Do not summarize or condense.

Out of Scope

Do NOT implement:

Tax calculation
Promotions
Coupons
FX service
price conversion engine
full B2B pricing
contracts
quantity-tier UI
Inventory
Cart
Order
Payment
Buy Box
Shopeiva
Data Grid
Design System
commercial UI

Do not issue T004 or P03 Gate.

Validation

Run current:

dotnet restore
dotnet build
dotnet test

Frontend:

cd src/frontend
npm ci
npm run typecheck
npm run lint
npm run build

Then:

git diff --check
git status --short --branch

Manual checks:

no Product.Price
no Offer.Price
explicit Market/Currency/SalesChannel
tax-exclusive base price
effective dating
ambiguity prevented
tenant isolation
strong Persian docs
Git

Suggested commit:

feat add Tooba pricing foundation [TB-P03-T003]

Push origin/main, fetch, require:

HEAD == origin/main

No force push.

Result Contract

Return:

BEGIN_TOOBA_CURSOR_RESULT_V1

Protocol-Version: 1
Task-ID: TB-P03-T003
Phase: P03 — Commerce Core
Status: PASS | BLOCKED | RECOVERY_CONFLICT

Summary:
...

Repository-Recovery:
- ...

Pricing-Module:
- ...

Money:
- ...

Price-Record:
- ...

Market-Currency:
- ...

Sales-Channel:
- ...

Validity:
- ...

Price-Selection:
- ...

Future-B2B-Seams:
- ...

Tax-FX-Boundaries:
- ...

Tenant-Isolation:
- ...

Persian-Documentation:
- ...

Tests:
- ...

Validation:
- backend restore:
- backend build:
- backend tests:
- postgres integration tests:
- frontend install:
- frontend typecheck:
- frontend lint:
- frontend build:
- git diff --check:

Git:
- Commit:
- Push:
- Final-HEAD:
- Final-Origin-Main:
- Final-Status:
- Head-Matches-Origin:

Source-of-Truth:
- Last-Architect-Accepted-Task:
- Current-Issued-Task:
- Current-Phase:
- Task-State:
- Recovery-Ready:

Architectural-Concerns:
- ...

Blockers:
- ...

END_TOOBA_CURSOR_RESULT_V1
CRITICAL — DO NOT LEAVE PIPELINE

After sending RESULT:

WAIT HERE for the USER / Architect to provide the next valid task
in this SAME chat/session.

You MUST remain inside the Tooba Architect-controlled pipeline.

Do NOT:

close this chat/session
end the agent workflow
leave PIPELINE mode
treat RESULT as the end of the work
move to another chat
wait outside this pipeline
invent the next task
infer the next task
prepare the next task
execute TB-P03-T004

After RESULT, stay active in this SAME chat/session and wait here until the USER / Architect sends the next valid Envelope.

Only when a new valid Envelope is provided in this SAME chat/session may you execute the next task.

Cursor PASS is not Architect ACCEPT.

Leaving or ending the pipeline after RESULT is a protocol violation.

END_TOOBA_CURSOR_TASK_V1
