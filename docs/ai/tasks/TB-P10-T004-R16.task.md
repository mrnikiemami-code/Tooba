PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_REPAIR_TASK

Task-ID:
TB-P10-T004-R16

Parent-Task:
TB-P10-T004-R15

Channel:
tooba-main

WorkerId:
tooba-worker-01

AgentType:
cursor

Status:
REPAIR

USER_VISUAL_ACCEPTED=NO

Title:
Customer Pending-Payment UX — Countdown, Reservation Cycle Visibility, Retry After Expiry

Architect Review

TB-P10-T004-R15 is ACCEPTED.

Accepted foundation:

Reservation Cycle is distinct from Payment Attempt

Cycle #1 starts at committed Order/payment reserve

failed attempts inside an Active cycle never extend/reset ExpiresAt

new cycle starts only after previous cycle ended + authoritative reacquire succeeds

failed reacquire is audit-visible without consuming CycleNumber

MaxReservationCycles includes initial cycle

Offer > Category > Store > Platform policy precedence

multi-line uses strictest/minimum effective policy

Manual Review uses same-cycle transition; reacquire after expiry creates a new cycle

runtime A–J PASS

focused tests green

anti-pattern scan CLEAN

HEAD==origin/main

Worker IDLE

This task wires the customer-facing pending-payment UX on top of the already-locked R15 model.

Do NOT change reservation lifecycle semantics.
Do NOT make frontend authoritative for expiry.
Do NOT reset timers on retry.
Do NOT create duplicate Orders.
Do NOT redesign the whole Cart page.
Do NOT create TB-P10-T005.

Recovery State

Phase: P10
Last Architect-accepted: TB-P10-T004-R15
Last Implementation: TB-P10-T004-R15
Current Repair: TB-P10-T004-R16
Worker: tooba-worker-01
Expected branch: main
Expected previous HEAD: fd8b75f005a8cea314bad9d7c7eb82140bfaf6a0
USER_VISUAL_ACCEPTED=NO

Applicable Locks

Read:

docs/architecture/TOOBA-LOCKS.md

docs/architecture/STOREFRONT-CHECKOUT-PLAN.md

Preserve:

LOCK-SF-001…084

Cart does not hard-reserve

Order commit starts reservation

server ExpiresAt authoritative

retry inside active cycle does not extend timer

new cycle only after expired/released + reacquire

same Order across retries

payment success finalizes shopping Cart/current purchase

Payment Succeeded terminal for new initiation

committed Order proof independent from Active Cart

no raw reservation enum/error UX

1. Recovery Preflight

Record:

Phase

accepted/implementation/current task

Worker

branch

HEAD

origin/main

HEAD==origin/main

tracked dirty state

task-owned untracked state

Unexpected tracked divergence => BLOCK.

Evidence:
docs/evidence/TB-P10-T004/r16-recovery-start.md

2. Mandatory Discovery

Inspect current Shopeiva Cart page and current Storefront flows:

/cart

header mini-cart

current empty-cart view

current pending Order/payment data source

customer guest/auth ownership

order retry action

payment page navigation

current Order reservation-cycle projection from R15

current Cart finalization after Payment success

R13 fresh Active Cart behavior after commit

any existing pending-payment card/section

responsive layout in Shopeiva reference/current implementation

Create:
docs/evidence/TB-P10-T004/r16-customer-pending-payment-discovery.md

Document exactly where the new block fits without redesigning Shopeiva structure.

3. Customer UX Contract

Add a customer-facing section on Cart page equivalent to:

در انتظار پرداخت

This section is separate from the current Active Cart contents.

It represents committed, unpaid Orders that are still eligible for payment/retry.

Each pending-payment card should show only useful business information:

Order human reference

item thumbnails / compact item summary

payable amount

current payment state

reservation countdown when current cycle is Active

reservation cycle number only if useful in UX; do not expose technical wording unless justified

primary action: پرداخت

after expiry: بررسی موجودی و پرداخت مجدد

Do NOT expose:

Reservation IDs

Cycle internal enum names

raw ExpiresAt

raw payment/inventory codes

4. Cart Page Coexistence

Required behavior:

Before first Order commit

Cart works normally.

After Order commit, before Payment success

source Cart has been converted according to current lifecycle

Active Cart may be empty/new

committed pending Order appears under در انتظار پرداخت

customer can continue shopping in a new Active Cart

pending Order remains independently payable

After Payment success

pending-payment card disappears from unpaid/pending list

normal paid Order remains accessible from customer Orders

current Active Cart remains independent

This must preserve R13 separation between Active Cart and committed Order proof.

5. Countdown UX

When current Reservation Cycle is Active and has ExpiresAt:

Display a reverse countdown like:
09:30

Copy equivalent:
موجودی این سفارش تا پایان این زمان برای شما نگه داشته می‌شود.

Critical rules:

timer is presentation only

server ExpiresAt + server time is source of truth

no frontend mutation/release

no frontend timer reset on failed payment

no polling loop to keep reservation alive

countdown reaches 00:00 visually and triggers one normal state refresh/revalidation, not a rapid poll

Implementation may use a local display timer derived from server timestamps.
That timer MUST NOT decide business state.

6. Failed Payment Inside Active Cycle

If payment attempt fails while cycle is still Active:

pending card remains

same countdown continues from original ExpiresAt

CTA remains پرداخت / retry

retry uses same Order and same active Reservation Cycle

timer does not reset

no new Cycle created

Customer-safe copy may indicate:
پرداخت ناموفق بود؛ تا پایان مهلت رزرو می‌توانید دوباره تلاش کنید.

7. After Reservation Expiry

When server reports current cycle Expired/ended:

Card remains visible if Order is still eligible for retry.

Show:
مهلت رزرو موجودی پایان یافته است.

Primary action:
بررسی موجودی و پرداخت مجدد

On click:

backend checks MaxReservationCycles

backend authoritatively reacquires inventory

if success:

new Reservation Cycle

retry TTL starts

same Order

navigate/start payment

if stock unavailable:

no Payment attempt

no new active cycle

show:
این سفارش در حال حاضر قابل تأمین نیست.

if max cycles reached:

show localized retry-limit outcome

Frontend must not infer or manufacture Cycle #N.

8. Retry Cycle UX

For a new retry cycle:

countdown reflects new server ExpiresAt

retry TTL may differ from initial TTL

failed Payment Attempts within this cycle still do not reset it

if cycle expires again, same expired UX applies

if max cycles reached, no retry CTA

Optional human copy:
مهلت رزرو مجدد

Do NOT clutter normal customer UI with full audit history; full cycle history belongs to Admin.

9. Multiple Pending Orders

If customer has more than one unpaid committed Order:

show multiple pending-payment cards

each card uses its own committed proof / authenticated ownership

each countdown independent

actions scoped to correct Order

no proof overwrite

no single global countdown state

For guest:
only Orders accessible by valid committed proof(s) are shown.

If current product supports only one guest pending Order at a time, document limitation and ensure no cross-order authorization bug.

10. Payment State Handling

Pending-payment cards must only include Orders/payment states where customer action is meaningful.

Examples:

awaiting online payment

failed but retryable

expired but retryable

manual awaiting evidence where customer still must submit

manual rejected and retry allowed

Do NOT show as payable:

Succeeded

cancelled terminal

fully refunded terminal

manual AwaitingAdmin after evidence submitted, unless the card is shown as informational and has no pay-again CTA

For Manual AwaitingAdmin:
show status:
در انتظار بررسی پرداخت
No payment retry CTA unless backend capability permits.

11. Backend Projection / API

Prefer a dedicated/batched customer pending-payment projection rather than frontend stitching many endpoints.

Required projection per Order:

human Order reference

payable amount/currency

compact item data

payment status

canInitiatePayment

canRetryPayment

manual-awaiting-review flag/capability

current reservation cycle status

current cycle number

ExpiresAt

server current time or remaining seconds

max cycles / retries remaining if needed for UX

supply status when expired/retry-relevant

correct action capability

No N+1 item/media/payment queries.

12. Guest/Auth Ownership

Authenticated:

use authenticated customer ownership

Guest:

use committed Order/Checkout proof

active Cart proof is irrelevant

new Active Cart cannot authorize old Order

wrong committed proof denied

no Order enumeration

Preserve R13.

13. Empty Cart UX

When Active Cart is empty but one or more pending Orders exist:

Do NOT show only:
سبد خرید شما خالی است

Preferred layout:

normal empty Active Cart area may remain

در انتظار پرداخت is prominent enough to be obvious

primary actionable pending card appears clearly

Match Shopeiva design language, do not clone external brand assets.

14. Responsive / RTL

Required:

FA RTL

EN LTR

mobile and desktop

countdown legible

CTA not clipped

thumbnails do not overflow

user-authored widths/layout refinements preserved

15. No Polling / Efficient Refresh

Forbidden:

1.5s API polling

per-second API requests

polling to detect expiry

Allowed:

local visual countdown

one state refresh when local display reaches expiry

user-triggered refresh/action

normal navigation refresh

Document request count.

Evidence:
docs/evidence/TB-P10-T004/r16-network.md

16. Error UX

Map outcomes cleanly:

unavailable:
این سفارش در حال حاضر قابل تأمین نیست.

max cycles reached:
تعداد دفعات مجاز رزرو مجدد موجودی برای این سفارش به پایان رسیده است.

payment already succeeded:
پرداخت این سفارش قبلاً با موفقیت انجام شده است.

ownership failure:
correct access message, never ثبت سفارش انجام نشد

No raw backend codes.

17. Focused Tests

Required:

Projection:

active pending Order returned

Succeeded excluded from payable list

Manual AwaitingAdmin shown informational/no retry

multiple pending Orders scoped correctly

no N+1

Countdown:
6. uses server ExpiresAt
7. display counts down locally
8. failed Payment does not reset countdown
9. no API per second
10. at 00:00 only one refresh/revalidation

Retry:
11. active cycle retry -> same cycle
12. expired + stock available -> new cycle + same Order
13. expired + unavailable -> no Payment attempt
14. max cycles -> no retry
15. retry cycle uses retry TTL

Ownership:
16. guest committed proof works
17. new Cart secret cannot authorize pending Order
18. wrong proof denied
19. authenticated works

UX:
20. empty Active Cart + pending Order displays pending card
21. paid Order disappears from pending-pay section
22. no raw errors
23. FA RTL
24. EN LTR

Regression:
25. R13 cart rotation
26. R14 paid initiation guard
27. R15 cycle semantics
28. recovery guard
29. git diff --check

Do NOT run unrelated full suites.

18. Runtime Matrix — REQUIRED

A. Initial pending Order

commit

pending card visible

countdown correct

B. Payment fail at mid-cycle

card remains

countdown does NOT reset

retry works same cycle

C. Cycle expires

card changes to expired state

no API polling storm

D. Retry after expiry + stock available

same Order

new cycle

new retry countdown

E. Retry after expiry + stock unavailable

no Payment attempt

unavailable message

F. Max cycle reached

no retry CTA

limit message

G. Manual AwaitingAdmin

informational card

no pay-again CTA

H. Payment success

card disappears from pending section

I. Active new Cart + pending old Order

both coexist correctly

J. Guest + authenticated

K. mobile/desktop smoke

19. Anti-Pattern Gate

Scan for:

frontend-authoritative expiry

timer reset after failed payment

per-second API polling

duplicate Order creation from retry

Active Cart used for pending Order ownership

global single pending-order proof overwrite

raw cycle enums/codes in UI

duplicated retry policy in frontend

first-order-only shortcut

N+1 item/payment requests

Any occurrence => REPAIR.

Evidence:
docs/evidence/TB-P10-T004/r16-antipattern-scan.md

20. Lock Registry

Add non-duplicate locks equivalent to:

pending-payment Order is shown independently from Active Cart

countdown is derived from server ExpiresAt and is presentation-only

failed payment inside active cycle never resets customer countdown

expired retry creates a new cycle only after backend reacquire

pending-payment UI is capability-driven

multiple pending Orders are independently scoped

no polling is used to maintain or decide reservation state

21. Evidence

Add under docs/evidence/TB-P10-T004/:

r16-recovery-start.md

r16-customer-pending-payment-discovery.md

r16-pending-payment-projection.md

r16-cart-page.md

r16-countdown.md

r16-retry-ux.md

r16-ownership.md

r16-network.md

r16-focused-validation.md

r16-runtime.md

r16-antipattern-scan.md

r16-recovery-sot.md

22. Recovery / Pipeline

At completion:

Phase P10

Last Architect-accepted before review: TB-P10-T004-R15

Last Implementation: TB-P10-T004-R16

Current Issued/Repair: none

USER_VISUAL_ACCEPTED=NO

Worker: tooba-worker-01 — IDLE

branch main

ending HEAD recorded

HEAD==origin/main

task-owned worktree clean

no TB-P10-T005

Run recovery guard.

Acceptance

PASS only if:

pending-payment section exists on Cart

countdown uses server ExpiresAt

no frontend lifecycle authority

failed payment does not reset timer

expired retry uses same Order + new cycle only after reacquire

max-cycle limit honored

unavailable stock blocks Payment attempt

Manual AwaitingAdmin has no pay-again CTA

payment success removes pending card

active new Cart and pending old Order coexist

guest/auth ownership correct

no per-second API polling

no N+1

runtime A–K PASS

anti-pattern scan CLEAN

focused tests green

recovery exact

HEAD==origin/main

Worker IDLE

no TB-P10-T005

USER_VISUAL_ACCEPTED=NO.

Result Contract

Return ONLY canonical BRIDGE-WAKE-V1 Result with:

Summary
Applicable-Locks
Recovery-Start
Discovery
Pending-Payment-Projection
Cart-Page
Countdown
Retry-UX
Ownership
Network
Focused-Validation
Runtime
AntiPattern-Scan
Lock-Registry
Recovery-SoT
Git
Architectural-Concerns
Visual-Concerns
Blockers

END_TOOBA_REPAIR_TASK