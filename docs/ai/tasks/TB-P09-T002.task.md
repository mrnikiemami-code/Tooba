PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_TASK

Task-ID:
TB-P09-T002

Parent-Task:
TB-P09-T001-R2

Channel:
tooba-main

WorkerId:
tooba-worker-01

AgentType:
cursor

Status:
ISSUED

USER_VISUAL_ACCEPTED=NO

Title:
Order Detail Operational Completeness — Internal Notes, Operational History, Invoice/Receipt Actions

Architect Decision

TB-P09-T001 is ACCEPTED after R1/R2.

Accepted foundations:

backend-authoritative Order lifecycle operations

domain-authoritative cancel guard

one ReturnEligibility SoT

settlement-independent valid returns

immutable completed settlement

idempotent seller refund adjustment

real Marketplace integration-event path through MassTransit PostgreSQL transport/outbox

minimal Orders UI preserved

This task continues Orders completion with the next missing operational capabilities identified in T001 audit.

Goal

Complete the practical Order Detail experience with the MINIMUM possible UI change.

Focus only on capabilities already identified as missing/partial:

internal operational notes

operational history/timeline visibility

invoice/receipt print/download actions using existing document/payment/order data

Do NOT redesign Orders list or Order detail.
Do NOT revisit lifecycle/return/settlement logic unless required for integration.

Mandatory Discovery Before Changes

Before implementing:

Inspect current Order detail/workspace and all sections already present.

Inspect existing audit/history/event infrastructure in:

Checkout/Orders

Fulfillment

Returns

Payments

Settlement

Inspect existing document/invoice/receipt generation if any.

Inspect existing notes/comment entities in other modules to reuse conventions.

Inspect current permissions/capabilities before adding new ones.

Inspect current UI design-system components suitable for:

compact notes

timeline/history

print/download actions

drawer/dialog where needed

Do NOT create duplicates.

If an equivalent subsystem already exists, reuse it.

If architectural ownership is unclear:
STOP and report ARCHITECTURE-PATTERN-UNCLEAR.

UI Lock — Minimal Change

Preserve current:

Orders grid

Order detail layout

existing cards and sections

عملیات سفارش

current contextual action menu

current typography/spacing

Admin navigation

No page redesign.

Only add compact operational affordances where needed.

1. Internal Order Notes

Provide Admin internal notes for operational use.

Required behavior:

notes are INTERNAL only

never shown to customer/storefront

append-only by default

author/user identity captured

created timestamp captured

optional edit/delete only if an existing project convention explicitly supports it; otherwise do not add

plain text sufficient for first version; no rich editor

reasonable max length + validation

newest/oldest ordering chosen consistently with existing UX

loading/error/empty states

localized FA/EN UI

Backend:

Order/Checkout-owned or a clearly appropriate existing operational module

no foreign-module DB shortcut

proper command/query

audit-friendly

authorization enforced backend-side

Do NOT reuse customer comments/reviews for internal notes.

2. Operational History / Timeline

Expose a unified, read-only operational history for the Order detail.

Do NOT create a second audit/event store.

Compose from existing authoritative events/projections where possible.

History should include meaningful operations available in current system, such as:

order created

payment state changes

cancellation

processing/preparation

packed

shipment created

tracking assigned

dispatched

delivered

return requested

return approved/rejected

refund completed/retried

seller settlement accrual

seller refund adjustment

Rules:

human-readable labels

actor displayed as توسط سیستم / توسط {name}, never technical actor ids as primary text

timestamp localized in UI

FA Jalali / EN Gregorian according to current localization convention

canonical UTC/NodaTime backend

paged/bounded query

no N+1

no raw event payload/JSON in normal UI

stable ordering

If some event source cannot safely be unified now, show only authoritative available events and document the gap; do not fake entries.

3. Invoice / Receipt Actions

Audit first.

If an existing invoice/receipt generation path exists:

reuse it

expose practical action(s) in Order detail / operations menu

If no existing canonical invoice generator exists:

implement the smallest sellable version using existing Order snapshot/payment data

do NOT create a large accounting subsystem

do NOT introduce tax/accounting semantics not already modeled

Expected user-visible actions as applicable:

چاپ فاکتور

دانلود فاکتور

چاپ رسید پرداخت / دانلود رسید پرداخت only if the existing payment model supports meaningful receipt data

Document must:

use snapshot values from the Order, not current mutable Product/Offer data

show order number/date

customer/recipient data already available in the Order snapshot

seller/store data appropriate to edition

line items

quantity

unit/line totals

discounts/tax/shipping only if actually modeled

grand total

payment status/method where available

RTL-safe Persian layout and LTR-safe English layout

printable without UI chrome

Do NOT expose internal settlement values on customer-facing invoice.

Do NOT put raw provider secrets/ids on receipt.

4. Permissions

Reuse existing capabilities first.

Likely semantics:

Order View permits reading history if existing policy says so

internal note create may require order.handle/manage equivalent

invoice/receipt view/download should follow order.view/payment.view as appropriate

Do NOT invent unnecessary granular permissions.

Backend authorization remains authoritative.

5. Placement in Current Order Detail

Keep visual change minimal.

Preferred composition:

one compact یادداشت داخلی section/card

one compact تاریخچه عملیات section/tab/panel using existing page pattern

invoice/receipt actions placed inside existing عملیات سفارش or existing header action cluster

Do NOT add a new second navigation system.
Do NOT add another large sidebar.
Do NOT duplicate data already visible elsewhere.

6. Orders Grid

No redesign.

Only if there is a clear existing Operations-menu slot:

expose invoice/print there as optional contextual action if appropriate

Do not add new permanent columns/buttons.

7. Error UX

No raw:

Bad Request

machine code

stack trace

JSON

database/provider exception

Localized FA/EN messages for:

note validation failure

invoice unavailable

receipt unavailable

permission denied

history load failure

8. Performance

paged history

bounded notes query

no N+1

no broad module fan-out per grid row

Order detail may make scoped parallel queries if current pattern supports it

do not reload whole page after adding note if scoped refresh is enough

9. Focused Validation

Backend focused tests:

add/read internal note

authorization

note validation

history ordering/paging

history includes representative Order/Fulfillment/Return/Refund operations available in fixtures

invoice uses Order snapshot values

no current Product/Offer mutation affects historical invoice

receipt privacy where applicable

Frontend focused tests:

note add + refresh

history loading/empty/paging

actor labels

FA/EN date behavior according to existing helpers

invoice/receipt action visibility

no raw errors

current Order operations regression

minimal UI regression

Also:

relevant build/typecheck/lint

recovery guard

git diff --check

No unrelated full repository suites.

10. Runtime Smoke

Use:
Backend: http://127.0.0.1:5088
Frontend: http://127.0.0.1:3000

Verify one realistic Order detail:

Existing detail still visually intact

Add one internal note

Reload and confirm note persists

Confirm note is not exposed on storefront/customer response/path

Open operational history and verify representative lifecycle events

Print/download invoice

Confirm invoice uses Order snapshot values

If payment receipt exists, open/print/download it

Confirm permissions hide/deny unavailable operations

Keep smoke focused and short.

11. Recovery / Pipeline

Update:

Phase: P09

Last Architect-accepted: TB-P09-T001

Current Issued: TB-P09-T002

Current Repair: (none)

Last Implementation on completion: TB-P09-T002

USER_VISUAL_ACCEPTED=NO

Worker Next State: IDLE

do NOT invent/start TB-P09-T003

Run recovery guard.

12. Evidence

Create:
docs/evidence/TB-P09-T002/

Required concise evidence:

discovery.md

internal-notes.md

operational-history.md

invoice-receipt.md

permissions.md

minimal-ui.md

runtime-smoke.md

recovery-sot.md

13. Git

Required:

preserve user-authored changes

commit task changes

push origin/main

HEAD==origin/main

no task-owned uncommitted changes

Acceptance

PASS only if ALL:

current Order UI preserved

no duplicate audit/history subsystem

internal notes are operational/internal only

notes persist with actor/time

storefront/customer does not receive internal notes

operational history is bounded/paged and human-readable

representative lifecycle/payment/fulfillment/return/refund events are visible where authoritative data exists

invoice/receipt reuses existing paths or minimal canonical implementation

invoice uses Order snapshot data

no internal settlement values leak into customer invoice

permissions enforced backend-side

localized FA/EN UX

no raw errors

focused tests green

runtime smoke complete

recovery guard passes

HEAD==origin/main

Worker returns IDLE

no TB-P09-T003 invented

USER_VISUAL_ACCEPTED=NO.

Result Contract

Return ONLY canonical BRIDGE-WAKE-V1 result with:

Summary
Discovery
Internal-Notes
Operational-History
Invoice-Receipt
Permissions
Minimal-UI
Error-UX
Performance
Focused-Validation
Runtime-Smoke
Recovery-SoT
Git
Architectural-Concerns
Visual-Concerns
Blockers

END_TOOBA_TASK