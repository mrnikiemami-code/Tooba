PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_TASK

Task-ID:
TB-P10-T011

Parent-Task:
TB-P10-T010

Channel:
tooba-main

WorkerId:
tooba-worker-01

AgentType:
cursor

Status:
ISSUED

USER_VISUAL_ACCEPTED=NO

Title:
Landing Section Foundation — Canonical PageSection Model, Approved Section Registry, Controlled Data Sources

Architect Decision

TB-P10-T010 is Architect-ACCEPTED.

Accepted baseline now includes:

canonical StoreLandingPage

Draft / Published lifecycle

Store + Locale + Slug routing

reserved route protection

Store HomePageId contract

Published-only public resolution

no rebuild/redeploy requirement for adding a Page

LOCK-SF-001…171

This task implements the next Landing/Home foundation layer:

canonical PageSection model

approved SectionType registry

controlled section configuration

controlled product/content data-source contract

backend/API support needed by the future Page Composer

This task does NOT implement:

drag/drop composer UI

full Admin Page Builder

Menu builder

landing template gallery

free-form layout editor

arbitrary HTML/CSS/JS

TB-P10-T012

Product / UX Direction — LOCKED

The future Page Composer must be understandable to a normal Store Admin.

Users must NEVER need to:

write SQL

write API queries

enter internal IDs manually

edit JSON

understand SectionType enum names

write CSS/HTML/JS

They should choose:

a section visually

its content source

products/categories/brands/articles through searchable pickers

order/show-hide settings through simple UI

This task builds the backend/domain contract that makes that UX possible later.

Recovery State

Phase: P10
Last Architect-accepted: TB-P10-T010
Last Implementation: TB-P10-T010
Current Task: TB-P10-T011
Worker: tooba-worker-01
Expected branch: main
Expected previous HEAD: aad601062b0ebd4efd6343f8eb351fa1579cecbe
USER_VISUAL_ACCEPTED=NO

Preserve LOCK-SF-001…171.

1. Recovery Preflight

Record:

branch

HEAD

origin/main

git status --short

Recovery/SoT

Host :5088

FE :3000

unrelated local user-authored changes

Unexpected tracked divergence => RECOVERY_CONFLICT/BLOCK.

Evidence:
docs/evidence/TB-P10-T011/recovery-start.md

2. Mandatory Discovery — Existing Home Composition / Content Blocks

Inspect only relevant code.

Determine:

current PageComposition/Home section mechanism

current Home hard-coded/DB-driven sections

current product rail/list/card data sources

existing category/brand/article/review/banner projections

any existing section config DTOs

existing Admin pickers/search endpoints

any section registry or rendering registry already present

Do NOT duplicate an existing composition model if it can be safely evolved.

Evidence:
docs/evidence/TB-P10-T011/section-discovery.md

3. Canonical PageSection Model

Create/reuse ONE canonical PageSection model owned by StoreLandingPage.

Required concepts:

PageSectionId

PageId

SectionType

SortOrder

IsEnabled

controlled configuration

CreatedAt / UpdatedAt according to project conventions

The section model must remain Store-safe through Page ownership.

Do NOT store:

executable HTML

executable CSS

executable JS

arbitrary SQL/query text

arbitrary unvalidated config blobs

If JSON is used for section config, it must be schema-controlled per SectionType and validated before persistence.

4. Approved Section Registry

Create one code-owned typed registry for approved SectionTypes.

First release foundation should support only sections that map cleanly to current Storefront/Home capabilities.

Target initial set:

Hero

ProductCollection

CategoryGrid

BrandStrip

PromoBanner

ArticleList

Reviews/Testimonials

RichText

If one of these does not map to current capabilities cleanly, document and defer it rather than adding fake support.

Each registry entry should define:

stable technical key

human-friendly future label metadata if architecture supports it

config schema/type

allowed data-source modes

renderer key/contract

whether section can be disabled

safe defaults

Do NOT build the Admin chooser UI yet.

Evidence:
docs/evidence/TB-P10-T011/section-registry.md

5. Controlled Product Data Sources

For ProductCollection, support controlled source types equivalent to:

Manual

Category

Brand

Newest

BestSelling

Featured

Discounted

The Store Admin will later choose one from UI.

Required:

no raw query input

no SQL

no arbitrary sort/filter expression

source config validated

Store scope enforced

take/limit bounded by safe server-side rules

Manual uses Product IDs selected by picker later, not free-text user IDs in final UX

If some source type is not currently supported by backend data, document it and implement only the safe subset; do not fake business semantics.

6. Other Controlled Data Sources

Where applicable:

CategoryGrid:

Manual category selection and/or approved category projection

BrandStrip:

Manual brand selection and/or approved brand projection

ArticleList:

Manual / Latest / Category if current content architecture supports it

Reviews/Testimonials:

existing approved review/testimonial source only

Hero / PromoBanner / RichText:

controlled content fields

sanitized text/media references

no arbitrary script/style

Do not over-generalize into a universal query language.

7. Section Configuration Validation

Every SectionType must have explicit validation.

Examples:

required title only where relevant

bounded item counts

media reference validation

source-specific required fields

no cross-Store referenced entity

sort order bounded/normalized

duplicate Manual IDs handled consistently

empty configuration safely rejected or rendered as no-op according to section contract

Avoid giant generic validation framework if typed validators already exist.

8. Page Ownership / Store Isolation

Required:

Section belongs to one Page

Page belongs to one Store

Store A cannot add/update/read Section of Store B through Admin API

public rendering can only resolve Sections of the resolved Published Page

referenced Product/Category/Brand/Article must belong to the same Store/catalog scope where applicable

Backend-authoritative.

9. Ordering / Enable-Disable Contract

Required:

deterministic SortOrder

enabled sections only for public render

disabled section remains persisted for editor reuse

reordering must not require delete/recreate

stable PageSectionId across reorder

no duplicate ambiguous sort order after canonical normalization

Do NOT build drag/drop UI yet.

10. Public Page Projection

Extend public Page resolution to include a safe ordered section projection.

Required:

only Published Page

only enabled Sections

section config exposed as typed/render-safe projection

no internal DB/config leakage

no Admin-only metadata

no N+1 across section list if avoidable

Do NOT render the full visual section system yet unless a minimal renderer is already naturally available.
This task is contract/data foundation.

11. Admin API Foundation

Provide/reuse canonical Admin endpoints/services sufficient for future composer:

list Page sections

add section

update section config

enable/disable

reorder

delete section

API should accept human-intent DTOs, not raw DB entities.

No giant “save arbitrary page JSON” endpoint.

Do not expose raw technical config to normal UI later; backend contract may still use typed DTOs.

12. Minimal Useful Tests Only

Per Architect rule: keep tests minimal and risk-driven.

Essential tests only:

invalid SectionType rejected

section belongs to same Store/Page scope

ProductCollection raw/arbitrary query input impossible/rejected

cross-Store referenced entity rejected

reorder preserves Section IDs and deterministic order

disabled section excluded from public projection

Published Page returns ordered enabled sections

Draft Page still public 404

one representative controlled ProductCollection source resolves correctly

recovery guard + git diff --check

Reuse existing Store/Page auth tests instead of duplicating them.

Do NOT create large combinatorial test matrices for every field/source.

13. Runtime Proof

Use Host :5088 + FE :3000.

Required runtime scenario:

A. create/publish a temporary landing Page
B. add Hero
C. add ProductCollection using one real supported controlled source
D. add one additional non-product Section
E. verify order
F. disable middle Section -> public projection excludes it
G. reorder -> public projection order changes without new Section IDs
H. invalid SectionType/config -> rejected
I. restore/remove temporary runtime data

Record:

Page slug

section IDs

section types

enabled state

sort order

source type

public response order/status

No manual SQL to fake final state.

14. Performance / Query Discipline

Required:

no per-product theme/section query

avoid N+1 for PageSection list

controlled source limits

no loading every product in Store to satisfy a small section

future cache invalidation point documented for Page/Section writes

Do not prematurely build distributed cache if current architecture does not require it.

Evidence:
docs/evidence/TB-P10-T011/performance-contract.md

15. No Visual Builder Yet

Do NOT create:

drag/drop UI

section thumbnails

full Page edit screen

preview device switcher

visual template selector

Those belong to next UI/composer task.

Do not create placeholder UI that looks unfinished.

16. Anti-Pattern Gate

Scan for:

arbitrary query language

SQL/filter expression from user

executable HTML/CSS/JS

giant arbitrary page JSON blob

unvalidated polymorphic config

cross-Store references

delete/recreate reorder

one endpoint replacing all page state with opaque JSON

Section builder UI sneaking in

Menu builder sneaking in

Any real occurrence => REPAIR.

Evidence:
docs/evidence/TB-P10-T011/antipattern-scan.md

17. Lock Registry

Add non-duplicate locks after LOCK-SF-171 equivalent to:

Landing Pages are composed only from approved code-owned SectionTypes.

PageSection configuration is typed/schema-validated and cannot execute arbitrary HTML/CSS/JS.

Product/content sections use approved controlled data sources; user-authored SQL/arbitrary query expressions are forbidden.

PageSection ownership and all referenced commerce/content entities remain Store-scoped.

Public Page projection includes only enabled Sections of a Published Page in deterministic order.

Reordering preserves stable PageSection identity; reorder is not delete/recreate.

Section configuration limits are server-enforced to prevent unbounded page/query cost.

Use next canonical LOCK-SF numbers.
Do not renumber existing locks.

18. Evidence

Create:
docs/evidence/TB-P10-T011/

Required:

recovery-start.md

section-discovery.md

section-model.md

section-registry.md

controlled-data-sources.md

validation-isolation.md

public-projection.md

performance-contract.md

focused-validation.md

runtime.md

antipattern-scan.md

recovery-sot.md

Keep evidence concise.

19. Recovery / Pipeline

At completion:

Phase P10

Last Architect-accepted before review: TB-P10-T010

Last Implementation: TB-P10-T011

Current Issued/Repair: none

USER_VISUAL_ACCEPTED=NO

Worker IDLE

branch main

ending HEAD

HEAD==origin/main

task-owned clean

unrelated local user changes preserved

do NOT invent/start TB-P10-T012

Run recovery staleness guard.

Acceptance

PASS only if ALL:

canonical PageSection model established

approved typed Section registry established

no arbitrary HTML/CSS/JS/query execution path

controlled product/content source contract exists

Store isolation enforced

referenced entities Store-scoped

enable/disable works

deterministic reorder preserves Section IDs

public projection returns only enabled ordered Sections for Published Page

Draft remains public 404

no giant arbitrary page JSON endpoint

performance bounds documented/enforced

only essential focused tests added

runtime proof green

anti-pattern scan CLEAN

Lock registry updated without duplicates

Recovery exact

HEAD==origin/main

Worker IDLE

no TB-P10-T012

USER_VISUAL_ACCEPTED remains NO

Result Contract

Return ONLY canonical BRIDGE-WAKE-V1 Result with:

Summary
Applicable-Locks
Recovery-Start
Section-Discovery
Section-Model
Section-Registry
Controlled-Data-Sources
Validation-Isolation
Public-Projection
Performance-Contract
Focused-Validation
Runtime
AntiPattern-Scan
Lock-Registry
Recovery-SoT
Git
Architectural-Concerns
Visual-Concerns
Blockers

END_TOOBA_TASK