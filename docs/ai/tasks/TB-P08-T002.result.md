PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_WORKER_RESULT

Task-ID:
TB-P08-T002

Parent-Task:
TB-P08-T001-R2

Channel:
tooba-main

Status:
PASS

Worker:
tooba-worker-01

Bridge-UUID:
e37c2f76-a976-4d84-bf71-bc755933db45

Summary:
Implemented Content-owned article category taxonomy (content.categories) with language-aware trees, canonical AppCategoryTree admin UX, SEO/DAM reuse, article CategoryId wiring, migration, focused BE/FE tests, recovery SoT. USER_VISUAL_ACCEPTED=NO.

Content-Taxonomy-Discovery:
Legacy string Category EXISTS_AND_REUSE; ContentCategory table GENUINELY_MISSING and added; Catalog categories not reused.

Category-Model:
ContentCategory entity + EF migration; slug unique per LanguageCode; hierarchy without L1/L2/L3 cap; archive guards for children/articles.

Language-Semantics:
Independent fa-IR/en-US trees; cross-language parent and article assignment rejected server-side.

Tree-UX:
/admin/content/categories + [categoryId] routes; AppCategoryTree search/selection/RTL-LTR; article counts on nodes.

Category-Workspace:
Tabs عمومی/SEO/رسانه/مقالات/تاریخچه with VIEW/EDIT via useAdminFormMode.

SEO:
Category SeoTitle/SeoDescription endpoints; no second SEO subsystem.

Media-DAM:
Optional ImageMediaAssetId via MediaLibraryDialog; unassign clears reference only.

Article-Relation:
articles.category_id FK; Create/Update commands and admin create picker (same language).

Menu:
Admin nav: محتوا / بلاگ + دسته‌بندی مقالات under ops.

Query-Performance:
Tree/article counts via grouped queries; no cross-module SQL joins.

Permissions:
AdminPanelAccess on all category endpoints; localized mutation error mapping in content-category-api.

Focused-Validation:
dotnet build PASS; ContentCategory* + ContentFoundation tests 7/7; content-category-admin-screen.test.ts 2/2; recovery guard 3/3.

Smoke-Check:
Not run (runtime smoke optional per task); implementation ready at /admin/content/categories.

Recovery-SoT:
P08; last accepted TB-P08-T001-R2; last implementation TB-P08-T002; issued/repair (none); USER_VISUAL_ACCEPTED=NO.

Git:
HEAD=b8e5cd57
origin/main=b8e5cd57
HEAD==origin/main

Architectural-Concerns:
Public category storefront routes not wired in this task; legacy Category string kept for published filters until dedicated routing task.

Visual-Concerns:
USER_VISUAL_ACCEPTED=NO; no formal visual evidence captured.

Blockers:
None.

END_TOOBA_WORKER_RESULT
