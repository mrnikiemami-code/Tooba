PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_WORKER_RESULT

Task-ID:
TB-P09-T001

Channel:
tooba-main

WorkerId:
tooba-worker-01

AgentType:
cursor

Status:
PASS

USER_VISUAL_ACCEPTED=NO

## Summary
Delivered Admin Order Operations Foundation with minimal UI change: backend-authoritative lifecycle actions, one return-eligibility SoT (30-day window; settlement never gates eligibility), admin GET/POST operations + return-eligibility APIs, one contextual «عملیات» menu on Orders grid (View kept) and «عملیات سفارش» on detail. Reused Fulfillment/Returns/Payment/Settlement directories; no new workflow engine. Worker returns IDLE. No TB-P09-T002 invented.

## Existing-Operations-Audit
Mapped cancel (partial→HTTP), fulfillment process/pack/ship/deliver (existing seller commands reused for admin), return/refund (existing directory), settlement accrual + AdjustFromRefund debit, payout existing; notes/invoice still missing (out of scope). Evidence: docs/evidence/TB-P09-T001/existing-operations-audit.md

## Order-Lifecycle-Actions
AdminOrderOperationsComposer projects only valid+permitted actions (cancel, mark_processing, mark_packed, create_shipment, assign_tracking, dispatch_shipment, deliver_shipment, request_return, approve/reject_return, retry_refund). Invalid transitions rejected with `order.operation.invalid` and FA detail.

## Permission-Integration
Reuses catalog `order.cancel|handle|fulfill|refund`, `return.manage`, `fulfillment.manage` with platform effective-access helper (legacy tenant-admin allow-all when no granular grants).

## Return-Eligibility
`IReturnEligibilityEvaluator` / `ReturnEligibilityEvaluator` is the single SoT; ReturnDirectory.CreateAsync consumes it. ReasonCodes: not_paid, not_delivered, window_expired, nothing_returnable, eligible, order_missing, fulfillment_missing. Never reads Settlement.

## Settlement-Return-Rule
Completed seller settlement does not end return rights. Post-settlement valid return keeps statements immutable; AdjustFromRefundAsync posts idempotent Debit adjustment for future payable.

## Financial-Adjustment
Reused Settlement `AdjustFromRefundAsync` / `PostDebitFromRefund` (SourceType refund); no second ledger.

## Refund-Semantics
Refund remains inside Returns approve + Payment/Wallet gateways; admin retry-refund preserved; idempotent attempts.

## Fulfillment-Operations
Admin operations execute via IFulfillmentDirectory (processing/packed/shipment/tracking/dispatch/deliver); no fake FE status.

## Operational-History
No duplicate history subsystem; financialEvents compose retained; ops leave existing module audit/outbox trails.

## Orders-Grid-UI
View action retained; one contextual operations menu added in Operations column; no permanent multi-button row redesign.

## Order-Detail-UI
Layout preserved; one «عملیات سفارش» affordance in header action cluster.

## Error-UX
Stable codes `order.operation.denied|invalid|failed` with human FA messages; no stack/raw JSON in normal UI path.

## Performance
Per-checkout operations query; no N+1 grid fan-out; scoped refresh after action.

## Focused-Validation
Backend focused eligibility/ops/return/settlement suites green (13). Frontend admin-order-operations tests green (4). Recovery guard green (3). git diff --check clean on commit.

## Runtime-Smoke
Host rebuilt on :5088 after ship. PendingPayment → cancel projected; not_paid eligibility. Paid unpaid-ship → cancel/mark_processing/mark_packed/create_shipment; not_delivered. Invalid deliver POST → 400 `order.operation.invalid` FA. FE `/admin/orders` 200. Settlement-independence covered by evaluator design + tests/evidence.

## Recovery-SoT
Phase: P09
Last Architect-accepted baseline: TB-P08-T016-R5
Current Issued / Last Implementation: TB-P09-T001
Current Repair: (none)
USER_VISUAL_ACCEPTED=NO
Worker Next State: IDLE
Evidence: docs/evidence/TB-P09-T001/
No TB-P09-T002 invented.

## Git
Commit: cf605d972da4fc8124b696c2a3aa7c634f2df306
Pushed origin/main; HEAD==origin/main YES
App_Data untracked (not committed)

## Architectural-Concerns
SellerOrder.Cancel domain remains weakly guarded; Host/composer policy restricts cancel projection. Admin return create uses CreateAdminInitiatedAsync with customer PlacedByUserId as requester.

## Visual-Concerns
USER_VISUAL_ACCEPTED=NO. UI changes intentionally minimal; no Orders redesign.

## Blockers
None for Worker PASS. Architect review required for ACCEPT.

END_TOOBA_WORKER_RESULT
