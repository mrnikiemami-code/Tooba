PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_REPAIR_TASK

Task-ID:
TB-P09-T022-R1

Parent-Task:
TB-P09-T022

Channel:
tooba-main

WorkerId:
tooba-worker-01

AgentType:
cursor

Status:
REPAIR

USER_VISUAL_ACCEPTED=NO

Title:
Consolidated Package Visual Runtime Repair — Fix Admin FE 500 + Real Browser Smoke

Architect Review

TB-P09-T022 is technically strong, but it is NOT fully accepted because the final gate explicitly required real FE visual smoke and the Worker reported:

FE admin HTML shell returned 500 during smoke

This means the visual/runtime gate was not actually completed.

The backend/package projection/history/runtime matrix A–J are accepted as evidence.
This repair is narrowly about restoring the frontend runtime path and completing real visual smoke.

Do NOT redesign Consolidated Package.
Do NOT alter domain behavior that already passed.
Do NOT add product scope.
Do NOT start TB-P09-T023.

Applicable Locks

Read first:
docs/architecture/TOOBA-LOCKS.md

Preserve:

all LOCK-OPS-013…023

Shopeiva/Tooba visual contracts

no dead controls

no raw GUIDs/errors

USER_VISUAL_ACCEPTED remains NO until user explicitly accepts

Admin runtime must be actually viewable

1. Reproduce FE 500

Run the real frontend used by this repo, normally:
http://127.0.0.1:3000

and backend normally:
http://127.0.0.1:5088

Reproduce the exact Admin HTML shell 500.

Capture:

route

server stack/error

browser/network failing request

whether failure is SSR/render, API fetch, auth, env, build artifact, or fixture/data issue

Document:
docs/evidence/TB-P09-T022/r1-fe500-discovery.md

Do not mask the error with static/mock HTML.

2. Fix Root Cause

Repair the smallest root cause only.

Examples depending on discovery:

SSR null/reference failure

invalid DTO assumption

missing package field

auth/dev-context issue

stale frontend build/cache

broken route loader

API contract mismatch

bad fixture reference

Requirements:

no catch-all swallowing

no hardcoded test-only bypass

no disabled SSR just to pass

no mock package projection

3. Real Admin Runtime

After fix, prove the actual Admin page loads with HTTP 200 and renders:

Order Detail

اقلام و ارسال

بسته‌بندی مرکزی

Use a real multi-seller Order fixture/runtime record.

Verify:

no 500

no hydration/runtime console error

no endless spinner

no broken API loop

no dead button caused by render exception

4. Visual Contract Smoke

On the real page verify visually:

Visibility

single-seller Order: central section hidden

multi-seller Order: section visible

Before package

ایجاد بسته تجمیعی

only eligible ready Shipments

selection UI readable

minimum 2 distinct Sellers enforced

After create

compact central package card

human package reference

status

seller/member count

provider/tracking

member Shipment cards still visible

conflicting member actions locked/read-only

explanatory text visible

Rebuild

cancel package before dispatch

old package remains historical

new package can be created

Dispatch / Deliver

action buttons render correctly

post-dispatch membership immutable

delivered state coherent

UI Quality

RTL correct

no overlap

no raw GUID

no raw enum

no .000000

no technical English leakage in Persian locale

dialogs fit viewport

no duplicated Shipment block

current beautiful Orders layout preserved

Do not redesign.

5. Customer Tracking Runtime

Open the actual customer tracking/order view.

Verify:

authenticated owned customer path renders

valid guest-secret path renders where exposed through current FE/BFF

central tracking primary

cancelled package not primary

no-package legacy tracking still renders

no FE 500

6. Browser/FE Error UX

Verify no raw frontend crash for:

stale package

cancelled package

unauthorized customer access

invalid guest secret

Normal UI should use existing localized error surfaces.

7. Focused Validation

Required:

existing T022 Host tests remain green

existing T022 FE tests remain green

add regression for exact FE 500 root cause if testable

frontend build/typecheck

real runtime HTTP 200 for Admin route

real runtime HTTP 200 for supported Customer route

browser console free of new uncaught errors on tested paths

recovery guard

git diff --check

Do NOT run unrelated full repo suite.

8. Runtime Evidence

Update:
docs/evidence/TB-P09-T022/

Add:

r1-fe500-discovery.md

r1-fe500-fix.md

r1-admin-runtime.md

r1-customer-runtime.md

r1-visual-smoke.md

r1-focused-validation.md

r1-recovery-sot.md

Record actual runtime URLs and tested Order reference in evidence.
Keep normal UI free of raw GUIDs.

9. Recovery / Pipeline

Phase: P09

Last Architect-accepted: TB-P09-T021-R1

TB-P09-T022 under repair

Current Repair: TB-P09-T022-R1

Last Implementation on completion: TB-P09-T022-R1

USER_VISUAL_ACCEPTED=NO

Worker Next State: IDLE

do NOT invent/start TB-P09-T023

Run recovery guard.

10. Git

Required:

preserve user-authored changes

commit + push if changes needed

HEAD==origin/main

no task-owned uncommitted changes

Acceptance

PASS only if ALL:

exact FE 500 root cause identified

real Admin Order Detail route returns 200

Consolidated Package section renders on real runtime

single/multi-seller visibility correct

create/rebuild/dispatch/deliver UI renders correctly

member lock explanation visible

customer tracking view renders without 500

no raw GUID/enum/technical crash leakage

no broad UI redesign

T022 technical regressions remain green

frontend build/typecheck green

browser/runtime smoke complete

recovery guard passes

HEAD==origin/main

Worker IDLE

no TB-P09-T023 invented

USER_VISUAL_ACCEPTED=NO.

Result Contract

Return ONLY canonical BRIDGE-WAKE-V1 result with:

Summary
Applicable-Locks
FE500-Discovery
FE500-Fix
Admin-Runtime
Customer-Runtime
Visual-Smoke
Error-UX
Focused-Validation
Recovery-SoT
Git
Architectural-Concerns
Visual-Concerns
Blockers

END_TOOBA_REPAIR_TASK