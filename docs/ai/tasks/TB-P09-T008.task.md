PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_TASK

Task-ID:
TB-P09-T008

Parent-Task:
TB-P09-T007

Channel:
tooba-main

WorkerId:
tooba-worker-01

AgentType:
cursor

Status:
ISSUED

USER_VISUAL_ACCEPTED=NO

Title:
Shipping & Delivery Operations Center — Cross-Order Work Queue on Existing Fulfillment Backend

Architect Decision

TB-P09-T007 is ACCEPTED.

Accepted:

Order-attributable financial history

customer receipt/refund + seller payout/adjustment projection

scope-aware operational history

compact history filters

no new history page

current Order Detail UI preserved

This task implements the separate Admin menu area ارسال و تحویل as the operational work queue across Orders.

This is NOT a second fulfillment system.

It must reuse the same seller/line/quantity/shipment backend rules already accepted in T004–T007.

Speed / Validation Lock

User explicitly wants faster delivery.

Run ONLY necessary focused tests for touched code plus:

focused build/typecheck

recovery guard

git diff --check

short runtime smoke

Do NOT replay the full Orders lifecycle from zero.
Do NOT rerun unrelated T001–T007 suites.

Product Role — Locked

سفارش‌ها و پرداخت
= detail + operations for one Order

ارسال و تحویل
= cross-order operational queue for all seller fulfillment/shipment work

Both MUST use the SAME backend/domain commands.

No duplicate lifecycle rules.
No second shipment model.
No second fulfillment aggregate.

1. Mandatory Discovery

Inspect:

current Admin sidebar ارسال و تحویل

any existing shipping/delivery page

current Fulfillment queries

seller-group/line/quantity DTOs from T005/T006

shipment actions/projections

AppDataGrid usage

bulk-selection patterns

current permissions

current filters/saved views

Reuse existing patterns.

If an existing page already partially implements this role:
extend it; do not replace blindly.

2. Main Work Queue

Create/complete one operational grid centered on shipment/fulfillment work.

The grid should be based on authoritative operational rows such as Shipment / seller fulfillment group, not raw Customer Order only.

At minimum show useful columns:

سفارش

فروشنده

مرسوله

اقلام / تعداد

روش ارسال

وضعیت عملیاتی

کد رهگیری

گیرنده

مقصد / شهر if available

زمان ایجاد / آخرین تغییر

عملیات

Keep user-resizable columns.

Do NOT set restrictive maxWidth values that prevent manual expansion.

Horizontal scroll must work when needed.

Use canonical AppDataGrid.
No raw AgGridReact.

3. Operational Quick Filters

Provide compact operational filters/tabs such as:

همه

نیازمند اقدام

آماده پردازش

آماده بسته‌بندی

آماده ارسال

بدون کد رهگیری

در مسیر

تحویل‌شده

مشکل‌دار

Only include states that map cleanly to current backend semantics.

Do NOT invent fake status values.

Filters must be server-side / query-backed where current grid architecture supports it.

4. Seller / Shipping Method Filters

Support filtering by:

seller

shipping method/provider

current operational state

Reuse existing filter patterns.

No client-only filtering over a small page of server data when server-side grid filtering already exists.

5. Bulk Operations — Same Backend Commands

Allow selecting multiple eligible rows/shipments for fast operational work.

Examples where valid:

بسته‌بندی

ایجاد مرسوله

ثبت/assign tracking if the operation naturally supports bulk input

ارسال

ثبت تحویل

Rules:

bulk operation is only available when ALL selected rows are compatible

seller/method constraints respected

no cross-seller shipment creation

no invalid mixed-state batch

backend revalidates every row/quantity

partial success behavior must follow existing project convention; do not invent silent partial mutation

If a bulk action cannot be safely supported yet, do not expose it.

6. Row Actions

Each row has:

View / open related Order detail

compact contextual operations menu

only backend-projected valid actions

No duplicate giant عملیات text button.

Use the compact kebab style already accepted in Orders grid.

7. Action Projection

Reuse T005/T006 authoritative action projection.

The Shipping & Delivery page must not recreate eligibility rules in frontend.

Examples:

Ready -> pack

Packed -> create shipment / reverse packing if allowed

Shipment Created -> tracking / cancel shipment

Dispatched -> deliver

Delivered -> read-only from fulfillment perspective

Return/Refund remains in Order/Return workflow, not a shipment reverse.

8. Multi-Order / Multi-Seller Safety

Required:

one Shipment remains one seller

selection cannot create a mixed-seller Shipment

line/quantity allocations remain bounded

no over-allocation

no operation can mutate another seller's fulfillment

Backend authoritative.

9. Shipping Method Awareness

Grid must display human method labels:

پست

تیپاکس

اسنپ / پیک آنلاین

پیک فروشگاه

تحویل حضوری

Do not show provider codes as primary labels.

Existing provider-specific metadata may be available in row detail/drawer if useful, but:

no raw JSON

no secrets

no heavy redesign

10. Detail Interaction

Prefer keeping user in the work queue.

For quick operational detail:

use existing drawer/modal pattern if needed

For full Order context:

open canonical Order Detail via View

Do NOT create page-inside-page navigation.
Do NOT duplicate the full Order Detail inside Shipping & Delivery.

11. Problem / Attention State

نیازمند اقدام / مشکل‌دار should be based on real conditions only.

Examples where authoritative data supports:

missing required tracking before dispatch

failed/blocked shipment operation

invalid/expired carrier state if modeled

shipment waiting beyond configured threshold only if such rule exists

Do NOT invent arbitrary SLA timers in this task.

12. Permission Model

Reuse existing:

fulfillment.manage

order.view

seller/tenant scoping

relevant shipping permissions

View-only:

can inspect queue if current capability allows

no mutation actions

Operational user:

sees only permitted actions

Backend authoritative.

13. RTL / LTR

FA:

full RTL

human Persian statuses

EN:

full LTR

human English labels

Tracking numbers/technical references may render LTR locally without breaking page direction.

14. Error UX

Human FA/EN messages for:

incompatible selection

cross-seller selection

invalid state

shipment already dispatched

tracking missing/duplicate

over-allocation

permission denied

No raw codes/JSON/DB errors.

15. Performance

This page may contain many rows.

Required:

server-side pagination/filter/sort

no N+1

bounded action-projection strategy

no per-row broad module fan-out

saved column state remains functional

scoped refresh after action, not full app reload

16. Focused Tests — ONLY NECESSARY

Backend:

work queue query filters

seller scoping

state filtering

action projection reuse

incompatible bulk selection rejected

cross-seller shipment creation rejected

bulk action validates each row

no over-allocation regression

Frontend:

AppDataGrid used

quick filters work

columns resizable / horizontal scroll

compact row kebab

View opens Order Detail

bulk actions only on compatible selection

human provider/status labels

RTL regression

Also:

focused build/typecheck

recovery guard

git diff --check

No unrelated full suite.

17. Runtime Smoke — SHORT

Use:
Backend: http://127.0.0.1:5088
Frontend: http://127.0.0.1:3000

One existing local dataset is enough.

Verify:

Shipping & Delivery page opens

multiple operational rows visible

quick filter changes result

seller/method filter works

select compatible rows -> valid bulk action appears

incompatible selection -> blocked/no invalid action

row kebab shows correct backend actions

one real action executes and row refreshes

View opens canonical Order Detail

widths/horizontal scroll work

Do NOT replay full Order lifecycle.

18. User UI Preservation

Do not alter accepted Orders UI while implementing this page.

Specifically preserve:

Orders grid

Order Detail

financial tabs

اقلام و ارسال

notes/history

user width/horizontal-scroll refinements

Only ارسال و تحویل should materially change in this task.

19. Evidence

Create:
docs/evidence/TB-P09-T008/

Required concise evidence:

discovery.md

work-queue.md

filters.md

bulk-actions.md

action-projection.md

permissions.md

performance.md

runtime-smoke.md

ui-preservation.md

recovery-sot.md

Keep evidence concise.

20. Recovery / Pipeline

Update:

Phase: P09

Last Architect-accepted: TB-P09-T007

Current Issued: TB-P09-T008

Current Repair: none

Last Implementation on completion: TB-P09-T008

USER_VISUAL_ACCEPTED=NO

Worker Next State: IDLE

do NOT invent/start TB-P09-T009

Run recovery guard.

21. Git

Required:

preserve user-authored changes

commit task changes

push origin/main

HEAD==origin/main

no task-owned uncommitted changes

Acceptance

PASS only if ALL:

ارسال و تحویل functions as a cross-order operational work queue

same fulfillment backend/domain commands reused

no duplicate lifecycle logic

AppDataGrid used

quick filters are authoritative

seller/method/state filtering works

compatible bulk actions work

incompatible/cross-seller actions rejected

compact row operations menu

human method/status labels

server-side pagination/filter/sort

no N+1

accepted Orders UI remains unchanged

only necessary focused tests run

short runtime smoke complete

recovery guard passes

HEAD==origin/main

Worker IDLE

no TB-P09-T009 invented

USER_VISUAL_ACCEPTED=NO.

Result Contract

Return ONLY canonical BRIDGE-WAKE-V1 result with:

Summary
Discovery
Work-Queue
Quick-Filters
Seller-Method-Filters
Bulk-Actions
Row-Actions
Action-Projection
Multi-Seller-Safety
Permissions
RTL-LTR
Error-UX
Performance
Focused-Validation
Runtime-Smoke
User-UI-Preservation
Recovery-SoT
Git
Architectural-Concerns
Visual-Concerns
Blockers

END_TOOBA_TASK