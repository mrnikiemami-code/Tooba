# Tooba — TB-P01-T001 — Platform Foundation Bootstrap

BEGIN_TOOBA_CURSOR_TASK_V1

Protocol-Version: 1
Task-ID: TB-P01-T001
Phase: P01 — Platform Foundation
Type: Implementation / Repository & Application Skeleton
Repository: https://github.com/mrnikiemami-code/Tooba
Primary-Branch: main
Implementation-Agent: Cursor
Architect: ChatGPT
Execution-Mode: PIPELINE
Depends-On: TB-P00-GATE
Architect-Decision-On-Dependency: ACCEPTED

## Objective

Start the first implementation phase of Tooba by creating the minimum production-grade application skeleton that all later work can safely build on.

This task is intentionally narrow.

Implement only:

```text
repository/application skeleton
backend modular-monolith skeleton
frontend Next.js shell skeleton
shared configuration/bootstrap conventions
basic solution/build verification
durable P01 source-of-truth transition
```

Do NOT implement business features.

Do NOT implement Identity, Catalog, Pricing, Tax, Cart, Order, Payment, Search, UI workspaces, Data Grid, Design System, or Shopeiva adaptation in this task.

---

## 1. Repository Recovery

Before edits:

```bash
git rev-parse --show-toplevel
git fetch origin
git branch --show-current
git rev-parse HEAD
git rev-parse origin/main
git status --short --branch
```

Require:

```text
branch = main
HEAD == origin/main
```

Expected predecessor:

```text
ae113bd151b64686f7f376613bfd7abdf55bc7e2
```

If synchronized `main` legitimately advanced, continue only from current safe synchronized `main`.

Unsafe or ambiguous repository state =>

```text
RECOVERY_CONFLICT
```

Never:

```text
force push
rewrite history
destructive reset
silent stash
delete unknown work
commit unrelated work
import Shopeiva
modify external reference files
```

---

## 2. P00 Closure / P01 Transition

Update durable SoT so that it clearly records:

```text
TB-P00-GATE = ARCHITECT ACCEPTED
P00 = COMPLETE
P01 = IN_PROGRESS
TB-P01-T001 = ISSUED / AWAITING_ARCHITECT_ACCEPT
```

P01 title:

```text
Platform Foundation
```

Do not mark P01 complete.

---

## 3. Read Before Implementing

Read at minimum:

```text
AGENTS.md
docs/PROJECT-STATE.md
docs/ROADMAP.md
docs/ai/TOOBA-PIPELINE-PROTOCOL.md
docs/ai/TOOBA-PIPELINE-CONTROLLER.md
docs/ai/TOOBA-RECOVERY-CONTEXT.md
docs/prompts/START-HERE-IF-CHATGPT-IS-LOST.md
docs/architecture/01-capability-domain-map.md
docs/architecture/02-edition-tenant-deployment.md
docs/architecture/03-data-ownership-and-module-contracts.md
docs/architecture/18-observability-logging-audit.md
docs/architecture/19-caching-infrastructure-abstractions.md
docs/architecture/20-frontend-ux-template-adaptation.md
docs/architecture/27-p00-gate-review.md
```

Repository remains Source of Truth.

---

## 4. Detect Existing Application State

Before creating files, inspect the repository.

Determine whether backend/frontend skeleton already exists.

Report:

```text
existing solution/project files
existing package.json files
existing src/apps folders
existing build tooling
existing test projects
```

Hard rule:

Do not blindly overwrite an existing application skeleton.

If an existing structure conflicts materially with accepted P00 architecture:

```text
BLOCKED
```

and report it.

If structure exists and is compatible, extend minimally.

---

## 5. Backend Direction

Use the accepted stack direction from repository/technical inventory.

Unless repository SoT says otherwise, backend should be prepared as:

```text
ASP.NET Core
Modular Monolith
PostgreSQL-ready
OpenTelemetry-ready
```

This task does NOT install/configure the full database or OpenTelemetry pipeline unless already required to make the bootstrap coherent.

The backend skeleton should make module ownership visible.

A preferred conceptual shape is:

```text
src/
  backend/
    Tooba.sln
    BuildingBlocks/
    Host/
    Modules/
```

Exact names may follow repository conventions if better.

Do not create one project per theoretical class.

Do not create all domain modules fully.

Create only enough structure to demonstrate:

```text
Host
BuildingBlocks / shared technical primitives
Modules boundary
one minimal sample/placeholder module boundary only if necessary for build validation
```

Avoid speculative implementation.

---

## 6. Module Boundary Rule

No module may directly reference another module's persistence internals.

At skeleton level, preserve future layering such as:

```text
Domain
Application
Infrastructure
Contracts
```

but do not generate dozens of empty projects unless justified.

Prefer a lean bootstrap that can grow deliberately.

Document the chosen project/folder convention.

---

## 7. Backend Host

Create a minimal runnable backend Host.

Required:

```text
application starts
health/readiness endpoint or equivalent minimal host verification
environment configuration loads
structured logging foundation is not contradicted
```

Do NOT expose business APIs yet.

Do NOT build authentication yet.

---

## 8. PostgreSQL Readiness

Do not build domain schemas.

Preserve configuration seam for PostgreSQL.

No SQL Server assumptions.

No SQLite substitution as architecture truth.

If package installation is necessary for the chosen foundation, keep it minimal.

Do not create migrations yet unless absolutely required for bootstrap, which is not expected.

---

## 9. Configuration Foundation

Create coherent environment/configuration organization.

Preserve future dimensions:

```text
Edition
Tenant
Database
Observability
Cache
External providers
```

Do NOT yet implement tenant resolution.

No secrets committed.

Provide development-safe sample configuration only.

---

## 10. Frontend Direction

Create or normalize a minimal frontend application shell using accepted direction:

```text
Next.js App Router
React
TypeScript
Tailwind CSS
Server Component First
```

Do NOT migrate Shopeiva.

Do NOT copy Shopeiva components.

Do NOT build commercial UI yet.

The frontend shell should only prove:

```text
app starts
basic root layout
basic route
build succeeds
TypeScript/lint baseline
```

If a compatible Next.js app already exists, do not recreate it.

---

## 11. Frontend Architecture Guardrails

Even in skeleton form preserve:

```text
Server Component First
Client Components only where interaction requires them
no giant global client store
no UI coupled directly to backend DB/module internals
```

Do not invent feature folder structure beyond what is needed.

---

## 12. Shopeiva Rule

This task MUST NOT perform Deep Shopeiva Study.

This task MUST NOT import Shopeiva.

This task MUST NOT migrate template code.

The mandatory future sequence remains:

```text
Deep Shopeiva Study
→ REUSE / ADAPT / REBUILD / DROP / DEFER map
→ Design System extraction
→ Data Grid foundation
→ Workspace interaction patterns
→ serious UI implementation
```

This sequence must remain durable in ROADMAP.

---

## 13. Professional Data Grid Rule

Do NOT implement Data Grid here.

But ensure no bootstrap decision makes the future mandatory professional reusable Data Grid difficult.

Data Grid future requirements remain:

```text
typed filters
sorting
reorder
resize
show/hide
saved views
pagination
selection
bulk actions
export
sticky behavior
keyboard accessibility
RTL/LTR
responsive strategy
server-side large dataset handling
```

---

## 14. Repository Layout Documentation

Create:

```text
docs/architecture/28-platform-foundation-bootstrap.md
```

Document:

```text
chosen repository/application layout
backend project/folder purpose
frontend project/folder purpose
module-boundary convention
configuration convention
build/run commands
what was intentionally NOT implemented
how this skeleton preserves P00 architecture
```

This is not an ADR.

---

## 15. Developer Experience

Provide simple root-level commands or clear documentation for:

```text
backend restore/build/run
frontend install/build/run
```

Prefer existing repository tooling if present.

Do not introduce heavyweight orchestration unless necessary.

No Docker requirement in this task unless the repository already depends on it.

---

## 16. Build Quality

Backend:

```text
restore succeeds
build succeeds
```

Frontend:

```text
dependency install succeeds
typecheck/build succeeds
lint succeeds if lint is configured
```

Do not suppress compiler/linter errors with broad ignores.

---

## 17. Tests

Do not create fake unit tests just to have tests.

If bootstrap includes a meaningful minimal smoke/integration test convention, it may be added.

Otherwise document:

```text
no business tests yet because no business implementation exists
```

Build verification is mandatory.

---

## 18. Security Baseline

Ensure:

```text
no committed secrets
no debug credentials
no permissive CORS wildcard introduced without reason
no sample admin account
no hard-coded tenant
no hard-coded database password
```

Do not implement security features beyond baseline.

---

## 19. No Premature Infrastructure

Do NOT implement yet:

```text
Redis
Elasticsearch/OpenSearch
SpiceDB integration
message broker
full outbox
background scheduler
carrier/payment providers
AI provider
email/SMS provider
```

P01 will introduce technical foundations incrementally through later scoped tasks.

Architecture seams may be documented; implementation waits.

---

## 20. SoT Updates

Update:

```text
docs/PROJECT-STATE.md
docs/ROADMAP.md
docs/ai/TOOBA-RECOVERY-CONTEXT.md
```

Expected:

```text
Last Architect Accepted Work: TB-P00-GATE
Current Issued Task: TB-P01-T001
Current Phase: P01 — Platform Foundation
Task State: AWAITING_ARCHITECT_ACCEPT
P00: COMPLETE
P01: IN_PROGRESS
```

Durably preserve future mandatory UI packages:

```text
Deep Shopeiva Study
Template reuse map
Design System extraction
Professional Data Grid foundation
Workspace interaction patterns
Visual acceptance gates
```

---

## 21. Save Complete Envelope Verbatim

Save this exact task to:

```text
docs/ai/tasks/TB-P01-T001.task.md
```

Verify markers:

```text
BEGIN_TOOBA_CURSOR_TASK_V1
END_TOOBA_CURSOR_TASK_V1
```

---

## 22. Explicit Out of Scope

Do NOT implement:

```text
Identity
Party
SpiceDB
Tenant resolution
Catalog
Seller
Offer
Pricing
Tax
Promotion
Inventory
Cart
Checkout
Order
Payment
Fulfillment
Returns
Reviews
Content
Search
Media
Analytics
AI/RAG
Notifications
Admin
Seller dashboard
Customer dashboard
Data Grid
Design System
Shopeiva migration
Deep Shopeiva Study
```

Do NOT issue T002.

Do NOT issue a P01 Gate.

---

## 23. Validation

Run appropriate commands for actual created structure.

At minimum:

```bash
git diff --check
git status --short --branch
```

Backend validation should include actual restore/build.

Frontend validation should include actual install/build/typecheck/lint where configured.

Manual checks:

```text
P00 marked COMPLETE only because Architect accepted Gate
P01 IN_PROGRESS
T001 awaiting Architect acceptance
backend skeleton runs/builds
frontend shell builds
no business feature implementation
no Shopeiva import
no Data Grid implementation
no secrets
no cross-module coupling
ROADMAP retains deep template/Data Grid mandatory future work
```

---

## 24. Git Commit & Push

Suggested commit:

```text
feat bootstrap Tooba platform foundation [TB-P01-T001]
```

Then:

```bash
git push origin main
git fetch origin
git rev-parse HEAD
git rev-parse origin/main
git status --short --branch
```

Require:

```text
HEAD == origin/main
```

No force push.

---

## 25. Result Contract

Return exactly:

```text
BEGIN_TOOBA_CURSOR_RESULT_V1

Protocol-Version: 1
Task-ID: TB-P01-T001
Phase: P01 — Platform Foundation
Status: PASS | BLOCKED | RECOVERY_CONFLICT

Summary:
...

Repository-Recovery:
- Repo-Root:
- Branch:
- Starting-HEAD:
- Starting-Origin-Main:
- Starting-Status:

Existing-State-Detected:
- Backend:
- Frontend:
- Build tooling:
- Conflicts:

Changes:
- ...

Repository-Layout:
- ...

Backend-Foundation:
- ...

Frontend-Foundation:
- ...

Configuration:
- ...

Architecture-Guardrails:
- ...

Shopeiva-DataGrid-Guardrails:
- ...

Validation:
- backend restore:
- backend build:
- frontend install:
- frontend typecheck:
- frontend lint:
- frontend build:
- git diff --check:
- manual checks:

Git:
- Commit:
- Push:
- Final-HEAD:
- Final-Origin-Main:
- Final-Status:
- Head-Matches-Origin: YES | NO

Source-of-Truth:
- P00-State:
- Last-Architect-Accepted-Work:
- Current-Issued-Task:
- Current-Phase:
- Task-State:
- Recovery-Ready: YES | NO

Architectural-Concerns:
- ...

Blockers:
- ...

END_TOOBA_CURSOR_RESULT_V1
```

---

## 26. Pipeline Continuity — MANDATORY

After RESULT:

```text
WAITING
```

Remain inside the Tooba Architect-controlled pipeline.

Do not leave PIPELINE mode.

Do not invent, infer, prepare, or execute:

```text
TB-P01-T002
P01 Gate
Identity
Tenant
database implementation
OpenTelemetry implementation
Shopeiva study
Data Grid
Design System
UI implementation
```

without a new valid Architect envelope.

Cursor PASS is not Architect ACCEPT.

After RESULT, stop execution and remain:

```text
WAITING
```

---

## 27. UI / UX Protection — MANDATORY PROJECT RULE

Tooba is a commercial product.

Weak UI/UX is considered product failure.

This bootstrap task does not implement commercial UI, but it must not create architectural obstacles for the later mandatory UX foundations.

Hard rules remain:

```text
Backend/module boundary != UI boundary
Build PASS != UI ACCEPT
Functional PASS != Visual ACCEPT
Desktop PASS != Mobile PASS
LTR PASS != RTL PASS
```

Mandatory future UI sequence remains durable:

```text
Deep Shopeiva Study
Design System extraction
Professional reusable Data Grid
Workspace interaction patterns
Product/Order/Seller/Customer workflows
Visual evidence
Architect visual ACCEPT
```

END_TOOBA_CURSOR_TASK_V1
