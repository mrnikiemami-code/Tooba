# TB-P09-T005-R1 Result

Status: PASS

PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

## Summary

Verification-first repair: completed missing live runtime matrix for seller-scoped line/qty fulfillment without UI rework. No product code defects requiring fix. USER_VISUAL_ACCEPTED=NO. Worker IDLE. No TB-P09-T006.

## Runtime-Partial-Quantity

Line `…ae0e…` ordered=5; pack 2 → packed=2 remaining=3; later pack remaining → packed=5. Evidence: `r1-runtime-partial-quantity.md`.

## Runtime-Reverse-Packing

`unpack` projected while unallocated; unpack 2 → packed=0 status Processing; repack for next scenarios. Evidence: `r1-runtime-reverse-pack.md`.

## Runtime-Shipment-Subset

Shipment `d8fee687-…` Created with only qty=2 of line `…ae0e…`; sibling lines remain unallocated. Evidence: `r1-runtime-shipment-allocation.md`.

## Runtime-Allocation-Guard

Fully allocated line unpack → HTTP 400 FA «بازگشت از بسته‌بندی برای تعداد تخصیص‌یافته یا ارسال‌شده مجاز نیست.» State unchanged.

## Runtime-Shipment-Cancel-Release

Pre-dispatch cancel → Cancelled (auditable); allocations released; replacement shipment `8fdd9430-…` created. Evidence: `r1-runtime-shipment-cancel-release.md`.

## Runtime-Dispatch-Irreversibility

Tracking + dispatch OK; post-dispatch cancel_shipment / unpack rejected with human FA. Evidence: `r1-runtime-dispatch-irreversibility.md`.

## Runtime-Delivery

`deliver_shipment` → Delivered; lineShipped=2; fulfillment InTransit (partial).

## Runtime-Return-Deadline

After deliver: isReturnable=true, window=7, deadline `تا ۱۴۰۵/۰۶/۲۳`, remaining `۷ روز باقی‌مانده`, policy `۷ روز پس از تحویل`. Evidence: `r1-runtime-return-deadline.md`.

## Status-Humanization

FA: ReadyToFulfill→آماده پردازش; Packed→بسته‌بندی‌شده; Dispatched→ارسال‌شده; Delivered→تحویل‌شده.

## Operations-Projection

Stage captures: before pack mark_*; packed unpack+create_shipment; Created cancel_shipment+assign_tracking; after dispatch deliver; cancel absent; request_return after deliver.

## Cancellation-Regression

`cancel` after dispatch/deliver → `order.cancel.forbidden` HTTP 400.

## User-UI-Preservation

Grid flex/scroll, actions widths/filters, items overflow-x-auto preserved. Evidence: `r1-ui-preservation.md`.

## Focused-Validation

- BE focused: 25 passed
- FE admin/grid/fulfillment: 32 passed
- recovery-staleness.guard: 3 passed
- git diff --check: clean on staged paths

## Runtime-Visual-Smoke

FE `/admin/orders` + detail 200; Host ops matrix executed live on Paid multi-seller checkout.

## Recovery-SoT

Architect T004; Repair TB-P09-T005-R1; Implementation TB-P09-T005-R1; Issued (none); USER_VISUAL_ACCEPTED=NO.

## Git

Evidence/SoT commit on main; HEAD==origin/main.

## Architectural-Concerns

Seeded simple-order dispatch can hit inventory reservation mismatch; multi-seller path proved dispatch/deliver. Offer editor still deferred.

## Visual-Concerns

USER_VISUAL_ACCEPTED=NO.

## Blockers

None.
