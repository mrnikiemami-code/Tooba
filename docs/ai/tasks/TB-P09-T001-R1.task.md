PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_REPAIR_TASK

Task-ID:
TB-P09-T001-R1

Parent-Task:
TB-P09-T001

Channel:
tooba-main

WorkerId:
tooba-worker-01

AgentType:
cursor

Status:
REPAIR

USER_VISUAL_ACCEPTED=NO

Title:
Order Operations Foundation Repair — Domain Guard Hardening + Real Post-Settlement Return Runtime Proof

Why Repair Is Required

TB-P09-T001 delivered the intended UI and architecture with minimal UI change, but two acceptance gaps remain:

SellerOrder.Cancel is reported as weakly guarded at domain level.
Host/composer/UI projection is NOT sufficient as the authoritative safety boundary.
Invalid lifecycle transitions must be rejected by the domain/application path itself.

The required seller-settled return scenario was not exercised end-to-end in runtime.
The Result explicitly says settlement-independence was covered by evaluator design + tests/evidence.
The acceptance criteria required a real runtime smoke proving:

return remains eligible after seller settlement when policy allows

completed settlement remains immutable

an idempotent seller debit/adjustment is created

future payable reflects that adjustment

This repair must address ONLY these gaps.

Do NOT redesign Orders UI.
Do NOT add new operations.
Do NOT create new modules/subsystems.

Mandatory Discovery Before Changes

Inspect current implementation from TB-P09-T001:

SellerOrder aggregate cancel transition

admin order operation execution path

fulfillment/order lifecycle guards

ReturnEligibilityEvaluator

ReturnDirectory create/approve flow

Settlement AdjustFromRefundAsync / PostDebitFromRefund

Payment refund gateway

existing tests/evidence

Follow current CQRS/module conventions.

Do NOT:

add business logic to endpoint

create a second guard layer with duplicated rules

create a new ledger

create a new workflow engine

create cross-module SQL JOINs

1. Domain/Application Cancel Guard Hardening

The authoritative mutation path must reject invalid cancellation regardless of caller/UI.

Required:

identify the canonical cancellation transition owner

enforce valid/invalid cancellation based on current Order lifecycle and existing business rules

direct command/API calls must not bypass the rule

UI/composer may still project/hide action, but domain/application remains authoritative

return stable machine code for invalid cancel

map to human FA/EN message

Do not blindly add guards in multiple layers.
Prefer one canonical domain/application invariant source.

Focused tests must include:

cancel allowed state

cancel forbidden state

repeated cancel/idempotent behavior according to current domain policy

direct operation call cannot bypass lifecycle rule

2. Real Seller-Settled Return Runtime Scenario

Create/use deterministic development data in the current dev DB only.

Required runtime scenario:

Find/create an Order that is:

paid

delivered

still inside return window

seller settlement already completed

Confirm before return:

ReturnEligibility = eligible

completed seller settlement exists

capture settlement immutable identifiers/amounts for comparison

Create valid return.

Approve return/refund through existing proper flow.

Verify:

completed prior settlement record is NOT modified/deleted

return/refund completes through existing Return + Payment path

exactly ONE seller debit/adjustment/receivable entry is created

adjustment amount matches seller-side refundable economic impact according to existing settlement policy

retry/repeated processing does NOT create a duplicate adjustment

seller future payable/balance calculation reflects the debit

Capture concise before/after evidence.

No fake direct SQL mutation to manufacture the final result.
Use existing application/domain APIs/commands for business transitions.

Direct DB reads for verification are acceptable if repository evidence conventions allow, but mutation must use the application/domain path.

3. Settlement Immutability Assertion

Add focused automated coverage proving:

completed settlement remains immutable after refund adjustment

adjustment is additive, not settlement rewrite

retry does not duplicate debit

return eligibility does not inspect settlement state as a gating rule

4. Refund / Adjustment Failure Safety

Verify what happens if:

refund succeeds but adjustment posting retries

adjustment already exists

callback/command is retried

Use existing idempotency/source keys.

Do NOT invent distributed transaction architecture in this repair.

If current architecture already uses outbox/eventual consistency, preserve it and test the existing mechanism.

5. Minimal UI Regression

No new UI required.

Quick regression only:

Orders grid View still present

contextual operations menu still present

Order detail عملیات سفارش still present

invalid cancel hidden where composer can know it

backend still rejects invalid direct call

6. Focused Validation

Backend:

cancel guard tests

return eligibility tests

post-settlement return adjustment tests

settlement immutability

duplicate adjustment/idempotency

refund retry safety

Frontend:

minimal operations menu regression only

Also:

relevant compile/typecheck/lint

recovery guard

git diff --check

No unrelated full repository suite.

7. Runtime Smoke — REQUIRED

Use:

Backend:
http://127.0.0.1:5088

Frontend:
http://127.0.0.1:3000

Must actually execute and document:

one forbidden cancel direct call -> rejected

one seller-settled delivered Order inside return window

return request + approve/refund

immutable old settlement

new debit adjustment

no duplicate adjustment on retry

future payable/balance reflects adjustment

If this runtime scenario cannot be completed due to missing deterministic data or an existing architectural gap:
return BLOCKED/PARTIAL with exact cause.
Do NOT claim PASS from tests alone.

8. Recovery / Pipeline

Update:

Phase: P09

Last Architect-accepted baseline: TB-P08-T016-R5

TB-P09-T001 under repair

Current Repair: TB-P09-T001-R1

Last Implementation on completion: TB-P09-T001-R1

USER_VISUAL_ACCEPTED=NO

Worker Next State: IDLE

do NOT invent/start TB-P09-T002

Run recovery guard.

9. Evidence

Create/update:

docs/evidence/TB-P09-T001/

Required concise repair evidence:

r1-cancel-domain-guard.md

r1-settled-return-runtime.md

r1-settlement-immutability.md

r1-adjustment-idempotency.md

r1-recovery-sot.md

10. Git

Required:

preserve user-authored changes

commit repair

push origin/main

HEAD==origin/main

no task-owned uncommitted changes

Acceptance

PASS only if ALL:

invalid cancellation is rejected by authoritative domain/application logic

UI projection is not the only lifecycle guard

direct invalid cancel call is rejected

real runtime seller-settled return scenario completed

seller settlement does NOT gate otherwise-valid return

completed settlement remains unchanged

one and only one seller debit/adjustment is created

adjustment is idempotent on retry

future seller payable/balance reflects adjustment

refund path remains inside existing Payment/Returns architecture

no duplicate subsystem created

minimal Orders UI unchanged

focused validation green

recovery guard passes

HEAD==origin/main

Worker returns IDLE

no TB-P09-T002 invented

USER_VISUAL_ACCEPTED=NO.

Result Contract

Return ONLY canonical BRIDGE-WAKE-V1 result with:

Summary
Cancel-Guard-Root-Cause
Cancel-Guard-Repair
Settled-Return-Runtime
Settlement-Immutability
Financial-Adjustment
Adjustment-Idempotency
Refund-Retry-Safety
Minimal-UI-Regression
Focused-Validation
Runtime-Smoke
Recovery-SoT
Git
Architectural-Concerns
Visual-Concerns
Blockers

END_TOOBA_REPAIR_TASK
