PIPELINE-PROTOCOL: BRIDGE-V2

TASK-ID: TB-P05-T012
PROJECT: Tooba
PHASE: P05 — Operational Surface Integration
CHANNEL: tooba-main
STATUS: ISSUED
TASK-TYPE: PRODUCT IMPLEMENTATION
WORKER-POLICY: ONE WORKER = ONE ACTIVE TASK

# Reviews & Ratings — Add Missing Backend Capability Behind Existing Shopeiva Review UI

TB-P05-T011 is Architect-accepted.

## Locked convergence rule

```text
Backend capability exists but Shopeiva lacks UI
→ add the minimum Shopeiva-compatible UI

Shopeiva meaningful capability exists but Backend lacks capability
→ add the missing Backend capability in the correct owning module
→ connect the existing Shopeiva UI to live backend truth
```

No redesign. Reuse the actual Shopeiva PDP review/rating section. Implement the
smallest production-grade Reviews/Ratings capability with:

- dedicated architecture-consistent ownership;
- authenticated actor-owned submission;
- integer rating scale 1–5 and server validation;
- one review per customer per product;
- Pending → Published / Rejected moderation lifecycle;
- server-authorized Admin moderation queue;
- Published-only public list, average, count and useful distribution;
- truthful verified-purchase proof through an Order-owned gateway when safe;
- real product-card rating only when count is greater than zero;
- AggregateRating JSON-LD only for real Published reviews;
- deterministic, idempotent Development-only reviews for published, zero and
  pending visual states.

Forbidden: cross-module SQL, foreign DbContext navigation, Product.Reviews ORM
collection, frontend-calculated aggregate authority, request-supplied actor
authority, fake stars/counts/verified badges/discounts, public pending/rejected
content or private identity.

## Evidence

Create `docs/evidence/TB-P05-T012/`:

1. `01-review-domain-decision.md`
2. `02-shopeiva-review-backend-convergence.md`
3. `03-pdp-rating-summary-desktop.png`
4. `04-pdp-published-reviews-desktop.png`
5. `05-pdp-review-form.png`
6. `06-pdp-zero-review-state.png`
7. `07-admin-review-moderation.png`
8. `08-admin-review-denied.png`
9. `09-pdp-reviews-mobile-390x844.png`
10. `10-product-card-real-rating.png`
11. `11-review-seed-proof.md`
12. `12-review-data-map.md`
13. `13-review-seo-proof.md`
14. `14-shopeiva-review-fidelity.md`

## Validation

Run unfiltered backend restore/build/tests with zero warnings/errors/failures/
skips. Run frontend `npm ci`, typecheck, lint, all repository-supported suites,
build, and `git diff --check`.

## Source of Truth

Before Architect review:

```text
PIPELINE = BRIDGE-V2
TB-P05-T010 = ACCEPTED
TB-P05-T011 = ACCEPTED
TB-P05-T012 = AWAITING_ARCHITECT_ACCEPT
P05 = IN_PROGRESS
```

Commit:

```text
feat add live product reviews ratings behind Shopeiva UI [TB-P05-T012]
```

Return the complete `BEGIN_TOOBA_WORKER_RESULT` through Bridge. Worker PASS is
not Architect ACCEPT. Do not self-issue another Task.

END_TASK
