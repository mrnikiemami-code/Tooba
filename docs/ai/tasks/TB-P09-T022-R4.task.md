PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_REPAIR_TASK

Task-ID:
TB-P09-T022-R4

Parent-Task:
TB-P09-T022-R3

Channel:
tooba-main

WorkerId:
tooba-worker-01

AgentType:
cursor

Status:
REPAIR

USER_VISUAL_ACCEPTED=NO

Title:
Paid-Order Reservation Runtime Proof — Real TTL Expiry + Dispatch + Cancel/Restore Verification

Architect Review

TB-P09-T022-R3 is functionally convincing and the implementation direction is correct:

paid-order reservation commit clears ExpiresAt

Inventory owns the commit

expiry worker naturally skips committed rows

Dispatch Consume invariant remains strict

Released reservations are not resurrected

Restore already reacquires with durable ExpiresAt = null

focused tests are green

HEAD==origin/main

However, the R3 task explicitly required REAL runtime smoke and the Result states:

Host stopped for rebuild locks; proof from focused tests

That does NOT satisfy the runtime acceptance contract.

This repair is ONLY to perform and document the missing real runtime proof.

Do NOT change Consolidated Package.
Do NOT change shipping-method behavior.
Do NOT redesign Inventory.
Do NOT alter the already-passing reservation implementation unless runtime reveals a real defect.
Do NOT start TB-P09-T023.

Applicable Locks — READ FIRST

Read:
docs/architecture/TOOBA-LOCKS.md

Preserve LOCK-OPS-024..028 and all prior reservation/restore locks.

1. Start Real Runtime

Run the actual application stack used for Tooba:

Backend:
http://127.0.0.1:5088

Frontend if needed:
http://127.0.0.1:3000

Ensure backend is fully ready before runtime validation.

Do not substitute unit/integration tests for runtime proof.

2. Runtime Scenario A — Paid Order Survives Cart TTL

Use a real runtime Cart/Order with inventory-backed product.

Required sequence:

create Cart reservation with normal Cart TTL

record:

reservation id

status

ExpiresAt

stock reserved

confirm payment before expiry

verify SAME current reservation is committed/promoted:

status remains valid active state

ExpiresAt = null or exact canonical durable equivalent

invoke/run expiry worker after the original expiry instant

verify reservation is NOT released

verify stock reserved is still correct

create/continue normal fulfillment

Dispatch

verify Consume succeeds

verify reservation becomes Consumed through normal invariant

verify stock reserved/on_hand math is correct

No DB hand-editing.

3. Runtime Scenario B — Cancel / Restore

Using a real paid pre-dispatch Order:

verify committed active reservation

Cancel Order

verify current reservation becomes Released exactly once

Restore Order

verify replacement reservation is created/reacquired authoritatively

verify:

OrderLine reference

Fulfillment active reference
both point to current replacement reservation

verify replacement ExpiresAt = null / durable equivalent

invoke expiry worker after old Cart expiry

verify replacement remains active

Dispatch

verify replacement reservation is consumed successfully

verify old Released reservation remains untouched

4. Runtime Scenario C — Decimal Quantity

Repeat or combine with quantity:
1.25

Verify exact values through:

reserve

commit

expiry-worker pass

cancel

restore

dispatch/consume

No truncation.

5. Payment-vs-Expiry Boundary Runtime Check

If practical in runtime without artificial unsafe manipulation:

create reservation near expiry

confirm payment close to expiry

run expiry worker

Verify one safe outcome:

committed reservation survives

If forcing exact concurrency would require intrusive hacks, do NOT fake it; focused concurrency tests from R3 remain the proof for the race itself. Runtime must still prove the normal boundary behavior.

6. Error UX Runtime Fallback

Trigger or inspect one safe stale/non-active reservation case through supported runtime/API path if possible.

Verify normal Admin-facing response uses stable localized mapping and does not expose:

Held

Released

raw exception

SQL/update count

GUID details in user-facing message

Do not corrupt data just to manufacture this case.

7. Focused Regression

Re-run only:

PaidOrderReservationLifecycleTests

InventoryFoundationTests

WholeOrderCancelUntilDispatchTests

T020-R2 restore/rebind focused tests

recovery guard

git diff --check

No unrelated full suite.

8. Evidence

Update:
docs/evidence/TB-P09-T022/

Add:

r4-runtime-paid-ttl.md

r4-runtime-cancel-restore.md

r4-runtime-decimal.md

r4-runtime-error-ux.md

r4-focused-validation.md

r4-recovery-sot.md

Evidence must include actual runtime timestamps and before/after reservation state.

Do not commit secrets.

9. Recovery / Pipeline

Phase: P09

Last Architect-accepted: TB-P09-T022-R2

TB-P09-T022-R3 implementation accepted pending runtime proof

Current Repair: TB-P09-T022-R4

Last Implementation on completion: TB-P09-T022-R4

USER_VISUAL_ACCEPTED=NO

Worker Next State: IDLE

do NOT invent/start TB-P09-T023

Run recovery guard.

10. Git

If no code change is needed:

evidence-only commit is acceptable

Required:

HEAD==origin/main

no task-owned uncommitted changes

Acceptance

PASS only if ALL:

real backend runtime is running

paid Order reservation is shown committed/durable at runtime

original Cart TTL expiry passes without releasing paid reservation

Dispatch succeeds afterward

Cancel releases committed reservation

Restore creates/rebinds durable replacement reservation

expiry worker does not release restored reservation

Dispatch after Restore succeeds

decimal 1.25 is exact

no Inventory invariant weakened

no Consolidated Package code/UI touched

focused regression green

recovery guard passes

HEAD==origin/main

Worker IDLE

no TB-P09-T023 invented

USER_VISUAL_ACCEPTED=NO.

Result Contract

Return ONLY canonical BRIDGE-WAKE-V1 result with:

Summary
Applicable-Locks
Runtime-Paid-TTL
Runtime-Cancel-Restore
Runtime-Decimal
Runtime-Expiry-Boundary
Runtime-Error-UX
Focused-Validation
Recovery-SoT
Git
Architectural-Concerns
Visual-Concerns
Blockers

END_TOOBA_REPAIR_TASK
