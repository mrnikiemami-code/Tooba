PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_REPAIR_TASK

Task-ID:
TB-P10-T004-R18

Parent-Task:
TB-P10-T004-R17

Channel:
tooba-main

WorkerId:
tooba-worker-01

AgentType:
cursor

Status:
REPAIR

USER_VISUAL_ACCEPTED=NO

Title:
Reservation Policy Admin UX — Store / Category / Offer Overrides for Initial Hold, Retry Hold and Max Cycles

Architect Review

TB-P10-T004-R17 is ACCEPTED.

Accepted:

Admin Order Detail now exposes Reservation Cycle audit

immutable history is visible

current cycle/reason/expiry/used-max/retry state is visible

reacquire failure shortages are visible

Orders/Payments grids expose compact reservation state without N+1

no timer reset/extend action exists

Confirm auto-reacquire behavior remains unchanged

focused tests green

anti-pattern scan CLEAN

HEAD==origin/main

Worker IDLE

One implementation note remains:

coordinator stores reacquire-fail event detail using Status.ToString().
This is acceptable as an internal audit detail only because Admin does NOT use that string as structured business data. Do not expand this into UI parsing. If touched in R18, prefer stable structured fields; otherwise leave it internal.

The next required piece is the business-manageable Reservation Policy UX.

The user explicitly requires reservation timing and retry behavior to be configurable, including tighter policies for flash-sale / limited-stock Offers.

Do NOT change lifecycle semantics from R15–R17.
Do NOT add timer-extension bypasses.
Do NOT redesign the entire Admin.
Do NOT create TB-P10-T005.

Recovery State

Phase: P10
Last Architect-accepted: TB-P10-T004-R17
Last Implementation: TB-P10-T004-R17
Current Repair: TB-P10-T004-R18
Worker: tooba-worker-01
Expected branch: main
Expected previous HEAD: 67cb63e2808b467bf0e7afdba85224d52e7efefc
USER_VISUAL_ACCEPTED=NO

Applicable Locks

Read:

docs/architecture/TOOBA-LOCKS.md

docs/architecture/STOREFRONT-CHECKOUT-PLAN.md

Preserve:

LOCK-SF-001…096

Reservation Cycle != Payment Attempt

Initial/Retry TTLs distinct

MaxReservationCycles includes initial cycle

Offer > Category > Store > Platform precedence

multi-line uses shortest TTL + strictest max

server ExpiresAt authoritative

immutable policy snapshot per cycle

no manual timer extension/reset

SupplyStatus distinct from reservation status

Product != Offer

pricing/inventory ownership separation

Admin commercial UX rules

AppDataGrid canonical

1. Recovery Preflight

Record:

Phase

accepted/implementation/current task

Worker

branch

HEAD

origin/main

HEAD==origin/main

tracked dirty state

task-owned untracked state

Unexpected tracked divergence => BLOCK.

Evidence:
docs/evidence/TB-P10-T004/r18-recovery-start.md

2. Mandatory Discovery

Inspect current configuration and Admin surfaces for:

platform/default reservation settings

Store settings

Category settings/workspace

Product/Offer workspace

Seller Offer editing permissions

current Store payment timeout settings from R10

R15 policy resolution implementation

inheritance/override UI patterns

validation/error patterns

Create:
docs/evidence/TB-P10-T004/r18-policy-admin-discovery.md

Required table:
Level | Existing storage | Existing Admin surface | Can override? | Who may edit? | Effective-value display pattern

3. Locked Policy Fields

Expose these R15 policy fields:

InitialReservationHoldMinutes

RetryReservationHoldMinutes

MaxReservationCycles

Do NOT add speculative cooldown or unrelated settings.

Meaning:

InitialReservationHoldMinutes

How long Cycle #1 keeps inventory after Order enters Payment.

RetryReservationHoldMinutes

How long Cycle #2+ keeps reacquired inventory.

MaxReservationCycles

Maximum total real reservation cycles for one Order, INCLUDING Cycle #1.

All labels/help text must be human/business facing.

4. Precedence — LOCKED

Effective resolution remains:

Offer override > Category override > Store default > Platform default

Admin UI must clearly distinguish:

inherited value

overridden value

effective value

Do NOT duplicate resolved values into lower-level entities just to simplify UI.

Removing an override must restore inheritance, not copy the parent value.

5. Store Settings UX

Extend the existing relevant Store settings section.

Section:
سیاست رزرو موجودی

Fields:

مدت رزرو اولیه

مدت رزرو مجدد

حداکثر دفعات رزرو

Show concise help:

initial hold starts when Order enters payment phase

retry hold applies only after previous reservation ended and stock was reacquired

payment failure inside active reservation does not reset timer

max count includes initial reservation

Use current Save/Cancel settings pattern.

6. Category Override UX

In the existing Admin Category workspace/config area, add an optional override section:

سیاست رزرو موجودی

Each field must support:

ارث‌بری از فروشگاه

explicit override

If Category already inherits from another Category in a way relevant to settings, do NOT invent hidden hierarchical traversal unless current R15 resolution already defines it.
Use the exact backend resolution already accepted.

Show effective value.

Do not clutter category tree/grid itself unless a compact indicator already fits current design.

7. Offer Override UX

This is critical for flash-sale / limited-stock scenarios.

In existing Offer workspace, add:

سیاست رزرو موجودی

Fields:

Initial hold override

Retry hold override

Max cycles override

Support:

inherit from Category/Store

explicit override

effective value preview

Example help:
برای فروش‌های پرترافیک می‌توانید زمان رزرو این پیشنهاد را کوتاه‌تر کنید تا موجودی برای مدت طولانی قفل نشود.

Do NOT add Product-level override unless R15 actually implemented Product as a policy level.
Locked precedence is Offer > Category > Store > Platform.

8. Seller Permissions

Because Offer belongs to commerce/seller domain, inspect existing ownership/authorization.

Do NOT automatically allow Seller to change reservation policy.

Preferred rule unless existing permission model already says otherwise:

Admin can configure Store/Category/Offer reservation policy

Seller may edit Offer override ONLY if an explicit permission exists and delegation ceiling allows it

otherwise Seller sees effective value read-only or does not see config surface

Backend authorization is authoritative.

Document chosen behavior.

Evidence:
docs/evidence/TB-P10-T004/r18-permissions.md

9. Validation

Use safe bounds from existing configuration conventions.

Required:

Initial hold > 0

Retry hold > 0

MaxReservationCycles >= 1

reject absurd/out-of-range values using stable validation

integer minute/cycle semantics unless backend already supports another canonical unit

No silent coercion.

Admin-safe localized errors.

10. Flash Sale Guardrail UX

Do NOT create a new campaign engine.

But when Offer override is materially stricter than inherited value, show an informational note only.

Example:
این پیشنهاد از سیاست رزرو کوتاه‌تری نسبت به تنظیمات فروشگاه استفاده می‌کند.

If Admin chooses extremely long hold for an Offer with limited inventory, do not block without product policy.
A non-blocking warning is acceptable if supported cleanly:
زمان رزرو طولانی می‌تواند در فروش‌های پرترافیک باعث قفل‌شدن موجودی شود.

No heuristic based on live stock unless already available cheaply.

11. Effective Policy Preview

Store/Category/Offer surfaces should show effective values after inheritance resolution.

Offer example:

رزرو اولیه: 3 دقیقه — overridden

رزرو مجدد: 2 دقیقه — inherited from Category

حداکثر دفعات: 2 — inherited from Store

Do not make frontend resolve precedence itself.
Backend returns effective policy + source.

12. Existing Cycle Snapshot Integrity

Changing Settings affects FUTURE cycles only.

Required:

existing cycle policy snapshot stays unchanged

Admin R17 history still displays historical policy used when the cycle started

current Settings UI must not retroactively rewrite old cycles

Explicitly test.

13. Multi-Line Effective Policy

No UI needs to manually calculate Order-level minimums.

Backend remains authoritative.

But document/help may explain:
در سفارش‌های چندکالایی، سخت‌گیرانه‌ترین سیاست اقلام اعمال می‌شود.

R15 semantics remain:

shortest effective TTL

strictest max cycles

14. No Timer Extension Action

Settings change must not extend an already active cycle.

Example:

Cycle #1 started with 3 minutes

Admin changes Offer to 10 minutes

current cycle remains on original snapshotted 3-minute deadline

next future cycle/order uses new policy

This is mandatory.

15. Audit Settings Changes

Use existing Admin audit mechanism.

Record:

level: Store/Category/Offer

field changed

old override

new override

actor

timestamp

Do not log secrets.
Do not rewrite historical reservation-cycle events.

16. Performance

Required:

Offer workspace loads effective policy in existing/focused request

Category workspace no N+1

Store settings normal

no runtime supply/reservation queries just to render settings

no polling

Evidence:
docs/evidence/TB-P10-T004/r18-performance.md

17. Anti-Pattern Gate

Scan for:

frontend resolving Offer > Category > Store precedence

duplicated effective policy calculations

Product-level override accidentally added

Seller unauthorized mutation

settings change mutating active cycle ExpiresAt

copying inherited values as overrides

magic fallback values in UI

raw config keys in UI

first-line policy shortcut

current Settings used to render historical cycle instead of snapshot

Any occurrence => REPAIR.

Evidence:
docs/evidence/TB-P10-T004/r18-antipattern-scan.md

18. Focused Tests

Resolution:

Platform default

Store override

Category override

Offer override

removing Offer override restores inheritance

removing Category override restores Store

backend returns effective source

Validation:
8. Initial <=0 rejected
9. Retry <=0 rejected
10. Max <1 rejected
11. valid boundaries persist

Snapshots:
12. active cycle unchanged after Settings edit
13. historical cycle snapshot unchanged
14. next new cycle uses new setting

Multi-line:
15. shortest TTL remains authoritative
16. strictest max remains authoritative

Permissions:
17. Admin allowed
18. Seller denied without explicit permission
19. Seller allowed only with explicit permission if such permission exists

UX:
20. Store fields/save/cancel
21. Category inherit/override
22. Offer inherit/override/effective preview
23. FA RTL
24. EN LTR
25. no raw config keys

Regression:
26. R15 policy resolver
27. R16 countdown uses cycle snapshot ExpiresAt
28. R17 historical policy display
29. recovery guard
30. git diff --check

Do NOT run unrelated full suites.

19. Runtime Matrix — REQUIRED

A. Store default save and effective resolution
B. Category override
C. Offer override
D. clear Offer override -> Category effective
E. clear Category override -> Store effective
F. active cycle not extended by settings change
G. future retry/new cycle uses updated policy
H. Offer flash-sale style 2-minute retry policy
I. permissions
J. Admin audit event

20. Lock Registry

Add non-duplicate locks equivalent to:

Reservation policy overrides are configurable at Store, Category and Offer levels

precedence remains Offer > Category > Store > Platform

inheritance removal restores parent policy rather than copying values

effective policy is backend-resolved

Settings changes affect future cycles only; active/historical cycle snapshots are immutable

Seller mutation requires explicit backend permission

21. Evidence

Add under docs/evidence/TB-P10-T004/:

r18-recovery-start.md

r18-policy-admin-discovery.md

r18-store-settings.md

r18-category-override.md

r18-offer-override.md

r18-permissions.md

r18-validation.md

r18-policy-snapshot-integrity.md

r18-audit.md

r18-performance.md

r18-focused-validation.md

r18-runtime.md

r18-antipattern-scan.md

r18-recovery-sot.md

22. Recovery / Pipeline

At completion:

Phase P10

Last Architect-accepted before review: TB-P10-T004-R17

Last Implementation: TB-P10-T004-R18

Current Issued/Repair: none

USER_VISUAL_ACCEPTED=NO

Worker: tooba-worker-01 — IDLE

branch main

ending HEAD recorded

HEAD==origin/main

task-owned worktree clean

no TB-P10-T005

Run recovery guard.

Acceptance

PASS only if:

Store/Category/Offer reservation policy UX exists

Initial/Retry/Max fields configurable

inheritance/override/effective source clear

backend resolves precedence

active/historical cycles never change from Settings edits

future cycles use updated policy

seller permissions authoritative

no Product-level accidental policy

no N+1/polling

runtime A–J PASS

anti-pattern scan CLEAN

focused tests green

recovery exact

HEAD==origin/main

Worker IDLE

no TB-P10-T005

USER_VISUAL_ACCEPTED=NO.

Result Contract

Return ONLY canonical BRIDGE-WAKE-V1 Result with:

Summary
Applicable-Locks
Recovery-Start
Discovery
Store-Settings
Category-Override
Offer-Override
Permissions
Validation
Policy-Snapshot-Integrity
Audit
Performance
Focused-Validation
Runtime
AntiPattern-Scan
Lock-Registry
Recovery-SoT
Git
Architectural-Concerns
Visual-Concerns
Blockers

END_TOOBA_REPAIR_TASK