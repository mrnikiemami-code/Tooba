PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_TASK

Task-ID:
TB-P10-T022-R20

Parent-Task:
TB-P10-T022-R19

Channel:
tooba-main

WorkerId:
tooba-worker-01

AgentType:
cursor

Status:
ISSUED

Appearance-USER_VISUAL_ACCEPTED=YES
Builder-USER_VISUAL_ACCEPTED=NO

Title:
Amazing Campaign Admin Workspace — Manage Campaigns, Offers, Schedule and Real Promo Prices Without Raw IDs

Architect Review

TB-P10-T022-R19 is ACCEPTED.

The Amazing Offers vertical is now commercially safe end-to-end:

Builder/Storefront
→ PromotionCampaign
→ canonical Pricing
→ Cart
→ Checkout
→ immutable Order snapshot.

Remaining product gap:
campaigns are still Development-seeded rather than manageable by an ordinary Admin user.

This task adds the MINIMAL professional Admin management workspace for merchandising campaigns.

This is NOT the general P11 Admin roadmap.
Do NOT start TB-P11-T002.
Do NOT refactor unrelated Admin pages.

Canonical Start

Last Architect-accepted:
TB-P10-T022-R19

Last Implementation:
TB-P10-T022-R19

Expected previous HEAD:
5ea60ab714fcc684e69b138b52590c1f3f735737

User-work ancestor:
18ca10c9

Locks:
LOCK-SF-001…421

Appearance:
YES

Builder:
NO

Worker Protocol

No heartbeat.
No polling.
No IDLE loop.

After canonical Result through Bridge:
STOP completely.

1. Recovery Preflight

Verify:

main

HEAD==origin/main

R19 campaign-aware Cart/Checkout intact

R18 canonical campaign pricing intact

R17 PromotionCampaign Builder source intact

18ca10c9 ancestor

user work preserved

unrelated stashes/.tmp untouched

Evidence:
docs/evidence/TB-P10-T022-R20/recovery-start.md

Unexpected tracked divergence => RECOVERY_CONFLICT.

2. Scope — Merchandising Campaign Admin Only

Add/manage a focused Admin workspace for:

کمپین‌های فروش

or repository-consistent Persian naming.

The initial built-in system type exposed here is:
پیشنهاد شگفت‌انگیز

Architecture remains generic for MerchandisingPromotionType.

Do NOT expose:

internal type IDs

raw GUIDs

raw JSON

pricing qualifier internals

AMAZING code to normal Admin users

Do NOT build a giant generic marketing platform.

3. Navigation

Add one sensible Admin navigation entry under the existing most appropriate commerce/catalog/marketing area.

User-facing label should be Persian and understandable.

Examples:

کمپین‌های فروش

or پیشنهادهای ویژه

Choose based on existing Admin information architecture.

No duplicate navigation tree.

Evidence:
docs/evidence/TB-P10-T022-R20/navigation.md

4. Campaign List — Canonical AppDataGrid

Use the canonical professional Admin grid conventions.

Columns should include human-readable equivalents of:

عنوان

نوع کمپین

وضعیت

شروع

پایان

اولویت

تعداد کالا/Offer

آخرین تغییر

عملیات

Required:

search

relevant filters

sort

pagination

RTL

pinned operations column where canonical Admin grid does so

icon-only operations with tooltips if consistent with grid standard

no raw IDs

Filters:

نوع کمپین

Draft / Published / Archived

فعال الان / آینده / منقضی

date range if canonical grid supports it cleanly

Do not rebuild DataGrid.

5. Create Campaign Flow

Create action:
کمپین جدید

For system PromotionType:
ordinary user chooses human-readable:
پیشنهاد شگفت‌انگیز

No code field exposed.

Required fields:

Title in current Admin editing language / translation workspace

optional Subtitle

optional BadgeText

StartAt

EndAt

Priority

lifecycle state

Store scope:
derive/use current Admin Store context.
Do not expose raw StoreId.

Validation:

required title

StartAt < EndAt

clear Persian errors

first invalid focus

no save with malformed time window

6. Campaign Edit Workspace

Use one coherent workspace, not scattered unrelated pages.

Recommended sections/tabs:

اطلاعات کمپین

کالاها

قیمت‌گذاری

ترجمه‌ها only if current Admin i18n pattern requires a separate tab

مرور / status summary if appropriate

Do NOT overbuild.

The workspace should make these states obvious:

پیش‌نویس

زمان‌بندی‌شده

فعال

منقضی

بایگانی‌شده

Runtime active remains derived.
Do not let Admin manually toggle an Active truth field.

Evidence:
docs/evidence/TB-P10-T022-R20/workspace.md

7. Offer Selection — Canonical Selector/Grid

Inside کالاها:

Use existing selector/AppDataGrid patterns.

User must select by human data:

product title

variant where relevant

seller

normal current price

stock/availability

SKU/code only if already user-facing

No GUID picker.

Required operations:

add offers

remove offers

reorder SortOrder

prevent duplicate membership

prevent cross-store offers

show unavailable/out-of-stock state clearly

Important:
membership can exist while offer later becomes unavailable,
but Storefront runtime will filter it canonically.

Do not duplicate catalog browsing logic.

Evidence:
docs/evidence/TB-P10-T022-R20/offer-selector.md

8. Reordering

Admin must be able to control CampaignOffer.SortOrder.

Use a lightweight interaction consistent with current Admin:

drag/reorder if existing pattern is stable
OR

move up/down controls
OR

sortable ordering field if that is the established pattern

No timing hacks.
No local-only order state that fails persistence.

Prove save/reload order.

9. Campaign Pricing UI

Inside قیمت‌گذاری:

Use canonical Pricing/AuthoredPrice APIs introduced by R18.

For each campaign member, show:

product/offer identity

normal current canonical price

campaign selling price

derived discount percent preview

pricing scope dimensions that are actually relevant in the current Store

Do NOT expose:

QualifierKind

QualifierKey

raw campaign ID

internal D qualifier encoding

Do NOT duplicate normal/list price.

Campaign price input must be:

currency-safe

dimension-aware

validated > 0

constrained according to existing pricing rules

If promo price >= normal price:

allow only if existing business rules permit,

but clearly show there is no effective discount

never fabricate discount badge

Prefer warning if not commercially meaningful.

Evidence:
docs/evidence/TB-P10-T022-R20/pricing-ui.md

10. Bulk Price Editing — Minimal

If easy within existing grid patterns, support efficient editing for multiple members.

Acceptable:

inline campaign-price cell editing

batch apply percentage reduction ONLY if safely derived into actual authored campaign prices and user confirms

Do NOT introduce a new rule engine.

If batch percentage editing would expand scope materially:
defer it.

Single-row reliable editing is enough for PASS.

11. Translation UX

Use existing localization conventions.

At minimum Admin can manage:

FA Title/Subtitle/Badge

EN Title/Subtitle/Badge

Do not add section locale.
Do not expose locale keys as raw technical codes if existing UI has human language names.

Fallback remains runtime canonical behavior.

12. Lifecycle Actions

Required actions:

save draft

publish/schedule

archive

restore from archive only if domain supports it safely

UI must distinguish:

Published but future = زمان‌بندی‌شده

Published and within window = فعال

Published but past EndAt = منقضی

Do NOT mutate lifecycle every second.

Publish validation must ensure:

valid time window

at least one member if current product decision requires it

translations required by current Store language policy

no malformed pricing records

Do not require every member to have promo override; normal-price fallback remains valid.

13. Safe Delete / Archive

Prefer archive over destructive delete for published/history-bearing campaigns.

Never:

delete SellerOffer

delete Product

delete Order history

alter historical Order totals

If deleting Draft-only campaign is allowed by domain:
make behavior explicit and safe.

14. Builder Integration Proof

Do NOT change R17 source UX unless needed for regression.

After Admin creates/edits a real active campaign:

Product Showcase پیشنهاد شگفت‌انگیز must resolve it automatically when CampaignId=null

no page re-save needed

SortOrder reflected

promo prices reflected

archived/expired campaign stops being selected

This is a regression/integration proof, not Builder redesign.

15. Cart/Checkout Regression Proof

For one Admin-created active campaign:

product appears on Storefront at campaign price

add to Cart

server validates campaign context

Cart price matches

checkout revalidates

Order snapshots accepted amount

Do not reimplement R19.

16. Real Browser Runtime — REQUIRED

Use real Host + Frontend + Admin.

Required flow:

A. login Admin
B. open campaign list
C. create پیشنهاد شگفت‌انگیز
D. enter FA title + EN title
E. set active window
F. add at least 5 real offers
G. reorder offers
H. set promo prices for at least 3
I. save draft
J. reload and prove persistence
K. publish
L. list shows فعال
M. open Builder page that uses پیشنهاد شگفت‌انگیز
N. Preview resolves Admin-created campaign
O. public Storefront resolves same campaign
P. order matches SortOrder
Q. campaign prices visible
R. out-of-stock/unavailable member filtered
S. add one promo item to Cart
T. prove server-authoritative promo price
U. archive campaign
V. Storefront no longer resolves it as active
W. no console errors
X. no polling/request loop

Capture screenshots:

campaign list

create/edit basic info

offer selector

offer membership/order

pricing UI

active campaign status

Builder Preview

public Storefront

Evidence:
docs/evidence/TB-P10-T022-R20/browser-proof.md

17. Permissions

Reuse existing Admin authorization conventions.

If current Admin Access Control is known fail-open from P11 audit:
do NOT solve the entire Access Control system here.

At minimum:

use the nearest existing campaign/commerce management permission pattern if one exists

do not introduce a second auth system

document current limitation honestly

Do NOT begin P11 Access Control repair.

Evidence:
docs/evidence/TB-P10-T022-R20/permissions.md

18. API / Validation

Admin APIs must:

derive/enforce Store scope

validate campaign type

validate time window

validate membership

validate pricing scope

prevent duplicate membership

prevent cross-store access

not accept client price as commerce truth outside canonical AuthoredPrice creation/update path

No raw EF entity payloads if current API style uses DTOs.

19. Focused Tests

Required:

1 campaign list Store-scoped
2 create Draft Amazing campaign
3 FA/EN translations persist
4 invalid Start/End rejected
5 publish future => scheduled
6 published active window => active derived
7 expired displayed correctly
8 archive removes active eligibility
9 offer selector excludes/prevents cross-store
10 duplicate membership rejected
11 reorder persists
12 campaign price save uses canonical AuthoredPrice qualifier
13 normal price unchanged
14 derived discount correct
15 member without promo price remains valid
16 source CampaignId=null resolves Admin-created active campaign
17 Builder Preview sees Admin changes without page snapshot
18 Storefront SSR sees Admin changes
19 out-of-stock filtered
20 Cart receives campaign context
21 checkout authoritative price unchanged from R19
22 Order snapshot correct
23 archive makes dynamic source empty/next eligible campaign according to resolver priority
24 no raw GUID/code in normal Admin UI
25 existing R17/R18/R19 tests remain green
26 recovery-staleness PASS
27 git diff --check PASS
28 18ca10c9 ancestor PASS
29 HEAD==origin/main PASS

Keep tests focused.

20. Capability Map Update

This task materially adds Admin ownership for Merchandising Campaign management.

Update ONLY relevant sections of:
docs/architecture/TOOBA-CAPABILITY-MAP.md

Record compactly:

Admin Campaign workspace route/component/API

Campaign management ownership

Offer membership management

Campaign pricing authoring path

lifecycle management

Last Verified Task = TB-P10-T022-R20

Do not broad-audit.
Do not make file mandatory for unrelated tasks.

21. Anti-Pattern Gate

Reject:

general P11 Admin work

giant marketing platform

raw GUID UI

raw AMAZING code UI

raw pricing qualifier UI

duplicate product selector

duplicate DataGrid

client-authoritative price

CampaignOffer.PromoAmount

manual Active boolean truth

destructive published campaign delete without history strategy

campaign edits mutating historical Order totals

Builder redesign

ProductCard redesign

polling

timing hacks

unrelated cleanup

user-work overwrite

Evidence:
docs/evidence/TB-P10-T022-R20/antipattern-scan.md

22. Locks

Add after LOCK-SF-421:

LOCK-SF-422 Merchandising campaign Admin UX exposes human-readable campaign, offer, seller, lifecycle and pricing concepts; raw campaign IDs, PromotionType codes and Pricing qualifier internals are not normal-user UI.
LOCK-SF-423 Campaign membership authoring reuses canonical SellerOffer/Catalog selectors and persists only CampaignOffer membership/order; it never copies product truth into campaign records.
LOCK-SF-424 Campaign price authoring uses canonical AuthoredPrice campaign qualifier APIs and pricing dimensions; Admin campaign UI never owns an independent promotional-price store.
LOCK-SF-425 Merchandising campaign runtime status shown in Admin is derived from lifecycle + StartAt/EndAt; there is no manually maintained Active truth toggle.
LOCK-SF-426 Published/history-bearing merchandising campaigns use archive/history-safe lifecycle semantics and must never mutate historical Order pricing snapshots.
LOCK-SF-427 A PromotionCampaign Product Showcase with CampaignId=null tracks the currently eligible Admin-managed campaign dynamically; campaign membership/price changes do not require page content snapshot rewrites.

Do not renumber existing locks.

23. Completion State

At completion:

Phase:
P10 / Builder Acceptance + Amazing Campaign Operational Management

Last Architect-accepted before review:
TB-P10-T022-R19

Last Implementation:
TB-P10-T022-R20

Expected:

real Admin campaign list/workspace exists

Admin can create/edit/publish/archive Amazing campaigns

Admin can select/reorder SellerOffers

Admin can author real campaign prices through canonical Pricing

FA/EN campaign text manageable

Builder dynamic source sees Admin changes

Storefront SSR sees Admin changes

Cart/Checkout integrity remains intact

Development seed remains useful but no longer the only way to manage campaigns

no general P11 work

Appearance YES

Builder NO until user visual acceptance

main

HEAD==origin/main

18ca10c9 ancestor

user work preserved

locks through LOCK-SF-427

Do NOT write Worker IDLE.

After canonical Result through Bridge:
STOP completely.

Acceptance

PASS only if:

Admin can create a real Amazing campaign

no raw technical IDs/codes in normal UX

offer membership + ordering persist

campaign pricing uses canonical Pricing

lifecycle/status correct

real Browser proof completes

Builder + Storefront dynamically reflect Admin-created campaign

Cart/Checkout remains server-authoritative

no unrelated P11 work

user work preserved

canonical Result delivered

Worker stops

Result Contract

Return ONLY canonical BRIDGE-WAKE-V1 Result with:

Summary
Applicable-Locks
Recovery-Start
Navigation
Campaign-List
Create-Flow
Workspace
Offer-Selector
Reordering
Pricing-UI
Translations
Lifecycle-Actions
Archive-Semantics
Builder-Integration
Cart-Checkout-Regression
Browser-Proof
Permissions
API-Validation
Focused-Validation
Capability-Map
AntiPattern-Scan
Lock-Registry
Recovery-SoT
Git
Visual-Concerns
Architectural-Concerns
Blockers

Do not include Worker-IDLE.

After sending Result through Bridge:
STOP completely.

END_TOOBA_TASK