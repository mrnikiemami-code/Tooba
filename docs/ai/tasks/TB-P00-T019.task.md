# Tooba — TB-P00-T019 — Observability, Logging & Audit Architecture

BEGIN_TOOBA_CURSOR_TASK_V1

Protocol-Version: 1
Task-ID: TB-P00-T019
Phase: P00 — Architecture / Discovery
Type: Architecture Design / Observability, Logging & Audit
Repository: https://github.com/mrnikiemami-code/Tooba
Primary-Branch: main
Implementation-Agent: Cursor
Architect: ChatGPT
Execution-Mode: PIPELINE
Depends-On: TB-P00-T018
Architect-Decision-On-Dependency: ACCEPTED

## Objective

Design Tooba's observability, technical logging, tracing, metrics, business audit and security-audit architecture deeply enough to support:

- OpenTelemetry as the vendor-neutral observability foundation;
- structured technical logging;
- distributed tracing;
- application/infrastructure metrics;
- business audit trails;
- security audit events;
- tenant/store correlation;
- marketplace seller context;
- request / message / background-job correlation;
- privacy and secret redaction;
- production diagnostics;
- performance analysis;
- alerting/readiness;
- future external APM/log vendors without architecture lock-in;
- operational troubleshooting across future microservices.

This task is architecture documentation only.

Do not install OpenTelemetry packages, logging providers, collectors, dashboards, exporters, SIEMs, or monitoring infrastructure.

---

## 1. Repository Recovery

Before edits:

```bash
git rev-parse --show-toplevel
git fetch origin
git branch --show-current
git rev-parse HEAD
git rev-parse origin/main
git status --short --branch
```

Require:

```text
branch = main
HEAD == origin/main
working tree clean
```

Unsafe state =>

```text
RECOVERY_CONFLICT
```

Never force push, rewrite history, destructively reset, silently stash, or overwrite unknown work.

Expected predecessor:

```text
ed119238e1451f181200a68d2538bb36440a721b
```

If synchronized main legitimately advanced, continue from current safe main.

---

## 2. Source-of-Truth Transition

Update:

```text
TB-P00-T018 = ACCEPTED
TB-P00-T019 = ISSUED / AWAITING_ARCHITECT_ACCEPT
```

Roadmap:

```text
P00 AI Assistant / RAG = COMPLETE
P00 Observability / Logging / Audit = IN_PROGRESS
```

Do not mark T019 accepted.

---

## 3. Create Architecture Document

Create:

```text
docs/architecture/18-observability-logging-audit.md
```

It must contain all sections below.

---

# A. Core Separation

Hard rules:

```text
Technical Log != Business Audit
Technical Log != Security Audit
Metric != Audit
Trace != Audit
Analytics != Audit
```

Define:

```text
Technical Logging
= diagnostic structured events for engineering/operations

Tracing
= causal execution path across requests/messages/jobs

Metrics
= numerical signals over time

Business Audit
= durable record of important business actions/state changes

Security Audit
= durable record of security-sensitive activity
```

Analytics remains a separate behavioral capability.

---

# B. OpenTelemetry Foundation

Confirmed requirement:

```text
OpenTelemetry
```

must be the vendor-neutral observability foundation.

Architecture should support:

```text
Traces
Metrics
Logs correlation
Resource attributes
Baggage where safe
Context propagation
Exporters
Collector
```

Do not lock a telemetry backend/vendor.

---

# C. Vendor Neutrality

Application/domain architecture must not depend on:

```text
Datadog SDK
New Relic SDK
Application Insights SDK
Grafana vendor APIs
Elastic APM APIs
```

directly.

Vendor-specific exporters/integrations belong at infrastructure edge.

Future vendor replacement should not require business-domain rewrite.

---

# D. Structured Logging

Technical logs must be structured.

Prefer semantic fields such as:

```text
EventName
TraceId
SpanId
CorrelationId
TenantId
Deployment/Edition
Module
Operation
Entity reference
SellerId where relevant
OrderId where relevant
Duration
Outcome
ErrorCode
```

Avoid unstructured string-only logging as the primary strategy.

Do not create exact logging schema yet.

---

# E. Correlation Model

Define correlation across:

```text
HTTP request
background job
domain/application command
integration event
message
checkout
order
payment attempt
inventory reservation
AI request
search indexing
media processing
```

Potential identifiers:

```text
TraceId
SpanId
CorrelationId
CausationId
OperationId
Business Entity IDs
```

Do not overload one ID for all meanings.

---

# F. Distributed Tracing

Even in a Modular Monolith, trace boundaries should reflect logical module/application operations.

Future microservice extraction should preserve trace propagation.

Trace:

```text
incoming request
→ application use case
→ module contract
→ DB/external call
→ message/outbox
→ consumer
```

No implementation.

---

# G. Span Design

Avoid a span for every tiny method.

Instrument meaningful boundaries such as:

```text
HTTP request
application use case
database operation
external HTTP call
message publish/consume
background job
search query
payment provider call
AI provider call
media transform
```

Need sampling policy later.

---

# H. Metrics

Define metric categories:

```text
request rate
error rate
latency
dependency latency
DB pool/utilization where observable
queue backlog
job failures
cache hit ratio
search latency
indexing lag
payment failures
inventory reservation conflicts
media processing latency
AI latency/token/cost signals
analytics ingestion lag
```

Business KPI metrics may also exist but must not replace authoritative business reporting.

---

# I. RED / USE Style

Recommend operational metric thinking around:

```text
Rate
Errors
Duration
```

for services/use cases, and where appropriate:

```text
Utilization
Saturation
Errors
```

for resources.

Do not over-instrument arbitrary counters.

---

# J. SLO / SLI Readiness

Preserve future definitions for:

```text
availability
latency
error rate
checkout success
payment provider health
search latency
indexing freshness
media processing freshness
AI availability
```

Do not invent exact production SLO numbers in P00.

Classify exact thresholds:

```text
NEEDS_LATER_P00_DETAIL
```

---

# K. Error Taxonomy

Design a consistent conceptual error taxonomy.

Differentiate:

```text
Validation error
Business rule rejection
Authorization denial
Not found
Conflict/concurrency
Dependency failure
Timeout
Transient infrastructure error
Permanent infrastructure error
Unexpected defect
```

Logs/metrics/traces should be able to distinguish them.

Do not expose internal details directly to users.

---

# L. Exception Logging

Avoid:

```text
log exception at every layer
```

causing duplicate noisy logs.

Define principle:

- enrich context as error propagates;
- log once at meaningful boundary where outcome is known;
- preserve trace/span error state;
- use expected business failures as structured outcomes, not stack traces everywhere.

---

# M. Request Logging

Need safe request telemetry.

May include:

```text
route template
method
status
duration
trace
tenant
authenticated subject opaque id where appropriate
```

Avoid logging raw:

```text
password
OTP
tokens
Authorization headers
cookies
payment secrets
full sensitive bodies
```

---

# N. PII / Secret Redaction

Hard requirement.

Sensitive values include:

```text
password
OTP
session/token secrets
API keys
payment secrets
PAN/CVV
national ID
phone/email where unnecessary
addresses
private AI context
```

Need:

```text
redaction
allowlist-based logging where practical
sensitive-field annotations/policy
structured logging filters
```

Do not rely on developers remembering manually every time.

---

# O. Tenant Context

Single-Store observability must carry:

```text
TenantId
```

where safe/useful.

Never log raw DB connection strings or secrets.

Tenant identity should be immutable durable ID, not raw hostname as canonical identity.

Marketplace uses deployment/resource/seller context appropriately.

---

# P. User / Subject Context

For security/business observability, may carry:

```text
IdentityId
PartyId
OrganizationId
SellerId
```

as opaque references where justified.

Do not log excessive PII.

Authorization decisions may include subject/resource/permission references.

---

# Q. Business Audit

Define Business Audit as durable and queryable history for significant business operations.

Examples:

```text
Product approved
Seller approved/suspended
Price changed
Inventory adjusted
Order cancelled
Refund requested/completed
Content published
Promotion activated
Tenant setting changed
Organization membership changed
```

Audit should include:

```text
Who/Actor
What action
Target
When
Tenant/Scope
Reason where applicable
Before/After summary or changed fields where safe
Correlation
Source
```

Do not use technical logs as the only audit record.

---

# R. Security Audit

Security-sensitive events include:

```text
login success/failure
password changed
MFA enabled/disabled
recovery completed
identifier changed
external identity linked
session revoked
authorization denial where security-relevant
privileged permission change
tenant admin change
suspicious access
```

Security audit has different retention/access requirements from general logs.

---

# S. Audit Immutability Direction

Audit records should be append-oriented and tamper-resistant in architecture.

Do not imply they are literally impossible to alter without designing storage.

Preserve:

```text
append-oriented history
restricted mutation
integrity/reconciliation
export/retention
```

Exact cryptographic tamper evidence is later detail.

---

# T. Audit Ownership

Each business module owns semantic meaning of its auditable business actions.

A shared Audit capability/infrastructure may persist normalized audit envelopes.

Do not centralize business logic into an Audit module.

Conceptual flow:

```text
module action
→ audit fact/envelope
→ durable audit store
```

---

# U. Technical Log Retention vs Audit Retention

Hard separation:

```text
technical log retention
!=
audit retention
```

Logs may be sampled/rotated differently.

Audit may require longer durable retention.

Exact retention periods need later legal/business policy.

---

# V. Analytics Separation

Do not use analytics events as audit evidence by default.

Example:

```text
AddToCart analytics event
```

is not equivalent to:

```text
Admin changed payment configuration
```

Analytics may be lossy/best effort for some events; Audit may require durability.

---

# W. Outbox / Messaging Observability

Future outbox/inbox/message processing needs visibility into:

```text
publish lag
consumer lag
retry count
dead-letter/failure
duplicate detection
idempotency outcome
message age
```

Trace/correlation context should propagate through messages where safe.

---

# X. Background Jobs

Every background job should expose:

```text
JobName
JobRunId
TenantId where applicable
StartedAt
Duration
Outcome
Retry
ProcessedCount
FailureCount
Trace/Correlation
```

No ambient tenant guessing.

---

# Y. Database Observability

Need visibility into:

```text
query latency
connection failures
pool saturation
transaction duration
deadlocks/conflicts
migration status
```

Do not log raw SQL parameters containing sensitive values indiscriminately.

Cross-module direct DB access remains forbidden regardless of observability.

---

# Z. Cache Observability

Cache abstraction should expose:

```text
hit/miss
latency
eviction/invalidation
backend failures
key-space/category
```

Do not log full sensitive cache keys if they contain private data.

No Redis required initially.

---

# AA. Search Observability

Preserve metrics/traces for:

```text
query latency
zero-result rate
engine errors
index lag
document count
rebuild status
```

Search analytics remains separate behavioral data.

---

# AB. Payment Observability

Need safe signals:

```text
provider latency
attempt status
decline category
callback lag
signature failure count
reconciliation mismatches
refund failures
```

Never log card secrets/provider secrets.

---

# AC. Inventory Observability

Need:

```text
reservation conflict rate
reservation expiry
oversell-prevention rejection
release failures
stock reconciliation anomalies
external sync lag
```

---

# AD. Media Observability

Need:

```text
upload failure
processing latency
transform failure
CDN failure
variant generation
storage growth
broken references
```

---

# AE. AI Observability

Need safe:

```text
provider/model
latency
token usage
retrieval latency
citation coverage
tool-call outcomes
cost signals
fallbacks/errors
evaluation score
```

Do not indiscriminately log prompt/conversation content.

Sensitive AI contexts require redaction/limited capture.

---

# AF. SEO / Web Observability

Need signals for:

```text
render failures
404/5xx
redirect loops
canonical/hreflang validation failures
sitemap generation failures
CWV
```

Some belong to scheduled diagnostics rather than runtime metrics.

---

# AG. Frontend Observability

Preserve browser-side signals such as:

```text
JS errors
route failures
Core Web Vitals
failed API calls
critical interaction errors
```

Do not capture sensitive form fields.

Frontend telemetry must be tenant/site scoped.

---

# AH. UI / UX Operational Signals

Because UI quality is product-critical, observability should support future measurement of:

```text
frontend errors
slow interactions
failed images
checkout step failures
search latency
mobile performance
CWV regressions
```

Do not equate analytics engagement metrics with technical UX health.

---

# AI. Health Checks

Define layered health concepts:

```text
liveness
readiness
dependency health
degraded capability
```

Avoid marking entire application down because optional AI provider is unavailable.

Examples:

```text
AI unavailable -> core commerce still ready
Search unavailable -> storefront degraded
Primary DB unavailable -> critical readiness failure
```

Exact health policy later.

---

# AJ. Alerting Readiness

Architecture must expose actionable signals suitable for alerts.

Avoid alerting on every error log.

Good alert candidates may derive from:

```text
sustained error rate
latency threshold
queue backlog
payment failure spike
database saturation
cross-tenant security anomaly
indexing lag
job failures
```

Exact thresholds later.

---

# AK. Sampling

High-volume traces/logs may require sampling.

Critical:

```text
security audit
business audit
payment reconciliation facts
```

must not be accidentally lost due to generic telemetry sampling.

Sampling policy applies differently to traces/logs/audit.

---

# AL. Environment / Deployment Context

Telemetry should identify:

```text
service/application
version
environment
deployment/edition
instance
region where applicable
```

Single-Store must additionally support TenantId on relevant spans/logs.

Do not leak secrets/build paths unnecessarily.

---

# AM. Release / Version Correlation

Need ability to correlate incidents with:

```text
application version
commit/build
deployment time
feature/config version where relevant
```

No implementation.

---

# AN. Feature Flag Observability

If feature flags are later used, traces/logs may need active flag context for debugging.

Do not copy entire flag payload into every log.

Exact feature-flag architecture may be later.

---

# AO. Audit Query UX

Professional Admin/Security UX may require:

```text
filter by actor
target/resource
action
date range
tenant/seller
success/failure
correlation
before/after summary
```

Audit UI must not be a raw log-file viewer.

Access controlled by SpiceDB.

---

# AP. Operational Dashboard UX

Professional operational dashboards should show:

```text
health
error trends
latency
queue/job state
payment issues
search/index freshness
media failures
AI degradation
tenant-specific incidents
```

Backend module boundaries must not dictate fragmented UI.

Use cohesive operational workflows.

---

# AQ. Trace-to-Business Navigation

Operations staff should be able to move conceptually between:

```text
OrderId
PaymentId
TraceId
Audit event
Job/message correlation
```

without exposing private customer data broadly.

This improves diagnosis of complex failures.

---

# AR. Incident Diagnostics

Architecture should enable questions like:

```text
Why did this order fail?
Why was payment captured but order remained pending?
Why did this tenant see no search results?
Why is this product image broken?
Why did AI cite stale content?
```

Observability must carry enough correlation/provenance to answer these without direct database archaeology as the normal workflow.

---

# AS. Data Export / SIEM Readiness

Security audit and logs may later export to:

```text
SIEM
log platform
object archive
security analytics
```

Use adapters/exporters.

Do not make SIEM vendor part of domain architecture.

---

# AT. Failure of Observability Backend

Hard principle:

```text
Telemetry backend failure should not normally break core commerce.
```

Technical logs/traces/metrics should degrade safely.

Business/Security Audit durability may require stronger local buffering/outbox semantics.

Exact buffering architecture later.

---

# AU. Audit Delivery Reliability

Classify audit events as higher reliability than normal telemetry.

Need future consideration of:

```text
local durable write
outbox
retry
reconciliation
```

Do not permit a critical audited business change to be considered fully recorded only because a remote log collector accepted a best-effort event.

---

# AV. Audit Before/After Data

Avoid blindly serializing entire entity snapshots.

Prefer:

```text
changed fields
business-relevant before/after
opaque references
reason
```

while protecting sensitive data.

Exact diff representation later.

---

# AW. Security Event Severity

Security audit/telemetry may classify:

```text
Informational
Suspicious
High Risk
Critical
```

or equivalent.

Do not lock final taxonomy.

Need hooks for future anomaly detection/security operations.

---

# AX. Data Ownership Matrix

Create matrix across:

```text
Observability
Business Audit
Security Audit
Analytics
Identity
Authorization
Order
Payment
Inventory
Search
Media
AI
```

Against:

```text
trace
metric
technical log
business action audit
security event
behavioral event
order truth
payment truth
authorization decision
AI request metadata
```

Mark:

```text
OWNER
SOURCE
CONSUMER
CORRELATION
NOT_OWNER
```

---

# AY. Failure Matrix

Analyze:

```text
Telemetry Exporter Down
Collector Down
Log Backend Down
Metric Backend Down
Trace Sampling
Audit Store Down
Audit Delivery Retry
Redaction Failure
PII Detected In Log
Missing Tenant Context
Broken Trace Propagation
Clock Skew
Duplicate Audit Event
Frontend Telemetry Blocked
```

For each classify:

```text
block request?
buffer?
retry?
fail closed?
drop?
alert?
reconcile?
```

Important:

Normal telemetry failure usually must not block commerce.

Critical audit failure may require stricter behavior depending on operation.

Classify exact per-operation policy as:

```text
NEEDS_LATER_P00_DETAIL
```

---

# AZ. Testing Strategy — Architecture Level

Future implementation must test:

```text
trace propagation
message correlation
tenant context
secret redaction
PII redaction
duplicate exception logging avoidance
audit durability
audit authorization
cross-tenant telemetry separation
payment secret exclusion
AI prompt-content exclusion policy
job correlation
frontend/backend trace correlation
telemetry backend outage degradation
audit backend failure policy
```

No tests now.

---

# BA. Decision Summary

Classify:

```text
RECOMMENDED_FOR_ADR
NEEDS_LATER_P00_DETAIL
DEFERRED
```

At minimum recommend:

1. OpenTelemetry is the vendor-neutral observability foundation.
2. Technical Logging, Tracing, Metrics, Business Audit, Security Audit and Analytics are separate concerns.
3. Structured logging is required.
4. Trace/Correlation/Causation/business identifiers remain distinct concepts.
5. Context propagates across requests, jobs and messages.
6. Sensitive data uses systematic redaction/minimization.
7. TenantId is included in relevant Single-Store telemetry without exposing connection secrets.
8. Business/Security Audit is durable and not replaced by technical logs.
9. Audit records are append-oriented/tamper-resistant in design.
10. Audit retention is independent from technical-log retention.
11. Vendor-specific APM/log/SIEM SDKs stay behind infrastructure/export boundaries.
12. Optional telemetry backend failure does not normally break commerce.
13. Critical audit delivery has stronger reliability/reconciliation than normal telemetry.
14. Frontend observability and Core Web Vitals are first-class because UI quality is product-critical.
15. Health distinguishes liveness/readiness/degraded optional dependencies.
16. SLO/alerting thresholds are operational policy, not hardcoded domain constants.
17. Operational dashboards are workflow-oriented, not raw log viewers.
18. Audit access is authorization-controlled through SpiceDB.
19. Release/build/version correlation is required for production diagnosis.
20. Observability must support future microservice extraction without redesign.

Do not create final ADR yet.

---

## 4. Consistency Updates

If required, minimally align:

```text
docs/architecture/03-data-ownership-and-module-contracts.md
docs/architecture/04-identity-authentication.md
docs/architecture/05-spicedb-authorization.md
docs/architecture/11-payment.md
docs/architecture/14-search-indexing.md
docs/architecture/15-media-image-pipeline.md
docs/architecture/16-first-party-analytics.md
docs/architecture/17-ai-assistant-rag.md
```

No unrelated redesign.

---

## 5. Update SoT

Update:

```text
docs/PROJECT-STATE.md
docs/ROADMAP.md
docs/ai/TOOBA-RECOVERY-CONTEXT.md
```

Expected:

```text
Last Architect Accepted Task: TB-P00-T018
Current Issued Task: TB-P00-T019
Task State: AWAITING_ARCHITECT_ACCEPT
Current Phase: P00 — Architecture / Discovery
Observability / Logging / Audit: IN_PROGRESS
```

No next task authorized.

---

## 6. Save Complete Envelope Verbatim

Save exact task to:

```text
docs/ai/tasks/TB-P00-T019.task.md
```

Verify exact markers:

```text
BEGIN_TOOBA_CURSOR_TASK_V1
END_TOOBA_CURSOR_TASK_V1
```

Do not save a shortened pointer.

---

## 7. Explicit Out of Scope

Do NOT:

- install OpenTelemetry packages;
- configure collectors/exporters;
- choose APM/logging vendor;
- implement logging;
- implement tracing;
- implement metrics;
- create audit DB/schema;
- implement SIEM;
- implement dashboards;
- implement alerts;
- integrate Shopeiva;
- create final ADR.

---

## 8. Validation

Run:

```bash
git diff --check
git status --short --branch
```

Manual checks:

- T018 ACCEPTED;
- T019 awaiting Architect acceptance;
- OpenTelemetry is explicit;
- logs/traces/metrics/audit/analytics separated;
- structured logging represented;
- correlation/causation represented;
- tenant context represented;
- secrets/PII redaction represented;
- business/security audit durability represented;
- telemetry vendor neutrality represented;
- optional observability failure does not normally block commerce;
- frontend/CWV observability represented;
- UI/UX operational quality represented;
- no application code;
- no vendor selected.

---

## 9. Git Commit & Push

Commit:

```text
docs design Tooba observability audit [TB-P00-T019]
```

Then:

```bash
git push origin main
git fetch origin
git rev-parse HEAD
git rev-parse origin/main
git status --short --branch
```

Require:

```text
HEAD == origin/main
working tree clean
```

No force push.

---

## 10. Result Contract

Return exactly:

```text
BEGIN_TOOBA_CURSOR_RESULT_V1

Protocol-Version: 1
Task-ID: TB-P00-T019
Phase: P00 — Architecture / Discovery
Status: PASS | BLOCKED | RECOVERY_CONFLICT

Summary:
...

Repository-Recovery:
- Repo-Root:
- Branch:
- Starting-HEAD:
- Starting-Origin-Main:
- Starting-Status:

Changes:
- ...

Observability-Boundary:
- ...

OpenTelemetry:
- ...

Logging-Tracing-Metrics:
- ...

Business-Security-Audit:
- ...

Tenant-Privacy:
- ...

Module-Operational-Signals:
- ...

Frontend-UIUX:
- ...

Reliability-Failure-Behavior:
- ...

Decision-Summary:
- RECOMMENDED_FOR_ADR:
- NEEDS_LATER_P00_DETAIL:
- DEFERRED:

Validation:
- git diff --check:
- other checks:
- manual consistency review:

Git:
- Commit:
- Push:
- Final-HEAD:
- Final-Origin-Main:
- Final-Status:
- Head-Matches-Origin: YES | NO

Source-of-Truth:
- Last-Architect-Accepted-Task:
- Current-Issued-Task:
- Current-Phase:
- Task-State:
- Recovery-Ready: YES | NO

Architectural-Concerns:
- ...

Blockers:
- ...

END_TOOBA_CURSOR_RESULT_V1
```

---

## 11. Pipeline Continuity — MANDATORY

After RESULT:

```text
WAITING
```

Remain inside the Tooba Architect-controlled pipeline.

Do not leave PIPELINE mode.

Do not invent, infer, prepare, or execute unauthorized work.

Do not advance to `TB-P00-T020` or any other task without a new valid Architect envelope containing:

```text
BEGIN_TOOBA_CURSOR_TASK_V1
...
END_TOOBA_CURSOR_TASK_V1
```

Cursor PASS is not Architect ACCEPT.

After sending RESULT, stop execution and remain WAITING for Architect review.

---

## 12. UI / UX Protection — MANDATORY PROJECT RULE

Tooba is a commercial product and weak UI/UX is considered product failure.

All architectural decisions must preserve future ability to build:

```text
professional storefront
professional Admin panel
professional Seller panel
polished Customer dashboard
intentional mobile UX
RTL/LTR support
accessibility
responsive design
high-quality loading/empty/error states
strong visual hierarchy
commercial-grade interaction design
```

For observability and operations specifically, future Admin/Operations UI must be workflow-oriented and readable.

It must support high-quality views such as:

```text
health overview
incident drill-down
order/payment correlation
audit history
tenant/seller scope
search/index freshness
job/queue state
frontend/CWV health
AI degradation
clear severity/status
filters
timeline
mobile/responsive behavior where appropriate
RTL/LTR
accessible tables/charts
loading/empty/error states
```

Hard rule:

```text
Backend/module boundary != UI boundary
```

Do not create disconnected log/audit CRUD screens simply because technical storage is separated.

Future UI implementation tasks must require visual evidence and Architect visual acceptance.

Build/test PASS alone is not sufficient acceptance for UI work.

END_TOOBA_CURSOR_TASK_V1
