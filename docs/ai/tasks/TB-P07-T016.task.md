PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

TASK-ID: TB-P07-T016
PARENT-TASK: TB-P07-T014
PROJECT: Tooba
PHASE: P07
CHANNEL: tooba-main
STATUS: ISSUED
TASK-TYPE: PRODUCT ADMIN UX ALIGNMENT — CATEGORY ASSIGNMENT + ATTRIBUTE SOURCE + VISUAL CONTRACT
WORKER-POLICY: ONE WORKER = ONE ACTIVE TASK

Architect Recovery Note

A wrong SEO task previously collided with Task-ID TB-P07-T015. TB-P07-T015 is now permanently treated as the blocked/collided identifier and MUST NOT be reused.

That mistaken task was stopped safely before implementation.

Confirmed stop state:

no SEO implementation started

no source/catalog code changed

no migration created/applied

no database change

no commit

no push

runtime kept alive

only an untracked mistaken envelope exists:
docs/ai/tasks/TB-P07-T015.task.md

Do NOT treat that mistaken envelope as canonical. Do NOT reuse TB-P07-T015 for implementation.

This file is the canonical TB-P07-T016 task.

Do NOT delete/reset/stash unrelated files.
Preserve the mistaken untracked envelope until repository policy allows safe replacement/archive.

Accepted Baseline

TB-P07-T014 is ACCEPTED.

Canonical baseline:

Product Catalog Admin exists.

Product list uses customized Tooba AppDataGrid.

Product Workspace has VIEW / EDIT.

Product Attributes are typed and Category-schema-driven.

Product Variants are real and category-axis-driven.

Product Media tab is real.

Product != Offer.

no Product.Price / Product.Stock.

no direct Pricing / Inventory joins.

Media DAM remains deferred.

baseline HEAD == origin/main:
c1894d619e23baa4b7ea10757a733503bb4d3931

USER-LOCKED PRODUCT UX RULES
Rule A — Product Category Assignment Is Level-3 Only

Current approved catalog assignment rule:

Level 1
→ navigation only

Level 2
→ navigation only

Level 3
→ selectable / assignable to Product

Example:

کالای دیجیتال
  → موبایل و تبلت
    → گوشی موبایل   ✅ assignable

Product MUST NOT be assigned to Level 1 or Level 2.

The Category picker may browse/search all 3 levels, but only Level 3 is selectable.

Backend must enforce the same rule.

Rule B — Product Attributes Come ONLY From Category Schema

Product does NOT create arbitrary canonical attributes.

Canonical flow:

Category
→ Effective Category Attribute Schema
→ Product fills values

FORBIDDEN in Product Workspace:

creating AttributeDefinition

افزودن ویژگی جدید that creates schema locally on Product

selecting arbitrary attributes outside effective Category schema

If such an action currently exists in Product Attributes UI, remove it.

Canonical AttributeDefinition creation remains in Admin Category / Attribute management.

LOCKED PRODUCT VISUAL CONTRACT

Before coding, open and inspect this exact user-approved reference:

D:\Users\User\source\repos\SarvNewVerRequirment\reference\Image\ChatGPT Image Aug 29, 2026, 05_39_35 AM.png

This image is a LOCKED VISUAL CONTRACT for Product Admin UX.

Use it to align:

overall composition

Product Workspace hierarchy

cards

tabs

spacing

action hierarchy

Category picker presentation

RTL geometry

Attributes / Variants / Media composition

Do NOT treat it as loose inspiration.

Do not copy malformed generated text blindly.
Use correct Persian Tooba labels while preserving visual structure.

Global UX Principle

UI/UX must be understandable for an ordinary non-technical user without training.

Therefore:

no raw IDs

no implementation terminology

clear VIEW / EDIT

obvious primary actions

progressive disclosure

safe destructive actions

human-readable category paths

simple product attribute editing

Goal

Align existing Product Admin UX to the approved visual contract and enforce:

Level-3-only Product Category assignment.

Product attributes only from effective Category schema.

No Product-local attribute-definition creation.

Preserve Category-change impact safety.

Preserve Variant safety.

Preserve existing customized AppDataGrid.

Do NOT implement Product SEO in this task.

SEO is reclassified to TB-P07-T017 after this task is accepted.

1 — Product Category Picker Audit

Inspect current picker.

Required:

searchable

hierarchical

3-level display

Level 1/2 expandable but not assignable

Level 3 selectable

human path shown

no raw CategoryId

parent levels visually intentional, not broken/disabled-looking

Selected path example:

کالای دیجیتال > موبایل و تبلت > گوشی موبایل

2 — Picker Interaction

Level 1 click:

expand/open

do not assign

Level 2 click:

expand/open

do not assign

Level 3 click:

select

show selected state

enable confirmation

Do not close picker on parent click as if selection succeeded.

3 — Search

Search all category names.

For each result:

reveal path

Level 1/2 result remains navigation-only

Level 3 result selectable

no IDs

4 — Backend Assignment Validation

Backend MUST reject Product assignment to Level 1 or Level 2.

Frontend-only enforcement is insufficient.

Localized error concept:

محصول باید به یک دسته‌بندی سطح سوم اختصاص داده شود.

Use existing validation/error patterns.

5 — Product Create Flow

+ محصول جدید

Initial step:

valid Level-3 Category

Product Name

human Slug

Draft

Create must not succeed with Level 1/2 category.

Keep progressive flow.

6 — Existing Product With Invalid Parent-Level Category

Do NOT silently reassign legacy/current data.

If existing Product is assigned to a non-Level-3 category:

show explicit warning

allow VIEW

require explicit valid category selection before relevant edit/publish path

do not auto-migrate silently

Document handling.

7 — Product Attributes UX Correction

Audit Product Workspace ویژگی‌ها.

Remove any action that means Product-local schema creation.

Forbidden examples:

افزودن ویژگی جدید

ایجاد ویژگی جدید

arbitrary attribute definition selector outside Category schema

The form must be driven directly from effective Category schema.

8 — Required / Optional Attributes

Required:

always shown

clearly marked

required for readiness/save rules as already implemented

Optional:

shown according to UX

may remain empty

user does not need to "add" it first if it belongs to Category schema

Do not turn category-schema fields into manually added Product fields.

9 — Empty Schema UX

If selected category has no effective attributes:

Show:

برای این دسته‌بندی هنوز ویژگی‌ای تعریف نشده است.

Optional authorized action:

مدیریت ویژگی‌های دسته‌بندی

This navigates to Category Admin.

It MUST NOT create an attribute inside Product Workspace.

10 — Category Change Safety

Preserve T012 behavior:

Changing Category:

target must be valid Level 3

preview attribute impact

preserve compatible values

expose incompatible/orphaned values

show newly required fields

explicit confirmation

no silent data loss

11 — Variant Safety

Preserve T013 behavior.

Category change must include variant impact:

changed axes

affected variants

no automatic hard delete

explicit review required

12 — Product Visual Alignment

Compare current Product Admin runtime to locked reference image.

Adjust where needed:

Product workspace header

tabs

card grouping

form section hierarchy

Category picker

Attributes tab

Variants tab

Media tab

action buttons

spacing/density

RTL layout

Do NOT redesign the Product list grid.

13 — Canonical AppDataGrid Lock

Use only:

src/frontend/design-system/app-data-grid/AppDataGrid.tsx

FORBIDDEN:

raw AgGridReact

new grid wrapper

new Product list/table

duplicate page-specific grid CSS

Preserve all existing customizations.

14 — VIEW / EDIT

Preserve global Admin rule:

VIEW:

readable values

no mutation controls

edit action only with permission

EDIT:

inputs

Save / Cancel

dirty-state safety

Do not regress.

15 — Authorization

Use existing SpiceDB-centered authorization.

Backend authoritative.

Product category mutation and Product attributes mutation must not depend only on frontend permission checks.

16 — Tests

Frontend:

picker renders 3 levels

Level 1 not assignable

Level 2 not assignable

Level 3 assignable

parent click expands rather than assigns

search returns path

search parent result non-selectable

selected full path shown

create blocks invalid category

no Product-local attribute-definition creation action

attributes render from effective Category schema

empty schema links to Category management

required/optional behavior

category-change attribute impact preserved

category-change variant impact preserved

VIEW / EDIT preserved

no raw IDs

mobile

visual-contract layout regression

Backend:

reject Level 1 Product assignment

reject Level 2 Product assignment

accept Level 3 Product assignment

create enforces assignable category

update category enforces assignable category

Product attribute outside effective schema rejected

T012 attribute validation preserved

T013 variant safety preserved

17 — Documentation

Create/update:

docs/catalog/PRODUCT-CATEGORY-ASSIGNMENT.md

Document:

3-level rule

navigation vs assignable levels

backend enforcement

Category picker UX

legacy invalid-category handling

Product attributes are Category-schema-driven

no Product-local AttributeDefinition creation

future extensibility if assignment depth becomes configurable

Update:

docs/catalog/PRODUCT-CATALOG-ADMIN.md

docs/catalog/PRODUCT-ATTRIBUTES.md

recovery/project-state docs

18 — Validation

Backend:

restore

build

full tests

Frontend:

typecheck

lint

tests

production build

Require:

0 errors

0 failed

0 skipped

no new warnings

Always:

git diff --check

19 — Runtime

Keep alive:

Backend :5088

Frontend :3000

Shopeiva :3001

Product Admin:
http://localhost:3000/fa/admin/products

20 — Git

Commit + push.

Require:

HEAD == origin/main

tracked tree clean

unrelated user changes preserved

Do not commit the mistaken SEO envelope as canonical T016 content.

If repository task-envelope bookkeeping requires replacing/archive of the mistaken untracked file:

do so explicitly and safely

preserve auditability

report exactly what happened

Acceptance

PASS only if ALL:

locked Product visual reference opened and used

Product Admin visually aligned to reference

canonical AppDataGrid unchanged

Level 1 not assignable

Level 2 not assignable

Level 3 assignable

backend enforces same

full human category path shown

no raw IDs

Product cannot create arbitrary attributes

Product attributes come only from effective Category schema

empty-schema path navigates to Category Admin

category-change attribute impact preserved

category-change variant safety preserved

VIEW / EDIT preserved

full validation green

runtime alive

Git synchronized

USER_VISUAL_ACCEPTED remains NO until manual user review.

Result Contract

Return ONLY:

PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_WORKER_RESULT

Task-ID:
TB-P07-T016
Parent-Task:
TB-P07-T014
Channel:
tooba-main
Status:
PASS | FAIL | BLOCKED | RECOVERY_CONFLICT

Summary:
...

Collision-Recovery:

mistaken SEO envelope found:

mistaken envelope handling:

SEO code implemented:

migration/database touched:

wrong commit/push created:

Visual-Contract:

reference path:

reference opened:

current UI compared:

alignment changes:

AppDataGrid preserved:

Category-Assignment:

Level 1 assignable:

Level 2 assignable:

Level 3 assignable:

picker:

search:

full path:

backend validation:

invalid legacy handling:

Product-Attributes:

arbitrary create/add action:

removed:

effective Category schema:

required:

optional:

empty schema UX:

Category config navigation:

outside-schema values blocked:

Category-Change:

attribute impact:

variant impact:

explicit confirmation:

silent loss:

View-Edit:

VIEW:

EDIT:

permission-aware:

dirty-state:

Authorization:

SpiceDB:

backend authoritative:

Validation:

backend restore:

backend build:

backend full tests:

frontend typecheck:

frontend lint:

frontend tests:

frontend build:

passed:

failed:

skipped:

warnings:

new warnings:

git diff --check:

Documentation:

PRODUCT-CATEGORY-ASSIGNMENT:

PRODUCT-CATALOG-ADMIN:

PRODUCT-ATTRIBUTES:

recovery/state:

Runtime:

Backend:

Frontend:

Shopeiva:

Product Admin:

kept alive:

Git:

branch:

commit:

push:

final HEAD:

origin/main:

synchronized:

tracked tree:

unrelated user changes:

Architectural-Concerns:
...

Visual-Concerns:
...

Blockers:
...

END_TOOBA_WORKER_RESULT

After Result:

Worker -> IDLE / Waiting

keep runtimes alive

do not invent T017

do not continue the stopped SEO task

do not add extra prose

END_TASK