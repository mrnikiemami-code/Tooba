PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_RESULT

Task-ID:
TB-P07-T039

Parent-Task:
TB-P07-T038

Channel:
tooba-main

Status:
PASS

Worker:
tooba-worker-01

Bridge-UUID:
04e1217b-530c-4fa9-87e2-debcf318a3c2

Summary:
Variant axis capability UX repair: distinct ValueKind vs capability-disabled reasons in Category Attributes; editable IsVariantAxisAllowed on Attribute Definition with impact-aware disable preview; backend authoritative validation with stable codes; no cascade mutation on enable/disable. Demo intent preserved (color/storage allowed, screen_size capability false, five_g Boolean). USER_VISUAL_ACCEPTED=NO.

Variant-Axis-Semantics:
IsVariantAxisAllowed = may be variant axis on definition; Category IsVariant = actually used; Product variants from effective Primary Category schema only.

ValueKind-Rules:
Text/Instant/Boolean forbidden; Enumeration supported; Number supported only when capability allows; Boolean not reinterpreted as Enumeration.

Disabled-Reasons:
FA/EN distinct copy in variant-axis-messages.ts; category-attributes-panel shows kind vs capability reason; screen_size shows capability message not type-invalid.

Capability-Edit:
catalog-attribute-ui edit + create: قابل استفاده برای تنوع with helper; PUT variant-axis-capability endpoint.

Enable-Capability:
false→true for Enumeration/Number when supported; backend rejects unsupported ValueKinds (catalog.attribute.variant_axis.value_kind.invalid); does not mutate bindings/products.

Disable-Impact:
GET disable-preview returns category bindings, affected categories, product/combination counts, canDisable; impact dialog when blocked.

Blocking-Rule:
true→false blocked when IsVariant binding exists (catalog.attribute.variant_axis.in_use); no cascade disable; zero-usage allows disable with confirm.

Category-Binding:
ValidateVariantAxis on bind/update; inheritance preserved; integration tests for enable-without-mutation and disable-blocked.

Product-Regression:
Variant axes from category schema only; no arbitrary definition creation; no Price/Stock introduced.

Localization:
fa/en messages in variant-axis-messages.ts; admin-error-map for stable codes; RTL/LTR via existing admin patterns.

Architecture:
CatalogDomain rules + CatalogDirectory impact/set + CatalogAttributeEndpoints; FE api/ui/panel; Shopeiva untouched.

Recovery-SoT:
Current TB-P07-T039; guard updated; USER_VISUAL_ACCEPTED=NO.

Validation:
BE focused 9/0; FE tsc OK; FE focused 12/12; guard 3/3; git diff --check OK.

Focused-Verification:
Contract tests cover five_g/screen_size/color/storage semantics, enable without binding mutation, in-use disable block.

Runtime:
Host :5088 live (kept running); FE :3000; Shopeiva :3001.

Evidence:
docs/evidence/TB-P07-T039/

Git:
HEAD=PLACEHOLDER
origin/main=PLACEHOLDER
HEAD==origin/main

Architectural-Concerns:
None blocking.

Visual-Concerns:
USER_VISUAL_ACCEPTED=NO — human should confirm disabled reasons and capability edit in Admin UI.

Blockers:
None.

END_TOOBA_RESULT
