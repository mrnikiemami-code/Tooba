PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_TASK

Task-ID:
TB-P07-T029

Parent-Task:
TB-P07-T028

Channel:
tooba-main

Title:
Real Media DAM + Upload Library — Product Media, Category Image/Icon/Banner, Reuse/Select/Unassign

Role:
You are the implementation Worker for Tooba.
Implement ONLY the real Media DAM / Upload Library and its Product + Category Admin integration.
Do NOT start Product-create wizard, Rich Text Editor/reference completion, or final acceptance gate yet.

Repository / Safety:

Resolve repository root with git rev-parse --show-toplevel.

Record branch / HEAD / origin/main / git status --short.

Preserve unrelated user/agent changes.

Do NOT reset, clean, stash, checkout-away, or overwrite unrelated dirty files.

Read relevant Catalog Media/Product/Category/Admin/Storage docs and code before editing.

Avoid repo-wide scans.

ARCHITECT ACCEPTANCE

TB-P07-T028 is ACCEPTED.

Accepted baseline:

Attributes and Variants are human-readable.

Variant flow is guided and no longer exposes Axis/Schema/Definition jargon.

Product attributes still come from Primary Category effective schema.

Product != Offer.

no Product.Price / Product.Stock.

USER_VISUAL_ACCEPTED=NO.

HUMAN-REPORTED DEFECT

The user explicitly reported:

Product Media currently does not open a real upload dialog.

Clicking media creates/fakes something instead of uploading/selecting a real asset.

Category has no way to add image or icon.

The final Product Admin reference expects a real visible media collection/gallery.

Category needs image/icon support for navigation, MegaMenu, PLP cards, Home sections, etc.

The previous placeholder/reference-only media seam is no longer sufficient.
Media DAM is now REQUIRED.

CORE ARCHITECTURAL DECISION

Create/use a canonical Media capability that owns binary/file metadata.
Catalog MUST NOT own file bytes.
Catalog entities only reference Media assets.
No duplicate binary storage subsystem inside Catalog.

Conceptually:
MediaAsset

Id

file/storage key

original filename

content type

byte size

width/height when image

checksum/hash if existing convention allows

status/readiness

created/updated metadata

created-by actor if architecture supports it

optional derived/thumbnail references if architecture supports them

CatalogProductMediaReference:

existing Product assignment relation remains Product-owned assignment metadata

AssetId

IsPrimary

DisplayOrder

AltText

Category media assignments:

Category image

Category icon

Category banner/cover

Prefer role-based relation rather than fixed binary columns if architecture supports it.
Do not duplicate MediaAsset data in Catalog.

A — STORAGE / UPLOAD

Audit existing storage abstractions first.

If repository already has file storage abstraction, local filesystem storage, blob/S3-compatible provider abstraction, or upload pipeline, reuse it.

If none exists, implement the smallest extraction-safe storage abstraction.

Development/runtime storage may use local filesystem only if:

behind interface/provider abstraction

path is configurable

no hardcoded Windows-only path

no domain module depends directly on filesystem API

Required upload behavior:

real multipart upload

multiple files

supported image MIME validation

sane size limit from config

filename normalization

server-generated asset ID

no trust in client filename

safe extension/content-type validation

reject unsupported files with stable error code

no path traversal

B — MEDIA LIBRARY

Provide a reusable Admin Media Library dialog/drawer.

Entry points:

Product Media tab

Category image/icon/banner selectors

Library must support:

upload files

browse existing media

search by filename/alt/metadata where available

paging

thumbnail preview

selection

multi-select where Product gallery uses it

single-select where Category role uses it

upload progress/state

empty/loading/error states

Do NOT load entire media library into browser.
Use server pagination/query.
No fake asset creation.

C — PRODUCT MEDIA FLOW

Product Media tab must become a fully real flow.

Button:
افزودن رسانه

opens Media Library with:

آپلود فایل

کتابخانه رسانه‌ها

User may:

upload one or multiple images

select existing media

add selected media to Product

After assignment:

large primary image

thumbnail strip/grid

selected state

set primary

reorder

AltText

unassign

count

readiness

Critical:
حذف از محصول != حذف فایل از کتابخانه

Normal Product Media UX should unassign only.
Asset deletion from DAM, if implemented, must be a separate permissioned action in Media Library.

D — PRODUCT MEDIA READINESS

Product summary/readiness card should reflect real media state.

Examples:

0 images -> incomplete

images but no primary -> incomplete

primary exists -> partial/ready according to existing Product readiness rules

Alt completeness may contribute if current readiness architecture supports it

Use progress/status visual consistent with locked Product Admin reference.
No meaningless hardcoded count.

E — CATEGORY MEDIA

Category Admin must support real media.

At minimum:

تصویر دسته

آیکن دسته

بنر / تصویر کاور

Each:

opens same reusable Media Library

supports selecting existing asset

supports upload

preview

replace

remove/unassign

no raw GUID

If current public UI does not yet consume every role:

store/reference correctly

do not fake front-end use

document integration seam

BUT the Admin selector must be real

F — CATEGORY GENERAL UX

Integrate Category media into Category Workspace without clutter.

Recommended:

compact media card in General tab or dedicated visual section

three roles with previews

clear empty states

actions: انتخاب / تغییر / حذف

Do NOT expose:

MediaAssetId text field

GUID input

technical storage path

G — CATEGORY TREE / MEGAMENU INTEGRATION

Do NOT redesign approved Category Tree.

If Category icon/image is already consumed by MegaMenu/navigation:
wire real Media asset URLs through current contracts.

If not currently consumed:

preserve current behavior

expose a clean contract for later rendering

document it

do not invent unrelated visual redesign

H — THUMBNAILS / IMAGE DELIVERY

Audit existing image processing.

If image resizing/thumbnail infrastructure exists:
reuse it.

If not, minimum acceptable:

preserve original upload

provide browser-safe display URL

record width/height

use CSS/object-fit for admin thumbnails

Do NOT invent a heavyweight image processing service unless already architecturally planned.

I — MEDIA URL / ACCESS

Media serving must be safe and environment-aware.

Use existing static media endpoint or signed/public media endpoint according to current architecture.

No direct absolute local filesystem paths in frontend/API contracts.
No user-visible storage key.

J — MEDIA LIBRARY UX

Normal Persian UX:

Title:
کتابخانه رسانه‌ها

Tabs:

کتابخانه

آپلود فایل

Search:
جستجو در رسانه‌ها

Selection:

Product: multiple

Category image/icon/banner: single

Cards/grid show:

thumbnail

filename or human label

dimensions if useful

file size if useful

selected state

No technical jargon.
English equivalent required.

K — UPLOAD UX

Upload area:

drag & drop

choose files

multi-file

progress

success/failure per file

retry if practical

human error messages

Server validates allowed image types and size.

L — ASSET DELETE POLICY

Unassigning an asset from Product/Category must NOT delete binary asset.

If DAM delete is implemented:

separate explicit action

permission check

confirmation

prevent or warn when asset still referenced

reference-count/query through contracts, no cross-module joins

stable error code if still in use

If full asset deletion is too broad:

omit delete action from normal library in this task

document as later DAM administration concern

Do NOT overload Product unassign into global delete.

M — ALT TEXT

Product AltText remains assignment-scoped.

For Category, if alt is needed and current SEO/accessibility model supports it:

store assignment-scoped localized alt or current canonical alt seam

Do NOT misuse MediaAsset global title as Product-specific alt.

N — ERROR UX

Use centralized T026 Admin error normalizer.

Stable error codes for:

unsupported media type

file too large

upload failed

asset not found

duplicate Product assignment

invalid category media role

stale update

storage unavailable

No:

Bad Request

raw HTTP

raw JSON

exception

filesystem path

fa/en mappings required.

O — PERMISSIONS

Backend authoritative.

At minimum distinguish:

view media

upload media

assign media

unassign media

delete asset if implemented

VIEW mode:

preview only

EDIT mode:

upload/select/assign/unassign/reorder/alt

P — PERFORMANCE

Media Library:

server paging

search

deterministic sorting

no all-assets fetch

avoid N+1 metadata calls

Product gallery:

load assigned assets in one query/composed contract

deterministic DisplayOrder

Q — LOCALIZATION / RTL

fa/en required.

Localize:

Media Library

upload state

errors

buttons

category media roles

empty state

Product gallery controls

fa RTL
en LTR

R — ARCHITECTURE LOCKS

Preserve:

Product != Offer

no Product.Price / Product.Stock

Product media assignment remains Catalog-owned relation

binaries owned by Media capability

no Catalog binary blob/table

Primary/Additional Category architecture

Category Tree approved visuals

AppDataGrid

Attribute/Filter UX

Variant UX

SEO

Publishing

History

Shopeiva untouched

S — TESTS

Backend:

upload success

multi-upload

unsupported type

size limit

safe filename/path

media list paging/search

asset retrieval/serving contract

Product assign existing media

duplicate Product assignment

set primary

reorder

alt

unassign != delete

Category image assign

Category icon assign

Category banner assign

Category replace/unassign

reference-safe delete if delete implemented

localized stable errors

Frontend:

Media Library opens

browse tab

upload tab

upload progress

multiple select Product

single select Category role

Product add

Product primary

thumbnails

reorder

AltText

unassign

Category image/icon/banner preview/select/remove

no raw GUID

no fake media creation

fa/en

RTL/LTR

centralized errors

T — FULL VALIDATION

Run:

backend restore

backend build

backend full tests

frontend typecheck

frontend lint

frontend full relevant tests

frontend production build

git diff --check

Require:

0 errors

0 failed

0 skipped

no new warnings

U — LIVE RUNTIME VERIFICATION

Keep alive:

Backend :5088

Frontend :3000

Shopeiva :3001

Verify LIVE:

Product:

open Product EDIT -> Media

click افزودن رسانه

real dialog opens

upload image

image appears in library

assign to Product

thumbnail appears

set primary

edit AltText

reorder

unassign

asset remains in library

Category:

open Category EDIT

select/upload Category image

select/upload icon

select/upload banner

save/reload

previews persist

remove one assignment

asset remains in library

Error:

upload invalid file

human localized error

no Bad Request

V — EVIDENCE

Create:
docs/evidence/TB-P07-T029/

At minimum:

media-architecture.md

product-media-live.md

category-media-live.md

storage-provider.md

Record:

binary ownership

storage abstraction

upload limits/types

Product unassign semantics

Category roles

live routes/actions

remaining DAM limitations, if any

W — GIT

Commit + push after green validation/live verification.

Require:

HEAD == origin/main

scoped tracked tree clean

unrelated user changes preserved

FOLLOW-UP LOCK — DO NOT IMPLEMENT NOW

Still planned:

TB-P07-T030:
Dedicated Product Create Flow + Rich Text Editor + Locked Reference Completion

TB-P07-T031:
Final Catalog Admin Human Acceptance Gate

ACCEPTANCE

PASS only if ALL:

clicking Product افزودن رسانه opens a REAL Media Library

actual file upload works

no fake asset creation

existing media can be selected

Product gallery is backed by real uploaded assets

unassign != asset delete

Category supports real image/icon/banner selection/upload

no raw MediaAsset GUID in normal UX

no Catalog binary storage subsystem

server validates upload

fa/en works

centralized errors used

full validation green

runtimes alive

Git synchronized

USER_VISUAL_ACCEPTED=NO.

RESULT CONTRACT

Return ONLY:

PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_WORKER_RESULT

Task-ID:
TB-P07-T029

Parent-Task:
TB-P07-T028

Channel:
tooba-main

Status:
PASS | FAIL | BLOCKED | RECOVERY_CONFLICT

Summary:
...

Media-Architecture:

Media owner:

Catalog binary storage:

storage abstraction:

development provider:

configurable path/provider:

MediaAsset model:

thumbnail/image processing:

Upload:

multipart:

multi-file:

MIME validation:

size limit:

safe filename:

path traversal:

progress:

invalid-file error:

Media-Library:

real dialog:

browse:

upload:

search:

paging:

Product multi-select:

Category single-select:

no all-assets fetch:

raw GUID:

Product-Media:

real upload:

select existing:

primary:

thumbnails:

selected state:

reorder:

AltText:

unassign:

unassign deletes asset:

readiness:

fake CTA:

Category-Media:

image:

icon:

banner:

upload:

select existing:

preview:

replace:

unassign:

raw MediaAssetId input:

Asset-Delete:

implemented:

separate action:

referenced asset safety:

Product unassign behavior:

Errors:

centralized normalizer:

unsupported type:

too large:

storage failure:

asset missing:

raw Bad Request:

raw HTTP:

raw code:

filesystem path exposed:

Permissions:

view:

upload:

assign:

unassign:

delete asset:

Localization:

fa:

en:

RTL:

LTR:

Regression:

Product != Offer:

Product.Price:

Product.Stock:

Primary/Additional Categories:

Category Tree:

Attributes:

Filters:

Variants:

AppDataGrid:

SEO:

Publishing:

History:

Shopeiva:

Validation:

backend restore:

backend build:

backend full tests:

frontend typecheck:

frontend lint:

frontend tests:

frontend production build:

passed:

failed:

skipped:

warnings:

new warnings:

git diff --check:

Live-Verification:

Product Media dialog:

Product upload:

Product assign:

Product primary:

Product Alt:

Product reorder:

Product unassign:

asset remains:

Category image:

Category icon:

Category banner:

Category reload:

Category unassign:

invalid upload:

raw error:

Backend:

Frontend:

Shopeiva:

kept alive:

Evidence:

path:

architecture:

product live:

category live:

storage:

Git:

branch:

commit:

push:

final HEAD:

origin/main:

synchronized:

tracked tree:

unrelated user changes:

Architectural-Concerns:
...

Visual-Concerns:
USER_VISUAL_ACCEPTED=NO

Blockers:
...

END_TOOBA_WORKER_RESULT

After Result:

Worker -> IDLE / Waiting

do not invent next task

do not add explanation outside Result envelope

END_TOOBA_TASK