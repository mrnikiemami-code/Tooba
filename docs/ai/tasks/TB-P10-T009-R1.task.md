PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_REPAIR_TASK

Task-ID:
TB-P10-T009-R1

Parent-Task:
TB-P10-T009

Channel:
tooba-main

WorkerId:
tooba-worker-01

AgentType:
cursor

Status:
REPAIR

USER_VISUAL_ACCEPTED=NO

Title:
Product Card Skin Visual Proof Repair — Persistent Screenshots, Admin Preview Evidence, Cross-Surface Visual Verification

Architect Review

TB-P10-T009 is NOT Architect-accepted yet.

The implementation/result is functionally strong, but the task explicitly required persistent visual captures for:

Admin skin controls

default Product Card

each alternate skin

Home + PLP + PDP-related consistency

dark-mode example

alternate-palette example

The Result states:
Browser screenshot files did not persist; runtime proof is HTTP markers + raw JSON.

That is insufficient for a UI/visual feature, especially because:

ProductCardSkin is primarily a visual capability

USER_VISUAL_ACCEPTED remains NO

visual geometry/appearance was part of the contract

the user explicitly requires the final Appearance/Page UI to be understandable, professional, and visually reviewable

Therefore T009 remains unaccepted until visual evidence is persisted and reviewable.

Do NOT redesign the skins.
Do NOT change business behavior.
Do NOT add new skins.
Do NOT implement Landing/Page.
Do NOT start TB-P10-T010.

Recovery State

Phase: P10
Last Architect-accepted: TB-P10-T008-R1
Last Implementation: TB-P10-T009
Current Repair: TB-P10-T009-R1
Worker: tooba-worker-01
Expected branch: main
Expected previous HEAD: 7a6df2c7bb8ab621eb74c5801d48518e9c3ac005
USER_VISUAL_ACCEPTED=NO

Preserve LOCK-SF-001…164.

1. Recovery Preflight

Record:

branch

HEAD

origin/main

git status --short

Recovery/SoT

Host :5088

FE :3000

current PaletteKey / ThemeMode / ProductCardSkin

unrelated local user-authored changes

Unexpected tracked divergence => RECOVERY_CONFLICT/BLOCK.

Evidence:
docs/evidence/TB-P10-T009/r1-recovery-start.md

2. Persistent Screenshot Infrastructure

Use the project/browser runtime mechanism that can actually persist screenshots to repository evidence.

Required:

screenshots must exist as real files after the run

filenames deterministic

no temporary-only browser artifacts

no manual user capture required

screenshots must be readable after Worker completes

Store under:
docs/evidence/TB-P10-T009/screenshots/

If existing tooling has a canonical screenshot/evidence helper, use it.
Do not create a brittle one-off if reusable project tooling exists.

Evidence:
docs/evidence/TB-P10-T009/r1-screenshot-method.md

3. Admin Appearance Visual Evidence

Capture the real Admin Appearance UI showing:

Product Card style section

all four skin options visible

currently selected skin

preview state

dirty state after selecting another skin

saved state after successful save

The UI must be understandable to a normal Store Admin:

no raw enum names as primary UX unless mapped to friendly labels

no technical IDs exposed

no raw JSON/config

clear current selection

clear Save/Cancel state

visually professional and coherent with current Admin settings

If the UI is technically functional but confusing or visibly unfinished, report Visual-Concern rather than hiding it.

Required screenshot files:

admin-skin-default.png

admin-skin-preview.png

admin-skin-saved.png

4. Four Skin Visual Evidence

Persist one representative screenshot for each skin using the same product/card context where practical:

classic.png

clean.png

elevated.png

glass.png

Required:

same canonical Product Card behavior

same geometry

same data/content

visual differences must be visible enough to justify four separate options

no skin may look broken in light mode

If two skins are visually indistinguishable in real runtime, report that as a Visual-Concern.

5. Cross-Surface Consistency Visual Evidence

Persist screenshots proving the selected Store skin is reused consistently on:

Home product rail

PLP/category listing

PDP related/recommended products

Cart recommendations if available in current runtime

Wishlist only if easy to reach and already part of canonical flow

Required files:

skin-home.png

skin-plp.png

skin-pdp-related.png

skin-cart-recommendation.png if applicable

Do not fabricate a missing recommendation section.
If a surface is not available with current seeded/runtime data, document why.

6. Theme + Palette Composition Visual Evidence

Capture:

A. one alternate skin in Dark mode
B. one alternate skin with a non-default PaletteKey
C. one alternate skin with both Dark + non-default palette if practical

Required files:

skin-dark.png

skin-alt-palette.png

skin-dark-alt-palette.png if scenario available

Verify:

text readable

border/surface coherent

glass/elevation treatment still understandable

CTA/actions still accessible

status colors remain semantic

7. Visual Geometry Guard

For each skin compare:

image aspect ratio

title block height/flow

price position

action positions

hover action behavior

card width

grid density

No skin may alter the locked geometry contract.

Document:
docs/evidence/TB-P10-T009/r1-visual-geometry.md

8. Default Visual Equivalence Recheck

Restore:

ProductCardSkin = classic

PaletteKey = tooba-blue

ThemeMode = canonical default

Capture:

classic-restored.png

Verify the classic card remains visually equivalent to accepted T008-R1 baseline.

No surprise persistent style leakage from previously selected skins.

9. Runtime Recheck

Use Host :5088 + FE :3000.

Repeat focused A–N only as needed to prove visuals:

A. classic default
B. clean
C. elevated
D. glass
E. dark composition
F. alternate palette composition
G. Home/PLP/PDP-related consistency
H. restore classic/default

Record:

API save count

ProductCardSkin

PaletteKey

ThemeMode

SSR/root markers

data-product-card-skin

console errors

hydration warnings

Evidence:
docs/evidence/TB-P10-T009/r1-runtime.md

10. Functional Regression

Re-run only focused regression:

appearance save

invalid skin rejected

Store isolation

Home/PLP/PDP/card tests

test

test

geometry guards

account/cart/checkout critical regression

recovery guard

git diff --check

No unrelated full repair work.

11. Anti-Pattern Recheck

Confirm no:

second Product Card behavior component

page-local skin registry

raw CSS/HTML/JS from DB

arbitrary style payload

per-card appearance fetch

localStorage as Store appearance SoT

screenshot-only CSS hacks

test-only visual branches

new layout variant

Landing/Page implementation

Evidence:
docs/evidence/TB-P10-T009/r1-antipattern-scan.md

12. Lock Registry

No new lock unless a genuinely new invariant is discovered.

Preserve LOCK-SF-001…164.
Do not renumber.

13. Recovery / Pipeline

At completion:

Phase P10

Last Architect-accepted before review: TB-P10-T008-R1

Last Implementation: TB-P10-T009-R1

Current Issued/Repair: none

USER_VISUAL_ACCEPTED=NO

Worker IDLE

branch main

ending HEAD

HEAD==origin/main

task-owned clean

unrelated local user changes preserved

do NOT invent/start TB-P10-T010

Run recovery staleness guard.

Acceptance

PASS only if ALL:

all required screenshot files persist after runtime

Admin skin UI is visually reviewable

all four skins have persistent runtime screenshots

visual differences are meaningful but geometry-safe

Home/PLP/PDP-related consistency is visually proven

dark-mode composition visually proven

alternate-palette composition visually proven

classic/default restoration visually proven

no visual geometry drift

no business behavior divergence

focused tests green

anti-pattern scan CLEAN

Recovery exact

HEAD==origin/main

Worker IDLE

no TB-P10-T010

USER_VISUAL_ACCEPTED remains NO

Result Contract

Return ONLY canonical BRIDGE-WAKE-V1 Result with:

Summary
Applicable-Locks
Recovery-Start
Screenshot-Method
Admin-Skin-Visual
Four-Skin-Visual
Cross-Surface-Visual
Theme-Palette-Visual
Visual-Geometry
Default-Restore
Focused-Validation
Runtime
AntiPattern-Scan
Lock-Registry
Recovery-SoT
Git
Architectural-Concerns
Visual-Concerns
Blockers

END_TOOBA_REPAIR_TASK
