PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_REPAIR_TASK

Task-ID:
TB-P10-T009-R2

Parent-Task:
TB-P10-T009-R1

Channel:
tooba-main

WorkerId:
tooba-worker-01

AgentType:
cursor

Status:
REPAIR

USER_VISUAL_ACCEPTED=NO

Title:
Product Card Skin UX + Runtime Integrity Repair — Real Preview Thumbnails, Human Labels, Hydration/Render Loop Cleanup

Architect Review

TB-P10-T009-R1 is NOT Architect-accepted yet.

Persistent screenshots now exist and the cross-surface visual proof is substantially improved. However, the Result itself reports three issues that conflict with the visual/UX acceptance contract:

Admin option thumbnails are empty chrome boxes

English keys remain as secondary captions

one Admin Maximum update depth console error plus existing hydration attribute mismatches during Playwright runtime

The user explicitly requires Appearance/Page configuration UI to be:

understandable for a normal user

professional

visually clear

not technically leaky

A skin selector made of empty chrome boxes does not adequately communicate what each option means.
A Maximum update depth error is not acceptable in a settings UI runtime.
Hydration mismatches must be root-caused and eliminated if they are in the task-owned Appearance/Skin path.

Do NOT redesign Product Card behavior.
Do NOT add new skins.
Do NOT change grid geometry.
Do NOT implement Landing/Page.
Do NOT start TB-P10-T010.

Recovery State

Phase: P10
Last Architect-accepted: TB-P10-T008-R1
Last Implementation: TB-P10-T009-R1
Current Repair: TB-P10-T009-R2
Worker: tooba-worker-01
Expected branch: main
Expected previous HEAD: 3af90dcc0417b25c2ab7f8083bc11d68793b5acb
USER_VISUAL_ACCEPTED=NO

Preserve LOCK-SF-001…164.

1. Recovery Preflight

Record:

branch

HEAD

origin/main

git status --short

Recovery/SoT

Host :5088

FE :3000

current PaletteKey / ThemeMode / ProductCardSkin

unrelated local user-authored changes

Unexpected tracked divergence => RECOVERY_CONFLICT/BLOCK.

Evidence:
docs/evidence/TB-P10-T009/r2-recovery-start.md

2. Reproduce Admin UX Problems

Open the real Admin Appearance page and reproduce:

A. Product Card skin option tiles

capture current empty/thin chrome-box problem

identify why preview content is not informative

B. English technical keys

identify every exposed raw key: classic/clean/elevated/glass or similar

classify whether it is primary label, secondary caption, debug text, data attribute only

C. Runtime console

reproduce Maximum update depth

capture component stack / call path

reproduce hydration mismatch

identify exact attributes/components involved

Evidence:
docs/evidence/TB-P10-T009/r2-defect-reproduction.md

Do not patch blindly before root cause is documented.

3. Human-Centered Skin Selector

Repair the Admin Product Card style selector so a normal merchant can understand each choice without technical knowledge.

Required:

Persian human-facing name for each skin

short Persian explanation

meaningful visual preview

selected/current state obvious

preview cards should show a realistic mini product card composition, not an empty rectangle

visual differences between skins should be understandable at a glance

Suggested user-facing naming may be equivalent to:

کلاسیک

ساده

برجسته

شیشه‌ای

Technical stable keys may remain internally, but:

do not expose raw enum/key as visible UI unless there is a strong product reason

no JSON/config identifiers shown to users

Preview should include enough real/card-like content to show:

image well

title

price

CTA/action treatment

border/radius/shadow/surface difference

No duplicate business logic is allowed.

4. Preview Must Reuse Canonical Skin Definitions

The Admin preview must derive from the same canonical Product Card skin registry.

Allowed:

lightweight preview component that consumes canonical skin presentation tokens

the real canonical Product Card component with safe preview data if architecture allows

Forbidden:

separately hard-coded fake styles

duplicated skin CSS

a second registry

preview-only special cases that can drift from Storefront

Evidence:
docs/evidence/TB-P10-T009/r2-admin-preview-contract.md

5. Fix Maximum Update Depth

Root-cause and eliminate the Admin Maximum update depth error.

Inspect:

effect dependency loops

state synchronization between saved/current/draft appearance

preview state

query invalidation

derived objects recreated each render

ThemeMode/Palette/Skin state coupling

Required:

no render loop

no repeated save/update side effect

no repeated appearance fetch caused by state churn

no timing/debounce/sleep workaround

no suppressing console error

Evidence:
docs/evidence/TB-P10-T009/r2-render-loop-fix.md

6. Hydration Mismatch Cleanup

Root-cause the hydration attribute mismatches seen in the Appearance/Skin runtime.

Required:

determine whether mismatch is task-owned or pre-existing unrelated

if task-owned in Appearance/Theme/Skin path, fix it

if genuinely unrelated and outside scope, prove exact source and show Appearance/Skin pages are clean

Do NOT dismiss generic hydration warnings without attribution.

For task-owned path:

SSR markup/data attributes/classes must match hydrated client state

ProductCardSkin marker must not differ server/client

Theme/Palette/Skin preview should not mutate Storefront SSR markers

Evidence:
docs/evidence/TB-P10-T009/r2-hydration.md

7. Visual Quality Recheck

Re-capture Admin screenshots after repair:

Required:

admin-skin-default-r2.png

admin-skin-preview-r2.png

admin-skin-saved-r2.png

The screenshots must prove:

four options are visually meaningful

labels are Persian/human-readable

selected state is obvious

Save/Cancel/dirty state is understandable

no raw technical keys in visible primary/secondary UI

overall panel is professional and consistent with Admin design language

8. Four-Skin Distinctness Recheck

Re-capture:

classic-r2.png

clean-r2.png

elevated-r2.png

glass-r2.png

Use same representative product.

Acceptance:

differences are visible and meaningful

geometry remains identical

none are cosmetically broken

dark/light compatibility remains

If two skins remain effectively indistinguishable, repair presentation tokens enough to make the intended distinction clear WITHOUT changing geometry or behavior.

9. Runtime Console Must Be Clean

Use Host :5088 + FE :3000.

During:

Admin Appearance open

switch palette

switch ThemeMode

switch ProductCardSkin

preview

cancel

save

navigate Storefront Home/PLP/PDP/Cart

Required:

no Maximum update depth

no task-owned hydration mismatch

no React render-loop warnings

no duplicate save

no duplicate appearance GET storm

no uncaught exceptions

Document exact console result.

Evidence:
docs/evidence/TB-P10-T009/r2-runtime-console.md

10. Functional Regression

Re-run:

valid skin save

invalid skin 400

palette preserved

ThemeMode preserved

Store isolation

cache invalidation

Home/PLP/PDP/Cart skin consistency

default restore

dark compatibility

alternate palette compatibility

account/cart/checkout critical regressions

11. Focused Validation

Required:

Admin appearance tests

Product Card skin tests

preview registry-sharing tests

no render-loop test if practical

hydration/SSR marker tests

test

test

Home/PDP structure guards

scoped lint/typecheck

full tsc state reported truthfully

recovery guard

git diff --check

Do NOT repair unrelated Admin/grid typecheck failures unless this task directly owns the failing files.

12. Anti-Pattern Gate

Scan for:

preview-specific duplicate skin registry

hard-coded preview-only CSS

raw technical key visible to user

effect loop hidden with timeout/debounce

console suppression

hydration warning suppression

duplicate Product Card behavior component

localStorage as Store appearance SoT

page-local skin fetch

grid/layout change

Landing/Page implementation

Any introduced real occurrence => REPAIR.

Evidence:
docs/evidence/TB-P10-T009/r2-antipattern-scan.md

13. Lock Registry

Add a non-duplicate lock only if a genuinely new invariant is needed.

Candidate only if not already covered:

Store Admin appearance choices must use human-readable labels and meaningful visual previews; stable technical keys remain internal.

If added, append after LOCK-SF-164.
Do not renumber existing locks.

14. Recovery / Pipeline

At completion:

Phase P10

Last Architect-accepted before review: TB-P10-T008-R1

Last Implementation: TB-P10-T009-R2

Current Issued/Repair: none

USER_VISUAL_ACCEPTED=NO

Worker IDLE

branch main

ending HEAD

HEAD==origin/main

task-owned clean

unrelated local user changes preserved

do NOT invent/start TB-P10-T010

Run recovery staleness guard.

Acceptance

PASS only if ALL:

empty chrome-box previews replaced by meaningful product-card previews

Persian/human-readable labels/explanations used

raw technical skin keys not visibly leaked to normal Admin UI

all four skin choices visually distinguishable

canonical registry remains single source

Maximum update depth root-caused and eliminated

Appearance/Skin runtime has no task-owned hydration mismatch

Admin console clean during select/preview/cancel/save

Save/Cancel/dirty UX clear

geometry/business behavior unchanged

cross-surface application preserved

dark/palette compatibility preserved

focused tests green

screenshots persisted

anti-pattern scan CLEAN

Recovery exact

HEAD==origin/main

Worker IDLE

no TB-P10-T010

USER_VISUAL_ACCEPTED remains NO

Result Contract

Return ONLY canonical BRIDGE-WAKE-V1 Result with:

Summary
Applicable-Locks
Recovery-Start
Defect-Reproduction
Admin-Skin-UX
Admin-Preview-Contract
Render-Loop-Fix
Hydration
Four-Skin-Distinctness
Runtime-Console
Focused-Validation
Runtime
AntiPattern-Scan
Lock-Registry
Recovery-SoT
Git
Architectural-Concerns
Visual-Concerns
Blockers

END_TOOBA_REPAIR_TASK