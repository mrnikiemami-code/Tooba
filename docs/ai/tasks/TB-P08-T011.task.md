# TB-P08-T011

Bridge UUID: `91e526da-ca47-4a58-9ff3-a4a92fd21bb6`  
Channel: `tooba-main`  
Task ID: `TB-P08-T011`

Full downloadable task body lives on Bridge. Summary of Worker requirements:

## Scope

1. **Article list language tabs** — active languages from Admin API; URL `?language=`; server-side locale filter only; no locale column/filter; create link carries language.
2. **Draft-first create** — dynamic languages; create without `authorId`; no author-availability gate; navigate to workspace.
3. **Author picker** — FE uses `/v1/admin/content/authors/picker?activeOnly=true`.
4. **Backend** — allow null author on draft; empty `authorDisplayName`; `CanChangeLocale()` pristine-draft policy + gallery lock in directory.
5. **FE locale lock** — mirror pristine-draft policy; admin VIEW/EDIT language UX + searchable author picker.
6. **Error map** — locale_locked, author codes, language inactive.
7. **Evidence + recovery SoT** — update PROJECT-STATE / recovery / staleness guard; `USER_VISUAL_ACCEPTED=NO`; do not invent T012.
