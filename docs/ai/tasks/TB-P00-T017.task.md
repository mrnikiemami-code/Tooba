# Tooba — TB-P00-T017 — First-Party Analytics Architecture

BEGIN_TOOBA_CURSOR_TASK_V1

Protocol-Version: 1
Task-ID: TB-P00-T017
Phase: P00 — Architecture / Discovery
Type: Architecture Design / First-Party Analytics
Repository: https://github.com/mrnikiemami-code/Tooba
Primary-Branch: main
Implementation-Agent: Cursor
Architect: ChatGPT
Execution-Mode: PIPELINE
Depends-On: TB-P00-T016
Architect-Decision-On-Dependency: ACCEPTED

## Objective

Design Tooba's first-party analytics architecture deeply enough to support:

- PageView and navigation analytics;
- Visitor / Session concepts;
- ProductView;
- Search analytics;
- AddToCart;
- Checkout;
- Purchase / Conversion;
- campaign / attribution;
- marketplace seller analytics;
- single-store tenant analytics;
- content and landing-page analytics;
- search zero-result and click-through analytics;
- funnel analysis;
- privacy-aware event collection;
- asynchronous/non-blocking ingestion;
- future recommendation/personalization;
- future AI/ranking inputs;
- operational dashboards without Analytics becoming business source of truth;
- professional Admin/Seller analytics UX;
- future data warehouse / stream / analytics platform evolution.

This task is architecture documentation only.

Do not implement analytics SDKs, trackers, databases, dashboards, cookies, consent UI, event pipelines, warehouse, BI, or Shopeiva integration.

---

## 1. Repository Recovery

Before edits:

```bash
git rev-parse --show-toplevel
git fetch origin
git branch --show-current
git rev-parse HEAD
git rev-parse origin/main
git status --short --branch
```

Require:

```text
branch = main
HEAD == origin/main
working tree clean
```

Unsafe state =>

```text
RECOVERY_CONFLICT
```

Never force push, rewrite history, destructively reset, silently stash, or overwrite unknown work.

Expected predecessor:

```text
07f508daf5467bc95c3916212deba1edb21625c5
```

If synchronized main legitimately advanced, continue from current safe main.

---

## 2. Source-of-Truth Transition

Update:

```text
TB-P00-T016 = ACCEPTED
TB-P00-T017 = ISSUED / AWAITING_ARCHITECT_ACCEPT
```

Roadmap:

```text
P00 Media / Image Pipeline = COMPLETE
P00 First-Party Analytics = IN_PROGRESS
```

Do not mark T017 accepted.

---

## 3. Create Architecture Document

Create:

```text
docs/architecture/16-first-party-analytics.md
```

It must contain all sections below.

---

# A. Core Separation

Hard rules:

```text
Analytics != Business Source of Truth
Analytics != Audit Log
Analytics != Technical Logging
Analytics != Authorization
```

Analytics owns behavioral/derived observation data.

Business modules remain authoritative for:

```text
Order
Payment
Pricing
Inventory
Catalog
Party
Authorization
```

Analytics may consume business facts but must not become the system that decides them.

---

# B. Analytics Event Model

Define a normalized conceptual event envelope.

Potential fields:

```text
EventId
EventName
OccurredAt
ReceivedAt
Tenant/Deployment
VisitorId
SessionId
IdentityId where appropriate
PartyId where appropriate
Anonymous/Pseudonymous subject
Page/Route context
Locale
Market
Currency
SalesChannel
Device/UserAgent summary
Referrer
Campaign attribution
Entity references
Properties
CorrelationId
TraceId where safe
SchemaVersion
```

Do not create schema.

Need explicit event versioning.

---

# C. Event Taxonomy

At minimum preserve event families:

```text
PageView
SessionStarted
ProductViewed
CategoryViewed
BrandViewed
SearchPerformed
SearchResultClicked
SearchZeroResult
FilterApplied
SortChanged
AddToCart
RemoveFromCart
CartViewed
CheckoutStarted
CheckoutStepCompleted
CheckoutFailed
OrderPlaced
PaymentSucceeded
PurchaseCompleted
ContentViewed
LandingViewed
CTAClicked
SectionImpression
PromotionViewed
PromotionClicked
```

Exact names remain later detail.

Avoid uncontrolled arbitrary event naming.

---

# D. Client vs Server Events

Separate:

```text
client-observed UX event
server-confirmed business event
```

Examples:

```text
button click -> client-observed
OrderPlaced -> server-confirmed
PaymentSucceeded -> server-confirmed
```

Do not trust client events as proof of revenue/business completion.

Critical conversion facts must come from owning backend domains/events.

---

# E. Purchase / Revenue Truth

Hard rule:

```text
Analytics purchase event is derived from accepted Order/Payment business facts.
```

Do not calculate business revenue truth from client-side thank-you-page events alone.

Need deduplication/idempotency around conversion events.

---

# F. Visitor

Define conceptual:

```text
Visitor
```

as analytics/experience observation identity, not authentication identity.

A visitor may be:

```text
anonymous
authenticated
returning
cross-session
```

Do not automatically merge all device/browser visitors into one legal person.

---

# G. Session

Define analytics Session separately from authentication Session.

Hard rule:

```text
Analytics Session != Authentication Session
```

Analytics session may represent:

```text
bounded browsing activity
```

Authentication session represents security/authentication lifecycle.

Do not reuse auth session token as analytics identifier.

---

# H. Anonymous to Authenticated Transition

Design conceptual handling for:

```text
anonymous visitor
→ login/register
→ authenticated identity
```

Need ability to associate future analytics according to privacy/product policy.

Do not automatically rewrite all historical anonymous data onto a known person unless explicit policy allows it.

Exact identity stitching:

```text
NEEDS_LATER_P00_DETAIL
```

---

# I. Tenant Isolation

Single-Store:

Every analytics event must carry resolved:

```text
TenantId
```

or equivalent immutable tenant context.

No cross-store analytics leakage.

Marketplace:

Use deployment/marketplace context plus seller/entity dimensions where relevant.

Do not invent fake tenant semantics for Marketplace.

---

# J. Locale / Market / Currency

Preserve:

```text
Locale != Market != Currency
```

Analytics events may need all three.

Examples:

```text
Locale -> presentation language
Market -> commercial context
Currency -> displayed/order currency
```

Do not infer one from another.

---

# K. Sales Channel

Analytics must preserve:

```text
SalesChannel
```

where commercially meaningful.

Future comparisons may include:

```text
DIRECT
MARKETPLACE
B2B
CORPORATE
AFFILIATE
API
```

Exact values deferred.

---

# L. Attribution

Design conceptual attribution dimensions:

```text
utm_source
utm_medium
utm_campaign
utm_content
utm_term
referrer
landing page
affiliate/campaign identifier
internal campaign
```

Do not let arbitrary raw query parameters become permanent uncontrolled schema.

Need normalized attribution model.

---

# M. First-Touch / Last-Touch

Preserve future ability to calculate:

```text
first-touch
last-touch
multi-touch future
```

Do not lock one attribution methodology.

Store enough normalized evidence to derive models later.

---

# N. Search Analytics

Search analytics must support:

```text
query
normalized query
result count
zero-result
filters
sort
clicked result
clicked position
entity type
conversion after search
```

Privacy-aware.

This data may later improve:

```text
ranking
synonyms
typo handling
recommendations
```

Search remains authoritative for search behavior execution; Analytics observes outcomes.

---

# O. Product Analytics

Preserve:

```text
ProductView
OfferView where relevant
AddToCart
Wishlist future if enabled
Compare future if enabled
Purchase
Revenue attribution
```

Do not promote template-only features into requirements.

Only document future-compatible extension points.

---

# P. Content Analytics

Content/Page Composition may emit:

```text
ContentView
LandingView
SectionImpression
CTA click
ProductCollection interaction
Scroll-depth future
```

Do not encourage excessive/noisy tracking by default.

Need stable:

```text
ContentId
RevisionId where meaningful
PageDefinitionId
SectionInstanceId
```

or equivalent opaque references.

---

# Q. Marketplace Seller Analytics

Seller-facing analytics may need:

```text
views
offer impressions
clicks
add-to-cart
orders
conversion
revenue projection
stockout effect
search visibility
```

But Seller dashboards must only expose seller-authorized data.

Do not let seller analytics access canonical data of other sellers improperly.

---

# R. Admin Analytics

Admin may need cross-business views:

```text
GMV/revenue
orders
conversion
traffic
search performance
category performance
seller performance
tenant/store performance
content performance
campaign performance
```

Use analytics/read models.

Do not achieve dashboards through cross-module transactional joins.

---

# S. Analytics Ingestion

Hard requirement:

Customer-facing synchronous requests must not unnecessarily block on analytics persistence.

Conceptual:

```text
request/use-case
→ emit analytics observation/fact
→ asynchronous ingestion/buffer
→ analytics storage/projection
```

Server-confirmed business events may flow via:

```text
outbox/integration events
```

No implementation.

---

# T. Delivery Reliability

Different event classes may have different reliability requirements.

Example:

```text
UI hover/click -> lossy may be acceptable
PurchaseCompleted -> durable delivery required
```

Define classes such as:

```text
BEST_EFFORT
IMPORTANT
BUSINESS_CONFIRMED
```

or equivalent conceptual policy.

Do not lock names.

---

# U. Idempotency / Deduplication

Need:

```text
EventId
producer identity
deduplication
business-event correlation
```

Critical events like Purchase must not double count because of retries.

Client events may require different duplicate handling.

---

# V. Event Ordering

Do not assume perfect global event order.

Need handling for:

```text
late events
out-of-order events
retry
clock skew
client offline send
```

Use:

```text
OccurredAt
ReceivedAt
```

or equivalent.

Business-confirmed events can rely on domain event timestamps/versions where available.

---

# W. Event Schema Versioning

Analytics events evolve.

Need:

```text
SchemaVersion
backward-compatible consumers
transform/migration pipeline
event contract governance
```

Avoid editing historical event meaning silently.

---

# X. Analytics Storage Boundary

Do not lock vendor/database.

Future storage may evolve through:

```text
PostgreSQL initial analytical tables
ClickHouse
data warehouse
lake
stream processor
BI platform
```

Architecture should keep event contracts and ingestion boundaries stable.

Do not force transactional app modules to query analytics tables directly for business decisions.

---

# Y. Operational vs Analytical Read Models

Separate:

```text
operational read model
```

from:

```text
analytical aggregate
```

Examples:

```text
Order detail -> operational
Orders per day -> analytical
Search conversion by query -> analytical
```

Admin may compose both through BFF/dashboard application layer.

---

# Z. Real-Time vs Batch

Preserve support for:

```text
near-real-time counters
hourly/daily aggregates
batch historical analysis
```

Do not require all analytics to be real time.

UI should communicate freshness if data is delayed.

---

# AA. Funnel Model

Support funnel analysis across:

```text
PageView
ProductView
AddToCart
CheckoutStarted
OrderPlaced
PaymentSucceeded / PurchaseCompleted
```

Need stable identity/correlation dimensions without coupling business domains to analytics implementation.

Do not hardcode one funnel only.

---

# AB. Conversion

Define conversion events based on authoritative server facts.

Potential dimensions:

```text
OrderId
Buyer context pseudonymous reference
Tenant
Seller
Market
SalesChannel
Currency
Amount
ItemCount
Campaign attribution
Search attribution
```

Avoid leaking unnecessary PII.

---

# AC. Money in Analytics

Money values must include:

```text
Amount
Currency
```

No bare revenue number.

Do not aggregate different currencies blindly.

Cross-currency reporting may require explicit FX/reporting policy later.

Analytics does not retroactively change Order money truth.

---

# AD. Privacy / Data Minimization

Architecture principles:

```text
collect only needed data
avoid raw sensitive identifiers where unnecessary
pseudonymize/hash where appropriate
separate security/audit data
avoid passwords/tokens/OTP/payment secrets
avoid raw PAN/CVV
```

Do not make legal compliance claims.

Consent/legal policy remains later product/legal detail.

---

# AE. PII Boundary

Avoid stuffing:

```text
email
phone
national ID
full name
address
```

into general analytics events by default.

Prefer opaque/pseudonymous references.

If a use-case requires PII, it needs explicit policy and narrow handling.

---

# AF. Cookies / Client Storage

Do not implement or lock cookie strategy.

Document future need to distinguish:

```text
essential/security storage
analytics visitor/session storage
consent-driven optional tracking
```

Exact consent/cookie policy:

```text
NEEDS_LATER_P00_DETAIL
```

---

# AG. External Analytics Integration

First-party analytics must coexist with optional external tools such as:

```text
Google Analytics
Tag Manager
Meta Pixel
other marketing analytics
```

Do not make external trackers the source of truth.

Use adapter/integration boundary.

Tooba first-party events should remain available independently.

---

# AH. Analytics Event Bus Boundary

Do not require a specific broker initially.

Architecture must allow:

```text
in-process buffered delivery
database-backed outbox/queue
future message broker
future stream platform
```

Consumer-facing request path should remain decoupled.

---

# AI. Recommendation Boundary

Future Recommendation may consume:

```text
views
clicks
cart events
purchases
search behavior
content interactions
```

Analytics provides behavioral signals.

Recommendation owns recommendation model/output.

Do not make Analytics decide recommendations directly.

---

# AJ. Personalization Boundary

Future personalization may consume analytics signals but must respect:

```text
tenant
authorization
privacy
market
locale
customer context
```

Search/Content/Pricing remain separate owners.

---

# AK. AI Boundary

Future AI may consume aggregated/approved analytics signals.

Examples:

```text
popular queries
zero-result patterns
content engagement
product interest trends
```

Do not expose raw sensitive behavioral history indiscriminately to AI.

AI authorization/privacy architecture remains separate.

---

# AL. Ranking Feedback

Search ranking may later consume analytics-derived signals.

Need controlled feature feed:

```text
popularity
CTR
conversion
zero-result query patterns
```

Analytics events must not directly mutate ranking weights without a controlled versioned ranking pipeline.

---

# AM. Bot / Internal Traffic

Preserve ability to distinguish:

```text
bots
health checks
admin/internal traffic
test automation
preview/staging
```

from real customer analytics.

Do not count known system traffic as commerce behavior by default.

Exact detection policy later.

---

# AN. Event Quality

Need data-quality controls for:

```text
missing required dimensions
invalid currency
unknown event version
duplicate IDs
impossible timestamps
cross-tenant references
malformed entity refs
```

Invalid events should not silently poison analytics.

---

# AO. Late / Offline Client Events

Client may send buffered events late.

Need rules around:

```text
OccurredAt
ReceivedAt
session attribution
campaign attribution
retention
```

No implementation.

---

# AP. Analytics Retention

Different event classes may have different retention.

Do not lock periods.

Preserve:

```text
raw event retention
aggregate retention
deletion/anonymization support
```

Exact policies later.

---

# AQ. Tenant Deletion / Data Lifecycle

Single-Store tenant lifecycle may require:

```text
analytics export
retention
deletion/anonymization
```

Do not allow tenant deletion to leave uncontrolled identifiable analytics remnants.

Exact legal/business rules later.

---

# AR. Dashboards

Professional dashboards should be decision-oriented.

Admin examples:

```text
revenue trend
conversion funnel
top categories/products
search zero-result
seller performance
campaign performance
content performance
stockout impact
```

Seller examples:

```text
offer views
conversion
orders
revenue
search visibility
stockout impact
```

Do not reduce analytics UI to raw event tables.

---

# AS. UI / UX for Analytics

Analytics UX must provide:

```text
clear KPI hierarchy
date range
comparison period
filters
drill-down
loading states
empty states
error states
data freshness
export where appropriate
mobile usability
RTL/LTR
accessible charts/tables
```

Charts must not be the only representation where accessibility requires tabular/text alternatives.

Do not expose backend event schema directly as the dashboard IA.

---

# AT. Dashboard Authorization

SpiceDB must control access to analytical scopes.

Examples:

```text
tenant analytics
seller analytics
finance analytics
content analytics
security-sensitive reports
```

Avoid N+1 authorization checks; use scope-aware projections/bulk authorization.

---

# AU. Analytics Caching

Aggregates/dashboard results may be cached.

Cache keys must include relevant:

```text
Tenant
Seller
Date Range
Market
Currency/reporting currency
SalesChannel
Filter dimensions
Authorization scope where applicable
```

No cross-tenant leakage.

Analytics caching must not affect business transaction truth.

---

# AV. Reporting Currency

Multi-currency reporting requires explicit policy.

Do not sum:

```text
100 USD + 100 EUR
```

as 200 generic revenue.

Preserve:

```text
original transaction amount/currency
optional normalized reporting amount/currency
FX source/timestamp/version
```

Exact reporting conversion policy:

```text
NEEDS_LATER_P00_DETAIL
```

---

# AW. Event-to-Aggregate Flow

Conceptual:

```text
events
→ validated ingestion
→ raw/normalized event store
→ aggregates/projections
→ dashboards/exports/models
```

Do not force dashboard queries over raw event logs.

---

# AX. Rebuild / Recompute

Aggregates should be recomputable where possible.

Need:

```text
replay/rebuild
aggregate version
backfill
late-event correction
```

Do not make irreversible counters the only analytical truth.

---

# AY. Reconciliation

Need reconciliation between:

```text
Analytics Purchase count
Order authoritative count
Payment authoritative count
```

Differences must be observable.

Analytics does not overwrite Order/Payment to "fix" discrepancies.

---

# AZ. Observability of Analytics Pipeline

Need:

```text
ingestion lag
drop rate
invalid events
duplicate rate
queue/backlog
consumer failures
aggregate freshness
event volume
tenant volume anomalies
```

Technical telemetry separate from analytics product metrics.

---

# BA. Export / BI Readiness

Preserve future:

```text
CSV/export
scheduled reports
BI tools
warehouse export
data science
```

Do not expose transactional DB joins as BI strategy.

Use analytical datasets/projections.

---

# BB. Data Ownership Matrix

Create matrix across:

```text
Analytics
Catalog
Search
Cart
Checkout
Order
Payment
Inventory
Content
Page Composition
Authorization
Recommendation future
AI future
```

Against:

```text
PageView
ProductView
SearchPerformed
AddToCart
CheckoutStarted
Purchase fact
Revenue truth
inventory truth
content truth
permission
behavioral aggregate
recommendation output
AI knowledge
```

Mark:

```text
OWNER
SOURCE
OBSERVATION
CONSUMER
PROJECTION
NOT_OWNER
```

---

# BC. Failure Matrix

Analyze:

```text
Client Event Lost
Duplicate Event
Invalid Event Schema
Ingestion Unavailable
Queue Backlog
Late Event
Cross-Tenant Event
Purchase Event Missing
Analytics Store Unavailable
Aggregate Build Failure
Dashboard Stale
External Analytics Vendor Down
Authorization Scope Error
```

For each classify:

```text
block customer request?
retry?
drop?
durable?
reconcile?
alert?
customer/admin-visible freshness?
```

Hard direction:

Most analytics failures must not break customer purchase paths.

Business-confirmed conversion facts need stronger delivery/reconciliation than generic UI observations.

---

# BD. Testing Strategy — Architecture Level

Future implementation must test:

```text
event schema versioning
deduplication
tenant isolation
anonymous/authenticated transition
server vs client event authority
purchase dedupe
multi-currency reporting
late events
search attribution
campaign attribution
seller scope
dashboard authorization
aggregate rebuild
pipeline failure degradation
privacy-sensitive field exclusion
```

Do not write tests now.

---

# BE. Decision Summary

Classify:

```text
RECOMMENDED_FOR_ADR
NEEDS_LATER_P00_DETAIL
DEFERRED
```

At minimum recommend:

1. First-party Analytics is a dedicated capability.
2. Analytics is not business truth, audit, or technical logging.
3. Client-observed and server-confirmed events are distinct.
4. Purchases/conversions derive from authoritative server business facts.
5. Analytics Visitor/Session are separate from Identity/Auth Session.
6. Analytics event contracts are versioned.
7. Tenant/Marketplace scope is explicit in every event.
8. Locale/Market/Currency remain separate.
9. Analytics ingestion does not unnecessarily block customer request paths.
10. Event reliability class may vary by business importance.
11. Critical conversion events are idempotent/deduplicated.
12. PII is minimized; secrets/payment credentials never enter analytics.
13. External analytics vendors remain optional adapters, not truth.
14. Search/Recommendation/AI may consume controlled analytics signals, not raw unrestricted data.
15. Multi-currency reporting preserves original money and explicit FX provenance.
16. Aggregates are rebuildable/recomputable.
17. Analytics pipeline has reconciliation against Order/Payment truth.
18. Professional Admin/Seller analytics UX is KPI/workflow-oriented, not raw-event CRUD.
19. Dashboard authorization is scope-aware and SpiceDB-governed.
20. Analytics pipeline health/freshness is observable.

Do not create final ADR yet.

---

## 4. Consistency Updates

If required, minimally align:

```text
docs/architecture/03-data-ownership-and-module-contracts.md
docs/architecture/08-pricing-market-currency.md
docs/architecture/10-cart-checkout-order.md
docs/architecture/11-payment.md
docs/architecture/12-content-page-composition.md
docs/architecture/14-search-indexing.md
```

No unrelated redesign.

---

## 5. Update SoT

Update:

```text
docs/PROJECT-STATE.md
docs/ROADMAP.md
docs/ai/TOOBA-RECOVERY-CONTEXT.md
```

Expected:

```text
Last Architect Accepted Task: TB-P00-T016
Current Issued Task: TB-P00-T017
Task State: AWAITING_ARCHITECT_ACCEPT
Current Phase: P00 — Architecture / Discovery
First-Party Analytics: IN_PROGRESS
```

No next task authorized.

---

## 6. Save Complete Envelope Verbatim

Save exact task to:

```text
docs/ai/tasks/TB-P00-T017.task.md
```

Verify exact markers:

```text
BEGIN_TOOBA_CURSOR_TASK_V1
END_TOOBA_CURSOR_TASK_V1
```

Do not save a shortened pointer.

---

## 7. Explicit Out of Scope

Do NOT:

- implement analytics SDK/tracker;
- create event database/schema;
- choose analytics vendor;
- add Google Analytics/Tag Manager;
- implement consent/cookie UI;
- implement dashboards;
- create warehouse/lake;
- implement recommendation;
- implement personalization;
- implement AI analytics ingestion;
- integrate Shopeiva;
- create final ADR.

---

## 8. Validation

Run:

```bash
git diff --check
git status --short --branch
```

Manual checks:

- T016 ACCEPTED;
- T017 awaiting Architect acceptance;
- Analytics != business truth/audit/logging;
- client vs server events separated;
- purchase truth from Order/Payment;
- Visitor/Analytics Session != Identity/Auth Session;
- event versioning represented;
- tenant isolation represented;
- Locale != Market != Currency;
- ingestion non-blocking for normal customer requests;
- conversion dedupe represented;
- privacy/PII minimization represented;
- external analytics optional;
- multi-currency reporting safe;
- Search/Recommendation/AI consume controlled signals;
- dashboard authorization represented;
- professional UI/UX represented;
- no application code;
- no template feature promoted.

---

## 9. Git Commit & Push

Commit:

```text
docs design Tooba first-party analytics [TB-P00-T017]
```

Then:

```bash
git push origin main
git fetch origin
git rev-parse HEAD
git rev-parse origin/main
git status --short --branch
```

Require:

```text
HEAD == origin/main
working tree clean
```

No force push.

---

## 10. Result Contract

Return exactly:

```text
BEGIN_TOOBA_CURSOR_RESULT_V1

Protocol-Version: 1
Task-ID: TB-P00-T017
Phase: P00 — Architecture / Discovery
Status: PASS | BLOCKED | RECOVERY_CONFLICT

Summary:
...

Repository-Recovery:
- Repo-Root:
- Branch:
- Starting-HEAD:
- Starting-Origin-Main:
- Starting-Status:

Changes:
- ...

Analytics-Boundary:
- ...

Event-Model:
- ...

Visitor-Session:
- ...

Client-Server-Authority:
- ...

Attribution-Search-Content:
- ...

Tenant-Privacy:
- ...

Ingestion-Reliability:
- ...

Reporting-Dashboards:
- ...

Recommendation-AI:
- ...

Decision-Summary:
- RECOMMENDED_FOR_ADR:
- NEEDS_LATER_P00_DETAIL:
- DEFERRED:

Validation:
- git diff --check:
- other checks:
- manual consistency review:

Git:
- Commit:
- Push:
- Final-HEAD:
- Final-Origin-Main:
- Final-Status:
- Head-Matches-Origin: YES | NO

Source-of-Truth:
- Last-Architect-Accepted-Task:
- Current-Issued-Task:
- Current-Phase:
- Task-State:
- Recovery-Ready: YES | NO

Architectural-Concerns:
- ...

Blockers:
- ...

END_TOOBA_CURSOR_RESULT_V1
```

---

## 11. Pipeline Continuity — MANDATORY

After RESULT:

```text
WAITING
```

Remain inside the Tooba Architect-controlled pipeline.

Do not leave PIPELINE mode.

Do not invent, infer, prepare, or execute unauthorized work.

Do not advance to `TB-P00-T018` or any other task without a new valid Architect envelope containing:

```text
BEGIN_TOOBA_CURSOR_TASK_V1
...
END_TOOBA_CURSOR_TASK_V1
```

Cursor PASS is not Architect ACCEPT.

After sending RESULT, stop execution and remain WAITING for Architect review.

---

## 12. UI / UX Protection — MANDATORY PROJECT RULE

Tooba is a commercial product and weak UI/UX is considered product failure.

All architectural decisions must preserve future ability to build:

```text
professional storefront
professional Admin panel
professional Seller panel
polished Customer dashboard
intentional mobile UX
RTL/LTR support
accessibility
responsive design
high-quality loading/empty/error states
strong visual hierarchy
commercial-grade interaction design
```

For Analytics specifically, future dashboards must be designed around decisions and workflows, not backend event tables.

They must preserve:

```text
KPI hierarchy
comparison periods
filters
drill-down
data freshness
accessible charts + tabular/text alternatives
mobile usability
RTL/LTR
loading states
empty states
error states
export where appropriate
seller/admin scope clarity
```

Hard rule:

```text
Backend/module boundary != UI boundary
```

Future UI implementation tasks must require visual evidence and Architect visual acceptance.

Build/test PASS alone is not sufficient acceptance for UI work.

END_TOOBA_CURSOR_TASK_V1
