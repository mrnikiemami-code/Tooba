PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

TASK-ID: TB-P07-T008
PARENT-TASK: TB-P07-T007-R2
PROJECT: Tooba
PHASE: P07
CHANNEL: tooba-main
STATUS: ISSUED
TASK-TYPE: CATEGORY FACETS — PLP FILTER CONFIGURATION + ADMIN UX
WORKER-POLICY: ONE WORKER = ONE ACTIVE TASK

Architect Acceptance

TB-P07-T007-R2 is ACCEPTED.

Canonical baseline:

T007 architectural gap is closed.

AttributeDefinition owns intrinsic identity/capability.

CategoryAttributeBinding owns category-specific:

IsRequired

IsFilterable

IsVariantAxis

IsComparable

DisplayOrder

nearest category binding wins in effective schema.

sibling categories remain isolated.

Category Tree remains USER-APPROVED and MUST NOT be redesigned.

final synchronized repository state:
HEAD == origin/main
1445e82cb07efd39df48189075932341520a623c

Locked UX Principle

The Admin UI must be understandable for an ordinary non-technical user without training.

For Facets:

user must understand “which filters appear on the product listing page”

avoid the word “facet” in normal Persian UI unless needed in docs

use clear labels such as:
فیلترهای صفحه محصولات

advanced behavior must be progressively disclosed

do not expose raw IDs, backend keys, enum names, or query syntax

Hard Locks

DO NOT redesign or restyle AppCategoryTree.

DO NOT change current Category Workspace composition.

Preserve VIEW / EDIT permission-aware form mode.

If any grid is needed, use ONLY:
src/frontend/design-system/app-data-grid/AppDataGrid.tsx

Raw AgGridReact is forbidden.

No page-specific duplicate grid CSS.

No cross-module SQL JOIN.

Pricing/Inventory remain separate from Catalog.

Facet configuration is Category-specific.

T007 effective schema is the source of eligible attributes.

Goal

Implement the real فیلترها tab in Category Workspace.

This tab configures how eligible category attributes appear as filters on the Storefront Category PLP.

Target future runtime flow:

Mega Menu
→ Category canonical route
→ CategoryId
→ effective category schema
→ facet configuration
→ PLP filter UI
→ filtered Product Master listing

T008 owns the CONFIGURATION and contract foundation.

Do NOT build the full Storefront PLP yet.

1 — Facet Eligibility

Only effective category attributes with:

IsFilterable == true

are eligible for facet configuration.

Eligibility comes from T007 effective schema.

Do NOT use definition-global IsFilterable behavior.

Inherited attributes that are effectively filterable are eligible too.

2 — Category Facet Configuration

Create/use module-owned model conceptually:

CategoryFacetConfiguration
- CategoryId
- AttributeDefinitionId
- DisplayType
- SortOrder
- IsVisible
- IsSearchable
- IsCollapsedByDefault
- MinSelectedCount? only if justified
- MaxVisibleOptions? only if justified
- ShowCounts

Use project conventions and avoid speculative fields that are not useful.

Configuration must be category-specific.

3 — Display Types

Support practical filter presentation types based on attribute value kind/capability.

At minimum:

CheckboxList
SearchableSelect
Range
ColorSwatch
BooleanToggle

If current typed model requires a safer reduced set, use that, but preserve extensibility.

Validation examples:

Number/Decimal → Range allowed

Boolean → BooleanToggle

Enumeration → CheckboxList / SearchableSelect

Color-like enum → ColorSwatch if configured/capable

Text should not become Range

invalid combinations rejected backend-side

Do not store arbitrary component names from frontend.

4 — Default Display-Type Suggestion

To reduce cognitive load, system should suggest a sensible display type.

Examples:

Boolean → کلید روشن/خاموش
Number → بازه
Enumeration → چندانتخابی
Color enum → رنگ

User may change it when allowed.

Do not require ordinary users to understand technical type mapping.

5 — Admin Facets Tab — VIEW Mode

Tab label:
فیلترها

Page heading can use:
فیلترهای صفحه محصولات

VIEW mode should show configured filters in their final order.

For each item show:

localized attribute name

display type using human Persian label

source/inherited context if helpful

visible/hidden

searchable

collapsed/default-open state

option count behavior

order

No raw IDs.

No disabled form fields.

6 — Admin Facets Tab — EDIT Mode

Authorized user enters explicit ویرایش.

User can:

add eligible filter

configure display type

show/hide

reorder

toggle searchable when relevant

choose collapsed/default-open

choose show counts

remove facet configuration from current category

Save/Cancel returns to VIEW.

Do not mutate T007 attribute assignment flags from this screen except through a clearly separate intentional action if absolutely needed.

7 — Add Filter Flow

Action:
افزودن فیلتر

Show only effective attributes where:

IsFilterable == true

not already configured in current category effective facet set unless override semantics apply

Selector:

searchable

localized attribute name

value type shown in human language if useful

inherited/local source if useful

no IDs

After select:

suggest display type

show only relevant configuration controls

save

8 — Inheritance

Facet configuration should support inheritance from parent Category.

Example:

Parent کالای دیجیتال:

برند → CheckboxList

Child موبایل inherits it.

Child may:

inherit unchanged

override presentation/order/visibility for current category

add local facet

hide inherited facet for current category if architecture allows explicit override

No physical copying to every descendant.

9 — Override Semantics

Use nearest configuration wins for presentation behavior.

Conceptually:

nearest CategoryFacetConfiguration
wins

while eligibility still depends on T007 effective IsFilterable.

If child changes presentation:

parent remains unchanged

sibling remains unchanged

same AttributeDefinition remains the same canonical attribute

Avoid duplicate effective filters.

10 — Effective Facet Resolver

Create a module-owned resolver/query that returns final effective facets for a Category.

Conceptual DTO:

EffectiveCategoryFacet
- AttributeDefinitionId
- LocalizedName
- ValueKind
- DisplayType
- SortOrder
- IsVisible
- IsSearchable
- IsCollapsedByDefault
- ShowCounts
- SourceCategoryId
- IsInherited

Do not leak persistence entities.

11 — Ordering

Filter order must be deterministic.

Admin can reorder configured local/overridden facets.

Inherited order must remain stable.

If local and inherited facets merge:

define deterministic effective ordering

document it

avoid random order changes after save

Use accessible reorder controls or drag pattern consistent with Tooba.

12 — Remove vs Hide

Distinguish:

حذف تنظیم فیلتر این دسته

from

مخفی کردن فیلتر در این دسته

For inherited facet:

removing local override should fall back to parent configuration

hiding should require explicit current-category override if supported

Do not delete AttributeDefinition.

Do not disable IsFilterable globally from this UI.

13 — Searchable Options

IsSearchable applies only where useful, such as:

large enum/checklist

searchable select

Disable/hide irrelevant configuration for range/boolean.

Use progressive disclosure.

14 — Color Swatch

ColorSwatch is allowed only where options have enough color metadata/capability.

Do NOT infer arbitrary hex values from localized names.

If color metadata does not yet exist:

implement capability contract cleanly

do not fake swatches with random colors

fallback to checkbox/select and document gap

15 — Range Facet

For numeric attributes:

range filter metadata should stay type-safe

do not require manual min/max if later product data can derive bounds

config may allow step/unit/display behavior only if current architecture supports it

T008 does NOT need aggregate product-value min/max computation unless already available.

Document that PLP can derive/receive runtime bounds later.

16 — PLP Contract

Provide a clean Storefront-facing contract for future PLP:

CategoryId + locale
→ EffectiveFacetDefinitions

This contract describes filter UI schema, not the current selected user values.

Later PLP query will send filter values through a typed Catalog Listing Query.

Do NOT expose Admin persistence shape directly to Storefront.

17 — Typed Filter Value Contract

Document the future request shape by value kind.

Conceptually:

Enum:
attributeId + selectedOptionIds[]

Range:
attributeId + min/max

Boolean:
attributeId + true/false

Color:
attributeId + optionIds[]

No raw SQL predicates.
No AG Grid filter models.
No arbitrary JSON expression language.

Do not fully implement PLP filtering if outside current task.

18 — SEO Safety

Facet configuration itself must NOT automatically create indexable URLs.

Document future rule:

category canonical page remains canonical

filtered PLP query states are non-canonical by default unless explicit SEO route strategy later says otherwise

Do not create programmatic SEO pages in T008.

19 — Authorization

Use SpiceDB-centered authorization.

Conceptually:

view facet configuration

edit facet configuration

Backend authoritative.

View-only users see VIEW mode only.

20 — API Contracts

Provide/use clean APIs for:

get effective facets for category

get local facet configs

create/update facet config

reorder

remove override/config

hide/show where modeled

Use module-owned contracts.

No cross-module joins.

21 — Persistence

Add migration if needed.

Constraints/indexes:

unique CategoryId + AttributeDefinitionId

deterministic order

valid enum/display type persistence

safe delete/override semantics

Preserve existing Catalog ownership.

22 — Frontend UX

Keep visual language consistent with General/Translations/Attributes tabs.

Prefer clear cards/list rows over a complex grid unless grid materially improves usability.

If grid is used:
ONLY Tooba Canonical AppDataGrid.

Useful badges/labels:

فعال

مخفی

ارث‌برده‌شده

قابل جستجو

بازه

چندانتخابی

رنگ

روشن/خاموش

Use localized helper text sparingly.

23 — Mobile

Mobile must remain usable:

one-column filter configuration

easy reordering alternative if drag is poor on touch

dialogs/sheets fit screen

Save/Cancel obvious

no horizontal overflow

24 — Tests

Backend at minimum:

only IsFilterable effective attributes eligible

non-filterable attribute rejected

create local facet config

inherit parent facet

child override

sibling unaffected

remove override falls back to parent

hide override behavior if implemented

duplicate config rejected

invalid DisplayType/value kind rejected

deterministic ordering

effective resolver returns one facet per attribute

Storefront contract returns localized facet schema

authorization where testable

Frontend:

VIEW mode

EDIT mode

eligible attribute selector

non-filterable absent

display-type suggestion

invalid controls hidden/disabled

inherited/local distinction

child override action

reorder

remove override

hide/show if implemented

localized labels

view-only cannot edit

mobile

accessibility basics

Tree remains untouched

25 — Documentation

Create/update:

docs/catalog/CATEGORY-FACETS.md

Document:

eligibility from T007

configuration model

inheritance

override precedence

display types

Storefront PLP contract

typed filter values

SEO/canonical rule

T009/T010 handoff as appropriate

Update recovery/project state docs.

26 — Validation

Backend:

restore

build

full tests

Frontend:

typecheck

lint

tests

production build

Require:

0 errors

0 failed

0 skipped

no new warnings

Always:

git diff --check

27 — Runtime

Keep alive:

Backend :5088

Frontend :3000

Shopeiva :3001

Preview:
http://localhost:3000/fa/admin/catalog/categories

28 — Git

Commit + push.

Require:

HEAD == origin/main

tracked tree clean

unrelated user changes preserved

Acceptance

PASS only if ALL:

Category Tree unchanged

Filters tab is real

VIEW / EDIT preserved

eligibility comes from category-specific effective IsFilterable

CategoryFacetConfiguration is category-specific

inheritance works without copying

child override does not affect parent/sibling

effective resolver deterministic

invalid display type/value type combinations rejected

ordinary user labels are understandable

no raw IDs

no raw AgGridReact

no duplicate grid implementation

Storefront facet schema contract exists

typed future filter-value contract documented

no automatic SEO-indexable filtered URLs introduced

full validation green

runtime alive

Git synchronized

USER_VISUAL_ACCEPTED remains NO until user manually reviews runtime.

Result Contract

Return ONLY:

PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_WORKER_RESULT

Task-ID:
TB-P07-T008
Parent-Task:
TB-P07-T007-R2
Channel:
tooba-main
Status:
PASS | FAIL | BLOCKED | RECOVERY_CONFLICT

Summary:
...

Tree-Lock:

AppCategoryTree touched:

visual changed:

behavior changed:

Facet-Model:

entity/path:

fields:

uniqueness:

migration:

Eligibility:

source:

category-specific IsFilterable:

non-filterable rejection:

Display-Types:

supported:

validation:

default suggestion:

color capability:

range behavior:

Inheritance:

resolver:

parent inheritance:

child override:

precedence:

sibling isolation:

remove override fallback:

duplicate prevention:

deterministic order:

Admin-UX:

VIEW:

EDIT:

add filter:

eligible selector:

inherited/local distinction:

override action:

reorder:

hide/show:

remove:

ordinary-user labels:

mobile:

accessibility:

Storefront-Contract:

endpoint/contract:

localized:

typed schema:

selected values separated:

SEO rule:

Authorization:

SpiceDB:

view-only:

edit:

backend authoritative:

API:

endpoints/contracts:

backend touched:

frontend integration:

gaps:

Canonical-AppDataGrid:

used:

raw AgGridReact:

new wrapper:

Validation:

backend restore:

backend build:

backend full tests:

frontend typecheck:

frontend lint:

frontend tests:

frontend build:

passed:

failed:

skipped:

warnings:

new warnings:

git diff --check:

Documentation:

CATEGORY-FACETS:

recovery/state:

Runtime:

Backend:

Frontend:

Shopeiva:

Category Admin:

kept alive:

Git:

branch:

commit:

push:

final HEAD:

origin/main:

synchronized:

tracked tree:

unrelated user changes:

Architectural-Concerns:
...

Visual-Concerns:
...

Blockers:
...

END_TOOBA_WORKER_RESULT

After Result:

Worker -> IDLE / Waiting

keep runtimes alive

do not invent T009

do not add extra prose

END_TASK