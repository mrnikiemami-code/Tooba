# Tooba — TB-P00-T006 — SpiceDB Authorization Architecture

BEGIN_TOOBA_CURSOR_TASK_V1

Protocol-Version: 1
Task-ID: TB-P00-T006
Phase: P00 — Architecture / Discovery
Type: Architecture Design / SpiceDB Authorization
Repository: https://github.com/mrnikiemami-code/Tooba
Primary-Branch: main
Implementation-Agent: Cursor
Architect: ChatGPT
Execution-Mode: PIPELINE
Depends-On: TB-P00-T005
Architect-Decision-On-Dependency: ACCEPTED

## Objective

Design Tooba's SpiceDB authorization architecture deeply enough to support Authentication != Authorization, ReBAC rather than a fixed role matrix, subject/resource/relation/permission vocabulary, tenant/store isolation, seller-scoped authorization, admin least privilege, customer self-service via trusted relationships, future B2B membership/delegation, use-case-boundary checks, internal `IAuthorizationService` hiding SpiceDB, bulk/list authorization, caveats, machine subjects, audit, fail-closed behavior, and conservative caching.

This task is architecture documentation only. Do not implement SpiceDB deploy, schema, SDK, middleware, authorization code, or Party/Organization/seller roles.

---

## 1. Repository Recovery

Before edits:

```bash
git rev-parse --show-toplevel
git fetch origin
git branch --show-current
git rev-parse HEAD
git rev-parse origin/main
git status --short --branch
```

Require:

```text
branch = main
HEAD == origin/main
working tree clean
```

Unsafe state =>

```text
RECOVERY_CONFLICT
```

Never force push, rewrite history, destructively reset, silently stash, or overwrite unknown work.

Expected predecessor from T005 RESULT:

```text
1f95eff901b25f5c2b63d2a1de9ef45ac9399568
```

If synchronized main legitimately advanced, continue from current safe main.

---

## 2. Source-of-Truth Transition

Update:

```text
TB-P00-T005 = ACCEPTED
TB-P00-T006 = ISSUED / AWAITING_ARCHITECT_ACCEPT
```

Roadmap:

```text
P00 identity/authentication = COMPLETE
P00 SpiceDB authorization = IN_PROGRESS
```

Do not mark T006 accepted.

---

## 3. Create Architecture Document

Create:

```text
docs/architecture/05-spicedb-authorization.md
```

It must contain all sections below.

---

# A. Core Separation

Define and separate:

```text
Authentication answers: Who is this principal?
Authorization answers: May this subject perform this action on this resource?
```

Hard rule:

```text
Authentication != Authorization
```

SpiceDB is the authorization decision system.

Identity must not duplicate it with a fixed role matrix (`IsAdmin`, `Role=Seller`, …).

Party/Organization owns business relationship lifecycles. SpiceDB stores/projects authorization-relevant relationships needed for checks. SpiceDB is not the system of record for catalog copy, prices, or order lines.

---

# B. Authorization Vocabulary

Define:

```text
Subject
Resource
Relation
Permission
Caveat / Contextual Condition
Tuple / Relationship
Authorization Check
Authorization Write
Authorization Projection
```

Business names map onto these concepts. SpiceDB does not become the owner of business data. Caveats are extra authorization-relevant constraints, not pricing logic.

---

# C. Subject Model

Candidate subject types:

```text
Identity
Party
Organization
Service Principal
System Actor
API Client
```

The authenticated principal is an Identity. Business acting-as is a Party (for B2B, a Party in Organization context). Checks carry who logged in and which Party/Organization they act as — via relationships, not swapped accounts.

Do not conflate Identity with Organization. Preserve future:

```text
human Identity acts on behalf of Organization
```

via membership/delegation, not duplicated logins. Machine subjects are distinct from human Identities.

---

# D. Resource Model

Example resources:

```text
Tenant / Store
Organization
Seller
Catalog Product management scope
Offer
Order
Customer profile
Content item
Landing page
Promotion
Support ticket
Media asset
Admin operational capability
Report / Analytics view
```

Not every row is a SpiceDB resource. Define granularity criteria. Catalog Product management scope is not Catalog Product identity. Sellers do not gain canonical product-write from seller relations. Do not lock SpiceDB schema.

---

# E. Relationship-Based Access Control

Candidate relation semantics, not schema:

```text
member
owner
manager
editor
viewer
seller_operator
finance_operator
support_agent
customer_self
organization_buyer
organization_admin
```

Prefer scoped relationships over universal global roles.

```text
user A is manager of seller X
```

is not:

```text
user A is global seller_manager
```

Global relations are exceptional bootstrap/super-admin policy, not the default.

---

# F. Tenant / Store Scope

Authorization architecture must work across:

```text
Marketplace deployment
Single-Store shared deployment
```

Single-Store: tenant/store is already resolved by trusted host routing; every check includes that store/tenant resource or a parented resource; a permission on store A never authorizes store B; namespaces/prefixes/object ids must prevent tuple collision.

Marketplace: do not invent a fake Single-Store tenant; use marketplace, seller, and resource relationships in that dedicated deployment.

Candidate isolation approaches may be described. Exact schema:

```text
NEEDS_LATER_P00_DETAIL
```

---

# G. B2B Organization Authorization

Conceptual relationships, future, not implemented:

```text
Identity -> Party
Party -> Organization membership
Organization -> buyer/admin/finance roles
Organization -> contracts/accounts
Organization -> orders
delegated acting authority
approval hierarchy
```

Permissions that must remain possible:

```text
may_create_order
may_approve_order
may_view_org_orders
may_manage_org_users
may_view_credit
may_use_contract_price
```

`may_use_contract_price` is authorization to request a Party/contract-scoped quote. Pricing still owns the quote. SpiceDB does not compute prices. Do not implement B2B now.

---

# H. Seller / Marketplace Authorization

Seller-scoped candidates:

```text
seller owner
seller admin
catalog contributor
offer manager
inventory operator
order operator
finance viewer
support operator
```

Seller permissions do not imply ownership of canonical Catalog Product truth. Offer/inventory/order operations stay within those modules’ ownership.

---

# I. Admin Authorization

Do not design Admin as:

```text
IsAdmin = true
```

Capability/relationship directions:

```text
catalog moderation
seller approval
order support
payment operations
content publishing
promotion management
analytics access
security administration
tenant operations
```

Least privilege: grant the relation on the needed resource/scope. Global superuser/bootstrap exists only as explicit exceptional policy, not the default.

---

# J. Customer Self-Service

```text
view own profile
edit own addresses
view own orders
cancel eligible own order
view own support tickets
manage own sessions
```

“Own” is a relationship from trusted server-side facts after authentication, never from client-supplied ids alone. Eligibility to cancel remains an Order business rule after the permission check. Identity owns session lifecycle; SpiceDB may gate “manage own sessions” but does not store session tokens.

---

# K. Ownership vs Authorization Truth

Business domains remain source of truth for membership/ownership facts.

SpiceDB holds what the decision engine needs.

Synchronization direction:

```text
business fact changes
→ integration/outbox event
→ authorization relationship update
```

or a synchronous write in the same use-case when the next check must not see a lag. Do not pick one consistency model for every relation. Security-sensitive revocations lean synchronous or fail-closed until sync is confirmed.

---

# L. Authorization Consistency

Analyze strongly consistent check vs eventually updated relationship, revocation latency, and security-sensitive changes. Do not rely on stale local caches for security-critical authorization unless bounded by later policy.

Exact per-relation consistency:

```text
NEEDS_LATER_P00_DETAIL
```

---

# M. Check Placement

Checks belong at application/use-case boundaries and sensitive domain operations. UI hiding is not a security boundary. UI may call Check/Lookup for visibility; the server repeats the check on the command.

---

# N. Authorization Service Contract

Conceptual internal contract:

```text
IAuthorizationService
CheckAsync(subject, permission, resource, context)
BulkCheckAsync(...)
LookupResourcesAsync(...)
```

Names are conceptual. Business modules consume this contract, not the SpiceDB SDK. Enables in-process adapter now, remote later, fakes in tests. No code in this task.

---

# O. Bulk / List Authorization

Admin/Seller/B2B lists must not N+1 Check per row.

Patterns:

```text
bulk check
lookup resources
pre-filtered projections
authorization-aware query composition
```

SpiceDB looks up ids/permissions. The owning module still loads business data via its own store/projection. Do not move order lines into SpiceDB to make a grid fast. List authorization is first-class for professional dashboards.

---

# P. Contextual / Caveated Permissions

Possible future conditions:

```text
market
sales channel
order state
time window
MFA assurance level
tenant status
contract validity
```

Do not encode pricing formulas or checkout workflow in SpiceDB. Caveats only when the authorization question depends on them. Exact caveat strategy:

```text
NEEDS_LATER_P00_DETAIL
```

---

# Q. Service-to-Service / Machine Authorization

Future subjects:

```text
service principal
API client
integration identity
```

Do not reuse human user sessions as machine identity. Authenticate machine credentials in Identity or a dedicated machine-auth path; authorize with SpiceDB using machine subject types.

---

# R. Auditability

Safe audit context:

```text
subject
permission
resource
decision
tenant/store
correlation/trace
reason/context where appropriate
```

Do not log full relationship dumps or secrets.

Differentiate:

```text
authorization decision telemetry
relationship mutation audit
business audit
```

Identity security events remain Identity-owned. Authorization decision/mutation audit is authorization-owned.

---

# S. Failure Behavior

Fail closed for:

```text
SpiceDB unavailable
malformed resource
unknown subject
unknown permission mapping
tenant mismatch
relationship sync uncertainty in security-critical path
```

Do not silently allow on infrastructure failure. Controlled exceptions only if later explicit policy proves a path is safe. Default: deny.

---

# T. Caching

Conservative:

```text
positive-result cache
negative-result cache
relationship/version tokens
short TTL
revocation impact
```

No Redis required initially. No cross-tenant cache keys/leakage. Security-sensitive operations may bypass or tightly bound caches.

---

# U. Testing Strategy — Architecture Level

Future implementation must support:

```text
permission matrix tests
relationship graph tests
cross-tenant denial tests
revocation tests
seller isolation tests
B2B delegation tests
customer self-scope tests
admin least-privilege tests
bulk/list authorization tests
```

No tests written in this task.

---

# V. Relationship to Domain Boundaries

```text
Identity -> authenticated principal
Party -> business parties/memberships
Seller -> seller membership/business roles
Content -> content ownership/workflow
Order -> order resource/business ownership
Authorization -> decision infrastructure / policy graph integration
```

No module bypasses authorization by querying another module’s tables.

---

# W. Migration / Bootstrap Concerns

Future needs:

```text
initial super-admin
tenant owner provisioning
seller owner provisioning
organization owner provisioning
relationship backfill
schema version rollout
permission migration
```

Do not solve with hardcoded permanent superuser in application code. Bootstrap is an explicit, auditable provisioning path that then relies on the same check machinery.

---

# X. Decision Summary

Classify:

```text
RECOMMENDED_FOR_ADR
NEEDS_LATER_P00_DETAIL
DEFERRED
```

At minimum recommend for ADR:

1. SpiceDB as authorization decision foundation.
2. Authentication separate from authorization.
3. Business domains remain source of truth for business relationships.
4. SpiceDB represents authorization-relevant relationships/permissions.
5. No fixed role matrix duplicated in Identity.
6. Tenant/store isolation in authorization scope.
7. Seller-scoped relationship model.
8. B2B organization/delegation readiness.
9. Application/use-case boundary checks, not UI-only checks.
10. Internal authorization abstraction hides SpiceDB SDK.
11. Fail closed on authorization infrastructure failure.
12. Bulk/list authorization is first-class.
13. Authorization audit/telemetry without secrets.
14. Machine subjects distinct from human identities.

Do not create the final ADR in this task.

---

## 4. Consistency Updates

If required, minimally align:

```text
docs/architecture/01-capability-domain-map.md
docs/architecture/03-data-ownership-and-module-contracts.md
docs/architecture/04-identity-authentication.md
```

Do not redesign unrelated domains.

---

## 5. Update SoT

Update:

```text
docs/PROJECT-STATE.md
docs/ROADMAP.md
docs/ai/TOOBA-RECOVERY-CONTEXT.md
```

Expected:

```text
Last Architect Accepted Task: TB-P00-T005
Current Issued Task: TB-P00-T006
Task State: AWAITING_ARCHITECT_ACCEPT
Current Phase: P00 — Architecture / Discovery
Identity / Authentication: COMPLETE
SpiceDB Authorization: IN_PROGRESS
```

No next task authorized.

---

## 6. Save Complete Envelope Verbatim

Save this exact task to:

```text
docs/ai/tasks/TB-P00-T006.task.md
```

Verify exact markers:

```text
BEGIN_TOOBA_CURSOR_TASK_V1
END_TOOBA_CURSOR_TASK_V1
```

Do not save a shortened pointer.

---

## 7. Explicit Out of Scope

Do NOT:

- deploy SpiceDB or write SpiceDB schema DSL;
- choose or integrate a SpiceDB SDK;
- write authorization middleware or application code;
- create DB schemas/migrations;
- implement Party, Organization, B2B, seller roles, admin UI, or Identity;
- choose production SpiceDB hosting;
- install Redis as an authorization cache requirement;
- integrate Shopeiva;
- create final ADR;
- invent or execute `TB-P00-T007`.

---

## 8. Validation

Run:

```bash
git diff --check
git status --short --branch
```

Manual checks:

- T005 ACCEPTED; T006 awaiting Architect acceptance;
- no application code; no SpiceDB schema/SDK/deploy;
- Authentication != Authorization; Identity does not duplicate a SpiceDB role matrix;
- tenant/store isolation; seller-scoped ReBAC; B2B multi-organization actor model remains possible;
- checks at use-case boundaries, not UI-only; `IAuthorizationService` hides SpiceDB;
- bulk/list authorization is first-class; fail closed on authorization infrastructure failure;
- no template feature promoted to requirements.

---

## 9. Git Commit & Push

Commit:

```text
docs design Tooba SpiceDB authorization [TB-P00-T006]
```

Then:

```bash
git push origin main
git fetch origin
git rev-parse HEAD
git rev-parse origin/main
git status --short --branch
```

Require:

```text
HEAD == origin/main
working tree clean
```

No force push.

---

## 10. Result Contract

Return exactly:

```text
BEGIN_TOOBA_CURSOR_RESULT_V1

Protocol-Version: 1
Task-ID: TB-P00-T006
Phase: P00 — Architecture / Discovery
Status: PASS | BLOCKED | RECOVERY_CONFLICT

Summary:
...

Repository-Recovery:
- Repo-Root:
- Branch:
- Starting-HEAD:
- Starting-Origin-Main:
- Starting-Status:

Changes:
- ...

Authorization-Boundary:
- ...

Subject-Resource-Model:
- ...

Tenant-Seller-B2B-Scoping:
- ...

Check-Placement:
- ...

Consistency-Synchronization:
- ...

Bulk-Authorization:
- ...

Failure-Caching:
- ...

Decision-Summary:
- RECOMMENDED_FOR_ADR:
- NEEDS_LATER_P00_DETAIL:
- DEFERRED:

Validation:
- git diff --check:
- other checks:
- manual consistency review:

Git:
- Commit:
- Push:
- Final-HEAD:
- Final-Origin-Main:
- Final-Status:
- Head-Matches-Origin: YES | NO

Source-of-Truth:
- Last-Architect-Accepted-Task:
- Current-Issued-Task:
- Current-Phase:
- Task-State:
- Recovery-Ready: YES | NO

Architectural-Concerns:
- ...

Blockers:
- ...

END_TOOBA_CURSOR_RESULT_V1
```

---

## 11. Pipeline Continuity

After RESULT:

```text
WAITING
```

Do not invent or execute `TB-P00-T007`.

Architect reviews T006.

If ACCEPTED and no real blocker exists, Architect automatically issues the next authorized task.

END_TOOBA_CURSOR_TASK_V1
