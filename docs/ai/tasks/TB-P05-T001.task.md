# Tooba — TB-P05-T001 — Seller Panel Live Slice — Products & Orders

BEGIN_TOOBA_CURSOR_TASK_V1

Protocol-Version: 1
Task-ID: TB-P05-T001
Phase: P05 — Operational Surface Integration
Type: Sell-First Seller Vertical Slice
Repository: https://github.com/mrnikiemami-code/Tooba
Branch: main
Execution-Mode: PIPELINE

## Architect Decision

`TB-P04-GATE` is ACCEPTED.

```text
P04 = COMPLETE
P05 = IN_PROGRESS
```

P05 objective:

```text
connect remaining core operational/customer surfaces
using Shopeiva with minimum visual change
```

Locked rule remains:

```text
PRESERVE SHOPEIVA
→ MINIMUM CHANGE
→ CONNECT REAL TOOBA BACKEND
→ SELL QUICKLY
→ POLISH/HARDEN LATER
```

Do NOT invent a new Seller UI.

Use the purchased Shopeiva Vendor Panel as the primary visual source.

---

# Repository Recovery

Run:

```bash
git rev-parse --show-toplevel
git fetch origin
git branch --show-current
git rev-parse HEAD
git rev-parse origin/main
git status --short --branch
```

Expected predecessor:

```text
e37058b9a9b90047dedc6f9354dc99cc4bfa6306
```

Require:

```text
branch = main
HEAD == origin/main
safe/known working tree
```

No force push.
No destructive reset.
No silent stash.
No history rewrite.

---

# Objective

Build the first real Seller operational slice:

```text
Seller Panel Shell
→ Seller Products
→ Seller Product Detail/Edit seam
→ Seller Orders
→ Seller Order Detail
```

All business data must come from real Tooba backend/application contracts.

No demo JSON business truth.

This is NOT full Seller completeness.

This task exists to make the Seller side commercially demonstrable as fast as possible.

---

# Shopeiva Reuse — CRITICAL

Use the Shopeiva Vendor Panel patterns already discovered in T006.

Prefer actual structure/pattern reuse for:

```text
vendor shell
sidebar
topbar
dashboard chrome
products table/list
product actions
orders table/list
order details
filters/toolbars
cards
mobile navigation
empty/loading/error states
```

Do NOT redesign these surfaces.

Allowed deviation:

```text
replace Shopeiva simple operational tables with Tooba Professional Data Grid where operationally valuable
```

But embed the Grid inside Shopeiva panel language.

Do not turn the page into the earlier custom T005 visual language.

---

# Seller Identity / Authorization

Use existing Identity + Party + SpiceDB foundations.

A Seller user must only access Seller-owned operational data.

Do NOT implement frontend-only filtering.

Authorization must be enforced at use-case/API boundary.

At minimum prove:

```text
Seller A cannot read Seller B product/offer/order operational data
```

Use existing seller/party relationship model.

Do not invent a separate auth system.

---

# Seller Product Semantics

Preserve:

```text
Catalog Product != Seller Offer
```

Seller operational UI must not imply that a Seller owns the canonical Product definition if current model says otherwise.

For this first slice, expose the appropriate seller-controlled commercial seam, for example:

```text
Seller Offer
seller SKU
price
availability
publication/activation state
associated Catalog Product
```

If seller editing of canonical Catalog fields is not architecturally allowed:

```text
do not fake it
```

Show canonical product context as read-only and seller-owned Offer/commercial fields as editable where supported.

---

# Seller Products Route

Adapt Shopeiva:

```text
/vendor-panel/products
```

or equivalent Tooba route.

Use Tooba Professional Data Grid if appropriate.

Minimum live columns:

```text
Product
Seller SKU
Offer status
Current resolved/authored price
Availability
Last updated
Actions
```

No:

```text
Product.Price
Product.Stock
```

Use correct module contracts.

---

# Seller Product Detail / Edit Seam

Adapt Shopeiva:

```text
/vendor-panel/products/[id]/edit
```

Minimum seller-controlled fields should reflect existing domain capabilities.

Examples if supported:

```text
Seller SKU
Offer active/inactive
seller-authored pricing context
seller availability/inventory seam
```

Do not add unsupported domain fields just because template has them.

For advanced Pricing/Inventory editing that is not yet safely exposed:

```text
show bounded read-only/live section
or narrow supported command
```

Do not redesign the backend in this task.

---

# Seller Orders Route

Adapt Shopeiva:

```text
/vendor-panel/orders
```

Seller must only see Seller-owned Order slices.

Minimum live fields:

```text
display order reference
order date
buyer/customer-safe display
seller line count
amount relevant to seller order
payment state
order status
actions
```

Do not expose another seller's lines.

---

# Seller Order Detail

Adapt Shopeiva order-detail pattern.

Minimum live information:

```text
order reference
seller-owned lines only
product/title snapshot
quantity
seller commercial amount
payment state
shipping snapshot appropriate to seller
customer/recipient info only as allowed
timeline/status seam
```

No raw internal IDs as primary UX.

No Payment secrets.

---

# Multi-Seller Isolation — REQUIRED

Use at least one live multi-seller checkout/order from existing seed/evidence or create bounded seed data.

Prove:

```text
Seller A view contains only Seller A order
Seller B view contains only Seller B order
same CheckoutGroup does not leak cross-seller lines
```

Create:

```text
docs/evidence/TB-P05-T001/multi-seller-isolation.md
```

---

# Data Grid

Use Tooba Professional Data Grid on:

```text
Seller Products
Seller Orders
```

where feasible.

Required initial operational behavior:

```text
sorting
filtering
pagination
column visibility
responsive presentation
```

Do not implement every advanced Grid feature if already covered by foundation and not necessary for this slice.

Preserve Shopeiva toolbar/spacing/shell.

---

# Seller Dashboard Shell

A full analytics dashboard is NOT required.

But the Seller panel landing route should be usable and visually complete enough to navigate to:

```text
Products
Orders
```

It may show real minimal summary cards if contracts exist:

```text
active offers
open orders
paid orders
```

Do not invent fake revenue charts.

---

# Customer-Safe / Operator-Safe Errors

Use Persian messages appropriate for seller operators.

Do not expose internal exception details.

Preserve structured error codes.

---

# Mobile

Seller Products and Orders must work at:

```text
390x844
```

No giant desktop table forced horizontally.

Use responsive Data Grid strategy already defined.

---

# Live Evidence

Store:

```text
docs/evidence/TB-P05-T001/
```

Capture:

```text
01-seller-dashboard-desktop.png
02-seller-products-desktop.png
03-seller-products-mobile-390x844.png
04-seller-product-detail.png
05-seller-orders-desktop.png
06-seller-orders-mobile-390x844.png
07-seller-order-detail.png
08-seller-authorization-denied.png
09-multi-seller-isolation.md
```

All evidence must be:

```text
live Tooba
real backend data
Shopeiva visual language
no fixture/demo business truth
no debug overlay
```

---

# Architecture Evidence

Create:

```text
docs/evidence/TB-P05-T001/seller-surface-architecture-map.md
```

Show:

```text
Seller UI
→ Host/Public Seller API
→ authorization boundary
→ Party/Seller identity
→ Offer
→ Pricing
→ Inventory
→ Order seller slice
```

Prove:

```text
no cross-module SQL JOIN
no frontend seller filtering as authority
no Product.Price
no Product.Stock
```

---

# Focused Tests

Add/verify:

```text
Seller A products only
Seller B products only
Seller A orders only
Seller B orders only
cross-seller access denied
Offer identity preserved
seller Order slice does not leak other seller lines
```

Also add frontend mapping/UI tests for Products/Orders data adapters.

---

# Full Validation

Backend:

```bash
dotnet restore src/backend/Tooba.slnx
dotnet build src/backend/Tooba.slnx
dotnet test src/backend/Tooba.slnx
```

NO filters.

Require:

```text
warnings = 0
errors = 0
failed = 0
skipped = 0
```

Frontend:

```bash
cd src/frontend
npm ci
npm run typecheck
npm run lint
npm run test:grid
npm run test:workspace
npm run test:product-workspace
```

Run:

```text
storefront
cart
checkout/order
payment
new seller tests
```

Then:

```bash
npm run build
git diff --check
git status --short --branch
```

---

# Non-Blocking Deferred Concern

Carry from P04:

```text
Payment request missing IdempotencyKey can currently produce 500/NRE.
```

Do not silently lose it.

Do NOT fix it in T001 unless the Seller slice directly touches that code.

Also carry:

```text
Cart replace-hold release→reserve competition window.
```

---

# Acceptance Conditions

PASS requires:

```text
Seller shell visibly Shopeiva-based
Seller Products live
Seller Product detail/edit seam live
Seller Orders live
Seller Order detail live
multi-seller isolation proven
SpiceDB/use-case authorization enforced
Seller A cannot access Seller B operational data
Data Grid integrated without visual redesign
desktop works
mobile 390x844 works
no Product.Price
no Product.Stock
no cross-module SQL JOIN
full validation green
```

---

# SoT

Update:

```text
docs/PROJECT-STATE.md
docs/ROADMAP.md
docs/ai/TOOBA-RECOVERY-CONTEXT.md
docs/prompts/START-HERE-IF-CHATGPT-IS-LOST.md
```

Record:

```text
TB-P04-GATE = ACCEPTED
P04 = COMPLETE
P05 = IN_PROGRESS
TB-P05-T001 = AWAITING_ARCHITECT_ACCEPT
```

Keep locked:

```text
Preserve Shopeiva
Minimal UI change
Connect backend
Sell fast
P06 all core surfaces live
```

---

# Save Envelope VERBATIM

Save:

```text
docs/ai/tasks/TB-P05-T001.task.md
```

---

# Commit / Push

After work:

```bash
git diff --check
git status --short --branch
git add ...
git commit -m "feat connect seller products orders surface [TB-P05-T001]"
git push origin main
git fetch origin
git rev-parse HEAD
git rev-parse origin/main
git status --short --branch
```

Require:

```text
HEAD == origin/main
```

---

# Result Contract

Return:

```text
BEGIN_TOOBA_CURSOR_RESULT_V1

Protocol-Version: 1
Task-ID: TB-P05-T001
Phase: P05 — Operational Surface Integration
Status: PASS | BLOCKED | RECOVERY_CONFLICT

Summary:
...

Repository-Recovery:
- ...

Seller-Identity:
- seller user:
- Party:
- authorization:
- cross-seller denied:

Shopeiva-Reuse:
- shell:
- sidebar/topbar:
- products:
- product detail:
- orders:
- order detail:
- deviations:

Seller-Products:
- live route:
- fields:
- Offer semantics:
- Pricing:
- Inventory:
- actions:

Seller-Orders:
- live route:
- fields:
- seller slice:
- payment state:
- shipping snapshot:

Multi-Seller-Isolation:
- checkout/order:
- Seller A:
- Seller B:
- leakage:

Data-Grid:
- Products:
- Orders:
- sorting:
- filtering:
- pagination:
- mobile:

Visual-Evidence:
- 01:
- 02:
- 03:
- 04:
- 05:
- 06:
- 07:
- 08:
- 09:
- architecture map:

Tests:
- seller products isolation:
- seller orders isolation:
- authorization:
- frontend seller tests:

Validation:
- backend restore:
- backend build:
- warnings:
- errors:
- backend passed:
- failed:
- skipped:
- npm ci:
- typecheck:
- lint:
- grid:
- workspace:
- product-workspace:
- storefront:
- cart:
- checkout/order:
- payment:
- seller:
- frontend build:
- git diff --check:

Architecture:
- Product.Price:
- Product.Stock:
- cross-module SQL join:
- frontend seller filtering authority:

Git:
- commit:
- push:
- final HEAD:
- origin/main:
- synchronized:
- final status:

Source-of-Truth:
- P04:
- TB-P04-GATE:
- P05:
- TB-P05-T001:
- Recovery-ready:

Architectural-Concerns:
- ...

Visual-Concerns:
- ...

Blockers:
- ...

END_TOOBA_CURSOR_RESULT_V1
```

After RESULT surface Seller screenshots directly in this SAME Architect-controlled session.

# CRITICAL — RESULT DOES NOT END THIS AGENT SESSION

After RESULT:

```text
PIPELINE_STATE: WAITING_FOR_ARCHITECT_IN_SAME_SESSION
```

Then:

```text
DO NOT CLOSE OR LEAVE THIS SESSION.
WAIT HERE FOR THE NEXT AUTHORIZED ENVELOPE.
```

Do not finish.
Do not exit.
Do not leave PIPELINE.
Do not self-advance.
Do not invent TB-P05-T002.

RESULT = CONTROL RETURNED TO ARCHITECT.
RESULT != WORKFLOW FINISHED.

Cursor PASS is not Architect ACCEPT.

Leaving or ending the pipeline after RESULT is a protocol violation.

END_TOOBA_CURSOR_TASK_V1
