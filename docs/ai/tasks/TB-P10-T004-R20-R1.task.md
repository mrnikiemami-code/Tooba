PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_REPAIR_TASK

Task-ID:
TB-P10-T004-R20-R1

Parent-Task:
TB-P10-T004-R20

Channel:
tooba-main

WorkerId:
tooba-worker-01

AgentType:
cursor

Status:
REPAIR

USER_VISUAL_ACCEPTED=NO

Title:
Visual Gate Repair — Long-Hold Countdown Formatting + Admin Operational History Localization

Architect Review

TB-P10-T004-R20 is NOT YET ACCEPTED.

The main real-browser gate is strong and most R20 scope passed, including:

real Host :5088 and FE :3000

Cart pending-payment UX desktop/mobile

countdown sourced from server ExpiresAt

failed payment does not reset hold

expired retry behavior

Manual AwaitingAdmin no pay-again

Admin reservation audit

Store/Category/Offer policy UX

Seller read-only

no polling / no new 401 / no N+1

minimal repairs only

HEAD==origin/main

However, two visual/UX defects remain inside the R20 acceptance scope:

Long manual-review hold renders as 1439:18 using minute-style MM formatting.
This is not acceptable customer-facing time presentation for ~24h holds.

Admin operational history can still expose raw technical token:
GATEWAY_REJECTED
R20 acceptance explicitly requires no raw enum/code in normal UX.

These must be repaired before R20 can be accepted.

The following are NOT blockers for this repair:

screenshot RTL mirroring artifact

pre-existing mixed-language EN chrome outside the new reservation surfaces

Do NOT change reservation lifecycle semantics.
Do NOT change TTL policy.
Do NOT add polling.
Do NOT redesign pages.
Do NOT create TB-P10-T005.

Recovery State

Phase: P10
Last Architect-accepted: TB-P10-T004-R19
Last Implementation: TB-P10-T004-R20
Current Repair: TB-P10-T004-R20-R1
Worker: tooba-worker-01
Expected branch: main
Expected previous HEAD: e22ffdbdd59fbd216e7fc9993829bf09e3af2ef2
USER_VISUAL_ACCEPTED=NO

Applicable Locks

Preserve LOCK-SF-001…102 and all R15–R20 semantics.

Especially:

server ExpiresAt authoritative

frontend countdown presentation only

no timer reset after failed payment

no per-second API polling

no raw enum/code in normal Admin/customer UX

centralized localization

immutable audit history

1. Recovery Preflight

Record:

Phase

Last Architect-accepted

Last Implementation

Current Repair

Worker

branch

HEAD

origin/main

HEAD==origin/main

tracked dirty state

task-owned untracked state

Unexpected tracked divergence => BLOCK.

Evidence:
docs/evidence/TB-P10-T004/r20-r1-recovery-start.md

2. Long-Hold Countdown Formatting

Current defect:
manual review around 24h renders like:
1439:18

Required behavior:

short durations may remain MM:SS

durations >= 60 minutes must use a human-readable hour-aware format

Preferred display:

under 1 hour: 09:30

1h to <24h: 02:14:37




=24h: either 1 روز و 02:14:37 OR a concise localized equivalent

For Persian customer UX, prioritize readability:

۲۳ ساعت و ۵۹ دقیقه
or

23:59:18
with surrounding Persian explanatory copy

Choose ONE consistent existing design-friendly format.

Critical:

formatting only

server ExpiresAt remains authoritative

no lifecycle mutation

no timer extension

no policy rewrite

no extra API polling

The same formatter should be reused where appropriate across:

Cart pending-payment countdown

Admin active reservation countdown

manual-review countdown

Avoid duplicated formatter logic.

3. Countdown Accessibility

Ensure accessible text is human-readable for long durations.

Do not expose only:
1439:18

Provide equivalent meaningful accessible label such as:
۲۳ ساعت و ۵۹ دقیقه تا پایان مهلت رزرو

No per-second screen-reader spam if avoidable.

4. Admin Operational History Raw Code Repair

Current defect:
Admin operational history may display:
GATEWAY_REJECTED

Requirement:
Normal Admin UX must never expose raw enum/status/code token.

Implement centralized localization/presentation mapping for operational-history event/status labels.

At minimum map the encountered value:

GATEWAY_REJECTED
to a human business label equivalent:

FA: ردشده توسط درگاه پرداخت

EN: Rejected by payment gateway

But do not hardcode only one if the same renderer can expose other raw codes.

Discovery:
inspect the operational-history renderer and list all values that can currently fall through raw.

Preferred:

centralized mapper

stable fallback that is humanized/localized, not raw enum output

backend stable code may remain in data/audit; frontend presentation maps it

Do NOT parse Status.ToString() for business logic.

Evidence:
docs/evidence/TB-P10-T004/r20-r1-operational-history-discovery.md

5. Preserve Immutable Audit

Only presentation changes are allowed.

Do NOT:

rewrite stored events

mutate historical reservation cycles

alter payment status

alter gateway records

Admin UI should map stored stable values to human labels.

6. Focused Tests

Required:

Countdown:

09m30s => 09:30

59m59s => short format

60m00s => hour-aware format

23h59m => human-readable hour format

24h+ => no 1440 style output

expiry at zero still triggers at most one refresh

failed payment does not reset ExpiresAt

no API call per second

Operational history:
9. GATEWAY_REJECTED renders human FA
10. GATEWAY_REJECTED renders human EN
11. no raw enum/code in normal renderer
12. unknown value uses safe human fallback, not raw technical token if current localization architecture supports it
13. audit data itself unchanged

Regression:
14. R16 pending-payment tests
15. R17 Admin audit tests
16. R18 policy UI tests if shared formatter touched
17. recovery guard
18. git diff --check

Do NOT run unrelated full suites.

7. Real Browser Validation — REQUIRED

Use real browser against existing runtime.

A. FA Cart manual AwaitingAdmin

long countdown readable

no 1439:xx

B. Admin Order Detail active/manual review countdown

hour-aware readable format

C. Admin operational history

no GATEWAY_REJECTED

human Persian label visible

D. EN smoke

long duration readable

gateway-rejected history human English label

E. Network

no polling introduced

no new console errors

Capture evidence paths.

8. Anti-Pattern Gate

Scan for:

duplicated countdown formatting

hardcoded 24h special case

frontend lifecycle decisions based on formatted text

per-second API fetch

raw enum fallback

UI parsing Status.ToString()

broad unrelated i18n rewrite

CSS/layout hacks

Any occurrence => REPAIR.

Evidence:
docs/evidence/TB-P10-T004/r20-r1-antipattern-scan.md

9. Lock Registry

No new architectural lock expected.

State:
No new lock required; LOCK-SF-001…102 sufficient.

10. Evidence

Add:

r20-r1-recovery-start.md

r20-r1-operational-history-discovery.md

r20-r1-countdown-format.md

r20-r1-admin-history-localization.md

r20-r1-focused-validation.md

r20-r1-runtime.md

r20-r1-antipattern-scan.md

r20-r1-recovery-sot.md

under:
docs/evidence/TB-P10-T004/

11. Recovery / Pipeline

At completion:

Phase P10

Last Architect-accepted before review: TB-P10-T004-R19

Last Implementation: TB-P10-T004-R20-R1

Current Issued/Repair: none

USER_VISUAL_ACCEPTED=NO

Worker: tooba-worker-01 — IDLE

branch main

ending HEAD recorded

HEAD==origin/main

task-owned worktree clean

no TB-P10-T005

Run recovery guard.

Acceptance

PASS only if ALL:

no customer/admin countdown renders 60+ minute durations as giant MM

manual-review ~24h display is human-readable

countdown remains server-authoritative

no polling introduced

GATEWAY_REJECTED is never shown raw in normal Admin UX

operational history uses centralized human presentation

stored audit/history remains unchanged

FA and EN smoke pass

browser/network clean

focused tests green

anti-pattern scan CLEAN

recovery exact

HEAD==origin/main

Worker IDLE

no TB-P10-T005

USER_VISUAL_ACCEPTED=NO.

Result Contract

Return ONLY canonical BRIDGE-WAKE-V1 Result with:

Summary
Applicable-Locks
Recovery-Start
Operational-History-Discovery
Countdown-Format
Admin-History-Localization
Focused-Validation
Runtime
AntiPattern-Scan
Lock-Registry
Recovery-SoT
Git
Architectural-Concerns
Visual-Concerns
Blockers

END_TOOBA_REPAIR_TASK