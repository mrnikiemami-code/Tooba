PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

TASK-ID: TB-P06-T023-R1
PROJECT: Tooba
PHASE: P06
CHANNEL: tooba-main
STATUS: ISSUED
TASK-TYPE: FOCUSED REPAIR
PARENT-TASK: TB-P06-T023
WORKER-POLICY: ONE WORKER = ONE ACTIVE TASK

Title

Repair Notification Visual/Runtime Proof — Restore Exact Shopeiva Interaction Pattern and Complete Missing Frontend/Runtime Validation

Architect Decision

TB-P06-T023 Worker PASS is NOT yet Architect ACCEPTED.

Functional Notification capability is provisionally correct and MUST NOT be unnecessarily reworked.

Repair is required for exactly these gaps:

Frontend production build was not rerun.

Original Shopeiva runtime was not proven in final runtime/browser evidence.

react-toastify -> inline flash was reported as a deviation and is not automatically acceptable under the locked Shopeiva visual/behavior contract.

Scope

Do ONLY the focused repairs/evidence needed to close these gaps.

Do NOT redesign Notification.
Do NOT rewrite Notification backend.
Do NOT add realtime/push.
Do NOT alter unrelated Storefront/Customer/Seller/Admin UI.

ABSOLUTE SHOPEIVA LOCK

These remain locked:

CSS

Tailwind

JS behavior

toast/feedback behavior where source exists

animation

transition

hover/focus/active

spacing

typography

responsive/mobile

icons/badges

card/list geometry

If the original Shopeiva notification source uses toastify or another explicit feedback pattern, Tooba MUST reproduce that behavior as closely as technically compatible.

Do not replace source interaction with a newly invented inline flash merely to avoid a dependency.

If exact library reuse is impossible/incompatible, implement the closest behavior using already-available project primitives ONLY if:

visual result matches source

timing/placement/interaction matches source

evidence explicitly proves parity

no foreign UI appears

A — Recovery

Run:

git rev-parse --show-toplevel
git fetch origin
git branch --show-current
git rev-parse HEAD
git rev-parse origin/main
git status --short --branch

Expected predecessor:

221e3bc7b189ec443faa42b3ee29ee728030b0d7

Require:

main

HEAD == origin/main

safe tracked tree

If conflict:
RECOVERY_CONFLICT

B — Re-open Exact Shopeiva Notification Source

Inspect actual Shopeiva notification source used in T023.

Document precisely:

source route/file

feedback mechanism after read/delete/filter actions

whether react-toastify is actually used there

toast position

duration

icon/type

animation/transition

mobile behavior

Evidence:

docs/evidence/TB-P06-T023-R1/01-shopeiva-feedback-source.md

C — Repair Feedback Behavior

If source uses toastify:

restore equivalent toast interaction

reuse existing dependency if already available

if dependency must be added, add only if small/safe and consistent with source

preserve exact-ish placement/timing/behavior

no generic custom notification banner

If source does NOT actually use toastify for these actions:

prove this from source

remove/repair any unnecessary inline-flash divergence so final behavior matches source

No broad UI refactor.

Evidence:

02-feedback-parity-repair.md

D — Verify Shared Customer/Seller Notification UI

Confirm both:

/customer-panel/notifications
/vendor-panel/notifications

still reuse the intended notification component/system and do not fork CSS/JS.

Evidence:

03-shared-notification-ui-proof.md

E — Original Shopeiva Runtime

Start Original Shopeiva.

Verify it is actually running.

Capture exact URL(s) for comparable Notification UI if routable.

If direct route requires auth/demo state:

prove runtime is up

navigate as far as technically possible

capture the source-equivalent screen or component state

Do not substitute source-file inspection alone for runtime proof unless runtime route is technically unreachable and this is documented with evidence.

Evidence:

04-shopeiva-runtime-proof.md

F — Browser Side-by-Side

Capture real browser comparison:

Customer Notification:

Tooba

Shopeiva

Seller Notification:

Tooba

Shopeiva comparable Vendor screen/pattern

At minimum compare:

layout geometry

spacing

typography

unread state

icons

badges

action feedback

hover/focus

responsive/mobile

Evidence:

05-browser-side-by-side.md

G — Full Frontend Validation

Run:

cd src/frontend
npm run typecheck
npm run lint
npm run test
npm run test
npm run test
npm run test
npm run build

Also run Notification-specific tests/guards.

ALL must PASS.

Record exact output summary.

Evidence:

06-frontend-validation.md

H — Backend Regression Check

Backend is not expected to require logic changes.

Still run targeted Notification tests.

If any backend code changes are made, run full:

dotnet restore src/backend/Tooba.slnx
dotnet build src/backend/Tooba.slnx
dotnet test src/backend/Tooba.slnx

Require:

warnings = 0
errors = 0
failed = 0
skipped = 0

Evidence:

07-backend-regression.md

I — Final Runtime

Keep running where technically possible:

Backend

Tooba Frontend

Original Shopeiva

Verify:

Backend:
http://127.0.0.1:5088

Tooba Frontend:
http://127.0.0.1:3000

Original Shopeiva:
http://127.0.0.1:3001

Verify:

health/live 200

health/ready 200

Customer Notifications 200/auth flow

Seller Notifications 200/auth flow

Shopeiva comparison route/runtime

Evidence:

08-final-runtime.md

J — Visual Contract Recheck

Explicitly report:

Customer CSS = MATCH/PRESERVED
Seller CSS = MATCH/PRESERVED
JS interaction = MATCH/PRESERVED
Feedback/toast behavior = MATCH/PRESERVED
Animation = MATCH/PRESERVED
Transition = MATCH/PRESERVED
Hover/Focus = MATCH/PRESERVED
Responsive = MATCH/PRESERVED
Unauthorized deviation = NONE

If any remain:
Task cannot PASS.

Evidence:

09-visual-contract-recheck.md

K — Source of Truth

If repair succeeds:

P06 = IN_PROGRESS
TB-P06-T022 = ACCEPTED
TB-P06-T023 = REPAIRED_BY_TB-P06-T023-R1
TB-P06-T023-R1 = AWAITING_ARCHITECT_ACCEPT

NOTIFICATION_BACKEND = LIVE
CUSTOMER_NOTIFICATION_UI = LIVE
SELLER_NOTIFICATION_UI = LIVE
NOTIFICATION_UNREAD = LIVE
REALTIME_NOTIFICATIONS = DEFERRED
FAKE_NOTIFICATIONS = FORBIDDEN
VISUAL_CONTRACT = SHOPEIVA_LOCKED
NOTIFICATION_VISUAL_PARITY = PROVEN

Update:

docs/PROJECT-STATE.md

docs/ROADMAP.md

docs/ai/TOOBA-RECOVERY-CONTEXT.md

docs/prompts/START-HERE-IF-CHATGPT-IS-LOST.md

Acceptance Criteria

PASS requires ALL:

no unnecessary backend rewrite

exact Shopeiva feedback interaction restored/proven

no inline-flash deviation unless source itself proves equivalent

Customer notification UI source parity proven

Seller notification UI source parity proven

Original Shopeiva runtime proven

side-by-side browser evidence

typecheck pass

lint pass

critical-storefront pass

storefront pass

customer pass

seller pass

notification tests pass

frontend production build pass

backend targeted regression pass

final runtimes healthy

unauthorized visual deviation = NONE

HEAD == origin/main

tracked tree clean

Worker may report:

TRANSACTIONAL_NOTIFICATIONS_VISUALLY_ACCEPTABLE_FOR_ARCHITECT_REVIEW

Worker must NOT report:

USER_VISUAL_ACCEPTED
PRODUCT_FULLY_READY
PRODUCTION_GO_LIVE_READY

Git

git add ...
git commit -m "fix restore Notification Shopeiva parity [TB-P06-T023-R1]"
git push origin main
git fetch origin
git rev-parse HEAD
git rev-parse origin/main
git status --short --branch

Require:
HEAD == origin/main
tracked tree clean

Result Contract

PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_WORKER_RESULT

Task-ID:
TB-P06-T023-R1

Channel:
tooba-main

Status:
PASS | FAIL | BLOCKED | RECOVERY_CONFLICT

Summary:
...

Recovery:

predecessor:

HEAD/origin:

tree:

Shopeiva-Source:

notification file:

feedback mechanism:

toast library:

position:

timing:

animation:

mobile:

Repair:

inline flash removed:

toast/source behavior restored:

dependency added:

reason:

unrelated UI changed:

Shared-UI:

Customer component:

Seller component:

duplicate CSS:

duplicate JS:

Browser-Proof:

Customer Tooba:

Customer Shopeiva:

Seller Tooba:

Seller Shopeiva:

desktop:

mobile:

Visual-Fidelity:

CSS:

JS:

feedback:

animation:

transition:

hover/focus:

responsive:

unauthorized deviation:

Validation:

typecheck:

lint:

critical-storefront:

storefront:

customer:

seller:

notification:

frontend build:

backend targeted:

backend full if changed:

warnings:

errors:

failed:

skipped:

git diff --check:

Runtime:

Backend:

Frontend:

Shopeiva:

health/live:

health/ready:

Customer Notifications:

Seller Notifications:

Shopeiva comparison route:

Source-of-Truth:

P06:

T022:

T023:

T023-R1:

Notification backend:

Customer UI:

Seller UI:

realtime:

visual parity:

visual contract:

Git:

commit:

push:

final HEAD:

origin/main:

synchronized:

tracked tree:

Architectural-Concerns:
...

Visual-Concerns:
...

Blockers:
...

END_TOOBA_WORKER_RESULT

After Result:

return to IDLE

do not self-issue another Task

keep Backend + Tooba Frontend + Original Shopeiva running where technically possible

END_TASK