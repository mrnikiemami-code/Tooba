PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_TASK

Task-ID:
TB-P07-T029-R1

Parent-Task:
TB-P07-T029

Channel:
tooba-main

Title:
Media DAM Acceptance Repair — Full Validation, Frontend Production Verification, Upload-State UX

Role:
You are the implementation Worker for Tooba.
Repair ONLY the acceptance gaps left by TB-P07-T029.
Do NOT start T030.
Do NOT redesign Media architecture unless required to close the explicit gaps below.

ARCHITECT REVIEW

TB-P07-T029 is NOT fully accepted yet.

Preserve the functional Media DAM work:

canonical Media module owns binaries/metadata

Catalog stores references only

real multipart upload exists

Product Media opens a real Media Library

existing assets can be selected

Product unassign != asset delete

Category supports image/icon/banner assignment

centralized media error codes exist

live API verification was performed

But T029 explicitly required FULL validation and the Worker reported:

backend full suite: NOT run

frontend typecheck: NOT run

frontend lint: NOT run

frontend production build: NOT run

git diff --check: NOT separately run/logged

Therefore PASS is not acceptable yet.

Also close this UX gap:

required upload progress/state

current Result says browser-native only, no application-level upload state

USER_VISUAL_ACCEPTED=NO.

A — PRESERVE MEDIA DAM IMPLEMENTATION

Do not regress:

Tooba.Media ownership

IMediaObjectStore

LocalFileMediaStore dev provider

configurable media root/limits

safe multipart upload

server-side MIME/size validation

Media Library paging/search

Product multi-select

Category single-select

Product gallery

Category image/icon/banner

unassign != delete

no raw GUID in normal UX

no Catalog binary storage

centralized media errors

B — UPLOAD STATE / PROGRESS UX

Media Library upload tab must provide visible application-level upload state.

For each selected file show:

filename

queued/uploading/succeeded/failed state

visible progress if current transport supports byte progress without a heavy dependency

otherwise an indeterminate progress state while uploading

success state

localized failure state

retry where practical

overall uploading state preventing ambiguous duplicate submissions

Do NOT fake percentage if actual byte progress is unavailable.
Do NOT rely only on browser-native picker behavior.
fa/en required.

C — LIVE FRONTEND VERIFICATION

Product:

Product EDIT → Media

click افزودن رسانه

real Media Library dialog opens

upload tab visible

select/upload image

application-level upload state visible

upload completes

asset appears in library

select and assign

gallery updates

set primary

edit AltText

reorder

unassign

reopen library and confirm asset remains

Category:

Category EDIT

set image

set icon

set banner

reload

previews persist

replace one

unassign one

asset remains in DAM

Invalid file:

localized media error

no Bad Request/raw HTTP/raw code

D — FULL BACKEND VALIDATION

Run:

backend restore

backend build

FULL backend test suite, not scoped-only

MigrationRunner/tests if repository convention includes them

Require:

0 failed

0 skipped

no new warnings

E — FULL FRONTEND VALIDATION

Run:

frontend typecheck

frontend lint

frontend full relevant test suites

frontend production build

Require:

0 failed

0 skipped

no new warnings

F — GIT DIFF CHECK

Run explicitly:
git diff --check

Must PASS.

G — RUNTIME

Keep alive:

Backend :5088

Frontend :3000

Shopeiva :3001

Verify current HEAD binaries are loaded.
Do not leave stale pre-repair processes.

H — ACCEPTED DEFERRED ITEMS

These may remain deferred and are NOT blockers:

global DAM asset deletion UI

derived thumbnail generation

Category role-table normalization

But current image delivery must work and evidence must not claim generated thumbnails if originals are used.

I — EVIDENCE UPDATE

Update:
docs/evidence/TB-P07-T029/

Add/update:

full-validation.md

upload-ux.md

live-verify.log

Record exact commands/results for:

backend full tests

frontend typecheck

lint

tests

production build

git diff --check

Product live upload flow

Category live media flow

J — GIT

Commit + push only after all repair validation is green.

Require:

HEAD == origin/main

scoped tracked tree clean

unrelated user changes preserved

ACCEPTANCE

PASS only if ALL:

T029 Media DAM functionality preserved

Product Media real dialog/upload/select flow works live

Category image/icon/banner works live

upload has visible application-level state/progress UX

invalid upload produces localized human error

backend FULL suite passes

frontend typecheck passes

frontend lint passes

frontend tests pass

frontend production build passes

git diff --check passes

current HEAD loaded in runtime

no new warnings

Git synchronized

USER_VISUAL_ACCEPTED=NO.

RESULT CONTRACT

Return ONLY:

PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_WORKER_RESULT

Task-ID:
TB-P07-T029-R1

Parent-Task:
TB-P07-T029

Channel:
tooba-main

Status:
PASS | FAIL | BLOCKED | RECOVERY_CONFLICT

Summary:
...

Preserved-Media-DAM:

Media owner:

Catalog binary storage:

Product Library:

Category media:

unassign != delete:

raw GUID normal UX:

Upload-UX:

per-file state:

progress:

indeterminate fallback:

success:

failure:

retry:

duplicate-submit protection:

fa:

en:

Live-Product:

dialog:

upload tab:

upload:

upload state visible:

library appearance:

assign:

primary:

AltText:

reorder:

unassign:

asset remains:

Live-Category:

image:

icon:

banner:

reload:

replace:

unassign:

asset remains:

Errors:

invalid file:

raw Bad Request:

raw HTTP:

raw code:

localized fa:

localized en:

Validation:

backend restore:

backend build:

backend FULL tests:

backend passed:

backend failed:

backend skipped:

frontend typecheck:

frontend lint:

frontend tests:

frontend production build:

frontend passed:

frontend failed:

frontend skipped:

warnings:

new warnings:

git diff --check:

Runtime:

Backend:

Frontend:

Shopeiva:

current HEAD loaded:

kept alive:

Evidence:

full-validation:

upload-ux:

live verification:

path:

Git:

branch:

commit:

push:

final HEAD:

origin/main:

synchronized:

tracked tree:

unrelated user changes:

Architectural-Concerns:
...

Visual-Concerns:
USER_VISUAL_ACCEPTED=NO

Blockers:
...

END_TOOBA_WORKER_RESULT

After Result:

Worker -> IDLE / Waiting

do not invent next task

do not add explanation outside Result envelope

END_TOOBA_TASK