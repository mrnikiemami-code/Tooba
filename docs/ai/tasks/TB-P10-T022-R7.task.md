PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_TASK

Task-ID:
TB-P10-T022-R7

Parent-Task:
TB-P10-T022-R6

Channel:
tooba-main

WorkerId:
tooba-worker-01

AgentType:
cursor

Status:
ISSUED

Appearance-USER_VISUAL_ACCEPTED=YES
Builder-USER_VISUAL_ACCEPTED=NO

Title:
Preview Context Unification — Strict Store/Sample Separation, Empty-State Layout Placeholders, Dynamic Locale, Responsive Media Focal Crop

Architect Decision

Runtime inspection exposed four preview-contract defects that MUST be fixed before expanding any other industry:

Store-data preview leaks Template/Sample content when Store content is missing.

Empty Store sections disappear or show poor no-data text instead of preserving the real template geometry.

iframe preview and full-page preview can render materially different hero/banner composition/crop.

Preview language is not a first-class dynamic context driven from current language/translation tables.

This repair fixes all four together.

Do NOT expand to other industries.
Do NOT implement clone.
Do NOT create TB-P10-T023.

Recovery State

Phase: P10
Last Architect-accepted: TB-P10-T022-R6
Last Implementation: TB-P10-T022-R6
Current Repair: TB-P10-T022-R7
Expected previous HEAD: 7f6e23c811d69d27e011a5d9f43ee2725cb2afe9
Worker: tooba-worker-01

Preserve LOCK-SF-001…296.

1. Recovery Preflight

Record:

branch / HEAD / origin/main / git status

current Fashion preview routes

current Admin iframe URL generation

current “نمایش در یک صفحه” URL generation

current preview source resolver

current locale/language tables and translation resolution

current media schema and crop/object-position behavior

current Store-mode fallback logic

current empty-section behavior

unrelated local changes

Unexpected tracked divergence => RECOVERY_CONFLICT/BLOCK.

Evidence:
docs/evidence/TB-P10-T022-R7/recovery-start.md

2. One Canonical Preview Context

Define ONE shared preview context consumed by both iframe and full-page preview.

Conceptually:

TemplatePreviewContext

TemplateKey

SourceMode = Sample | Store

Locale

DeviceMode = Desktop | Tablet | Mobile | FullPage

Store/current-store context resolved by existing architecture

Do NOT create duplicate rendering/business logic for iframe vs full page.

Both must resolve through the SAME:

data resolver

locale resolver

composition

shared renderer

media crop contract

The user-added “نمایش در یک صفحه” action must open the same canonical preview URL/context, not a separate content implementation.

3. Strict Source Isolation

HARD LOCK.

Sample mode

Reads ONLY Template Catalog:

TemplateProduct*

TemplateCategory*

TemplateBrand*

Template landing/banner content

explicitly designed shared demo content

Store mode

Reads ONLY operational Store data:

Product*

Category*

Brand*

Store banner/content

Store reviews/articles/stories where applicable

Critical:

Store has NO banner => MUST NOT show Template/Sample banner.

Store has NO reviews => MUST NOT show Sample reviews.

Store has NO articles => MUST NOT show Sample articles.

Store has NO brands/categories/products => MUST NOT substitute Template Catalog rows.

NO implicit fallback Store -> Sample.

4. Preview-Only Empty Layout Placeholders

Store mode must still communicate template appearance when Store lacks content.

Therefore:

keep the actual Section

keep the actual Variant geometry

keep the actual spacing/aspect/layout

render Preview-only placeholder content inside that Section

Placeholders:

are NOT database records

are NOT Template Catalog entities

are NOT cloned

exist only in preview rendering

are visually distinguishable as preview guidance

never appear on published Storefront

Examples:

Banner with no Store banner:

preserve exact banner geometry

neutral placeholder area

user-facing text equivalent to: “تصویر بنر فروشگاه شما در این قسمت نمایش داده می‌شود”

Reviews with no Store reviews:

preserve actual review-card layout/count

generic preview cards

clear label that this is layout preview only

Articles/products/categories/brands with no Store rows:

preserve real Section geometry

show layout placeholders only

DO NOT inject Fashion Template rows

Use one shared preview-placeholder policy, not scattered hardcoded page-specific fallbacks.

5. Placeholder Coverage

At minimum cover:

Hero/Carousel

StoryRail

CategoryShowcase

ProductShowcase

ProductRankedList

BannerShowcase

BrandShowcase

ReviewsShowcase

ArticleShowcase

Promo/RichText where content can be absent

Reuse the same Variant renderer with preview-placeholder data contracts where possible.
Do NOT create parallel fake renderer components.

6. Dynamic Locale from Existing Language Tables

Locale becomes a first-class Preview Context value.

Audit existing Language/Locale/Translation structures and use them directly.

Requirements:

Preview language options come dynamically from existing backend language configuration

NO hardcoded fa, en, ar list

selected locale flows into iframe URL and full-page URL

Template Catalog reads requested locale translations

Store mode reads operational requested locale translations

fallback uses current system/store default-language semantics

do NOT hardcode fa-IR as fallback unless current configuration says so

direction RTL/LTR comes from current language metadata/configuration if available

language selector belongs at Preview toolbar/page level, not Section level

Required toolbar:
Desktop / Tablet / Mobile + Language selector.

7. Translation Coverage

Dynamic locale must affect, where existing models support localization:

Template category names

Template product names/descriptions

Template brand localized fields

landing/home section titles/config

Store category/product/brand translations

Review/article/story display

Preview placeholder text

Do NOT invent a new translation framework.
If an existing entity is not localized today, document it rather than creating a parallel localization system.

8. Iframe / Full-Page Content Identity

For the same:

Template

SourceMode

Locale

Device class/desktop contract

iframe and full-page must resolve the SAME:

section order

section variant

content IDs

media IDs

localized strings

hero/banner asset

User-facing status text should be only product language such as:

“پیش‌نمایش با داده نمونه”

“پیش‌نمایش با اطلاعات فروشگاه شما”

Remove developer-facing text such as “Template Catalog پایدار — بدون مخلوط فروشگاه عملیاتی” from final UI.

9. Responsive Media Crop / Focal Point

The user explicitly requires hero/banner imagery to remain visually correct across:

large desktop monitors

normal desktop

tablet

mobile

A plain object-fit: cover without composition control is insufficient.

First audit current media schema.

If media model already supports focal/crop metadata

Use it consistently in Template and Store preview/rendering.

If media model does NOT support it

Add the MINIMAL standard media-composition fields needed to the existing relevant media model.

Preferred minimal semantics:

FocalPointX normalized 0..1

FocalPointY normalized 0..1

Optional only if truly needed by current renderer:

controlled CropMode/FitMode enum

Do NOT create device-specific pixel crop fields.
Do NOT add arbitrary per-breakpoint coordinates unless technically proven necessary.

Render semantics should derive object-position from normalized focal point.

10. Fashion Media Focal Data

Populate appropriate focal points for Fashion:

Hero

campaign banners

category imagery where crop matters

product media only if current ProductCard crop contract uses focal metadata

The Fashion woman/shopping-bags hero must preserve intended subject placement across wide and narrow viewports.

No one-off image-specific CSS.

11. Store Media Compatibility / DB Performance

For operational Store media:

use stored focal metadata when available

existing rows without focal data use deterministic safe default, normally center

do NOT mass-update existing large tables just to fill focal defaults

nullable new fields with null => center semantics are acceptable/preferred if appropriate

Document migration/storage impact.

12. Device Contracts

Desktop:

current Desktop preview contract

Tablet:

real tablet viewport width

Mobile:

real phone viewport width

Full-page:

actual browser viewport

No CSS zoom/scale.

Same asset + same focal metadata + same Section variant must be used across contexts.
Natural crop differences are acceptable; losing the intended subject/composition is not.

13. Published Storefront Safety

Preview-only placeholders MUST NEVER appear on real published Storefront.

Real Storefront keeps existing production empty/omission behavior as appropriate.

This task changes preview behavior, not published-data truth.

14. Admin Theme Isolation

Recheck:

Storefront theme applies only inside preview surface

Admin toolbar/language/device controls remain Admin theme

Seller Panel remains unaffected

15. Runtime Proof

Host :5088 + FE :3000.

Scenario A — Sample mode

Fashion Sample mode

configured locale selected dynamically

Template Catalog content shown

Scenario B — Store mode with missing Banner

Store has no matching banner

NO Template/Sample banner appears

real Banner Section geometry remains

preview-only banner placeholder appears

Scenario C — Store missing Reviews/Articles

NO Sample review/article rows

preview-only layout placeholders appear

Scenario D — Source purity

Store mode content IDs only from operational Store data

Template content hits = 0 for Store content resolver

Sample mode operational hits = 0

Scenario E — Locale

language options come from configured languages

switch to another configured locale if available

URL/context changes dynamically

translated Template/Store strings resolve

fallback follows configured default language

RTL/LTR follows configured metadata

Scenario F — iframe vs Full Page

For same Template + Source + Locale:

same hero/banner media asset

same section sequence

same localized content

same focal metadata

Scenario G — Responsive crop

Capture:

wide desktop full-page

normal desktop iframe

tablet

mobile

Hero/banner subject must remain intentionally framed.

Required screenshots:

store-mode-no-banner-no-sample-leak.png

store-mode-banner-placeholder.png

store-mode-empty-reviews-placeholder.png

store-mode-empty-articles-placeholder.png

sample-mode-fashion-pure.png

preview-language-selector.png

preview-locale-rtl.png

preview-locale-ltr.png (if a configured LTR locale exists)

iframe-hero.png

fullpage-hero.png

hero-wide-desktop.png

hero-tablet.png

hero-mobile.png

admin-theme-isolated-r7.png

Evidence:
docs/evidence/TB-P10-T022-R7/

16. Focused Tests

Required only:

Store mode NEVER falls back to Template/Sample Banner

Store mode NEVER falls back to Template/Sample Product/Category/Brand

missing Store content uses preview-only placeholder

preview placeholders never persist to DB

preview placeholders never render on published Storefront

locale options come from current language configuration, not hardcoded list

locale flows into canonical preview context

configured default-locale fallback works

iframe/full-page same content/media identity for same context

focal-point crop contract works across desktop/tablet/mobile

operational large-table migration has no forced mass data rewrite

Admin theme isolation

critical-storefront

recovery-staleness

git diff --check

No screenshot pixel tests.
No all-industry matrix.

17. Anti-Pattern Gate

Reject:

Store -> Sample implicit fallback

Template banner shown in Store mode when Store has no banner

placeholder records inserted into DB

duplicate iframe/full-page renderer

hardcoded language array

hardcoded fa-IR fallback outside current configuration

device-specific magic crop CSS

one-off image-specific CSS selectors

CSS zoom for device preview

mass update of large operational tables merely to populate focal defaults

Admin theme pollution

clone implementation

other-industry expansion

polling/timeouts/workarounds

Evidence:
docs/evidence/TB-P10-T022-R7/antipattern-scan.md

18. Locks

Add non-duplicate locks after LOCK-SF-296:

LOCK-SF-297 Preview source modes are strictly isolated: Store mode never falls back to Template/Sample catalog content.
LOCK-SF-298 Missing Store content in preview preserves real Section/Variant geometry using non-persistent preview-only placeholders.
LOCK-SF-299 Preview-only placeholders must never persist and must never appear on published Storefront.
LOCK-SF-300 Iframe and full-page preview consume one canonical preview context and shared renderer/data resolver.
LOCK-SF-301 Preview locale is dynamic from existing language configuration/translation tables and is page-level, not section-level.
LOCK-SF-302 Same preview context must resolve identical content/media identity in iframe and full-page rendering.
LOCK-SF-303 Hero/banner responsive media uses standard focal-point semantics; device-specific magic crop coordinates are forbidden.
LOCK-SF-304 Existing large operational media data must not require mass rewrite merely to adopt focal-point defaults; null/default-center semantics are permitted.
LOCK-SF-305 User-facing preview UI must not expose internal architecture terms such as Template Catalog purity/debug text.

19. Recovery / Pipeline

At completion:

Phase P10

Last Architect-accepted before review: TB-P10-T022-R6

Last Implementation: TB-P10-T022-R7

Current Issued/Repair: none

Appearance USER_VISUAL_ACCEPTED=YES

Builder USER_VISUAL_ACCEPTED=NO

Worker IDLE

branch main

HEAD==origin/main

task-owned clean

unrelated user changes preserved

do NOT invent/start TB-P10-T023

Acceptance

PASS only if ALL:

Store/Sample source isolation is strict

missing Store banner does NOT show Sample banner

missing Store sections preserve layout via preview-only placeholders

placeholders never persist or leak to published Storefront

iframe/full-page share one canonical context

language selector is dynamic from existing language tables

locale affects Template and Store translation reads

configured fallback/direction work

hero/banner media identity is identical across iframe/full-page for same context

focal point/crop works intentionally across large desktop/tablet/mobile

any new media fields are minimal/standard/storage-conscious

no operational Catalog contamination

Admin theme remains isolated

focused tests pass

anti-pattern CLEAN

locks/recovery/git exact

Worker IDLE

no TB-P10-T023

Result Contract

Return ONLY canonical BRIDGE-WAKE-V1 Result with:

Summary
Applicable-Locks
Recovery-Start
Canonical-Preview-Context
Source-Isolation
Preview-Placeholder-Contract
Section-Placeholder-Coverage
Language-Configuration-Audit
Dynamic-Locale
Translation-Resolution
Iframe-FullPage-Identity
Media-Schema-Audit
Focal-Point-Implementation
Responsive-Media-Proof
Store-Media-Compatibility
Published-Storefront-Safety
Admin-Theme-Isolation
Focused-Validation
Runtime
Visual-Evidence
AntiPattern-Scan
Lock-Registry
Recovery-SoT
Git
Architectural-Concerns
Visual-Concerns
Blockers

END_TOOBA_TASK