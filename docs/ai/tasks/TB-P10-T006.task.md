PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_TASK

Task-ID:
TB-P10-T006

Parent-Task:
TB-P10-T005-R1

Channel:
tooba-main

WorkerId:
tooba-worker-01

AgentType:
cursor

Status:
ISSUED

USER_VISUAL_ACCEPTED=NO

Title:
Store Appearance Admin — Curated Palette Selection, Preview, Persistence, Cache Invalidation, Live Storefront Apply

Architect Decision

TB-P10-T005-R1 is Architect-ACCEPTED.

Accepted baseline now includes:

canonical StoreAppearanceSettings

Store-scoped PaletteKey + reserved ThemeMode

semantic token foundation

typed palette registry

SSR/root CSS-variable application

Store isolation

future controlled custom theme contract

future Landing/Home/Page contract

LOCK-SF-001…138

This task implements the first user-facing Appearance capability:

Store Admin can choose from curated palette presets, preview the result, save it, and the live Storefront applies the selected palette without rebuild/redeploy.

This task does NOT implement:

arbitrary/custom color builder

free-form CSS/HTML/JS

Dark Mode painting/toggle

Product Card Skin selection

Landing/Page builder

Menu builder

TB-P10-T007

Recovery State

Phase: P10
Last Architect-accepted: TB-P10-T005-R1
Last Implementation: TB-P10-T005-R1
Current Task: TB-P10-T006
Worker: tooba-worker-01
Expected branch: main
Expected previous HEAD: 29ca9f0f66103af19283a84f2ef98bfffcf34bb8
USER_VISUAL_ACCEPTED=NO

Preserve LOCK-SF-001…138.

1. Recovery Preflight

Record:

branch

HEAD

origin/main

git status --short

Recovery/SoT

current Host :5088 process/runtime

current FE :3000 runtime

unrelated local user-authored changes

Unexpected tracked divergence => RECOVERY_CONFLICT/BLOCK.

Evidence:
docs/evidence/TB-P10-T006/recovery-start.md

2. Mandatory Discovery — Existing Admin Settings UX

Inspect targeted Admin code only.

Determine:

canonical Store settings route/page/workspace

existing tabs/sections and save pattern

authorization/permission model

API/BFF conventions for Store settings

dirty/save/error handling

notification/toast conventions

existing preview component patterns

current StoreAppearanceSettings API write path, if any

whether Admin currently has an Appearance/Theme placeholder

Do NOT create a parallel settings shell if one already exists.

Evidence:
docs/evidence/TB-P10-T006/admin-appearance-discovery.md

3. Curated Palette Registry

Expand the typed palette registry from T005 to a small professional curated set.

Target:

6 to 8 production-quality presets

one remains the canonical existing tooba-blue

other presets should be visually distinct but commercially safe

each palette must define all required brand tokens used by Storefront

system semantic colors danger/success/warning remain untouched

Palette keys must be:

stable

machine-safe

code-owned

not editable from DB

Do NOT add arbitrary user-defined colors.

Document palette names/keys/tokens.

Evidence:
docs/evidence/TB-P10-T006/palette-set.md

4. Admin Appearance Surface

Add a real Appearance section/tab to the canonical Store Admin settings.

Required UX:

clear title/description

palette preset cards/swatches

active/current selection

preview before save

Save

Cancel/reset unsaved selection

dirty state

loading state

save-in-progress state

success feedback

API error surfaced clearly

unknown/deprecated stored PaletteKey handled safely

No fake/nonfunctional controls.

Do not expose ThemeMode yet except if existing model requires read-only representation.
Dark mode is not part of this task.

5. Palette Preview

Preview must be controlled and representative.

At minimum preview:

primary CTA/button

link/accent

simple card/surface

text/muted text

border

status colors visibly remain semantic and unchanged

Preview must use the SAME palette registry/tokens as runtime, not duplicated hard-coded sample colors.

Preview is local/unsaved until Save.

Do NOT mutate live Storefront appearance while merely previewing in Admin unless the existing settings architecture explicitly supports reversible preview isolation.

6. Persistence API

Implement/reuse canonical backend write path for StoreAppearanceSettings PaletteKey.

Required:

backend-authoritative Store scope

validate PaletteKey against approved registry/contract

reject invalid arbitrary values

preserve ThemeMode

upsert/update according to current aggregate conventions

concurrency handling consistent with existing settings infrastructure

no direct frontend DB assumptions

no cross-store update

Do not store color hex values from Admin.
Store only approved PaletteKey.

7. Permissions

Appearance settings are Store Admin capability.

Required:

unauthorized caller cannot update

Store A admin cannot update Store B appearance

backend enforces permission/scope

frontend hides/disables according to current permission UX, but backend remains authoritative

Use existing authorization model.
Do not invent a parallel permission system.

If a precise existing permission already covers Store settings, reuse it.
If a new permission is genuinely required, add it through canonical permission registry/migration and document rationale.

8. Cache Invalidation

T005 uses cached Store appearance projection.

On successful save:

invalidate only the affected Store appearance cache

next Storefront request resolves new palette

no global cache flush

no waiting for fixed 5-minute TTL

no app restart

no frontend rebuild/redeploy

Prove:
Save palette A -> B
then Storefront reflects B on next appropriate request/navigation.

Evidence:
docs/evidence/TB-P10-T006/cache-invalidation.md

9. SSR / Live Storefront Apply

After successful save:

first HTML response for Storefront must carry selected palette marker/tokens

no client-only recolor required

no FOUC

no hydration mismatch

existing semantic-tokenized surfaces change accordingly

deferred hard-coded surfaces may remain unchanged and must be explicitly listed

Do NOT silently claim full-site recolor while Home/PDP/Shipping hard-coded areas remain deferred.

Evidence:
docs/evidence/TB-P10-T006/live-apply.md

10. Default / Fallback Safety

Required:

existing stores with no appearance row remain tooba-blue

unknown/deprecated PaletteKey does not crash

invalid write is rejected

read fallback remains safe

fallback must not leak another Store's palette

deleting/resetting appearance, if current aggregate supports it, returns canonical default without orphan state

Do not add destructive reset if no canonical settings reset pattern exists.

11. Store Isolation

Prove with at least two distinct Store scopes where test infrastructure permits:

Store A = palette X

Store B = palette Y

A receives X only

B receives Y only

changing A does not invalidate/change B

Admin scope cannot cross-update

If local runtime only has one configured store, cover the second scope with focused integration tests rather than fabricating production-like data manually.

Evidence:
docs/evidence/TB-P10-T006/store-isolation.md

12. Visual Contract

This task intentionally changes brand palette only when user selects a non-default preset.

Rules:

tooba-blue remains visually equivalent to current accepted default

layout/spacing/typography/DOM interaction behavior unchanged

status colors remain semantic

no Shopeiva structural redesign

no Product Card layout changes

no Home section changes

no dark mode

Capture representative visual evidence for at least:

Admin Appearance settings

Home/header with default palette

Home/header with one alternate palette

Product Card with alternate palette

Cart CTA with alternate palette

restore to default

USER_VISUAL_ACCEPTED remains NO until Architect/user review.

13. Deferred Hard-Coded Surface Inventory

T005 already deferred Home/PDP/Shipping hexes.

Re-scan and produce a concise inventory:

file/component

hard-coded value

surface

whether safe to migrate in future token-completion task

whether Shopeiva visual lock applies

Do NOT expand this task into a full migration.

Evidence:
docs/evidence/TB-P10-T006/deferred-token-inventory.md

14. Focused Tests

Required:

Backend:

GET default appearance

valid palette update

invalid PaletteKey rejected

ThemeMode preserved

Store isolation

unauthorized update rejected

cache invalidated for changed Store only

unknown persisted key safe fallback/read behavior

Frontend/Admin:
9. current palette displayed
10. select preset -> dirty
11. cancel -> prior value
12. save -> API once
13. save success -> clean
14. save failure -> remains dirty + error surfaced
15. preview uses registry tokens
16. status colors unaffected
17. unknown current key handled safely
18. no arbitrary color input

Storefront:
19. SSR marker/tokens reflect saved palette
20. default remains current blue
21. no hydration mismatch
22. no palette request polling

Regression:
23. account/header identity
24. logout Cart persistence
25. re-login restore
26. atomic checkout boundary

Also:

relevant lint/typecheck scoped to task

existing full tsc state reported truthfully

test relevant scope

test

recovery guard

git diff --check

Do NOT repair unrelated pre-existing Admin/grid typecheck issues in this task.

15. Real Runtime — REQUIRED

Run canonical:

Host :5088

FE :3000

Flow:

A. open Admin Appearance for Store
B. verify current tooba-blue
C. choose alternate curated palette
D. inspect preview before save
E. Cancel -> value/preview returns to current
F. choose alternate again
G. Save
H. verify exactly one successful write
I. open Storefront fresh navigation/request
J. verify SSR/root palette marker is alternate
K. verify representative tokenized surfaces changed
L. verify status colors did not change
M. reopen Admin -> alternate persists
N. restore tooba-blue
O. fresh Storefront -> accepted default blue restored

Record:

Store scope

PaletteKey before/after

API status

cache invalidation proof

SSR marker

console

relevant network counts

No manual DB edit to simulate save.

16. Anti-Pattern Gate

Scan for:

arbitrary color input

arbitrary CSS/HTML/JS

hex values persisted from Admin instead of PaletteKey

frontend-only persistence

localStorage as appearance SoT

global cache flush

sleep/wait for TTL instead of invalidation

full-page reload required as save mechanism

duplicate appearance state

per-card appearance API calls

polling

runtime Tailwind class generation from palette values

changing danger/success/warning with brand preset

hard-coded Store ID

cross-store update path

dark mode implementation sneaking in

Product Card skin implementation sneaking in

Landing/Page implementation sneaking in

Any introduced real occurrence => REPAIR.

Evidence:
docs/evidence/TB-P10-T006/antipattern-scan.md

17. Lock Registry

Add non-duplicate locks after LOCK-SF-138 equivalent to:

Store Admin appearance palette selection persists only approved PaletteKey values; raw colors are not stored by this preset feature.

Palette preset registry is code-owned and typed; DB references stable keys only.

Appearance save invalidates only the affected Store cache and must not require TTL wait/restart/redeploy.

Admin preview and live Storefront consume the same canonical palette token definitions.

Brand palette changes must not redefine danger/success/warning semantic colors.

Appearance write authorization and Store isolation are backend-authoritative.

Use next canonical LOCK-SF numbers.
Do not renumber existing locks.

18. Evidence

Create:
docs/evidence/TB-P10-T006/

Required:

recovery-start.md

admin-appearance-discovery.md

palette-set.md

admin-appearance-ui.md

persistence-api.md

permissions.md

cache-invalidation.md

live-apply.md

store-isolation.md

deferred-token-inventory.md

focused-validation.md

runtime.md

antipattern-scan.md

recovery-sot.md

19. Recovery / Pipeline

At completion:

Phase P10

Last Architect-accepted before review: TB-P10-T005-R1

Last Implementation: TB-P10-T006

Current Issued/Repair: none

USER_VISUAL_ACCEPTED=NO

Worker IDLE

branch main

ending HEAD

HEAD==origin/main

task-owned clean

unrelated local user changes preserved

do NOT invent/start TB-P10-T007

Run recovery staleness guard.

Acceptance

PASS only if ALL:

canonical Admin settings surface used

6–8 curated production-quality palettes exist

tooba-blue remains canonical default

no arbitrary color builder

real preview works

Save/Cancel/dirty/error states work

PaletteKey persists through canonical backend

invalid key rejected

Store authorization/isolation enforced

cache invalidated immediately for affected Store only

no restart/redeploy/TTL wait needed

Storefront SSR uses saved palette

no FOUC/hydration mismatch

status semantic colors unchanged

existing stores safely default

deferred hard-coded surfaces truthfully inventoried

R4 account/cart/checkout regressions preserved

focused tests green

runtime A–O green

anti-pattern scan CLEAN

Lock registry updated without duplicates

Recovery exact

HEAD==origin/main

Worker IDLE

no TB-P10-T007

USER_VISUAL_ACCEPTED remains NO

Result Contract

Return ONLY canonical BRIDGE-WAKE-V1 Result with:

Summary
Applicable-Locks
Recovery-Start
Admin-Appearance-Discovery
Palette-Set
Admin-Appearance-UI
Persistence-API
Permissions
Cache-Invalidation
Live-Apply
Store-Isolation
Deferred-Token-Inventory
Focused-Validation
Runtime
AntiPattern-Scan
Lock-Registry
Recovery-SoT
Git
Architectural-Concerns
Visual-Concerns
Blockers

END_TOOBA_TASK