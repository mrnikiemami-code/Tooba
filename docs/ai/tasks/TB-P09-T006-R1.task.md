PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_REPAIR_TASK

Task-ID:
TB-P09-T006-R1

Parent-Task:
TB-P09-T006

Channel:
tooba-main

WorkerId:
tooba-worker-01

AgentType:
cursor

Status:
REPAIR

USER_VISUAL_ACCEPTED=NO

Title:
Offer Return Policy + Shipping Provider Forms Final Runtime Proof — Tipax/Courier + Snapshot Immutability

Architect Review

TB-P09-T006 delivered the intended architecture and focused tests, but the Result's runtime proof is incomplete relative to the issued acceptance criteria.

The task required runtime proof for:

Offer default policy

Offer custom policy

non-returnable Offer

immutable OrderLine snapshot after Offer change

Post shipment

Tipax shipment

at least one courier/local shipment

partial delivery where only delivered quantity starts the return clock

The Result explicitly proves only:

shipping-methods endpoint

one Post create_shipment runtime

focused tests for the rest

Therefore TB-P09-T006 is NOT architect-accepted yet.

This repair is verification-first and defect-only.

DO NOT redesign UI.
DO NOT revert any user-authored width/scroll/filter/layout changes.
DO NOT add live carrier integrations.
DO NOT invent new architecture unless runtime reveals a concrete defect.

1. Preserve Current UI — Locked

Preserve all current user-authored and accepted UI:

Orders grid width behavior

horizontal scrolling

grid filters

compact kebab

seller-group layout

اقلام و ارسال

financial sections

notes

operational history

payment history

Offer editor current appearance

Create Shipment modal current appearance

No visual redesign in this repair.

2. Runtime Return Policy Proof

Use LOCAL/Development only.

Create/use deterministic Offers and fresh Orders through supported application/API/UI paths.

Required runtime cases:

A. Default Policy

Store default = 7 days

Offer = Default

Create fresh Order

OrderLine snapshot shows effective 7-day return policy

B. Custom Policy

Seller override enabled

Offer = Custom

use a non-default value, e.g. 14 days

Create fresh Order

OrderLine snapshot shows 14 days

C. Non-Returnable

Non-returnable allowed

Offer = NonReturnable

Create fresh Order

OrderLine snapshot shows not returnable

normal UI says غیرقابل مرجوعی

D. Snapshot Immutability

After creating at least one Order from A/B:

change Offer return policy

change store default if practical

reload old Order

old OrderLine snapshot MUST remain unchanged

Do NOT directly edit old OrderLine snapshot to prove this.

3. Runtime Seller Override Governance

Prove runtime behavior for:

seller override allowed -> custom option accepted

seller override disabled -> custom/non-returnable rejected or unavailable according to configured rules

invalid custom days below/above min/max rejected if those constraints are configured

human FA error

no raw technical code

If min/max are optional and not configured in current dev fixture, configure deterministic local values for smoke only through normal config.

4. Runtime Post Shipment

Repeat/confirm one Post shipment using the actual dynamic modal/API contract.

Verify:

Post method enabled

required Post fields accepted

provider metadata persisted

selected seller/line/quantity correct

shipment Created/pre-dispatch

no fake carrier booking

next actions projected

5. Runtime Tipax Shipment — REQUIRED

Create a real Tipax shipment in local runtime.

Verify:

Tipax appears only when enabled

Tipax-specific fields/validation used

recipient/address snapshot prefill/reuse where appropriate

normalized Tipax metadata persisted

selected seller/line/quantity correct

shipment remains pre-dispatch

no fake carrier success

next actions projected

If any Tipax field is unnecessarily duplicated despite Order snapshot already containing it, fix narrowly.

6. Runtime Courier/Local Shipment — REQUIRED

Use at least ONE of:

snapp_courier

store_courier

in_person

Prefer whichever is fully modeled.

Verify:

method-specific form/contract

no postal-only fields forced where inappropriate

metadata persisted

correct seller/line/quantity allocation

pre-dispatch state

no fake external booking

If using Snapp without live adapter:

prove it remains manual/external-provider record only

no fake API success

7. Enabled/Disabled Method Proof

Prove runtime:

enabled shipping method appears in /v1/admin/shipping-methods

disabled method does not appear / cannot be used

direct request with disabled method rejected backend-side

No frontend-only enforcement.

8. Split Delivery Return Clock — Live Proof

This is required in runtime, not tests only.

Create/use one OrderLine with quantity > 1 and split it across at least two Shipments.

Example:

ordered quantity = 5

Shipment A = qty 2

Shipment B = qty 3

Then:

dispatch/deliver Shipment A only

keep Shipment B undelivered

Verify:

delivered qty 2 has canonical delivered timestamp

return eligibility/deadline exists for those 2 units

undelivered qty 3 does NOT start its return clock

line-level/admin projection does not incorrectly claim all 5 have same deadline

after later delivery of Shipment B, its own return timing begins from its delivery timestamp

If current UI cannot express multiple delivery slices clearly:

fix minimally in current line detail/status presentation

do NOT redesign the entire section

9. Human UI Verification

Verify normal FA UI shows:

Return policy:

استفاده از سیاست پیش‌فرض فروشگاه

مهلت اختصاصی

غیرقابل مرجوعی

Shipping:

پست

تیپاکس

chosen courier/local method

Return timing:

human date/status

no enum codes

no raw provider metadata JSON

No raw:

Default

Custom

NonReturnable

post/tipax/snapp_courier internal codes as primary user label

10. Provider Metadata Privacy / Safety

Verify runtime/API output:

no provider secret stored in Shipment metadata

no config secret exposed to normal Order UI

only operational metadata needed for shipment shown

No raw provider request/response dump in normal UI.

11. Focused Validation

Re-run focused T006 suites plus any defect-specific regression tests.

Required categories:

return policy default/custom/non-returnable

seller override governance

snapshot immutability

Post provider validation

Tipax provider validation

courier/local provider validation

enabled/disabled method enforcement

split-delivery return clock

existing T005 fulfillment allocation regression

UI preservation

Also:

recovery guard

git diff --check

No unrelated full repository suite.

12. Runtime / Browser Smoke

Use:
Backend: http://127.0.0.1:5088
Frontend: http://127.0.0.1:3000

Required runtime evidence must include actual identifiers/data for:

default-policy Offer + OrderLine

custom-policy Offer + OrderLine

non-returnable Offer + OrderLine

old snapshot before/after Offer change

Post Shipment

Tipax Shipment

courier/local Shipment

split line quantity

Shipment A delivered timestamp/deadline

Shipment B undelivered state

Shipment B later delivered timestamp/deadline if executed

USER_VISUAL_ACCEPTED remains NO.

13. Evidence

Update:
docs/evidence/TB-P09-T006/

Add:

r1-runtime-return-policy-default.md

r1-runtime-return-policy-custom.md

r1-runtime-return-policy-nonreturnable.md

r1-runtime-snapshot-immutability.md

r1-runtime-tipax.md

r1-runtime-courier.md

r1-runtime-method-enablement.md

r1-runtime-split-delivery-return-clock.md

r1-ui-preservation.md

r1-recovery-sot.md

14. Recovery / Pipeline

Update:

Phase: P09

Last Architect-accepted: TB-P09-T005

TB-P09-T006 under repair

Current Repair: TB-P09-T006-R1

Last Implementation on completion: TB-P09-T006-R1

USER_VISUAL_ACCEPTED=NO

Worker Next State: IDLE

do NOT invent/start TB-P09-T007

Run recovery guard.

15. Git

If no code defect:

evidence-only commit acceptable

If runtime reveals defects:

fix narrowly

preserve all user UI changes

Required:

push origin/main

HEAD==origin/main

no task-owned uncommitted changes

Acceptance

PASS only if ALL:

default Offer policy runtime proven

custom Offer policy runtime proven

non-returnable Offer runtime proven

old OrderLine snapshot remains immutable after Offer/default changes

seller override governance runtime proven

Post runtime proven

Tipax runtime proven

courier/local runtime proven

disabled method backend-rejected

provider metadata persisted without secrets

split delivery return clock proven live

undelivered remainder does not start return clock

later delivery starts its own return timing

human FA labels verified

no UI redesign

user width/scroll/filter/layout refinements preserved

focused validation green

recovery guard passes

HEAD==origin/main

Worker IDLE

no TB-P09-T007 invented

USER_VISUAL_ACCEPTED=NO.

Result Contract

Return ONLY canonical BRIDGE-WAKE-V1 result with:

Summary
Runtime-Default-Policy
Runtime-Custom-Policy
Runtime-NonReturnable
Runtime-Snapshot-Immutability
Runtime-Override-Governance
Runtime-Post
Runtime-Tipax
Runtime-Courier
Runtime-Method-Enablement
Runtime-Split-Delivery-Return-Clock
Human-UI
Provider-Metadata-Safety
Focused-Validation
User-UI-Preservation
Recovery-SoT
Git
Architectural-Concerns
Visual-Concerns
Blockers

END_TOOBA_REPAIR_TASK