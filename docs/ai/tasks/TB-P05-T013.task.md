PIPELINE-PROTOCOL: BRIDGE-V2

TASK-ID: TB-P05-T013
PROJECT: Tooba
PHASE: P05 — Operational Surface Integration
CHANNEL: tooba-main
STATUS: ISSUED
TASK-TYPE: PRODUCT IMPLEMENTATION

# Wishlist — Add Missing Backend Capability Behind Existing Shopeiva Wishlist UI

Implement a production-grade private Wishlist in a dedicated Domain/Application/Infrastructure module. Store only WishlistItemId, server-derived owner UserId, ProductId and CreatedAt; enforce unique owner/product. Provide idempotent add, safe remove, own list, batched membership and count. Validate Published products through the Catalog application gateway and compose current presentation through Host boundaries without cross-module SQL.

Expose `/v1/customer/wishlist` list/add/remove/batch-membership endpoints using `CurrentAuthenticatedSession` and the established Development/Testing actor seam; production missing actor returns 401. Reuse Storefront composition for requested product IDs with live Offer/Pricing/Inventory/Reviews truth and an honest unavailable state. Register module, DI, project references, migration, Outbox, deterministic Development seed, dashboard capability/count, Persian public API documentation, backend tests, and initial decision/convergence evidence.

Worker scope for this execution is backend and backend tests only. Do not modify frontend, commit, push, Source-of-Truth documents, capture visual evidence, or post a Bridge result.

Acceptance includes private actor isolation, no request actor authority, no price/stock persistence, no foreign FK/navigation/cross-module SQL, idempotency, batching, migration/schema ownership, Development-only idempotent seed across up to three available categories, focused backend build/tests, and `git diff --check`.

END_TASK
