# Tooba — TB-P00-T001 — Repository & Purchased Template Technical Inventory

BEGIN_TOOBA_CURSOR_TASK_V1

Protocol-Version: 1
Task-ID: TB-P00-T001
Phase: P00 — Architecture / Discovery
Type: Technical Inventory / Discovery
Repository: https://github.com/mrnikiemami-code/Tooba
Primary-Branch: main
Implementation-Agent: Cursor
Architect: ChatGPT
Execution-Mode: PIPELINE
Depends-On: TB-P00-T000
Architect-Decision-On-Dependency: ACCEPTED

## Objective

Create a factual technical inventory of:

1. the current canonical Tooba repository;
2. the purchased Shopeiva template as an architecture/adaptation input;
3. reusable assets/capabilities vs risks/gaps;
4. constraints that later P00 architecture tasks must account for.

This is discovery only.

Do not integrate the template.
Do not implement product features.
Do not choose final bounded contexts.
Do not redesign the frontend.

---

## 1. Mandatory Repository Recovery

Run before edits:

```bash
git rev-parse --show-toplevel
git fetch origin
git branch --show-current
git rev-parse HEAD
git rev-parse origin/main
git status --short --branch
```

Normal execution requires:

```text
branch = main
HEAD == origin/main
working tree = clean
```

If unsafe or unknown state exists, stop with:

```text
RECOVERY_CONFLICT
```

Do not force push, rewrite history, destructively reset, silently stash, or overwrite unknown work.

Expected predecessor commit from accepted TB-P00-T000:

```text
012e1a7dc9eb9a80944e61716da25a53dbd6d34c
```

If `origin/main` has legitimately advanced after that commit, do not fail merely because HEAD is newer; require only safe synchronized `main` and inspect history.

---

## 2. Accept TB-P00-T000 in SoT

As part of this task, update repository Source of Truth to reflect the Architect decision:

```text
TB-P00-T000 = ACCEPTED
```

and make `TB-P00-T001` the current issued / awaiting Architect acceptance task.

Do not claim T001 accepted.

---

## 3. Authoritative Architect-Supplied Template Inventory

The purchased archive supplied to Architect is:

```text
shopeiva.zip
```

It is **not currently present in the canonical repository**.

The Architect directly inspected the archive and verified these facts.

Treat them as authorized discovery evidence for this task:

### Framework / package baseline

```text
Next.js 16.2.6
React 19.2.4
React DOM 19.2.4
Tailwind CSS 4
ESLint 9
eslint-config-next 16.2.6
```

### Relevant dependencies present

```text
axios
chart.js
framer-motion
fuse.js
lucide-react
next-themes
persian-date
persian-datepicker
react-chartjs-2
react-hook-form
react-loading-skeleton
react-otp-input
react-paginate
react-toastify
swiper
zod
zustand
```

### App Router

The template uses:

```text
src/app/
```

with route groups and dynamic routes.

Architect counted approximately:

```text
73 page/layout/route entry files
```

at direct archive inspection time.

### Verified page/capability examples

The archive contains storefront routes/pages for concepts including:

```text
home
alternate home variants
product detail
categories
category/subcategory
brands
brand detail
search
cart
payment
offers/sale
best seller
most viewed
new products
trending
compare
gift card
coupons
premium
referral
seller list/profile
login
register
forgot password
static informational pages
blogs/article
```

Customer area includes concepts such as:

```text
dashboard
profile
addresses
orders
wishlist
wallet
gift cards
notifications
tickets
settings
```

Vendor area includes concepts such as:

```text
vendor registration
vendor dashboard
analytics
products
orders
customers
reviews
coupons
gift cards
wallet
tickets
settings
```

### Data / assets

The archive includes substantial local static assets and demo content, including:

```text
public/images/
public/fonts/
public/jsons/
```

Examples observed include product/category/brand/banner/payment/blog imagery and local demo JSON datasets.

The template includes Persian-oriented assets and date/font dependencies.

### Important interpretation

These facts do NOT mean these template concepts are approved Tooba domain boundaries or approved production implementations.

The template is:

```text
UI/UX implementation input
+
potential reusable frontend code/assets
+
technical migration/adaptation input
```

It is NOT:

```text
domain architecture truth
backend architecture truth
SEO architecture truth
security architecture truth
tenant architecture truth
authorization truth
```

---

## 4. Repository Inventory

Inspect the canonical repository as it exists after T000.

Document:

- top-level files/directories;
- whether application source exists yet;
- current documentation/pipeline structure;
- any build/tooling/configuration already present;
- any existing language/framework runtime files;
- any CI/CD files;
- any Docker/container files;
- any database/migration artifacts;
- any frontend/backend source;
- any tests;
- any secrets/config risks;
- whether Shopeiva/template code is already present;
- whether `.gitignore` is adequate for current bootstrap state.

Do not add speculative application tooling.

---

## 5. Create Technical Inventory Document

Create:

```text
docs/architecture/00-technical-inventory.md
```

(See executed inventory in that file.)

---

## 14. Pipeline Continuity

After RESULT:

```text
WAITING
```

Do not invent or execute `TB-P00-T002`.

END_TOOBA_CURSOR_TASK_V1
