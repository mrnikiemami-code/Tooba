PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

TASK-ID: TB-P07-T014
PARENT-TASK: TB-P07-T013
PROJECT: Tooba
PHASE: P07
CHANNEL: tooba-main
STATUS: ISSUED
TASK-TYPE: PRODUCT MEDIA — GALLERY + PRIMARY MEDIA + ADMIN UX

Architect Acceptance

TB-P07-T013 is ACCEPTED.

Baseline:

Product Attributes and Variants are real.

Variants come from effective Category IsVariantAxis.

Product != Offer.

no Product.Price / Product.Stock.

no Pricing/Inventory direct joins.

canonical AppDataGrid untouched.

HEAD == origin/main:
c2819d8e7423a0b93490afc0969ed8ca082a264f

Locked UX

Product media must be simple for ordinary Admin users:

clearly identify main image

clearly show gallery order

easy add/remove/reorder

no raw MediaAsset IDs or storage paths

VIEW / EDIT mandatory

mobile-friendly

reuse existing Media infrastructure

Hard Locks

Reuse existing Media module/contracts.

Catalog references Media assets; do not store duplicate binary blobs.

Do not invent a second upload/storage subsystem.

Product != Offer.

No Price/Stock fields.

Raw AgGridReact forbidden.

If a grid is needed, only Tooba canonical AppDataGrid.

Goal

Implement real Product Workspace رسانه tab:

Product gallery

primary image

deterministic order

add/remove/reorder

optional localized alt/caption where architecture supports it

variant-specific media only if existing model supports it cleanly

media readiness

1 — Media Audit

Inspect:

existing Media module

existing upload/select APIs

existing Product media relationships

Shopeiva PDP gallery expectations

Reuse existing infrastructure.

2 — Product Media Assignment

Use/complete a Catalog-owned assignment model, conceptually:

ProductId

MediaAssetId

SortOrder

IsPrimary

VariantId? only if safe/existing

contextual alt/caption if needed

Do not duplicate media bytes.

3 — Primary Image

Exactly one primary image when media exists.

Rules:

first image may auto-become primary

user can set another primary

backend enforces uniqueness

removing primary follows a deterministic fallback policy

UI label:
تصویر اصلی

4 — Ordering

Gallery order must be deterministic.

Allow:

drag/drop on desktop if clean

accessible up/down alternative

touch-friendly mobile behavior

Do not make user type SortOrder numbers.

5 — VIEW Mode

Show:

main image

gallery thumbnails

count

readiness

variant badge if relevant

No mutation controls.

6 — EDIT Mode

Authorized user can:

add existing media

upload through current Media flow if available

reorder

set primary

remove Product assignment

optionally associate to Variant if supported

Save/Cancel or safe immediate actions consistent with existing Tooba form mode

7 — Media Selection / Upload

Prefer existing:

انتخاب از رسانه‌ها

بارگذاری رسانه

If async upload exists:

progress

failure/retry

no ghost assignments

no duplicate submission

8 — Alt Text

If alt/caption is Product-contextual, store on assignment/presentation data, not shared global asset metadata.

Do not overwrite a shared asset's global metadata just for one Product.

9 — Variant Media

If current model supports variant media:

allow optional assignment to canonical Variant

Product-level gallery remains default

variant media can override/add when variant selected

If not supported:

keep Product-level gallery only

document extension point

do not force fragile schema

10 — Shopeiva Compatibility

Data must support future Shopeiva PDP gallery:

primary

ordered gallery

thumbnails

variant media if supported

Do not redesign PDP here.

11 — Readiness

Provide:

HasPrimaryImage

MediaCount

IsReady

Human UI:

تصویر اصلی تعیین نشده

۳ رسانه

رسانه کامل است

T016 publishing will consume readiness.

12 — Delete vs Unassign

Removing media from Product:

removes assignment only

does NOT hard-delete shared MediaAsset

Asset lifecycle remains Media module concern.

13 — Authorization

Use SpiceDB-centered authorization:

view Product media

edit Product media

Backend authoritative.

14 — API Contracts

Provide/use clean contracts for:

get Product media

add/assign

reorder

set primary

remove assignment

readiness

variant association if supported

No direct Media persistence joins if module boundary forbids them.

15 — Persistence

Migration only if needed.

Enforce:

deterministic order

one primary

unique Product + MediaAsset assignment where appropriate

safe Variant relation within same Product if supported

No cross-module FK that hurts extraction readiness.

16 — PLP/PDP Handoff

After T014:

Product list may use primary thumbnail

PLP may use primary media

PDP may consume ordered gallery

Do not create duplicate media stores.

17 — Tests

Backend:

add assignment

first/explicit primary

primary uniqueness

reorder

remove

unassign does not delete shared asset

readiness

variant association safety if supported

no Product.Price/Stock

Frontend:

VIEW

EDIT

add

upload/select path

reorder

set primary

remove

view-only

no raw IDs

mobile

accessibility basics

18 — Documentation

Create/update:

docs/catalog/PRODUCT-MEDIA.md

docs/catalog/PRODUCT-CATALOG-ADMIN.md

recovery/project-state docs

19 — Validation

Backend:

restore

build

full tests

Frontend:

typecheck

lint

tests

production build

Require:

0 errors

0 failed

0 skipped

no new warnings

Always:

git diff --check

20 — Runtime

Keep alive:

Backend :5088

Frontend :3000

Shopeiva :3001

21 — Git

Commit + push.

Require:

HEAD == origin/main

tracked tree clean

unrelated user changes preserved

Acceptance

PASS only if:

Product رسانه tab is real

existing Media infrastructure reused

assignment separated from Media asset storage

primary image supported

ordering deterministic

add/remove/reorder work

unassign never hard-deletes shared asset

VIEW / EDIT preserved

no raw IDs

variant media safe or explicitly deferred

readiness exists

PLP/PDP handoff ready

Product != Offer preserved

no Product.Price/Product.Stock

no raw AgGridReact

validation green

runtime alive

Git synchronized

USER_VISUAL_ACCEPTED remains NO until manual review.

Result Contract

Return ONLY:

PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_WORKER_RESULT

Task-ID:
TB-P07-T014
Parent-Task:
TB-P07-T013
Channel:
tooba-main
Status:
PASS | FAIL | BLOCKED | RECOVERY_CONFLICT

Summary:
...

Media-Audit:

existing Media module:

reused upload/select flow:

new subsystem created:

Shopeiva gallery compatibility:

Media-Model:

assignment:

primary:

ordering:

variant association:

alt/caption:

migration:

Media-UX:

VIEW:

EDIT:

add:

upload/select:

reorder:

set primary:

remove:

mobile:

accessibility:

Readiness:

HasPrimaryImage:

MediaCount:

IsReady:

Boundary:

Catalog stores binary:

Media ownership:

unassign deletes asset:

direct Media persistence join:

Product != Offer:

Product.Price:

Product.Stock:

API:

get:

add:

reorder:

primary:

remove:

readiness:

gaps:

Validation:

backend build/tests:

frontend typecheck/lint/tests/build:

failed:

skipped:

warnings:

git diff --check:

Documentation:

PRODUCT-MEDIA:

PRODUCT-CATALOG-ADMIN:

recovery/state:

Runtime:

Backend:

Frontend:

Shopeiva:

kept alive:

Git:

branch:

commit:

push:

final HEAD:

origin/main:

synchronized:

tracked tree:

unrelated user changes:

Architectural-Concerns:
...

Visual-Concerns:
...

Blockers:
...

END_TOOBA_WORKER_RESULT

After Result:

Worker -> IDLE / Waiting

keep runtimes alive

do not invent next task

do not add extra prose

END_TASK
