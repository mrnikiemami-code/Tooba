PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

TASK-ID: TB-P07-T005
PARENT-TASK: TB-P07-T004
PROJECT: Tooba
PHASE: P07
CHANNEL: tooba-main
STATUS: ISSUED
TASK-TYPE: CATEGORY ADMIN UI — APP CATEGORY TREE + WORKSPACE SHELL
WORKER-POLICY: ONE WORKER = ONE ACTIVE TASK

Architect Acceptance of T004

TB-P07-T004 is ACCEPTED.

Canonical baseline:

CatalogCategory in Catalog module

ParentCategoryId adjacency

statuses Draft / Published / Archived

SortOrder + IsVisible

CatalogCategoryTranslation

locale-specific slug with unique (Locale, Slug)

slug history + route resolver

whole-tree Admin query

move/reorder contracts

migration 20260828172727_AddCategoryFoundation applied

backend tests green

HEAD == origin/main at e03f98488f5f9e46c000cd00930cc4a4426ee552

Do NOT reopen T004 architecture without a real blocker.

Locked UX Principle

UI/UX MUST be understandable for an ordinary non-technical user without training.

Rules:

human labels, no technical IDs in normal UI

obvious primary actions

progressive disclosure

preserve context

minimize cognitive load

destructive actions must be safe

professional visual quality is mandatory

Locked Visual Contract

Use the approved reference image:
docs/evidence/TB-P07-T004/visual-contract-admin-categories.png

This image is a LOCKED VISUAL CONTRACT.

Do NOT redesign it.
Do NOT replace it with generic Ant Design styling.
Do NOT build a CRUD-looking page.

Desktop:

Tree panel

selected Category Workspace panel

polished RTL

compact but breathable

Tooba visual language

Mobile:

Tree/List first

select category

Workspace full page

clear back navigation

Canonical Component Locks
Grid

If any grid is needed, USE ONLY:
src/frontend/design-system/app-data-grid/AppDataGrid.tsx

This is the customized Tooba canonical grid.

FORBIDDEN:

raw AgGridReact

new grid wrapper

new standalone table

page-specific duplicate grid CSS

Tree

Build AppCategoryTree as a Tooba-owned wrapper over Ant Design Tree.
Do NOT leak Ant Tree types/props into feature/page/domain code.

Goal

Build the first real Admin Category experience using the T004 backend foundation.

Routes:
/{locale}/admin/catalog/categories
/{locale}/admin/catalog/categories/{categoryId}
/{locale}/admin/catalog/categories/{categoryId}/{tab}

User must be able to:

understand hierarchy immediately

search categories

expand/collapse

select/open category

create root category

create child category

move/reorder safely

preserve selected category in URL

refresh/back/forward without losing context

see clean Category Workspace shell

1 — AppCategoryTree

Required:

Ant Design Tree underneath

controlled expanded/selected state

Tooba custom node renderer

RTL/LTR

search integration

drag/drop

keyboard accessible

touch-friendly

virtual scrolling where safe

loading/empty/error states

Node:

chevron

category/folder icon

localized name

subtle status

separate drag handle

optional actions menu

Interaction contract:
Chevron -> expand/collapse only
Category name -> select/open workspace
Drag handle -> move/reorder
Actions menu -> secondary actions

2 — Tree API

Use:
GET /v1/admin/catalog/categories/tree?locale=...

Use CategoryTreeNodeDto.

No recursive N+1 loading.

3 — Tree Search

search localized names

highlight matches

auto-expand ancestors

clear restores usable context

no UUID/technical keys

local search if whole tree already loaded

4 — Create Root Category

Primary CTA:

دسته‌بندی جدید

Initial fields only:

Name active locale

Parent = root

Status = Draft by default

Slug auto-generated but editable

Save

Do NOT show SEO/attributes/facets/mega-menu on first create.

After save:

refresh tree

select new category

navigate to workspace

5 — Create Child

Node action:
افزودن زیرمجموعه

Parent preselected.
After create: expand parent + select child.

6 — Move / Reorder

Use T004 MoveCategory/ReorderCategorySiblings contracts.

UX:

before/inside/after drop indicator

invalid drop visibly rejected

self/descendant move blocked

rollback on failure

localized success/error feedback

no accidental move from title click

7 — Workspace Shell

Desktop:
Tree remains visible while workspace opens beside it.

Header:

localized name

breadcrumb/path

status

storefront route/preview if available

Prepare tabs:

عمومی

ترجمه‌ها

ویژگی‌ها

فیلترها

مگامنو

محصولات

SEO

تنظیمات

تاریخچه

T005 MUST NOT fake future functionality.

Implement:

shell/navigation

General summary

Translation status/readiness

8 — General Summary

Show clean summary:

Parent

Status

Sort order

IsVisible

current locale Name

current locale Slug

image/icon preview

translation coverage

children count

product count only if efficient

storefront route

No dense debug table.

9 — Translation Awareness

Do NOT duplicate categories per language.

Show simple status:
فارسی: کامل
English: ناقص
العربية: ایجاد نشده

10 — Deep Link

Required:

click category -> URL updates

refresh -> same selected category

Back/Forward works

ancestors auto-expand

invalid/deleted id handled cleanly

11 — Ordinary-User UX

Avoid:

giant forms

UUIDs

raw backend enums

raw permission names

nested modal chains

too many actions at once

Use:

concise labels

obvious hierarchy

familiar icons

predictable actions

progressive disclosure

strong empty/loading/error states

12 — Styling

Generic Tree/Workspace styling belongs in reusable design-system components.
Ant default skin must not dominate.

13 — Accessibility

keyboard navigation

visible focus

accessible labels

icon-only actions need tooltip + aria-label

touch targets ~40–44px

adequate contrast

sane RTL behavior

14 — Mobile

Do NOT squeeze split-pane onto mobile.

Mobile:
Tree/List -> select category -> full-page Workspace
with Back to Categories and compact sticky header.

15 — Error / Empty States

Handle:

no categories

no search results

failed tree load

failed summary

failed move/reorder

invalid route id

archived category

missing translation

Empty CTA:
اولین دسته‌بندی را ایجاد کنید

16 — API Integration

Use real T004 APIs.
No fake production tree.

Required:

tree

category summary

create

move

reorder

Only narrow backend changes if a real UI blocker exists.

17 — Canonical AppDataGrid

If Products preview/list appears:
USE ONLY src/frontend/design-system/app-data-grid/AppDataGrid.tsx

No raw AG Grid.
No new grid CSS.

18 — Tests

Frontend:

tree render

RTL

search

expand/collapse

title select

chevron behavior

drag handle separation

valid/invalid move

root create

child create

deep link

refresh selection

navigation logic

translation status

empty/error

mobile

accessibility basics

Backend only if touched.

19 — Validation

Frontend:

typecheck

lint

tests

production build

Backend if touched:

restore

build

full tests

Require:

0 errors

0 failed

0 skipped

no new warnings

Always:

git diff --check

20 — Runtime

Keep alive:
Backend :5088
Frontend :3000
Shopeiva :3001

Preview:
http://localhost:3000/fa/admin/catalog/categories

21 — Git

Commit + push.

Require:

HEAD == origin/main

tracked tree clean

unrelated user changes preserved

22 — Acceptance

PASS only if:

visual reference preserved

ordinary user can understand the page without training

AppCategoryTree wraps Ant Tree

Ant API does not leak into page/domain code

tree search works

expand/select/drag are distinct

root/child creation simple

move/reorder safe

deep link works

refresh/back/forward work

desktop Tree + Workspace composition works

mobile full-page Workspace works

translation status is understandable

no raw technical IDs

no raw AgGridReact

customized Tooba AppDataGrid remains canonical

no page-specific generic grid CSS

no fake future-tab functionality

validation green

runtime alive

Git synchronized

USER_VISUAL_ACCEPTED remains NO until manual user review.

Result Contract

Return ONLY:

PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_WORKER_RESULT

Task-ID:
TB-P07-T005
Parent-Task:
TB-P07-T004
Channel:
tooba-main
Status:
PASS | FAIL | BLOCKED | RECOVERY_CONFLICT

Summary:
...

Visual-Contract:

reference image:

redesign performed:

composition matched:

ordinary-user UX:

AppCategoryTree:

path:

Ant Tree version:

wrapper:

raw Ant API leakage:

RTL:

LTR:

search:

virtual scroll:

expand:

select:

drag handle:

move:

reorder:

invalid drop:

accessibility:

mobile:

Create-Flow:

root category:

child category:

progressive disclosure:

post-create selection:

Workspace:

route:

selected category:

breadcrumb:

status:

general summary:

translation status:

tabs:

deep link:

refresh:

back/forward:

invalid id:

mobile:

Canonical-AppDataGrid:

raw AgGridReact used:

Tooba AppDataGrid reused:

page-specific grid CSS introduced:

API:

tree:

category summary:

create:

move:

reorder:

backend touched:

gaps:

Validation:

frontend typecheck:

frontend lint:

frontend tests:

frontend build:

backend restore:

backend build:

backend full tests:

failed:

skipped:

warnings:

new warnings:

git diff --check:

Runtime:

Backend:

Frontend:

Shopeiva:

Category Admin:

kept alive:

Git:

branch:

commit:

push:

final HEAD:

origin/main:

synchronized:

tracked tree:

unrelated user changes:

Visual-Concerns:
...

Architectural-Concerns:
...

Blockers:
...

END_TOOBA_WORKER_RESULT

After Result:

Worker -> IDLE / Waiting

keep runtimes alive

do not invent T006

do not add extra prose