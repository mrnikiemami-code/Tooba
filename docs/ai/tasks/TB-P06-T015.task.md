PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

TASK-ID: TB-P06-T015
PROJECT: Tooba
PHASE: P06
CHANNEL: tooba-main
STATUS: ISSUED
TASK-TYPE: PAGE COMPOSITION + ADMIN LANDING MANAGEMENT + LIVE STOREFRONT
WORKER-POLICY: ONE WORKER = ONE ACTIVE TASK

Title

Landing Page Composition End-to-End — Admin-Controlled Shopeiva Sections with Live Data and Zero Visual/Interaction Drift

Architect Context

Architect ACCEPTED:

TB-P06-T014 = ACCEPTED
P06 = IN_PROGRESS

Current commercial priority remains:

Storefront must be sellable/presentable.

Customer Panel must be usable.

Seller Panel must be usable.

Admin Panel must be professional.

Blog/Content must be live.

UI must never drift away from Shopeiva.

The user specifically requires the landing/home composition to be configurable without breaking the purchased template.

Architectural Boundary

Content != Page Composition

Content owns semantic content such as:

Article

Editorial content

FAQ

SEO metadata

publishing lifecycle

Page Composition owns:

which approved Section appears

section order

visibility

approved variant

data source/configuration

Page Composition MUST NOT become a free-form page builder.

ABSOLUTE VISUAL CONTRACT

Shopeiva is a LOCKED UI/interaction contract.

Admin may configure composition, but MUST NOT be allowed to arbitrarily edit:

CSS

classes

spacing

typography

border radius

shadow

animation

transition

carousel implementation

autoplay behavior

hover/focus/active

responsive behavior

arbitrary HTML/JS

arbitrary Tailwind classes

arbitrary component geometry

Required model:

Approved Shopeiva Section Catalog
-> safe configuration
-> live backend bindings
-> ordered composition
-> runtime render

NOT:

free-form builder
-> arbitrary CSS/HTML
-> visual drift

Native-Fit Rule

If Tooba needs a section capability not represented exactly in Shopeiva:

Find the closest real Shopeiva source components.

Compose only from those patterns.

Use existing Tooba design tokens.

The result must look as if Shopeiva originally shipped it.

If it looks appended or foreign:
VISUAL REGRESSION.

Repository Recovery

Run:

git rev-parse --show-toplevel
git fetch origin
git branch --show-current
git rev-parse HEAD
git rev-parse origin/main
git status --short --branch

Expected predecessor:

3dd9d60c9abac0a8b6878013242d6899be61506e

Require:

main

HEAD == origin/main

safe tracked tree

If conflict:
RECOVERY_CONFLICT

A — Start Runtimes Before Work

Start:

PostgreSQL

Tooba Backend

Tooba Frontend

Original Shopeiva

Verify:

health/live = 200

health/ready = 200

Tooba Home = 200

Shopeiva Home = 200

Admin = reachable

Create:

docs/evidence/TB-P06-T015/01-runtime-before-composition.md

B — Inspect Actual Shopeiva Home Source FIRST

Map the real Home sections from Shopeiva source.

At minimum inspect all sections actually present in the purchased template, including where applicable:

Hero

promotional banner/slider

category blocks

featured categories

best sellers

newest products

product rails

brands

editorial/article sections

customer reviews/testimonials

promotional cards

service/value propositions

any source-native carousel/slider blocks

For each section record:

source file

component

DOM/JSX structure

CSS/Tailwind

JS behavior

carousel dependency

autoplay

transition

animation

hover behavior

responsive behavior

required data shape

Create:

docs/evidence/TB-P06-T015/02-shopeiva-section-catalog-source-map.md

C — Canonical Section Catalog

Create a strongly controlled Section Catalog.

Each section type must have a stable identifier, for example conceptually:

HERO
CATEGORY_GRID
BEST_SELLERS
NEWEST_PRODUCTS
PRODUCT_RAIL
BRANDS
LATEST_ARTICLES
CUSTOMER_REVIEWS
PROMO_BANNER

Use actual repository naming conventions.

Catalog entry must define:

Type

approved renderer

allowed variants

allowed configuration schema

allowed data source configuration

whether localization is supported

whether section may be repeated

safe defaults

Do NOT persist React component names as arbitrary user-controlled executable input.

Create:

03-section-catalog.md

D — Page Composition Domain

Create/verify a proper Page Composition owner.

Minimum concepts:

PageDefinition

PageSection

PageKey/RouteKey

SectionType

DisplayOrder

IsVisible

Variant

Configuration

Locale scope where appropriate

Tenant scope

Version/concurrency token if repository conventions support it

For MVP, support Home/Landing first.

Do NOT mix Article/Content body ownership into Page Composition.

No cross-module SQL JOIN.

Create:

04-page-composition-domain.md

E — Safe Configuration Model

Configuration must be typed/validated.

Examples:

Hero:

approved media reference

approved content reference

CTA target

approved variant

Product Rail:

title

source kind

category/filter/reference

item count within safe bounds

approved variant

Brands:

title

data source

item count

Articles:

title

category/source

item count

Forbidden configuration:

arbitrary CSS

arbitrary HTML

arbitrary JS

arbitrary className

arbitrary animation code

arbitrary SQL/query text

Create:

05-safe-section-configuration.md

F — Live Data Source Boundaries

Section data must come from correct domain owners through application contracts/APIs.

Examples:

Product sections:
Catalog/Search/Pricing/Offer boundaries as appropriate.

Brands:
canonical Catalog/Brand owner.

Articles:
Content module.

Reviews:
canonical Reviews owner.

No cross-module SQL JOIN.

No static demo arrays in production-visible UI.

Create:

06-section-data-source-boundaries.md

G — Tenant / Edition Isolation

Single-Store:

per-tenant composition

resolved through canonical tenant/domain mapping

same publish

tenant-specific landing order/visibility/config allowed

Marketplace:

marketplace composition

isolated from Single-Store tenants

No tenant leakage.

Create:

07-page-composition-edition-isolation.md

H — Locale / Multilingual Behavior

Composition and content localization must remain distinct.

Allow safe locale-specific overrides only where necessary, such as:

localized section title

locale-specific hero content

locale-specific article/category source

But do not conflate:

Locale

Market

Currency

RTL/LTR rendering must continue using canonical app direction system.

Create:

08-page-composition-i18n.md

I — Admin Composition APIs

Create live Admin APIs:

get Home composition

list available section types/variants

add approved section

reorder

show/hide

update typed configuration

remove where allowed

restore default composition

preview/draft capability if already practical within current architecture

Authorization:
SpiceDB/Admin use-case boundary.

No arbitrary payload execution.

Create:

09-admin-composition-api.md

J — Admin Page Composition UI LIVE

Build inside accepted Tooba Admin shell.

The UI must feel native to Shopeiva/Tooba Admin.

Admin should be able to:

see current Home sections in order

enable/disable

reorder

choose approved variant

edit safe configuration

inspect section type

restore default

save changes

see validation errors

Reorder UX:

use an existing source-derived interaction pattern

if drag/drop is added, it must be professional, keyboard-safe where practical, and visually native

do not introduce a visually foreign library default

No arbitrary CSS editor.
No HTML editor.
No JS editor.

Create:

10-admin-page-composition-ui.md

K — Home Runtime Renderer

Refactor/verify Home so composition order comes from Page Composition.

Critical rule:

The renderer for each Section MUST reuse the existing accepted Shopeiva-derived Home components.

Do NOT rewrite accepted Home sections.

The job is:

composition
-> select existing approved renderer
-> bind live data
-> render in configured order

Preserve all:

CSS

carousel

autoplay

animation

transitions

hover/focus

responsive behavior

Create:

11-home-composition-runtime.md

L — Default Composition

Create a canonical default that reproduces the currently accepted Home ordering/behavior.

First migration/deployment must NOT visually scramble Home.

Default composition should map exactly to the accepted current Home sections.

Create:

12-default-composition.md

M — Empty / Invalid / Missing Section Safety

Required:

unknown SectionType fails safely

invalid config does not crash Home

missing data source produces source-faithful empty/hide behavior

disabled section does not render

invalid section cannot inject markup/script

failed one-section data source should not necessarily kill entire Home if architecture supports isolated failure

Create:

13-composition-runtime-safety.md

N — SEO Safety

Page Composition must not break:

Home title

description

canonical

structured data

locale handling

heading hierarchy

crawlability

Do not allow Admin composition to create arbitrary duplicate H1 unless Section Catalog explicitly controls heading roles.

Create:

14-composition-seo-safety.md

O — Cache / Runtime Refresh

Composition changes should become visible safely without deployment.

Implement/verify:

cache invalidation or bounded refresh

tenant-aware cache keys

locale-aware where necessary

no stale cross-tenant composition

no restart required for ordinary composition edit

Create:

15-composition-cache-refresh.md

P — Browser-Driven End-to-End Proof

Create a legitimate runtime proof:

capture current Home

in Admin change section order

save

reload Home

prove new order

hide one section

prove hidden

re-enable/restore default

prove original accepted order restored

Use real runtime and API.

No direct DB manipulation.

Create:

16-composition-e2e-proof.md

Q — Shopeiva Visual / Motion Proof

For at least:

Hero

one product carousel/rail

Brands or Categories

Articles

Reviews/Testimonial if present

Compare Original Shopeiva vs Tooba after composition-driven rendering.

Prove:

CSS unchanged

JS behavior unchanged

carousel implementation unchanged

autoplay unchanged

transitions unchanged

animation unchanged

hover/focus unchanged

responsive unchanged

Desktop + mobile.

Create:

17-visual-motion-parity.md

R — Admin Native-Fit Proof

Capture:

composition list/editor

reorder state

edit/config state

validation state

mobile/responsive state

Document which Shopeiva/accepted Admin source patterns each UI element derives from.

Create:

18-admin-native-fit-proof.md

S — No Fake Data / Unsafe Builder Audit

Confirm:

no fake section stats

no fake product arrays

no fake article arrays

no arbitrary HTML

no arbitrary CSS

no arbitrary JS

no arbitrary class injection

no raw SQL/query builder

no direct DB cross-module reads

Create:

19-composition-safety-audit.md

T — Tests

Backend integration tests at minimum:

get default Home composition

add approved section

reject unknown section type

reorder

hide/show

typed config validation

restore default

Single-Store tenant isolation

Marketplace isolation

unauthorized Admin denied

no arbitrary config execution

live data contract mapping

Frontend tests at minimum:

runtime renders configured order

hidden section absent

existing Home renderer reused

carousel/motion guard preserved

RTL rendering

LTR rendering

Admin reorder/save

invalid config displays error

Create:

20-composition-tests.md

U — Commercial UI Readiness Re-Check

Re-check the T014 matrix after this capability.

Confirm no regression to:

Storefront

Customer

Seller

Admin

Blog

Update estimates honestly.

Do NOT claim 100% merely because composition is live.

Create:

21-readiness-recheck.md

V — Validation

Backend:

dotnet restore src/backend/Tooba.slnx
dotnet build src/backend/Tooba.slnx
dotnet test src/backend/Tooba.slnx

Require:

warnings 0

errors 0

failed 0

skipped 0

Frontend:

cd src/frontend
npm run typecheck
npm run lint
npm run test
npm run test
npm run test
npm run test
npm run build

Run:

admin tests

content tests

composition tests

i18n tests

All PASS.

Always:

git diff --check

Create:

22-final-validation.md

W — Final Runtime

After validation keep running:

Backend

Tooba Frontend

Original Shopeiva

Verify:

health/live 200

health/ready 200

Home 200

Admin Page Composition route

Blog 200

Customer Dashboard 200/auth flow

Seller Dashboard 200/auth flow

Admin Dashboard 200/auth flow

Create:

23-final-runtime.md

X — User Preview URLs

Return exact URLs for:

Tooba Home

Admin Page Composition

Blog

Customer Dashboard

Seller Dashboard

Admin Dashboard

Original Shopeiva Home

Also provide a short preview instruction:

which Admin change to make

which Home result to observe

No secrets.

Y — Source of Truth

Update:

P06 = IN_PROGRESS
TB-P06-T014 = ACCEPTED
TB-P06-T015 = AWAITING_ARCHITECT_ACCEPT

PAGE_COMPOSITION_BACKEND = LIVE
HOME_COMPOSITION = LIVE
ADMIN_PAGE_COMPOSITION = LIVE
SECTION_CATALOG = LOCKED_APPROVED_COMPONENTS
FREE_FORM_PAGE_BUILDER = FORBIDDEN

STOREFRONT_UI = LIVE_AUDITED
CUSTOMER_UI = LIVE_AUDITED
SELLER_UI = LIVE_AUDITED
ADMIN_UI = LIVE_AUDITED
BLOG_CONTENT = LIVE

VISUAL_CONTRACT = SHOPEIVA_LOCKED
NEW_UI_RULE = SOURCE_DERIVED_NATIVE_FIT

Update:

docs/PROJECT-STATE.md

docs/ROADMAP.md

docs/ai/TOOBA-RECOVERY-CONTEXT.md

docs/prompts/START-HERE-IF-CHATGPT-IS-LOST.md

Evidence

Create:

docs/evidence/TB-P06-T015/

Required:
01-runtime-before-composition.md
02-shopeiva-section-catalog-source-map.md
03-section-catalog.md
04-page-composition-domain.md
05-safe-section-configuration.md
06-section-data-source-boundaries.md
07-page-composition-edition-isolation.md
08-page-composition-i18n.md
09-admin-composition-api.md
10-admin-page-composition-ui.md
11-home-composition-runtime.md
12-default-composition.md
13-composition-runtime-safety.md
14-composition-seo-safety.md
15-composition-cache-refresh.md
16-composition-e2e-proof.md
17-visual-motion-parity.md
18-admin-native-fit-proof.md
19-composition-safety-audit.md
20-composition-tests.md
21-readiness-recheck.md
22-final-validation.md
23-final-runtime.md

Acceptance Criteria

PASS requires ALL:

actual Shopeiva Home source inspected

approved Section Catalog implemented

no free-form builder

typed safe configuration

Admin composition API live

Admin composition UI live

Home order driven by composition

existing Shopeiva-derived renderers reused

default composition preserves accepted Home

hide/show works

reorder works

restore default works

runtime change requires no deployment

tenant isolation

locale/RTL/LTR safe

no arbitrary CSS/HTML/JS/class injection

no cross-module SQL JOIN

CSS preserved

JS preserved

carousel preserved

autoplay preserved

animation preserved

transitions preserved

responsive preserved

browser E2E proof

visual side-by-side proof

Admin native-fit proof

backend 0/0/0/0

frontend all green

runtime healthy

repo synchronized

Worker may report:

PAGE_COMPOSITION_LIVE

Worker must NOT report:

FREE_FORM_BUILDER
PRODUCT_FULLY_READY
USER_VISUAL_ACCEPTED

Git

git add ...
git commit -m "feat add controlled landing page composition [TB-P06-T015]"
git push origin main
git fetch origin
git rev-parse HEAD
git rev-parse origin/main
git status --short --branch

Require:
HEAD == origin/main
tracked tree clean

Result Contract

PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_WORKER_RESULT

Task-ID:
TB-P06-T015

Channel:
tooba-main

Status:
PASS | FAIL | BLOCKED | RECOVERY_CONFLICT

Summary:
...

Shopeiva-Source:

Home files:

Section components:

carousel/motion dependencies:

Section-Catalog:

types:

variants:

safe config:

arbitrary CSS/HTML/JS:

unknown type behavior:

Page-Composition:

owner:

PageDefinition:

PageSection:

order:

visibility:

tenant:

locale:

concurrency:

Data-Sources:

Products:

Brands:

Articles:

Reviews:

cross-module SQL:

Admin:

API:

UI:

reorder:

show/hide:

variants:

config:

restore default:

validation:

native-fit:

Home:

runtime composition:

existing renderers reused:

default order:

runtime refresh:

cache:

visual drift:

Multilingual:

RTL:

LTR:

locale overrides:

locale/market/currency separation:

SEO:

canonical:

heading hierarchy:

metadata:

structured data:

Safety:

arbitrary HTML:

arbitrary CSS:

arbitrary JS:

arbitrary class injection:

fake data:

E2E:

reordered:

hidden:

restored:

direct DB mutation:

browser proof:

Visual-Fidelity:

CSS:

JS:

carousel:

autoplay:

animation:

transition:

hover/focus:

responsive:

Original Shopeiva comparison:

unauthorized deviation:

Tests:

default:

add:

unknown type:

reorder:

hide/show:

validation:

restore:

tenant isolation:

marketplace:

auth:

runtime order:

hidden:

visual guards:

RTL:

LTR:

admin save:

Validation:

backend restore:

backend build:

backend tests:

warnings:

errors:

failed:

skipped:

typecheck:

lint:

critical-storefront:

storefront:

customer:

seller:

admin:

content:

composition:

i18n:

frontend build:

git diff --check:

Runtime:

Backend:

Tooba Frontend:

Shopeiva:

health/live:

health/ready:

USER-PREVIEW:

Home:

Admin Page Composition:

Blog:

Customer Dashboard:

Seller Dashboard:

Admin Dashboard:

Original Shopeiva Home:

preview steps:

Readiness-After:

Storefront %:

Customer %:

Seller %:

Admin %:

Blog %:

sale/demo blockers:

next largest gaps:

Source-of-Truth:

P06:

T014:

T015:

page composition:

Home composition:

Admin composition:

free-form builder:

visual contract:

new UI rule:

Git:

commit:

push:

final HEAD:

origin/main:

synchronized:

tracked tree:

Architectural-Concerns:
...

Visual-Concerns:
...

Blockers:
...

END_TOOBA_WORKER_RESULT

After Result:

return to IDLE

do not self-issue another Task

keep Backend + Tooba Frontend + Original Shopeiva running where technically possible

END_TASK