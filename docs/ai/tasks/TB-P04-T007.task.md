# Tooba — TB-P04-T007 — Shopeiva Storefront Live Integration — Home / Listing / PDP

Captured from Architect chat overlay text (not Download).

BEGIN_TOOBA_CURSOR_TASK_V1

Protocol-Version: 1
Task-ID: TB-P04-T007
Phase: P04 — Experience Foundation
Type: Presentation-First Live Vertical Slice
Repository: https://github.com/mrnikiemami-code/Tooba
Branch: main
Execution-Mode: PIPELINE

Architect Decision / Locked Delivery Strategy

TB-P04-T006 is ACCEPTED.

The runtime Shopeiva Visual Atlas is now the UI source of truth for the initial sellable experience.

Locked rule:

PRESERVE SHOPEIVA FIRST
→ make the minimum UI changes
→ replace template/demo truth with real Tooba API truth
→ keep Tooba architecture boundaries
→ reach sellable product quickly
→ enhance multilingual/LTR/multi-currency UI later

Do NOT redesign Shopeiva.
Do NOT invent a new Storefront visual language.
Do NOT refactor the whole template.

Commercial objective:

Shopeiva beauty
+
Tooba backend
=
presentable/sellable live product
Objective

Integrate Shopeiva visual structure into Tooba frontend for the first real Storefront slice:

1. Storefront shell/header/footer
2. Home page product/category merchandising
3. Product listing / category-style listing
4. Product Detail Page

All displayed business data on accepted live routes must come from real Tooba APIs/backend composition.

No fixture/demo JSON may be accepted business truth.

Repository Recovery

Run:

git rev-parse --show-toplevel
git fetch origin
git branch --show-current
git rev-parse HEAD
git rev-parse origin/main
git status --short --branch

Expected predecessor:

f3d420c87cfd48fbf332f7b7ac418ad2536a541e

Require branch main, HEAD == origin/main, safe/known working tree.

No force push, destructive reset, silent stash, or history rewrite.

Shopeiva Reference

Use:

docs/evidence/TB-P04-T006/

including the runtime atlas and reuse blueprint.

A previously resolved external reference path was:

D:\\Users\\User\\source\\repos\\SarvNewVerRequirment\\reference\\shopeiva

Do NOT hard-code this machine-specific path. Discover safely on the current machine.

Do not copy the entire Shopeiva repository wholesale. Bring across only selected components/assets needed by this slice.

MINIMUM-CHANGE RULE — CRITICAL

For every visual component ask:

Can the existing Shopeiva component/layout be preserved?

If YES, preserve it.

Only change what is necessary for:

real Tooba data
routing compatibility
architecture correctness
accessibility blocker
theme color token
responsive integration

The result must still look recognizably like Shopeiva.

Initial Presentation Scope

For this first sellable integration:

primary language = Persian
primary direction = RTL

Do not block delivery on full multilingual/LTR UI.

Backend/domain multi-currency architecture remains intact, but advanced currency-switching UX is deferred.

Do not create Product.Price.

Approved Theme Direction

Apply only a controlled token-level shift from Shopeiva red to the approved professional blue.

Use central semantic theme values.

Preserve semantic success/warning/danger colors.

Do NOT change layout/composition because of the color change.

Route 1 — Storefront Home

Adapt Shopeiva Home with minimum visual change.

Preserve where practical:

header
search position
category navigation
hero/promo composition
category cards
product card family
deal/offer sections
brand/content sections
footer/trust/newsletter
responsive behavior

Minimum live sections:

header
category navigation seam
hero/promotional composition
real product section
real product cards
footer/trust shell

Static presentational banners may remain if no Content/Campaign backend exists yet, but product/business truth must never be fake.

Route 2 — Product Listing

Implement one real listing route using Shopeiva listing visual language.

Minimum live data:

product image/media seam
localized title
category
resolved display price
promotion if real
availability
published/sellable filtering
link to PDP

No Admin-only state.
No raw UUIDs.

Product Cards

Reuse/adapt Shopeiva product card composition.

Map real Tooba truth:

Catalog Product
→ Variant context
→ Seller Offer(s)
→ Pricing
→ Promotion
→ Tax presentation
→ Inventory availability

For Marketplace, select the visible offer through an application/storefront resolver.

Forbidden:

first-row-wins
frontend price guessing
frontend stock guessing
Product.Price
Product.Stock

If canonical Buy Box does not yet exist, create a narrow application-level resolver seam and document its temporary deterministic rule.

Route 3 — Product Detail Page

Adapt Shopeiva PDP substantially as-is.

Preserve patterns such as:

breadcrumbs
gallery
product identity
rating/review seam
variant selection
price block
availability
quantity
primary add-to-cart seam
trust chips
description/specification tabs
seller information

Use real Tooba backend truth.

For multiple Seller Offers:

show one primary/resolved offer
show a clear other-sellers / seller-offer seam

Do not build a large seller comparison feature yet.

Must preserve:

Product != Variant != Offer != Price != Inventory
Seller / Offer Presentation

Use human-readable seller names.

Never expose raw PartyId/OfferId as primary UX.

Minimum PDP seller information:

seller
price
availability
seller SKU secondary if useful
Media

Use real Tooba media where available.

If the current live Catalog lacks enough media, use a bounded development presentation-image seam without making image URL Product business truth.

Document limitation.

Header / Search

Preserve Shopeiva header composition.

Do not implement Fuse/demo client-side business truth.

Use a real narrow search/listing route if supported; otherwise keep a bounded working seam and document it.

Category Navigation

Preserve Shopeiva category UI.

Use real Catalog categories where available.

If public category API is insufficient, add only the narrowest proper read contract/API needed.

No frontend DB access.
No cross-module SQL join.

Footer / Static Presentation

Reuse/adapt Shopeiva footer.

Do not build CMS in this task.

Static legal/contact/trust presentation may remain initially if not falsely claiming unsupported services.

Cart Boundary

Full Cart integration is NOT required in this task.

The PDP CTA must be architecturally ready for the next task.

Do not fake successful cart mutation using Shopeiva local/demo state.

If existing Tooba Cart API can be wired safely within bounded scope, it may be connected; otherwise document the explicit next boundary.

SEO — Minimum Sellable Baseline

For public routes implement/preserve:

server-rendered content where practical
title
description
canonical
indexability rules
semantic headings
Product structured-data seam

Do not attempt full SEO completion.

Public API / Read Model Principle

Frontend consumes public/application contracts.

Allowed composition:

Catalog
Offer
Pricing
Promotion
Tax
Inventory
Party/Seller display

Forbidden:

cross-module SQL join
frontend direct DB
frontend domain-truth guessing
Product.Price
Product.Stock
Visual Acceptance — MANDATORY

Capture LIVE screenshots from Tooba, not external Shopeiva runtime.

Store:

docs/evidence/TB-P04-T007/

Minimum:

01-tooba-home-desktop-rtl.png
02-tooba-home-mobile-rtl.png
03-tooba-listing-desktop-rtl.png
04-tooba-listing-mobile-rtl.png
05-tooba-pdp-desktop-rtl.png
06-tooba-pdp-mobile-rtl.png
07-tooba-pdp-multi-seller.png
08-tooba-blue-theme-header-cards.png

Recommended viewport:

Desktop: 1440x900
Mobile: 390x844

No fixture banner.
No debug controls.
No architecture lecture in customer UI.

Shopeiva Fidelity Evidence

Create:

docs/evidence/TB-P04-T007/shopeiva-fidelity.md

Record:

Shopeiva source route/pattern
Tooba target route
preserved components/layout
minimal changes made
business data replaced with Tooba API
reason for any visual deviation

Goal:

prove the template was mounted, not reinvented
Live Data Evidence

Create:

docs/evidence/TB-P04-T007/live-data-map.md

Example:

Product title → Catalog
Seller → Party/Offer
Display price → Pricing resolver
Availability → Inventory
Promotion badge → Promotion

No demo JSON.

Acceptance Conditions

PASS requires:

Shopeiva visual identity clearly recognizable
blue theme applied minimally/centrally
Tooba Home live
Tooba listing live
Tooba PDP live
business data from Tooba backend
multi-seller PDP seam visible
no Product.Price
no Product.Stock
no cross-module SQL join
no frontend domain-truth guessing
desktop RTL works
mobile RTL works
visual evidence captured
Validation

Backend:

dotnet restore src/backend/Tooba.slnx
dotnet build src/backend/Tooba.slnx
dotnet test src/backend/Tooba.slnx

Require warnings=0, errors=0, failed=0, skipped=0.

Frontend:

cd src/frontend
npm ci
npm run typecheck
npm run lint
npm run test:grid
npm run test:workspace
npm run test:product-workspace
npm run build

Add focused Storefront tests if new behavior is testable.

Then:

git diff --check
git status --short --branch
Architecture Recheck

Must remain true:

Product != Variant
Product != Offer
Offer != Price
Product != Inventory
Pricing != Tax
Backend/module boundary != UI boundary
Frontend visual reuse != domain-model reuse
SoT Updates

Update:

docs/PROJECT-STATE.md
docs/ROADMAP.md
docs/ai/TOOBA-RECOVERY-CONTEXT.md
docs/prompts/START-HERE-IF-CHATGPT-IS-LOST.md

Record:

TB-P04-T006 = ACCEPTED
TB-P04-T007 = AWAITING_ARCHITECT_ACCEPT
P04 = IN_PROGRESS

Sell-first rule:
preserve Shopeiva with minimum change,
connect Tooba live backend,
sell quickly,
enhance later.

Initial presentation:
Persian RTL first.
Full multilingual/LTR and advanced currency UX later.
Save Envelope VERBATIM

Save:

docs/ai/tasks/TB-P04-T007.task.md
Commit / Push
git diff --check
git status --short --branch
git add ...
git commit -m "feat integrate Shopeiva storefront live slice [TB-P04-T007]"
git push origin main
git fetch origin
git rev-parse HEAD
git rev-parse origin/main
git status --short --branch

Require HEAD == origin/main.

Result Contract

Return:

BEGIN_TOOBA_CURSOR_RESULT_V1

Protocol-Version: 1
Task-ID: TB-P04-T007
Phase: P04 — Experience Foundation
Status: PASS | BLOCKED | RECOVERY_CONFLICT

Summary:
...

Repository-Recovery:
- root:
- branch:
- predecessor:
- final HEAD:
- origin/main:
- synchronized:

Shopeiva-Reuse:
- Home source patterns:
- Listing source patterns:
- PDP source patterns:
- Header/footer:
- preserved:
- deviations:

Theme:
- blue token approach:
- layout changed due to theme:
- semantic status colors preserved:

Live-Routes:
- Home:
- Listing:
- PDP:

Live-Data:
- Catalog:
- Category:
- Offer:
- Seller:
- Pricing:
- Promotion:
- Tax:
- Inventory:
- Media:

Multi-Seller:
- product:
- offers:
- primary offer policy:
- other-seller seam:

Cart-Boundary:
- CTA state:
- real cart mutation included:
- next boundary:

SEO:
- title:
- description:
- canonical:
- structured-data seam:

Architecture:
- Product.Price:
- Product.Stock:
- cross-module SQL join:
- frontend domain guessing:

Visual-Evidence:
- 01:
- 02:
- 03:
- 04:
- 05:
- 06:
- 07:
- 08:
- Fidelity document:
- Live-data map:

Validation:
- backend restore:
- backend build:
- warnings:
- errors:
- backend tests passed:
- backend failed:
- backend skipped:
- npm ci:
- typecheck:
- lint:
- grid tests:
- workspace tests:
- product-workspace tests:
- storefront focused tests:
- frontend build:
- git diff --check:

Git:
- commit:
- push:
- final HEAD:
- final origin/main:
- final status:
- synchronized:

Source-of-Truth:
- TB-P04-T006: ACCEPTED
- TB-P04-T007: AWAITING_ARCHITECT_ACCEPT
- P04: IN_PROGRESS
- Sell-first Shopeiva preservation rule recorded:
- Recovery-ready:

Architectural-Concerns:
- ...

Visual-Concerns:
- ...

Blockers:
- ...

END_TOOBA_CURSOR_RESULT_V1

After RESULT, surface the new Tooba live screenshots directly in this SAME chat/session.

CRITICAL — RESULT DOES NOT END THIS AGENT SESSION

After RESULT:

PIPELINE_STATE: WAITING_FOR_ARCHITECT_IN_SAME_SESSION

Then:

DO NOT CLOSE OR LEAVE THIS SESSION.
WAIT HERE FOR THE NEXT AUTHORIZED ENVELOPE.

Do not finish.
Do not exit.
Do not leave PIPELINE.
Do not self-advance.
Do not invent TB-P04-T008.

RESULT = CONTROL RETURNED TO ARCHITECT.
RESULT != WORKFLOW FINISHED.

Cursor PASS is not Architect ACCEPT.

Leaving or ending the pipeline after RESULT is a protocol violation.

END_TOOBA_CURSOR_TASK_V1
