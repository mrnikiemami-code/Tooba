PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

TASK-ID: TB-P07-T001
PROJECT: Tooba
PHASE: P07
CHANNEL: tooba-main
STATUS: ISSUED
TASK-TYPE: ADVANCED CATALOG FOUNDATION
WORKER-POLICY: ONE WORKER = ONE ACTIVE TASK

Title

Professional Category Attribute Schema & Product Variant Axes — Real Catalog Model Foundation with Shopeiva-Locked Admin/Seller UX

Architect Context

ACCEPTED:

TB-P06-T029 = ACCEPTED

P06 = COMPLETE

Commercial Readiness Gate = PASSED

USER_VISUAL_ACCEPTED remains separate/non-blocking

P07 starts focused Advanced Catalog work.

Current known state:

Product sale flow LIVE

default/single Variant LIVE

advanced Category-scoped attributes NOT YET PROVEN

product-specific variant axes NOT YET PROVEN

variant matrix NOT YET PROVEN

advanced variant work was intentionally deferred until commercial core was ready

Objective

Build the correct foundation for:

Category Attribute Definitions

Category-scoped inheritance

Product Attribute Values

Product Variant Axes

typed values and validation

Do NOT build the full variant matrix/generator yet unless required minimally to prove the foundation.

Non-Negotiable Domain Rules

Product != Variant != Offer

Attribute Definition != Attribute Value

Category assignment must not be represented as free-form JSON blobs if typed schema exists.

Do NOT add:
Product.Price
Product.Stock
Variant.Price
Variant.Stock
as shortcuts.

Pricing remains Pricing owner.
Inventory remains Inventory owner.
Offer remains Offer owner.

A — Recovery

Run:
git rev-parse --show-toplevel
git fetch origin
git branch --show-current
git rev-parse HEAD
git rev-parse origin/main
git status --short --branch

Expected predecessor:
00e7b93c3cd70bb20daffdfa3cad1115918586d7

Require:
main
HEAD == origin/main
safe tracked tree

If conflict:
RECOVERY_CONFLICT

B — Runtime Before Work

Start and KEEP RUNNING:

Backend :5088

Tooba Frontend :3000

Original Shopeiva :3001

Verify:
health/live 200
health/ready 200

Do not stop after Result.

C — Existing Catalog Audit FIRST

Audit actual current models/APIs/UI for:

Product

Variant

Category

Brand

Product creation/edit

Seller product management

attribute/specification-like fields

options

variant selectors

Shopeiva PDP option/variant UI

Classify:
LIVE
PARTIAL
MISSING
DEFERRED

Evidence:
docs/evidence/TB-P07-T001/01-catalog-variant-audit.md

D — Category Attribute Definition Model

Implement canonical typed definitions owned by Catalog.

Minimum fields:

Id

Code

localized Name

DataType

Unit optional

IsRequired

IsFilterable

IsComparable

IsVariantAxisAllowed

IsMultivalue

DisplayOrder

Validation metadata

Status

Supported initial DataTypes should be explicit, e.g.:
Text
Integer
Decimal
Boolean
SingleSelect
MultiSelect

Do not invent unsupported types.

E — Attribute Options

For select-like definitions:

stable option IDs

localized labels

display order

active/inactive

optional normalized code

Product values must reference real option IDs, not copied labels.

F — Category Attribute Schema

Allow Category to bind Attribute Definitions.

Required:

explicit ordering

required/optional override only where architecture permits

inheritance from parent Category

deterministic effective schema

child override rules documented

no cycles

no cross-module SQL joins

Provide API to obtain effective schema for a Category.

G — Product Attribute Values

Product stores values against real AttributeDefinition IDs.

Required:

typed validation

required validation from effective Category schema

select values must reference allowed options

invalid data rejected

unknown attribute rejected

values localized only where type requires it

No arbitrary attribute JSON bypass.

H — Product Variant Axes

A Product may designate a subset of effective category attributes as Variant Axes only when:
IsVariantAxisAllowed = true.

Examples:
Color
Size
Storage

Rules:

product-specific axis selection

axes must come from effective Category schema

no duplicate axis

stable ordering

selected axis values use real option IDs/typed values

Do NOT yet implement unlimited combinatorial generation unless minimally required.

I — Variant Identity Foundation

Existing Variant must be able to reference its selected axis-value combination.

Required:

stable combination identity

duplicate combination prevented

default/single Variant remains backward compatible

no breaking current sellable product flow

J — Category Change Policy

If Product changes Category:

recompute effective schema

detect invalid orphan values

do not silently discard data

return actionable validation/impact result

Document safe policy.

K — Admin APIs

Admin must be able to:

manage Attribute Definitions

manage select Options

assign Attributes to Categories

inspect effective inherited schema

create/edit Product values

choose Product Variant Axes

Use Access Control permissions.
Add canonical permissions if missing.

L — Seller APIs

Seller may edit product attribute values/axes only within Seller product permissions and only where current marketplace ownership workflow permits.

Seller must NOT redefine platform/category Attribute Definitions unless explicitly authorized future policy.

M — Shopeiva Source Discovery

Inspect actual Shopeiva:

Product create/edit forms

category selectors

specification sections

PDP option selectors

filter/attribute displays

variant-like selectors

Map exact components/behavior.

Do not invent visual system.

N — Admin UI

Use accepted Admin shell + Shopeiva-derived patterns.

Required:

Attribute Definitions page

Category Schema editor

effective inherited schema preview

Product attribute editor

Product Variant Axes selector

Professional UX:

search

grouping

ordering

typed editor controls

validation

no wall of raw JSON

O — Seller Product UI

Integrate into existing Seller Product create/edit.

Required:

after Category selection, load effective schema

typed fields

required indicators

variant-axis selection only for allowed attributes

no fake controls

preserve Shopeiva geometry

P — Storefront/PDP Compatibility

Do not redesign PDP.

If current Product has advanced axis data:

expose typed variant-axis metadata in API

only minimally bind existing Shopeiva PDP selector if safe and non-breaking

If full variant selection/cart integration requires matrix task:
defer honestly to next task.

Current single/default Variant PDP must remain functional.

Q — Search/Filter Foundation

If IsFilterable:
expose normalized typed attribute projection suitable for future faceted search.

Do NOT implement broad search-engine redesign in this Task.

No cross-module joins.

R — Development Seed

Development/Test-only, idempotent.

Seed realistic schema:
Category:
Mobile

Attributes:

Brand-like attribute only if Brand domain should NOT be duplicated; otherwise do not create duplicate Brand attribute

Color

Storage

RAM

Screen Size

Use typed examples.

At least:
Color and Storage = VariantAxisAllowed
Screen Size = comparable/filterable, not axis if chosen policy

Seed one product with:

real values

two selected axes

multiple axis values sufficient to prove combination foundation

No Production seed.

S — Tests

Minimum:
1 effective category schema
2 parent inheritance
3 child override policy
4 required validation
5 invalid option reject
6 unknown attribute reject
7 typed numeric validation
8 axis allowed validation
9 duplicate axis reject
10 duplicate variant combination reject
11 category change orphan detection
12 seller cannot redefine schema
13 admin permission deny
14 tenant isolation
15 backward compatibility default Variant
16 seed idempotency

T — E2E

No direct DB mutation.

Admin:
1 create/edit Attribute Definition
2 bind to Category
3 inspect effective schema
4 edit Product values
5 choose Variant Axes

Seller:
1 open Product
2 Category loads real schema
3 fill values
4 choose allowed axes
5 save/reload persists

Storefront:
existing sellable Product flow still works.

U — Browser Proof

Capture:

Admin Attribute Definitions

Category Schema editor

effective inheritance preview

Admin Product attribute editor

Seller Product attribute editor

Variant Axis selector

selected mobile product PDP compatibility

mobile where appropriate

Shopeiva comparison

V — Visual Fidelity

Shopeiva locked:
CSS
JS
spacing
typography
inputs
selects
cards
tabs
accordions
hover/focus
transition
responsive/mobile

No generic schema-builder visual foreignness.

W — Validation

Backend:
dotnet restore src/backend/Tooba.slnx
dotnet build src/backend/Tooba.slnx
dotnet test src/backend/Tooba.slnx

Require:
0 warnings
0 errors
0 failed
0 skipped

Frontend:
cd src/frontend
npm run typecheck
npm run lint
npm run test
npm run test
npm run test
npm run test
npm run build

Also run catalog/product/attribute/access-control/i18n suites if available.

Always:
git diff --check

X — Runtime MUST REMAIN ALIVE

After validation KEEP RUNNING:

Backend

Tooba Frontend

Original Shopeiva

Do not stop after Result.

Y — USER-PREVIEW

Return exact concrete URLs:

Admin Attribute Definitions

Admin Category Schema

Admin seeded Product edit

Seller seeded Product edit

Storefront seeded PDP

Original Shopeiva comparison route(s)

exact dev identity/context

No placeholders.

Z — Source of Truth

If successful:

P07 = IN_PROGRESS
TB-P06-T029 = ACCEPTED
TB-P07-T001 = AWAITING_ARCHITECT_ACCEPT

CATEGORY_ATTRIBUTE_SCHEMA = LIVE
CATEGORY_ATTRIBUTE_INHERITANCE = LIVE
PRODUCT_TYPED_ATTRIBUTES = LIVE
PRODUCT_VARIANT_AXES = LIVE
VARIANT_COMBINATION_FOUNDATION = LIVE
FULL_VARIANT_MATRIX = DEFERRED
FACETED_SEARCH_INTEGRATION = DEFERRED
VISUAL_CONTRACT = SHOPEIVA_LOCKED

Update canonical recovery docs.

Acceptance Criteria

PASS requires:

typed attribute definitions

category binding

inheritance

typed product values

product-specific variant axes

duplicate combination protection

no Product.Price/Stock shortcuts

no arbitrary JSON bypass

Admin UI

Seller UI

current sale flow non-regressed

Dev seed

browser proof

backend 0/0/0/0

frontend green

runtimes alive

exact preview URLs

repo synchronized

Worker may report:
CATEGORY_ATTRIBUTE_FOUNDATION_LIVE
PRODUCT_VARIANT_AXES_LIVE

Worker must NOT report:
FULL_VARIANT_MATRIX_LIVE
FACETED_SEARCH_COMPLETE
USER_VISUAL_ACCEPTED
PRODUCT_FULLY_READY
PRODUCTION_GO_LIVE_READY

Git

git add ...
git commit -m "feat add category attributes and variant axes [TB-P07-T001]"
git push origin main
git fetch origin
git rev-parse HEAD
git rev-parse origin/main
git status --short --branch

Require HEAD == origin/main and tracked tree clean.

Result Contract

PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_WORKER_RESULT

Task-ID:
TB-P07-T001
Channel:
tooba-main
Status:
PASS | FAIL | BLOCKED | RECOVERY_CONFLICT

Summary:
...

Audit:

Category:

Product:

Variant:

attributes:

Shopeiva:

Attribute-Definitions:

types:

options:

localization:

validation:

Category-Schema:

binding:

inheritance:

override:

effective schema:

cycle safety:

Product-Values:

typed:

required:

invalid option:

unknown attribute:

category change:

Variant-Axes:

allowed:

product-specific:

value selection:

combination identity:

duplicate prevention:

full matrix:

Admin:

APIs:

permissions:

definitions UI:

category schema UI:

product attributes UI:

Seller:

APIs:

permissions:

product attributes UI:

axis selector:

schema redefine:

Storefront:

PDP compatibility:

current sale flow:

deferred:

Seed:

Development only:

idempotent:

Category:

Attributes:

Product:

axes:

Visual-Fidelity:

Shopeiva source:

CSS/JS:

forms:

cards/tabs:

hover/focus:

responsive:

foreign UI:

unauthorized deviation:

Validation:

backend restore:

backend build:

backend tests:

warnings:

errors:

failed:

skipped:

typecheck:

lint:

storefront:

seller:

catalog:

frontend build:

git diff --check:

Runtime:

Backend:

Frontend:

Shopeiva:

health/live:

health/ready:

kept alive:

USER-PREVIEW:

Admin Attribute Definitions:

Admin Category Schema:

Admin Product:

Seller Product:

Storefront PDP:

Original Shopeiva:

dev preview identity/context:

preview steps:

Source-of-Truth:

P07:

T029:

T001:

category attributes:

product attributes:

variant axes:

full matrix:

visual contract:

Git:

commit:

push:

final HEAD:

origin/main:

synchronized:

tracked tree:

Architectural-Concerns:
...
Visual-Concerns:
...
Blockers:
...

END_TOOBA_WORKER_RESULT

After Result:

return to IDLE

DO NOT STOP Backend / Tooba Frontend / Original Shopeiva

do not self-issue another Task

END_TASK