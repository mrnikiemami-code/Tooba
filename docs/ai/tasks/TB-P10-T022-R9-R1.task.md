PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_TASK

Task-ID: TB-P10-T022-R9-R1
Parent-Task: TB-P10-T022-R9
Channel: tooba-main
WorkerId: tooba-worker-01
AgentType: cursor
Status: ISSUED

Appearance-USER_VISUAL_ACCEPTED=YES
Builder-USER_VISUAL_ACCEPTED=NO

Title:
Store Pages SSR Performance Repair — Landing Route Latency, Query Efficiency, Metadata Render Path

Architect Review:
TB-P10-T022-R9 is NOT fully Architect-accepted yet.

Reason:
Worker reported FE /landing/{slug} SSR observed ~90–120 seconds during capture while Host resolve remained fast.

This is a material production/SEO/performance concern and must be repaired before R10.

Do NOT start R10 yet.
Do NOT broaden scope.
Do NOT invent TB-P10-T023.

Recovery State:
Phase P10
Last Architect-accepted: TB-P10-T022-R8-R2
Last Implementation: TB-P10-T022-R9
Current Repair: TB-P10-T022-R9-R1
Expected previous HEAD: 28b000ebd571e12bc76d25042daa2fa9fa8463bb
Worker: tooba-worker-01
Preserve LOCK-SF-001…324.

Recovery Preflight
Record branch/HEAD/origin/main/git status, FE :3000, Host :5088, /landing/{slug} route, Home / route, metadata generation path, page resolve API calls, language/translation calls, caching/revalidation, unrelated local changes.
Unexpected tracked divergence => RECOVERY_CONFLICT/BLOCK.
Evidence: docs/evidence/TB-P10-T022-R9-R1/recovery-start.md

Reproduce and Measure
Measure cold/warm:

/landing/{slug}

/

metadata generation

APIs used by SSR
Capture TTFB, server render time, backend API time, request count, DB query count if available.
Do not guess.
Evidence: docs/evidence/TB-P10-T022-R9-R1/performance-baseline.md

Root Cause
Investigate:

duplicate fetch between metadata and page

sequential fetch waterfalls

repeated language/translation requests

repeated Store/Page resolution

retries/timeouts

development-only blocking work

N+1 backend queries

sync filesystem/work

uncached static/template data

unrelated sitemap/SEO work on request path

public FE self-fetch where direct shared resolver is more appropriate
No timeout workaround.

Shared Request Resolver
Use one canonical request resolver for page + SEO data.
Require request-scoped dedupe, Store/locale isolation, no cross-tenant stale leakage.

Backend Query Efficiency
Audit Store Page resolve query.
No N+1.
Fetch only needed page/composition/SEO/translation data.
Use indexed Store/slug/type/status predicates.
Do not load full Catalog for metadata/page shell unless required.
Add minimal justified index if missing.

Metadata Performance
Metadata generation must not duplicate expensive composition/data graph work.
Read only title/description/canonical/robots/OG/alternates/H1-derived data as needed.
Reuse canonical resolver where practical.

Composition/Data Loading
Keep R9 rendering correctness.
Parallelize independent section data safely.
Reject one HTTP call per section, one DB query per card, polling, sleeps, magic retries.

Caching/Revalidation
Use current project/Next caching correctly.

no public caching of private Admin preview

public Store Page cache may be Store+locale+page scoped

Home activation/edit invalidates correctly

Landing edit/publish invalidates correctly

no stale cross-store leakage

Performance Acceptance
90–120s stalls must be eliminated.
Warm SSR in local dev should be stable in low-single-digit seconds under normal conditions.
Document cold compile/dev-bundle overhead separately.
Repeated requests must not progressively slow down.

SEO Regression
Reverify title, description, canonical, robots, OG, hreflang, H1, structured data, sitemap rules.

Runtime Proof
Host :5088 + FE :3000.
Required:
A. cold Landing
B. 3 warm Landing requests
C. Home request
D. another locale if configured
E. metadata intact
F. Home activation/restore invalidates correctly
G. no Store cross-contamination
Evidence: docs/evidence/TB-P10-T022-R9-R1/runtime.md

Focused Validation
Required:

request resolver dedupe guard

duplicate metadata/page fetch guard

backend query-shape/count guard if feasible

cache Store/locale isolation

Home/Landing invalidation

SEO regression

critical-storefront

recovery-staleness

git diff --check
No synthetic benchmark suite.

Anti-Pattern Gate
Reject:

increasing timeout

hiding latency with spinner

polling/retry loops

disabling SEO

cache without Store/locale isolation

global tenant cache

N+1

duplicate metadata/page fetch chain

unrelated refactor
Evidence: docs/evidence/TB-P10-T022-R9-R1/antipattern-scan.md

Locks
Add non-duplicate locks after 324 only if not already covered:
LOCK-SF-325 Store Page SSR uses a deduplicated canonical request resolver for page/SEO data rather than duplicate metadata/page fetches.
LOCK-SF-326 Public Store Page caching/revalidation is Store+locale+page scoped and invalidated on relevant publish/Home-selection changes.
LOCK-SF-327 Performance fixes must not weaken SEO metadata, tenant isolation, or content freshness.

Recovery / Pipeline
At completion:

Last Architect-accepted before review: TB-P10-T022-R8-R2

Last Implementation: TB-P10-T022-R9-R1

Current Issued/Repair: none

Appearance YES

Builder NO

Worker IDLE

main

HEAD==origin/main

unrelated work preserved

do NOT start R10

do NOT invent TB-P10-T023

PASS only if:

root cause documented

90–120s application SSR stall eliminated

warm Landing SSR stable in low-single-digit seconds locally

duplicate data work reduced

no N+1

cache isolation safe

edits/Home changes invalidate

SEO remains complete

focused tests pass

anti-pattern CLEAN

Worker IDLE

Return ONLY canonical BRIDGE-WAKE-V1 Result with:
Summary
Applicable-Locks
Recovery-Start
Performance-Baseline
Root-Cause
Shared-Resolver
Backend-Query-Efficiency
Metadata-Performance
Composition-Loading
Caching-Revalidation
Performance-Result
SEO-Regression
Focused-Validation
Runtime
AntiPattern-Scan
Lock-Registry
Recovery-SoT
Git
Architectural-Concerns
Visual-Concerns
Blockers

END_TOOBA_TASK