PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

TASK-ID: TB-P07-T004
PROJECT: Tooba
PHASE: P07
CHANNEL: tooba-main
STATUS: ISSUED
TASK-TYPE: CATALOG FOUNDATION — CATEGORY DOMAIN + TRANSLATIONS + ROUTING CONTRACT
WORKER-POLICY: ONE WORKER = ONE ACTIVE TASK

Visual Contract

The user will provide the approved Admin Catalog UI reference image together with this Task.

That image is a LOCKED VISUAL CONTRACT for the upcoming Catalog Admin work.

For this Task:

do NOT fully implement all pictured screens yet

DO preserve the architecture needed to reproduce those screens exactly in later tasks

do NOT redesign/reimagine the approved visual language

future Category Admin Tree/Workspace screens must match the supplied reference composition, spacing, density, hierarchy, tabs/cards, and Tooba styling

The existing canonical AppDataGrid must be reused anywhere a data grid is needed.
Do NOT create another grid implementation.

The approved Tree direction is:
AppCategoryTree wrapper over Ant Design Tree.

Goal

Establish the complete Category catalog foundation before UI implementation.

The model must support:

Category Tree
→ Admin Category Workspace
→ Translations
→ SEO
→ Attribute Schema / Inheritance
→ Facet Configuration
→ Mega Menu binding
→ Storefront canonical Category route
→ PLP products + dynamic facets

This Task is FOUNDATION ONLY:

domain model

contracts

persistence

routing identity model

translation model

slug history/redirect model

API/application contracts needed by next UI task

architecture docs/tests

Do NOT build the full Tree UI yet.
That is T005.

Locked Product/Catalog Principles

Admin owns Product Master / Category / Attributes / Variant axes.

Seller does NOT create canonical Product Master.

Seller later selects Product/Variant and creates Offer/Pricing/Inventory.

Category is NOT merely a menu folder.

Every Category node is an independent catalog entity and may own a storefront route.

Mega Menu is presentation/navigation over Category; it is NOT the source of truth for Category.

Category hierarchy and URL identity are separate.

Multi-language data MUST use translation records. Do NOT add NameFa, NameEn, etc.

Slug is locale-specific.

Slug changes must be redirect-safe.

Category must be ready for PLP/facet architecture; do not design it only for Admin Tree.

1 — Category Aggregate

Design/implement canonical language-independent Category fields conceptually:

Id

ParentId?

Status

SortOrder

ImageMediaAssetId?

IconMediaAssetId?

IsVisible

CreatedAt

UpdatedAt

Version/concurrency according to existing conventions

Use existing Tooba ID/time/entity conventions.

Do not store translated Name/Slug/Descriptions in Category.

Hierarchy rules:

source of truth may be adjacency via ParentId

self-parent forbidden

descendant-as-parent forbidden

subtree move valid only without cycle

sibling order deterministic

do not physical-cascade-delete important catalog history by default

2 — CategoryTranslation

Create translation model:

CategoryId

Locale

Name

Slug

ShortDescription?

Description?

SeoTitle?

SeoDescription?

MetaKeywords? only if existing SEO conventions justify it

Required:

one translation per Category + Locale

normalized locale

Name required for publishable locale

Slug required for routable locale

unique locale+slug route identity

no locale-specific columns

3 — Locale-Specific Slug / Route Identity

Hierarchy must NOT define canonical URL identity.

Preferred stable conceptual route:

/{locale}/category/{localizedSlug}

Exact route must align with existing Tooba routing conventions.

Rules:

CategoryId immutable identity

ParentId = taxonomy hierarchy

localized Slug = route identity

moving Category must not force descendant URLs to change

4 — Category Slug History

Implement redirect history foundation:

CategoryId

Locale

OldSlug

ChangedAt

When slug changes:

old slug remains resolvable as historical

current slug wins

later Storefront can return permanent redirect

prevent loops/conflicts

5 — Category Route Resolver Contract

Create contract:

(locale, slug)
→ current Category
OR historical match + current canonical slug
OR not found

Result conceptually includes:

CategoryId

Locale

CurrentSlug

IsRedirect

CanonicalPath

Do not leak persistence entities.

6 — Category Tree Query Contract

Prepare efficient API/query contract for T005 Tree.

Conceptual node:

Id

ParentId

Name resolved for locale

Slug

Status

SortOrder

HasChildren

ProductCount only if efficient/module-safe

Must support:

search

expand/collapse

drag/drop move

sibling reorder

locale-aware labels

No recursive N+1 API design.

7 — Category Move / Reorder Contract

Define commands for:

MoveCategory

ReorderCategory

Validate:

self-parent forbidden

descendant parent forbidden

parent exists

sibling order deterministic

concurrency

authorization

no cross-module SQL joins

8 — Category Lifecycle

Use explicit status consistent with Tooba conventions, conceptually:

Draft

Published/Active

Archived/Inactive

Storefront later only resolves eligible states.

9 — Future Attribute / Facet Extension Points

Do NOT fully build these now, but architecture must leave clean extension points for:

CategoryAttributeAssignment

AttributeInheritance

CategoryFacetConfiguration

VariantAxisAssignment

Do NOT hide this future model in arbitrary JSON blobs.

10 — Mega Menu Separation

Mega Menu remains separate.

Future relation conceptually:
MegaMenuItem → CategoryId

Category must not own menu-specific layout/column positioning as taxonomy truth.

11 — Storefront PLP Contract

Document future flow:

localized category route
→ Category Route Resolver
→ CategoryId
→ Catalog Listing Query

products in category/subtree

subcategories

dynamic facets

sorting

pagination

localized SEO

Parent category must be subtree-capable.

12 — Persistence / Module Ownership

Keep Category in Catalog ownership.

No cross-module SQL JOIN.

Media references use existing module contracts.

Do not introduce a cross-boundary generic taxonomy service.

13 — Authorization

Admin category mutations must use existing SpiceDB-centered authorization path.

Cover conceptually:

view

create

edit

move/reorder

publish/archive

delete if allowed

No local hardcoded role checks.

14 — APIs Needed for T005/T006

Prepare clean contracts/handlers/endpoints using existing Tooba conventions for:

GET tree

GET category workspace summary

POST create

update core fields

update translation

move

reorder

route resolution/internal storefront contract as appropriate

Do not expose DB entities directly.

15 — Multi-Language UX Contract

Document:

Global fields:

Parent

Status

SortOrder

Image/Icon

Localized fields:

Name

Slug

ShortDescription

Description

SEO title

SEO description

T006 UI will use locale switcher/tabs over the same CategoryId.

16 — Documentation

Create:
docs/catalog/CATEGORY-ARCHITECTURE.md

Include:

aggregate

translations

slug uniqueness

slug history

route resolution

tree/move rules

multilingual rules

Mega Menu separation

PLP/facet future flow

Admin UI target

hierarchy != route identity

Update recovery/project-state docs per pipeline rules.

17 — Tests

Backend tests at minimum:

create root

create child

translation per locale

duplicate slug same locale rejected

same slug different locale according to policy

slug change creates history

historical slug resolves redirect

current slug resolves canonical

self-parent forbidden

move under descendant forbidden

valid subtree move

reorder siblings

archived/non-public route behavior

locale resolution

authorization path where testable

no Product grid/query regression

18 — Frontend

No full Category UI yet.

Only add frontend contracts/types if required for API compatibility.

Do NOT build temporary CRUD/tree UI.

19 — Validation

Backend:

restore

build

full tests

Frontend if touched:

typecheck

lint

existing grid/admin tests

production build

Require:

0 errors

0 failed

0 skipped

no new warnings

Always:

git diff --check

20 — Runtime

Keep alive:

Backend :5088

Frontend :3000

Shopeiva :3001

21 — Git

Commit + push.

Require:

HEAD == origin/main

tracked tree clean

unrelated user changes preserved

22 — Acceptance

PASS only if:

Category is independent tree entity

translations stored separately

no NameFa/NameEn columns

localized slug supported

locale+slug uniqueness enforced

slug history/redirect foundation implemented

hierarchy independent from URL identity

tree query contract ready for Ant-based Admin Tree

move/reorder rules implemented

route resolver exists

Mega Menu remains separate

PLP/facet future architecture documented

module ownership preserved

SpiceDB authorization path preserved

full validation green

no regression to canonical AppDataGrid

docs complete

Git synchronized

Result Contract

Return ONLY:

PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_WORKER_RESULT

Task-ID:
TB-P07-T004
Channel:
tooba-main
Status:
PASS | FAIL | BLOCKED | RECOVERY_CONFLICT

Summary:
...

Visual-Contract:

reference image received:

redesign performed:

canonical AppDataGrid preserved:

future AppCategoryTree direction:

Category:

module:

entity/path:

parent model:

status:

sort:

cycle prevention:

move/reorder:

Translations:

entity/path:

locale:

name:

slug:

unique rule:

SEO fields:

locale columns introduced:

Slug-History:

entity/path:

history creation:

redirect resolution:

loop/conflict prevention:

Routing:

resolver:

current slug:

historical slug:

canonical path:

hierarchy dependency:

Tree-Contract:

query/API:

node DTO:

locale-aware:

N+1:

search readiness:

lazy/whole-tree strategy:

Future-Extensions:

attributes:

inheritance:

facets:

variant axes:

mega menu:

PLP:

Authorization:

SpiceDB:

permissions:

Validation:

backend restore:

backend build:

backend full tests:

passed:

failed:

skipped:

frontend touched:

frontend typecheck:

frontend lint:

frontend tests:

frontend build:

warnings:

new warnings:

git diff --check:

Documentation:

category architecture:

recovery/state docs:

Runtime:

Backend:

Frontend:

Shopeiva:

kept alive:

Git:

branch:

commit:

push:

final HEAD:

origin/main:

synchronized:

tracked tree:

unrelated user changes:

Architectural-Concerns:
...

Visual-Concerns:
...

Blockers:
...

END_TOOBA_WORKER_RESULT

After Result:

Worker -> IDLE / Waiting

keep runtimes alive

do not invent T005

do not add extra prose

END_TASK