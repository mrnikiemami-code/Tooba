PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

TASK-ID: TB-P06-T024-R1
PROJECT: Tooba
PHASE: P06
CHANNEL: tooba-main
STATUS: ISSUED
TASK-TYPE: FOCUSED ACCESS-CONTROL REPAIR
PARENT-TASK: TB-P06-T024
WORKER-POLICY: ONE WORKER = ONE ACTIVE TASK

Title

Access Control Completion — Real Data-Backed Scope Selectors, Runtime Order Filtering and Complete UI Capability Projection

Architect Decision

TB-P06-T024 Worker PASS is NOT yet Architect ACCEPTED.

The core Access Control Center is provisionally accepted and MUST NOT be redesigned:

Permission Catalog

dynamic Roles

Assignments

PlatformSellerCeiling

SpiceDB v3 integration

shared Admin/Seller UI

effective access

typed resource-scope foundation

Mobile-vs-Books authorization policy

Repair is required because:

Scope must be selected from REAL Tooba domain/database resources, not free-form text or abstract IDs.

Mobile-vs-Books must be enforced in actual Seller Order list/detail/actions, not policy tests only.

Navigation/Action Projection was reported PARTIAL and must be completed for current live commercial modules.

ABSOLUTE SHOPEIVA LOCK

Preserve T024 UI and all locked Shopeiva behavior:
CSS, Tailwind, JS behavior, animation, transition, hover/focus/active, spacing, typography, responsive/mobile, cards, tabs, lists, search, checkbox/toggle behavior, modal/drawer, component geometry.

Only add source-derived UI required for real scope selection and capability projection.
Any foreign/appended UI = VISUAL REGRESSION.

A — Recovery

Run:
git rev-parse --show-toplevel
git fetch origin
git branch --show-current
git rev-parse HEAD
git rev-parse origin/main
git status --short --branch

Expected predecessor:
2cbdbe95f11fd6f995e8515835af5764079bc609

Require main, HEAD == origin/main, safe tracked tree.
If conflict: RECOVERY_CONFLICT.

B — Real Resource Scope Sources

Audit canonical Tooba owners/read APIs for:

Category

Product

Brand

Warehouse

Store

OrderSegment

Classify each:
LIVE_RESOURCE_SOURCE / NO_REAL_RESOURCE_YET / DEFERRED

Do not fabricate resources and do not cross-module SQL JOIN.

Evidence:
docs/evidence/TB-P06-T024-R1/01-real-scope-resource-map.md

C — Real Category Scope Selector

Category scope selector MUST read from the real Catalog/Category owner.

Required:

real Category IDs

real names

real hierarchy/tree where supported

search

selection/removal

clear state

loading/empty/error

tenant/ownership filtering

no free-form string

no raw UUID entry in normal UI

Persist:
PermissionId + ScopeKind=Category + real CategoryId

Evidence:
02-real-category-scope-selector.md

D — Other Real Scope Selectors

Where canonical read APIs exist, wire real searchable selectors for:

Product

Brand

Warehouse

Store

Use owner contracts/APIs only.
If no owner read API exists, either add a small correct owner API or mark that selector DEFERRED and unavailable. Never fake.

Evidence:
03-other-real-scope-selectors.md

E — Shared ScopeEditor UX

Extend the existing shared Access Control component system.

Required:

Scope Type

Search

real resource list/tree

selected resources

remove

clear

loading/empty/error

ceiling-disabled state

Same shared component for Admin/Seller.
No generic foreign tree/select library styling.

Evidence:
04-shared-real-scope-editor.md

F — Order Category Authorization Projection

Implement the minimum correct projection/gateway needed to determine which Seller order lines/resources belong to Category scope.

Constraints:

no Order -> Catalog SQL JOIN

no foreign repository/table access

no runtime N+1 Catalog calls across order lists

extraction-ready

use snapshot/projection/event-owned authorization data where appropriate

Evidence:
05-order-category-auth-projection.md

G — Runtime Seller Order Filtering

For employee:
order.view + Category=Mobile

Required:

Mobile-only order visible

Books-only order absent

mixed Mobile+Books policy explicit and safe

unauthorized Book line details never leak

counts/statistics must not reveal unauthorized order data

Prefer scoped order/line projection if whole-order UI would leak data.

Evidence:
06-runtime-order-list-scope.md

H — Runtime Detail Enforcement

Direct order/detail URL/API must enforce scope.

Test:

Mobile detail ALLOW

Books detail DENY

guessed ID DENY

mixed order does not expose unauthorized line data

Evidence:
07-runtime-order-detail-scope.md

I — Runtime Action Enforcement

For order.handle / fulfillment-related capability:

Mobile action ALLOW

Books action DENY

mixed order action only affects authorized resources where domain permits

unsafe whole-order action denied unless user has unscoped/global permission

Evidence:
08-runtime-order-action-scope.md

J — Seller UI Scope Truth

Seller employee order UI must use backend-scoped data, not frontend filtering.

Required:

authorized rows only

authorized totals/counts only

actions according to effective permissions

Shopeiva Seller Orders UI visually unchanged

Evidence:
09-seller-order-ui-scope-proof.md

K — Complete Navigation Projection

Complete effective-permission-driven navigation for current live modules.

At minimum:
Seller:

Products

Orders

Fulfillment

Promotions

Reviews

Stories

Notifications

Settlement

Returns/Refunds

Access Control

Admin:

Products/Catalog

Orders

Fulfillment

Promotions

Reviews

Stories

Content

Page Composition

Payments

Returns/Refunds

Settlement

Access Control

No *.view => nav hidden.
View without manage => page readable, mutation actions absent/disabled according to source pattern.
No hardcoded role-name logic.

Evidence:
10-navigation-capability-projection.md

L — Complete Action Projection

At minimum prove:

Product view/create/edit/publish

Order view/fulfill/refund where supported

Promotion view/manage

Review view/moderate

Story role-specific actions

Payment view/reconcile

AccessControl view/manage

UI projection from effective capability payload.
Backend remains authority.

Evidence:
11-action-capability-projection.md

M — Effective Access Preview with Real Names

Preview must show real human-readable scope values, e.g.:

Orders

View

Handle

Scope: Category

موبایل

Do not show only raw UUIDs or SpiceDB tuple syntax.

Evidence:
12-effective-access-real-resource-preview.md

N — Scoped Delegation Ceiling

Support Category scope intersection.

Example:
Platform ceiling:
order.handle, Category=Electronics

Seller role:
order.handle, Category=Mobile

Effective:
Mobile

Seller attempts Books outside ceiling:
DENY and disabled in UI.

Evidence:
13-scoped-delegation-ceiling.md

O — Real Mobile-vs-Books E2E

Using legitimate APIs/UI only, no direct DB mutation:

Create/use real:

Category Mobile

Category Books

Product/Offer under Mobile

Product/Offer under Books

Mobile order

Books order

mixed order if supported

Create real Seller employee:
Role = Mobile Order Operator
Permissions = order.view + order.handle
Scope = real Mobile Category selected through ScopeEditor

Prove:

Mobile visible

Books hidden

direct Books detail denied

Mobile action allowed

Books action denied

effective preview names Mobile

nav/actions consistent

Evidence:
14-mobile-vs-books-real-e2e.md

P — Browser Proof

Capture:
Admin Access Control:

real scope selector

Category search/tree

Seller ceiling

Seller Access Control:

Role editor

real Category selector

assignment

effective preview

Restricted employee:

scoped Orders

Mobile order

Books absent

allowed action

denied direct detail

Desktop + mobile for Access Control.
Original Shopeiva comparison for touched UI patterns.

Evidence:
15-browser-proof.md

Q — Authorization Tests

Minimum:

real Category selector

fake Category ID rejected

foreign tenant Category rejected

scope outside ceiling rejected

Mobile list allow

Books list filtered

Books detail denied

Mobile handle allow

Books handle deny

mixed order no unauthorized leakage

scoped ceiling intersection

Product real scope if live

Brand real scope if live

nav hidden on view deny

action hidden on manage deny

direct endpoint deny

tenant isolation

Evidence:
16-scope-runtime-tests.md

R — Visual Fidelity

Prove:

existing Access Control design preserved

selector is source-derived

search/list/tree/badges source-derived

animation/transition/hover/focus preserved

mobile intentional

Seller Orders visual structure unchanged

navigation visually unchanged

unauthorized deviation NONE

Evidence:
17-visual-fidelity.md

S — Validation

Backend:
dotnet restore src/backend/Tooba.slnx
dotnet build src/backend/Tooba.slnx
dotnet test src/backend/Tooba.slnx

Require:
warnings=0
errors=0
failed=0
skipped=0

Frontend:
cd src/frontend
npm run typecheck
npm run lint
npm run test
npm run test
npm run test
npm run test
npm run build

Also run access-control, order-scope, admin, seller, navigation/action, i18n tests.

Always:
git diff --check

Fix trailing-whitespace in T024/T024-R1 evidence touched by this Task.

Evidence:
18-final-validation.md

T — Runtime

Keep Backend, Tooba Frontend and Original Shopeiva running.

Verify:

health/live 200

health/ready 200

Admin Access Control

Seller Access Control

real Category selector

scoped employee Order list/detail/action

Evidence:
19-final-runtime.md

U — USER-PREVIEW

Only after ALL validation/build/runtime checks pass, return exact runnable URLs:

Admin Access Control

Admin Seller Access

Seller Access Control

Seller Roles

Seller Users

Restricted Seller Orders

Mobile Order

Original Shopeiva references

V — Source of Truth

If successful:

P06 = IN_PROGRESS
TB-P06-T023-R1 = ACCEPTED
TB-P06-T024 = REPAIRED_BY_TB-P06-T024-R1
TB-P06-T024-R1 = AWAITING_ARCHITECT_ACCEPT

ACCESS_CONTROL_CENTER = LIVE
ADMIN_ROLE_MANAGEMENT = LIVE
SELLER_ROLE_MANAGEMENT = LIVE
SELLER_PERMISSION_CEILING = LIVE
USER_ROLE_ASSIGNMENT = LIVE
UI_CAPABILITY_PROJECTION = LIVE
RESOURCE_SCOPE_FOUNDATION = LIVE
REAL_DATA_SCOPE_SELECTORS = LIVE
CATEGORY_SCOPED_ORDER_ACCESS = LIVE
CATEGORY_SCOPED_ORDER_FILTERING = LIVE
SPICEDB_AUTHORIZATION = LIVE
VISUAL_CONTRACT = SHOPEIVA_LOCKED

Report each other scope kind LIVE or DEFERRED individually.

Update:

docs/PROJECT-STATE.md

docs/ROADMAP.md

docs/ai/TOOBA-RECOVERY-CONTEXT.md

docs/prompts/START-HERE-IF-CHATGPT-IS-LOST.md

Acceptance Criteria

PASS requires ALL:

T024 core preserved

Category selector uses real Category owner/data

no free-form scope strings

no raw UUID normal-user entry

real resource IDs persisted

human-readable resource names shown

no cross-module SQL JOIN

order category authorization projection

Seller order list runtime filtering

Seller order detail enforcement

Seller order action enforcement

explicit safe mixed-order policy

real Mobile-vs-Books E2E

current live navigation projection no longer PARTIAL

major action projection no longer PARTIAL

backend authority preserved

scoped ceiling intersection proven

Shopeiva-native UI

backend 0/0/0/0

frontend build PASS

runtimes healthy

repo synchronized

Worker may report:
ACCESS_CONTROL_CENTER_COMPLETE_FOR_CURRENT_SCOPE
CATEGORY_SCOPED_ORDER_ACCESS_LIVE
REAL_DATA_SCOPE_SELECTORS_LIVE

Worker must NOT report:
ALL_RESOURCE_SCOPES_FULLY_INTEGRATED unless individually proven
USER_VISUAL_ACCEPTED
PRODUCT_FULLY_READY
PRODUCTION_GO_LIVE_READY

Git

git add ...
git commit -m "fix complete resource-scoped access control [TB-P06-T024-R1]"
git push origin main
git fetch origin
git rev-parse HEAD
git rev-parse origin/main
git status --short --branch

Require HEAD == origin/main and tracked tree clean.

Result Contract

PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_WORKER_RESULT

Task-ID:
TB-P06-T024-R1

Channel:
tooba-main

Status:
PASS | FAIL | BLOCKED | RECOVERY_CONFLICT

Summary:
...

Real-Scope-Sources:

Category:

Product:

Brand:

Warehouse:

Store:

OrderSegment:

Category-Selector:

owner/API:

real IDs:

tree:

search:

free-form:

raw UUID UI:

tenant/seller filtering:

Other-Selectors:

Product:

Brand:

Warehouse:

Store:

deferred:

Order-Scope-Projection:

architecture:

cross-module join:

order-line category data:

mixed-order policy:

Runtime-Order-Filtering:

Mobile list:

Books list:

mixed:

counts leakage:

Runtime-Order-Detail:

Mobile:

Books:

guessed ID:

mixed leakage:

Runtime-Order-Actions:

Mobile handle:

Books handle:

whole-order unsafe action:

Scoped-Ceiling:

Platform scope:

Seller scope:

intersection:

outside ceiling:

Navigation:

Seller modules:

Admin modules:

remaining partial:

Actions:

Products:

Orders:

Promotions:

Reviews:

Stories:

Payments:

Access Control:

remaining partial:

E2E:

real Mobile category:

real Books category:

real Mobile order:

real Books order:

employee:

role:

real scope selected:

Mobile visible:

Books hidden:

Books detail denied:

Mobile action:

Books action:

direct DB mutation:

Visual-Fidelity:

Access Control CSS:

selector UI:

search:

list/tree:

badges:

animation/transition:

responsive:

Seller Order UI:

navigation:

unauthorized deviation:

Validation:

backend restore:

backend build:

backend tests:

warnings:

errors:

failed:

skipped:

typecheck:

lint:

critical-storefront:

storefront:

customer:

seller:

access-control:

order scope:

navigation:

frontend build:

git diff --check:

Runtime:

Backend:

Frontend:

Shopeiva:

health/live:

health/ready:

USER-PREVIEW:

Admin Access Control:

Admin Seller Access:

Seller Access Control:

Seller Roles:

Seller Users:

Restricted Seller Orders:

Mobile Order:

Shopeiva references:

preview steps:

Source-of-Truth:

P06:

T024:

T024-R1:

Access Control:

real data scopes:

category order access:

navigation projection:

action projection:

visual contract:

Git:

commit:

push:

final HEAD:

origin/main:

synchronized:

tracked tree:

Architectural-Concerns:
...

Visual-Concerns:
...

Blockers:
...

END_TOOBA_WORKER_RESULT

After Result:

return to IDLE

do not self-issue another Task

keep Backend + Tooba Frontend + Original Shopeiva running where technically possible

END_TASK
