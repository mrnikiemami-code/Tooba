PIPELINE-PROTOCOL: BRIDGE-V2

TASK-ID: TB-P05-T014
PROJECT: Tooba
PHASE: P05 — Operational Surface Integration
CHANNEL: tooba-main
STATUS: ISSUED
TASK-TYPE: PRODUCT IMPLEMENTATION
WORKER-POLICY: ONE WORKER = ONE ACTIVE TASK

Title

Customer Address Book — Add Missing Backend Capability Behind Existing Shopeiva Address UI and Connect Checkout

Architect Context

TB-P05-T013 is Architect-ACCEPTED.

Locked rule:
BACKEND capability exists but Shopeiva lacks UI
→ add the minimum Shopeiva-compatible UI

SHOPEIVA meaningful capability exists but Backend lacks capability
→ add the missing Backend capability in the correct owning module
→ connect the existing Shopeiva UI to live backend truth

No redesign.

Earlier Customer Panel work left Addresses as an honest shell because backend capability did not exist.
Shopeiva already contains meaningful customer-address and checkout-address presentation.

Objective

Implement the smallest production-grade customer Address Book needed to make the existing Shopeiva address UI live and integrate it with the current checkout flow.

usable repeat-purchase experience

This is NOT a redesign task.

Critical UI Contract

SHOPEIVA STRUCTURE = LOCKED UI CONTRACT

Reuse the actual Shopeiva:

Customer Address page

Address cards/forms

Checkout address selection UI

DO NOT:

redesign address cards

create a new checkout layout

invent a new customer-panel structure

replace Shopeiva form language

Allowed:

live bindings

validation states

loading/error/empty states

minimum controls needed for real CRUD

Tooba blue token

Persian localization

Repository Recovery

Run first:

git rev-parse --show-toplevel
git fetch origin
git branch --show-current
git rev-parse HEAD
git rev-parse origin/main
git status --short --branch

Expected predecessor:
c6ba562a0a9e0344118c72263ae3535ec3639dde

Require:

branch = main

HEAD == origin/main

safe/known working tree

If repository state materially conflicts with Source of Truth, return RECOVERY_CONFLICT.

No force push.
No destructive reset.
No silent stash.
No history rewrite.

Required Reading

AGENTS.md

docs/PROJECT-STATE.md

docs/ROADMAP.md

docs/ai/TOOBA-PIPELINE-PROTOCOL.md

docs/ai/TOOBA-PIPELINE-CONTROLLER.md

docs/ai/TOOBA-RECOVERY-CONTEXT.md

docs/prompts/START-HERE-IF-CHATGPT-IS-LOST.md

docs/evidence/TB-P05-T003/

docs/evidence/TB-P04-T009/

docs/evidence/TB-P05-T008/

docs/evidence/TB-P05-T013/

actual Shopeiva customer address source

actual Shopeiva checkout/shipping address source

Part A — Ownership Decision

Before coding, determine the correct ownership model.

Expected semantics:
Customer Address

AddressId

Owner identity

Recipient name

Phone

Country

Province/State

City

Postal code

Address line(s)

Building/Unit optional

Label optional

IsDefault

CreatedAt

UpdatedAt

Use repository-consistent naming.
Do not overbuild a geographic platform.

Create:
docs/evidence/TB-P05-T014/01-address-domain-decision.md

Part B — Ownership / Privacy

Addresses are private customer data.
Authority must be server-side.
Use current authenticated session identity.

Forbidden:

request-supplied UserId as authority

frontend-only ownership checks

public arbitrary address lookup

Requirements:
A can CRUD A addresses
B cannot read/update/delete A addresses
missing actor → 401

Part C — Core CRUD

Minimum operations:

Create Address

List Current Customer Addresses

Get Current Customer Address

Update Address

Delete Address

Set Default Address

Rules:

one default address at most per owner

setting one default clears previous default atomically

deleting default selects no replacement unless existing product convention says otherwise

validation server-side

Part D — Address Validation

At minimum:

recipient name required

phone required and bounded

country required

city required

address line required

postal code bounded/validated generically

Do not hard-code Iranian-only domain rules into core model unless current Market architecture requires them.

Part E — Checkout Integration

Current checkout already works. Do NOT replace it.

Authenticated checkout can:

list saved addresses

select saved address

use selected address as shipping/recipient input

Critical:
Order/Checkout must preserve immutable address snapshot.

Flow:
Customer AddressBook Address
→ selected during Checkout
→ Checkout validates ownership
→ Checkout snapshots recipient/shipping fields
→ Order keeps historical snapshot

Do NOT make historical Orders depend on mutable AddressBook rows.

Part F — Guest Checkout

Do not break guest checkout.

Guest:
→ continues inline address flow

Authenticated customer:
→ may select saved address OR use inline/new address according to Shopeiva flow

Do not force account creation.
Do not redesign auth.

Part G — Save Address from Checkout

If Shopeiva already has a meaningful save-address control, connect it.
If not, do not invent a redesign-heavy flow.

Part H — Customer Panel

Replace previous Address honest shell with real data.

Reuse actual Shopeiva:

address cards

create/edit form

default badge/control

delete action

mobile structure

No redesign.

Part I — Checkout UI Fidelity

Use actual Shopeiva checkout address section.

Bind:

saved addresses

selected state

add/edit navigation if template supports it

default marker

inline guest path

No new checkout card family.

Part J — Customer/Party Boundary

Identity/User remains separate from Party/Organization.
Do not casually embed address persistence into Identity if it violates architecture.
Use correct owning module and contracts.
No cross-module ORM navigation.

Part K — Development Seed

Create deterministic Development-only address seed if needed.

Minimum:

one demo customer with 2+ addresses

one default

one alternate

idempotent

no production impact

no real personal data

Part L — Convergence Evidence

Create:
docs/evidence/TB-P05-T014/02-shopeiva-address-backend-convergence.md

Record:

Shopeiva feature

Backend capability before

Backend capability after

Binding path

Checkout integration

Minimal UI additions

Deferred capability

Out of Scope

geocoding

map picker

delivery-zone engine

shipping-rate engine

address SaaS validation

pickup network

fulfillment

Architecture Guardrails

Forbidden:

cross-module SQL join

foreign-module DbContext navigation

Order relying on mutable AddressBook row for historical truth

frontend ownership authority

request-supplied customer identity authority

All Tooba-owned public C#/TS Classes/Interfaces/Methods/Properties added or modified require meaningful Persian XML/TSDoc.

Evidence

Create:
docs/evidence/TB-P05-T014/

Required:
01-address-domain-decision.md
02-shopeiva-address-backend-convergence.md
03-customer-address-list-desktop.png
04-customer-address-create.png
05-customer-address-edit-default.png
06-customer-address-empty-state.png
07-checkout-saved-address-selection.png
08-checkout-selected-address-snapshot-proof.md
09-address-auth-isolation-proof.md
10-customer-address-mobile-390x844.png
11-checkout-mobile-address-390x844.png
12-address-data-map.md
13-shopeiva-address-fidelity.md
14-guest-checkout-regression-proof.md

Tests

Backend minimum:

create own address

list own addresses

get own address

update own address

delete own address

set default

single default invariant

A cannot read B

A cannot mutate B

missing actor 401

checkout rejects another actor AddressId

checkout accepts own AddressId

checkout creates immutable shipping snapshot

editing AddressBook after order does not change order snapshot

guest inline checkout still works

seed repeatability

Frontend:

address list mapping

empty state

create form

edit form

default state

delete

401/403

checkout saved-address selection

guest inline path

mobile rendering

Validation

Backend:
dotnet restore src/backend/Tooba.slnx
dotnet build src/backend/Tooba.slnx
dotnet test src/backend/Tooba.slnx

Require:
warnings = 0
errors = 0
failed = 0
skipped = 0

Frontend:
cd src/frontend
npm ci
npm run typecheck
npm run lint

Run all supported suites including:
grid
workspace
product workspace
storefront
cart
checkout/order
payment
seller
customer
admin
public merchandising
PDP
reviews
wishlist
addresses

Then:
npm run build
git diff --check
git status --short --branch

Acceptance Conditions

PASS requires:

real private AddressBook backend exists

correct ownership documented

server-side A/B isolation

CRUD works

one-default invariant enforced

Shopeiva Address UI reused

previous Address shell replaced with live data

Checkout can select authenticated customer's saved address

Checkout validates ownership

historical Order uses immutable shipping/recipient snapshot

changing/deleting AddressBook row cannot rewrite historical Order

guest checkout still works

no redesign

no cross-module SQL

desktop valid

mobile 390x844 valid

full validation green

Source of Truth

Update:

docs/PROJECT-STATE.md

docs/ROADMAP.md

docs/ai/TOOBA-RECOVERY-CONTEXT.md

docs/prompts/START-HERE-IF-CHATGPT-IS-LOST.md

Expected state before Architect review:
PIPELINE = BRIDGE-V2
TB-P05-T012 = ACCEPTED
TB-P05-T013 = ACCEPTED
TB-P05-T014 = AWAITING_ARCHITECT_ACCEPT
P05 = IN_PROGRESS

Worker must NOT mark Architect ACCEPT.

Preserve locked rules:
FAST CONNECT FIRST
SHOPEIVA STRUCTURE = LOCKED UI CONTRACT
BACKEND ↔ SHOPEIVA MUST CONVERGE BOTH WAYS
NO REDESIGN
NO FAKE BUSINESS DATA
SELL QUICKLY
P06 ALL CORE SURFACES LIVE

Git

git diff --check
git status --short --branch
git add ...
git commit -m "feat add live customer address book and checkout selection [TB-P05-T014]"
git push origin main
git fetch origin
git rev-parse HEAD
git rev-parse origin/main
git status --short --branch

Require:
HEAD == origin/main
working tree clean

Result Contract — BRIDGE-V2

Return through Bridge:

PIPELINE-PROTOCOL: BRIDGE-V2

BEGIN_TOOBA_WORKER_RESULT

Task-ID:
TB-P05-T014

Channel:
tooba-main

Status:
PASS | FAIL | BLOCKED | RECOVERY_CONFLICT

Summary:
...

Repository-Recovery:
...

Domain:

owning module:

address model:

owner identity:

default semantics:

order snapshot semantics:

Authorization:

actor source:

A own:

B access A:

missing actor:

checkout foreign AddressId:

Operations:

create:

list:

get:

update:

delete:

set default:

Checkout:

saved address list:

selection:

ownership validation:

snapshot:

guest path:

save-from-checkout:

Historical-Safety:

mutable AddressBook affects placed Order:

snapshot test:

Shopeiva-Reuse:

address list:

create/edit:

default UI:

checkout address:

mobile:

redesign:

minimal additions:

Seed:

environment:

deterministic:

idempotent:

address count:

Evidence:
...

Tests:
...

Validation:
...

Architecture:

cross-module SQL:

foreign DbContext navigation:

Order mutable Address reference:

frontend ownership authority:

Source-of-Truth:

T012:

T013:

T014:

P05:

Git:
...

Architectural-Concerns:
...

Visual-Concerns:
...

Blockers:
...

END_TOOBA_WORKER_RESULT

SYSTEM-BRIDGE-ALERT is NOT a Task Result.
Do not self-issue another Task.
After Result, control returns through Bridge to Architect for ACCEPT / REPAIR / BLOCK.

END_TASK
