Tooba — TB-P04-T010 — Live Payment Boundary

BEGIN_TOOBA_CURSOR_TASK_V1

Protocol-Version: 1
Task-ID: TB-P04-T010
Phase: P04 — Experience Foundation
Type: Live Commerce Payment Boundary
Repository: https://github.com/mrnikiemami-code/Tooba
Branch: main
Execution-Mode: PIPELINE

Architect Decision

TB-P04-T009 is ACCEPTED.

The live sellable path now reaches:

Storefront
→ PDP
→ Cart
→ Checkout
→ Order
→ PendingPayment

This task connects the real Payment boundary.

Locked commercial rule remains:

PRESERVE SHOPEIVA
→ MINIMUM UI CHANGE
→ CONNECT TOOBA BACKEND
→ SELL QUICKLY
→ HARDEN/POLISH LATER

Do NOT redesign Checkout/Confirmation.

Do NOT build unrelated payment features.

Repository Recovery

Run first:

git rev-parse --show-toplevel
git fetch origin
git branch --show-current
git rev-parse HEAD
git rev-parse origin/main
git status --short --branch

Expected predecessor:

902801a98fab7d9bb7d31d0cd6a776b75d8ee304

Require:

branch = main
HEAD == origin/main
safe/known working tree

No force push.
No destructive reset.
No silent stash.
No history rewrite.

Objective

Connect real Payment initiation and payment-result handling for an existing PendingPayment Order / CheckoutGroup.

Required flow:

Order PendingPayment
→ initiate Payment
→ provider abstraction
→ redirect / provider handoff
→ callback/result verification
→ durable Payment state
→ durable Order payment state
→ customer result page

No fake paid state.

No frontend-only payment state.

No provider secret exposed to frontend.

Existing Architecture — REUSE FIRST

Recover and use the existing P03 Payment implementation.

Do NOT create a second Payment model.

Prior accepted architecture includes a durable:

Payment → Order

relationship.

Inspect existing:

Payment module
payment aggregate/entities
application contracts
provider/gateway abstractions
Order transition hooks
idempotency/concurrency handling
existing tests

Extend minimally.

Initial Provider Scope

For T010, implement a provider boundary that is production-ready in shape.

If a real PSP provider is NOT already configured:

use a deterministic sandbox/dev provider adapter

Requirements:

same interface shape as future real PSP
server-side only
no fake frontend success
callback/result must still pass through backend verification

Do NOT hard-code Tooba business logic to one Iranian gateway.

Provider must be replaceable.

Do NOT scrape or simulate a third-party website.

Payment States

Use explicit durable states.

At minimum support meaningful equivalents of:

Created / Pending
RedirectRequired or ProviderPending
Succeeded
Failed
Cancelled if supported

Do not collapse all failures to generic success=false without state.

State transitions must be validated.

No double-success side effect.

Payment Initiation

From the real Order / CheckoutGroup confirmation:

customer clicks پرداخت
→ frontend calls backend payment-initiation endpoint
→ backend verifies order is payable
→ backend creates/reuses appropriate Payment attempt
→ provider adapter returns redirect/action

Do not trust the frontend amount.

Backend must use durable Order/payment snapshot truth.

Required checks:

Order exists
Order is PendingPayment/payable
currency supported
payable amount > 0 unless legitimate zero-pay case
not already paid
not cancelled
Idempotency

Payment initiation must be safe against:

double click
browser retry
network retry
same idempotency key

Do not create unlimited duplicate payment attempts for identical retry.

If multiple legitimate attempts are supported after failure/cancel:

model them explicitly

Do not confuse retry replay with new business attempt.

Provider Callback / Result

Implement the backend callback/result boundary.

Requirements:

provider transaction/reference captured
server verifies provider result
amount/currency/order correlation verified
duplicate callback safe
out-of-order callback safe where possible
failed verification does not mark paid

Do NOT accept:

?success=true

from frontend as payment truth.

The browser redirect/result page is presentation only.

Backend verification is authoritative.

Order State Transition

On verified successful Payment:

Payment → Succeeded
Order → appropriate paid/confirmed payment state

Use existing Order transition rules.

Do not modify unrelated fulfillment state.

On failed Payment:

Order remains unpaid/payable according to current model

Customer may retry if allowed.

Do not create duplicate Order.

Multi-Seller / CheckoutGroup

Payment is against the correct commercial payable scope.

Recover current P03 design and preserve it.

If CheckoutGroup contains multiple Seller Orders:

one customer payment may cover the CheckoutGroup total

Do NOT independently charge each seller unless existing architecture explicitly says so.

Document exactly:

Payment owner/scope
CheckoutGroup relation
Seller Order relation

No seller settlement/payout in this task.

Amount Integrity

Payment amount comes only from backend durable Order/Checkout snapshot.

No frontend calculation.

Verify provider result against expected:

amount
currency
payment/order correlation
provider transaction id

No:

Product.Price
live recompute from Product
frontend submitted payable amount as authority
Customer UI — SHOPEIVA PRESERVATION

Use Shopeiva payment/confirmation visual language.

Do NOT redesign.

On Order confirmation/PendingPayment page add the minimal real action:

پرداخت سفارش

Preserve:

order summary
amount
current status
payment CTA
result feedback
mobile behavior

Add real result states:

payment pending
payment successful
payment failed
retry available when legitimate

No fake success animation before backend confirms.

Payment Result Route

Create/adapt a non-indexable route such as existing architecture prefers:

/payment/result

or the nearest existing route.

Do not put secrets in query strings.

A non-secret payment/order correlation token/reference may be used if required.

The result page must refetch authoritative backend Payment/Order state.

Security

Mandatory:

provider secret/config server-side
no payment credentials in frontend bundle
callback verification server-side
structured logs redact secrets
guest secret not exposed
payment reference safe for UI

Do not log full sensitive provider payloads without redaction.

Do not store card data.

Tooba is not a card-data processor in this task.

Observability

Add/ensure useful telemetry for:

payment initiation
provider handoff
callback received
callback verified
payment succeeded
payment failed
duplicate callback
order transition

Use existing OpenTelemetry/logging foundation.

Include correlation identifiers safe for diagnostics.

Remember:

Technical Log != Business Audit

If existing Business Audit seam exists for payment status transition, use it.
Do not invent a massive audit subsystem here.

Customer-Safe Errors

Map failures to Persian customer-safe messages.

Examples:

امکان شروع پرداخت در حال حاضر وجود ندارد.
پرداخت تأیید نشد. در صورت کسر وجه، وضعیت را دوباره بررسی کنید.
این سفارش قبلاً پرداخت شده است.

Technical/provider error detail must not be primary customer text.

SEO / Privacy

Payment and result routes:

noindex

No sensitive information in page metadata.

Live Evidence

Store under:

docs/evidence/TB-P04-T010/

Required:

01-order-pending-payment-desktop.png
02-payment-initiation-desktop.png
03-payment-provider-sandbox-handoff.md
04-payment-success-result-desktop.png
05-payment-success-result-mobile-390x844.png
06-payment-failure-result.png
07-duplicate-initiation-proof.md
08-duplicate-callback-proof.md
09-payment-order-state-proof.md

If sandbox provider uses an internal development handoff page, make it visually explicit that it is a sandbox/dev provider and not a real production bank page.

Do not fake an external bank brand.

Live Flow Evidence

Create:

docs/evidence/TB-P04-T010/live-payment-flow.md

Record:

CheckoutGroupId
OrderId/reference
PaymentId
expected amount
currency
provider
provider reference
initiation result
callback/result
Payment final state
Order final payment state
duplicate initiation behavior
duplicate callback behavior

Redact secrets.

Architecture Evidence

Create:

docs/evidence/TB-P04-T010/payment-architecture-map.md

Show:

Customer UI
→ Host/Public Payment API
→ Payment application
→ Payment Provider Interface
→ Provider Adapter
→ callback verification
→ Payment state
→ Order state transition

Prove:

frontend never marks paid
provider is replaceable
amount from durable backend snapshot
no cross-module SQL join
Focused Tests

Add/verify tests for:

initiate payable order
reject already-paid order
reject invalid amount/scope
duplicate initiation idempotency
successful provider verification
failed provider verification
amount mismatch rejected
currency mismatch rejected
duplicate callback idempotency
successful payment transitions Order once
failed payment does not mark Order paid
multi-seller CheckoutGroup payment scope
Full Validation

Backend:

dotnet restore src/backend/Tooba.slnx
dotnet build src/backend/Tooba.slnx
dotnet test src/backend/Tooba.slnx

NO filters.

Require:

warnings = 0
errors = 0
failed = 0
skipped = 0

Frontend:

cd src/frontend
npm ci
npm run typecheck
npm run lint
npm run test:grid
npm run test:workspace
npm run test:product-workspace

Run:

storefront tests
cart tests
checkout/order tests
new payment tests

Then:

npm run build
git diff --check
git status --short --branch
Acceptance Conditions

PASS requires:

real Order initiates real backend Payment
provider boundary replaceable
sandbox/dev provider valid if no real PSP configured
frontend cannot self-declare success
callback/result verified server-side
amount/currency correlation verified
duplicate initiation safe
duplicate callback safe
Payment durable state correct
Order payment state correct
failed Payment does not mark paid
multi-seller CheckoutGroup payment scope preserved
desktop works
mobile works
Shopeiva visual language preserved
no secrets leaked
full validation green
Deferred — NOT T010

Do not implement:

refund
partial refund
capture/void complexity
seller settlement
payout
wallet
gift card
multiple real PSPs
advanced payment routing
B2B credit terms

Those remain later work.

Architecture Recheck

Must remain true:

Payment != Order
Payment Provider != Payment domain
Order snapshot != live Product truth
frontend != payment authority
Product != Offer
Offer != Price

Carry existing deferred concern:

Cart replace-hold release→reserve competition window
SoT

Update:

docs/PROJECT-STATE.md
docs/ROADMAP.md
docs/ai/TOOBA-RECOVERY-CONTEXT.md
docs/prompts/START-HERE-IF-CHATGPT-IS-LOST.md

Record:

TB-P04-T009 = ACCEPTED
TB-P04-T010 = AWAITING_ARCHITECT_ACCEPT
P04 = IN_PROGRESS

Keep locked:

Preserve Shopeiva
Minimal UI change
Connect Tooba backend
Sell fast
P06 core integration deadline
Save Envelope VERBATIM

Save:

docs/ai/tasks/TB-P04-T010.task.md
Commit / Push

After work:

git diff --check
git status --short --branch
git add ...
git commit -m "feat connect live payment boundary [TB-P04-T010]"
git push origin main
git fetch origin
git rev-parse HEAD
git rev-parse origin/main
git status --short --branch

Require:

HEAD == origin/main
Result Contract

Return:

BEGIN_TOOBA_CURSOR_RESULT_V1

Protocol-Version: 1
Task-ID: TB-P04-T010
Phase: P04 — Experience Foundation
Status: PASS | BLOCKED | RECOVERY_CONFLICT

Summary:
...

Repository-Recovery:
- ...

Payment-Scope:
- CheckoutGroup:
- Seller Orders:
- Payment owner/scope:
- amount:
- currency:

Provider:
- interface:
- adapter:
- real/sandbox:
- secrets server-side:
- redirect/handoff:

Initiation:
- PaymentId:
- idempotency:
- duplicate behavior:
- already-paid behavior:

Callback:
- verification:
- provider reference:
- amount verification:
- currency verification:
- duplicate callback:
- failed verification:

States:
- initial Payment:
- successful Payment:
- failed Payment:
- Order before:
- Order after success:
- Order after failure:

UI:
- pending:
- CTA:
- success:
- failure:
- retry:
- mobile:
- Shopeiva preserved:

Security:
- frontend provider secret:
- frontend marks paid:
- sensitive logging:
- noindex:

Observability:
- traces/logs:
- business audit:

Visual-Evidence:
- 01:
- 02:
- 03:
- 04:
- 05:
- 06:
- 07:
- 08:
- 09:
- Live flow:
- Architecture map:

Tests:
- initiation:
- already-paid:
- amount mismatch:
- currency mismatch:
- duplicate initiation:
- provider success:
- provider failure:
- duplicate callback:
- order transition:
- multi-seller scope:

Validation:
- backend restore:
- backend build:
- warnings:
- errors:
- backend passed:
- failed:
- skipped:
- npm ci:
- typecheck:
- lint:
- grid:
- workspace:
- product-workspace:
- storefront:
- cart:
- checkout/order:
- payment:
- frontend build:
- git diff --check:

Architecture:
- frontend payment authority:
- cross-module SQL join:
- Product.Price:
- Product.Stock:

Git:
- commit:
- push:
- final HEAD:
- origin/main:
- synchronized:
- final status:

Source-of-Truth:
- TB-P04-T009: ACCEPTED
- TB-P04-T010: AWAITING_ARCHITECT_ACCEPT
- P04: IN_PROGRESS
- Recovery-ready:

Architectural-Concerns:
- ...

Visual-Concerns:
- ...

Blockers:
- ...

END_TOOBA_CURSOR_RESULT_V1

After RESULT, surface live Payment screenshots/evidence in this SAME Architect-controlled session.

CRITICAL — RESULT DOES NOT END THIS AGENT SESSION

After RESULT:

PIPELINE_STATE: WAITING_FOR_ARCHITECT_IN_SAME_SESSION

Then:

DO NOT CLOSE OR LEAVE THIS SESSION.
WAIT HERE FOR THE NEXT AUTHORIZED ENVELOPE.

Do not finish.
Do not exit.
Do not leave PIPELINE.
Do not self-advance.
Do not invent TB-P04-T011.

RESULT = CONTROL RETURNED TO ARCHITECT.
RESULT != WORKFLOW FINISHED.

Cursor PASS is not Architect ACCEPT.

Leaving or ending the pipeline after RESULT is a protocol violation.

END_TOOBA_CURSOR_TASK_V1
