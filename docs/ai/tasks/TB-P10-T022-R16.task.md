PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_TASK

Task-ID:
TB-P10-T022-R16

Parent-Task:
TB-P10-T022-R15

Channel:
tooba-main

WorkerId:
tooba-worker-01

AgentType:
cursor

Status:
ISSUED

Appearance-USER_VISUAL_ACCEPTED=YES
Builder-USER_VISUAL_ACCEPTED=NO

Title:
Amazing Offers Runtime Resolver + Real Test Data — Active/Future/Expired Campaigns, Availability Filtering, Deterministic Selection

Architect Review

TB-P10-T022-R15 is ACCEPTED.

Foundation is correct:

MerchandisingPromotionType table/master data exists.

AMAZING is seeded idempotently with FA/EN display text.

MerchandisingCampaign is separate from checkout PromotionDefinition.

Campaign translations exist.

SellerOffer membership exists.

normal price truth remains AuthoredPrice.

stock truth remains StockPosition.

promo-price storage is correctly DEFERRED instead of introducing an unsafe scalar amount.

lifecycle active state is derived.

Store boundary is enforced by application/domain layer because SellerOffer has no StoreId column.

no Builder/Storefront/UI work leaked into R15.

This task is the NEXT PHASE:
RUNTIME READ MODEL + QUERY/RESOLVER + REAL DEVELOPMENT TEST DATA.

Do NOT integrate Builder yet.
Do NOT add the "پیشنهاد شگفت‌انگیز" option yet.
Do NOT change Product Showcase UI yet.
Do NOT implement Admin Campaign management UI yet.
Do NOT touch P11/Admin roadmap.
Do NOT invent a promo-price scalar.

Canonical Start

Last Architect-accepted:
TB-P10-T022-R15

Last Implementation:
TB-P10-T022-R15

Expected previous HEAD:
afe628e33558161a77394f7029947f72ec6d2973

User-work ancestor:
18ca10c9

Locks:
LOCK-SF-001…398

Appearance:
YES

Builder:
NO

Worker Protocol

No heartbeat.
No polling.
No IDLE loop.

After canonical Result through Bridge:
STOP completely.

1. Recovery Preflight

Verify:

branch main

HEAD==origin/main

R15 migration/schema present

AMAZING master seed present

18ca10c9 ancestor

user work preserved

unrelated stashes/.tmp untouched

Evidence:
docs/evidence/TB-P10-T022-R16/recovery-start.md

Unexpected tracked divergence => RECOVERY_CONFLICT.

2. Runtime Read Model — Strong but Minimal

Implement a dedicated merchandising campaign read model for Storefront/Builder consumption later.

The read model must NOT expose EF entities directly.

At minimum include:

Campaign:

CampaignId

PromotionTypeCode

localized Title

localized Subtitle nullable

localized BadgeText nullable

StartAt

EndAt

Priority

IsTeasing / lifecycle projection if supported by current campaign state + time

derived RemainingDuration or EndAt only; do NOT persist countdown

Members:

SellerOfferId

Product/Variant identity needed by existing ProductCard projection

SortOrder

current marketable/availability status

canonical current selling price from existing pricing pipeline

canonical inventory availability from existing inventory pipeline

existing order-limit information where available

image/title/slug/product projection required by existing Product Showcase resolver

Do NOT duplicate ProductCard DTO logic if an existing projection can be reused.

Evidence:
docs/evidence/TB-P10-T022-R16/read-model.md

3. Active Campaign Selection — Deterministic

R15 intentionally allows multiple campaigns.

Define deterministic selection for:
Store + PromotionTypeCode + now

Required ordering:

runtime-active only

highest Priority

latest StartAt

stable deterministic tiebreaker (e.g. Id) according to project conventions

Do NOT rely on database natural row order.

If multiple active AMAZING campaigns exist, resolver must always choose the same winner.

Document:
docs/evidence/TB-P10-T022-R16/active-selection.md

4. Future / Teasing Query

Add a separate query path for future campaigns.

Goal:
support a later "فردای شگفت‌انگیز" / teasing experience without leaking it into active results.

Required:

future Published/Scheduled campaign

StartAt > now

ordered by nearest StartAt, then Priority

locale-aware text

never returned by active resolver

No UI yet.

5. Expired / Archived Exclusion

Prove:

EndAt <= now => not active

Archived => not active

Draft => not active

future => not active

Disabled equivalent if project has it => not active

No background job required for truth.

6. Availability / Marketability Filtering

The active member resolver must only return products/offers that are actually eligible for Storefront display.

Reuse canonical truth.

At minimum filter/exclude:

unavailable/invalid SellerOffer

out-of-stock according to StockPosition/current availability pipeline

non-marketable product/variant state

cross-store mismatch

unpublished/invisible product if current Product Showcase rules already filter it

Do not copy availability logic.
Reuse existing query/projection/service.

Document exact reused path:
docs/evidence/TB-P10-T022-R16/availability-filter.md

7. Price Projection — NO DUPLICATION

Use canonical pricing.

For this phase:

return normal canonical selling price from existing AuthoredPrice/pricing resolver

campaign promotional override remains deferred

do NOT fabricate discount percent

do NOT fabricate compare-at price

do NOT create PromoAmount

do NOT infer discount if canonical data cannot prove it

The read model may expose future extension fields as nullable only if current contract style supports it cleanly.

Document:
docs/evidence/TB-P10-T022-R16/price-projection.md

8. Quantity / Order Limits

Reuse current Offer/order-limit semantics.

Project into read model only if needed for future Storefront:

MinOrderQuantity if canonical

MaxOrderQuantity if canonical

available quantity only if existing public projection already exposes it safely

Do NOT introduce new campaign allocation yet.

9. Locale / Translation Resolution

Resolve campaign text by requested Page/route locale using existing fallback rules.

Required proof:

fa -> FA campaign translation

en -> EN campaign translation

missing requested locale -> configured fallback behavior

no section-owned locale

no hardcoded Persian in resolver

Product translations follow existing Product Showcase locale behavior.

Evidence:
docs/evidence/TB-P10-T022-R16/localization.md

10. Store / Edition Scope

Runtime queries must always be Store-scoped.

Marketplace:

active campaign may contain multiple sellers

all returned SellerOffers must belong to valid current Store context

Single-Store:

same query/model

no multi-store UI

no cross-store leakage

Because SellerOffer has no StoreId column:

document the authoritative current path that proves SellerOffer belongs to caller Store/tenant

do not trust a caller-provided StoreId alone without existing tenant/store boundary checks

Evidence:
docs/evidence/TB-P10-T022-R16/store-scope.md

11. Idempotent Development/Test Seed — REQUIRED

Create REAL development/test data using current database entities, not frontend fake data.

Seed must be explicitly Development/Test scoped.
It must NOT silently populate production.

Create at minimum:

A. Active AMAZING campaign

Published

currently active window

high priority

FA + EN translations

5+ real SellerOffers

explicit member SortOrder

B. Second active AMAZING campaign

lower priority

proves deterministic winner

C. Future/Teasing AMAZING campaign

starts later

FA + EN translation

D. Expired AMAZING campaign

EndAt in past

E. Archived or Draft campaign

Member conditions should include where existing data permits:

multiple real products

multiple sellers in Marketplace dev data

one out-of-stock member that MUST be filtered

one invalid/non-marketable member if safe to seed

one offer with existing order-limit behavior

one explicitly ordered first member

Seed must be:

idempotent

deterministic

easy to reset via existing dev seed conventions

no duplicate campaigns on restart

Evidence:
docs/evidence/TB-P10-T022-R16/test-data.md

12. Seed Time Stability

Do NOT create seed windows that expire permanently based on a hardcoded 2026 timestamp.

For Development bootstrap:

derive active/future/expired windows relative to current canonical Instant
OR

use deterministic refresh/upsert semantics that keep intended scenario valid each bootstrap

Tests themselves must use injected/fixed clock where possible.

Avoid flaky time-dependent tests.

13. Resolver API / Internal Service

Implement the minimal internal/application query surface needed by the next Builder task.

Required capabilities conceptually:

ResolveActiveByTypeAsync(storeContext, typeCode, locale, now, take)

ResolveFutureByTypeAsync(...)

ResolveCampaignMembersAsync(campaignId, locale, take/page)

Use project naming conventions.

Return stable DTO/read model.

No public unauthenticated new API endpoint is required unless current Builder/Storefront architecture necessarily consumes Host APIs.
If endpoint is required by existing architecture:

make it read-only

Store-scoped

locale-aware

paginated/limited

no raw internal implementation leakage

Do NOT build generic campaign Admin CRUD endpoint here unless necessary for dev seed verification.

14. Pagination / Limit

Avoid "load all members".

Support:

take/limit

deterministic ordering

safe upper bound consistent with existing Product Showcase source conventions

If paging is already standardized, reuse it.

No full-table member load.

15. Query Performance

Validate generated query / SQL shape.

Required:

campaign lookup uses Store + Type + lifecycle/time index path

membership lookup uses CampaignId + SortOrder

no N+1 for products/prices/translations

inventory filtering does not cause per-row query

price resolution does not cause per-row query where existing bulk resolver exists

no full SellerOffer scan

Capture representative SQL/query plan if project tooling supports it.

Evidence:
docs/evidence/TB-P10-T022-R16/query-performance.md

16. Cache Strategy — Minimal

Do not prematurely add a complex cache.

If existing Product Showcase/source resolver has canonical cache:

reuse or prepare compatible key shape

At minimum define cache identity for next phase:
Store + PromotionType + Locale + campaign/version/window

Invalidation considerations:

campaign publish/update

membership reorder/add/remove

translation update

stock/price changes

If cache is NOT implemented in this task, document why and extension point.

No polling.

17. Runtime / Data Verification

Using real dev DB:

Verify:

AMAZING type exists

active high-priority campaign exists

lower-priority overlapping campaign exists

active resolver chooses high-priority deterministically

future campaign excluded from active

future resolver finds future campaign

expired excluded

archived/draft excluded

out-of-stock member excluded

ordered members stable

FA translation correct

EN translation correct

normal price comes from canonical pricing path

inventory comes from canonical inventory path

no fake promo price

no duplicate seed on second startup

Evidence:
docs/evidence/TB-P10-T022-R16/runtime-data-proof.md

18. Focused Tests

Required:

active campaign selection deterministic

priority wins

StartAt tiebreaker

stable Id tiebreaker

future excluded from active

expired excluded

archived excluded

draft excluded

future resolver works

Campaign members ordered

duplicate membership impossible

out-of-stock filtered

invalid/non-marketable filtered

cross-store leakage rejected

FA translation

EN translation

fallback locale

canonical price projection

canonical inventory projection

no promo scalar

seed idempotent

active seed stable across restart

query count/N+1 guard where available

migration/schema regression

critical existing Product Showcase source tests remain green

recovery-staleness PASS

git diff --check PASS

18ca10c9 ancestor PASS

HEAD==origin/main PASS

Keep tests focused and meaningful.

19. Anti-Pattern Gate

Reject:

Builder UI implementation

Storefront rail implementation

Campaign Admin UI

Offer.IsAmazing

new promotion enum identity

checkout PromotionDefinition reuse

fake frontend data

hardcoded forever-expiring dev timestamps

scalar PromoAmount

fabricated discount percent

duplicate inventory truth

per-member N+1 price query

per-member N+1 inventory query

full SellerOffer scan

full campaign-member load without limit

per-second jobs

polling

raw GUID UI

P11 work

user-work overwrite

Evidence:
docs/evidence/TB-P10-T022-R16/antipattern-scan.md

20. Locks

Add after LOCK-SF-398:

LOCK-SF-399 Active merchandising campaign selection is deterministic by runtime eligibility, Priority, StartAt and stable tiebreaker; database natural order is never relied upon.
LOCK-SF-400 Future/teasing merchandising campaigns are queried separately and never leak into active Storefront sources before StartAt.
LOCK-SF-401 Campaign member visibility reuses canonical SellerOffer/Product marketability, Pricing and StockPosition availability truth; campaign membership alone never makes an unavailable offer storefront-visible.
LOCK-SF-402 Development/test merchandising campaign seeds are idempotent, environment-scoped and time-stable; production is never populated by demo campaign bootstrap.
LOCK-SF-403 Merchandising read models may expose only canonical pricing/inventory facts; fabricated discount, compare-at, sold-percentage or campaign promo price values are forbidden.
LOCK-SF-404 Merchandising runtime queries are Store-scoped, locale-aware, ordered and bounded; no full-offer scan or N+1 pricing/inventory query is allowed.

Do not renumber existing locks.

21. Completion State

At completion:

Phase:
P10 / Builder Acceptance + Promotion Engine Runtime Foundation

Last Architect-accepted before review:
TB-P10-T022-R15

Last Implementation:
TB-P10-T022-R16

State:

core campaign schema exists

AMAZING type exists

runtime resolver exists

active/future selection exists

real Development/Test campaigns seeded

availability filtering works

localization works

NO Builder source UI yet

NO Storefront Product Showcase integration yet

NO Campaign Admin UI

promo-price override still deferred

Appearance YES

Builder NO

P11 retained for later

main

HEAD==origin/main

18ca10c9 ancestor

user work preserved

locks through LOCK-SF-404

Do NOT write Worker IDLE.

After canonical Result through Bridge:
STOP completely.

Acceptance

PASS only if:

resolver uses real campaign foundation

selection is deterministic

future/expired states are correct

real DB seed exists and is idempotent

invalid/out-of-stock offers are filtered through canonical truth

locale resolution works

price/inventory are canonical

no fake promo pricing

query is bounded and no N+1

no Builder/UI/Storefront feature leakage

user work preserved

Result delivered through Bridge

worker stops

Result Contract

Return ONLY canonical BRIDGE-WAKE-V1 Result with:

Summary
Applicable-Locks
Recovery-Start
Read-Model
Active-Selection
Future-Teasing
Availability-Filter
Price-Projection
Quantity-Projection
Localization
Store-Scope
Test-Data
Seed-Time-Stability
Resolver-API
Pagination
Query-Performance
Cache-Strategy
Runtime-Data-Proof
Focused-Validation
AntiPattern-Scan
Lock-Registry
Recovery-SoT
Git
Architectural-Concerns
Blockers

Do not include Worker-IDLE.

After sending Result through Bridge:
STOP completely.

END_TOOBA_TASK