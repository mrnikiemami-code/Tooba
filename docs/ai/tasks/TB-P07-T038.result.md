PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_RESULT

Task-ID:
TB-P07-T038

Status:
PASS

Summary:
Admin Product grid now has separate دسته اصلی (leaf-only) and نمایش در دسته‌های دیگر (chips, max 3 + +N from payload, no extra API). Grid DTO uses primaryCategoryName + additionalCategoryNames[] with batched leaf names (no ancestor path overfetch). Category→Products preserves T037 actions. Category tree selection follows route, expands ancestors, scrolls selected node. Recovery SoT → T038. USER_VISUAL_ACCEPTED=NO.

Product-Grid-Columns:
Separate columns دسته اصلی / نمایش در دسته‌های دیگر in product-list.tsx; categorySummary kept as leaf-joined compatibility only.

Primary-Category-Cell:
primaryCategoryName = localized leaf only; no full path in cell/tooltip/subtitle.

Additional-Category-Chips:
additional-category-chips-cell.tsx — max 3 chips + Popover +N from additionalCategoryNames[]; zero => —.

Query-Performance:
BuildListItemsForProductIdsAsync: one LoadNamesAsync batch for category IDs; removed BuildCategoryPathMapAsync for grid list projection.

Category-Products-Regression:
T037 intact: Primary no trash, L1/L2 blocked, display assign/remove works; panel uses same leaf/chip columns.

Tree-Selection:
selectedKeys from route categoryId when node exists; collectAncestorIds expand; AppCategoryTree scrollIntoView on selected.

Tree-DeepLink:
Refresh/back-forward/deep-link keep route-synced selection; invalid category clears stale tree selection.

Tree-Search:
Search expand + route selection preserved after clear (contract tests).

Localization:
fa RTL column headers; leaf names from fa-IR localized texts.

Architecture:
L3-only assignment unchanged; AppDataGrid/AppCategoryTree canonical; Shopeiva untouched.

Recovery-SoT:
Last Architect-accepted TB-P07-T037; Current TB-P07-T038; guard passes.

Validation:
Host focused 19/0; MigrationRunner 4/0; FE tsc/lint/build green; relevant tests 55/0; guard 3/0; live-proof PASS.

Live-Verification:
POST /v1/admin/products/query — 50 rows leaf-only fields, no path separators; +N UI payload-driven (contract).

Runtime:
Host :5088 OK; FE :3000 OK; Shopeiva :3001 OK.

Evidence:
docs/evidence/TB-P07-T038/

Git:
HEAD=a12383eb1ec75678c84b0c76b370970ab2518126
origin/main=a12383eb1ec75678c84b0c76b370970ab2518126
HEAD==origin/main

Architectural-Concerns:
None blocking.

Visual-Concerns:
USER_VISUAL_ACCEPTED=NO — human should confirm chips/+N and tree selection in browser.

Blockers:
None.

END_TOOBA_RESULT
