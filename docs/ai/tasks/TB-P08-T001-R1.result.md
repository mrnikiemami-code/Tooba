PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_WORKER_RESULT

Task-ID:
TB-P08-T001-R1

Parent-Task:
TB-P08-T001

Channel:
tooba-main

Status:
PASS

Worker:
tooba-worker-01

Bridge-UUID:
d4ee2590-455b-4e49-9621-3ad02dacdf7c

Summary:
Replaced T001 in-memory SupportedLocaleRegistry with DB-backed Localization module (`localization.languages`). Preserved Order Detail status-card repair and accepted UI. ContentArticle.Locale validates against persisted active languages. Idempotent fa/en bootstrap. USER_VISUAL_ACCEPTED=NO.

Order-Status-Preservation:
admin-order-status-cards.ts unchanged semantics — order vs payment lifecycle independent; T042/T043 layout preserved; FE regression tests 6/6 pass.

Persisted-Language-Model:
Modules/Localization (Domain/Application/Infrastructure); entity Language with code, urlPrefix, display/native names, direction, culture, calendar policy, active/default, sortOrder, CreatedAt/UpdatedAt; migration InitialLocalization.

Language-Invariants:
Unique code/urlPrefix; exactly one active default; default must be active; cannot deactivate default without replacement; stable LanguageErrorCodes; reference guard for Content articles.

Bootstrap:
LanguageDirectory.BootstrapAsync seeds fa-IR (RTL/Jalali/default) + en-US (LTR/Gregorian) when empty; idempotent; LanguageBootstrapHostedService on startup.

Registry-Source-of-Truth:
Deleted SupportedLocaleRegistry; ILanguageDirectory + LocalizationDbContext authoritative; frontend SUPPORTED_LOCALE_DEFINITIONS marked routing bootstrap fallback only; Admin loads /v1/admin/languages.

Language-Admin:
LocaleAdminEndpoints async CRUD (GET/POST/PUT/PATCH); language-list grid adds urlPrefix column; mapSupportedLocale handles API fields.

Content-Language-Reference:
ContentDirectory.EnsureActiveLanguageCodeAsync on create/update; ContentLanguageReferenceGuard; scalar Locale preserved; no translation rows.

Date-Direction-Policy:
RTL/LTR + Jalali/Gregorian persisted per language; existing jalali.ts/routing.ts reused; UTC storage unchanged.

Focused-Validation:
BE LanguageDirectoryPersistenceTests 5/5; FE node:test order-status + locales 7/7; recovery guard 3/3; git diff --check clean.

Migration:
20260901220000_InitialLocalization.cs compiles; Host build artifacts/p08-t001-r1-build OK.

Recovery-SoT:
P08; last accepted TB-P07-T043; TB-P08-T001 under repair; current repair (none); last implementation TB-P08-T001-R1; USER_VISUAL_ACCEPTED=NO.

Git:
HEAD=951807d5e809f5f561b70acb971562aac34fb55a
origin/main=951807d5e809f5f561b70acb971562aac34fb55a
HEAD==origin/main

Architectural-Concerns:
Frontend routing still uses static bootstrap array until storefront loads languages API (future); code/urlPrefix immutability after reference not yet enforced in Update path UI.

Blockers:
None.

END_TOOBA_WORKER_RESULT
