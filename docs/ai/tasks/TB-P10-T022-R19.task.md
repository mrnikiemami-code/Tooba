PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_TASK

Task-ID:
TB-P10-T022-R19

Parent-Task:
TB-P10-T022-R18

Channel:
tooba-main

WorkerId:
tooba-worker-01

AgentType:
cursor

Status:
ISSUED

Appearance-USER_VISUAL_ACCEPTED=YES
Builder-USER_VISUAL_ACCEPTED=NO

Title:
Campaign-Aware Cart Quote Integrity — Server-Authoritative Amazing Price from Add-to-Cart through Checkout

Architect Review

TB-P10-T022-R18 is ACCEPTED.

Accepted state:

campaign promo price is now modeled inside canonical Pricing via:
PriceQualifierKind.MerchandisingCampaign

QualifierKey = CampaignId

no scalar PromoAmount

Preview and published SSR show real promo prices

discount semantics are derived

Development promo-price seeds exist

TOOBA-CAPABILITY-MAP exists

critical remaining commercial gap:
Cart/Checkout still resolves Base-only price and does not yet carry/resolve merchandising campaign context.

This task closes THAT gap only.

Do NOT redesign Cart.
Do NOT redesign Checkout.
Do NOT add Campaign Admin UI.
Do NOT add Builder features.
Do NOT touch P11.
Do NOT trust any client-submitted price.

Canonical Start

Last Architect-accepted:
TB-P10-T022-R18

Last Implementation:
TB-P10-T022-R18

Expected previous HEAD:
2941562023ffb1da6a2367402b7df19091e8bbf4

User-work ancestor:
18ca10c9

Locks:
LOCK-SF-001…416

Appearance:
YES

Builder:
NO

Worker Protocol

No heartbeat.
No polling.
No IDLE loop.

After canonical Result through Bridge:
STOP completely.

1. Recovery Preflight

Verify:

branch main

HEAD==origin/main

R18 campaign price qualifier present

R17 PromotionCampaign source intact

18ca10c9 ancestor

user work preserved

unrelated stashes/.tmp untouched

Evidence:
docs/evidence/TB-P10-T022-R19/recovery-start.md

Unexpected tracked divergence => RECOVERY_CONFLICT.

2. Narrow Discovery — Cart/Checkout Pricing Path ONLY

Do NOT re-audit whole repo.

Trace exact server-authoritative path from:

Storefront ProductCard
→ add-to-cart request
→ Cart line creation/update
→ cart quote/reprice
→ shipping
→ final checkout commit
→ Order line pricing

Document:

request DTOs

Cart line persisted fields

whether pricing context/qualifier is persisted

current ResolvePriceAsync call sites

reprice points

checkout validation points

order snapshot fields

currency/market/channel context

current stale-price handling

Evidence:
docs/evidence/TB-P10-T022-R19/cart-pricing-discovery.md

3. Core Invariant

The client may indicate SOURCE CONTEXT but never authoritative price.

Allowed client intent:

product / seller offer identity

optional merchandising campaign identity or equivalent source context

Forbidden:

trusted unit price

trusted discount %

trusted compare-at price

Server must:

validate campaign exists

validate campaign is current Store

validate campaign is runtime-active

validate SellerOffer is a member

resolve canonical campaign-scoped AuthoredPrice using R18 pricing qualifier

validate canonical currency/market/channel dimensions

fall back according to explicit business rule only when appropriate

No client authority over amount.

4. Campaign Context on Add-to-Cart

When customer clicks add-to-cart from a PromotionCampaign-sourced ProductCard, carry a safe campaign context to the server.

Preferred:
MerchandisingCampaignId (nullable) or repository-equivalent.

Rules:

normal ProductCard outside campaign sends no campaign context

campaign ID is an eligibility hint/context, not price authority

server validates membership + active window

arbitrary/wrong campaign ID cannot discount unrelated offer

Do not overload unrelated fields.

5. Cart Line Context Persistence

Determine whether campaign context must persist on CartLine.

Required business behavior:
A line added from active Amazing campaign should retain enough source context for authoritative repricing while in Cart.

If existing CartLine has a generic pricing/qualifier/source metadata pattern, reuse it.

Otherwise add the smallest explicit field/reference needed, e.g. nullable MerchandisingCampaignId.

Do NOT persist:

discount percent

timer

campaign title

fake compare-at

client price

Migration must be minimal and reversible.

6. Add/Update Cart Server Pricing

For campaign-context line:

Server resolution:

verify active campaign

verify Store

verify SellerOffer membership

resolve campaign price through canonical Pricing engine

use campaign price if eligible

otherwise apply explicit expiry/ineligibility policy defined below

For normal line:

existing Base pricing unchanged

No duplicated pricing engine.

7. Expiry / Ineligibility Policy — REQUIRED

Define deterministic commerce behavior when a campaign line is later repriced and campaign is no longer eligible.

Examples:

campaign expired

campaign archived

membership removed

promo AuthoredPrice removed

stock unavailable

Recommended price policy:

reprice to current valid canonical normal price if product remains purchasable

mark line as price-changed so UI can surface existing quote-change semantics if available

never silently preserve expired discount as authoritative checkout price

If current Cart already has canonical "price changed / re-quote required" mechanics, reuse them.

If business rules already define stricter behavior, preserve them and document.

Evidence:
docs/evidence/TB-P10-T022-R19/expiry-policy.md

8. Cart Read Model / Display

Cart UI must reflect server-authoritative line pricing.

For active valid promo:

effective unit price = campaign price

compare-at/original price may display canonical normal price if current cart UI supports it cleanly

discount % derived if current UI supports it

If campaign expired and line reprices:

display new authoritative price

use existing changed-price notification pattern if available

Do NOT redesign Cart.

9. Quantity Changes / Repricing

Changing quantity must not lose campaign context accidentally.

Required:

quantity update revalidates/reprices according to current canonical policy

min/max SellerOffer constraints still apply

inventory truth still applies

no client-supplied amount accepted

10. Login / Logout / Cart Merge

Preserve existing cart ownership locks.

Campaign context must survive legitimate cart persistence/merge where the line survives.

Required:

authenticated cart survives logout

anonymous interim cart merge remains canonical

merge does not invent duplicate discounted lines incorrectly

same SellerOffer with different valid pricing context must follow explicit merge policy

Preferred merge policy:
if line identity currently deduplicates only by SellerOffer, reprice merged line authoritatively using currently valid context rather than preserving duplicate prices.

Do not regress LOCK-SF-117…120 and checkout/cart invariants.

Evidence:
docs/evidence/TB-P10-T022-R19/cart-merge.md

11. Shipping / Checkout Revalidation

Before final checkout commit:

Server MUST revalidate/re-resolve price.

Required:

do not rely only on price captured when line was added

current campaign eligibility checked

canonical price resolver invoked

if price changed, follow existing quote-change confirmation/validation semantics

no Order committed with stale unauthorized promo price

The final Order line must snapshot the authoritative accepted price after successful validation.

Evidence:
docs/evidence/TB-P10-T022-R19/checkout-revalidation.md

12. Atomic Commit Boundary

Preserve existing checkout atomicity locks.

Campaign-price revalidation must occur BEFORE atomic successful order commit.

If validation fails:

Cart remains Active

lines remain

no Order/Cycle commit

no Payment navigation

Do not move Cart conversion earlier.

13. Order Snapshot / Auditability

Order line should retain enough immutable commercial truth to explain what customer paid.

Reuse existing order snapshot fields.

If there is an existing discount/promotion metadata snapshot mechanism, use it only if semantically correct.

At minimum prove:

final unit amount

currency

quantity

SellerOffer/Product identity

campaign identity/reference if needed for audit/reporting

Do NOT make future Order totals depend on live campaign tables.

14. Security Cases

Explicitly test:

arbitrary CampaignId with unrelated SellerOffer

CampaignId from another Store/tenant

expired campaign

future campaign

archived campaign

campaign member removed after add

promo price removed

wrong currency

wrong channel/market where applicable

tampered client unit price ignored

Any unauthorized discount path => FAIL.

15. Real End-to-End Runtime Proof

Use real Development campaign/product seed from R16/R18.

Required:

A. open published page with پیشنهاد شگفت‌انگیز
B. choose a product with real promo price
C. add to cart
D. Cart shows server-authoritative promo price
E. refresh Cart; same valid price
F. change quantity; still server-authoritative
G. navigate Shipping; Cart remains Active
H. final checkout revalidates price
I. successful commit snapshots promo price to Order
J. Payment opens only after commit

Then prove expiry/change behavior using controlled test setup:
K. line added while promo valid
L. campaign made ineligible / clock advanced
M. cart reprice no longer accepts stale promo
N. normal price or canonical changed-price policy applies
O. stale promo cannot reach Order

Security:
P. tampered arbitrary CampaignId cannot discount
Q. client-supplied fake price ignored

Evidence:
docs/evidence/TB-P10-T022-R19/runtime-proof.md

16. Focused Tests

Required:

normal add-to-cart pricing unchanged

active valid campaign context gets campaign price

campaign membership required

wrong campaign rejected/no discount

cross-store campaign rejected

future campaign no discount

expired campaign no discount

archived campaign no discount

missing promo price falls back correctly

client fake price ignored

campaign context persists on CartLine or canonical equivalent

quantity update revalidates

cart reload revalidates or preserves valid canonical quote per current design

campaign expiry causes canonical reprice/change behavior

membership removal invalidates promo

price removal invalidates promo

wrong currency/dimension rejected

checkout revalidates before commit

failed revalidation leaves Cart Active

successful checkout Order amount equals authoritative campaign price

Order snapshot remains stable after campaign changes

logout/login preserves valid line context

cart merge cannot duplicate/exploit discount

existing non-promo checkout tests remain green

R17/R18 PromotionCampaign source tests remain green

recovery-staleness PASS

git diff --check PASS

18ca10c9 ancestor PASS

HEAD==origin/main PASS

Keep tests focused.

17. Capability Map Update

Because this task materially changes Pricing ↔ Cart/Checkout ownership, update ONLY affected sections of:

docs/architecture/TOOBA-CAPABILITY-MAP.md

Add compact verified facts:

how merchandising campaign context reaches Cart

authoritative server pricing boundary

checkout revalidation ownership

Order snapshot ownership

Last Verified Task = TB-P10-T022-R19

Do NOT expand into broad audit.
Do NOT make capability-map reading mandatory for unrelated tasks.

18. Anti-Pattern Gate

Reject:

trusting client price

trusting client discount %

CampaignId as automatic discount authorization

duplicated price engine

stale promo preserved through checkout without validation

expired campaign price committed to Order

campaign title/timer persisted on CartLine

per-line pricing N+1 if batch path is available

optimistic cart clearing

moving conversion before commit

cart state in localStorage as source of truth

Builder changes

Storefront redesign

P11 work

broad repo audit

user-work overwrite

Evidence:
docs/evidence/TB-P10-T022-R19/antipattern-scan.md

19. Locks

Add after LOCK-SF-416:

LOCK-SF-417 MerchandisingCampaign identity carried from Storefront/Cart is pricing context only, never price authority; server validates Store, active window, membership and canonical Pricing dimensions before applying campaign price.
LOCK-SF-418 Cart lines that participate in merchandising pricing retain only the minimal campaign pricing context needed for authoritative repricing; client amount, discount percent, timer and display metadata are never Cart truth.
LOCK-SF-419 Cart quantity changes, reloads, merges and final checkout must preserve/revalidate campaign pricing context through the canonical Pricing engine; stale or ineligible campaign discounts cannot survive authoritative reprice.
LOCK-SF-420 Final checkout revalidates merchandising campaign eligibility and price before atomic Order commit; a failed or changed-price validation cannot convert the Cart or navigate to Payment.
LOCK-SF-421 Order pricing is an immutable authoritative snapshot of the accepted checkout quote and never depends on later live campaign state for historical totals.

Do not renumber existing locks.

20. Completion State

At completion:

Phase:
P10 / Builder Acceptance + Amazing End-to-End Commerce Integrity

Last Architect-accepted before review:
TB-P10-T022-R18

Last Implementation:
TB-P10-T022-R19

Expected:

campaign price works Storefront → Cart → Shipping → Checkout → Order

server authoritative

campaign context validated

stale promo cannot reach Order

normal non-campaign pricing unchanged

cart persistence/merge semantics preserved

capability map updated only for affected ownership

no Campaign Admin UI yet

no Builder redesign

Appearance YES

Builder NO

P11 retained for later

main

HEAD==origin/main

18ca10c9 ancestor

user work preserved

locks through LOCK-SF-421

Do NOT write Worker IDLE.

After canonical Result through Bridge:
STOP completely.

Acceptance

PASS only if:

no client-authoritative price path

active campaign promo applies in Cart

invalid/expired/tampered campaign cannot discount

quantity/reload/merge semantics safe

checkout revalidates before commit

Order snapshots correct authoritative price

normal pricing unchanged

atomic checkout locks preserved

capability map updated narrowly

user work preserved

canonical Result delivered

Worker stops

Result Contract

Return ONLY canonical BRIDGE-WAKE-V1 Result with:

Summary
Applicable-Locks
Recovery-Start
Cart-Pricing-Discovery
Campaign-Context
CartLine-Persistence
Add-To-Cart-Pricing
Expiry-Policy
Cart-Display
Quantity-Repricing
Cart-Merge
Checkout-Revalidation
Atomic-Commit
Order-Snapshot
Security-Validation
Runtime-Proof
Focused-Validation
Capability-Map
AntiPattern-Scan
Lock-Registry
Recovery-SoT
Git
Architectural-Concerns
Blockers

Do not include Worker-IDLE.

After sending Result through Bridge:
STOP completely.

END_TOOBA_TASK