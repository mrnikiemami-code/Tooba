PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_TASK

Task-ID:
TB-P07-T030-R1

Parent-Task:
TB-P07-T030

Channel:
tooba-main

Title:
Product Create + Visual Polish Acceptance Repair — Complete Guided Flow, Full Validation, Runtime Reference Proof

Role:
You are the implementation Worker for Tooba.
Repair ONLY the remaining acceptance gaps in TB-P07-T030.
Do NOT start T031.
Preserve all successful T030 work.

TB-P07-T030 is NOT fully accepted.

Preserve:

dedicated /admin/products/new

inline create card removed

TipTap rich editor

DOMPurify sanitization

controlled font family/size

readiness-card polish

fa/en direction handling

real Media DAM integration from T029

no Product Price/Stock

But the Result does NOT satisfy T030 acceptance because:

Create workflow reports only:
category -> structure -> translations -> review

The locked create workflow required the user to be guided through:

Primary Category

base structure

Translations

Attributes

Variants

Media

SEO

Review/Create

Attributes, Variants, Media, and SEO cannot be silently omitted from the creation experience.

T030 explicitly required full backend validation:

backend restore

backend build

backend FULL tests

MigrationRunner where applicable

Worker skipped them as "FE-only".
That does NOT satisfy the task contract.

Visual acceptance evidence is too weak:
"composition closer" is not enough.
The task required a live side-by-side comparison against the locked reference and repair if Product Admin still looks materially weaker.

USER_VISUAL_ACCEPTED remains NO.

Do not regress:

/admin/products/new

no ?create=1

no inline create card

TipTap editor

sanitized rich content

controlled font family/size

readiness cards

Product workspace tabs

Media DAM

Variant UX from T028

Primary/Additional Category model

centralized Admin error normalization

Product != Offer

no Price/Stock

The create experience must clearly include ALL relevant authoring stages.

Required visible stages:

دسته اصلی

اطلاعات پایه

ترجمه‌ها

ویژگی‌ها

تنوع‌ها

رسانه

SEO

بررسی و ایجاد

English equivalents required.

The user must understand the sequence without training.

You may use:

stepper

wizard

progressive workspace

But all stages must be reachable and real.

Do NOT create fake placeholder steps.

If Attributes/Variants/Media/SEO need a persisted ProductId:

Use a transparent Draft-first flow.

Recommended:

collect minimal Primary Category + required identity/translation data

create Draft at the appropriate point

continue same creation workspace against that Draft

keep user inside the creation flow

do not bounce awkwardly to unrelated screens

Clearly communicate:
پیش‌نویس محصول ایجاد شد؛ می‌توانید اطلاعات را تکمیل کنید.

Avoid duplicate Product creation on retry.

Must use Primary Category effective schema.

Show:

required/optional attributes

typed controls

human labels

completion indicator

Product cannot create AttributeDefinition here.

If no attributes:
show human empty state and allow continue.

Reuse T028 guided Variant UX.

Show:

Variant-enabled attributes

selectable values

preview combinations

generate/update

default variant

impact summary

No Axis/Schema jargon.
No Price/Stock.

If no Variant-enabled attributes:
show understandable empty state and allow continue.

Reuse real T029 Media DAM.

Must allow:

open Media Library

upload/select

assign images

set primary

reorder

AltText

No fake CTA.

Creation step must visibly reflect media readiness.

Must provide actual Product SEO authoring:

SEO title

meta description

public URL preview / current slug seam

readiness

search preview where existing implementation supports it

Respect current locale/slug architecture.
Do not invent per-locale slug storage if absent.

Final review must summarize:

Primary Category

Brand

translations completeness

required attributes completion

Variant readiness

Media readiness

SEO readiness

publication readiness

Actions:

go back to incomplete stage

save Draft

complete creation

Do NOT automatically publish unless current architecture explicitly requires it.
Default remains Draft.

Open exact locked reference:

D:\Users\User\source\repos\SarvNewVerRequirment\reference\Image\ChatGPT Image Aug 29, 2026, 06_33_46 PM.png

Compare LIVE side-by-side with:

Product List

Product Create

Product VIEW

Product EDIT

Translations

Attributes

Variants

Media

SEO

Publishing

History

Evaluate and repair:

header hierarchy

card density

spacing

typography

readiness cards

inspector/sidebar balance

tab styling

Product media presentation

button hierarchy

empty/loading/error states

overall premium/commercial feel

If Product Admin still materially looks like a prototype or materially weaker than reference:
do NOT PASS.

Document concrete differences remaining.

Cards must communicate actual completion, not decorative color.

At minimum:

Media

Translations

Attributes

Variants

SEO

Publish readiness

Each card:

real derived state

progress/count

status text

action/link

accessible non-color meaning

Verify live save/reload for both fa/en.

Required:

heading

bold

italic

underline

ordered/unordered lists

link

table

quote

undo/redo

controlled font family

controlled font size

sanitization

RTL in fa

LTR in en

Inline image may remain disabled because DAM-safe insertion was explicitly allowed to be deferred if no fake action is shown.

No plain textarea fallback in normal full-description editing.

Even if changes are frontend-heavy, run the complete validation required by T030:

backend restore

backend build

backend FULL tests

MigrationRunner/tests if repository has them

Require:

0 failed

0 skipped

no new warnings

Do NOT mark N/A.

Run:

typecheck

lint

full relevant frontend tests

production build

Require:

0 failed

0 skipped

no new warnings

Run explicitly:
git diff --check

Must pass.

Keep alive:

Backend :5088

Frontend :3000

Shopeiva :3001

Verify current HEAD is loaded.

Live create flow must prove:

open /fa/admin/products/new

choose Primary Category

enter base structure

enter fa translation

enter en translation

fill Attributes

configure Variants or valid empty state

upload/select Media

complete SEO

review

create/save Draft

reopen resulting Product

data persists

The user has explicitly stated:
buyers judge the product heavily by appearance.

Therefore:

spacing must look intentional

typography hierarchy must be polished

cards must be visually balanced

dialogs must look finished

states must be polished

no ugly default browser controls

no giant empty whitespace

no awkward inline forms

no inconsistent border/radius/shadow system

no mismatched active states

no raw technical copy

no unfinished visual sections

Reuse design-system primitives/tokens.

Verify:

desktop

tablet

mobile

fa RTL

en LTR

Create wizard/stepper must remain usable on mobile.
Editor toolbar must not overflow destructively.
Media dialog must fit viewport.

Preserve:

Product != Offer

Product.Price absent

Product.Stock absent

Product.Inventory absent

Variant.Price absent

Variant.Stock absent

Primary Category drives schema

Additional Categories discovery-only

Product cannot create AttributeDefinition

Media owned by Media module

AppDataGrid canonical

Category Tree unchanged

Shopeiva unchanged

centralized errors

Publishing lifecycle

History

Update/create:
docs/evidence/TB-P07-T030/

Required:

create-flow-complete.md

visual-reference-comparison.md

full-validation.md

live-verification.md

Document:

all 8 stages

live route checks

exact reference path

remaining material visual mismatch

backend full suite result

frontend full validation

git diff --check

If existing screenshot tooling is available:
capture Product Create + Product EDIT overview + Media + Translations.
Do not invent screenshot infrastructure.

Commit + push only after all acceptance criteria pass.

Require:

HEAD == origin/main

scoped tracked tree clean

unrelated changes preserved

PASS only if ALL:

dedicated Product create route remains

no inline create card

all 8 create stages are present and functional

Draft-first transition is understandable

Attributes step is real

Variants step is real

Media step uses real DAM

SEO step is real

Review step summarizes readiness

TipTap rich editor is real and persists fa/en

controlled fonts/sizes work

full backend restore/build/tests pass

MigrationRunner/tests pass where applicable

frontend typecheck/lint/tests/build pass

git diff --check passes

live runtime compared side-by-side with locked reference

no material prototype-level visual mismatch remains

Product/Variant Price/Stock remain absent

current HEAD loaded

Git synchronized

USER_VISUAL_ACCEPTED=NO.

Return ONLY:

PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_WORKER_RESULT

Task-ID:
TB-P07-T030-R1

Parent-Task:
TB-P07-T030

Channel:
tooba-main

Status:
PASS | FAIL | BLOCKED | RECOVERY_CONFLICT

Summary:
...

Create-Flow:

route:

inline create:

stage 1 Primary Category:

stage 2 Base:

stage 3 Translations:

stage 4 Attributes:

stage 5 Variants:

stage 6 Media:

stage 7 SEO:

stage 8 Review:

Draft-first:

duplicate-create protection:

final persisted Product:

Rich-Editor:

TipTap:

headings:

bold:

italic:

underline:

lists:

link:

table:

quote:

undo/redo:

font family:

font size:

sanitization:

fa save/reload:

en save/reload:

inline image action exposed:

Reference-Comparison:

reference opened:

side-by-side live:

Product List:

Product Create:

Product VIEW:

Product EDIT:

header:

cards:

spacing:

typography:

tabs:

media:

states:

remaining material visual mismatch:

prototype-level sections remaining:

pixel-perfect claimed:

Readiness:

Media:

Translations:

Attributes:

Variants:

SEO:

Publish:

derived from real state:

links/actions:

Responsive:

desktop:

tablet:

mobile:

fa RTL:

en LTR:

editor toolbar:

dialog fit:

Architecture:

Product != Offer:

Product.Price:

Product.Stock:

Product.Inventory:

Variant.Price:

Variant.Stock:

Primary Category:

Additional Categories:

Product local AttributeDefinition:

Media ownership:

Category Tree:

AppDataGrid:

Shopeiva:

Validation:

backend restore:

backend build:

backend FULL tests:

backend passed:

backend failed:

backend skipped:

MigrationRunner:

frontend typecheck:

frontend lint:

frontend tests:

frontend production build:

frontend failed:

frontend skipped:

warnings:

new warnings:

git diff --check:

Live-Verification:

create route:

Primary Category:

translations:

Attributes:

Variants:

Media:

SEO:

review:

Draft save/create:

reopen persisted:

reference comparison:

Backend:

Frontend:

Shopeiva:

current HEAD loaded:

kept alive:

Evidence:

create flow:

visual comparison:

full validation:

live:

path:

Git:

branch:

commit:

push:

final HEAD:

origin/main:

synchronized:

tracked tree:

unrelated user changes:

Architectural-Concerns:
...

Visual-Concerns:
USER_VISUAL_ACCEPTED=NO

Blockers:
...

END_TOOBA_WORKER_RESULT

After Result:

Worker -> IDLE / Waiting

do not invent next task

do not add explanation outside Result envelope

END_TOOBA_TASK