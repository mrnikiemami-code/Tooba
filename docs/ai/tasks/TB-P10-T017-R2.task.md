PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_REPAIR_TASK

Task-ID:
TB-P10-T017-R2

Parent-Task:
TB-P10-T017-R1

Channel:
tooba-main

WorkerId:
tooba-worker-01

AgentType:
cursor

Status:
REPAIR

USER_VISUAL_ACCEPTED=NO

Title:
Final Product Media Visual Integrity Repair — PLP/Landing Card Images + Hydration Attribution Closure

Architect Review

TB-P10-T017-R1 fixed the first-paint SSR 500 and is technically strong, but TB-P10-T017 is still NOT Architect-accepted.

Remaining visual concern from the Worker Result:

PLP/Landing product-card photo wells still appear empty in the real 1440×1200 inspection captures even though <img> elements report naturalWidth=150.

This is not acceptable for the final user visual-acceptance pack until we determine whether the actual card media is visibly correct in the browser.

There is also a leftover hydration attribute warning/dev “1 Issue” overlay. R1 says it is not the repaired SSR throw, but final acceptance requires precise attribution and confirmation that it is not Appearance/surface/media task-owned.

This repair is narrowly scoped.

Do NOT:

redesign cards

change ProductCardSkin architecture

change the four-role surface architecture

add new Appearance controls

create screenshot-only hacks

hide warnings

start TB-P10-T018

Recovery State

Phase: P10
Last Architect-accepted: TB-P10-T016
Last Implementation: TB-P10-T017-R1
Current Repair: TB-P10-T017-R2
Worker: tooba-worker-01
Expected branch: main
Expected previous HEAD: a1900f7358e6bafe3365f4878087427f13311b07
USER_VISUAL_ACCEPTED=NO

Preserve LOCK-SF-001…202.

1. Recovery Preflight

Record:

branch

HEAD

origin/main

git status --short

Recovery/SoT

Host :5088

FE :3000

canonical appearance state

unrelated local user-authored changes

Unexpected tracked divergence => RECOVERY_CONFLICT/BLOCK.

Evidence:
docs/evidence/TB-P10-T017/r2-recovery-start.md

2. Diagnose PLP/Landing Product Media Wells

Use the exact PLP and published Landing routes from the final inspection flow.

For representative product cards inspect:

rendered <img> / Next Image element

src/currentSrc

naturalWidth/naturalHeight

clientWidth/clientHeight

computed display/visibility/opacity

object-fit/object-position

parent dimensions

overflow/clipping

z-index/stacking

placeholder/background layers

loading state

whether the image is visually transparent/blank asset

whether the asset is a real product image or placeholder

whether ProductCardSkin changes the media layer

Determine one exact classification:

A. Actual runtime rendering defect
B. Valid image but capture/viewport issue
C. Seed/product data has no meaningful media
D. Asset itself is blank/transparent/broken
E. Other — document precisely

Do not guess.

Evidence:
docs/evidence/TB-P10-T017/r2-media-root-cause.md

3. Repair Only If Real Runtime Defect

If classification A/D or another genuine rendering defect:

fix the canonical shared product-card media path

ensure Home/PLP/Landing use the same correct shared behavior

preserve all 4 ProductCardSkin variants

preserve layout dimensions unless the existing dimensions are the defect

no route-specific CSS

no forced screenshot delays in runtime code

If classification B:

do NOT change production UI

fix only the evidence capture sequence (wait for visible image state / scroll into view / stable browser frame)

If classification C:

do not fabricate production data

use an existing seeded/demo product that has valid media for the inspection screenshot if available

document the data limitation

4. Product Card Media Contract

Verify:

image/placeholder always has a visible deterministic state

no blank unexplained media well

missing-media fallback is intentional and visible

image loading does not leave a permanent empty shell

all 4 skins respect the same canonical media contract

PaletteTint/four-role surfaces do not obscure product media

If a real invariant is missing, add the smallest shared guard.

5. Hydration Warning Closure

Reproduce the remaining Next.js hydration attribute warning on a fresh:

Home

PLP

PDP

Landing

Record:

exact warning text

exact DOM element/component if identifiable

exact mismatched attribute/value

server value

client value

owner/source

whether it predates T015/T016/T017

If owned by:

Appearance

ThemeToggle

surface role mapping

ProductCard

media rendering

then fix it.

If unrelated:

document precise source and why it is outside this task

do NOT suppress it

confirm it does not alter visible final inspection state

Evidence:
docs/evidence/TB-P10-T017/r2-hydration-closure.md

6. Refresh Only Affected Screenshots

Re-capture truthful final visual evidence:

plp-tinted-r2.png

landing-tinted-r2.png

product-card-classic-r2.png

product-card-clean-r2.png

product-card-elevated-r2.png

product-card-glass-r2.png

At least one screenshot must clearly show a real product image or the intentional visible missing-media placeholder, not an unexplained empty rectangle.

Store:
docs/evidence/TB-P10-T017/screenshots/r2/

Do not regenerate unrelated palette/dark screenshots.

7. Concise Runtime Reproof

Use Host :5088 + FE :3000.

Run:

A. cold PLP -> 200
B. verify representative product media visibly rendered
C. cold published Landing -> 200
D. verify representative product media visibly rendered
E. Home product-card spot-check
F. cycle all 4 ProductCardSkin variants on one representative view
G. PaletteTint spot-check
H. restore canonical default:
tooba-blue / LightOnly / classic / Neutral / canonical Home

Record:

200 first response

image visible/fallback visible

console

hydration warning status

no 500

no retry loop

no polling

8. Minimal Useful Validation

Run only:

existing ProductCard/appearance/surface focused guards

critical-storefront

one shared media regression test only if a real bug was fixed and no existing test covers it

recovery-staleness

git diff --check

No broad screenshot test suite.
No per-skin combinatorial unit matrix.

9. Anti-Pattern Gate

Scan for:

route-specific product-image CSS

screenshot-only CSS

setTimeout/sleep in runtime image code

forced eager-loading everywhere without need

hydration suppression

fake data inserted just for screenshot

duplicated ProductCard media paths

page-specific palette/surface workaround

polling

Any introduced real occurrence => REPAIR.

Evidence:
docs/evidence/TB-P10-T017/r2-antipattern-scan.md

10. Lock Registry

No new locks unless a genuinely new invariant is discovered.

Preserve LOCK-SF-001…202.

11. Recovery / Pipeline

At completion:

Phase P10

Last Architect-accepted before review: TB-P10-T016

Last Implementation: TB-P10-T017-R2

Current Issued/Repair: none

USER_VISUAL_ACCEPTED=NO

Worker IDLE

branch main

ending HEAD

HEAD==origin/main

task-owned clean

unrelated local user changes preserved

do NOT invent/start TB-P10-T018

Run recovery staleness guard.

Acceptance

PASS only if ALL:

PLP/Landing product media concern is exactly classified

actual runtime media defect fixed if one exists

no unexplained empty product media wells remain in final evidence

all 4 ProductCardSkin variants preserve visible media/fallback

remaining hydration warning is either fixed if task-owned or precisely attributed if unrelated

no warning suppression/hack

first requests remain 200

four-role surface architecture unchanged

canonical default restored

affected screenshots persist

minimal validation only

anti-pattern scan CLEAN

Recovery exact

HEAD==origin/main

Worker IDLE

no TB-P10-T018

USER_VISUAL_ACCEPTED remains NO

Result Contract

Return ONLY canonical BRIDGE-WAKE-V1 Result with:

Summary
Applicable-Locks
Recovery-Start
Media-Root-Cause
Media-Repair
ProductCard-Media-Contract
Hydration-Closure
Focused-Validation
Runtime
Visual-Evidence
AntiPattern-Scan
Lock-Registry
Recovery-SoT
Git
Architectural-Concerns
Visual-Concerns
Blockers

END_TOOBA_REPAIR_TASK