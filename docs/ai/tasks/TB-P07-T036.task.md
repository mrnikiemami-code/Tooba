PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_TASK

Task-ID:
TB-P07-T036

Parent-Task:
TB-P07-T035

Channel:
tooba-main

Title:
Category Semantics Repair — Primary Category Migration, Attribute Inheritance Overrides, Display-Only Assignment, Correct PLP Facets

ARCHITECT DECISIONS

Category → Products manages ONLY نمایش در این دسته.
It can never create/change Primary Category.

Primary Category drives Product effective Attribute schema and Variant eligibility.
Changing it is a structural migration, not a normal combobox save.

AttributeDefinition is canonical and unique.
If inherited from L1, L2/L3 receive it once.
Child Categories may override Category-binding behavior for the SAME AttributeDefinition, not redefine it.

PLP facets are owned by the viewed Category.
Do NOT union viewed-category facets with each included Product's Primary Category facets.

USER_VISUAL_ACCEPTED=NO.

A — CATEGORY → PRODUCTS

Rename CTA to:
افزودن محصول برای نمایش در این دسته

Picker:

every add/bulk-add creates display/additional membership only

no role chooser

no Primary option

Rows:

Primary in current Category: badge دسته اصلی; no remove/change; action باز کردن محصول

Display membership: badge نمایش در این دسته; action حذف از این دسته

No membership: افزودن برای نمایش

Bulk:
افزودن موارد انتخاب‌شده برای نمایش (N)

B — PRIMARY CATEGORY CHANGE

Only Product Workspace may change Primary Category.

Replace simple direct mutation with:
تغییر دسته اصلی

Use a 3-step migration wizard.

Step 1:

searchable L3 Category picker/tree

current path + target path

Step 2: backend impact PREVIEW, no mutation.
Show:

ویژگی‌های قابل حفظ

ویژگی‌های جدید

ویژگی‌های خارج‌شونده

تنوع‌های تحت تأثیر

نمایش در دسته‌های دیگر

resulting Publish-readiness blockers

Step 3:

explicit confirmation

transactional migration

guide user to required remediation

C — ATTRIBUTE VALUE MIGRATION

Match ONLY by canonical AttributeDefinition ID.

If definition exists in target effective schema and type/value remains compatible:

preserve Product value

If definition is not in target:

remove/deactivate from active Product schema

preserve audit/history as appropriate

If target introduces required definition:

no fake value

mark Product incomplete until user fills it

No mapping by label similarity.

D — VARIANT MIGRATION

Compare canonical effective variant-enabled definitions/options.

If axes/options remain compatible:

preserve Variants

If axes/options change:

mark Variants as needing review/rebuild

do not silently generate/delete commercial combinations

Publish readiness becomes Not Ready until reconciled

Reuse existing human Variant UX.

No Price/Stock.

E — ADDITIONAL MEMBERSHIP DURING PRIMARY CHANGE

If target Category was already a display membership:

atomically promote it to Primary

remove duplicate display relation

Other display memberships remain.

F — PUBLISHED SAFETY

If a Published Product's Primary Category migration creates structural incompatibility:

do not leave an inconsistent Published Product

use lifecycle/readiness behavior consistent with current architecture

document decision

never silently expose broken Attribute/Variant state publicly

G — ATTRIBUTE INHERITANCE

Canonical behavior:

L1 binds AttributeDefinition A
→ L2 inherits A
→ L3 inherits A

Effective schema contains A exactly once.

If L2 wants different behavior:

same AttributeDefinition ID

child Category binding override

no duplicate AttributeDefinition

Nearest explicit Category binding in ancestry wins for binding-level behavior.

H — OVERRIDABLE CATEGORY-BINDING BEHAVIOR

Support existing binding-owned controls such as:

Required

Filterable

Variant-enabled

Comparable

display order

facet-related binding metadata where already owned

Do not duplicate definition-owned type/options.

If current storage uses complete child binding snapshots instead of sparse overrides, that is acceptable if:

same AttributeDefinition ID

one effective entry

documented.

I — ATTRIBUTE ADMIN UX

Inherited row displays:

name

به ارث رسیده از <parent>

effective chips

action تنظیم اختصاصی برای این دسته

When overridden:

badge تنظیم اختصاصی

action بازگشت به تنظیمات والد

Reset:

removes child override

immediately falls back to nearest ancestor

never deletes AttributeDefinition or parent binding

J — DUPLICATE PREVENTION

افزودن ویژگی موجود:

inherited/effective definitions excluded or disabled

If user tries to create/select same canonical feature already inherited:
show:
این ویژگی از قبل از دسته والد به ارث رسیده است. در صورت نیاز تنظیمات استفاده آن را برای این دسته تغییر دهید.

Backend remains authoritative for duplicate prevention.

K — PLP FACET SEMANTICS

For /category/X:

Visible facets =

effective configured facets of Category X

plus approved global facets already modeled globally

NEVER:
Category X facets + union of every included Product Primary Category facets.

L — ADDITIONAL PRODUCT PARTICIPATION IN TARGET FACETS

A Product displayed in Category X keeps its Primary Category schema.

For each facet configured on X:

if Product has a value for the SAME canonical AttributeDefinition, it contributes to that facet bucket

if it lacks that definition/value, it contributes to no bucket for that facet

No synthetic value.
No name-based mapping.

When no facet is selected:

Product may appear due to display membership

When a facet value is selected:

Product lacking that matching value does not match

M — FACET COUNTS

Counts are computed from Products discoverable in X:

Primary members

display members

existing subtree semantics

But buckets exist only for X's facets/global facets.

Products missing a facet value:

count in total results

do not count in any option bucket for that facet

Server-side/deterministic.

N — BRAND FACET

Brand is Product.BrandId, not AttributeDefinition.

If Brand is a global PLP facet:

options come from Brands represented among discoverable Products

brandless Product visible when no Brand filter selected

brandless Product does not match selected Brand

no persisted fake بدون برند Brand entity

O — OPTIONAL COMPATIBILITY HINT

When adding Product for display in Category X, optionally compute:

target facets it can satisfy

target facets for which it has no value

Show non-blocking hint, e.g.:
این محصول در این دسته نمایش داده می‌شود، اما برای 3 فیلتر این دسته مقدار متناظر ندارد.

Do NOT force Additional Category attributes onto Product.

P — MIGRATION PREVIEW API

Add/reuse non-mutating backend contract conceptually returning:

currentCategory

targetCategory

preservedAttributes

addedAttributes

removedAttributes

requiredMissing

variantCompatibility

preserved/affected Variant counts

additional-membership effects

resulting readiness blockers

Separate confirmed mutation command.

Q — HISTORY

Primary Category migration history must be human-readable:

old Category

new Category

attribute preserved/new/removed counts

Variant impact

actor/time

No raw JSON/GUIDs.

R — TESTS

Backend:

inherited Attribute appears once

child duplicate attach blocked

child override changes effective behavior

reset override falls back to parent

nearest ancestor wins

same AttributeDefinition identity preserved

migration preview does not mutate

compatible attributes preserved

removed attributes leave active schema

new required values block readiness

compatible Variants preserved

incompatible Variants require review

target Additional promotes safely to Primary

Category→Products can never set Primary

bulk add creates display membership only

PLP X exposes X facets only

no source-category facet union

common canonical attribute contributes

missing facet value creates no bucket

selected filter excludes missing value

Brand global facet semantics

no Product/Variant Price/Stock

Frontend:

display-only Category Products labels/actions

no Primary selector there

3-step migration wizard

human impact preview

inherited source + override/reset UX

inherited attribute cannot be re-added

mixed-primary PLP facet behavior

fa/en RTL/LTR

success/error feedback

S — LIVE VERIFICATION

With seeded demo data:

Inheritance:

verify L1 Attribute inherited once in L2/L3

create L2 override

verify L3 follows L2

reset override

verify fallback to L1

Category Products:

add Product whose Primary is elsewhere

verify display-only role

remove it

verify Primary unchanged

Primary migration:

choose Product with Attributes/Variants

preview compatible/incompatible target

cancel => no mutation

execute controlled migration

verify values/Variants/readiness

restore seed state afterward

PLP:

Category with mixed Primary/display Products

filter panel contains target Category facets only

common canonical values aggregate

unrelated source Primary facets never appear

selected filter excludes Products lacking matching value

T — ARCHITECTURE LOCKS

Preserve:

Product != Offer

no Product/Variant Price/Stock/Inventory

Primary Category schema-driving

display memberships discovery-only

Product cannot create AttributeDefinition

Media DAM ownership

normalized Tags

AppDataGrid

Category Tree

Shopeiva unchanged

U — FULL VALIDATION

Run:

backend restore/build/FULL tests

MigrationRunner/tests

frontend typecheck/lint/relevant/full tests/production build

git diff --check

Require 0 failed, 0 skipped, no new warnings.

V — EVIDENCE

Create docs/evidence/TB-P07-T036/:

primary-category-migration.md

attribute-inheritance.md

category-products-display-only.md

plp-facet-semantics.md

live-verification.md

full-validation.md

W — GIT / RUNTIME

Commit + push after green validation.
Require HEAD==origin/main, scoped tracked tree clean, unrelated changes preserved.

Keep:
Backend :5088
Frontend :3000
Shopeiva :3001
Verify current HEAD loaded.

ACCEPTANCE

PASS only if:

Category→Products can only create display membership

Primary cannot be changed there

Primary change uses impact-preview migration wizard in Product Workspace

inherited Attribute appears once

child override uses same canonical AttributeDefinition

reset-to-parent works

duplicate inherited Attribute cannot be re-added

migration preserves compatible data and blocks readiness for incompatible/missing requirements

PLP facets are owned by viewed Category, not unioned from Product Primary Categories

displayed Products contribute only matching canonical facet values

missing values filter correctly

Brand global facet remains canonical

no Product/Variant Price/Stock

full validation green

runtime current

Git synchronized

USER_VISUAL_ACCEPTED=NO.

RESULT CONTRACT

Return ONLY canonical BRIDGE-WAKE-V1 result for TB-P07-T036 with sections:
Summary
Category-Products
Primary-Category-Migration
Attribute-Inheritance
Attribute-Overrides
Duplicate-Prevention
PLP-Facets
Additional-Product-Facet-Participation
Brand-Facet
History
Architecture
Validation
Live-Verification
Evidence
Git
Architectural-Concerns
Visual-Concerns
Blockers

END_TOOBA_TASK