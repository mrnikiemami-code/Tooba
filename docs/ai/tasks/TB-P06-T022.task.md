PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

TASK-ID: TB-P06-T022
PROJECT: Tooba
PHASE: P06
CHANNEL: tooba-main
STATUS: ISSUED
TASK-TYPE: PRODUCTION PAYMENT READINESS
WORKER-POLICY: ONE WORKER = ONE ACTIVE TASK

Title

Production Payment Readiness — Harden Real PSP Adapter Boundary, Callback Verification, Reconciliation, Idempotency and Operator Controls without UI Drift

Architect Context

Architect ACCEPTED:

TB-P06-T020 = ACCEPTED
TB-P06-T021 = ACCEPTED

Current product state:

SELLABLE_PRODUCT_FLOW_LIVE = YES
SELLABLE_DEMO = YES
PRODUCTION_GO_LIVE_READY = NO

Current proven payment state:

provider-agnostic payment foundation exists

deterministic sandbox PSP exists

PendingPayment -> Succeeded -> Order Paid works

production mode fails closed

callback authenticity/idempotency/reconciliation foundations exist from prior work

no real production PSP is yet claimed

Primary current gap:
PRODUCTION PAYMENT PROVIDER READINESS

Critical Constraint

Do NOT invent or hardcode a commercial PSP selection.

If the repository/environment contains an explicitly configured production provider or documented provider target:

implement/complete that adapter.

If no provider target/credentials are available:

complete the production-ready adapter boundary, secure configuration, operator controls, contract harness, reconciliation path, and fail-closed behavior.

report REAL_PSP_PROVIDER_CONFIGURATION_REQUIRED as an external go-live blocker.

do NOT fake a successful real-bank transaction.

ABSOLUTE UI LOCK

Any payment-facing UI touched in this Task must preserve existing Shopeiva/Tooba accepted behavior.

LOCKED:

CSS

Tailwind

JavaScript interaction behavior

animation

transition

hover/focus/active

spacing

typography

responsive/mobile

icons

badges

cards

modal/drawer

payment result layout

checkout layout

component geometry

Backend integration changes must not redesign Checkout or Payment Result.

Any unauthorized UI change:
VISUAL REGRESSION
=> Task cannot PASS.

A — Repository Recovery

Run:

git rev-parse --show-toplevel
git fetch origin
git branch --show-current
git rev-parse HEAD
git rev-parse origin/main
git status --short --branch

Expected predecessor:

f9c20bb8377f053050b64142d59e20cd53aed833

Require:

main

HEAD == origin/main

safe tracked tree

If conflict:
RECOVERY_CONFLICT

B — Start Runtime Before Work

Start and keep running where technically possible:

PostgreSQL

Backend

Tooba Frontend

Original Shopeiva

Verify:

health/live 200

health/ready 200

/fa/checkout

payment result route

admin operational payment route

sandbox payment still works before changes

Evidence:
docs/evidence/TB-P06-T022/01-runtime-before-payment-readiness.md

C — Audit Existing Payment Architecture

Inspect all existing payment modules/contracts from prior T008 and T021.

Document:

Payment aggregate/state machine

provider abstraction

sandbox adapter

production adapter placeholder/fail-closed behavior

initiation flow

callback/webhook flow

verification flow

idempotency keys

retry policy

reconciliation

payment/order coupling

outbox/integration events

tenant/market/provider resolution

operator/admin visibility

Classify:

LIVE
PARTIAL
MISSING
EXTERNAL_CONFIG_REQUIRED

Evidence:
02-payment-architecture-audit.md

D — Provider Configuration Discovery

Search repository/environment configuration safely for:

configured provider name

provider endpoint/base URL

merchant/account identifiers

callback route configuration

feature flags

secret references

DO NOT print secrets.

Do not dump environment variables containing credentials into evidence/logs.

Result:

PROVIDER_TARGET_FOUND
or
NO_PRODUCTION_PROVIDER_TARGET

Evidence:
03-provider-configuration-discovery.md

E — Production Provider Contract

Ensure canonical abstraction supports at minimum:

Create/Initiate Payment

Redirect/Payment URL or token

Verify

Callback/Webhook handling

Query/Status check

Reconcile

Refund capability contract where provider supports

provider transaction/reference identifiers

provider error classification

No provider-specific logic leaking into Order domain.

Evidence:
04-production-provider-contract.md

F — Secure Provider Configuration

Production PSP configuration must:

come from secure configuration/secret source

fail closed when required secret/config is absent

never expose merchant secret/key to browser

never log raw secrets

be tenant/edition aware if required

support environment separation

reject unsafe sandbox-in-production fallback

Create:
05-provider-secure-configuration.md

G — Payment State Machine Hardening

Verify exact transitions.

At minimum distinguish:

Created
Pending
RequiresAction / Redirected if applicable
Succeeded
Failed
Canceled
Expired
RefundPending if supported
Refunded / PartiallyRefunded where current foundation supports

Rules:

cannot transition Paid Order from forged callback

duplicate callbacks safe

out-of-order callbacks safe

terminal state protections

retries do not double-charge

Order Paid only after verified success

Create:
06-payment-state-machine.md

H — Callback/Webhook Authenticity

Production callback/webhook handling must:

verify provider signature/token/authenticity per adapter contract

validate provider transaction/reference

validate amount/currency where provider returns them

correlate to correct internal Payment

reject mismatches

be idempotent

not trust browser-return parameters as proof of payment

Browser return may drive UX only.
Backend verification determines payment truth.

Create:
07-callback-authenticity.md

I — Amount / Currency Integrity

Before marking success verify:

expected amount

expected currency

correct Payment

correct Order

provider reference uniqueness

no client-side authoritative total

Preserve:
Pricing/Tax/Promotion snapshots from Checkout/Order.

Create:
08-payment-amount-integrity.md

J — Idempotency

Prove idempotency for:

payment initiation

duplicate button press

retry after timeout

callback duplicate

verify duplicate

reconciliation duplicate

order-paid projection/event

No duplicate Payment charge intent caused by repeated user submit unless explicitly new attempt.

Create:
09-payment-idempotency.md

K — Timeout / Unknown Outcome

Production payment can have uncertain network outcomes.

Implement/verify safe handling for:

initiation timeout

redirect generated but response lost

callback missing

callback arrives late

verification endpoint timeout

provider says unknown/pending

app restart during payment

Do NOT mark failed merely because callback did not arrive immediately.

Create:
10-payment-unknown-outcome.md

L — Reconciliation

Provide real reconciliation workflow:

query Pending/Unknown payments

provider status query through adapter

verify success/failure

transition idempotently

update Order only from verified result

retry with bounded policy

operator-visible unresolved state

Use existing background processing if appropriate.

Create:
11-payment-reconciliation.md

M — MassTransit / Outbox

Payment integration events must be emitted after commit via required messaging architecture.

Canonical:
MassTransit
PostgreSQL SQL / Database Transport
Outbox

NO RabbitMQ.

Verify events such as:

payment.succeeded.v1
payment.failed.v1
payment.refunded.v1 if applicable

No duplicate downstream Order Paid.

Create:
12-payment-outbox-events.md

N — Refund Compatibility

Do not redesign Returns/Refunds.

Verify existing refund foundation can route to production provider adapter where supported.

If provider target absent:

define adapter capability contract

keep real refund external execution fail-closed

do not fake provider refund success

Create:
13-payment-refund-compatibility.md

O — Admin / Operator Payment Controls

Use existing Admin operational UI.

No redesign.

Admin must be able to inspect:

internal PaymentId

OrderId

state

amount/currency

provider name

provider reference

created/updated time

verified/reconciled state

failure category safe message

If current UI supports appropriate action:

Retry Verify / Reconcile may be exposed with permission + idempotency.

Do NOT expose:

provider secrets

raw sensitive payloads

arbitrary state mutation

Create:
14-admin-payment-operations.md

P — Customer Payment UX

Preserve current Checkout/Payment Result UI.

Required truthfulness:

Pending stays Pending when provider truth is unknown

Success only after backend-verified success

Failed when verified failed

retry path safe

no fake success based solely on return URL

No UI redesign.

Create:
15-customer-payment-truthfulness.md

Q — Audit / Observability

Record safe structured signals:

PaymentId

OrderId

Provider

Operation

Result classification

attempt number

correlation/trace

reconciliation outcome

Never log:

card data

secret

API key

sensitive callback token if reusable

raw provider credential

OTel spans for:

initiate

verify

callback

reconcile

Create:
16-payment-observability.md

R — Abuse / Security Checks

Verify:

forged callback rejected

amount tampering rejected

cross-tenant payment lookup rejected

foreign customer cannot inspect payment

seller cannot mutate payment state

admin requires permission

open redirect not introduced

callback URL allowlist/config safety

SSRF risk avoided in provider endpoint config

Create:
17-payment-security-tests.md

S — Real Provider Adapter — Conditional Execution

If PROVIDER_TARGET_FOUND:

Implement/complete the real adapter using official documented behavior already available in repo/docs/config.

Required:

initiate

verify

callback

query/reconcile

error mapping

production fail-closed

integration tests using mock/fake transport if no external test credentials

real sandbox/test-provider call only if credentials/environment safely permit

Do NOT expose secrets in Result.

If NO_PRODUCTION_PROVIDER_TARGET:

Do NOT pick a provider yourself.

Instead ensure:

adapter boundary complete

provider factory/registration production-ready

secure config contract

contract test suite

fail-closed adapter

operator readiness

reconciliation readiness

Report:
REAL_PSP_PROVIDER_CONFIGURATION_REQUIRED

Evidence:
18-real-provider-adapter.md

T — Provider Contract Test Harness

Create reusable provider contract tests.

Any production adapter should have to pass:

initiation mapping

success verification

failure verification

unknown/pending

duplicate callback

invalid authenticity

amount mismatch

currency mismatch

provider reference mismatch

timeout

reconciliation

refund capability if implemented

Create:
19-provider-contract-tests.md

U — E2E Payment Scenarios

Using sandbox and real adapter mock/contract harness:

Scenario 1:
Successful verified payment.

Scenario 2:
Forged callback -> rejected.

Scenario 3:
Duplicate callback -> no duplicate Order Paid.

Scenario 4:
Timeout/unknown -> Pending -> reconciliation -> success.

Scenario 5:
Amount mismatch -> not succeeded.

Scenario 6:
Production config absent -> fail closed.

If actual provider target/test credentials exist:
run legitimate provider test-mode scenario where technically possible.

No direct DB mutation.

Create:
20-payment-e2e-scenarios.md

V — Browser Proof

Use same current Shopeiva-derived Checkout/Payment Result pages.

Capture:

checkout before payment

pending/redirect state if applicable

verified success

verified failure or safe failed test case

Admin Payment operation view

mobile critical payment UX

Prove:
UI remained visually unchanged except data/state truthfulness if required.

Create:
21-payment-browser-proof.md

W — Visual Regression Audit

Confirm touched payment UI:

CSS unchanged

JS behavior unchanged unless strictly required for correct backend state handling

animations unchanged

transitions unchanged

buttons unchanged visually

spacing unchanged

typography unchanged

responsive unchanged

payment result geometry unchanged

Any required behavioral adjustment must be minimal and documented.

Create:
22-payment-visual-regression.md

X — Validation

Backend:

dotnet restore src/backend/Tooba.slnx
dotnet build src/backend/Tooba.slnx
dotnet test src/backend/Tooba.slnx

Require:

warnings = 0
errors = 0
failed = 0
skipped = 0

Frontend if touched:

cd src/frontend
npm run typecheck
npm run lint
npm run test
npm run test
npm run test
npm run test
npm run build

Run:

payment

checkout

order

promotion

refund

security

provider contract

reconciliation

tenant isolation tests

Always:
git diff --check

Create:
23-final-validation.md

Y — Runtime

Keep running:

Backend

Frontend

Shopeiva

Verify:

health/live

health/ready

checkout

payment result

Admin payment operations

sandbox success remains intact

Create:
24-final-runtime.md

Z — Production Readiness Matrix

Report separately:

PAYMENT_FOUNDATION_READY
REAL_PROVIDER_ADAPTER_READY
REAL_PROVIDER_CONFIGURED
REAL_PROVIDER_CREDENTIALS_AVAILABLE
REAL_PROVIDER_TEST_TRANSACTION_PROVEN
REAL_BANK_TRANSACTION_PROVEN
REFUND_PROVIDER_READY
RECONCILIATION_READY

Never collapse these into one vague payment ready.

Create:
25-payment-readiness-matrix.md

AA — Commercial Readiness Recheck

Update:

SELLABLE_DEMO

PAYMENT_PRODUCTION_READINESS %

Product sale readiness %

Marketplace sale readiness %

PRODUCTION_GO_LIVE blockers

next largest commercial gap

If provider/credentials absent:
external blocker must be explicit.

Create:
26-commercial-readiness-after-payment.md

AB — Source of Truth

Update:

P06 = IN_PROGRESS
TB-P06-T021 = ACCEPTED
TB-P06-T022 = AWAITING_ARCHITECT_ACCEPT

PAYMENT_FOUNDATION = LIVE
PAYMENT_SANDBOX = LIVE
PAYMENT_CALLBACK_VERIFICATION = LIVE
PAYMENT_RECONCILIATION = LIVE
PAYMENT_IDEMPOTENCY = LIVE
PAYMENT_PRODUCTION_ADAPTER = LIVE | READY_FOR_PROVIDER | PARTIAL
REAL_PSP_CONFIG = PRESENT | REQUIRED
REAL_BANK_PAYMENT = PROVEN | NOT_PROVEN

VISUAL_CONTRACT = SHOPEIVA_LOCKED

Update:

docs/PROJECT-STATE.md

docs/ROADMAP.md

docs/ai/TOOBA-RECOVERY-CONTEXT.md

docs/prompts/START-HERE-IF-CHATGPT-IS-LOST.md

Acceptance Criteria

PASS requires ALL:

existing payment architecture audited

production provider contract complete

secure configuration

fail-closed production behavior

callback authenticity

amount/currency integrity

idempotency

unknown outcome handling

reconciliation

outbox integration

refund compatibility

Admin operational visibility

truthful customer payment state

security tests

provider contract harness

sandbox regression passes

real adapter implemented if provider target exists

no fake real-bank success

Shopeiva UI not redesigned

backend 0/0/0/0

frontend green if touched

runtime healthy

repo synchronized

Worker may report one of:

PRODUCTION_PAYMENT_FOUNDATION_READY
PRODUCTION_PAYMENT_ADAPTER_LIVE
REAL_PSP_PROVIDER_CONFIGURATION_REQUIRED

Worker must NOT report:

REAL_BANK_PAYMENT_PROVEN
unless an actual authorized provider test/production transaction is truly proven.

Worker must NOT report:

PRODUCTION_GO_LIVE_READY
USER_VISUAL_ACCEPTED
PRODUCT_FULLY_READY

Git

git add ...
git commit -m "feat harden production payment readiness [TB-P06-T022]"
git push origin main
git fetch origin
git rev-parse HEAD
git rev-parse origin/main
git status --short --branch

Require:
HEAD == origin/main
tracked tree clean

Result Contract

PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_WORKER_RESULT

Task-ID:
TB-P06-T022

Channel:
tooba-main

Status:
PASS | FAIL | BLOCKED | RECOVERY_CONFLICT

Summary:
...

Audit:

Payment aggregate:

Provider abstraction:

Sandbox:

Production adapter:

Callback:

Verification:

Idempotency:

Reconciliation:

Refund:

Admin operations:

Provider-Discovery:

target found:

provider:

credentials available:

secrets exposed:

Provider-Contract:

initiate:

verify:

callback:

query:

reconcile:

refund:

error mapping:

Security:

callback authenticity:

amount mismatch:

currency mismatch:

reference mismatch:

forged callback:

cross-tenant:

foreign customer:

seller mutation:

admin auth:

open redirect:

SSRF:

Idempotency:

initiation:

callback:

verify:

reconcile:

Order Paid:

duplicate charge prevention:

Unknown-Outcome:

timeout:

missing callback:

late callback:

pending:

restart:

recovery:

Reconciliation:

background/manual:

provider query:

bounded retry:

unresolved state:

idempotent:

Messaging:

MassTransit:

PostgreSQL SQL transport:

Outbox:

payment.succeeded:

duplicates:

Refund:

contract:

provider path:

fake success:

Admin:

payment list/detail:

provider reference:

reconcile action:

secrets hidden:

arbitrary state mutation:

Customer-UX:

pending truth:

success truth:

failed truth:

retry:

visual drift:

Provider-Adapter:

mode:

target:

implementation:

contract tests:

actual provider test:

real bank claimed:

E2E:

verified success:

forged callback:

duplicate callback:

unknown->reconcile:

amount mismatch:

missing production config:

direct DB mutation:

Visual-Fidelity:

Checkout CSS:

Checkout JS:

Payment Result:

animation:

transition:

responsive:

unauthorized deviation:

Validation:

backend restore:

backend build:

backend tests:

warnings:

errors:

failed:

skipped:

payment tests:

provider contract tests:

reconciliation tests:

security tests:

typecheck:

lint:

storefront:

customer:

frontend build:

git diff --check:

Runtime:

Backend:

Frontend:

Shopeiva:

health/live:

health/ready:

Readiness-Matrix:

PAYMENT_FOUNDATION_READY:

REAL_PROVIDER_ADAPTER_READY:

REAL_PROVIDER_CONFIGURED:

REAL_PROVIDER_CREDENTIALS_AVAILABLE:

REAL_PROVIDER_TEST_TRANSACTION_PROVEN:

REAL_BANK_TRANSACTION_PROVEN:

REFUND_PROVIDER_READY:

RECONCILIATION_READY:

Readiness-After:

SELLABLE_DEMO:

payment production readiness %:

Product sale readiness %:

Marketplace sale readiness %:

production blockers:

next largest commercial gap:

Source-of-Truth:

P06:

T021:

T022:

payment foundation:

sandbox:

production adapter:

real PSP config:

real bank payment:

visual contract:

Git:

commit:

push:

final HEAD:

origin/main:

synchronized:

tracked tree:

Architectural-Concerns:
...

Visual-Concerns:
...

Blockers:
...

END_TOOBA_WORKER_RESULT

After Result:

return to IDLE

do not self-issue another Task

keep Backend + Tooba Frontend + Original Shopeiva running where technically possible

END_TASK
