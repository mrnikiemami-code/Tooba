PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_REPAIR_TASK

Task-ID:
TB-P09-T022-R2

Parent-Task:
TB-P09-T022-R1

Channel:
tooba-main

WorkerId:
tooba-worker-01

AgentType:
cursor

Status:
REPAIR

USER_VISUAL_ACCEPTED=NO

Title:
Customer Tracking UI Proof — Real Owned Customer Session + Consolidated Package Rendering

Architect Review

TB-P09-T022-R1 is largely accepted:

FE 500 was diagnosed as frontend process/cold-restart ECONNRESET, not package SSR/DTO logic

real Admin Order Detail returns HTTP 200

Consolidated Package Admin UI rendered in browser

single/multi-seller visibility verified

create/member-lock/dispatch UI verified

guest Host fulfillment access is 200 through valid proof

tracking labels were polished

backend/frontend focused tests remain green

However, the Worker explicitly reported:

Customer-panel shell loads 200; full authenticated customer tracking UI may still require owned session beyond guest Host API.

T022-R1 acceptance required the actual supported Customer tracking view, not only the shell and Host endpoint.

This repair is ONLY to prove/fix the real owned-customer UI path.

Do NOT modify Consolidated Package domain semantics.
Do NOT weaken authorization.
Do NOT invent a new authentication mechanism.
Do NOT redesign Customer Panel.
Do NOT start TB-P09-T023.

Applicable Locks — READ FIRST

Read:
docs/architecture/TOOBA-LOCKS.md

Preserve:

customer sees only owned Orders

guest access requires existing guest proof

active Consolidated Package central tracking is primary

cancelled package is never primary

member Shipment tracking/history preserved

no raw GUID/enum/exception

no auth bypass

USER_VISUAL_ACCEPTED remains NO

1. Discover Real Customer Session Path

Inspect the actual Customer Panel authentication/runtime flow:

how a customer session is established in dev/runtime

existing dev-context/customer fixture helpers

current login/session/cookie/token mechanism

customer Order list/detail route

tracking/fulfillment BFF path

ownership resolution

whether the previous T021/T022 runtime Order has an actual Customer Party/Account that can own the Order

Create:
docs/evidence/TB-P09-T022/r2-customer-session-discovery.md

Do not use fabricated headers or direct Host calls as a substitute for the real UI session path.

2. Real Owned Customer Fixture

Use or create a supported runtime fixture through existing mechanisms so that:

Customer account/actor exists

Order is genuinely owned by that customer

Order has 2+ Sellers

active Consolidated Package exists

central tracking reference/provider exists

If existing fixtures support this, reuse them.

Do not mutate production-like data unsafely.

3. Actual Customer UI Runtime

Using the real customer session:

authenticate/login through supported current mechanism

open Customer Panel

open owned Order

open/view tracking/fulfillment information

verify HTTP 200

verify no frontend runtime/hydration error

verify central Consolidated Package tracking is rendered as PRIMARY

verify member Shipment tracking/history remains available according to current UI

verify Delivered package renders coherently

verify no-package Order preserves legacy shipment tracking

This must be browser/runtime UI proof, not only API proof.

4. Authorization Negative Proof

In the same real UI/session architecture prove:

Customer A cannot open Customer B Order

logout/no session cannot access authenticated-only Order path

wrong/foreign store context denied

no Order existence leakage beyond current 404/security convention

Do NOT weaken existing 404 behavior.

5. Rebuild Projection UI Proof

Use a pre-dispatch central package scenario:

MP-1 created

MP-1 cancelled

MP-2 created

In Customer UI:

MP-2 is current/primary

MP-1 is not shown as active tracking

no stale frontend cache

reload preserves correct primary package

6. Guest UI/BFF Proof

If the Storefront currently has a guest order tracking UI:

prove it end-to-end with valid guest secret

wrong secret denied

If there is NO guest tracking UI by current product design:

do NOT invent one

document the boundary

Host/BFF guest proof from T021-R1 remains sufficient for guest backend support

The key missing proof in this repair is the authenticated owned Customer UI.

7. Error UX

On tested Customer UI paths:

no raw GUID

no enum names

no raw exception

no JSON dump

unauthorized access does not reveal sensitive existence details

8. Focused Validation

Required:

existing CustomerFulfillmentTrackingTests remain green

ConsolidatedPackageTests remain green

frontend Customer Panel tracking tests

add regression for real owned-customer projection if missing

auth/ownership negative test

rebuild/current-primary test

no-package legacy tracking test

focused frontend build/typecheck for touched Customer Panel area

runtime browser smoke

recovery guard

git diff --check

Do NOT run unrelated full repo suite.

9. Runtime Evidence

Update:
docs/evidence/TB-P09-T022/

Add:

r2-customer-session-discovery.md

r2-owned-customer-fixture.md

r2-customer-ui-runtime.md

r2-auth-negative.md

r2-rebuild-customer-ui.md

r2-guest-ui-boundary.md

r2-focused-validation.md

r2-recovery-sot.md

Record:

tested route

auth mechanism

human Order/package reference

HTTP status

browser outcome

Do not put secrets into committed evidence.

10. Recovery / Pipeline

Phase: P09

Last Architect-accepted: TB-P09-T021-R1

TB-P09-T022 under repair

Current Repair: TB-P09-T022-R2

Last Implementation on completion: TB-P09-T022-R2

USER_VISUAL_ACCEPTED=NO

Worker Next State: IDLE

do NOT invent/start TB-P09-T023

Run recovery guard.

11. Git

Required:

preserve user-authored changes

commit + push if changes/evidence needed

HEAD==origin/main

no task-owned uncommitted changes

Acceptance

PASS only if ALL:

real supported customer session path is identified

real owned Customer can open actual tracking UI

customer tracking UI renders HTTP 200 without runtime errors

active Consolidated Package is primary in the Customer UI

Delivered state coherent

cancelled package never remains primary

rebuilt package becomes current primary

no-package legacy tracking still works

member tracking/history remains preserved

cross-customer/cross-store access remains denied

no auth bypass introduced

no raw technical leakage

T021/T022 package regressions remain green

recovery guard passes

HEAD==origin/main

Worker IDLE

no TB-P09-T023 invented

USER_VISUAL_ACCEPTED=NO.

Result Contract

Return ONLY canonical BRIDGE-WAKE-V1 result with:

Summary
Applicable-Locks
Customer-Session-Discovery
Owned-Customer-Fixture
Customer-UI-Runtime
Authorization-Negative
Rebuild-Customer-UI
Guest-UI-Boundary
Error-UX
Focused-Validation
Recovery-SoT
Git
Architectural-Concerns
Visual-Concerns
Blockers

END_TOOBA_REPAIR_TASK