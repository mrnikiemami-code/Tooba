PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

TASK-ID: TB-P07-T007-R1
PARENT-TASK: TB-P07-T007
PROJECT: Tooba
PHASE: P07
CHANNEL: tooba-main
STATUS: ISSUED
TASK-TYPE: FOCUSED ARCHITECTURAL REPAIR — CATEGORY-SPECIFIC ATTRIBUTE BEHAVIOR
WORKER-POLICY: ONE WORKER = ONE ACTIVE TASK
BRIDGE-TASK-UUID: a408d52f-2f11-440d-b3e7-2a08d8a4d7c2

Architect Review of T007

TB-P07-T007 is NOT fully architecturally accepted yet.

The UI implementation is functionally good and the approved Category Tree remains untouched.

However the Result exposed a real architecture mismatch:

Current implementation:

IsFilterable is definition-global
IsComparable is definition-global
IsVariantAxisAllowed is definition-global
only required behavior has a per-category override

This is insufficient for Tooba.

Goal

Move category-dependent attribute behavior to CategoryAttributeAssignment / binding level while preserving global capability constraints on the canonical AttributeDefinition.

CategoryAttributeAssignment / CatalogCategoryAttributeBinding must support category-specific effective flags:

IsRequired
IsFilterable
IsVariantAxis
IsComparable
DisplayOrder

AttributeDefinition.IsVariantAxisAllowed = capability only; CategoryAttributeAssignment.IsVariantAxis = enabled here.

Update CatalogCategorySchemaResolver, migration, API, Admin UX (تنظیم برای این دسته / تنظیم رفتار), tests, and docs/catalog/CATEGORY-ATTRIBUTE-SCHEMA.md.

Acceptance: assignment carries per-category flags; effective schema resolves them; inheritance + child override; T008/Product Master ready; validation green; Git synchronized.

Result Contract: BEGIN_TOOBA_WORKER_RESULT … END_TOOBA_WORKER_RESULT (see Bridge envelope).

END_TASK
