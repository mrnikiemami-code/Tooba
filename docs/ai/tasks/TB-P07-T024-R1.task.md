PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_TASK

Task-ID:
TB-P07-T024-R1

Parent-Task:
TB-P07-T024

Channel:
tooba-main

Title:
Product Admin Completeness Repair — Locale-Correct Product Identity + Brand/Model/Codes + Final Reference Alignment

Role:
You are the implementation Worker for Tooba.
Repair ONLY the remaining Product Admin completeness issues.
Do NOT invent the next task.
Do NOT change unrelated architecture.

Repository / Safety:

Resolve repository root:
git rev-parse --show-toplevel

Record branch / HEAD / origin/main / git status --short.

Preserve unrelated user/agent changes.

Do NOT reset, clean, stash, checkout-away, or overwrite unrelated dirty files.

Read current Product/Catalog/Localization/Brand docs and relevant code before changing anything.

TB-P07-T024 is NOT fully accepted yet.

The Worker correctly improved:

General depth

real Translations editing

locale isolation

real Media gallery

Product-owned summary cards

no Product.Price / Product.Stock

full validation

However the Result still reports deferred Product completeness items:

brand assign deferred

model/series deferred

GTIN/EAN/UPC/barcode deferred

and the user explicitly clarified that a fixed English Product Name field is semantically wrong for a multilingual Product.

This repair must close those gaps correctly, without introducing bad fixed-language fields or commercial Offer data.

Product is multilingual.

FORBIDDEN Product Master scalar fields such as:

NameFa

NameEn

EnglishName

PersianName

ArabicName

TitleFa

TitleEn

Localized human text MUST come from locale-based translation/localized content storage.

Canonical rule:

Product Master
→ non-linguistic / language-neutral identity and relations

Localized Product Content
→ fa
→ en
→ ar
→ any enabled locale

For each locale, use the existing localized architecture for:

Product name/title

short summary

full description

localized SEO content where ownership belongs

Do NOT add a separate fixed "نام انگلیسی" field to General.

If any current General field is language-specific:

move it to Translations

keep General language-neutral

General should contain Product-owned language-neutral fields/relations such as, where supported and justified:

Category (Level-3)

Brand

Model / Series

Catalog internal code

GTIN / EAN / UPC / barcode

lifecycle/status metadata

relevant non-localized identifiers

timestamps in VIEW

primary media preview if useful

General should NOT duplicate:

localized name

localized short summary

localized full description
when those belong to Translations.

If current architecture intentionally stores one canonical fallback title/summary seam:

preserve compatibility

make locale ownership explicit

avoid presenting it as a second independent non-localized human title

do not create conflicting sources of truth

Translations must be the clear source for locale-specific Product content.

For each enabled locale:

name/title

short summary

full description

completion state

Save/Cancel

dirty-state protection

locale isolation

If SEO remains in dedicated SEO tab:

do not duplicate SEO fields here unless current architecture explicitly shares them

show clean handoff/status instead

Status labels:

کامل

ناقص

ایجاد نشده

Equivalent localized labels for English UI.

Editing English must not overwrite Persian.
Editing Persian must not overwrite English.

Audit the existing Brand capability before implementation.

If Brand is already a Catalog-owned entity/projection and Product has or should have an extraction-safe Brand relationship:

implement real Brand assignment in Product Admin

searchable human selector

no raw BrandId

VIEW/EDIT

backend-authoritative validation

no cross-module SQL JOIN if Brand ownership is separate

use existing contract/gateway if necessary

If Brand is already represented as an effective Category Attribute instead:

do NOT create a conflicting second Brand source

preserve one source of truth

document the chosen canonical model

PASS must not leave a visible fake/read-only Brand field that suggests functionality but cannot be changed.

Model/Series is a legitimate Product-owned concept for many categories but must not be forced globally if domain design says otherwise.

Audit current Product model and category schema.

Choose ONE consistent approach:

A) Product-owned Model/Series seam

if already present or broadly justified across Product types

persist with clear backend validation

VIEW/EDIT

OR

B) Category Attribute

if Model/Series is category-specific

represent it through effective Category schema

do not add duplicate Product scalar

Do NOT leave duplicate model values in both General and Attributes.

Document canonical ownership.

Product may have a Catalog-owned internal code if an existing convention exists.

Requirements:

clearly distinct from Seller Offer SKU

human-editable only if current domain permits

uniqueness enforced if architecture requires

no raw GUID as substitute

Do not invent Seller SKU.

Audit whether Product already has a standards-code seam or whether one is justified as Catalog-owned identity.

If supported/appropriate:

implement a Product identifier collection or existing canonical seam

support type such as GTIN/EAN/UPC where current conventions allow

validation should be deterministic but not over-restrictive without domain rule

human labels

no hardcoded single-country assumptions

VIEW/EDIT

localized error UX

Prefer a normalized identifier model over multiple fixed scalars if architecture already supports typed identifiers.

If architecture has no safe identifier model and introducing one requires a broad ADR:

raise Architectural Concern instead of inventing poor schema

BUT do not show a fake non-functional field in UI

Use the locked reference again:

D:\Users\User\source\repos\SarvNewVerRequirment\reference\Image\ChatGPT Image Aug 29, 2026, 06_33_46 PM.png

Compare current runtime side-by-side.

The key objective:
Product Admin must feel complete in content density and hierarchy while preserving Tooba architecture.

Do not copy:

price

stock

inventory

Seller offer fields

Replace those visual slots with Product-owned:

Brand / Model / Catalog Code / identifiers

readiness

translation completeness

media

variants

lifecycle

history

Preserve the T024 gallery improvements.

Do NOT regress:

large primary

thumbnails

reorder

set primary

AltText

unassign

readiness

Media DAM/upload library may remain deferred ONLY if:

no fake/dead CTA is exposed

assigned media still looks like a real gallery

deferred capability is not advertised as complete

Preserve localized error handling.

No normal UI:

Bad Request

raw HTTP status

raw error code

exception

stack trace

raw JSON

All new Brand/Model/Identifier validations must use the same localized mapping pattern.

Before PASS inspect live Product Admin for:

fixed language-specific General fields like English Name

duplicate sources of truth

read-only fake Brand assignment

unsupported model/barcode placeholders

به‌زودی

Coming soon

dead CTA

raw GUID

raw error

giant empty shell sections

Visible = functional.

Preserve:

Product != Offer

no Product.Price

no Product.Stock

no Product.Inventory

no Variant.Price

no Variant.Stock

no Pricing joins

no Inventory joins

Product Category Level-3

Category-schema-only Product attributes

real Variants

real Media gallery

SEO

Publishing lifecycle

History

dirty guard

canonical AppDataGrid

Category Tree

Category/Product navigation

Shopeiva

Backend:

locale-based Product names remain isolated

no NameFa/NameEn fixed fields introduced

Brand assignment behavior according to chosen canonical model

Model/Series ownership behavior

Catalog code behavior

identifier behavior if implemented

localized validation

no Product.Price/Stock

no Pricing/Inventory joins

full existing Product suites remain green

Frontend:

General has no fixed English-name field

Translations edits fa/en independently

completion statuses

Brand selector/edit if supported

Model/Series UI according to canonical ownership

Catalog code UI

GTIN/EAN/UPC UI if implemented

no duplicate source of truth

reference-layout regression

fa/en

RTL/LTR

error UX

no visible stub/dead field

Media gallery preserved

Run:

backend restore

backend build

backend full tests

frontend typecheck

frontend lint

frontend full relevant tests

frontend production build

git diff --check

Require:

0 errors

0 failed

0 skipped

no new warnings

Keep alive:

Backend :5088

Frontend :3000

Shopeiva :3001

Verify live:

Product VIEW

Product EDIT

General

Translations

Attributes

Variants

Media

SEO

Publishing

History

Commit + push after validation.

Require:

HEAD == origin/main

scoped tracked tree clean

unrelated user changes preserved

PASS only if ALL:

no fixed language-specific Product name field exists in General

localized Product names/content are clearly locale-based

fa/en editing is isolated

General is language-neutral

Brand ownership is canonical and usable if supported

Model/Series ownership is canonical, not duplicated

Catalog code is distinct from Seller Offer SKU

Product identifiers are correctly modeled if architecture supports them

unsupported fields are not shown as fake controls

locked visual reference is used again

Product Admin remains dense/complete

Media gallery preserved

no Product.Price/Product.Stock/Inventory

no Pricing/Inventory joins

localized errors preserved

no visible incomplete UX

full validation green

runtime alive

Git synchronized

USER_VISUAL_ACCEPTED remains NO until the user later reviews the final live Product Admin.

Return ONLY:

PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_WORKER_RESULT

Task-ID:
TB-P07-T024-R1

Parent-Task:
TB-P07-T024

Channel:
tooba-main

Status:
PASS | FAIL | BLOCKED | RECOVERY_CONFLICT

Summary:
...

Locale-Model:

fixed NameFa:

fixed NameEn:

fixed EnglishName:

General language-neutral:

locale-based name:

locale-based summary:

locale-based description:

fa/en isolation:

global compatibility seams:

Brand:

current canonical ownership:

assignment implemented:

selector:

backend validation:

duplicate source created:

Model-Series:

canonical ownership:

General or Attribute:

duplicate source:

VIEW/EDIT:

Catalog-Code:

canonical:

Seller SKU distinction:

uniqueness:

VIEW/EDIT:

Product-Identifiers:

model/seam:

GTIN:

EAN:

UPC:

barcode:

validation:

unsupported/fake fields exposed:

Visual-Contract:

reference opened:

runtime compared:

density/composition improvements:

commercial fields copied:

pixel-perfect claimed:

Translations:

real editor:

locale statuses:

fa:

en:

dirty guard:

SEO duplication:

Media:

gallery preserved:

primary:

thumbnails:

reorder:

AltText:

dead CTA:

DAM invented:

Error-UX:

raw Bad Request:

raw HTTP:

raw codes:

localized new errors:

Boundary:

Product != Offer:

Product.Price:

Product.Stock:

Product.Inventory:

Variant.Price:

Variant.Stock:

Pricing joins:

Inventory joins:

Incomplete-UX-Audit:

fixed English-name field:

duplicate source of truth:

fake Brand field:

fake Model field:

fake barcode field:

به‌زودی:

Coming soon:

dead CTA:

raw GUID:

visible unfinished section:

Validation:

backend restore:

backend build:

backend full tests:

frontend typecheck:

frontend lint:

frontend tests:

frontend production build:

passed:

failed:

skipped:

warnings:

new warnings:

git diff --check:

Runtime:

Backend:

Frontend:

Shopeiva:

Product VIEW:

Product EDIT:

General:

Translations:

Media:

kept alive:

Git:

branch:

commit:

push:

final HEAD:

origin/main:

synchronized:

tracked tree:

unrelated user changes:

Architectural-Concerns:
...

Visual-Concerns:
USER_VISUAL_ACCEPTED=NO

Blockers:
...

END_TOOBA_WORKER_RESULT

After Result:

Worker -> IDLE / Waiting

do not invent next task

do not add any explanation outside the Result envelope

END_TOOBA_TASK