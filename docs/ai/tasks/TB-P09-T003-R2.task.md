PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_REPAIR_TASK

Task-ID:
TB-P09-T003-R2

Parent-Task:
TB-P09-T003-R1

Channel:
tooba-main

WorkerId:
tooba-worker-01

AgentType:
cursor

Status:
REPAIR

USER_VISUAL_ACCEPTED=NO

Title:
Admin Orders Repair R2 — Real Fresh-Order Start, Manual/Card-to-Card Confirmation, List Return/Refund Visibility

Architect Review

TB-P09-T003-R1 is NOT architect-accepted yet.

R1 fixed important defects:

Operations menu clipping

restrictive Orders maxWidth

Persian operation/refund labels

internal-note delete rule

lifecycle actions from ReadyToFulfill onward

But the Result explicitly leaves two architectural/product gaps and one acceptance violation:

Admin card-to-card/manual payment confirmation not modeled

Orders list cannot clearly surface Return/Refund state without composed visibility

The required clean fresh Order was NOT driven from initial registration/payment-pending state; R1 reused an already ReadyToFulfill checkout

The user specifically wants to prove that a newly registered Order can be moved step-by-step from the beginning.

This repair must close those gaps with minimal UI change.

UI LOCK

Do NOT redesign the Orders grid or Order detail.

Allowed UI changes ONLY:

minimal action(s) required for manual/card-to-card payment confirmation

minimal correction to existing status cells so Return/Refund state is visible

defect-only feedback

Do NOT:

add a new page

add a new navigation system

add a large new panel

restyle the grid

add multiple new permanent columns unless absolutely necessary

1. Mandatory Discovery — Payment Model

Before implementation inspect current Payment architecture for:

payment methods/providers

wallet

online gateway

manual/card-to-card support if partially present

payment attempt status model

evidence/receipt data

admin payment operations

payment success integration events

order transition to Paid/ReadyToFulfill

idempotency

authorization

Inspect 2–3 analogous admin mutation flows.

Do NOT fake payment success in frontend.
Do NOT directly mutate Order status.

2. Manual / Card-to-Card Payment Confirmation

Implement the smallest canonical admin flow inside the existing Payment boundary.

Target business semantics:

For an Order using a manual/card-to-card payment method:

Initial:

Order exists

PaymentStatus = awaiting/manual confirmation

Order is NOT yet treated as paid/fulfillable

Admin operation(s):

تأیید واریز / تأیید پرداخت

optionally رد واریز only if the current Payment state model can support it cleanly

On confirm:

existing Payment domain/application command marks the payment as succeeded/confirmed

emits/uses the same canonical payment-success effects/integration event path as other successful payments

Order becomes eligible for the next lifecycle stage through existing architecture

no direct Checkout status hack

idempotent confirmation

duplicate confirm must not double-credit wallet/settlement/order state

If receipt/evidence attachment already exists, show/read it.
If it does not exist, do NOT build a large upload subsystem in this repair.

Payment Method Availability

If manual/card-to-card method configuration is not yet present:

add the smallest configuration/data seed needed in LOCAL DEV only

do not build a full payment-provider settings redesign here

3. Operations Matrix — Manual Payment States

Backend operations composer must support the real state.

At minimum:

Awaiting manual/card-to-card confirmation

Possible:

تأیید واریز

لغو سفارش if canonical cancellation policy allows

رد واریز only if implemented cleanly

Not possible:

processing

packing

shipment

delivery

return

refund

seller payout

After confirmation

Possible next operation:

شروع پردازش / current canonical equivalent

The menu must update immediately after successful confirmation.

Do NOT hardcode frontend state transitions.

4. True Fresh Order E2E — From Registration

R1 reused an existing ReadyToFulfill Order. That is insufficient.

Create ONE truly fresh Order through a supported application/UI/API path.

Required start:

Order registration/checkout creation

Payment pending/manual confirmation

Admin sees تأیید واریز

Admin confirms payment

Paid

Processing

Packed

Shipment created

Tracking

Dispatch

Delivered

Return requested

Return approved

بازگشت وجه

Settlement/adjustment behavior if applicable

At EACH step record:

Order status

Payment status

Fulfillment status

Return status

Refund status

Settlement status

projected operation codes

rendered Persian action labels

No SQL insertion of intermediate/final states.
Use application/domain/API paths.

5. Clean Development Data

The user asked to remove confusing historical test Orders.

Safety rules:

LOCAL/Development only

verify DB/environment explicitly

prefer a fresh local dev DB or supported reset/seed mechanism

preserve product/seller/payment/shipping master data

do not touch non-development data

do not perform risky ad-hoc destructive deletes if relationships are complex

Outcome:

user can see a clean Orders list centered around the fresh test Order

if full delete is unsafe, use a new isolated local dev DB and document it

6. Orders List — Return/Refund Visibility

User observed an Order had both Return and Refund but the list did not show that clearly.

Do NOT add a new large status system.

Preferred solution:

compose existing visible وضعیت cell from backend list DTO so the most operationally relevant current state is shown

examples:

مرجوعی در انتظار بررسی

مرجوعی تأیید شده

بازگشت وجه در انتظار

بازگشت وجه انجام شد

keep پرداخت column for payment status semantics

keep Order lifecycle semantics separate internally

If one cell cannot represent all semantics cleanly:

add at most ONE compact composed operational-status field/column, only if necessary

no redesign

no duplicate technical status columns

The list DTO/backend should provide authoritative composed state.
Frontend must not infer by guessing across multiple endpoints.

7. Return vs Refund Labels

Locked:

Return = مرجوعی

Refund = بازگشت وجه

No raw refund, returned, internal enum names, or operation codes in FA UI.

8. Settlement Action Visibility

Reconcile the matrix for seller finance actions.

For a delivered Order:

if seller payout/settlement is actually actionable in current architecture and user has capability, show the appropriate operation

if there is an active return/refund hold, do not offer payout incorrectly

after settlement completed, do not show duplicate payout

valid return may still occur while eligible

prior settlement immutable; seller adjustment on refund remains accepted behavior

Do NOT create duplicate finance commands.

9. Permissions

Verify:

View-only: no mutations

payment operator: manual confirmation only if permitted

fulfillment operator: relevant shipment actions

return/refund operator: relevant actions

finance operator: settlement action where supported

Backend authoritative.
No frontend-only permission logic.

10. Error UX

Human FA/EN messages for:

payment already confirmed

invalid manual confirmation

payment method not manual

cancellation no longer allowed

invalid lifecycle transition

duplicate refund

settlement hold/forbidden payout

No raw codes/JSON/exceptions.

11. Focused Tests

Backend required:

manual payment confirm happy path

duplicate confirm idempotency

confirm on wrong payment method rejected

payment success propagates to Order/next-stage eligibility

composer projects confirm action only in correct state

after confirm, processing action appears

list composed status for return/refund states

settlement action visibility/hold

existing cancel guard

note delete regression

existing return/refund/settlement regression

Frontend required:

Operations menu shows تأیید واریز

action executes and menu refreshes

status cell shows return/refund human state

no raw refund text

grid widths remain resizable

button appearance unchanged

existing detail UI unchanged

Also:

relevant build/typecheck/lint

recovery guard

git diff --check

No unrelated full repository suite.

12. Runtime / Browser Smoke — REQUIRED

Use:
Backend: http://127.0.0.1:5088
Frontend: http://127.0.0.1:3000

Must show in runtime:

clean/fresh Order registration

payment awaiting manual confirmation

Orders menu exposes تأیید واریز

confirmation succeeds

next operation becomes processing

continue through packing/shipment/tracking/dispatch/delivery

request Return

approve Return

execute بازگشت وجه

list now visibly reflects Return/Refund state

settlement/adjustment behavior remains correct

no false هیچ عملیاتی مجاز نیست during actionable stages

If any step cannot be completed, return PARTIAL/BLOCKED with exact reason.
Do NOT claim PASS with only pre-seeded ReadyToFulfill data.

13. Recovery / Pipeline

Update:

Phase: P09

Last Architect-accepted: TB-P09-T003

TB-P09-T003 remains under repair

Current Repair: TB-P09-T003-R2

Last Implementation on completion: TB-P09-T003-R2

USER_VISUAL_ACCEPTED=NO

Worker Next State: IDLE

do NOT invent/start TB-P09-T004

Run recovery guard.

14. Evidence

Create/update:
docs/evidence/TB-P09-T003/

Required:

r2-payment-discovery.md

r2-manual-payment-confirmation.md

r2-operations-matrix.md

r2-clean-dev-environment.md

r2-fresh-order-from-registration-e2e.md

r2-list-return-refund-visibility.md

r2-settlement-action-visibility.md

r2-runtime-visual-smoke.md

r2-recovery-sot.md

15. Git

Required:

preserve user-authored changes

commit repair

push origin/main

HEAD==origin/main

no task-owned uncommitted changes

Acceptance

PASS only if ALL:

true fresh Order created from registration

manual/card-to-card pending state is genuinely modeled

Admin تأیید واریز operation is real and backend-authoritative

confirmation uses Payment boundary and canonical success propagation

duplicate confirm is idempotent

next operation appears after confirmation

full lifecycle is executable from fresh Order through refund

Orders list visibly surfaces Return/Refund state

مرجوعی and بازگشت وجه are distinct

no raw refund/internal enum text in FA UI

settlement action visibility is correct

no false empty operations menu on actionable states

grid width repair remains intact

current visual design remains intact

permissions backend-authoritative

focused tests green

runtime/browser smoke complete

recovery guard passes

HEAD==origin/main

Worker returns IDLE

no TB-P09-T004 invented

USER_VISUAL_ACCEPTED=NO.

Result Contract

Return ONLY canonical BRIDGE-WAKE-V1 result with:

Summary
Payment-Discovery
Manual-Payment-Confirmation
Operations-Matrix
Clean-Dev-Environment
Fresh-Order-From-Registration-E2E
Orders-List-Return-Refund-Visibility
Settlement-Action-Visibility
Permissions
Error-UX
Focused-Validation
Runtime-Visual-Smoke
Recovery-SoT
Git
Architectural-Concerns
Visual-Concerns
Blockers

END_TOOBA_REPAIR_TASK