PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

TASK-ID: TB-P05-T015
PROJECT: Tooba
PHASE: P05 — Operational Surface Integration
CHANNEL: tooba-main
STATUS: ISSUED
TASK-TYPE: PRODUCT IMPLEMENTATION
WORKER-POLICY: ONE WORKER = ONE ACTIVE TASK

Title

Customer Profile Editing — Add Missing Backend Capability Behind Existing Shopeiva Profile UI

Context

TB-P05-GOV-MIGRATION-BRIDGE-WAKE-V1 is Architect-ACCEPTED.
TB-P05-T014 is Architect-ACCEPTED.

Locked rule:

BACKEND capability exists but Shopeiva lacks UI
→ add the minimum Shopeiva-compatible UI

SHOPEIVA meaningful capability exists but Backend lacks capability
→ add the missing Backend capability in the correct owning module
→ connect the existing Shopeiva UI to live backend truth

NO REDESIGN.

Earlier Customer Panel work exposed Profile as read-only. Shopeiva already contains meaningful profile-editing UI.

Objective

Make the existing Shopeiva customer profile page genuinely editable through backend-owned, server-authorized profile capability while preserving Identity/User != Party/Profile boundaries.

Critical UI Contract

SHOPEIVA STRUCTURE = LOCKED UI CONTRACT

Reuse the actual Shopeiva profile page, layout, grouping, controls, validation placement, responsive behavior and visual language.

DO NOT redesign.
DO NOT replace the form shell.
DO NOT invent a separate customer-account UI.

Allowed:

live bindings

minimum validation/error/success states

Tooba blue token

Persian localization

accessibility fixes without visual redesign

Repository Recovery

Run first:

git rev-parse --show-toplevel
git fetch origin
git branch --show-current
git rev-parse HEAD
git rev-parse origin/main
git status --short --branch

Expected predecessor:
30969343aba0fb4ecf4488da95770773a09c713e

Require:

branch main

HEAD == origin/main

safe/known working tree

If material conflict exists, return RECOVERY_CONFLICT.

No force push.
No destructive reset.
No silent stash.
No history rewrite.

Required Reading

AGENTS.md

docs/PROJECT-STATE.md

docs/ROADMAP.md

docs/ai/TOOBA-PIPELINE-PROTOCOL.md

docs/ai/TOOBA-PIPELINE-CONTROLLER.md

docs/ai/TOOBA-RECOVERY-CONTEXT.md

docs/prompts/START-HERE-IF-CHATGPT-IS-LOST.md

docs/evidence/TB-P05-T003/

docs/evidence/TB-P05-T014/

actual Shopeiva customer profile source/routes/components

Part A — Field Ownership Inventory

Before coding, inspect actual Shopeiva profile fields.

Create:
docs/evidence/TB-P05-T015/01-profile-field-ownership-inventory.md

For each field record:

field

meaning

current backend capability

owning module

editable/read-only/deferred

reason

binding

Inspect at least:

first name

last name

display name

email

mobile

avatar

birth date

gender if present

password/security fields if present

other meaningful profile metadata

Do not blindly persist every template field.

Part B — Ownership Boundary

Preserve:

Identity/User != Party/Organization/Profile

Authentication identifiers and credentials remain owned by Identity/Auth.

Do NOT update email/mobile/password through a generic profile repository if that bypasses:

uniqueness

OTP

verification

MFA/security policy

Descriptive customer profile data belongs in the correct Party/Profile boundary.

If a dedicated CustomerProfile capability is the narrowest extraction-safe option, use it.

No cross-module ORM graph.

Part C — Profile Write Capability

Implement the smallest safe profile write capability needed by Shopeiva.

Expected low-risk editable fields may include repository-consistent equivalents of:

FirstName

LastName

DisplayName

BirthDate optional

Avatar reference only if existing media seam safely supports it

Only implement fields justified by Shopeiva + product value + architecture.

Do not collect sensitive data merely because the template displays placeholders.

Part D — Email / Mobile

Inspect current Identity rules.

If email/mobile are authentication identifiers:

do NOT mutate through ordinary profile save

connect an existing verified-change flow if one exists

otherwise render read-only/honest state

report missing verified-change capability as Architectural Concern

Never bypass verification/security.

Part E — Password / Security

If Shopeiva contains password/security UI:

bind only to an existing safe Auth capability

otherwise keep honest/disabled or route to current security boundary

do NOT store password in profile

do NOT invent insecure password update

Part F — Avatar

If Shopeiva supports avatar:

connect only if existing media seam safely supports it

otherwise preserve display and document unavailable upload

do not deep-build media pipeline here

Part G — Server Authorization

Authority = authenticated actor.

Forbidden:

request-supplied UserId as authority

editing another customer by URL/body ID

frontend ownership authority

Require:

A reads/updates A

B cannot update A

missing actor → 401

Part H — Validation

Server-side practical validation:

names bounded

display name bounded

birth date reasonable if supported

Identity-owned fields delegated to Identity rules

Frontend validation does not replace backend validation.

Part I — Shopeiva Binding

Reuse actual Shopeiva profile form.

Bind:

initial live values

save

validation

success

error

read-only Identity fields where required

If Shopeiva contains a meaningful field unsupported by backend:
apply bidirectional convergence:

implement it if small, safe and correctly owned

otherwise report Architectural Concern

never fake persistence

Part J — Dashboard Consistency

After successful profile update:

customer dashboard/header identity should reflect safe updated display/profile values where appropriate

use existing revalidation/state approach

do not introduce new global state architecture solely for this task

Development Evidence

Use deterministic Development-only demo profile data if needed.
No real personal data.

Convergence Evidence

Create:
docs/evidence/TB-P05-T015/02-shopeiva-profile-backend-convergence.md

Record:

Shopeiva field/feature

backend before

backend after

owning module

editable/read-only

binding path

deferred capability

Out of Scope

Do NOT deep-build:

MFA

account recovery

verified email/mobile change unless already supported

password reset architecture

KYC

identity merge

social login

advanced avatar/media pipeline

notification preferences unless an existing real capability already owns them

Architecture Guardrails

Forbidden:

cross-module SQL join

foreign-module DbContext navigation

generic profile storage owning credentials

frontend ownership authority

unverified login-identifier mutation

insecure password handling

All Tooba-owned public C#/TS Classes/Interfaces/Methods/Properties added or modified require meaningful Persian XML/TSDoc per project policy.

Evidence

Create:
docs/evidence/TB-P05-T015/

Required:
01-profile-field-ownership-inventory.md
02-shopeiva-profile-backend-convergence.md
03-profile-desktop-before-save.png
04-profile-desktop-after-save.png
05-profile-validation.png
06-profile-readonly-identity-fields.png
07-profile-auth-isolation-proof.md
08-profile-dashboard-reflection.png
09-profile-mobile-390x844.png
10-profile-data-map.md
11-shopeiva-profile-fidelity.md
12-security-field-handling-proof.md

Tests

Backend minimum:

read own profile

update own allowed fields

A cannot update B

missing actor 401

invalid values rejected

Identity-owned fields cannot bypass verification rules

credentials/password not persisted in profile

read projection reflects update

Frontend minimum:

initial mapping

save mapping

validation errors

success state

read-only Identity fields when appropriate

401/403 behavior

dashboard/header reflection

mobile rendering

Validation

Backend:
dotnet restore src/backend/Tooba.slnx
dotnet build src/backend/Tooba.slnx
dotnet test src/backend/Tooba.slnx

Require:
warnings = 0
errors = 0
failed = 0
skipped = 0

Frontend:
cd src/frontend
npm ci
npm run typecheck
npm run lint

Run all supported suites including:
grid
workspace
product workspace
storefront
cart
checkout/order
payment
seller
customer
admin
public merchandising
PDP
reviews
wishlist
addresses
profile

Then:
npm run build
git diff --check
git status --short --branch

Acceptance Conditions

PASS requires:

actual Shopeiva Profile UI reused

no redesign

field/ownership inventory completed

safe profile write backend exists

Identity vs Profile/Party boundary preserved

server-side actor ownership

A/B isolation

authentication identifiers cannot bypass verification/security

password/security only through correct boundary

no fake persistence

UI reflects successful updates

desktop valid

mobile 390x844 valid

no cross-module SQL

full validation green

Source of Truth

Update:

docs/PROJECT-STATE.md

docs/ROADMAP.md

docs/ai/TOOBA-RECOVERY-CONTEXT.md

docs/prompts/START-HERE-IF-CHATGPT-IS-LOST.md

Expected before Architect review:
PIPELINE = BRIDGE-WAKE-V1
TB-P05-GOV-MIGRATION-BRIDGE-WAKE-V1 = ACCEPTED
TB-P05-T014 = ACCEPTED
TB-P05-T015 = AWAITING_ARCHITECT_ACCEPT
P05 = IN_PROGRESS

Worker must NOT mark Architect ACCEPT.

Preserve:
FAST CONNECT FIRST
SHOPEIVA STRUCTURE = LOCKED UI CONTRACT
BACKEND ↔ SHOPEIVA MUST CONVERGE BOTH WAYS
NO REDESIGN
NO FAKE BUSINESS DATA
SELL QUICKLY
P06 ALL CORE SURFACES LIVE

Git

git diff --check
git status --short --branch
git add ...
git commit -m "feat add live customer profile editing behind Shopeiva UI [TB-P05-T015]"
git push origin main
git fetch origin
git rev-parse HEAD
git rev-parse origin/main
git status --short --branch

Require:
HEAD == origin/main
working tree clean

Result Contract — BRIDGE-WAKE-V1

PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_WORKER_RESULT

Task-ID:
TB-P05-T015

Channel:
tooba-main

Status:
PASS | FAIL | BLOCKED | RECOVERY_CONFLICT

Summary:
...

Repository-Recovery:
...

Field-Inventory:

fields found:

Profile-owned:

Identity-owned:

read-only:

deferred:

Domain:

owning module:

profile model:

actor identity:

editable fields:

Authorization:

actor source:

A own:

B update A:

missing actor:

frontend authority:

Identity-Security:

email:

mobile:

password:

verification bypass:

Avatar:

capability:

behavior:

Operations:

read:

update:

validation:

dashboard reflection:

Shopeiva-Reuse:

profile page:

form layout:

field grouping:

mobile:

redesign:

minimal additions:

Evidence:
...

Tests:
...

Validation:
...

Architecture:

cross-module SQL:

foreign DbContext navigation:

Identity credentials in profile:

frontend ownership authority:

Source-of-Truth:

pipeline:

migration:

T014:

T015:

P05:

Git:
...

Architectural-Concerns:
...

Visual-Concerns:
...

Blockers:
...

END_TOOBA_WORKER_RESULT

BRIDGE-WAKE-V1 Rules

After successful Result submission, Coding Agent returns to IDLE.
No continuous idle polling.
BRIDGE-WAKE is control traffic, not Task/Result.
SYSTEM-BRIDGE-ALERT is not a Task Result.
Do not self-issue another Task.

END_TASK
