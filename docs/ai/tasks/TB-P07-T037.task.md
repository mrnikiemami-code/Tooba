PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_TASK

Task-ID:
TB-P07-T037

Parent-Task:
TB-P07-T036-R1

Channel:
tooba-main

Title:
Category Assignment Integrity + Invalid Product Cleanup + Recovery SoT Repair

Role:
You are the Tooba implementation Worker.
Implement ONLY the Product↔Category integrity repair, cleanup of invalid assignments/products, and Recovery/SoT synchronization described here.
Do NOT start a new phase.
Do NOT redesign unrelated Catalog areas.
USER_VISUAL_ACCEPTED remains NO.

Human runtime review found two live defects:

In Category → Products, rows whose current Category is the Product's Primary Category still show a delete/trash action even though deletion is not allowed in that context.

Products appear to have direct assignments to Level-1 / Level-2 Categories. This violates the locked Catalog rule:

L1 = navigation-only

L2 = navigation-only

L3 = Product-assignable

This task must also repair stale Recovery/SoT so fresh Workers do not start from old P06/P07-T020 state.

Enforce globally:

Primary Category:

MUST be Level-3

exactly one per Product

Display membership (نمایش در دسته‌های دیگر):

MUST also be Level-3

zero/many per Product

Level-1 / Level-2:

navigation-only

never direct Product assignment targets

PLP on L1/L2 aggregates Products from descendant L3 Categories through subtree discovery

This rule must be enforced in:

backend commands

validation/domain services

bulk assignment

migration flows

seed/demo services

UI pickers

tests

UI-only blocking is not sufficient.

If Product.PrimaryCategoryId == current Category:

Show:

badge دسته اصلی

action باز کردن محصول / مشاهده محصول

Do NOT render:

delete/trash

حذف از این دسته

change-category

disabled fake trash button

Optional tooltip/helper:
برای تغییر دسته اصلی، محصول را باز کنید.

If action does not exist, do not show a disabled control.

If Product is present only via display membership in current Category:

Show:

badge نمایش در این دسته

حذف از این دسته

مشاهده محصول

If no membership:

افزودن برای نمایش

Bulk add:

display-only

L3 current Category only

Any Product-category picker that creates Primary or display membership must only allow selectable L3 Categories.

For Category → Products:

if current Category is L1/L2, do NOT show add/assignment CTA

show an explanatory message:
محصول فقط به دسته‌های سطح ۳ متصل می‌شود. محصولات این دسته از زیر‌دسته‌های سطح ۳ نمایش داده می‌شوند.

Do not offer a hidden or disabled assignment control.

For Product Workspace Primary/Additional pickers:

L1/L2 may be visible in tree for navigation

only L3 nodes selectable

All assignment commands must reject L1/L2 targets with stable error code.

At minimum:

set/change Primary Category

add display membership

bulk display membership

Primary migration target

seed/demo assignment paths

Use localized frontend mapping; normal UI must not show raw errorCode/Bad Request.

Suggested machine code:
catalog.category.assignment.level.invalid

Do not depend on UI prevention alone.

Run a non-production integrity audit over current demo/dev Catalog data.

Identify:

Products with Primary Category at L1/L2

display memberships at L1/L2

duplicate Primary/display same Category

Products with more than one Primary

Products with no Primary

orphan Category assignments

Produce counts and Product IDs internally in evidence, but do not expose raw IDs in normal UI.

For the current non-production demo dataset:

Invalid display memberships to L1/L2:

remove them

Duplicate Additional equal to Primary:

remove Additional duplicate

Invalid Primary at L1/L2:

attempt deterministic repair ONLY if a valid L3 target can be unambiguously inferred from existing seed/source metadata or an existing valid L3 assignment

otherwise delete the invalid demo Product rather than guess

Product with no valid Primary after cleanup:

delete the demo Product and Catalog-owned dependents

The user explicitly requested wrongly assigned Products to be removed.

Do NOT fabricate a new Primary Category guess from text similarity.

Cleanup applies to non-production demo/dev Catalog data only.

Inspect T033/T034 demo seed logic.

Ensure future reset+seed cannot recreate L1/L2 Product assignments.

Add integrity assertions:

every Product Primary is L3

every Additional/display membership is L3

no duplicate Primary+Additional same Category

one Primary per Product

If seed currently creates invalid assignments, repair it.

Because direct assignments are removed, verify L1/L2 PLP still works through subtree semantics.

For an L2 Category:

Products from descendant L3 Primary/display memberships appear

no direct Product↔L2 relation needed

For an L1 Category:

Products from full descendant subtree appear

Facet semantics remain as decided in T036:

viewed Category owns visible facets

no union of every Product's Primary Category facets

After cleanup/reset+seed, report authoritative counts:

total Products

Products with L1/L2 Primary = 0

display memberships on L1/L2 = 0

Products with multiple Primary = 0

Products with no Primary = 0

duplicate Primary+Additional = 0

orphan assignments = 0

If invalid demo Products are deleted, total Product count may decrease from 283.
That is acceptable only if:

every remaining L3 leaf still has at least 3 realistic Products

otherwise reseed/replace with valid L3-only Products deterministically

Final requirement:

every L3 leaf has 3–5 Products

every Product remains Draft + Publish-Ready

Published=0

Archived=0

exactly 5 media/Product

fa/en complete

SEO complete

no Product/Variant Price/Stock

Live Category → Products must show:

Primary row:

دسته اصلی

view/open only

Display row:

نمایش در این دسته

remove + view

No membership:

add-for-display

L1/L2:

no add CTA

clear subtree-only helper

No confusing trash action on Primary rows.

The repository Recovery/SoT has been observed stale in fresh Worker sessions.

Synchronize the EXISTING canonical recovery documents now.

At minimum record:

Current phase: P07

Last Architect-accepted task: TB-P07-T036-R1

Current task under implementation/review: TB-P07-T037

Branch: main

final HEAD/origin after this task

USER_VISUAL_ACCEPTED=NO

current focus: Catalog/Admin UI + category integrity

Worker next state: IDLE / waits for Bridge Task

no invented next task

Update the actual repository recovery files used by fresh Worker startup, including the files already synchronized in T036-R1:

TOOBA-RECOVERY-CONTEXT.md

PROJECT-STATE.md
and any existing canonical START-HERE/ROADMAP state file if repository conventions require it.

Do NOT create a competing recovery system.

The repo-local staleness guard added in T036-R1 must now be strengthened/verified so it catches this exact failure mode.

It should fail if recovery state still points to stale tasks like:

TB-P06-T029

TB-P07-T020-R1
when current repository task state is TB-P07-T037.

Guard must be deterministic and repo-local.
Do NOT require live Bridge API during build/test.

Document:

what file is authoritative

how current Task ID is compared

what failure looks like

Backend:

Primary assignment rejects L1

Primary assignment rejects L2

display assignment rejects L1

display assignment rejects L2

bulk assignment rejects invalid level

Primary migration target rejects L1/L2

seed produces L3-only assignments

no duplicate Primary/Additional

exactly one Primary

cleanup removes invalid display membership

cleanup removes/deletes invalid Product when no deterministic repair exists

L1/L2 subtree PLP still returns descendant products

no Product/Variant Price/Stock

Frontend:

Primary row has no trash/remove

display row has remove

L1/L2 Category Products has no assignment CTA

L3 Category Products has display-add CTA

Product Category pickers select L3 only

localized invalid-level error

no raw Bad Request

recovery guard test passes

Using current seeded demo runtime:

Open a L3 Category:

verify Primary row has no trash

display row can remove

add display Product works

Open an L2 Category:

verify no add Product CTA

verify helper explains subtree behavior

verify descendant L3 Products still appear through PLP/subtree where applicable

Open an L1 Category:

same assignment prohibition

subtree products still discoverable

Attempt invalid assignment through API:

L1/L2 rejected with stable machine code

UI maps human message

Run integrity audit after cleanup:

zero invalid assignments

Run demo reset+seed again:

zero invalid assignments remain

per-L3 3–5 invariant remains

Run:

backend restore

backend build

backend FULL tests

MigrationRunner/tests

frontend typecheck

frontend lint

frontend full relevant Catalog/Product/Category/PLP tests

frontend production build

recovery staleness guard

git diff --check

Require:

0 failed

0 skipped

no new warnings

Keep alive:

Backend :5088

Frontend :3000

Shopeiva :3001

Verify current HEAD loaded.

Report:

git rev-parse HEAD

git rev-parse origin/main

git status --short

Require:

HEAD == origin/main

scoped tracked tree clean

unrelated dirty changes preserved

Create:
docs/evidence/TB-P07-T037/

Required:

assignment-level-rule.md

invalid-assignment-audit.md

cleanup-results.md

l1-l2-subtree-plp.md

category-products-ux.md

recovery-sot.md

recovery-guard.md

live-verification.md

full-validation.md

PASS only if ALL:

Primary Category can only be L3

display membership can only be L3

L1/L2 direct Product assignment is impossible in UI and backend

Primary rows show no trash/remove action

display rows show remove action

L1/L2 Category Products show no add CTA

invalid existing assignments are cleaned

invalid demo Products are deleted when no deterministic L3 repair exists

every remaining L3 leaf has 3–5 Products

all remaining Products Draft + Publish-Ready

Published=0

Archived=0

exactly 5 media/Product

L1/L2 subtree PLP still works

recovery docs reflect TB-P07-T037 current state

stale P06/P07-T020 recovery references no longer drive startup

staleness guard catches outdated recovery

full validation green

runtime current

HEAD==origin/main

USER_VISUAL_ACCEPTED=NO.

RESULT CONTRACT

Return ONLY canonical BRIDGE-WAKE-V1 result for TB-P07-T037 with sections:

Summary
Assignment-Level-Rule
Category-Products-UX
Invalid-Assignment-Audit
Cleanup
Demo-Seed
L1-L2-PLP
Data-Integrity
Recovery-SoT
Recovery-Guard
Architecture
Validation
Live-Verification
Runtime
Evidence
Git
Architectural-Concerns
Visual-Concerns
Blockers

END_TOOBA_TASK