PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_TASK

Task-ID:
TB-P08-T012-R1

Parent-Task:
TB-P08-T012

Channel:
tooba-main

Title:
Article Editor Replacement — Canonical CKEditor 5 + Tooba Media DAM Integration

Role:
Repair TB-P08-T012 according to the user's explicit editor decision.

The user has now locked CKEditor 5 as the Article editor.
Assume commercial licensing is already handled.
Do NOT add any licensing gate, warning, blocker, feature flag, or future licensing TODO.

Preserve ALL successful TB-P08-T012 fixes:

workspace header hierarchy

Save/dirty context

Media Content-Type repair

SEO image Content-Type repair

stale Category/Locale state repair

domain error-code promotion

localized error mapping

Featured image stale-overwrite repair

Gallery behavior

Article VIEW/EDIT

P08 Language/Author/Category/DAM/publication architecture

user-authored committed changes

Replace ONLY the Article rich-text editor implementation and harden its integration.

USER_VISUAL_ACCEPTED=NO.

Article/Blog editor = CKEditor 5.

Requirements:

self-hosted inside Tooba frontend

assume valid commercial license exists

no cloud dependency required for core editing

no CKEditor Cloud upload path

no CKBox or separate asset storage unless it is explicitly wired ONLY as a UI shell over Tooba DAM (prefer not to use it)

all article media must remain in Tooba Media DAM

Article Language controls RTL/LTR

backend sanitization/security remains authoritative

Product rich text may continue using existing TipTap/ProductRichTextEditor.
Do NOT migrate Product editor in this task.

TB-P08-T012 created:
ContentArticleRichTextEditor based on TipTap.

Replace Article usage with a CKEditor-based implementation.

Article pages must no longer instantiate TipTap/ProductRichTextEditor for Article Body.

You may keep TipTap packages/components for Product or other existing areas.
Do NOT remove shared dependencies used elsewhere.

Create one canonical Article editor component, e.g.:
ContentArticleEditor
or repository-convention equivalent.

Use current supported CKEditor 5 React/Next.js integration.

Requirements:

client-side component

no SSR crash

dynamic/client boundary where needed

stable with Next.js 15

no direct DOM access during SSR

no runtime dependency on external CKEditor CDN for core editor if self-hosted packages are used

Use only plugins actually needed for the Article CMS experience.

Configure a CMS-grade toolbar inspired by the user's reference image:

Reference:
D:\Users\User\source\repos\SarvNewVerRequirment\reference\Image\ChatGPT Image Sep 4, 2026, 05_27_51 AM.png

Required capabilities:

paragraph / heading styles

H2 / H3 / H4

bold

italic

underline

strikethrough

ordered list

bulleted list

indentation where supported

blockquote

alignment

link

table insert/edit

undo/redo

find/replace if available in selected licensed build

source/code editing only if safe and compatible with sanitization policy

image insert from Tooba DAM

image alt/caption

image alignment/resize where supported

safe media/embed only if allowlisted and compatible with current rendering/security policy

Do not enable premium/cloud features merely because they exist.
Use the minimum practical professional set.

CKEditor image insertion MUST use Tooba Media DAM.

Required user flow:

click image/media action in CKEditor

open canonical Tooba Media Library / MediaLibraryDialog

select existing asset or upload through existing Tooba DAM flow

insert selected asset into CKEditor content

store public-safe Tooba Media resolver URL/reference

retain MediaAssetId metadata in a safe structured/editor attribute if current sanitizer/rendering supports it

Forbidden:

CKEditor direct upload to external storage

CKEditor Cloud Services upload

base64

blob persistence in Content DB

separate article file store

direct filesystem path

Use direct React callback/state integration.
Do NOT reintroduce fragile window global callback hacks.

Support Article-use metadata:

alt text

caption

alignment

resize where supported

Shared DAM asset metadata must not be globally overwritten merely because one Article changes alt/caption.

Keep Article-use metadata inside editor content/assignment semantics according to existing safe HTML policy.

Direction comes from Article Language, not merely Admin UI locale.

fa Article:

CKEditor content area RTL

toolbar/UI remains usable in RTL

Persian shell labels where Tooba localization applies

en Article:

content area LTR

Changing Language on an allowed pristine Draft must reconfigure editor direction correctly without corrupting content.

CKEditor output must fit existing Content HTML rendering safely.

Preserve backend sanitization.
Review frontend sanitizer only as needed for CKEditor output.

Allow only the HTML/features actually emitted by configured CKEditor plugins.

Reject/remove:

script

event-handler attributes

javascript: URLs

unsafe iframe/embed

arbitrary dangerous style injection

base64 images

Do NOT weaken sanitizer just to make CKEditor output pass.

If CKEditor emits classes/attributes required for tables/images/alignment:

explicitly allow only known safe values

document the allowlist

Preserve T012 Save behavior:

General

Content

Category

Author

Media

SEO

Article Body save/reload must:

persist CKEditor HTML

reload identically enough for editing

not strip expected supported formatting

not silently lose tables/images/captions/alignment

No stale dirty-state loop.

Regression-protect:

Media mutation Content-Type: application/json

Featured image no stale overwrite

SEO image save/fallback works

Category save valid path works

localization.language.inactive promoted/mapped humanly

no raw Bad Request/JSON

Author picker remains fixed

T011 language tabs/draft-first/locale-lock remain intact

The Article editor must visually feel like a real CMS editor, not a thin toolbar.

Required:

clear toolbar grouping

comfortable writing canvas

adequate min-height

clean focus state

responsive toolbar wrapping/overflow

article body width/typography suitable for long-form writing

no Product-specific labels or styling leakage

Use existing Tooba design tokens around CKEditor shell where possible.

Do not restyle CKEditor so heavily that it becomes a bespoke editor project.
The point of CKEditor is to reuse a mature ready-made editing UX.

load CKEditor only on Article content editing route/mode where practical

avoid loading editor bundle on Article list

no Media Library fetch until picker opens

no unnecessary cloud calls

Document any meaningful bundle-size impact, but do NOT block task on it.

No full repository suites.

Frontend focused:

Article EDIT renders CKEditor, not TipTap

no SSR crash

fa RTL / en LTR

CKEditor toolbar expected capabilities

DAM picker opens from editor image action

selected DAM image inserts into CKEditor

HTML save/reload

table formatting survives

image/caption/alt survives

unsafe content sanitized

no base64/external uncontrolled upload path

Backend focused:

existing Article update tests

existing media/SEO/category save regressions from T012

Also:

relevant typecheck/lint

recovery guard

git diff --check

With Host/FE running:

fa Article:

open EDIT

confirm CKEditor loaded

enter formatted text

insert DAM image

add caption/alt if supported

insert table

save

reload

verify persistence

en Article:

verify LTR CKEditor

basic edit/save/reload

Also re-smoke:

Category save

Featured image

SEO image fallback/explicit

no raw technical errors

No long browser campaign.
No screenshot pack unless user asks.

Update:

Phase: P08

Last Architect-accepted: TB-P08-T011

TB-P08-T012 under repair

Current Repair: TB-P08-T012-R1

Last Implementation on completion: TB-P08-T012-R1

USER_VISUAL_ACCEPTED=NO

Worker Next State: IDLE

do NOT invent/start TB-P08-T013

Run recovery staleness guard.

Create/update:
docs/evidence/TB-P08-T012/

Required:

r1-ckeditor-integration.md

r1-toolbar-capabilities.md

r1-dam-integration.md

r1-sanitization.md

r1-runtime-smoke.md

r1-recovery-sot.md

Keep concise.

Preserve user-authored changes.

Required:

commit task changes

push origin/main

HEAD==origin/main

no task-owned uncommitted changes

PASS only if ALL:

Article Body editor is CKEditor 5

Article no longer uses TipTap/ProductRichTextEditor

Product editor remains unaffected

CKEditor is self-hosted/client-safe in Next.js

no licensing gate is introduced

no CKEditor cloud upload/storage dependency

Media insertion uses Tooba DAM only

no base64/direct binary Article storage

fa RTL / en LTR correct

professional CMS toolbar available

supported formatting survives save/reload

sanitizer remains strict/safe

T012 Media/SEO/Category/error fixes preserved

focused validation green

runtime smoke completed or an explicit technical blocker reported

recovery guard passes

HEAD==origin/main

Worker returns IDLE

no T013 invented

USER_VISUAL_ACCEPTED=NO.

Return ONLY canonical BRIDGE-WAKE-V1 result with:

Summary
CKEditor-Integration
TipTap-Removal-From-Article
Toolbar
DAM-Integration
Inline-Image-Metadata
RTL-LTR
Sanitization
Save-Reload
Preserved-T012-Repairs
Performance
Focused-Validation
Runtime-Smoke
Recovery-SoT
Git
Architectural-Concerns
Visual-Concerns
Blockers

END_TOOBA_TASK
