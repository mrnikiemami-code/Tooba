PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_REPAIR_TASK

Task-ID:
TB-P09-T002-R1

Parent-Task:
TB-P09-T002

Channel:
tooba-main

WorkerId:
tooba-worker-01

AgentType:
cursor

Status:
REPAIR

USER_VISUAL_ACCEPTED=NO

Title:
Order Operational History Repair — Human Actor Resolution Without Technical IDs

Why Repair Is Required

TB-P09-T002 delivered the intended notes/history/invoice functionality with minimal UI change, but its own Result reports:

Note actor display uses truncated operator id until Identity display-name lookup is added

This violates the task acceptance requirement that operational history/notes show human-readable actors and do not expose technical actor ids as primary UI text.

This repair is intentionally narrow.

Do NOT redesign the Order UI.
Do NOT change lifecycle, return, settlement, payment, invoice, receipt, or note persistence semantics.
Do NOT create a duplicate Identity subsystem.

Goal

Resolve Order internal-note and operational-history actor display through the existing Identity/user-resolution architecture so Admin sees:

توسط سیستم

توسط {نام نمایشی}

or a safe human fallback if the user is no longer resolvable

Never show:

raw GUID

truncated GUID

internal technical user id as primary actor label

Mandatory Discovery

Before changing code:

Inspect existing Identity/User lookup contracts.

Inspect 2–3 existing Admin screens that already resolve user/operator display names.

Reuse the existing application contract/gateway/projection pattern.

Respect module boundaries:

no direct Identity table access from Checkout/Orders

no cross-module SQL JOIN

no ORM navigation into Identity

Avoid N+1 actor resolution.

If no suitable Identity contract exists:

add the smallest read-only contract at the correct boundary

do not add a parallel user directory abstraction if one already exists

1. Actor Resolution Semantics

For notes/history entries with actor kind = system:

FA: توسط سیستم

EN equivalent using current localization conventions

For user/operator actors:

resolve human display name using existing Identity source

Preferred fallback order if supported by current Identity data:

display name / full name

username or email-like human identifier already considered safe for Admin UI

localized generic fallback such as کاربر حذف‌شده / کاربر نامشخص

Do NOT show technical ids to ordinary UI.

The technical actor id may remain in backend DTO/debug metadata only if current architecture requires it, but must not be the primary rendered label.

2. Batch / Bounded Resolution

Operational history is paged and may contain multiple actors.

Required:

collect distinct actor user ids for the current page/result

resolve in a bounded/batch way where possible

cache within request/composition scope if current patterns allow

no one-Identity-call-per-row N+1 pattern

Internal notes query should follow the same rule.

3. Backend Contract

Prefer backend to return enough human actor information so frontend does not independently call Identity per row.

Expected DTO semantics:

actorKind

actorDisplayName

actorId only if needed internally/backward compatibility

Frontend should render actorDisplayName/human label.

Do not duplicate name-formatting rules across frontend screens.

4. Deleted / Missing Identity

Handle historical audit integrity safely.

If actor identity no longer exists:

keep the history/note entry

render a human fallback

do not fail the whole history query

do not expose raw technical id as fallback

5. Permission / Privacy

Actor lookup must:

respect existing Admin authorization architecture

expose only the minimum display information required

not leak unrelated Identity profile fields

Do not broaden user-directory permissions unnecessarily if an internal application contract already supports secure display-name resolution.

6. Minimal UI Regression

No layout redesign.

Verify:

notes card unchanged except actor label

history UI unchanged except actor label

invoice/receipt unchanged

عملیات سفارش unchanged

Orders grid unchanged

No new buttons, tabs, cards, columns, or navigation.

7. Focused Tests

Backend:

system actor -> localized system label data

resolvable user -> human display name

deleted/missing user -> human fallback

multiple entries same actor -> no duplicate per-row lookup behavior

multiple distinct actors -> bounded resolution

history still paged

notes still append-only

Frontend:

no GUID/truncated GUID rendered

human actor label rendered

missing-user fallback rendered

existing notes/history layout regression green

Also:

relevant build/typecheck/lint

recovery guard

git diff --check

No unrelated full repository suite.

8. Runtime Smoke

Use:
Backend: http://127.0.0.1:5088
Frontend: http://127.0.0.1:3000

Verify one Order with:

at least one system history event

at least one operator note/action

Confirm visually/data-wise:

توسط سیستم

توسط {human name}

no raw/truncated id in normal UI

If possible, also verify missing/deleted actor fallback using deterministic fixture/test data.

9. Recovery / Pipeline

Update:

Phase: P09

Last Architect-accepted: TB-P09-T001

TB-P09-T002 under repair

Current Repair: TB-P09-T002-R1

Last Implementation on completion: TB-P09-T002-R1

USER_VISUAL_ACCEPTED=NO

Worker Next State: IDLE

do NOT invent/start TB-P09-T003

Run recovery guard.

10. Evidence

Create/update:

docs/evidence/TB-P09-T002/

Required concise evidence:

r1-identity-discovery.md

r1-actor-resolution.md

r1-no-n-plus-one.md

r1-runtime-smoke.md

r1-recovery-sot.md

11. Git

Required:

preserve user-authored changes

commit only repair changes

push origin/main

HEAD==origin/main

no task-owned uncommitted changes

Acceptance

PASS only if ALL:

raw/truncated technical actor ids are no longer shown in normal notes/history UI

system actor is human-readable

resolvable operator actor shows human display name

missing/deleted actor shows a human fallback

existing Identity architecture/contract is reused or minimally extended

no direct cross-module Identity DB access

no N+1 actor lookup

notes/history/invoice/receipt functionality remains intact

no Order UI redesign

focused tests green

runtime smoke complete

recovery guard passes

HEAD==origin/main

Worker returns IDLE

no TB-P09-T003 invented

USER_VISUAL_ACCEPTED=NO.

Result Contract

Return ONLY canonical BRIDGE-WAKE-V1 result with:

Summary
Identity-Discovery
Actor-Resolution
Missing-Actor-Fallback
No-N-Plus-One
Backend-Contract
Minimal-UI-Regression
Focused-Validation
Runtime-Smoke
Recovery-SoT
Git
Architectural-Concerns
Visual-Concerns
Blockers

END_TOOBA_REPAIR_TASK