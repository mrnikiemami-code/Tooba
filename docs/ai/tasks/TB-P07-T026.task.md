PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_TASK

Task-ID:
TB-P07-T026

Parent-Task:
TB-P07-T025

Channel:
tooba-main

Title:
Admin Catalog UX Repair I — Central Errors, Category Attributes/Filters, Human Schema UX, Navigation State

Role:
Implement ONLY this scoped repair. Do not invent the next task. Preserve unrelated changes.

Repository / Safety:

Resolve repo root with git rev-parse --show-toplevel.

Record branch, HEAD, origin/main, git status --short.

Preserve unrelated dirty/untracked changes.

Do not reset/clean/stash/overwrite unrelated work.

Read relevant Catalog/Admin/error/attribute/facet/navigation docs and code first.

Human runtime screenshots invalidate prior UX acceptance.
Observed defects:

raw Bad Request still appears in Admin toast.

creating a new Category AttributeDefinition fails.

Category Filters/Facets fail and are not understandable.

technical Schema رده screen exposes categoryId/definitionId/GUID-oriented UX.

Admin nav can keep previous sibling item active/blue.

Category media/icon is missing; planned in Media DAM follow-up.

Category Product selection is not scalable; planned separately.

Variant UX is unclear; planned separately.

Product media upload is fake/incomplete; planned separately.

USER_VISUAL_ACCEPTED=NO.

Normal Admin is for a non-technical user.
Never expose in normal UX:

Bad Request / Internal Server Error / HTTP status text

raw exception / raw JSON / raw backend errorCode

raw GUID/categoryId/definitionId

Schema/Definition/Axes jargon where human wording exists

fake/dead actions

visible unfinished functionality

Visible = functional.

Current page-by-page mapping is insufficient. Implement/repair ONE reusable Admin error normalization path used by all touched actions.

Normalize:

HTTP/fetch client failures

ProblemDetails

validation payloads

stable errorCode + safe args

network failures

unknown errors

UI contract:

backend stable machine code + safe args

centralized localized frontend mapping

field errors near fields

action/form human message

technical details only in logs/observability

Fallbacks:
fa: خطایی رخ داد. لطفاً دوباره تلاش کنید.
en: Something went wrong. Please try again.

Apply at minimum to:

Category Attribute create

Category Attribute attach

Category Filter/Facet save

existing Category Product assignment actions

Product workspace actions that use the same shared Admin client path

Do not create competing normalizers.

Repair live flow:
Category Workspace → ویژگی‌ها → ایجاد ویژگی جدید → fill form → Create and Add → create canonical AttributeDefinition → bind immediately to current Category → refresh list.

Requirements:

actual success, not fake success

localized duplicate/validation errors

modal closes on success

no stale state

same canonical AttributeDefinition library

keep افزودن ویژگی موجود

keep ایجاد ویژگی جدید

Product Workspace still cannot create AttributeDefinition

Human fields:

نام ویژگی

نوع مقدار

چندمقداری

رفتار ویژگی

Independent behavior controls:

الزامی

فیلتر

تنوع

مقایسه

Explain each briefly:

الزامی: برای محصولات این دسته باید مقدار داشته باشد.

فیلتر: مشتری می‌تواند در صفحه دسته بر اساس این ویژگی فیلتر کند.

تنوع: می‌تواند برای ساخت تنوع‌های محصول مثل رنگ یا سایز استفاده شود.

مقایسه: در جدول مقایسه محصولات نمایش داده می‌شود.

fa/en required.

Rename Persian tab/section to:
فیلترهای صفحه محصولات

Helper text:
مشخص کنید مشتریان در صفحه این دسته بتوانند محصولات را بر اساس کدام ویژگی‌ها فیلتر کنند.

English equivalent required.

This area configures existing/effective Category attributes only; it does NOT redefine AttributeDefinition.

Per filter show human controls:

Attribute name

display type

show on product-list page

searchable

collapsed by default

multi-select when meaningful

display order

Human display types where supported:

چندانتخابی

تک‌انتخابی

بازه عددی

بله/خیر

جستجوی متنی if semantically valid

Backend:

fix save/load

validate definition belongs to/effective for Category

stable error codes

no raw Bad Request

Frontend:

persisted values reload correctly

explanatory empty state

The screen exposing Schema/categoryId/definitionId/GUID is not acceptable normal Admin UX.

Remove it from normal navigation.
Represent actual concept inside Category Workspace → ویژگی‌ها as:

ویژگی‌های به‌ارث‌رسیده

ویژگی‌های مخصوص این دسته

Human explanation:
این ویژگی‌ها از دسته‌های والد به ارث رسیده‌اند و برای محصولات این دسته نیز قابل استفاده‌اند.

No typing GUID/categoryId/definitionId.
Preserve underlying inheritance model.
If internal debug page is genuinely needed, keep only behind explicit internal/dev convention; otherwise hide safely.

Keep one central reusable AttributeDefinition library.

Conceptual nav:
کاتالوگ
دسته‌بندی‌ها
مدیریت دسته‌بندی‌ها
ویژگی‌ها و فیلترها
مگامنو
محصولات
فهرست محصولات
افزودن محصول

Inside Category:

افزودن ویژگی موجود

ایجاد ویژگی جدید

Both use the same central definitions.
Rename/remove any ambiguous duplicate menu item such as a separate confusing افزودن ویژگی page.

Fix active-route logic.
Rules:

only actual active leaf has primary active styling

parent group may remain expanded

sibling/previous child must not stay active

/admin/products must not remain active when /admin/products/new is active

locale prefixes fa/en must work

deep-link/refresh highlights correct route

Tests required for Product List, Add Product, Category Management, Attribute Library, no double-active sibling.

Do not redesign approved AppCategoryTree.
Preserve search, expand/collapse, hierarchy, styling and existing actions except necessary scoped functional fixes.

Everything touched must support fa/en: labels, helper text, dialogs, explanations, display types, validation, errors, empty states, confirmations, nav.
fa RTL; en LTR.

Preserve:

Modular Monolith boundaries; no cross-module SQL joins

Product != Offer

no Product Price/Stock

current Level-3 Product Category rule for this task

Product attributes from effective Category schema

Product cannot create AttributeDefinition

canonical AppDataGrid

Category Tree

Brand canonical

Variants/Media/SEO/Publishing/History

Shopeiva untouched

Backend tests:

create AttributeDefinition success

create + attach flow

duplicate stable localized contract

invalid value type

filter/facet create/update/load

invalid definition/category relation rejected with stable code

Frontend tests:

create-new-attribute modal success/validation/error

behavior explanations

filter helper copy/save/reload

no technical Schema/GUID normal nav

inheritance human sections

active route state/no sibling double-active

raw Bad Request normalized

fa/en + RTL/LTR

Run:

backend restore

backend build

backend full tests

frontend typecheck

frontend lint

frontend relevant/full tests

frontend production build

git diff --check

Require 0 errors, 0 failed, 0 skipped, no new warnings.

Keep alive:

Backend :5088

Frontend :3000

Shopeiva :3001

Verify LIVE:

Create Category attribute → succeeds, attaches, visible immediately.

Trigger duplicate → human localized error, never Bad Request.

Configure Category filter → save succeeds; reload persists.

Navigate Product List / Add Product / Category / Attribute Library → only one active leaf.

No normal menu route exposes technical Schema/GUID form.

The following tasks are explicitly planned so NONE of the human-reported issues are lost:

TB-P07-T027 — Category ↔ Product Assignment at Scale + Multi-Category

Primary Category + Additional Categories

Primary drives Attribute Schema / variant axes / canonical breadcrumb

Additional categories are discovery/PLP/navigation only

scalable modal using canonical AppDataGrid

tabs: همه محصولات / انتخاب‌شده‌ها

server-side search/paging/filter

row Add/Remove, bulk Add/Remove

selected-count

remove category assignment

safe Primary reassignment

10k+ Product UX

TB-P07-T028 — Variant UX Redesign

explain ویژگی vs تنوع

step-by-step flow:

choose variant attributes

choose values

preview combinations

create/reconcile variants

choose default

remove Axis/Schema jargon

no Price/Stock

TB-P07-T029 — Real Media DAM / Upload Library

actual binary upload

reusable media library

multi-file upload/select

Product media assignment

Category image + icon + optional banner/cover

thumbnails

primary image

Alt

reorder

unassign != delete asset

no fake media generation

TB-P07-T030 — Product Create Flow + Rich Description + Reference Completion

remove persistent inline create card from Product list

dedicated /admin/products/new

proper creation workspace/wizard

CKEditor 5 or repo-approved rich editor

controlled font family + font size + headings/bold/italic/underline/lists/alignment/link/table/image/quote/undo/redo

completion/readiness cards/progress closer to locked Product Admin reference

fix any remaining Product visual mismatch

TB-P07-T031 — Final Catalog Admin Human Acceptance Gate

Product + Category + Attributes + Filters + Variants + Media

centralized errors

multilingual UX

navigation

no incomplete visible feature

runtime evidence

USER_VISUAL_ACCEPTED remains NO until user reviews

Commit + push after validation.
Require HEAD == origin/main, scoped tracked tree clean, unrelated changes preserved.

PASS only if ALL:

raw Bad Request no longer leaks in touched Admin paths

centralized normalizer is actually used

create Category AttributeDefinition works live

create + attach works live

Filters/Facets work live and persist

filter purpose is human-readable

technical Schema/GUID screen removed from normal Admin UX

central Attribute library + Category shortcuts remain one canonical system

nav double-active bug fixed

fa/en works

full validation green

runtimes alive

Git synchronized

USER_VISUAL_ACCEPTED=NO.

Return ONLY:

PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_WORKER_RESULT

Task-ID:
TB-P07-T026

Parent-Task:
TB-P07-T025

Channel:
tooba-main

Status:
PASS | FAIL | BLOCKED | RECOVERY_CONFLICT

Summary:
...

Central-Error-Normalization:

single reusable path:

ProblemDetails:

stable errorCode mapping:

unknown fallback:

raw Bad Request:

raw HTTP:

raw JSON:

touched actions migrated:

Category-Attribute-Create:

create:

attach:

immediate refresh:

duplicate handling:

validation:

modal closes on success:

Product local create remains blocked:

Attribute-UX:

human labels:

behavior explanations:

required:

filter:

variant:

comparable:

Filters:

renamed:

helper copy:

eligible attribute source:

display type:

save:

reload:

persistence:

backend validation:

raw error:

Schema-UX:

technical page in normal nav:

categoryId exposed:

definitionId exposed:

GUID entry exposed:

inherited section:

category-specific section:

underlying inheritance preserved:

Attribute-Library:

central library:

Category add existing:

Category create new:

duplicate library created:

ambiguous menu item:

Navigation:

Product List active:

Add Product active:

Category active:

Attribute library active:

double-active sibling:

locale routes:

Regression:

Category Tree:

AppDataGrid:

Product != Offer:

Product.Price:

Product.Stock:

Variants:

Media:

SEO:

Publishing:

History:

Shopeiva:

Validation:

backend restore:

backend build:

backend full tests:

frontend typecheck:

frontend lint:

frontend tests:

frontend production build:

passed:

failed:

skipped:

warnings:

new warnings:

git diff --check:

Live-Verification:

attribute create:

duplicate error:

filter save:

filter reload:

nav active state:

technical schema normal route:

Backend:

Frontend:

Shopeiva:

kept alive:

Git:

branch:

commit:

push:

final HEAD:

origin/main:

synchronized:

tracked tree:

unrelated user changes:

Architectural-Concerns:
...

Visual-Concerns:
USER_VISUAL_ACCEPTED=NO

Blockers:
...

END_TOOBA_WORKER_RESULT

After Result:

Worker -> IDLE / Waiting

do not invent next task

do not add any explanation outside the Result envelope

END_TOOBA_TASK