PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_TASK

Task-ID:
TB-P07-T027

Parent-Task:
TB-P07-T026

Channel:
tooba-main

Title:
Category ↔ Product Assignment at Scale — Primary + Additional Categories, AppDataGrid Picker, Bulk Add/Remove

Role:
You are the implementation Worker for Tooba.
Implement ONLY this scoped Category/Product assignment architecture + UX task.
Do NOT start Variant UX redesign, Media DAM, Product-create wizard, Rich Text Editor, or final acceptance gate yet.

Repository / Safety:

Resolve repository root:
git rev-parse --show-toplevel

Record branch / HEAD / origin/main / git status --short.

Preserve unrelated user/agent changes.

Do NOT reset, clean, stash, checkout-away, or overwrite unrelated dirty files.

Read relevant Catalog Product/Category/Attribute/Variant/SEO/PLP/Admin docs and code before editing.

Avoid repo-wide scans.

TB-P07-T026 is ACCEPTED.

Accepted baseline:

centralized Admin error normalization is in place

raw Bad Request/HTTP leaks are normalized in touched paths

Category attribute create+attach works live

Filters/Facets work and persist

filter purpose is human-readable

technical Schema/GUID page removed from normal Admin nav

central Attribute library remains canonical

Product list vs Add Product double-active bug fixed

Category Tree unchanged

USER_VISUAL_ACCEPTED=NO

Current Category → Products assignment UX is not scalable.

The user explicitly rejected a small checkbox list/modal because it will not work with 10,000+ products.

The user also asked whether a Product should belong to multiple Categories.

Architect decision:
YES, but with controlled semantics:

Product
→ exactly one Primary Category
→ zero or more Additional Categories

PRIMARY CATEGORY:

schema-driving category

Variant/Attribute schema source

primary breadcrumb/category identity

canonical category context for Product

must obey Level-3 assignability

ADDITIONAL CATEGORIES:

discovery/navigation/PLP placement

do NOT change effective Product Attribute schema

do NOT change Variant axes

do NOT create schema conflicts

may be zero or many

must obey allowed category assignment rules defined below

This replaces the current one-category-only UX/model where required.

Audit existing Product↔Category relation first.

Implement an extraction-safe canonical model supporting:

one Primary Category per Product

zero or more Additional Categories

no duplicate same Category assignment

no Product with two Primary Categories

Prefer a normalized relation model if current architecture supports it, e.g. conceptually:

CatalogProductCategoryAssignment

ProductId

CategoryId

Role: Primary | Additional

DisplayOrder if justified

CreatedAt/UpdatedAt as repository conventions require

OR evolve current existing relation safely.

Do NOT create:

comma-separated category IDs

JSON blob category IDs

cross-module join hacks

Migration/data compatibility:

existing single Category assignment becomes Primary Category

existing products remain valid

migration must be deterministic

Exactly one Primary Category is required when Product is structurally ready for publication.

Creation/draft may temporarily have none only if current lifecycle permits it.

Primary Category:

MUST be Level 3

drives effective Attribute Schema

drives Product Variant-axis eligibility

drives Product category path shown in Product Workspace

used for primary breadcrumb/canonical category context

is the only category allowed to affect schema-driven Product authoring

Changing Primary Category is high-impact.

Before changing:

compute impact on Product Attribute values

compute impact on Variant axes/combinations

show human confirmation

preserve existing impact-safety behavior

block unsafe silent data loss

If new Primary Category makes existing Product values invalid:

show clear impact summary

require explicit resolution/confirmation according to existing architecture

do not silently delete values

Additional Categories are for:

discovery

navigation

PLP inclusion

merchandising placement

They MUST NOT:

alter effective Attribute Schema

alter Variant axes

create Product Attribute definitions

become an alternate schema source

Default rule:

allow assignable leaf/category destinations that are valid for PLP placement.

If architecture currently requires only Level-3 product assignment everywhere, preserve Level-3 for Additional too unless a clear accepted category model allows otherwise.

Do NOT silently broaden Level-1/2 assignment without evidence.

A Product cannot have same Category as both Primary and Additional.

If assigning current Primary as Additional:

no-op / human message

If promoting an Additional Category to Primary:

run Primary change impact flow

old Primary may optionally become Additional if user chooses, otherwise remove according to explicit UI choice

no implicit hidden behavior

Replace the current small product-checkbox modal with a scalable full-size dialog/drawer using the canonical:

AppDataGrid

NO raw AgGridReact.
NO new grid wrapper.

Dialog title:
اختصاص محصولات به این دسته

Use two tabs:

همه محصولات

انتخاب‌شده‌ها (N)

همه محصولات must support:

server-side search

paging

status filter

Brand filter if contract available

current-category filter/status if useful

Product title

image

Brand

current Primary Category path

relevant status

assignment state

row action

bulk selection

Scale target:

10,000+ products

do NOT load all products into client memory

use server query/pagination

deterministic paging/sorting

For Category → Products grid:

When Product not assigned:

row action: افزودن

When Product already Additional in this Category:

show اضافه شده

row action may be حذف از این دسته

When Product Primary is this Category:

show badge دسته اصلی

normal remove must NOT silently detach it

For a Primary assignment:

action should guide user:
تغییر دسته اصلی
or:
ابتدا دسته اصلی دیگری انتخاب کنید

No ambiguous trash action for Primary.

Support:

multi-row selection

bulk add as Additional Category

bulk remove Additional assignments

Optional if safe and useful:

bulk set Primary is NOT required and likely should not exist because primary change has per-product impact.

Bulk action must:

show count

show progress/state

return per-row errors if partial failure is possible

preserve successful operations

humanize failures

not collapse everything into generic Bad Request

انتخاب‌شده‌ها (N) shows Products assigned to this Category.

Must distinguish:

Primary

Additional

Use canonical AppDataGrid.

Columns should be human:

image

Product

Brand

assignment type

primary category path if relevant

status

updated

actions

Actions:

remove Additional assignment

for Primary: change primary / inspect Product

open Product VIEW

edit if permission allows

No raw IDs.

Update Category → Products tab.

Top:

explanatory copy

assignment counts

button:
اختصاص محصولات

Grid:

real current assigned Products

Primary/Additional badge

remove/change actions

search

paging

AppDataGrid

Human helper copy example:
محصولات می‌توانند یک دسته اصلی و چند دسته اضافی داشته باشند. دسته اصلی ساختار ویژگی‌ها و تنوع‌های محصول را تعیین می‌کند؛ دسته‌های اضافی فقط برای نمایش و پیدا شدن محصول استفاده می‌شوند.

English equivalent required.

Product Workspace must support:

Primary Category:

one human path

change action

high-impact confirmation

Additional Categories:

zero or more

add/remove

human chips/list

no raw IDs

searchable picker for adding

cannot duplicate Primary

General should clearly distinguish:

دسته اصلی
and
دسته‌های اضافی

Do NOT use generic one-field Category if multi-category architecture now exists.

T030 will later replace the inline Product-create UI with a dedicated workflow.

For now:

keep current create flow working

on creation, selected Category becomes Primary Category

do NOT implement full new Product wizard in this task

do not duplicate create flows

Category PLP must include Product when:

Category is Product Primary
OR

Category is one of Product Additional Categories

Subtree semantics should preserve existing accepted category behavior.

Do NOT make Additional Category alter Product attributes.

No cross-module SQL JOIN.

Use Catalog-owned query/projection patterns.

Primary Category is the Product's primary category context.

Preserve current public Product canonical URL:
/{locale}/products/{slug}

Do NOT generate duplicate Product canonical URLs per category.

If category breadcrumb/link context is displayed:

Primary Category supplies canonical breadcrumb

Additional category listings link to the same canonical Product URL

Avoid duplicate-content SEO.

Human complaint:
Product can be added but cannot be removed.

Fix this completely.

Additional Category:

removable directly with confirmation if needed

unassign does NOT delete Product

Primary Category:

cannot simply remove if it would leave invalid state

user must choose another Primary Category or use allowed draft-safe workflow

human explanation

Category deletion/archive impact must preserve accepted integrity rules.

Use T026 centralized Admin error normalizer.

No:

Bad Request

HTTP text

raw code

raw JSON

exception

Add stable backend codes as needed for:

duplicate assignment

invalid Primary Category

invalid Additional Category

primary change conflict

schema impact conflict

assignment not found

cannot remove Primary

stale concurrency

All mapped fa/en.

Preserve VIEW/EDIT principles.

VIEW:

see Primary + Additional Categories

see assignment type

no mutation controls

EDIT:

change Primary

add/remove Additional

Category Product tab:

view-only users can inspect

edit actions only if allowed

Backend remains authoritative.

This feature MUST be designed for large catalogs.

Do not:

fetch all Products

fetch all assignments into browser

client-filter 10k products

Require:

paged server query

indexed query path

deterministic sort

efficient assignment existence checks

batch APIs if bulk action needs them

no N+1 remote/module calls

Document relevant indexes/migrations.

fa/en required.

Persian:
RTL

English:
LTR

Localize:

tabs

helper copy

assignment badges

filters

buttons

confirmations

errors

empty states

bulk action messages

All tabular Product selection/assignment UI must use:

src/frontend/design-system/app-data-grid/AppDataGrid.tsx

Must preserve:

sorting

typed filters

paging

selection

saved view support where wrapper provides it

RTL/LTR

canonical styles

No raw AG Grid.

Preserve:

Category Tree approved visuals

T026 error normalization

working Category Attribute create

working Filters/Facets

technical Schema page absent from normal nav

nav active-state fix

Product attributes from Primary Category effective schema only

Variants existing functionality

Media existing functionality

Product != Offer

no Product.Price

no Product.Stock

SEO

Publishing

History

Shopeiva untouched

Backend:

migration existing Category → Primary assignment

one Primary invariant

Additional many

duplicate assignment rejected/no-op appropriately

Primary must be valid Level-3

Additional validation

Primary change impact

remove Additional

cannot unsafe-remove Primary

promote Additional to Primary

PLP includes Primary/Additional

Additional does not affect Effective Attribute Schema

canonical Product URL unaffected

batch add/remove

paging/search 10k-oriented query contract

concurrency/stale updates

localized stable error codes

Frontend:

Category Products scalable AppDataGrid modal

All Products tab

Selected tab

server paging/search

row add/remove

Primary badge

Additional badge

bulk add/remove

no bulk Primary

Product Workspace Primary + Additional UX

change Primary confirmation

dirty guard where applicable

VIEW/EDIT permissions

fa/en

RTL/LTR

no raw IDs

normalized errors

Run:

backend restore

backend build

backend full tests

frontend typecheck

frontend lint

frontend full relevant tests

frontend production build

git diff --check

Require:

0 errors

0 failed

0 skipped

no new warnings

Keep alive:

Backend :5088

Frontend :3000

Shopeiva :3001

Verify LIVE:

Category → Products

open scalable picker

search

page

add Product

see in Selected

remove Additional

Primary Category

Product shows Primary

change Primary

impact confirmation appears

save

old/new state correct

Additional Categories

add at least two Additional Categories

Product appears in corresponding Category Product tabs

schema remains Primary-driven

Large-catalog behavior

prove server query/pagination path; do not require 10k seed if repository lacks it

document query contract/evidence

Error cases

duplicate assignment

unsafe Primary removal

no raw Bad Request

Create:
docs/evidence/TB-P07-T027/

At minimum:

assignment-model.md

category-product-picker.md

primary-category-impact.md

live-verification.md

Document:

migration behavior

query pagination

AppDataGrid usage

Primary vs Additional semantics

PLP semantics

SEO/canonical behavior

architecture boundaries

Commit + push only after green validation/live checks.

Require:

HEAD == origin/main

scoped tracked tree clean

unrelated user changes preserved

Still planned:

TB-P07-T028:
Product Variant UX Redesign

TB-P07-T029:
Real Media DAM / Upload Library + Category image/icon/banner

TB-P07-T030:
Dedicated Product Create Flow + Rich Text Editor + Reference Completion

TB-P07-T031:
Final Catalog Admin Human Acceptance Gate

Do not implement them in T027.

PASS only if ALL:

Product supports exactly one Primary Category + many Additional Categories

Primary drives Product schema

Additional does not alter schema

Category Product picker uses AppDataGrid

picker is server-paged/searchable and suitable for 10k+

All Products + Selected tabs exist

row add/remove works

bulk add/remove works for Additional assignments

Primary cannot be silently removed

Product Workspace supports Primary + Additional

Category Workspace supports add/remove

PLP includes both Primary and Additional assignments

Product canonical URL remains one URL

no raw Bad Request

fa/en works

full validation green

runtimes alive

Git synchronized

USER_VISUAL_ACCEPTED=NO.

Return ONLY:

PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_WORKER_RESULT

Task-ID:
TB-P07-T027

Parent-Task:
TB-P07-T026

Channel:
tooba-main

Status:
PASS | FAIL | BLOCKED | RECOVERY_CONFLICT

Summary:
...

Assignment-Model:

existing model:

migration:

Primary invariant:

Additional many:

duplicate prevention:

Level-3 rule:

schema-driving category:

Additional schema impact:

Primary-Category:

Product UX:

change flow:

impact analysis:

unsafe removal:

promote Additional:

old Primary behavior:

Additional-Categories:

Product UX:

add:

remove:

duplicate Primary:

discovery/PLP:

schema isolation:

Category-Product-Picker:

AppDataGrid:

raw AgGridReact:

All Products tab:

Selected tab:

server search:

server paging:

filters:

row add:

row remove:

bulk add:

bulk remove:

10k+ suitability:

client loads all products:

Category-Workspace:

assignment count:

Primary badge:

Additional badge:

remove:

open Product:

helper copy:

Product-Workspace:

Primary field:

Additional list:

add:

remove:

raw IDs:

VIEW:

EDIT:

PLP-SEO:

Primary included:

Additional included:

subtree:

Effective Attribute Schema:

canonical Product URL:

duplicate URL introduced:

Errors:

centralized normalizer:

duplicate:

invalid Primary:

invalid Additional:

primary conflict:

unsafe remove:

stale:

raw Bad Request:

raw HTTP:

raw code:

Performance:

query shape:

indexes:

N+1:

paging:

deterministic sort:

batch mutation:

Localization:

fa:

en:

RTL:

LTR:

Regression:

Category Tree:

Attribute create:

Filters:

Schema technical nav:

nav active state:

Variants:

Media:

Product != Offer:

Product.Price:

Product.Stock:

SEO:

Publishing:

History:

Shopeiva:

Validation:

backend restore:

backend build:

backend full tests:

frontend typecheck:

frontend lint:

frontend tests:

frontend production build:

passed:

failed:

skipped:

warnings:

new warnings:

git diff --check:

Live-Verification:

picker search:

picker paging:

add:

selected:

remove:

Primary change:

Additional two:

schema remains Primary-driven:

duplicate error:

unsafe Primary removal:

raw error:

Backend:

Frontend:

Shopeiva:

kept alive:

Evidence:

path:

assignment-model:

picker:

impact:

live:

Git:

branch:

commit:

push:

final HEAD:

origin/main:

synchronized:

tracked tree:

unrelated user changes:

Architectural-Concerns:
...

Visual-Concerns:
USER_VISUAL_ACCEPTED=NO

Blockers:
...

END_TOOBA_WORKER_RESULT

After Result:

Worker -> IDLE / Waiting

do not invent next task

do not add explanation outside Result envelope

END_TOOBA_TASK
