PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

TASK-ID: TB-P07-T012
PARENT-TASK: TB-P07-T011
PROJECT: Tooba
PHASE: P07
CHANNEL: tooba-main
STATUS: ISSUED
TASK-TYPE: PRODUCT MASTER ATTRIBUTES — CATEGORY-DRIVEN EDITOR
WORKER-POLICY: ONE WORKER = ONE ACTIVE TASK

Architect Acceptance

TB-P07-T011 is ACCEPTED.

Canonical baseline:

Product Catalog Admin uses the customized Tooba AppDataGrid.

Product Workspace has explicit VIEW / EDIT.

Product creation is progressive.

Category picker is human-readable.

Product != Offer.

No Product.Price / Product.Stock.

Pricing / Inventory boundaries preserved.

Locale-based Catalog LocalizedText is acceptable for now; do NOT add NameFa/NameEn.

baseline HEAD == origin/main:
c499cbd38dfe35449e499d97ca989bf8a52a691c

Locked UX Principle

The Product editor must be understandable for an ordinary non-technical Admin user.

For attributes:

no raw IDs

no schema jargon

typed controls

obvious required fields

understandable validation

safe category changes

VIEW / EDIT mandatory

Hard Locks

Do NOT change Category Tree.

Do NOT replace Product AppDataGrid.

Raw AgGridReact forbidden.

Product != Offer.

No Product.Price / Product.Stock.

No Pricing/Inventory direct SQL JOIN.

T007 effective Category schema is canonical.

Variant behavior comes from effective IsVariantAxis.

T012 implements Product ATTRIBUTE VALUES only.

Full variant matrix belongs to T013.

Goal

Implement real Product Workspace ویژگی‌ها tab:

Product.CategoryId
→ Effective Category Attribute Schema
→ Product Attribute Values
→ validation / save

1 — Product Attribute Value Model

Use/complete a Catalog-owned typed Product Attribute Value model.

Support existing Catalog value kinds:

Text

Number

Decimal if supported

Boolean

Enumeration

Multi-value enumeration

Instant/Date

other existing typed kinds

Do NOT use arbitrary unvalidated JSON as the primary model.

2 — Effective Schema

Consume T007 effective schema:

AttributeDefinitionId

localized name

ValueKind

IsMultiValue

IsRequired

IsVariantAxis

IsComparable

options

units/capabilities

deterministic order

UI never shows raw IDs.

3 — VIEW Mode

Show readable specifications:

label

formatted value

unit

multi-values as chips

missing required value clearly

optional variant badge

No disabled input forms.

4 — EDIT Mode

Use type-appropriate controls:

Text → text/textarea
Number/Decimal → numeric input
Boolean → yes/no or switch
Enumeration → select
Multi-value → multi-select
Date/Instant → locale-friendly date/time control
Color-like → labeled options; swatches only with real metadata

Save / Cancel / dirty-state required.

5 — Required Validation

Effective IsRequired is authoritative.

Frontend:

mark clearly

block invalid save

localized field errors

Backend:

validate required values

do not trust client

6 — Backend Validation

Reject:

unknown attribute

attribute outside effective schema

wrong value type

invalid enum option

multiple values where forbidden

invalid inactive option per policy

invalid variant-axis value

7 — Category Change Safety

Complete Product category-change impact analysis.

Compare old vs new effective schema.

Classify:

compatible values preserved

no longer applicable

incompatible type/options

newly required missing

Show human summary before final switch, e.g.:

۳ مقدار حفظ می‌شود
۲ ویژگی دیگر در دسته جدید وجود ندارد
۱ ویژگی الزامی جدید باید تکمیل شود

Explicit confirmation required.

Do NOT silently delete values.

8 — Incompatible Values Policy

After confirmed category change:

preserve compatible values

remove/archive incompatible active values according to domain policy

keep audit/history where architecture supports it

no invalid active values left

Document exact behavior.

9 — Product Readiness

Add attribute readiness result:

IsComplete

MissingRequired[]

InvalidValues[]

Later Publishing tab will consume it.

10 — Variant Handoff

Do NOT build full variants here.

Prepare T013 handoff:

effective IsVariantAxis attributes

selected axis values/options
→ variant matrix

Do not auto-delete existing variants if attribute edits affect axes.
Return explicit impact/review state.

11 — Localization

Attribute names/options use existing localized definitions.

Do not create language-specific Product attribute columns.

Do not assume all free-text attribute values are translatable; follow existing semantics and document gap if any.

12 — Product Workspace UX

ویژگی‌ها tab becomes real.

Top summary:

completion status

missing required count

category name/path

VIEW:

readable specs

EDIT:

typed controls

Save / Cancel

dirty-state protection

Preserve deep link/back/forward.

13 — Authorization

Use SpiceDB-centered authorization.

view Product attributes

edit Product attributes

Backend authoritative.
View-only users get no editable controls.

14 — API Contracts

Provide/use clean module-owned APIs for:

get effective schema + current values

update Product attribute values

validate values

category-change impact

readiness summary

No EF entities directly.
No arbitrary untyped dictionaries.

15 — Persistence

Migration only if needed.

Constraints/indexes should support:

ProductId + AttributeDefinitionId uniqueness where single-valued

option validity

efficient reads for PDP/PLP

No cross-module FKs.

16 — PLP Compatibility

Saved filterable Product values must participate in T010 PLP filtering.

Do not create a duplicate facet-value store.

Add integration tests where practical.

17 — Tests

Backend:

text/number/boolean/enum/multi-value saves

wrong type rejected

unknown attribute rejected

invalid option rejected

non-schema attribute rejected

required validation

schema ordering

category-change compatible preservation

category-change incompatible impact

no silent loss before confirmation

readiness

filterable value participates in PLP

no Pricing/Inventory coupling

Frontend:

VIEW

EDIT

typed controls

required UX

localized option labels

multi-select

Save/Cancel

dirty state

category-change impact

view-only cannot edit

no raw IDs

mobile

accessibility basics

18 — Documentation

Create/update:
docs/catalog/PRODUCT-ATTRIBUTES.md

Include:

typed value model

effective schema use

validation

category-change impact

readiness

T013 variant handoff

PLP/PDP handoff

localization policy

Update recovery/project state docs.

19 — Validation

Backend:

restore

build

full tests

Frontend:

typecheck

lint

tests

production build

Require:

0 errors

0 failed

0 skipped

no new warnings

Always:

git diff --check

20 — Runtime

Keep alive:

Backend :5088

Frontend :3000

Shopeiva :3001

Product Admin:
http://localhost:3000/fa/admin/products

21 — Git

Commit + push.

Require:

HEAD == origin/main

tracked tree clean

unrelated user changes preserved

Acceptance

PASS only if:

Product Attributes tab is real

VIEW / EDIT preserved

effective Category schema is canonical source

typed controls match ValueKind

required behavior is category-specific

backend validates values

no raw IDs

category change impact is explicit and safe

no silent data loss

readiness summary exists

T013 variant handoff ready

existing variants not silently destroyed

saved filterable values work in PLP

Product != Offer preserved

no Product.Price/Product.Stock

no Pricing/Inventory joins

no raw AgGridReact

validation green

runtime alive

Git synchronized

USER_VISUAL_ACCEPTED remains NO until manual review.

Result Contract

Return ONLY:

PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_WORKER_RESULT

Task-ID:
TB-P07-T012
Parent-Task:
TB-P07-T011
Channel:
tooba-main
Status:
PASS | FAIL | BLOCKED | RECOVERY_CONFLICT

Summary:
...

Canonical-AppDataGrid:

touched:

raw AgGridReact:

new wrapper:

product list regression:

Attribute-Value-Model:

entity/path:

typed storage:

supported types:

uniqueness:

migration:

Effective-Schema:

source:

required:

variant axis:

ordering:

localized labels/options:

Product-Attributes-UX:

VIEW:

EDIT:

controls:

required UX:

readiness:

Save:

Cancel:

dirty-state:

mobile:

accessibility:

Validation-Rules:

unknown attribute:

wrong type:

invalid option:

multi-value:

required:

backend authoritative:

Category-Change:

impact analysis:

compatible preservation:

incompatible values:

explicit confirmation:

silent loss:

new required fields:

Variant-Handoff:

effective axes:

existing variant safety:

T013 readiness:

PLP-Handoff:

filterable values queryable:

duplicate facet store:

integration tests:

Architecture:

Product != Offer:

Product.Price:

Product.Stock:

Pricing join:

Inventory join:

Authorization:

SpiceDB:

view-only:

edit:

backend authoritative:

API:

endpoints/contracts:

backend touched:

frontend integration:

gaps:

Validation:

backend restore:

backend build:

backend full tests:

frontend typecheck:

frontend lint:

frontend tests:

frontend build:

passed:

failed:

skipped:

warnings:

new warnings:

git diff --check:

Documentation:

PRODUCT-ATTRIBUTES:

recovery/state:

Runtime:

Backend:

Frontend:

Shopeiva:

Product Admin:

kept alive:

Git:

branch:

commit:

push:

final HEAD:

origin/main:

synchronized:

tracked tree:

unrelated user changes:

Architectural-Concerns:
...

Visual-Concerns:
...

Blockers:
...

END_TOOBA_WORKER_RESULT

After Result:

Worker -> IDLE / Waiting

keep runtimes alive

do not invent T013

do not add extra prose

END_TASK
