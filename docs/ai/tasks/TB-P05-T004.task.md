BEGIN_TOOBA_CURSOR_TASK_V1

Protocol-Version:
1

Task-ID:
TB-P05-T004

Phase:
P05 — Operational Surface Integration

Title:
Admin Fast Connect — Reuse Shopeiva Vendor/Admin Patterns with Live Tooba APIs

Status:
ISSUED

Objective:
Connect the fastest commercially valuable Admin/Operations surfaces to live Tooba backend data while preserving the purchased Shopeiva visual structure with minimum change.

This task is FAST CONNECT FIRST.

Primary goal:
Shopeiva existing operations/vendor visual patterns
+
Tooba Admin authority
+
Live APIs
=
Presentable operational Admin experience quickly

This is NOT a redesign task.

Required Reading:
- AGENTS.md
- docs/PROJECT-STATE.md
- docs/ROADMAP.md
- docs/ai/TOOBA-PIPELINE-PROTOCOL.md
- docs/ai/TOOBA-PIPELINE-CONTROLLER.md
- docs/ai/TOOBA-RECOVERY-CONTEXT.md
- docs/prompts/START-HERE-IF-CHATGPT-IS-LOST.md
- docs/evidence/TB-P04-T006/
- docs/evidence/TB-P05-T001/
- docs/evidence/TB-P05-T003/
- Existing Admin/Product Workspace evidence
- Existing Shopeiva Vendor/Admin-like route patterns

Repository Recovery:
Run first:

```bash
git rev-parse --show-toplevel
git fetch origin
git branch --show-current
git rev-parse HEAD
git rev-parse origin/main
git status --short --branch
```

Expected predecessor:

```text
6b98817cd54a4fe596264920832f3f23c870f002
```

Require:
- branch = main
- HEAD == origin/main
- safe/known working tree

No force push.
No destructive reset.
No silent stash.
No history rewrite.

CRITICAL UI CONTRACT:

SHOPEIVA STRUCTURE = LOCKED UI CONTRACT

DO NOT INTERPRET THE TEMPLATE.
DO NOT REIMAGINE THE TEMPLATE.
DO NOT SIMPLIFY THE TEMPLATE.
DO NOT SUBSTITUTE COMPONENTS.

PORT / REUSE THE EXISTING STRUCTURE.
REPLACE DATA BINDINGS ONLY.

If Shopeiva already has a usable shell/page/pattern, structural or visual deviation requires explicit Architect approval.

FAST CONNECT FIRST:
Before building any new Admin UI, search/reuse the closest Shopeiva Vendor/Admin-like pattern.

Do not spend this task on deep backend features if a surface can be connected with an existing backend contract or a narrow Host/API composition seam.

Scope:
Connect:
1. Admin Dashboard Shell
2. Products / Catalog Operations
3. Orders
4. Sellers
5. Customers
6. Basic operational summaries/navigation

Do not attempt full Admin completeness.

Admin Visual Source:
Shopeiva has no distinct canonical Admin area.

Therefore:
- reuse Shopeiva Vendor Panel shell/topbar/sidebar/card/form language
- adapt it for Admin/Operations semantics
- do NOT invent a third visual language
- do NOT return to the old custom T005 visual language

Approved composition:

```text
Shopeiva Vendor visual language
+
Tooba Professional Data Grid
+
Admin authorization
+
live Tooba APIs
```

Admin Dashboard:
Use Shopeiva dashboard/card composition.

Connect only real summary values where cheap and already available:
- active products/offers
- open orders
- sellers count
- customers/order buyers count
- paid/pending orders

Do not invent charts or revenue analytics if no reliable backend truth exists.

Products:
Use the existing Tooba Professional Data Grid inside Shopeiva panel/card/toolbars.

Minimum useful fields:
- Product
- publication state
- category
- brand
- active offer count
- availability summary
- price/offer summary where properly composed
- updated time
- actions

Architecture must remain:
Catalog Product != Seller Offer

Forbidden:
- Product.Price
- Product.Stock
- frontend business authority
- cross-module SQL join

Do not duplicate the T005 custom Product Workspace visual design.
Reuse its functional interaction/backend work only where useful.

Orders:
Connect live Admin Orders list and order detail.

Minimum fields:
- display order reference
- customer/recipient-safe display
- seller count
- line count
- payment state
- order state
- payable amount
- created date
- actions

Order detail:
- order/checkout reference
- seller orders
- line snapshots
- payment state
- shipping snapshot
- customer/recipient information appropriate for Admin
- status timeline seam if already supported

Do not expose internal secrets.

Sellers:
Connect a fast live Sellers surface using existing Party/Seller foundations.

Minimum:
- seller display name
- status/relationship
- active offers count if cheap
- order count if cheap
- actions/detail seam

Do not build seller onboarding workflow in this task.

Customers:
Connect a fast live Customers surface using existing customer/order identity data.

Minimum:
- display/customer identity
- order count
- last order/activity if available
- status/profile seam

If there is no full Customer CRM/read model, use the narrowest honest read composition available.
Do not invent CRM data.

Authorization:
Admin authorization must be server-side.

Use existing Identity/Authorization/SpiceDB foundations.

Frontend route visibility is not authority.

At minimum prove:
- authorized Admin actor can access Admin surfaces
- non-Admin actor is denied
- Seller actor cannot gain Admin access by changing headers/route values
- missing actor fails closed according to current auth mode

If production SpiceDB mode is not configured in local development, use the existing Development authorization adapter through the same contract. Do not invent a bypass.

Professional Data Grid:
Use existing Tooba Data Grid for Products, Orders, Sellers, Customers where appropriate.

Keep:
- sorting
- filtering
- pagination
- column visibility
- responsive behavior

But preserve Shopeiva shell/card/toolbars.

Do not show component-demo controls prominently.

UX:
Primary Admin UI should be Persian and human-readable.

Do not show raw UUIDs as primary content.
Do not leave English enum/status strings in primary operator UI.

Use proper money formatting:
- fa-IR
- ریال where current market/currency is IRR

Mobile:
Validate core Admin surfaces at:

```text
390x844
```

No giant desktop grid squeezed into mobile.
No page horizontal overflow.
Actions/filters must remain reachable.

Visual Evidence:
Create:

```text
docs/evidence/TB-P05-T004/
```

Required:

```text
01-admin-dashboard-desktop.png
02-admin-products-desktop.png
03-admin-products-mobile-390x844.png
04-admin-orders-desktop.png
05-admin-order-detail.png
06-admin-sellers-desktop.png
07-admin-customers-desktop.png
08-admin-auth-denied.png
09-admin-mobile-navigation.png
10-shopeiva-admin-fidelity.md
```

Create:

```text
docs/evidence/TB-P05-T004/admin-architecture-map.md
```

Show:

```text
Admin UI
→ Host/Admin API
→ server-side authorization
→ Catalog / Offer / Pricing / Inventory / Order / Party contracts
```

No direct DB.
No cross-module SQL join.

Fidelity Evidence:
In:

```text
docs/evidence/TB-P05-T004/10-shopeiva-admin-fidelity.md
```

For each surface record:
- Shopeiva source pattern reused
- Tooba target route
- structure preserved
- data binding replaced
- minimal deviations
- reason for any deviation

Acceptance principle:

```text
If Shopeiva has the pattern,
do not invent a replacement.
```

Out of Scope:
Do NOT implement:
- Fulfillment
- Returns/RMA
- Refunds
- Settlement/Payout
- Advanced Search
- Media pipeline
- Full Seller onboarding
- Customer CRM
- Advanced analytics
- Real PSP changes
- Deep Admin redesign
- Full content studio

Do not fix deferred P04/P05 concerns unless directly touched:
- Payment missing IdempotencyKey → 500/NRE
- Cart replace-hold release→reserve competition window

Tests:
Add/verify focused backend tests for:
- Admin actor allowed
- Seller/non-Admin actor denied
- missing actor denied
- Products composition
- Orders composition
- seller/customer read isolation/authorization where relevant

Add frontend tests for:
- Admin product mapping
- Admin order mapping
- Admin auth denied state
- responsive adapters if testable

Validation:
Backend:

```bash
dotnet restore src/backend/Tooba.slnx
dotnet build src/backend/Tooba.slnx
dotnet test src/backend/Tooba.slnx
```

No filters.

Require:
- warnings = 0
- errors = 0
- failed = 0
- skipped = 0

Frontend:

```bash
cd src/frontend
npm ci
npm run typecheck
npm run lint
npm run test:grid
npm run test:workspace
npm run test:product-workspace
```

Run all repository-supported suites:
- storefront
- cart
- checkout/order
- payment
- seller
- customer
- new admin tests

Then:

```bash
npm run build
git diff --check
git status --short --branch
```

Source of Truth:
Update:
- docs/PROJECT-STATE.md
- docs/ROADMAP.md
- docs/ai/TOOBA-RECOVERY-CONTEXT.md
- docs/prompts/START-HERE-IF-CHATGPT-IS-LOST.md

Record:

```text
TB-P05-T003 = ACCEPTED
TB-P05-T004 = AWAITING_ARCHITECT_ACCEPT
P05 = IN_PROGRESS
```

Keep locked:

```text
FAST CONNECT FIRST
Shopeiva Structure = LOCKED UI CONTRACT
Preserve first
Replace data binding only
Sell quickly
Enhance later
P06 all core surfaces live
```

Save Envelope VERBATIM:
Save exactly:

```text
docs/ai/tasks/TB-P05-T004.task.md
```

Git:
After work:

```bash
git diff --check
git status --short --branch
git add ...
git commit -m "feat fast connect admin operational surfaces [TB-P05-T004]"
git push origin main
git fetch origin
git rev-parse HEAD
git rev-parse origin/main
git status --short --branch
```

Require:

```text
HEAD == origin/main
```

Acceptance Conditions:
PASS requires:
- Shopeiva-derived Admin shell/patterns clearly recognizable
- no new custom Admin visual language
- live Admin Dashboard
- live Products
- live Orders + Order detail
- live Sellers
- live Customers
- real server-side Admin authorization
- Seller actor cannot access Admin
- Data Grid embedded inside Shopeiva visual shell
- desktop works
- 390x844 mobile works
- Persian operator UX
- no raw UUIDs as primary UX
- no Product.Price
- no Product.Stock
- no cross-module SQL join
- full validation green

Result Contract:
Return:

```text
BEGIN_TOOBA_CURSOR_RESULT_V1

Protocol-Version:
1

Task-ID:
TB-P05-T004

Phase:
P05 — Operational Surface Integration

Status:
PASS | FAIL | BLOCKED | RECOVERY_CONFLICT

Summary:
...

Repository-Recovery:
- root:
- branch:
- predecessor:
- final HEAD:
- origin/main:
- synchronized:
- working tree:

Admin-Authorization:
- actor source:
- authorization service:
- SpiceDB/dev adapter:
- Admin actor:
- Seller actor:
- missing actor:
- frontend authority:

Shopeiva-Reuse:
- shell:
- dashboard:
- products:
- orders:
- order detail:
- sellers:
- customers:
- mobile:
- deviations:

Live-Data:
- dashboard:
- products:
- orders:
- sellers:
- customers:

Data-Grid:
- products:
- orders:
- sellers:
- customers:
- mobile:

UX:
- Persian statuses:
- money formatting:
- raw IDs:
- operator errors:

Visual-Evidence:
- 01:
- 02:
- 03:
- 04:
- 05:
- 06:
- 07:
- 08:
- 09:
- 10:
- architecture map:

Tests:
- admin allowed:
- seller denied:
- missing actor:
- products:
- orders:
- frontend admin:

Validation:
- backend restore:
- backend build:
- warnings:
- errors:
- backend passed:
- failed:
- skipped:
- npm ci:
- typecheck:
- lint:
- grid:
- workspace:
- product-workspace:
- storefront:
- cart:
- checkout/order:
- payment:
- seller:
- customer:
- admin:
- frontend build:
- git diff --check:

Architecture:
- Product.Price:
- Product.Stock:
- cross-module SQL join:
- frontend admin authority:

Git:
- commit:
- push:
- final HEAD:
- origin/main:
- synchronized:
- final status:

Source-of-Truth:
- TB-P05-T003:
- TB-P05-T004:
- P05:
- Recovery-ready:

Architectural-Concerns:
- ...

Visual-Concerns:
- ...

Blockers:
- ...

END_TOOBA_CURSOR_RESULT_V1
```

CRITICAL — RESULT DOES NOT END THIS AGENT SESSION

After RESULT:

```text
PIPELINE_STATE: WAITING_FOR_ARCHITECT_IN_SAME_SESSION
```

Then:

```text
DO NOT CLOSE OR LEAVE THIS SESSION.
WAIT HERE FOR THE NEXT AUTHORIZED ENVELOPE.
```

Do not finish.
Do not exit.
Do not self-advance.
Do not invent TB-P05-T005.

RESULT = CONTROL RETURNED TO ARCHITECT.
RESULT != WORKFLOW FINISHED.

Cursor PASS is not Architect ACCEPT.
No Envelope = No Execution.
Repository = Source of Truth.

END_TOOBA_CURSOR_TASK_V1
