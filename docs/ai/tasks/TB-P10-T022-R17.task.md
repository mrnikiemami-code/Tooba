PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_TASK

Task-ID:
TB-P10-T022-R17

Parent-Task:
TB-P10-T022-R16

Channel:
tooba-main

WorkerId:
tooba-worker-01

AgentType:
cursor

Status:
ISSUED

Appearance-USER_VISUAL_ACCEPTED=YES
Builder-USER_VISUAL_ACCEPTED=NO

Title:
Product Showcase Promotion Source Integration — Builder «پیشنهاد شگفت‌انگیز» → Persist → Preview → Publish → Storefront

Architect Review

TB-P10-T022-R16 is ACCEPTED.

Accepted runtime foundation:

deterministic active campaign selection

separate future/teasing resolution

canonical SellerOffer/Price/Inventory reuse

locale fallback

Development-only idempotent campaign seed

bounded queries / no N+1

promo-price override still intentionally deferred

no Builder/Storefront integration yet

This task is the first real vertical integration into the existing Product Showcase source pipeline.

The user-facing goal is the existing Builder screen:

نمایش کالا → منبع محتوا

with a new source:
پیشنهاد شگفت‌انگیز

The internal implementation must remain generic:
PromotionCampaign

Do NOT create a one-off Amazing-only renderer.
Do NOT create a new Product Showcase section type.
Do NOT duplicate ProductCard.
Do NOT redesign the Builder wizard.

Canonical Start

Last Architect-accepted:
TB-P10-T022-R16

Last Implementation:
TB-P10-T022-R16

Expected previous HEAD:
26394e97693507b7127980a1b87b3d52eb76c40b

User-work ancestor:
18ca10c9

Locks:
LOCK-SF-001…404

Appearance:
YES

Builder:
NO

Worker Protocol

No heartbeat.
No polling.
No IDLE loop.

After canonical Result through Bridge:
STOP completely.

1. Recovery Preflight

Verify:

main

HEAD==origin/main

R16 schema/resolver/seed present

18ca10c9 ancestor

user work preserved

unrelated stashes/.tmp untouched

Evidence:
docs/evidence/TB-P10-T022-R17/recovery-start.md

Unexpected tracked divergence => RECOVERY_CONFLICT.

2. Reuse Existing Product Showcase Source Architecture

Inspect and extend the existing canonical source registry/contract used by Product Showcase.

Current accepted sources include:

Manual

Category

Brand

Newest

Add ONE generic source kind:
PromotionCampaign

Do NOT add:

Amazing

FlashSale

Clearance
as separate source kinds.

AMAZING is source configuration / promotion-type identity, not a new source engine.

Evidence:
docs/evidence/TB-P10-T022-R17/source-contract.md

3. Persisted Source Configuration

Define the smallest durable config compatible with current Builder section persistence.

Preferred semantics:

SourceType:
PromotionCampaign

Config:

PromotionTypeCode = AMAZING

CampaignId = nullable

Take / item limit only if existing source contract already supports it

Rules:

If CampaignId is null:

runtime resolves the current active campaign for Store + PromotionTypeCode

If CampaignId is present:

resolve that exact campaign

it must belong to current Store

it must be appropriate for the intended preview/publish context

no raw cross-store access

Do NOT persist:

localized campaign title

campaign product IDs copied into section JSON

price

inventory

timer

discount percent

Section stores source intent, not denormalized campaign output.

Evidence:
docs/evidence/TB-P10-T022-R17/persistence.md

4. User-Facing Builder UX

In نمایش کالا → منبع محتوا, add a clear ordinary-user option:

پیشنهاد شگفت‌انگیز

The user must NOT see:

GUID

PromotionCampaign

AMAZING

internal enum/code

raw JSON

This option maps internally to:

SourceType = PromotionCampaign

PromotionTypeCode = AMAZING

CampaignId = null by default

Keep the UI consistent with existing Source step.

Do NOT redesign geometry, spacing, wizard order, or Shopeiva visual language.

Evidence:
docs/evidence/TB-P10-T022-R17/builder-source-ui.md

5. Optional Campaign Selection — Minimal

Default UX:
پیشنهاد شگفت‌انگیز فعال

meaning CampaignId=null.

If existing UX can support it cleanly without clutter, add an optional advanced choice:
انتخاب کمپین مشخص

If implemented:

use human-readable localized campaign title

show lifecycle/time status

no raw IDs

no full unbounded dropdown

use existing selector/data-grid pattern if needed

Store-scoped only

If this would significantly expand scope:
defer explicit campaign picking.
Default active AMAZING source is sufficient for this task.

Document decision.

6. Source Resolver Adapter

Integrate PromotionCampaign into the existing Product Showcase source resolver.

Required path:

Builder section source
→ PromotionCampaign source config
→ IMerchandisingCampaignQuery
→ active campaign
→ ordered valid campaign members
→ canonical existing Product Showcase / ProductCard projection

Critical:
R16 read model is offer-centric.
This task must join/reuse the existing canonical Product Showcase projection so the final items have:

localized product title

slug/url identity

image

brand if current cards use it

canonical SellerOffer

canonical price

canonical availability

existing card actions/behavior

Do NOT invent a parallel product card DTO if current one is reusable.

Evidence:
docs/evidence/TB-P10-T022-R17/resolver-adapter.md

7. Locale Rules

The source inherits the Page locale.

Required:

FA page -> FA campaign/product projection

EN page -> EN campaign/product projection

normal existing fallback rules

no section-specific language selector

no persisted locale inside source config

Preserve LOCK-SF-396.

8. Preview Behavior

Builder Preview must use the REAL Development campaign data from R16.

No fake frontend products.
No hardcoded preview-only Amazing products.

When source = پیشنهاد شگفت‌انگیز:

resolve active AMAZING campaign

show its actual valid products

preserve SortOrder

filter invalid/out-of-stock members

use canonical ProductCard rendering

If no active campaign:
show the existing source-empty state pattern with a useful Persian message.

Do NOT silently fall back to:

Newest

Manual

Template sample

PreviewFake
for this source.

Evidence:
docs/evidence/TB-P10-T022-R17/preview.md

9. Publish / Storefront Runtime

The exact same persisted source config must resolve after publish.

Required:

Draft/Preview:
uses canonical preview/store context.

Published Storefront:

resolves current active AMAZING campaign at request/render time

does NOT snapshot campaign members into page JSON

if campaign changes, published section reflects current campaign according to cache rules

if campaign expires, products disappear / section uses canonical empty handling

future campaign must not appear before StartAt

Reuse existing Product Showcase section renderer.

No Amazing-specific renderer.

Evidence:
docs/evidence/TB-P10-T022-R17/storefront-runtime.md

10. SSR / Hydration

Preserve current SSR guarantees.

Published Product Showcase using PromotionCampaign must:

have product content in server-rendered HTML when active data exists

not wait for client-only fetch to render first meaningful content

preserve current ProductCard hydration/actions

avoid duplicate fetches

No client polling.

11. Pricing / Discount Display — IMPORTANT

Promo-price override remains deferred from R15/R16.

Therefore:

use canonical current price only

do NOT fabricate a lower price

do NOT fabricate discount %

do NOT show fake struck-through list price

do NOT infer "59%" style discount from campaign membership

The section may still be sourced from Amazing Offers.
Commercial discount rendering will be added only after pricing scope is correctly modeled.

Document visibly in evidence so future work cannot forget this gap.

Evidence:
docs/evidence/TB-P10-T022-R17/pricing-gap.md

12. Campaign Header / Badge

Do NOT force a visual redesign.

If Product Showcase already supports section title:

user-authored section title remains authoritative

Do NOT automatically replace it with campaign title unless current section semantics explicitly support source-derived heading.

Campaign BadgeText may be exposed to future rendering but must not redesign current cards in this task.

Keep scope on source integration.

13. Validation

Builder validation:

PromotionCampaign source is valid if:

PromotionTypeCode is recognized system type

CampaignId, if supplied, is valid/current Store

config shape is valid

Do NOT require an active campaign at page-save time.
Why:
a page may be configured before the campaign starts.

At runtime:

zero active campaign is a valid empty result, not corrupted page config.

14. Cache / Invalidation

Integrate with existing source/render cache if present.

Required cache identity must account for:

Store

page/section source config

PromotionType

Locale

active Campaign identity/version/window

Campaign updates that must not leave stale published output indefinitely:

publish/archive

StartAt/EndAt change

member add/remove/reorder

translation changes

Price/stock freshness must continue to follow canonical pricing/inventory behavior.

Do NOT add polling.

Evidence:
docs/evidence/TB-P10-T022-R17/cache.md

15. Real End-to-End Browser Proof — REQUIRED

Use real Development Host + Frontend.

Create or use a real editable Home/Landing page with Product Showcase.

Required flow:

A. open Builder
B. add/edit نمایش کالا
C. go to منبع محتوا
D. choose پیشنهاد شگفت‌انگیز
E. Review shows human-readable source
F. Save section/page
G. reload editor
H. source persists correctly
I. Preview shows products from active high-priority R16 AMAZING campaign
J. verify lower-priority overlapping campaign is NOT selected
K. publish page
L. open exact public route
M. public Storefront shows same active campaign products
N. verify out-of-stock seeded member is absent
O. verify ordering matches CampaignOffer.SortOrder
P. verify FA page localization
Q. verify EN page localization on corresponding page/route
R. verify no raw GUID/code shown in UI
S. verify no console errors
T. verify no polling / repeated request loop

If Home restore/canonical Home semantics could be affected:
do NOT alter canonical Home fallback behavior.
Prefer a Landing page for proof when safer.

Screenshots:

Source step with پیشنهاد شگفت‌انگیز selected

Review step

Preview with real campaign products

Published FA Storefront

Published EN Storefront

Evidence:
docs/evidence/TB-P10-T022-R17/browser-proof.md

16. Persistence Proof

Prove persisted config after save/reload.

Capture actual stored section source JSON/DTO with:

PromotionCampaign

AMAZING

CampaignId null or selected ID according to implemented UX

No copied product list.
No copied price/stock/timer.

Evidence:
docs/evidence/TB-P10-T022-R17/persistence-proof.md

17. Focused Tests

Required:

1 source registry recognizes PromotionCampaign
2 existing Manual unchanged
3 existing Category unchanged
4 existing Brand unchanged
5 existing Newest unchanged
6 source config round-trip persistence
7 default AMAZING config maps correctly
8 CampaignId null resolves active campaign
9 deterministic high-priority active campaign selected
10 future campaign excluded
11 expired campaign excluded
12 explicit CampaignId cross-store rejected if explicit selection exists
13 out-of-stock member excluded
14 order preserved
15 FA locale
16 EN locale
17 fallback locale
18 empty active campaign => valid empty result
19 no copied member IDs in persisted section config
20 canonical ProductCard projection reused
21 SSR contains campaign products
22 no fake discount display
23 publish/reload runtime works
24 source change does not affect existing source variants
25 no Home canonical restore regression
26 recovery-staleness PASS
27 git diff --check PASS
28 18ca10c9 ancestor PASS
29 HEAD==origin/main PASS

Keep tests focused.

18. Anti-Pattern Gate

Reject:

new Amazing-specific section renderer

ProductSourceType.Amazing

product IDs copied into page JSON for dynamic campaign source

raw GUID/code in Admin UI

fake frontend products

fake promo price

fake discount percent

campaign membership treated as inventory truth

client-only first render

polling

timing hacks

fallback to Newest when no campaign

section-owned locale

redesign of Builder

ProductCard duplication

P11 work

user-work overwrite

Evidence:
docs/evidence/TB-P10-T022-R17/antipattern-scan.md

19. Locks

Add after LOCK-SF-404:

LOCK-SF-405 Product Showcase promotion sourcing is represented by generic SourceType=PromotionCampaign; Amazing is configuration via stable PromotionType Code, never a dedicated Amazing source engine.
LOCK-SF-406 PromotionCampaign section persistence stores source intent only (type/code/optional campaign identity), never denormalized product, price, stock, timer or discount snapshots.
LOCK-SF-407 Product Showcase PromotionCampaign resolution reuses the canonical campaign query plus canonical Product Showcase/ProductCard projection; no campaign-specific ProductCard or renderer is allowed.
LOCK-SF-408 Page locale remains the sole locale context for PromotionCampaign source resolution in Builder Preview and published Storefront.
LOCK-SF-409 A missing active campaign is a valid empty dynamic-source result and must never silently fall back to Newest, Manual, Template sample or PreviewFake content.
LOCK-SF-410 Published PromotionCampaign sections resolve current campaign membership at runtime/SSR from canonical data rather than snapshotting members at page publish time.
LOCK-SF-411 Until campaign-scoped price qualifiers are correctly modeled, PromotionCampaign storefront output must use canonical normal price and must not fabricate promotional price, compare-at price or discount percentage.

Do not renumber existing locks.

20. Completion State

At completion:

Phase:
P10 / Builder Acceptance + Promotion Source Vertical Integration

Last Architect-accepted before review:
TB-P10-T022-R16

Last Implementation:
TB-P10-T022-R17

Expected state:

PromotionCampaign source exists

پیشنهاد شگفت‌انگیز visible in Product Showcase Source step

source config persists

Preview resolves real active campaign

published Storefront resolves same dynamic campaign source

FA/EN work

SSR works

real R16 seed proves behavior

NO promo-price override yet

NO Campaign Admin management UI yet

Appearance YES

Builder remains NO until user visually accepts

P11 retained for later

main

HEAD==origin/main

18ca10c9 ancestor

user work preserved

locks through LOCK-SF-411

Do NOT write Worker IDLE.

After Result through Bridge:
STOP completely.

Acceptance

PASS only if:

source integration is generic

Builder UI shows پیشنهاد شگفت‌انگیز cleanly

persistence round-trip works

preview uses real campaign data

publish/storefront works

SSR works

canonical ProductCard projection reused

out-of-stock filtering works

locale works

no fake promo pricing

existing source types regressions absent

user work preserved

canonical Result delivered

Worker stops

Result Contract

Return ONLY canonical BRIDGE-WAKE-V1 Result with:

Summary
Applicable-Locks
Recovery-Start
Source-Contract
Persistence
Builder-Source-UI
Campaign-Selection
Resolver-Adapter
Localization
Preview
Storefront-Runtime
SSR
Pricing-Gap
Validation
Cache
Browser-Proof
Persistence-Proof
Focused-Validation
AntiPattern-Scan
Lock-Registry
Recovery-SoT
Git
Visual-Concerns
Architectural-Concerns
Blockers

Do not include Worker-IDLE.

After sending Result through Bridge:
STOP completely.

END_TOOBA_TASK