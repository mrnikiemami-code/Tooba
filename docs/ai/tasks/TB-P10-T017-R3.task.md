PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_REPAIR_TASK

Task-ID:
TB-P10-T017-R3

Parent-Task:
TB-P10-T017-R2

Channel:
tooba-main

WorkerId:
tooba-worker-01

AgentType:
cursor

Status:
REPAIR

USER_VISUAL_ACCEPTED=NO

Title:
Storefront-Wide Theme Coverage Repair — Route Crawl + Semantic Surface Enforcement Across Every Storefront and Customer Page

Architect Review

TB-P10-T017-R2 was technically accepted, but USER VISUAL REVIEW has now found a material coverage defect.

Observed by user screenshots:

Home still contains large areas that do not visibly follow the intended four-role semantic surface system.

PDP/product page still contains major large white/fixed surfaces that do not inherit the global color architecture.

Login/auth page is only partially themed.

Checkout/Shipping is only partially themed.

Customer Panel/Account pages are still largely white and do not participate consistently in the Store theme.

The user cannot manually submit screenshots for every page and expects the architecture to guarantee coverage across the whole Storefront.

Therefore the previous "all page families mapped" acceptance was insufficient in real rendered coverage.

This repair must solve the COVERAGE PROBLEM SYSTEMICALLY.

Do NOT fix only the screenshots shown.
Do NOT patch page-by-page by visual guess.
Do NOT create page-specific theme families.
Do NOT start TB-P10-T018.

Product Requirement — LOCKED

The Storefront + Customer-facing application must use one global semantic color architecture everywhere.

Canonical global surface roles remain:

PageBackground
user-facing: زمینه صفحه

SectionSurface
user-facing: بخش اصلی

SectionAlternate
user-facing: بخش جایگزین

SectionAccent
user-facing: بخش برجسته

Additional canonical semantic surfaces may remain:

CardSurface

ElevatedSurface

InputSurface

HeaderSurface

FooterSurface

OverlaySurface

Border

Text

TextMuted

Primary / PrimaryStrong / OnPrimary

status colors

But large page regions may NOT silently fall back to arbitrary white/gray hardcoded backgrounds.

Scope

IN SCOPE:

all public Storefront pages

all locale Storefront pages (/fa/... and equivalent current locale architecture)

Home

Landing pages

category / PLP

search/listing

PDP/product pages

brand/vendor/seller storefront pages if present

article/blog/content pages

cart

login/auth/customer auth screens rendered inside Storefront shell

checkout

shipping

payment-facing Storefront pages

wishlist

customer/account panel

profile

addresses

orders/order details

notifications

tickets/support

wallet

gift cards

settings

any other customer-facing page reachable from customer panel

shared Storefront shell/header/footer

shared page wrappers and section wrappers used by the above

OUT OF SCOPE:

Admin panel

Seller backoffice/panel unless it is actually rendered as part of customer Storefront

arbitrary custom color builder

layout redesign

business workflow changes

Recovery State

Phase: P10
Last Architect-accepted: TB-P10-T017-R2
Last Implementation: TB-P10-T017-R2
Current Repair: TB-P10-T017-R3
Worker: tooba-worker-01
Expected branch: main
Expected previous HEAD: 5ab828075b679464ad1362c29bcc51f1eeb4ddfa
USER_VISUAL_ACCEPTED=NO

Preserve LOCK-SF-001…202.

1. Recovery Preflight

Record:

branch

HEAD

origin/main

git status --short

Recovery/SoT

Host :5088

FE :3000

current appearance state

unrelated local user-authored changes

Unexpected tracked divergence => RECOVERY_CONFLICT/BLOCK.

Evidence:
docs/evidence/TB-P10-T017/r3-recovery-start.md

2. Build an Authoritative Route Inventory

Do NOT rely on remembered page families.

Discover the actual application routes from the codebase.

Required discovery sources:

Next.js app/ route tree and route groups

dynamic route segments

shared layouts

customer/account navigation configuration

Storefront header/footer links

menu destinations

checkout/cart/auth flows

any route registry/navigation registry already in repo

Produce a canonical route inventory grouped into:

A. Public Storefront
B. Commerce flow
C. Auth
D. Customer panel/account
E. Content
F. Dynamic entity pages
G. Other customer-facing routes

For dynamic routes, record at least one real seeded/runtime sample URL per pattern.

Required table:
Route Pattern | Example URL | Auth State | Layout/Shell | Major Wrapper | Theme Coverage Status | Notes

Evidence:
docs/evidence/TB-P10-T017/r3-route-inventory.md

The route inventory is the source of truth for runtime crawl coverage.

3. Static Theme Coverage Scan

Scan Storefront/customer-facing source files for large-surface background patterns that bypass the semantic theme system.

At minimum detect candidates such as:

bg-white

bg-gray-*

bg-slate-*

bg-[#...]

inline background / backgroundColor

large fixed from-* / via-* / to-* decorative fills

CSS module fixed backgrounds

raw RGB/hex used on page/section wrappers

arbitrary neutral backgrounds on large panels

Do NOT mechanically replace every match.

Classify each candidate:

legitimate CardSurface

legitimate InputSurface

legitimate Header/Footer

media/decorative

status semantic

should be PageBackground

should be SectionSurface

should be SectionAlternate

should be SectionAccent

dead/unreachable code

Evidence:
docs/evidence/TB-P10-T017/r3-static-theme-scan.md

Required summary:

files scanned

candidate count

real violations count

intentional fixed surfaces count

repaired count

unresolved count

Unresolved real violations => BLOCK/REPAIR, not PASS.

4. Shared Semantic Wrapper Strategy

Fix coverage through SHARED ARCHITECTURE wherever possible.

Preferred techniques:

canonical Storefront page canvas wrapper

canonical SectionSurface helpers/classes/components

canonical panel/card primitives

customer panel content shell mapped to theme tokens

shared auth shell mapped to theme tokens

shared commerce/checkout shell mapped to theme tokens

Avoid repeating token classes in dozens of pages if a shared layout can own the behavior.

Do NOT introduce a new theme fetch per route/component.

5. Semantic Utility / Primitive Contract

Create or consolidate a small canonical API for surfaces, for example equivalent to:

StorefrontPageSurface

StorefrontSectionSurface

StorefrontPanelSurface
or token-based utility classes/data attributes

Exact names are implementation choice.

Required:

typed or constrained roles

PageBackground

SectionSurface

SectionAlternate

SectionAccent

CardSurface / ElevatedSurface where appropriate

no raw arbitrary color parameter

no free-form CSS parameter

The architecture must make the correct path easier than hardcoded backgrounds.

6. Home Coverage Repair

Crawl the actual Home DOM.

Every major vertical band/section must be classified.

Required:

outer canvas = PageBackground

each major content band has explicit semantic role

sections no longer float as white islands unless intentionally CardSurface

product cards remain CardSurface

promo/hero can use SectionAccent where appropriate

category/story/media regions remain readable

Neutral remains backward-compatible

PaletteTint creates visible hierarchy

Evidence:
docs/evidence/TB-P10-T017/r3-home-coverage.md

7. PDP / Product Page Coverage Repair

This is explicitly user-reported.

Classify and repair:

page canvas

primary PDP shell

media/product-info regions

seller/offer area

product description/details

specs

benefits/services

related products

recommendation bands

any sticky/summary panel if present

Do not make the central product reading/purchase region over-colored.
Use SectionSurface/CardSurface appropriately.

Required:

no large accidental fixed-white region breaking the palette

related sections visibly participate in semantic hierarchy

product cards remain readable

Evidence:
docs/evidence/TB-P10-T017/r3-pdp-coverage.md

8. Auth / Login Coverage Repair

The login screen shown by the user must participate in the Store theme.

Required:

page outer background -> PageBackground

auth block -> CardSurface/ElevatedSurface

supporting service strip -> SectionSurface/Alternate as appropriate

newsletter/footer preserves FooterSurface/brand role

no unrelated fixed-white page bands

Do NOT change auth behavior.

9. Cart / Checkout / Shipping / Payment Coverage Repair

Crawl the actual commerce flow.

Required:

page backgrounds participate in global theme

workflow header/step area participates

content zones mapped to section roles

forms remain InputSurface

order summary remains Card/ElevatedSurface

trust/status semantics remain independent

no large white canvas bypasses

Do NOT recolor status/error/success semantics.

10. Customer Panel Full Coverage Repair

This is explicitly user-reported and MUST be comprehensive.

Discover all customer panel routes from its real navigation.

At minimum inspect if present:

dashboard

orders

order details

favorites/wishlist

addresses

notifications

tickets

wallet

gift cards

profile

settings

any other reachable customer menu item

Required architecture:

customer panel outer canvas participates in PageBackground

sidebar remains a deliberate shell surface

main content containers map to SectionSurface / SectionAlternate

inner cards remain CardSurface

tables/lists/summary cards remain readable

no page-specific theme system for customer panel

palette + ThemeMode + BackgroundStyle inherited from same Store Appearance projection

Evidence:
docs/evidence/TB-P10-T017/r3-customer-panel-coverage.md

11. Content / Blog / Article Coverage

If content/blog routes exist:

outer canvas -> PageBackground

long-reading body uses readable SectionSurface/CardSurface

related-content bands -> SectionAlternate or SectionAccent as appropriate

no content-specific theme family

Long-form readability takes precedence over decorative saturation.

12. Automated Runtime Crawl — REQUIRED

Build a reusable Playwright/runtime crawler for theme coverage verification.

Do not create a one-off screenshot script tied only to this task.

The crawler must:

load the canonical route inventory

crawl anonymous routes

authenticate using the existing safe dev/test customer flow for customer-only routes

sample real dynamic routes from existing seeded/runtime data

run with:

Neutral / tooba-blue / LightOnly

PaletteTint / tooba-blue / LightOnly

PaletteTint / one contrasting palette (wine-burgundy or forest-green)

PaletteTint / DarkOnly representative subset

For every route capture:

HTTP/document status

page title/path

top-level major background colors

presence of semantic surface-role marker/token where applicable

screenshots for failures

console errors

500s

hydration warnings

appearance request count if practical

Do NOT require full combinatorial screenshots for every palette/page.

13. Rendered-Pixel Coverage Heuristic

Implement a practical inspection heuristic to detect suspicious unthemed large areas.

Goal:
find large hardcoded white/neutral zones when PaletteTint is active.

Allowed approaches:

DOM bounding boxes + computed background colors

semantic role markers + area thresholds

screenshot pixel region sampling

combination of the above

Required:

flag large visible blocks whose computed background is fixed white/neutral and whose semantic classification is not Card/Input/Media/Header/Footer/Status

report route + selector/component hint + bounding box + computed color

allow explicit intentional exclusions through central semantic classification, not random ignores

Keep it deterministic and maintainable.

Evidence:
docs/evidence/TB-P10-T017/r3-runtime-coverage-report.md

14. Coverage Gate

PASS requires:

100% of discovered Storefront/customer route patterns classified

every reachable static route crawled

every dynamic route pattern has at least one real representative sample

every customer navigation destination crawled

zero unresolved large-surface semantic violations

zero task-owned 500s

zero broken theme inheritance routes

If a route cannot be crawled because required seed/runtime data does not exist:

document it explicitly

prove its shared layout/wrapper coverage statically

do not silently omit it

15. Theme Inheritance Verification

For every route family verify inheritance of:

PaletteKey

ThemeMode/effective theme

BackgroundStyle

ProductCardSkin where product cards exist

No route may instantiate an independent appearance state.

No page-specific fetch.

16. Neutral Backward Compatibility

After repairs, Neutral must remain professional and close to the previously accepted neutral look.

Do NOT make Neutral pink/green/etc.

17. PaletteTint Quality

For representative:

Home

PDP

Checkout

Customer Orders/Order detail

Login

Landing

Verify:

PageBackground visible

SectionSurface visible

SectionAlternate visible where used

SectionAccent visible where appropriate

Card/Elevated separation remains strong

18. Required Final Screenshots

Persist only representative final proof, not every route screenshot.

Required:

home-r3.png

pdp-r3.png

plp-r3.png

login-r3.png

cart-r3.png

checkout-r3.png

customer-dashboard-r3.png

customer-orders-r3.png

customer-order-detail-r3.png

landing-r3.png

content-r3.png if route exists

dark-home-r3.png

dark-customer-r3.png

Additionally persist screenshots automatically only for crawl failures.

Store:
docs/evidence/TB-P10-T017/screenshots/r3/

19. Minimal Useful Tests

Do not explode test count.

Required:

existing Appearance tests

existing surface-role tests

critical-storefront

one route-inventory/coverage guard

one static semantic violation guard for major Storefront wrappers

one authenticated customer-panel theme inheritance guard

recovery-staleness

git diff --check

Prefer generalized coverage guards over page-specific tests.

20. Anti-Pattern Gate

Scan for:

page-specific theme token families

page-by-page duplicate theme state

route-specific palette definitions

arbitrary hardcoded page/section backgrounds

!important theme hacks

screenshot-only CSS

hydration suppression added for this repair

polling

per-section appearance fetch

blanket replacement of all white surfaces

CardSurface incorrectly used as entire page background

customer panel separate theme engine

Any real occurrence => REPAIR.

Evidence:
docs/evidence/TB-P10-T017/r3-antipattern-scan.md

21. Architecture Documentation

Update/create:
docs/architecture/storefront-global-surface-theme.md

Add:

route-wide coverage rule

customer-panel inclusion

semantic wrapper strategy

route inventory expectation

rule for new future pages:
every new Storefront/customer page must choose semantic surface roles and pass coverage guard

22. Lock Registry

Add non-duplicate locks after LOCK-SF-202 equivalent to:

Every public Storefront and customer-facing route must inherit the canonical Store Appearance projection; customer panel is not a separate theme system.

Every major page/section background must map to a canonical semantic surface role or an explicitly allowed Card/Input/Header/Footer/Media/Status role.

New Storefront/customer routes must be added to route coverage inventory and pass semantic surface coverage before acceptance.

Large hardcoded white/gray/arbitrary backgrounds are forbidden on Storefront/customer page/section wrappers when they bypass semantic theme roles.

Dynamic routes require representative runtime coverage; customer navigation destinations require authenticated crawl coverage.

Theme coverage is enforced by shared wrappers/primitives and generalized guards, not manual screenshot-by-screenshot patching.

Use next canonical LOCK-SF numbers.
Do not renumber existing locks.

23. Evidence

Create/update:
docs/evidence/TB-P10-T017/

Required new R3 evidence:

r3-recovery-start.md

r3-route-inventory.md

r3-static-theme-scan.md

r3-home-coverage.md

r3-pdp-coverage.md

r3-customer-panel-coverage.md

r3-runtime-coverage-report.md

r3-focused-validation.md

r3-antipattern-scan.md

r3-recovery-sot.md

screenshots/r3/

24. Recovery / Pipeline

At completion:

Phase P10

Last Architect-accepted before review: TB-P10-T017-R2

Last Implementation: TB-P10-T017-R3

Current Issued/Repair: none

USER_VISUAL_ACCEPTED=NO

Worker IDLE

branch main

ending HEAD

HEAD==origin/main

task-owned clean

unrelated local user changes preserved

do NOT invent/start TB-P10-T018

Run recovery staleness guard.

Acceptance

PASS only if ALL:

actual route inventory is complete

public Storefront routes are covered

commerce/auth routes are covered

full customer panel navigation is covered

dynamic patterns have real sample coverage

Home major surfaces participate

PDP major surfaces participate

Login participates

Checkout/Shipping/Payment participate

Customer panel participates

shared semantic architecture is used instead of page-specific color systems

zero unresolved large-surface hardcoded-background violations

runtime crawler passes

suspicious large-white-area heuristic passes or only contains centrally documented intentional semantic exclusions

PaletteTint visibly changes representative pages

Neutral remains backward-compatible

all routes inherit the same Store Appearance projection

no new task-owned 500

no appearance polling/fetch duplication

minimal generalized tests added

representative screenshots persist

anti-pattern scan CLEAN

locks updated without duplicates

Recovery exact

HEAD==origin/main

Worker IDLE

no TB-P10-T018

USER_VISUAL_ACCEPTED remains NO pending actual user inspection

Result Contract

Return ONLY canonical BRIDGE-WAKE-V1 Result with:

Summary
Applicable-Locks
Recovery-Start
Route-Inventory
Static-Theme-Scan
Shared-Semantic-Architecture
Home-Coverage
PDP-Coverage
Auth-Coverage
Commerce-Coverage
Customer-Panel-Coverage
Content-Coverage
Runtime-Crawler
Rendered-Pixel-Coverage
Theme-Inheritance
Neutral-Compatibility
PaletteTint-Quality
Focused-Validation
Visual-Evidence
AntiPattern-Scan
Lock-Registry
Recovery-SoT
Git
Architectural-Concerns
Visual-Concerns
Blockers

END_TOOBA_REPAIR_TASK