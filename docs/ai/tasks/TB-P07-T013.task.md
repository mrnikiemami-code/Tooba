PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

TASK-ID: TB-P07-T013
PARENT-TASK: TB-P07-T012
PROJECT: Tooba
PHASE: P07
CHANNEL: tooba-main
STATUS: ISSUED
TASK-TYPE: PRODUCT VARIANTS — AXES + MATRIX + ADMIN UX
WORKER-POLICY: ONE WORKER = ONE ACTIVE TASK

Architect Acceptance

TB-P07-T012 is ACCEPTED.

Baseline:

Product Attributes tab is real.

Product attribute values are typed and validated.

T007 effective Category schema is canonical.

category-specific IsVariantAxis is authoritative.

Product != Offer.

no Product.Price / Product.Stock.

no Pricing / Inventory direct joins.

AppDataGrid untouched.

baseline HEAD == origin/main:
3ff9367a23170cbc6bd979169b0963858b376566

Locked UX Principle

Use Persian term تنوع, not گونه.

The Product Variants UX must be understandable for ordinary non-technical Admin users:

show which attributes create variations

show selected values for each axis

preview combinations before applying

explain what will be added/kept/deactivated

never show raw IDs

Hard Architecture Locks

ProductVariant is canonical Catalog identity.

Price is NOT stored on ProductVariant.

Stock is NOT stored on ProductVariant.

Seller later creates Offer against Product/Variant.

Pricing / Inventory remain separate modules.

no cross-module SQL JOIN.

axes come only from effective Category attributes where IsVariantAxis == true.

AttributeDefinition capability must allow variant use.

raw AgGridReact forbidden.

if a grid is genuinely needed, reuse Tooba canonical AppDataGrid only.

Goal

Implement real Product Workspace تنوع‌ها tab:

Product.CategoryId
→ Effective Category Schema
→ IsVariantAxis attributes
→ product-applicable selected axis values
→ combination preview
→ canonical ProductVariants

1 — Canonical Variant Model

Use/complete existing Catalog ProductVariant.

Conceptually:

Id

ProductId

Status

SortOrder

IsDefault?

canonical SKU/InternalCode? only if existing domain supports it

CreatedAt / UpdatedAt

Variant selected axis values conceptually:

ProductVariantId

AttributeDefinitionId

AttributeOptionId / typed normalized axis value

Prefer option-backed axis values for enum/color/size-like axes.

2 — Combination Uniqueness

Within a Product, one exact axis combination must be unique.

Example:
Color=Black + Storage=256GB

must never be duplicated.

Backend authoritative.

3 — Axis Source

Only effective attributes where:
IsVariantAxis == true

If none:
show:
برای این دسته‌بندی ویژگی تنوع تعریف نشده است.

Do not let Product Admin invent arbitrary local axes.

4 — Axis Value Selection

For each axis show localized label/options.

Example:
رنگ:

مشکی

سفید

آبی

حافظه:

128GB

256GB

User selects which values apply to THIS Product.

No raw option IDs.

5 — Preview Combinations

Show readable combination preview before apply.

Example:

مشکی / 128GB

مشکی / 256GB

سفید / 128GB

سفید / 256GB

If combinations become large:

show total count

warn clearly

enforce/document a safe cap

never create thousands accidentally

6 — Reconciliation

Do NOT delete/regenerate everything blindly.

Compare desired vs existing:

unchanged

new

no longer selected

conflicts

Show impact before save, e.g.:

۴ تنوع بدون تغییر
۲ تنوع جدید
۱ تنوع دیگر انتخاب نشده است

7 — Removal / Archive Safety

Do not hard-delete variants casually if referenced by:

Seller Offers

Orders/history

Inventory

Prefer archive/deactivate when referenced.

Use module contracts/read models for reference safety.
No cross-module joins.

If no safe reference-check contract exists, default to archive/deactivate.

8 — Variant Status / Default

Support localized canonical state.

At minimum:

فعال

غیرفعال / بایگانی‌شده

If domain supports default variant:

exactly one default

label تنوع پیش‌فرض

9 — SKU / Internal Code

If existing ProductVariant has canonical SKU/internal code:

preserve it

make optional/editable per domain

enforce uniqueness if required

Do NOT conflate canonical ProductVariant code with future Seller Offer SKU.

10 — VIEW Mode

Readable cards/list.

Per variant:

selected axis values

status

default badge

SKU/code if relevant

optional offer count only through safe contract

No editable price or stock.

11 — EDIT Mode

Authorized user can:

select axis values

preview combinations

create missing combinations

deactivate/archive no-longer-selected combinations

reorder

choose default

edit canonical SKU/code if allowed

Save / Cancel / dirty-state required.

12 — Generation Rules

Generate Cartesian combinations only from selected values of effective axes.

1 axis:
2 values → 2 variants

2 axes:
2 colors × 3 storage → 6 variants

Deterministic order.
No duplicates.

If no axis:
do not invent a fake singleton variant unless existing domain explicitly requires it.

13 — Unsupported Axis Types

Variant axes should strongly prefer normalized option identity.

If an axis is not option-backed:

inspect current domain support

reject unsafe/free-text variant axes if necessary

surface Architectural Concern rather than inventing unreliable free-text variant combinations

14 — Product Attribute vs Variant Attribute

Document and enforce:

Product-level attributes
= apply to Product overall

Variant-axis attributes
= values that distinguish ProductVariants

Do not duplicate semantics incorrectly.

15 — Seller / Offer Handoff

Future flow:

Seller selects Product
→ selects canonical Variant
→ creates Offer
→ sets Price
→ sets Inventory

T013 must leave this contract clean.

Do NOT create Seller Offer records.

16 — Category Change / Schema Safety

Extend T012 category-change safety.

If category/schema change alters variant axes:

report impacted variants

do not auto-delete

require explicit resolution/review

prevent invalid silent finalization

17 — Variant Readiness

Add readiness result conceptually:

IsValid

MissingAxes[]

InvalidVariants[]

DuplicateCombinations[]

NoDefaultVariant? if required

Publishing flow later consumes it.

18 — Authorization

Use existing SpiceDB-centered authorization.

view variants

edit variants

Backend authoritative.

View-only users must not see mutation controls.

19 — APIs

Provide/use module-owned APIs for:

get effective variant axes

get Product variants

preview desired combinations

apply variant changes

set status/default/order

readiness

variant impact for category/schema change

Do not expose EF entities.

20 — Persistence

Use existing tables when possible.

If migration needed enforce:

ProductId relation inside Catalog

combination uniqueness

stable ordering

one default if supported

normalized axis value identity

no cross-module FKs

21 — Admin UX

Keep Product Workspace styling.

Suggested layout:

readiness summary

axis selectors

ساخت/به‌روزرسانی تنوع‌ها

combination preview

existing variants list/cards

Do not expose technical words like Cartesian product.

22 — Mobile / Accessibility

Mobile:

stacked axis selectors

readable combination cards

obvious save/cancel

no squeezed desktop matrix

Accessibility:

labels

keyboard interaction

visible focus

status not color-only

dialog focus management

icon buttons need tooltip/aria-label

23 — Tests

Backend:

one-axis generation

multi-axis generation

uniqueness

deterministic order

unchanged variant preserved

new variant created

removed variant safely deactivated

referenced variant not hard-deleted

invalid axis rejected

default uniqueness

readiness

category-change variant impact

no ProductVariant.Price

no ProductVariant.Stock

no Pricing/Inventory join

Frontend:

VIEW

EDIT

axis selection

preview

combination count

large warning

impact summary

Save/Cancel

default/status

view-only

no raw IDs

no price/stock controls

mobile

accessibility basics

24 — Documentation

Create/update:

docs/catalog/PRODUCT-VARIANTS.md

docs/catalog/PRODUCT-CATALOG-ADMIN.md

recovery/project state docs

Document:

Product vs Variant

axes

uniqueness

generation rules

archive/delete safety

default variant

Seller Offer handoff

Pricing/Inventory boundary

category/schema safety

readiness

25 — Validation

Backend:

restore

build

full tests

Frontend:

typecheck

lint

tests

production build

Require:

0 errors

0 failed

0 skipped

no new warnings

Always:

git diff --check

26 — Runtime

Keep alive:

Backend :5088

Frontend :3000

Shopeiva :3001

Product Admin:
http://localhost:3000/fa/admin/products

27 — Git

Commit + push.

Require:

HEAD == origin/main

tracked tree clean

unrelated user changes preserved

Acceptance

PASS only if:

تنوع‌ها tab is real

axes come from effective Category IsVariantAxis

user selects product-applicable axis values

combinations preview before apply

no duplicate combinations

existing variants reconciled safely

referenced variants not hard-deleted unsafely

status/default/order supported

Product vs Variant attribute boundary documented

no ProductVariant.Price

no ProductVariant.Stock

no Pricing/Inventory direct joins

Seller Offer handoff clean

category/schema change safe

variant readiness exists

ordinary-user UX

no raw IDs

no raw AgGridReact

full validation green

runtime alive

Git synchronized

USER_VISUAL_ACCEPTED remains NO until manual review.

Result Contract

Return ONLY:

PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_WORKER_RESULT

Task-ID:
TB-P07-T013
Parent-Task:
TB-P07-T012
Channel:
tooba-main
Status:
PASS | FAIL | BLOCKED | RECOVERY_CONFLICT

Summary:
...

Canonical-AppDataGrid:

used:

raw AgGridReact:

new wrapper:

product list regression:

Variant-Model:

entity/path:

axis value entity:

status:

default:

sku/code:

uniqueness:

migration:

Axes:

source:

effective IsVariantAxis:

supported axis types:

unsupported axis handling:

localized names/options:

Combination-Generation:

one axis:

multi-axis:

deterministic:

duplicate prevention:

safe maximum:

large warning:

Reconciliation:

unchanged:

new:

removed:

referenced variant safety:

hard delete:

archive/deactivate:

explicit confirmation:

Variants-UX:

VIEW:

EDIT:

axis selector:

preview:

impact summary:

status:

default:

sku:

Save:

Cancel:

dirty-state:

mobile:

accessibility:

Boundary:

Product attribute vs Variant attribute:

Product != Offer:

ProductVariant.Price:

ProductVariant.Stock:

Seller Offer handoff:

Pricing join:

Inventory join:

Category-Schema-Safety:

category change impact:

axis removal/change:

existing variant protection:

readiness:

Authorization:

SpiceDB:

view-only:

edit:

backend authoritative:

API:

axes:

current variants:

preview:

apply:

readiness:

impact:

gaps:

Validation:

backend restore:

backend build:

backend full tests:

frontend typecheck:

frontend lint:

frontend tests:

frontend build:

passed:

failed:

skipped:

warnings:

new warnings:

git diff --check:

Documentation:

PRODUCT-VARIANTS:

PRODUCT-CATALOG-ADMIN:

recovery/state:

Runtime:

Backend:

Frontend:

Shopeiva:

Product Admin:

kept alive:

Git:

branch:

commit:

push:

final HEAD:

origin/main:

synchronized:

tracked tree:

unrelated user changes:

Architectural-Concerns:
...

Visual-Concerns:
...

Blockers:
...

END_TOOBA_WORKER_RESULT

After Result:

Worker -> IDLE / Waiting

keep runtimes alive

do not invent next task

do not add extra prose

END_TASK
