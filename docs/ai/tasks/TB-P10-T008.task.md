PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_TASK

Task-ID:
TB-P10-T008

Parent-Task:
TB-P10-T007

Channel:
tooba-main

WorkerId:
tooba-worker-01

AgentType:
cursor

Status:
ISSUED

USER_VISUAL_ACCEPTED=NO

Title:
Storefront Light/Dark Theme — Controlled ThemeMode, Admin Selection, SSR Apply, User Choice, Full Token Coverage

Architect Decision

TB-P10-T007 is Architect-ACCEPTED.

Accepted baseline now includes:

canonical StoreAppearanceSettings

7 curated PaletteKey presets

full Storefront brand-token coverage across Header/Home/PLP/PDP/Cart/Shipping/Account

status semantic colors isolated from brand palette

Admin Appearance palette selection/preview/save

SSR palette application

immediate Store-scoped cache invalidation

LOCK-SF-001…150

This task implements controlled Light/Dark ThemeMode on top of the accepted token architecture.

This task does NOT implement:

arbitrary/custom color builder

Product Card Skin selection

Landing/Page builder

Menu builder

typography editor

free-form CSS/HTML/JS

TB-P10-T009

Recovery State

Phase: P10
Last Architect-accepted: TB-P10-T007
Last Implementation: TB-P10-T007
Current Task: TB-P10-T008
Worker: tooba-worker-01
Expected branch: main
Expected previous HEAD: a93e0a9cea06c79b58a60b467825fde48054cb2d
USER_VISUAL_ACCEPTED=NO

Preserve LOCK-SF-001…150.

1. Recovery Preflight

Record:

branch

HEAD

origin/main

git status --short

Recovery/SoT

Host :5088

FE :3000

current Store PaletteKey/ThemeMode

unrelated local user-authored changes

Unexpected tracked divergence => RECOVERY_CONFLICT/BLOCK.

Evidence:
docs/evidence/TB-P10-T008/recovery-start.md

2. Mandatory Discovery — Existing Dark Infrastructure

Inspect targeted code only.

Determine:

current ThemeProvider behavior

dark class application mechanism

Tailwind darkMode config

existing .dark tokens

existing persisted ThemeMode values

current user preference storage if any

current Admin Appearance settings shape

current StoreAppearanceSettings API

existing system preference detection

any hard-coded light surfaces still present after T007

Shopeiva dark classes available in reference source

Create:
docs/evidence/TB-P10-T008/dark-discovery.md

Required table:
Area | Current behavior | Store-controlled? | User-controlled? | Dark-ready? | Required action

Do NOT create duplicate theme providers.

3. Canonical ThemeMode Contract

Implement/reuse a controlled enum with exactly the modes supported by architecture.

Preferred modes:

LightOnly

DarkOnly

System

UserChoice

If current naming/conventions differ, use canonical project naming but preserve these semantics.

Semantics:

LightOnly

Storefront always light

no user toggle

DarkOnly

Storefront always dark

no user toggle

System

follows prefers-color-scheme

no persistent user override unless existing architecture explicitly requires it

UserChoice

Store provides both light/dark

user sees theme toggle

user preference persists client-side in the canonical preference mechanism

initial render must avoid incorrect flash

Do NOT allow arbitrary mode strings.

4. Admin Appearance — ThemeMode Selection

Extend the existing Admin Appearance tab.

Required:

Theme mode selector/cards

clear labels/explanations

current persisted mode

preview for light/dark behavior

dirty state integrated with existing Appearance save

Save/Cancel

PaletteKey + ThemeMode saved atomically through canonical settings API

invalid mode rejected backend-side

no separate settings endpoint if unnecessary

Do not expose custom tokens.

5. Appearance Persistence

Backend update path must:

validate ThemeMode

preserve PaletteKey

update StoreAppearanceSettings atomically

remain Store-scoped

enforce authorization

invalidate only changed Store appearance cache

no restart/redeploy/TTL wait

If current API uses one appearance payload, extend it rather than creating a conflicting write path.

6. Light/Dark Semantic Token Completion

The Storefront must use semantic surface/text/border tokens so dark mode is complete, not partial.

At minimum dark mode must correctly cover:

page background

surface

elevated surface

primary text

muted text

borders/dividers

header/nav

Mega Menu

Home sections

Product Cards

PLP

PDP

Cart/Mini Cart

Shipping/Checkout

Account dropdown/customer surfaces

shared forms/inputs

modals/popovers used by Storefront

buttons/links/focus states

Brand PaletteKey remains active in both light and dark modes.

Do NOT bind status colors to brand palette.

7. Dark Token Strategy

Use the accepted semantic-token architecture.

Required:

no page-local dark palette registries

no duplicated hard-coded dark colors per page

dark values centralized in canonical tokens/registry

palette and theme mode compose predictably

on-primary/contrast remains valid in both modes

surfaces/text meet readability requirements

If current .dark tokens can be reused safely, reuse them.
If they are insufficient, extend canonical tokens rather than adding page hacks.

8. SSR / First Paint

Dark mode must be correct on first paint.

Required:

LightOnly: first HTML renders light

DarkOnly: first HTML renders dark

System: first paint respects system preference without avoidable FOUC

UserChoice: saved user preference wins on first paint where technically available; fallback behavior must be deterministic

Do not rely on a visible post-hydration color flip.

Use the existing Next/SSR architecture and only the minimum safe bootstrap needed.

No inline arbitrary executable theme payload from DB.

Evidence:
docs/evidence/TB-P10-T008/ssr-first-paint.md

9. UserChoice Toggle

Only in UserChoice mode:

Required:

accessible theme toggle in Storefront header/account-area using existing UI language

clear light/dark state

keyboard accessible

no duplicate desktop/mobile independent state

one canonical client preference

preference persists across navigation/reload

preference is user-device preference, not Store database state

switching does not trigger appearance API polling or DB write

If logged-in preference persistence architecture already exists and is clearly canonical, reuse it; otherwise use controlled client preference storage for this task and document it.

Do not store commerce state in that storage.

10. Palette × Theme Composition

Verify all 7 curated palettes in both light and dark modes.

Required:

primary/accent remains recognizable

on-primary contrast valid

text/surface readable

borders visible

status semantics independent

no palette produces unusable dark surfaces

If a palette needs dark-specific token variants, add them to canonical palette definitions, not page-specific branches.

Evidence:
docs/evidence/TB-P10-T008/palette-theme-matrix.md

11. Accessibility

Validate at minimum:

primary CTA contrast

body text on background

muted text

input text/background/border

focus indicator

active tab/variant

destructive/error state

success/warning visibility

theme toggle accessible name/state

All supported palette × theme combinations must satisfy project contrast threshold.

12. Shopeiva Visual Contract

Home/PDP remain locked.

Dark implementation may change colors/surfaces only.

Do NOT change:

layout

spacing

typography

DOM hierarchy without necessity

interaction timing

animation behavior

product density

section order

Compare current Shopeiva dark behavior where useful, but preserve Tooba's accepted structure.

13. Default Behavior / Backward Compatibility

Existing Stores must not unexpectedly flip theme.

Required:

existing rows/defaults resolve to accepted light behavior unless current persisted ThemeMode explicitly says otherwise

tooba-blue + LightOnly/current default remains visually equivalent to T007

migration/default does not break existing Stores

No surprise dark rollout.

14. Runtime Scenarios — REQUIRED

Use:

Host :5088

FE :3000

Run at least:

A. Admin set LightOnly + tooba-blue
B. Storefront Home/PDP/Cart/Shipping -> light
C. Admin set DarkOnly
D. fresh Storefront -> dark on first paint
E. verify Home/PDP/Cart/Shipping dark
F. Admin set System
G. emulate light system -> light first paint
H. emulate dark system -> dark first paint
I. Admin set UserChoice
J. toggle dark
K. navigate Home→PLP→PDP→Cart→Shipping; stays dark
L. reload; stays dark
M. toggle light; reload; stays light
N. switch palette while dark; palette updates without breaking dark
O. restore canonical default mode and tooba-blue

Capture screenshots for:

Admin ThemeMode control

Home light/dark

PDP light/dark

Cart light/dark

Shipping light/dark

UserChoice toggle

one status/error state in dark

alternate palette in dark

Record:

Store PaletteKey

ThemeMode

effective mode

SSR/root markers/classes

console

hydration warnings

appearance request counts

15. Regression — Commerce/Auth Must Stay Untouched

Re-run focused regressions:

account identity

logout Cart persistence

re-login restore

anonymous merge

Shipping Cart remains Active pre-COMMIT

successful COMMIT converts

injected 409 keeps Cart + Shipping

no duplicate auth/merge/checkout requests

Theme work must not change commerce state behavior.

16. Focused Tests

Required backend/admin:

valid ThemeMode save

invalid ThemeMode rejected

PaletteKey preserved

Store isolation

unauthorized update rejected

cache invalidated only current Store

Frontend:
7. LightOnly applies light
8. DarkOnly applies dark
9. System follows preference
10. UserChoice exposes toggle
11. toggle state persists
12. desktop/mobile share same state
13. no appearance polling
14. no DB write on user toggle
15. palette switch works in dark
16. status colors independent
17. default light remains equivalent

Accessibility:
18. palette × dark contrast validation
19. toggle keyboard/accessibility state

Regression:
20. Home structure guard
21. PDP structure guard
22. storefront tests
23. critical storefront tests
24. R4 cart/auth/checkout scenarios

Also:

relevant lint/typecheck scoped to task

full tsc state reported truthfully

recovery guard

git diff --check

Do NOT repair unrelated pre-existing Admin/grid errors.

17. Anti-Pattern Gate

Scan for:

duplicate ThemeProvider

page-local dark registries

hard-coded per-page dark colors replacing semantic tokens

client-only visible theme flip

polling

user toggle writing Store DB

localStorage used as Store appearance SoT

multiple competing user theme keys

desktop/mobile independent toggle state

brand palette changing status semantics

CSS/HTML/JS from DB

dark mode changing layout

Product Card Skin implementation sneaking in

Landing/Page implementation sneaking in

Any introduced real occurrence => REPAIR.

Evidence:
docs/evidence/TB-P10-T008/antipattern-scan.md

18. Lock Registry

Add non-duplicate locks after LOCK-SF-150 equivalent to:

ThemeMode is a controlled Store appearance setting; arbitrary mode values are forbidden.

LightOnly/DarkOnly/System/UserChoice semantics are canonical and backend-validated.

Dark mode must be driven by semantic surface/text/border tokens, not page-local dark color branches.

UserChoice preference is user-device preference and must not mutate Store ThemeMode or commerce state.

First paint must resolve the effective theme without avoidable light/dark flash.

PaletteKey and ThemeMode compose through one canonical appearance system; status semantic colors remain independent.

Home/PDP dark implementation may change color/surface only; locked geometry/interactions remain unchanged.

Use next canonical LOCK-SF numbers.
Do not renumber existing locks.

19. Evidence

Create:
docs/evidence/TB-P10-T008/

Required:

recovery-start.md

dark-discovery.md

theme-mode-contract.md

admin-theme-mode.md

dark-token-mapping.md

ssr-first-paint.md

user-choice.md

palette-theme-matrix.md

accessibility.md

shopeiva-comparison.md

focused-validation.md

runtime.md

regression.md

antipattern-scan.md

recovery-sot.md

20. Recovery / Pipeline

At completion:

Phase P10

Last Architect-accepted before review: TB-P10-T007

Last Implementation: TB-P10-T008

Current Issued/Repair: none

USER_VISUAL_ACCEPTED=NO

Worker IDLE

branch main

ending HEAD

HEAD==origin/main

task-owned clean

unrelated local user changes preserved

do NOT invent/start TB-P10-T009

Run recovery staleness guard.

Acceptance

PASS only if ALL:

T007 accepted baseline preserved

canonical ThemeMode implemented

Admin can select and persist ThemeMode

invalid mode rejected

cache invalidates immediately

LightOnly works

DarkOnly works

System works

UserChoice works

first paint correct without avoidable FOUC

no hydration mismatch

full Storefront dark coverage on shared surfaces

Home/PDP locked geometry/interactions unchanged

all 7 palettes compose correctly with dark

contrast validation green

status colors independent

user toggle does not write Store DB

no duplicate theme state/provider

no polling

commerce/auth regressions preserved

focused tests green

runtime A–O green

anti-pattern scan CLEAN

Lock registry updated without duplicates

Recovery exact

HEAD==origin/main

Worker IDLE

no TB-P10-T009

USER_VISUAL_ACCEPTED remains NO

Result Contract

Return ONLY canonical BRIDGE-WAKE-V1 Result with:

Summary
Applicable-Locks
Recovery-Start
Dark-Discovery
Theme-Mode-Contract
Admin-Theme-Mode
Dark-Token-Mapping
SSR-First-Paint
User-Choice
Palette-Theme-Matrix
Accessibility
Shopeiva-Comparison
Focused-Validation
Runtime
Regression
AntiPattern-Scan
Lock-Registry
Recovery-SoT
Git
Architectural-Concerns
Visual-Concerns
Blockers

END_TOOBA_TASK