# Tooba — TB-P00-T016 — Media & Image Pipeline Architecture

BEGIN_TOOBA_CURSOR_TASK_V1

Protocol-Version: 1
Task-ID: TB-P00-T016
Phase: P00 — Architecture / Discovery
Type: Architecture Design / Media & Image Pipeline
Repository: https://github.com/mrnikiemami-code/Tooba
Primary-Branch: main
Implementation-Agent: Cursor
Architect: ChatGPT
Execution-Mode: PIPELINE
Depends-On: TB-P00-T015
Architect-Decision-On-Dependency: ACCEPTED

## Objective

Design Tooba's Media / Image architecture deeply enough to support:

- high-quality original assets;
- responsive image delivery;
- dynamic or pre-generated transforms;
- crop/resize/format/quality variants;
- focal point / crop intent;
- modern formats;
- cache/CDN delivery;
- product/category/brand/content/page-composition media;
- tenant/store branding;
- SEO/accessibility metadata;
- safe upload/processing;
- provider-swappable storage and transformation;
- future video/document/media expansion;
- professional UI/UX and Core Web Vitals;
- future service extraction.

This task is architecture documentation only.

Do not implement upload APIs, storage, image processing, CDN, UI, database schemas, or Shopeiva integration.

---

## 1. Repository Recovery

Before edits:

```bash
git rev-parse --show-toplevel
git fetch origin
git branch --show-current
git rev-parse HEAD
git rev-parse origin/main
git status --short --branch
```

Require:

```text
branch = main
HEAD == origin/main
working tree clean
```

Unsafe state =>

```text
RECOVERY_CONFLICT
```

Never force push, rewrite history, destructively reset, silently stash, or overwrite unknown work.

Expected predecessor:

```text
06c3cfd11ac1ff8d988d3fea233c248c1467aff4
```

If synchronized main legitimately advanced, continue from current safe main.

---

## 2. Source-of-Truth Transition

Update:

```text
TB-P00-T015 = ACCEPTED
TB-P00-T016 = ISSUED / AWAITING_ARCHITECT_ACCEPT
```

Roadmap:

```text
P00 Search / Indexing = COMPLETE
P00 Media / Image Pipeline = IN_PROGRESS
```

Do not mark T016 accepted.

---

## 3. Create Architecture Document

Create:

```text
docs/architecture/15-media-image-pipeline.md
```

It must contain all sections below.

---

# A. Core Separation

Hard rules:

```text
Media != Catalog
Media != Content
Media != Page Composition
Media != CDN Vendor
Media != Image Processing Vendor
```

Media owns:

```text
asset lifecycle
asset identity
metadata
transform requests/definitions
derived variants
delivery references
processing state
```

Business modules reference Media assets by opaque IDs/contracts.

---

# B. Media Asset

Define conceptual Media Asset.

Potential facts:

```text
MediaAssetId
Tenant/Deployment scope
Media Type
Original Object reference
Content Type
File size
Width / Height where applicable
Duration where applicable
Checksum
CreatedAt
CreatedBy
Processing status
Safety/validation status
Metadata
```

Do not create schema.

---

# C. Original Asset Principle

Hard principle:

```text
Preserve a high-quality original.
```

Derived variants should not become the only master copy.

Conceptual flow:

```text
Original
→ metadata extraction
→ validation
→ transform request
→ derived variant
→ cache/CDN
```

Need ability to regenerate variants later.

---

# D. Asset Types

Support architecture beyond only raster images.

Candidate types:

```text
Image
Video
Document
Icon
Logo
Future media
```

Initial implementation may focus on images.

Do not overbuild unused media types now.

---

# E. Storage Boundary

Design storage abstraction.

Conceptual:

```text
IMediaObjectStore
```

or equivalent.

Potential backends:

```text
local/object storage
S3-compatible storage
cloud blob storage
future vendor
```

Business domains must not depend on vendor SDKs.

No provider choice now.

---

# F. Transform Boundary

Design image transformation abstraction.

Conceptual:

```text
IImageTransformService
```

or equivalent.

Transforms may include:

```text
resize
crop
fit
cover
contain
quality
format
rotation
background
focal-point-aware crop
```

No processing library/provider choice now.

---

# G. Variant Model

Define conceptual derived variant.

Potential identity dimensions:

```text
AssetId
Width
Height
Crop/Fit Mode
Format
Quality Profile
Focal Point / Crop Intent
Transform Version
```

Derived variants must be deterministic/cacheable.

Avoid storing arbitrary duplicate image records with no lineage.

---

# H. Responsive Images

Architecture must support:

```text
srcset
sizes
multiple widths
responsive breakpoints
device-appropriate delivery
```

Frontend should request appropriate variants, not download one huge original everywhere.

Do not lock exact breakpoints in architecture.

---

# I. Modern Formats

Preserve support for:

```text
AVIF
WebP
JPEG
PNG
SVG where safe
```

Format negotiation may depend on client/browser/CDN capability.

Do not assume one output format.

SVG must receive special safety handling.

---

# J. Focal Point / Crop Intent

Professional content/product imagery often needs controlled crop.

Preserve:

```text
focal point
safe area
preferred crop
subject position
```

This is especially important for:

```text
hero banners
product cards
mobile crops
category tiles
landing-page sections
```

Do not make every frontend component invent crop logic independently.

---

# K. Semantic Media Roles

Business modules may attach semantic role metadata such as:

```text
ProductPrimary
ProductGallery
VariantImage
BrandLogo
CategoryHero
ContentInline
LandingHero
Banner
Thumbnail
OGImage
```

Do not put all role semantics inside Media if they belong to consuming domain.

Define boundary:

```text
Media owns asset
Consumer owns placement/role relationship
```

---

# L. Catalog Integration

Catalog may reference:

```text
primary image
gallery
variant-specific image
swatch
document/manual
```

Catalog does not store binaries or image processing logic.

Media does not own Product semantics.

No direct DB joins.

---

# M. Content Integration

Content may embed/reference Media assets.

Published content must reference approved/available assets.

Draft content may reference draft/private assets.

Public rendering must not leak private/draft media.

---

# N. Page Composition Integration

Reusable sections may request media roles/variants.

Examples:

```text
Hero
Banner
Editorial Card
Category Tile
Brand Grid
Product Carousel
```

Page Composition config references Media IDs and presentation intent.

Do not store arbitrary raw vendor URLs as the core architecture.

---

# O. Theme / Tenant Branding

Single-Store tenants may have:

```text
logo
favicon
brand imagery
default OG image
theme backgrounds
campaign assets
```

These remain tenant-scoped.

One shared publish; no per-tenant asset build required.

No cross-tenant media leakage.

---

# P. Upload Security

Architecture must require:

```text
content-type validation
extension vs actual file validation
size limits
dimension limits
malware scanning hook
decompression bomb protection
safe image decoding
SVG sanitization/restriction
metadata stripping policy
tenant/user authorization
```

Do not implement scanner/vendor.

---

# Q. EXIF / Metadata

Analyze metadata handling.

Possible metadata:

```text
EXIF
orientation
GPS
camera metadata
ICC profile
copyright
```

Privacy-sensitive metadata such as GPS may need stripping.

Preserve orientation/color correctness.

Exact retention policy later.

---

# R. SEO Metadata

Media should support inputs for:

```text
alt text
caption
title where meaningful
copyright/credit
semantic role
width/height
```

But ownership may vary:

```text
asset-level metadata
placement-level alt text
localized alt text
```

Analyze recommendation.

Hard principle:

The same image can require different alt text in different contexts.

Therefore do not force one global AltText on asset as the only model.

---

# S. Accessibility

UI must support:

```text
meaningful alt text
decorative-image semantics
known dimensions
reduced layout shift
keyboard-accessible media controls
captions/transcripts future for video
```

Editors must be guided/validated where alt text is required.

---

# T. Core Web Vitals

Media architecture must protect:

```text
LCP
CLS
bandwidth
render latency
```

Need:

```text
responsive variants
known dimensions/aspect ratio
priority hints only where appropriate
lazy loading below fold
preload for true LCP asset
optimized formats
CDN caching
```

Do not globally lazy-load LCP images.

---

# U. CDN Boundary

Design CDN/delivery abstraction.

Media URLs should be generated through controlled delivery service/configuration.

Do not scatter vendor-specific URLs across business data.

Need future support for:

```text
CDN host migration
signed/private delivery
cache busting/versioning
tenant host integration
```

No vendor choice.

---

# V. Public vs Private Media

Support:

```text
public storefront media
private/admin-only media
draft media
future customer-uploaded private documents
```

Do not expose everything through a public bucket.

Authorization/delivery strategy differs by classification.

---

# W. Cache Strategy

Derived variants are good cache candidates.

Use immutable/versioned URLs where possible.

Need:

```text
long-lived CDN/browser caching
transform versioning
new URL/version after content change
```

Avoid repeatedly reprocessing same transformation.

---

# X. On-Demand vs Pre-Generated Variants

Analyze both:

```text
on-demand transform
pre-generated transform
hybrid
```

Consider:

```text
shared hosting
CPU cost
first-request latency
CDN hit rate
popular standard sizes
rare editorial transforms
```

Recommend a flexible hybrid-ready architecture.

Exact initial implementation:

```text
NEEDS_LATER_P00_DETAIL
```

---

# Y. Processing Pipeline

Conceptual:

```text
Upload
→ validation
→ persist original
→ metadata extraction
→ security checks
→ processing state
→ standard derivatives if needed
→ available
→ publish/delivery
```

Need idempotent processing jobs.

No implementation.

---

# Z. Failure Handling

Need states such as:

```text
Uploaded
Processing
Ready
Failed
Quarantined
Deleted/Archived
```

Do not lock enum.

Rendering must degrade gracefully if derivative processing fails.

---

# AA. Deletion Lifecycle

Analyze:

```text
soft delete
archive
retention
reference check
orphan cleanup
physical deletion
```

Do not immediately delete an asset still referenced by published content/products.

Need safe reference/dependency handling without cross-module table joins.

Use contracts/reference index/events.

---

# AB. Orphan Detection

Preserve operational capability to detect:

```text
unreferenced originals
orphaned variants
failed processing artifacts
stale temp uploads
```

Cleanup must be safe and tenant-scoped.

---

# AC. Duplicate Detection

Checksum/hash may support:

```text
duplicate upload detection
storage deduplication
integrity
```

Do not make deduplication visible as shared cross-tenant business ownership unless security/privacy is safe.

Physical dedupe and logical asset identity are different.

---

# AD. Versioning / Replacement

Replacing an image may either:

```text
create a new MediaAsset
```

or:

```text
create a new asset version
```

Analyze tradeoffs.

Published historical references may need stable version identity.

Avoid mutable binary replacement that unexpectedly changes archived/order/legal evidence.

Exact policy later.

---

# AE. Product Image History

Order may snapshot/display product image reference for historical UI, but must not rely on current Product media if exact historical rendering is required.

Do not force Order to copy binaries.

Preserve versioned/stable Media references.

---

# AF. Search Integration

Search may consume:

```text
primary image delivery reference
image availability
```

as projection.

Search does not own Media asset truth.

Index should not hardcode fragile vendor URLs if stable Media delivery references are available.

---

# AG. SEO / Structured Data Integration

SEO may consume:

```text
primary image
OG image
image dimensions
product images
article images
```

from Media projections/contracts.

Image URLs in structured data must resolve publicly and consistently.

SEO does not own Media.

---

# AH. AI Integration

Future AI may consume approved images/documents where product scope allows.

Do not automatically expose all Media to AI.

Need:

```text
AI eligibility
authorization
published status
tenant
source relationship
```

AI media ingestion is a projection.

---

# AI. Analytics

Analytics may capture:

```text
asset load failures
image performance
hero impressions
media interaction
video plays future
```

Media remains not Analytics truth owner.

---

# AJ. Admin UX

Professional Media/Admin UX may need:

```text
media library
upload progress
processing status
crop/focal-point editor
alt/caption metadata
usage/references
filters
bulk operations
replace/version
preview
responsive preview
tenant isolation
```

Do not reduce Media management to a raw file list.

No UI implementation now.

---

# AK. Product Authoring UX

Critical commercial UX:

When an Admin/Seller edits a Product, media management should be integrated into the Product workspace UX even though Media is a separate backend module.

Hard rule:

```text
Backend/module boundary != UI boundary
```

Do not force users to leave Product editing and navigate to a separate Media CRUD screen for normal product-image workflows.

The UI may orchestrate Catalog + Media contracts into one cohesive workspace.

---

# AL. Content Authoring UX

Likewise, Content/Composition editors need integrated media picking/upload/cropping.

Do not expose backend module seams as workflow friction.

---

# AM. Seller Media

Marketplace seller uploads may require:

```text
authorization
seller-scoped quotas/policies
moderation
product association
quality checks
```

Seller-uploaded images must not automatically override canonical Catalog media without governance.

---

# AN. Media Quality Policy

Preserve future configurable quality rules:

```text
minimum resolution
aspect ratio guidance
background rules
file-size guidance
compression profile
logo transparency rules
marketplace product image quality
```

Do not hardcode policy yet.

---

# AO. External Media Import

Future imports may come from:

```text
supplier feed
seller feed
legacy CMS
remote URL
template/demo assets
```

Need safe ingestion:

```text
download through controlled service
validate
scan
normalize
store internally
record provenance
```

Do not hotlink external untrusted media as long-term truth by default.

---

# AP. Media Provenance

Preserve metadata for:

```text
uploaded by
import source
supplier
seller
original filename
source reference
created time
license/credit where applicable
```

No legal claims.

---

# AQ. Observability

Need telemetry for:

```text
upload failures
processing latency
transform failures
cache hit/miss where observable
CDN errors
broken asset references
variant generation volume
storage growth
tenant usage
```

No sensitive metadata leakage.

---

# AR. Quotas / Abuse

Future tenant/seller/user uploads may need:

```text
file size quota
storage quota
rate limit
transform abuse protection
dimension limits
request-cost controls
```

Do not implement.

---

# AS. Data Ownership Matrix

Create matrix across:

```text
Media
Catalog
Content
Page Composition
SEO
Search
Tenant/Theme
Order
AI
Analytics
```

Against:

```text
original binary
asset metadata
transform variant
focal point
product primary-image role
article placement
landing hero placement
alt text
OG image
search thumbnail
tenant logo
historical media ref
AI media projection
performance analytics
```

Mark:

```text
OWNER
REFERENCE
PLACEMENT_OWNER
CONSUMER
PROJECTION
NOT_OWNER
```

---

# AT. Failure Matrix

Analyze:

```text
Invalid Upload
Malware/Suspicious File
Oversized Image
Decode Failure
Transform Failure
CDN Unavailable
Missing Original
Missing Variant
Draft Asset Requested Publicly
Cross-Tenant Asset Request
Broken Product Reference
Deleted Referenced Asset
SVG Unsafe
Remote Import Failure
```

For each classify:

```text
fail upload?
quarantine?
fallback?
retry?
customer-visible degradation?
operational alert?
```

---

# AU. Testing Strategy — Architecture Level

Future implementation must test:

```text
tenant isolation
authorization
file validation
malformed images
SVG safety
responsive variants
focal-point crop
idempotent transform
cache/version behavior
missing variant fallback
draft/private access
product-media integration
content-media integration
cross-tenant CDN leakage
large image performance
LCP/CLS safeguards
```

No tests now.

---

# AV. Decision Summary

Classify:

```text
RECOMMENDED_FOR_ADR
NEEDS_LATER_P00_DETAIL
DEFERRED
```

At minimum recommend:

1. Media is separate from Catalog/Content/Page Composition.
2. Preserve high-quality original assets.
3. Derived variants are deterministic and regenerable.
4. Storage provider is behind internal abstraction.
5. Transform provider/library is behind internal abstraction.
6. Responsive image variants are first-class.
7. Focal point/crop intent is preserved.
8. Consumer owns semantic placement role; Media owns asset lifecycle.
9. Placement-level localized alt text is supported; one global asset AltText is insufficient.
10. Public/private/draft media are distinct security classes.
11. CDN/vendor URLs do not leak into business-domain ownership.
12. Immutable/versioned delivery URLs are preferred for cacheability.
13. On-demand/pre-generated hybrid architecture remains possible.
14. Upload security/validation/quarantine hooks are mandatory.
15. Media architecture protects LCP/CLS and visual quality.
16. Product/Content authoring UX integrates Media without exposing backend module seams.
17. Tenant/seller media isolation is mandatory.
18. Search/SEO/AI consume Media projections/contracts only.
19. Safe deletion/reference/orphan cleanup is required.
20. Media operations require observability and reconciliation.

Do not create final ADR yet.

---

## 4. Consistency Updates

If required, minimally align:

```text
docs/architecture/03-data-ownership-and-module-contracts.md
docs/architecture/07-catalog-product-offer.md
docs/architecture/12-content-page-composition.md
docs/architecture/13-seo-architecture.md
docs/architecture/14-search-indexing.md
```

No unrelated redesign.

---

## 5. Update SoT

Update:

```text
docs/PROJECT-STATE.md
docs/ROADMAP.md
docs/ai/TOOBA-RECOVERY-CONTEXT.md
```

Expected:

```text
Last Architect Accepted Task: TB-P00-T015
Current Issued Task: TB-P00-T016
Task State: AWAITING_ARCHITECT_ACCEPT
Current Phase: P00 — Architecture / Discovery
Media / Image Pipeline: IN_PROGRESS
```

No next task authorized.

---

## 6. Save Complete Envelope Verbatim

Save exact task to:

```text
docs/ai/tasks/TB-P00-T016.task.md
```

Verify exact markers:

```text
BEGIN_TOOBA_CURSOR_TASK_V1
END_TOOBA_CURSOR_TASK_V1
```

Do not save a shortened pointer.

---

## 7. Explicit Out of Scope

Do NOT:

- implement upload APIs;
- create DB schemas/migrations;
- install image libraries/services;
- choose storage vendor;
- choose CDN;
- implement transforms;
- implement Media UI;
- integrate Shopeiva;
- import template assets;
- implement AI/media indexing;
- create final ADR.

---

## 8. Validation

Run:

```bash
git diff --check
git status --short --branch
```

Manual checks:

- T015 ACCEPTED;
- T016 awaiting Architect acceptance;
- Media != Catalog/Content/Composition;
- high-quality original preserved;
- responsive variants represented;
- provider abstractions represented;
- focal point/crop represented;
- placement-level alt metadata represented;
- tenant/private/draft isolation represented;
- Core Web Vitals represented;
- Product/Content integrated UX preserved;
- Search/SEO/AI are consumers only;
- no application code;
- no provider/vendor selected;
- no template feature promoted.

---

## 9. Git Commit & Push

Commit:

```text
docs design Tooba media pipeline [TB-P00-T016]
```

Then:

```bash
git push origin main
git fetch origin
git rev-parse HEAD
git rev-parse origin/main
git status --short --branch
```

Require:

```text
HEAD == origin/main
working tree clean
```

No force push.

---

## 10. Result Contract

Return exactly:

```text
BEGIN_TOOBA_CURSOR_RESULT_V1

Protocol-Version: 1
Task-ID: TB-P00-T016
Phase: P00 — Architecture / Discovery
Status: PASS | BLOCKED | RECOVERY_CONFLICT

Summary:
...

Repository-Recovery:
- Repo-Root:
- Branch:
- Starting-HEAD:
- Starting-Origin-Main:
- Starting-Status:

Changes:
- ...

Media-Boundary:
- ...

Original-Variant-Model:
- ...

Storage-Transform-CDN:
- ...

Security-Privacy:
- ...

Catalog-Content-Composition:
- ...

SEO-Search-AI:
- ...

Tenant-Seller:
- ...

CWV-UIUX:
- ...

Decision-Summary:
- RECOMMENDED_FOR_ADR:
- NEEDS_LATER_P00_DETAIL:
- DEFERRED:

Validation:
- git diff --check:
- other checks:
- manual consistency review:

Git:
- Commit:
- Push:
- Final-HEAD:
- Final-Origin-Main:
- Final-Status:
- Head-Matches-Origin: YES | NO

Source-of-Truth:
- Last-Architect-Accepted-Task:
- Current-Issued-Task:
- Current-Phase:
- Task-State:
- Recovery-Ready: YES | NO

Architectural-Concerns:
- ...

Blockers:
- ...

END_TOOBA_CURSOR_RESULT_V1
```

---

## 11. Pipeline Continuity — MANDATORY

After RESULT:

```text
WAITING
```

Remain inside the Tooba Architect-controlled pipeline.

Do not leave PIPELINE mode.

Do not invent, infer, prepare, or execute unauthorized work.

Do not advance to `TB-P00-T017` or any other task without a new valid Architect envelope containing:

```text
BEGIN_TOOBA_CURSOR_TASK_V1
...
END_TOOBA_CURSOR_TASK_V1
```

Cursor PASS is not Architect ACCEPT.

After sending RESULT, stop execution and remain WAITING for Architect review.

---

## 12. UI / UX Protection — MANDATORY PROJECT RULE

Tooba is a commercial product and weak UI/UX is considered product failure.

All architectural decisions must preserve future ability to build:

```text
professional storefront
professional Admin panel
professional Seller panel
polished Customer dashboard
intentional mobile UX
RTL/LTR support
accessibility
responsive design
high-quality loading/empty/error states
strong visual hierarchy
commercial-grade interaction design
```

For Media specifically, future UI must support professional workflows such as:

```text
drag/drop upload
upload progress
image ordering
primary-image selection
variant imagery
crop/focal point
responsive previews
media picker
alt/caption editing
processing/error states
bulk operations
mobile authoring where practical
```

Hard rule:

```text
Backend/module boundary != UI boundary
```

Product and Content editors must be able to manage Media inside cohesive workflows without being forced into disconnected CRUD screens.

Future UI implementation tasks must require visual evidence and Architect visual acceptance.

Build/test PASS alone is not sufficient acceptance for UI work.

END_TOOBA_CURSOR_TASK_V1
