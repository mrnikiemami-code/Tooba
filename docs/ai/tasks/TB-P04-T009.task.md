Tooba — TB-P04-T009 — Live Checkout → Order Integration

BEGIN_TOOBA_CURSOR_TASK_V1

Protocol-Version: 1
Task-ID: TB-P04-T009
Phase: P04 — Experience Foundation
Type: Presentation-First Live Commerce Slice
Repository: https://github.com/mrnikiemami-code/Tooba
Branch: main
Execution-Mode: PIPELINE

Architect Decision

TB-P04-T008 is ACCEPTED.

Cart is now live and commercially safe enough to continue.

Accepted non-blocking hardening concern:

replace-hold currently uses release → reserve,
leaving a short competition window.

Do NOT redesign this in T009 unless Checkout exposes a real correctness failure.

Locked delivery rule remains:

PRESERVE SHOPEIVA
→ MINIMUM UI CHANGE
→ CONNECT TOOBA BACKEND
→ SELL QUICKLY
→ POLISH/HARDEN LATER

This task connects the next real purchase step:

Cart
→ Checkout
→ Order

Do NOT start a new visual design language.

Repository Recovery

Run first:

git rev-parse --show-toplevel
git fetch origin
git branch --show-current
git rev-parse HEAD
git rev-parse origin/main
git status --short --branch

Expected predecessor:

250ecf760c96740c2f1e165ef61d7a2c61a2532e

Require:

branch = main
HEAD == origin/main
safe/known working tree

No force push.
No destructive reset.
No silent stash.
No history rewrite.

Objective

Make the live customer purchase flow continue from the real Cart into real Checkout and real Order creation.

Required end-to-end flow:

Live Cart
→ Checkout page
→ buyer/shipping information
→ authoritative pricing/promotion/tax evaluation
→ order review
→ submit Checkout
→ real Order / CheckoutGroup created
→ Order confirmation page

The result must use existing Tooba Checkout/Order architecture.

No fake checkout success.

No frontend-only order object.

No demo JSON as business truth.

Shopeiva Preservation — CRITICAL

Use the purchased Shopeiva checkout/shipping/payment/order visual patterns.

Do NOT redesign checkout from scratch.

Preserve as much as practical:

checkout shell
step/progress visual language
shipping/address section
cart/order summary
price summary
payment-method visual area
submit order CTA
validation/error presentation
responsive behavior
order confirmation visual language

Only change what is required by Tooba backend semantics.

If Shopeiva has separate routes such as:

/shipping
/payment

reuse/adapt those patterns rather than inventing replacements.

Initial Sellable Scope

Keep this task intentionally narrow.

Initial accepted presentation scope:

Persian
RTL
current configured market/currency
guest/customer checkout path already supported by current identity/cart architecture

Do NOT block on:

full multilingual
LTR
advanced currency switcher
wallet
gift cards
referral
loyalty
B2B checkout
corporate credit

Those are later phases.

Checkout Business Truth

Use the existing Checkout/Order module implemented in P03.

Preserve the accepted invariant:

one CartId → at most one CheckoutGroup

Do not create a second checkout model.

Do not bypass the durable Cart → Checkout uniqueness/reconciliation logic.

Frontend must call proper Host/public application endpoints.

No frontend direct DB.

Checkout Composition

The checkout review must use authoritative backend composition.

Commercial ordering remains:

Pricing
→ Promotion
→ Tax
→ Order Snapshot

Do not calculate final checkout truth in React.

The frontend may render values returned by Checkout.

It must not derive authoritative totals independently.

Cart → Checkout

The Checkout action must start from the current real Cart.

Required:

CartId
commercial line identity
OfferId
quantity
authoritative current commercial state

Backend must revalidate before final order creation as existing architecture requires.

Do not trust stale frontend Cart display as final truth.

Buyer / Customer Context

Use existing Identity / Party foundations.

Do NOT build a new Identity system.

For current sellable flow, use the narrowest already-supported buyer context.

Document exactly whether checkout is:

guest
authenticated
development-seeded customer

and how:

BuyerPartyId
BookedBy/acting identity equivalent if relevant

is represented in current commerce model.

Do not implement B2B Party workflows here.

Shipping / Address

Use Shopeiva shipping/address layout.

Connect to the narrowest existing Tooba capability.

If a durable Address module is not yet implemented:

do NOT build a large Address domain in this task;

use a bounded checkout shipping/contact snapshot input owned by Checkout/Order if architecture already supports it;

document the later Customer Address persistence seam.

Minimum sellable fields may include:

recipient name
mobile/contact
province/city
postal address
postal code if current market needs it
shipping method seam

Do not add false logistics promises.

If Fulfillment/Shipping pricing is not yet implemented, use only backend-supported shipping behavior and state the limitation clearly.

Shipping Method

If current backend has no real Shipping/Fulfillment engine yet:

do not invent a complex shipping calculator

Use one bounded development/default shipping option only if it can be represented honestly in the Order snapshot.

Document:

shipping method is initial/default
advanced fulfillment deferred

No fake multi-carrier rates.

Pricing / Promotion / Tax

Checkout must surface backend-authoritative values.

At minimum:

items subtotal
promotion/discount if real
tax
shipping if real
final payable amount
currency

If no promotion:

no fake discount

Tax must come from Tooba Tax calculation, not Shopeiva static display logic.

Keep:

base price = Tax Exclusive
tax calculated separately
Checkout Revalidation

Before Order creation, revalidate:

Offer still active
Price still valid
Inventory still available
Cart not expired
commercial state not stale

If revalidation fails:

do not create Order
return structured error
show customer-safe Persian message
provide refresh/review path

No internal exception text as primary customer message.

Inventory / Reservation Conversion

Respect existing Cart/Checkout/Inventory architecture.

Determine and document what happens to Cart-held inventory when Checkout is created.

Required correctness:

Cart hold is not silently lost
Order is not created without valid commercial reservation/state
failed Checkout does not permanently strand inventory
duplicate Checkout request does not duplicate Order/CheckoutGroup

Do not invent cross-module SQL transaction.

Use accepted application/event/reconciliation seams.

Order Creation

Use existing Order module.

Order must contain durable snapshots required by existing architecture.

Do not reference live mutable Product/Price as sole historical truth.

Preserve:

commercial line snapshot
pricing snapshot
promotion snapshot as applicable
tax snapshot
buyer/shipping snapshot

according to current P03 model.

Idempotency

Checkout submit must be safe against:

double click
browser retry
network retry
same Cart submitted twice

Preserve existing:

one CartId → at most one CheckoutGroup

Add/verify focused HTTP/application tests.

UI must not create duplicate orders from repeated submit.

Order Confirmation Page

After successful Checkout/Order creation:

navigate to a real confirmation route

Use Shopeiva order/invoice confirmation visual language where available.

Show at minimum:

order/reference number
status
items summary
seller(s)
amount
tax
shipping snapshot
buyer/recipient
next payment state

Do not expose raw UUID as the only human-facing order reference if an appropriate display reference exists or can be narrowly composed.

Do not fake payment success.

Payment Boundary

This task stops at:

Order created
→ payment pending / payment next step

Do NOT integrate a real PSP unless it is already trivially supported and explicitly within bounded scope.

Do NOT mark the Order paid.

The next task will connect the payment flow.

Payment UI may preserve Shopeiva visual shell, but must clearly indicate real current state.

Customer-Safe Errors

Map checkout failures to clear Persian messages.

Examples:

موجودی یکی از کالاها تغییر کرده است.
قیمت یکی از کالاها تغییر کرده؛ لطفاً سفارش را دوباره بررسی کنید.
سبد خرید منقضی شده است.
اطلاعات ارسال کامل نیست.

Technical codes remain available for diagnostics but not primary UX.

SEO / Privacy

Checkout and Order confirmation are private/non-indexable.

Require:

noindex

Do not place personal checkout data into indexable URLs.

Do not leak guest Cart secrets/order secrets into logs/screenshots.

Visual Evidence — LIVE TOOBA ONLY

Store:

docs/evidence/TB-P04-T009/

Capture:

01-checkout-desktop-rtl.png
02-checkout-mobile-390x844.png
03-checkout-order-summary.png
04-checkout-tax-total.png
05-checkout-validation-error.png
06-order-confirmation-desktop.png
07-order-confirmation-mobile.png
08-duplicate-submit-proof.md

All screenshots must come from live Tooba.

No fixture/demo order truth.

Live Flow Evidence

Create:

docs/evidence/TB-P04-T009/live-checkout-order-flow.md

Record:

CartId
buyer context
shipping input
commercial revalidation
Pricing result
Promotion result
Tax result
CheckoutGroupId
OrderId/reference
final amount
payment state
duplicate-submit behavior

Redact secrets/personal data.

Architecture Evidence

Create:

docs/evidence/TB-P04-T009/checkout-order-architecture-map.md

Show:

Storefront Checkout UI
→ Host/Public Checkout API
→ Checkout application
→ Pricing
→ Promotion
→ Tax
→ Inventory validation/reservation handling
→ Order

Prove:

no cross-module SQL join
no Product.Price
no Product.Stock
no frontend authoritative total
one CartId → at most one CheckoutGroup
Tests — REQUIRED

Add focused tests for:

successful live Cart → Checkout
tax included in final backend result
no fake promotion
inventory/offer revalidation
expired Cart rejection
price/commercial stale rejection if supported
duplicate submit returns/reuses existing CheckoutGroup rather than duplicating
Order snapshot correctness
multi-seller Cart preserved into Order

Do not remove existing concurrency tests.

Full Validation

Backend:

dotnet restore src/backend/Tooba.slnx
dotnet build src/backend/Tooba.slnx
dotnet test src/backend/Tooba.slnx

NO filters.

Require:

warnings = 0
errors = 0
failed = 0
skipped = 0

Frontend:

cd src/frontend
npm ci
npm run typecheck
npm run lint
npm run test:grid
npm run test:workspace
npm run test:product-workspace

Run:

existing Storefront tests
existing Cart tests
new Checkout/Order focused tests

Then:

npm run build
git diff --check
git status --short --branch
Acceptance Conditions

PASS requires all:

real Cart enters Checkout
Checkout uses backend authoritative totals
real Tax result shown
real Order created
one CartId cannot produce duplicate CheckoutGroups
multi-seller line identity preserved
Order snapshot durable
Order is NOT falsely marked paid
Checkout desktop RTL works
Checkout mobile RTL works
Order confirmation works
customer-safe real error state works
Shopeiva visual language preserved
no Product.Price
no Product.Stock
no cross-module SQL join
full validation green
Architecture Recheck

Must remain true:

Cart != Checkout
Checkout != Order
Order snapshot != live Product truth
Product != Offer
Offer != Price
Pricing != Tax
Backend/module boundary != UI boundary
Deferred Non-Blocking Concern

Carry forward:

Cart replace-hold release→reserve competition window

Do not lose it from recovery context.

It may be hardened before high-concurrency production if current Checkout work does not naturally eliminate it.

SoT

Update:

docs/PROJECT-STATE.md
docs/ROADMAP.md
docs/ai/TOOBA-RECOVERY-CONTEXT.md
docs/prompts/START-HERE-IF-CHATGPT-IS-LOST.md

Record:

TB-P04-T008 = ACCEPTED
TB-P04-T009 = AWAITING_ARCHITECT_ACCEPT
P04 = IN_PROGRESS

Keep:

Preserve Shopeiva
Minimal UI change
Connect Tooba backend
Sell fast
P06 core integration deadline
Save Envelope VERBATIM

Save exactly:

docs/ai/tasks/TB-P04-T009.task.md
Commit / Push

After work:

git diff --check
git status --short --branch
git add ...
git commit -m "feat connect live checkout order flow [TB-P04-T009]"
git push origin main
git fetch origin
git rev-parse HEAD
git rev-parse origin/main
git status --short --branch

Require:

HEAD == origin/main
Result Contract

Return:

BEGIN_TOOBA_CURSOR_RESULT_V1

Protocol-Version: 1
Task-ID: TB-P04-T009
Phase: P04 — Experience Foundation
Status: PASS | BLOCKED | RECOVERY_CONFLICT

Summary:
...

Repository-Recovery:
- ...

Shopeiva-Reuse:
- checkout shell:
- shipping/address:
- summary:
- confirmation:
- deviations:

Buyer-Context:
- guest/authenticated:
- Party/identity mapping:
- persistence:

Shipping:
- address model:
- shipping method:
- fulfillment limitation:

Live-Checkout:
- CartId:
- CheckoutGroupId:
- revalidation:
- duplicate submit:
- expired/stale handling:

Pricing:
- subtotal:
- promotion:
- tax:
- shipping:
- final amount:
- currency:
- frontend authoritative calculation:

Inventory:
- hold/reservation behavior:
- failed checkout behavior:

Order:
- OrderId:
- display reference:
- snapshot:
- multi-seller:
- status:

Payment-Boundary:
- state:
- paid falsely:
- next step:

Customer-Errors:
- ...

Visual-Evidence:
- 01:
- 02:
- 03:
- 04:
- 05:
- 06:
- 07:
- 08:
- Live flow:
- Architecture map:

Tests:
- successful checkout:
- tax:
- revalidation:
- expired cart:
- duplicate submit:
- order snapshot:
- multi-seller:

Validation:
- backend restore:
- backend build:
- warnings:
- errors:
- backend passed:
- failed:
- skipped:
- npm ci:
- typecheck:
- lint:
- grid:
- workspace:
- product-workspace:
- storefront:
- cart:
- checkout/order:
- frontend build:
- git diff --check:

Architecture:
- Product.Price:
- Product.Stock:
- cross-module SQL join:
- frontend final-total truth:
- one CartId → at most one CheckoutGroup:

Git:
- commit:
- push:
- final HEAD:
- origin/main:
- synchronized:
- final status:

Source-of-Truth:
- TB-P04-T008: ACCEPTED
- TB-P04-T009: AWAITING_ARCHITECT_ACCEPT
- P04: IN_PROGRESS
- Recovery-ready:

Architectural-Concerns:
- ...

Visual-Concerns:
- ...

Blockers:
- ...

END_TOOBA_CURSOR_RESULT_V1

After RESULT, surface live Checkout/Order screenshots in this SAME Architect session.

CRITICAL — RESULT DOES NOT END THIS AGENT SESSION

After RESULT:

PIPELINE_STATE: WAITING_FOR_ARCHITECT_IN_SAME_SESSION

Then:

DO NOT CLOSE OR LEAVE THIS SESSION.
WAIT HERE FOR THE NEXT AUTHORIZED ENVELOPE.

Do not finish.
Do not exit.
Do not leave PIPELINE.
Do not self-advance.
Do not invent TB-P04-T010.

RESULT = CONTROL RETURNED TO ARCHITECT.
RESULT != WORKFLOW FINISHED.

Cursor PASS is not Architect ACCEPT.

Leaving or ending the pipeline after RESULT is a protocol violation.

END_TOOBA_CURSOR_TASK_V1
