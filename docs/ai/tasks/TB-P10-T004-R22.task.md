PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_REPAIR_TASK

Task-ID:
TB-P10-T004-R22

Parent-Task:
TB-P10-T004-R21-R1

Channel:
tooba-main

WorkerId:
tooba-worker-01

AgentType:
cursor

Status:
REPAIR

USER_VISUAL_ACCEPTED=YES

Title:
Checkout Identity Policy — Existing Login Gate + Development OTP + Anonymous Cart Merge

Architect Review

TB-P10-T004-R21-R1 is ACCEPTED.

Accepted:

customer cancel is canonical backend cancellation

active reservation releases on cancel

hide-pending is presentation-only and backend-owned

hide does not mutate Order/Payment/Supply/Reservation state

hide while active hold is denied

checkout commit is atomic

Inventory reservation + Order aggregate + Cycle #1 + Cart Converted commit together

frontend does not clear Cart before backend success

payment initiation is post-commit

multi-line/multi-seller/concurrency proofs passed

USER_VISUAL_ACCEPTED=YES

HEAD==origin/main

Worker IDLE

New locks accepted:

LOCK-SF-103 atomic checkout

LOCK-SF-104 hide is presentation-only

LOCK-SF-105 canonical customer cancel

This task implements the NEXT planned scope only:
Checkout identity policy and authentication gate using the EXISTING login UI.

Do NOT implement MaxOpenUnpaidOrders yet.
Do NOT implement churn/abuse quotas yet.
Do NOT redesign Login.
Do NOT create TB-P10-T005.

Product Decision

Default checkout identity policy for Tooba:

AuthenticatedOnly

Meaning:

browsing is anonymous-capable

AddToCart is anonymous-capable

Cart page is anonymous-capable

entering Shipping/Checkout/Payment requires authentication

existing Login page MUST be reused

Future optional policy may allow Guest Checkout, but that is NOT the default and must not weaken AuthenticatedOnly.

Development Login Fixture

For Development/Test only:

Mobile: 09111111111

OTP: 123456

Use only within existing development OTP/auth mechanism.

Do NOT hardcode production bypass.
Do NOT expose OTP in production UI/logs.

Recovery State

Phase: P10
Last Architect-accepted: TB-P10-T004-R21-R1
Last Implementation: TB-P10-T004-R21-R1
Current Repair: TB-P10-T004-R22
Worker: tooba-worker-01
Expected branch: main
Expected previous HEAD: 56a70cb152b475892408430eff9d9ae2d3b10ee8
USER_VISUAL_ACCEPTED=YES

Applicable Locks

Read:

docs/architecture/TOOBA-LOCKS.md

docs/architecture/STOREFRONT-CHECKOUT-PLAN.md

Preserve LOCK-SF-001…105.

Especially:

Cart != Reservation

atomic checkout commit

committed Order proof independent from Active Cart

no frontend authority over lifecycle

same Order across retry

Product != Offer

backend authoritative permissions/capabilities

FA RTL / EN LTR

Shopeiva UI contract

1. Recovery Preflight

Record:

Phase

Last Architect-accepted

Last Implementation

Current Repair

Worker

branch

HEAD

origin/main

HEAD==origin/main

tracked dirty state

task-owned untracked state

Unexpected tracked divergence => BLOCK.

Evidence:
docs/evidence/TB-P10-T004/r22-recovery-start.md

2. Mandatory Discovery

Inspect current:

Login page

login/OTP API

session creation

customer actor/session identity

anonymous Cart ownership

authenticated Cart ownership

Shipping route guard

Checkout route guard

Payment route guard

post-login redirect support

current Cart merge behavior, if any

current guest checkout path

current Store Settings architecture

Create:
docs/evidence/TB-P10-T004/r22-auth-discovery.md

Required table:
Route/Action | Anonymous allowed? | Auth required? | Existing behavior | Required behavior

3. Checkout Identity Policy

Add canonical Store-level setting:

CheckoutIdentityPolicy

Supported values:

AuthenticatedOnly

GuestAllowed

Default:
AuthenticatedOnly

For this task:

fully implement AuthenticatedOnly

preserve existing guest-capable infrastructure behind GuestAllowed only if safe

do not make GuestAllowed default

Backend returns effective policy.
Frontend must not invent it.

Admin Settings surface:
هویت مشتری در فرایند خرید

Options:

ورود الزامی

خرید مهمان مجاز

Help text:
در حالت ورود الزامی، کاربر می‌تواند سبد خرید را بدون ورود تکمیل کند اما برای ادامه فرایند خرید باید وارد حساب خود شود.

If GuestAllowed has material security/abuse implications, show a non-blocking warning.

4. Existing Login Page Only

Must reuse current Login UI and auth flow.

Forbidden:

new login modal

new OTP page

duplicate auth component

alternate login API

special checkout-only credential form

When anonymous customer attempts:

Shipping

Checkout

Payment entry

final checkout commit

redirect to existing Login page with safe return target.

After successful login:
return to the original intended checkout route.

5. Safe Return URL

Use a validated internal return target.

Requirements:

internal path only

no open redirect

preserve locale

preserve intended route

preserve checkout context without exposing secrets in querystring

Example:
/fa/login?returnTo=/fa/shipping

But use existing project conventions if available.

Wrong/external return targets must fall back safely.

6. Anonymous Cart Merge on Login

Critical behavior:

User may have anonymous Cart lines before login.

After successful authentication:

anonymous Cart must not disappear

customer Active Cart must not be blindly overwritten

canonical merge occurs backend-side

Rules:

same Offer/Variant line merges quantity using canonical cart semantics

distinct lines remain distinct

exact decimal quantities preserved

inventory is VALIDATED, not RESERVED

current price is authoritative

no hard Reservation is created during merge

no Order is created

no Payment attempt is created

If authenticated user already had an Active Cart:
merge anonymous into authenticated Cart.

If there is no authenticated Cart:
adopt/associate/merge using the cleanest canonical existing path.

7. Cart Merge Conflict Rules

For each merged line:

quantity must respect current business validation

unavailable Offer/Variant must not silently disappear

customer gets a business-safe message/status

changed price is accepted only via normal authoritative Cart refresh

duplicate entries must not be created for same canonical line identity

Document exact merge semantics.

Evidence:
docs/evidence/TB-P10-T004/r22-cart-merge.md

8. Existing Pending Orders

Logging in must NOT:

merge pending Orders into Cart

duplicate pending Orders

convert pending Orders back to Cart

reset Reservation Cycle

reset payment timer

Pending Orders remain independent and appear through existing pending-payment projection for authenticated owner.

If pre-login anonymous committed Orders are possible under old historical flows:

do not silently claim them

document current ownership behavior

only migrate ownership if an existing secure proof-to-account binding path already exists

No speculative ownership transfer.

9. Route Guards

For AuthenticatedOnly:

Anonymous:

Product/PLP/Home: allowed

Cart: allowed

AddToCart: allowed

Shipping: login gate

Checkout: login gate

Payment: login gate

submit checkout: backend rejects if unauthenticated even if frontend guard bypassed

Backend enforcement is mandatory.

Do not rely only on middleware/frontend.

10. Development OTP Fixture

In Development/Test environment only:

Mobile:
09111111111

OTP:
123456

Requirements:

existing Login page accepts this via existing auth flow

no production bypass

environment-gated

test fixture is documented

OTP is not logged as secret in production paths

If existing development OTP mechanism already supports fixed OTP, configure it there rather than adding special-case controller logic.

11. Session Continuity

After login:

authenticated session established

locale preserved

merged Cart visible

user returns to intended Shipping/Checkout route

no extra manual refresh required

header/customer identity updates correctly

pending Orders use authenticated ownership

12. Logout Behavior

On logout:

authenticated Orders remain account-owned

active authenticated Cart must not be leaked to anonymous next user

new anonymous Cart/session may be created as current architecture requires

no cross-user Cart exposure

Do not redesign logout.

13. Admin Settings UX

Add to existing Store Settings, preferably near checkout/order settings.

Show:
هویت مشتری در فرایند خرید

Current effective value:

ورود الزامی

خرید مهمان مجاز

Default:
ورود الزامی

Save/Cancel must use existing pattern.

Backend authorization mandatory.

No separate page if existing settings section is appropriate.

14. Security

Required:

no open redirect

no account/cart fixation

no cross-customer cart merge

no ability to submit checkout anonymously when AuthenticatedOnly

no querystring leakage of guest secret/payment proof

no reuse of another user's anonymous cart token

merge uses server-validated ownership

session rotation if current auth architecture requires it

15. Focused Tests

Policy:

default = AuthenticatedOnly

Store can switch to GuestAllowed

effective policy returned by backend

Route gate:
4. anonymous Cart allowed
5. anonymous Shipping redirected/gated
6. anonymous Checkout gated
7. anonymous Payment gated
8. backend submit rejects anonymous under AuthenticatedOnly
9. authenticated user passes

Login:
10. existing Login page used
11. valid returnTo returns to intended internal route
12. external returnTo rejected/falls back
13. locale preserved

Dev OTP:
14. 09111111111 + 123456 succeeds in Development
15. production path has no fixed OTP bypass

Merge:
16. anonymous-only cart -> authenticated cart
17. authenticated existing cart + anonymous cart merge
18. same line quantities merge correctly
19. decimal 1.25 exact
20. unavailable line remains visible/actionable, not silently lost
21. no Reservation created
22. no Order created
23. no Payment created

Isolation:
24. wrong customer cannot merge another cart
25. logout does not leak authenticated cart
26. pending Orders unchanged by login/merge

Regression:
27. R21 atomic checkout unchanged
28. R16 pending-payment UX unchanged
29. R20 countdown unchanged
30. recovery guard
31. git diff --check

Do NOT run unrelated full suites.

16. Real Browser Runtime — REQUIRED

Use Host :5088 and FE :3000.

A. anonymous user adds 2+ items to Cart
B. opens Cart successfully
C. clicks continue to Shipping/Checkout
D. redirected to EXISTING Login page
E. login with:

mobile 09111111111

OTP 123456
F. returns to intended checkout route
G. Cart lines still present after merge
H. authenticated Cart already has line -> merge proven
I. no Reservation exists before final checkout commit
J. pending-payment old Order remains independent
K. logout isolation smoke
L. Admin Settings toggle AuthenticatedOnly / GuestAllowed
M. switch back to AuthenticatedOnly

17. Anti-Pattern Gate

Scan for:

duplicate Login UI

frontend-only auth gate

open redirect

guest secret in URL

localStorage as cart-merge source of truth

merge creating Reservation

merge creating Order

cart overwrite instead of merge

first-line-only merge

hardcoded production OTP bypass

customer identity inferred from Cart token

broad auth rewrite unrelated to checkout

polling/retry hacks

Any occurrence => REPAIR.

Evidence:
docs/evidence/TB-P10-T004/r22-antipattern-scan.md

18. Lock Registry

Add non-duplicate locks equivalent to:

Default checkout identity policy is AuthenticatedOnly; Cart may remain anonymous but Shipping/Checkout/Payment require authenticated customer.

Existing Login experience is canonical; checkout must reuse it.

Anonymous Cart merge on login validates/merges Cart only and must not create Reservation, Order, or Payment.

Checkout auth policy is enforced by backend, not frontend alone.

Development fixed OTP is environment-gated and must not create a production bypass.

19. Planned NEXT Task — DO NOT IMPLEMENT NOW

After R22 PASS:

TB-P10-T004-R23
Max Open Unpaid Orders + Reservation Churn Anti-Abuse + Admin Settings

Planned policy concepts:

MaxOpenUnpaidOrdersPerCustomer

ReservationCommitWindowMinutes

MaxCheckoutCommitsPerCustomerInWindow

cancellation frees open-order slot but DOES NOT refund churn quota

hidden pending card does NOT free open-order slot

CustomerId + Store scoped

concurrency-safe

pre-reservation/pre-Order enforcement

Admin Settings

optional Offer-level flash-sale commit quota only if justified by discovery

Do NOT implement R23 scope now.

20. Evidence

Add:

r22-recovery-start.md

r22-auth-discovery.md

r22-identity-policy.md

r22-login-gate.md

r22-cart-merge.md

r22-development-otp.md

r22-security.md

r22-admin-settings.md

r22-focused-validation.md

r22-runtime.md

r22-antipattern-scan.md

r22-recovery-sot.md

under:
docs/evidence/TB-P10-T004/

21. Recovery / Pipeline

At completion:

Phase P10

Last Architect-accepted before review: TB-P10-T004-R21-R1

Last Implementation: TB-P10-T004-R22

Current Issued/Repair: none

USER_VISUAL_ACCEPTED=YES

Worker: tooba-worker-01 — IDLE

branch main

ending HEAD recorded

HEAD==origin/main

task-owned worktree clean

no TB-P10-T005

Run recovery guard.

Acceptance

PASS only if ALL:

AuthenticatedOnly is default

existing Login page is reused

Cart remains anonymous-capable

Shipping/Checkout/Payment require auth under AuthenticatedOnly

backend enforces auth policy

Development 09111111111 / 123456 works through existing login flow

safe internal returnTo

anonymous Cart survives login and merges correctly

existing authenticated Cart merges correctly

decimal quantities preserved

unavailable lines not silently lost

merge creates no Reservation/Order/Payment

pending Orders unaffected

logout isolation correct

Admin Setting works

security checks pass

runtime A–M PASS

anti-pattern scan CLEAN

focused tests green

recovery exact

HEAD==origin/main

Worker IDLE

no TB-P10-T005

Result Contract

Return ONLY canonical BRIDGE-WAKE-V1 Result with:

Summary
Applicable-Locks
Recovery-Start
Auth-Discovery
Identity-Policy
Login-Gate
Cart-Merge
Development-OTP
Security
Admin-Settings
Focused-Validation
Runtime
AntiPattern-Scan
Lock-Registry
Recovery-SoT
Git
Architectural-Concerns
Visual-Concerns
Blockers

END_TOOBA_REPAIR_TASK