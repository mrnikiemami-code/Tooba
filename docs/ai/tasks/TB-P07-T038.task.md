PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_TASK

Task-ID:
TB-P07-T038

Parent-Task:
TB-P07-T037

Channel:
tooba-main

Title:
Catalog Admin Grid + Category Tree UX Repair — Leaf-Only Primary Category, Additional Chips +N, Zero Path Overfetch, Route-Synced Tree Selection

Role:
You are the Tooba implementation Worker.
Implement ONLY the Catalog Admin grid/category-tree UX and query-shape repair described here.
Preserve all accepted T036/T037 category semantics.
Do NOT redesign unrelated Catalog areas.
USER_VISUAL_ACCEPTED remains NO.

ARCHITECT CONTEXT

Human review clarified the desired Admin UX and performance behavior:

Full Category path in Product/Admin grids is too verbose and causes unnecessary ancestor/path composition work.

The grid should separate Primary Category and display memberships (نمایش در دسته‌های دیگر).

Primary Category should show only the leaf Category name.

Display memberships should be individual chips, not comma-separated text.

Render at most 3 chips; if more exist render +N.

Opening +N must NOT trigger another query in the normal expected case; names are already in the row payload.

Category Tree must keep the currently loaded Category selected through click, route, deep-link, refresh, browser navigation, and search.

Performance lock: no full ancestor-path construction for this grid, no tooltip that requires full paths, no N+1, no per-row query for +N.

A — PRODUCT LIST GRID COLUMNS

Use two separate canonical AppDataGrid columns:

fa:

دسته اصلی

نمایش در دسته‌های دیگر

en:

Primary Category

Shown in Other Categories

Do NOT combine Primary and Additional memberships into one categorySummary text column.

B — PRIMARY CATEGORY CELL

Show ONLY the localized leaf name.

Example:
گوشی هوشمند

Do NOT show:
موبایل و تبلت > گوشی موبایل > گوشی هوشمند

Do NOT fetch/build full ancestor path for:

cell text

tooltip

hover

hidden metadata

No full-path tooltip in this grid.

If Product Workspace needs full path, preserve it there.

C — ADDITIONAL/DISPLAY CATEGORY COLUMN

Return/display localized leaf names only.

UI:

separate chip/badge per Category

no comma-separated raw text

max 3 chips inline

if count > 3 show +N

Example:
گوشی اقتصادی موبایل دانشجویی پیشنهاد ویژه +2

If zero:

show quiet —

D — +N BEHAVIOR

On +N:

show remaining leaf names using existing Popover/Dropdown/Tooltip design-system component

NO backend request

use already-returned additionalCategoryNames[]

Do not add lazy per-row fetch solely for +N.

E — QUERY / DTO SHAPE

Replace/rework path-heavy grid projection with explicit efficient fields conceptually equivalent to:

primaryCategoryName

additionalCategoryNames[]

optional additionalCategoryCount

Normal UI shows names only.
IDs may remain internally only if required for row actions.

F — QUERY PERFORMANCE

For this grid:

remove unnecessary ancestor-tree/path composition

do not load entire Category tree solely for grid Category cells

avoid N+1

avoid query per Product

avoid query per Category chip

Preferred:

obtain Product membership rows in batch

collect needed CategoryIds

resolve localized leaf names in one batched projection

group into Primary + Additional

Preserve modular-monolith boundaries; no forbidden cross-module SQL join.

G — CATEGORY → PRODUCTS GRID

Apply the same human display principle where membership info is shown:

current Category context clear

Primary Category leaf name only

display memberships use chips where multiple are shown

no unnecessary full paths

Preserve T037:

Primary row: no trash/remove

display row: remove allowed

no membership: add-for-display

L1/L2: no assignment CTA

all direct assignments L3-only

H — CATEGORY TREE CURRENT SELECTION

When a Category Workspace is loaded, the corresponding tree node MUST visibly be selected.

Must work for:

direct tree click

route navigation

browser refresh

direct deep-link

browser back/forward

opening Category from another Admin surface

selecting Category from tree search result

Canonical source of selection must be current Category identity from route/workspace state, not transient local click state.

I — TREE AUTO-EXPANSION

For nested current Category:

expand ancestor chain so selected node is visible

scroll selected node into view if safe with existing component

do not unnecessarily collapse unrelated user-expanded branches

J — TREE SEARCH + SELECTION

When using Category search:

opening search result loads Category

selected state follows current Category

after clearing search, same Category remains selected

ancestor chain expands in normal tree

exactly one selected node

Do not lose selection due to temporary filtered-tree state.

K — SELECTED VS HOVER

Selected state must be visually distinct from hover.

Requirements:

one selected node

clear but restrained selected treatment

preserve approved Category Tree visual contract

reuse existing AppCategoryTree/Ant Tree controlled selectedKeys or canonical equivalent

fa RTL / en LTR

Do NOT redesign the tree.

L — INVALID/DELETED CURRENT CATEGORY

If current Category is invalid/deleted/not accessible:

do not leave stale unrelated selection

clear selection or follow existing not-found behavior

M — BACKEND TESTS

Add/adjust tests:

Primary leaf name returned

full Primary path is not required/constructed for grid contract

Additional names separated from Primary

Additional names leaf-only

deterministic Additional ordering

no duplicates

zero Additional supported

fa/en localization correct

batch composition does not regress to N+1 where test seams can assert call counts

no L1/L2 Product assignment regression

If categorySummary remains for compatibility elsewhere, keep grid independent from path-heavy behavior and document compatibility usage.

N — FRONTEND TESTS

Product list:

separate Primary / Other Categories columns

Primary leaf-only

no full path

0 Additional => —

1–3 => all chips

4+ => first 3 chips + +N

+N shows remaining names WITHOUT API call

no comma-separated blob

fa/en RTL/LTR

Tree:

click selects

deep-link selects

refresh/current route selects

back/forward updates selected node

ancestor chain expands

search result selection persists after clearing

one selected node only

invalid Category leaves no stale selection

Category→Products:

T037 action semantics stay green.

O — LIVE VERIFICATION

Using seeded runtime:

Product List:

Product with Primary only => leaf-only Primary

Product with 1–3 display memberships => chips

safe sample with >3 display memberships => first 3 + +N

activate +N

verify no network request for Category names

Network/query:

no ancestor-path composition solely for grid cells

no per-row/per-chip requests

Tree:

click nested Category => selected

refresh => same selected

deep-link nested Category => ancestors expanded + selected

browser back/forward => selection follows route

search/open result/clear search => selection persists

T037 regression:

Primary rows no trash

L1/L2 no add CTA

L3 display assignment still works

Restore temporary memberships used for verification.

P — ARCHITECTURE LOCKS

Preserve:

Primary Category L3-only

display membership L3-only

L1/L2 navigation-only

L1/L2 PLP subtree discovery

Primary change only via migration wizard

Primary drives Attribute/Variant schema

display membership discovery-only

viewed Category owns PLP facets

Product != Offer

no Product/Variant Price/Stock/Inventory

AppDataGrid canonical

AppCategoryTree canonical

Shopeiva untouched

Q — RECOVERY / SOT

Update existing canonical Recovery/SoT:

Last Architect-accepted: TB-P07-T037

Current issued/implemented: TB-P07-T038

phase P07

USER_VISUAL_ACCEPTED=NO

branch main + final HEAD/origin

Worker next state IDLE / waits for Bridge Task

Run existing recovery staleness guard.

Do not create a competing recovery mechanism.

R — FULL VALIDATION

Backend:

restore

build

FULL tests

MigrationRunner/tests

Frontend:

typecheck

lint

full relevant Product/Category/AppDataGrid/Tree tests

production build

Also:

recovery staleness guard

git diff --check

Require:

0 failed

0 skipped

no new warnings

S — RUNTIME / GIT

Keep alive:

Backend :5088

Frontend :3000

Shopeiva :3001

Verify current HEAD loaded.

Require:

HEAD == origin/main

scoped tracked tree clean

unrelated dirty changes preserved

T — EVIDENCE

Create:
docs/evidence/TB-P07-T038/

Required:

product-grid-category-columns.md

product-grid-query-performance.md

additional-category-chips.md

category-tree-selection.md

live-verification.md

recovery-sot.md

full-validation.md

ACCEPTANCE

PASS only if ALL:

Product grid has separate دسته اصلی and نمایش در دسته‌های دیگر

Primary shows leaf name only

no full Category path fetched/built for grid tooltip/hidden display

Additional memberships are individual chips

max 3 inline chips




3 shows +N

+N reveals remaining names with NO backend query

no comma-separated Additional blob

query path batched/no N+1

tree selected node always matches loaded/current Category

deep-link/refresh/back-forward/search work

ancestor chain expands for nested current Category

selected vs hover clearly distinct without redesign

T037 L3-only/action semantics intact

Recovery/SoT updated to T038 and guard passes

full validation green

runtime current

HEAD == origin/main

USER_VISUAL_ACCEPTED=NO.

RESULT CONTRACT

Return ONLY canonical BRIDGE-WAKE-V1 result for TB-P07-T038 with sections:

Summary
Product-Grid-Columns
Primary-Category-Cell
Additional-Category-Chips
Query-Performance
Category-Products-Regression
Tree-Selection
Tree-DeepLink
Tree-Search
Localization
Architecture
Recovery-SoT
Validation
Live-Verification
Runtime
Evidence
Git
Architectural-Concerns
Visual-Concerns
Blockers

END_TOOBA_TASK
