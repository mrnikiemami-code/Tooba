PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_REPAIR_TASK

Task-ID:
TB-P10-T004-R24-R1-R1

Parent-Task:
TB-P10-T004-R24-R1

Channel:
tooba-main

WorkerId:
tooba-worker-01

AgentType:
cursor

Status:
REPAIR

USER_VISUAL_ACCEPTED=NO

Title:
Recipient Name Canonicalization Repair — New FirstName/LastName Must Win, Legacy RecipientName Only as Historical Fallback

Architect Review

TB-P10-T004-R24-R1 is NOT YET accepted visually.

The main repair is strong and accepted functionally:

Cart no longer empties after login

merged authenticated Active Cart survives Shipping

header badge remains correct until atomic checkout COMMIT

Shopeiva authenticated account menu reused

Orders / Account / Logout are wired

logout isolation passes

FirstName / LastName were introduced separately

atomic checkout regression passes

runtime A–T PASS

anti-pattern scan CLEAN

HEAD==origin/main

However one customer-facing defect remains inside the exact R24-R1 scope:

When a legacy saved address is selected, Payment summary still displays legacy RecipientName
(e.g. محمد لمامی) even though current explicit FirstName / LastName values were filled.

This creates two competing recipient-name sources and violates the new canonical name model.

The repair must establish one clear precedence rule without guessing/splitting legacy names.

Do NOT redesign address UI.
Do NOT bulk-guess legacy FirstName/LastName.
Do NOT alter unrelated checkout policy.
Do NOT create TB-P10-T005.

Locked Recipient Name Rule

For NEW / UPDATED checkout recipient data:

Canonical source:

FirstName

LastName

Display:
FirstName + " " + LastName

Legacy RecipientName is fallback ONLY when:

the historical address/order has no canonical FirstName/LastName values

If explicit FirstName and LastName exist, RecipientName MUST NOT override them.

This applies consistently across:

Shipping

Payment summary

checkout projection

committed Order recipient snapshot

Customer Orders

Admin Order Detail where recipient is rendered

Historical rows must remain readable without guessed splitting.

Recovery State

Phase: P10
Last Architect-accepted: TB-P10-T004-R24
Last Implementation: TB-P10-T004-R24-R1
Current Repair: TB-P10-T004-R24-R1-R1
Worker: tooba-worker-01
Expected branch: main
Expected previous HEAD: 6dd584f130cf81bbaf6fb47710177d9a2bd3e678
USER_VISUAL_ACCEPTED=NO

Applicable Locks

Preserve LOCK-SF-001…121.

Especially:

LOCK-SF-103 atomic checkout

LOCK-SF-117 Cart != Session

LOCK-SF-118 Cart survives login until COMMIT

LOCK-SF-119 canonical shared account header

LOCK-SF-120 canonical logout

LOCK-SF-121 new addresses store FirstName/LastName separately

1. Recovery Preflight

Record:

Phase

Last Architect-accepted

Last Implementation

Current Repair

Worker

branch

HEAD

origin/main

HEAD==origin/main

tracked dirty state

task-owned untracked state

Unexpected tracked divergence => BLOCK.

Evidence:
docs/evidence/TB-P10-T004/r24-r1-r1-recovery-start.md

2. Discovery — Competing Recipient Sources

Trace every recipient-name field/source from:

AddressBook entity

Shipping form

selected saved address projection

checkout draft

shipping commit DTO

Order recipient snapshot

Payment projection

Customer Order projection

Admin Order projection

Create:
docs/evidence/TB-P10-T004/r24-r1-r1-recipient-discovery.md

Required table:

Surface | FirstName | LastName | RecipientName | Current precedence | Required precedence

Find the exact path where legacy RecipientName wins over explicit FirstName/LastName.

3. Canonical Projection Rule

Create/reuse one backend/domain presentation rule equivalent to:

If:
FirstName and LastName are both non-empty
→ canonical display name = FirstName + LastName

Else if:
legacy RecipientName is non-empty
→ display legacy RecipientName

Else:
use current business-safe empty/unknown behavior

Do NOT:

parse RecipientName into first/last

infer surname boundaries

overwrite historical RecipientName

use frontend-specific precedence rules

Prefer backend canonical projection where practical.

4. Legacy Saved Address Selection

Required behavior:

Legacy address may have only:
RecipientName

When selected:

old record remains valid

UI must not invent split names

If user supplies/edits explicit:
FirstName
LastName

then those explicit values become canonical for the checkout recipient / updated address according to current save semantics.

Payment page must show the explicit first+last, not stale legacy RecipientName.

If user does NOT provide new explicit first/last and legacy address is used without edit:

legacy RecipientName fallback is acceptable

no guessed split

Document exact behavior.

5. New Address

For every newly created address:

FirstName required

LastName required

persisted separately

RecipientName legacy field must not become the authoritative new source

If RecipientName must remain populated temporarily for backward compatibility:

derive it deterministically from FirstName + LastName

document as compatibility mirror only

all new code reads canonical FirstName/LastName first

6. Order Snapshot

On successful checkout commit:

Snapshot must preserve independently:

RecipientFirstName

RecipientLastName

Legacy combined RecipientName may exist for compatibility only.

Payment summary and future Order views must prefer snapshot First/Last.

Historical old Orders:

if snapshot First/Last absent

fall back to historical RecipientName

No migration guessing.

7. Payment Summary

Explicitly repair current defect.

Scenario:

legacy saved address selected

checkout recipient FirstName=محمد

LastName=امامی

Payment summary MUST render:
محمد امامی

It must NOT render stale:
محمد لمامی
or any old RecipientName value.

Use committed checkout/order recipient snapshot as source, not re-read stale address display text if that causes divergence.

8. Cross-Surface Consistency

The exact same canonical recipient display must appear on:

Shipping summary after save

Payment summary

Customer Order detail

Admin Order detail

No surface-specific name joining logic if avoidable.

9. Focused Tests

Required:

Legacy:

legacy address with RecipientName only -> still readable

no guessed split performed

legacy address + explicit FirstName/LastName -> explicit values win

Payment summary uses explicit FirstName/LastName

Customer Order uses same

Admin Order uses same

New:
7. new address requires FirstName
8. new address requires LastName
9. both persist independently
10. compatibility RecipientName mirror, if kept, equals deterministic first+last
11. new projection reads First/Last first

Snapshot:
12. committed Order stores FirstName/LastName independently
13. payment reads committed snapshot
14. historical old Order falls back to RecipientName

Regression:
15. Cart survives login
16. header/account/logout unchanged
17. atomic checkout unchanged
18. address save unchanged apart from recipient source
19. FA RTL
20. EN LTR smoke
21. recovery guard
22. git diff --check

Do NOT run unrelated full suites.

10. Real Runtime — REQUIRED

Use Host :5088 + FE :3000.

A. Login using Development customer
B. Cart remains populated
C. Shipping
D. select a legacy saved address
E. enter/save explicit FirstName and LastName
F. continue to Payment
G. Payment summary shows exactly explicit FirstName + LastName
H. Customer Order detail shows same
I. Admin Order detail shows same
J. separately open an untouched historical legacy Order/address and prove RecipientName fallback still renders
K. no Cart/header regression

Capture screenshot evidence for:

Shipping explicit fields

Payment recipient

Customer Order recipient

Admin Order recipient

11. Anti-Pattern Gate

Scan for:

guessed splitting of RecipientName

frontend-only precedence

Payment re-reading stale AddressBook RecipientName instead of committed snapshot

multiple different name-format helpers

RecipientName overriding non-empty FirstName/LastName

destructive migration of historical names

hardcoded sample names

regression to combined-only storage

Any occurrence => REPAIR.

Evidence:
docs/evidence/TB-P10-T004/r24-r1-r1-antipattern-scan.md

12. Lock Registry

Add one non-duplicate lock if not already fully covered:

Explicit FirstName/LastName are canonical for new/updated recipient data and committed Order snapshots; legacy RecipientName is fallback only when canonical split fields are absent, and legacy names are never guessed-split.

13. Evidence

Add:

r24-r1-r1-recovery-start.md

r24-r1-r1-recipient-discovery.md

r24-r1-r1-recipient-canonicalization.md

r24-r1-r1-order-snapshot.md

r24-r1-r1-payment-summary.md

r24-r1-r1-focused-validation.md

r24-r1-r1-runtime.md

r24-r1-r1-antipattern-scan.md

r24-r1-r1-recovery-sot.md

under:
docs/evidence/TB-P10-T004/

14. Recovery / Pipeline

At completion:

Phase P10

Last Architect-accepted before review: TB-P10-T004-R24

Last Implementation: TB-P10-T004-R24-R1-R1

Current Issued/Repair: none

USER_VISUAL_ACCEPTED=NO

Worker: tooba-worker-01 — IDLE

branch main

ending HEAD recorded

HEAD==origin/main

task-owned worktree clean

no TB-P10-T005

Run recovery guard.

Acceptance

PASS only if ALL:

explicit FirstName/LastName always override legacy RecipientName

legacy RecipientName is fallback only when split fields absent

no guessed split

Payment summary shows canonical recipient

Customer Order and Admin Order agree

committed snapshot preserves FirstName/LastName separately

historical legacy rows still render safely

Cart/header/logout regressions remain green

real runtime A–K PASS

focused tests green

anti-pattern scan CLEAN

recovery exact

HEAD==origin/main

Worker IDLE

no TB-P10-T005

Result Contract

Return ONLY canonical BRIDGE-WAKE-V1 Result with:

Summary
Applicable-Locks
Recovery-Start
Recipient-Discovery
Recipient-Canonicalization
Order-Snapshot
Payment-Summary
Focused-Validation
Runtime
AntiPattern-Scan
Lock-Registry
Recovery-SoT
Git
Architectural-Concerns
Visual-Concerns
Blockers

END_TOOBA_REPAIR_TASK