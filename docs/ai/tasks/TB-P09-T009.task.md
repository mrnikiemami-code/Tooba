PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_TASK

Task-ID:
TB-P09-T009

Parent-Task:
TB-P09-T008

Channel:
tooba-main

WorkerId:
tooba-worker-01

AgentType:
cursor

Status:
ISSUED

USER_VISUAL_ACCEPTED=NO

Title:
Order Corrective Actions — Safe Rollback Matrix, Payment Decision Recovery, Shipment Rejection Recovery, Cancel Restore, Whole-Order Action Deduplication

Architect Context

TB-P09-T008 is technically accepted.

Manual user review of سفارش‌ها و پرداخت found real usability defects:

important actions can place the operator into a dead-end with no safe corrective path

لغو سفارش is duplicated multiple times in whole-order actions on multi-seller orders

rejected manual/card-to-card payment has no safe return to در انتظار تأیید واریز

shipment may need recovery even after tracking code is assigned, e.g. carrier/post rejects the parcel before dispatch

an accidentally cancelled Order may need restoration when no irreversible external effects have occurred

This task fixes ONLY those corrective-action rules and the duplicated whole-order action projection.

Do NOT start unrelated work.
Do NOT redesign UI.

Speed / Validation Lock

Run ONLY necessary focused tests for touched behavior plus:

focused build/typecheck

recovery guard

git diff --check

short runtime smoke for each new corrective action

Do NOT rerun full T001–T008 suites.

Core Product Rule — LOCKED

For every important lifecycle transition, classify correction as one of:

DirectReverseAllowed

CorrectiveActionRequired

Irreversible

No important user action should create an unnecessary dead-end.

Corrective actions:

backend-authoritative

permission-aware

auditable

repair/release downstream allocations/effects where required

never erase history

never rewrite immutable financial records

never allow arbitrary state jumps

This is NOT a generic workflow engine.

1. Mandatory Discovery

Inspect current:

Order whole-action composer

multi-seller action aggregation

Payment manual/card-to-card states

confirm/reject deposit commands

Order cancellation policy

inventory reservation/release behavior

Fulfillment shipment states

tracking assignment

shipment cancel/release behavior

dispatch boundary

return/refund/settlement boundaries

audit/history event model

current permission capabilities

current Order Detail action menu

Reuse existing reversible/corrective patterns if present.
Do NOT duplicate domain rules.

2. Whole-Order Action Deduplication — REQUIRED

Current defect:
multi-seller Order can show لغو سفارش multiple times.

Whole-Order action menu must contain each whole-order operation at most ONCE.

Required:

dedupe by canonical action code + whole-order scope

do not dedupe distinct seller-scoped actions inside اقلام و ارسال

whole-order menu remains whole-order only

multi-seller count must not multiply whole-order actions

Example:
3 seller Orders + cancel permitted => show ONE لغو سفارش.

3. Manual Payment Decision Recovery

Current flow:
در انتظار تأیید واریز
→ رد واریز

Need corrective action:
رد واریز
→ بازگرداندن به انتظار تأیید واریز

Allowed only when:

payment method is manual/card-to-card

payment is in rejected/manual-review-failed state

Order is not irreversibly cancelled

no replacement Payment superseded it

no successful payment/refund/settlement side effect exists

user has existing reconcile/manage capability

Required effects:

Payment returns to pending/manual-confirmation through Payment domain/application path

Order does NOT become Paid

no payment-succeeded event emitted

history records actor/time

تأیید واریز, رد واریز, and permitted لغو سفارش reappear

retry is safe/idempotent

Human FA label:
بازگرداندن به انتظار تأیید واریز

Do NOT patch DB status directly.

4. Packed Item Reverse — Preserve Existing Rule

Keep accepted T005 behavior:

بسته‌بندی‌شده
→ بازگشت از بسته‌بندی

Allowed only when selected quantity:

not allocated to active shipment

not dispatched

not delivered

Do not weaken this rule.

5. Shipment Rejection / Carrier Refusal Before Dispatch

Real-world case:

shipment created

tracking/reference may already be assigned

carrier/post rejects/refuses parcel

shipment was NOT dispatched/handed over

Need corrective action:
prefer human wording such as:

عدم پذیرش مرسوله
or

ابطال مرسوله

Allowed when:

not dispatched

not delivered

tracking may exist

no irreversible carrier handoff

Required effects:

shipment moves to auditable cancelled/rejected state

tracking/reference remains in history

active line/quantity allocations released

quantities become eligible for replacement shipment

user may create replacement shipment with same or another method

old shipment not hard-deleted

no finance rewrite

If current cancel_shipment already supports Created+tracking:
reuse it.
If it incorrectly blocks because tracking exists:
repair narrowly.

6. Tracking Correction Before Dispatch

Before dispatch:

allow tracking/reference correction where current model permits

audit old/new value

do not require full shipment rollback for typo correction

After dispatch:

no arbitrary tracking replacement unless current carrier sync/correction model explicitly allows it

7. Dispatch Boundary

After dispatch:
DO NOT allow simple reverse to:

packed

unpacked

cancelled/released as if shipment never left

If return-to-sender/carrier recall is not modeled, document as future separate corrective operation.

8. Delivered Boundary

After delivered:

no fulfillment rollback

use Return/مرجوعی

Refund remains separate

Do not add fake undo delivery.

9. Accidental Whole-Order Cancellation Recovery

Need:
بازگردانی سفارش لغوشده

Allowed only when ALL applicable checks pass:

Order is Cancelled

no shipment dispatched

no line delivered

no completed customer refund caused by cancellation

no irreversible seller settlement/payout effect

payment state is valid/recoverable

inventory reservation can be restored safely

no conflicting replacement/order state

user has required capability

Required:

restore correct prior/recoverable lifecycle state, not arbitrary hardcoded state

restore/reacquire inventory via Inventory boundary

restore eligible pre-dispatch fulfillment state only where valid

do NOT resurrect cancelled shipments blindly

payment state remains authoritative

if inventory reacquire fails:

restoration fails safely/atomically

Order stays cancelled

human error shown

Must be forbidden if:

refund completed

dispatch occurred

delivery occurred

inventory cannot be reacquired

irreversible financial effect exists

Human FA label:
بازگردانی سفارش لغوشده

10. Cancellation Snapshot / Prior State

Prefer existing audit/domain events.

If cancellation does not preserve enough information:
add the smallest canonical snapshot/event data needed, e.g.:

previous order state

relevant payment state/reference

reservation restoration metadata if needed

Do NOT create broad event sourcing.

11. Financial Boundaries

Corrective actions must not rewrite immutable finance history.

Payment reject recovery:

no fake payment success

Shipment rejection:

no settlement mutation

Order restore:

if refund completed => restoration forbidden

if irreversible payout/settlement effect exists => restoration forbidden or separate future compensating flow

12. Action Projection Matrix

Extend authoritative backend projection.

Examples:

Manual payment rejected:

بازگرداندن به انتظار تأیید واریز

permitted whole-order cancel if valid

Packed/unallocated:

بازگشت از بسته‌بندی

Shipment Created + tracking + not dispatched:

اصلاح کد رهگیری

عدم پذیرش/ابطال مرسوله

Cancelled Order, recoverable:

بازگردانی سفارش لغوشده

Dispatched:

no simple reverse

Delivered:

no fulfillment reverse

Frontend consumes projection.
No hardcoded corrective eligibility in React.

13. Confirmation UX

Use existing Tooba Dialog.

No browser prompt/confirm/alert.

Dialogs should briefly explain effects:

shipment rejection releases allocations

restoring cancelled Order attempts inventory reservation again

No heavy UI redesign.

14. Audit / Operational History

Every corrective action must produce human-readable history.

Examples:

رد واریز به حالت انتظار تأیید بازگردانده شد

بسته‌بندی ۲ قلم برگشت داده شد

مرسوله توسط حامل پذیرفته نشد — اقلام آزاد شدند

سفارش لغوشده بازگردانی شد

Include:

actor

time

seller/shipment/line/qty scope where relevant

previous state

resulting state

Never delete original history.

15. Permissions

Reuse existing:

payment.reconcile

order.handle / order.cancel equivalent

fulfillment.manage

existing inventory boundary permissions

Do not invent redundant permission families unless strictly necessary.

Backend authoritative.

16. Error UX

Human FA/EN messages for:

payment can no longer return to confirmation

replacement payment exists

shipment already dispatched

shipment already delivered

tracking cannot be changed after dispatch

cancelled Order cannot be restored after refund

cancelled Order cannot be restored after dispatch

inventory unavailable for restoration

irreversible financial effect blocks restoration

action already corrected/retried

No raw codes/JSON/exceptions.

17. Focused Tests — ONLY NECESSARY

Backend:

whole-order action dedupe on multi-seller Order

reject manual payment -> restore to pending confirmation

duplicate restore idempotency

payment restore forbidden after superseding/successful payment

shipment with tracking can be rejected/cancelled pre-dispatch

allocation released after shipment rejection

replacement shipment can be created

pre-dispatch tracking correction

post-dispatch shipment correction rejected

cancelled Order restore happy path

cancelled Order restore fails if inventory unavailable

restore forbidden after refund

restore forbidden after dispatch/delivery

existing pack reverse regression

Frontend:

only one لغو سفارش in whole-order menu

payment corrective action visibility

shipment corrective action visibility

cancelled Order restore visibility

no corrective action after irreversible boundary

confirmation Dialog

current layout/width/scroll preserved

RTL labels

Also:

focused build/typecheck

recovery guard

git diff --check

No full suite.

18. Runtime Smoke — SHORT

Use existing local data where possible.

Required short scenarios:

Multi-seller Order:

whole-order menu shows ONE لغو سفارش

Manual payment:

reject deposit

corrective restore to pending confirmation

تأیید واریز appears again

Shipment:

create shipment

assign tracking

mark carrier rejection/cancel before dispatch

allocations released

replacement shipment can be created

Cancelled Order:

cancel a safely recoverable Order

restore it

inventory reservation restored

One forbidden restore:

after irreversible effect backend rejects

Do NOT replay unrelated lifecycle stages.

19. User UI Preservation

Preserve all accepted/user-authored UI:

Orders grid widths

horizontal scroll

filters

kebab

Order Detail layout

اقلام و ارسال

financial tabs

history

notes

shipment modal styling

Only add minimal corrective-action menu/dialog entries.

20. Evidence

Create:
docs/evidence/TB-P09-T009/

Required concise evidence:

discovery.md

whole-order-dedup.md

payment-correction.md

shipment-rejection-correction.md

cancelled-order-restore.md

corrective-action-matrix.md

audit-history.md

runtime-smoke.md

ui-preservation.md

recovery-sot.md

Keep evidence concise.

21. Recovery / Pipeline

Update:

Phase: P09

Last Architect-accepted: TB-P09-T008

Current Issued: TB-P09-T009

Current Repair: none

Last Implementation on completion: TB-P09-T009

USER_VISUAL_ACCEPTED=NO

Worker Next State: IDLE

do NOT invent/start TB-P09-T010

Run recovery guard.

22. Git

Required:

preserve user-authored changes

commit task changes

push origin/main

HEAD==origin/main

no task-owned uncommitted changes

Acceptance

PASS only if ALL:

whole-order actions deduplicated on multi-seller Orders

لغو سفارش appears once

manual payment rejection has safe corrective return to pending confirmation

shipment with tracking can be safely rejected/cancelled before dispatch

rejected shipment releases allocations and can be replaced

tracking correction before dispatch works where supported

post-dispatch simple rollback remains forbidden

cancelled Order can be restored only when all safety checks pass

inventory restoration authoritative and failure-safe

restore blocked after irreversible refund/dispatch/delivery/financial effect

all corrective actions audited

frontend consumes backend action projection

no UI redesign

user width/scroll/filter/layout changes preserved

only necessary focused tests run

short runtime smoke complete

recovery guard passes

HEAD==origin/main

Worker IDLE

no TB-P09-T010 invented

USER_VISUAL_ACCEPTED=NO.

Result Contract

Return ONLY canonical BRIDGE-WAKE-V1 result with:

Summary
Discovery
Whole-Order-Action-Dedup
Payment-Corrective-Action
Shipment-Rejection-Corrective-Action
Tracking-Correction
Cancelled-Order-Restore
Corrective-Action-Matrix
Financial-Boundaries
Audit-History
Permissions
Error-UX
Focused-Validation
Runtime-Smoke
User-UI-Preservation
Recovery-SoT
Git
Architectural-Concerns
Visual-Concerns
Blockers

END_TOOBA_TASK