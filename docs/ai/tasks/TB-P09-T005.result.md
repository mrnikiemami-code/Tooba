# TB-P09-T005 Result

Status: PASS

PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

## Summary

Seller-scoped line/quantity fulfillment: QuantityPacked, pack/unpack selections, pre-dispatch shipment cancel/release, shared whole-group + selected command path, Order Line return-policy snapshot + deadline UI, human FA fulfillment statuses. User grid width/horizontal-scroll refinements preserved. USER_VISUAL_ACCEPTED=NO. Worker IDLE. No TB-P09-T006.

## User-UI-Preservation

Preserved AppDataGrid (no flex:1 default), legacy actions maxWidth, admin-screens orders actions width/minWidth + filterKind. Items table overflow-x-auto + min-w-[920px].

## Discovery

See `docs/evidence/TB-P09-T005/discovery.md`.

## Selection-Contract

`AdminOrderOperationRequest.Selections` + Host resolve-then-same-path for pack/unpack/create_shipment.

## Quantity-State

QuantityPacked + open Created allocation + QuantityShipped invariants enforced in domain.

## Whole-Group-Actions / Selected-Line-Actions

Empty selections → derive eligible; non-empty → validate & same PackSelections/CreateShipment/UnpackSelections.

## Reverse-Transitions

Unpack only unallocated; cancel_shipment only Created; post-dispatch reverse rejected.

## Shipment-Allocation

Explicit ShipmentItem rows; cancel releases; split line across shipments supported.

## Return-Policy-Snapshot / Return-Deadline-UI

OrderLine snapshot fields + AdminOrderLineView deadline/remaining FA displays.

## Status-Humanization

ReadyToFulfill → آماده پردازش; Packed/Dispatched/InTransit mapped.

## Operations-Projection

unpack + cancel_shipment projected; FE consumes Host actions.

## Cancellation-Interaction

Order cancel path unchanged; fulfillment cancel_shipment is pre-dispatch only.

## Audit-Events

Domain events: line packed/unpacked, shipment created/cancelled (+ existing dispatch/deliver).

## Permissions

order.handle / fulfillment.manage reused.

## Error-UX

Human FA InvalidOperationException / PlatformHttpException messages for eligibility failures.

## Focused-Validation

- BE: FulfillmentLineQuantityOperationsTests + OrderLineReturnPolicySnapshotTests + allocation + ReturnEligibilityEvaluator: 17 pass
- FE admin items/ops/grid: 27 pass
- recovery-staleness.guard: 3 pass

## Runtime-E2E

Migrations applied (`store-alpha`). Host `:5088` + FE `:3000` up. Live smoke on multi-seller Paid checkout: whole-group pack + selected-line pack via `POST /v1/admin/orders/{id}/operations` → 200; Packed + quantityPacked projections; return snapshot fields present. Details: `docs/evidence/TB-P09-T005/runtime-e2e.md`.

## UI-Regression

Grid kebab whole-order scope; financial sections retained; horizontal scroll preserved.

## Recovery-SoT

Architect-accepted TB-P09-T004; Issued TB-P09-T005; Implementation TB-P09-T005; Repair none; USER_VISUAL_ACCEPTED=NO.

## Git

Commit message: `feat(P09): TB-P09-T005 seller-scoped line/qty fulfillment + return deadline`

## Architectural-Concerns

Offer return-policy editor deferred; snapshot model ready (`docs/evidence/TB-P09-T005/offer-return-policy-contract.md`).

## Visual-Concerns

USER_VISUAL_ACCEPTED=NO.

## Blockers

None.

USER_VISUAL_ACCEPTED=NO
Worker Next State: IDLE
