PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_REPAIR_TASK

Task-ID:
TB-P10-T004-R24-R1-R4

Parent-Task:
TB-P10-T004-R24-R1-R3

Channel:
tooba-main

WorkerId:
tooba-worker-01

AgentType:
cursor

Status:
REPAIR

USER_VISUAL_ACCEPTED=NO

Title:
Storefront Auth/Cart Network Churn Repair — Eliminate Repeated auth/me, Duplicate Merge Risk, and Anonymous 401 Storm

Architect Review

TB-P10-T004-R24-R1-R3 is NOT Architect-accepted yet.

R3 functionally proves the three original defects are repaired:

authenticated header identity now resolves to mobile fallback

logout preserves the authenticated account Cart in DB and re-login restores it

Cart stays Active through Shipping and converts only after successful atomic COMMIT

injected commit failure keeps Cart intact and prevents Payment navigation

However, R3 Result contains network evidence that conflicts with the R3 acceptance contract:

auth/me 96 mount/refresh not interval

merge 3

14 anonymous /api/auth/me 401 after logout

R3 explicitly required:

no auth polling

no cart polling

no repeated merge

no 401 storm

no duplicate logout calls

no duplicate checkout submit

Therefore R3 cannot be accepted from the Result alone until this network behavior is explained and, if necessary, repaired.

Do NOT reopen already-fixed Cart persistence/conversion semantics unless directly required by the root cause below.
Do NOT redesign Storefront.
Do NOT create TB-P10-T005.

Recovery State

Phase: P10
Last Architect-accepted: TB-P10-T004-R24
Last Implementation: TB-P10-T004-R24-R1-R3
Current Repair: TB-P10-T004-R24-R1-R4
Worker: tooba-worker-01
Expected branch: main
Expected previous ending HEAD: cd4f441b0ed4b0d98a4093f2a91b0ad1bdffbfb2
USER_VISUAL_ACCEPTED=NO

Preserve LOCK-SF-001…125.

Especially preserve:

LOCK-SF-103 atomic checkout

LOCK-SF-117 Cart != Session

LOCK-SF-118 Cart survives login until COMMIT

LOCK-SF-119 canonical shared account header

LOCK-SF-120 canonical logout

LOCK-SF-123 logout hides never deletes account Cart

LOCK-SF-124 canonical header identity

LOCK-SF-125 conversion visible only after COMMIT

1. Recovery Preflight

Record:

git branch

HEAD

origin/main

git status --short

current Recovery/SoT values

Unexpected tracked divergence => BLOCK.

Evidence:
docs/evidence/TB-P10-T004/r24-r1-r4-recovery-start.md

2. Mandatory Network Root-Cause Discovery

Reproduce the exact R3 browser flow with Network + console instrumentation.

Trace every request to:

/api/auth/me

login

logout

cart/current

cart merge

checkout commit

For EACH /api/auth/me request, record:

timestamp

route

initiating component/hook

trigger/reason

authenticated vs anonymous

HTTP status

whether it was necessary

whether a concurrent identical request already existed

For EACH merge call, record:

timestamp

login/session transition that caused it

source anonymous Cart presence

target authenticated Cart

whether merge had already executed for the same transition

whether quantity changed

Create:
docs/evidence/TB-P10-T004/r24-r1-r4-network-root-cause.md

Required conclusion:
Why did R3 produce 96 auth/me requests, 3 merge calls, and 14 anonymous 401s?

Do NOT call this clean until the exact source is proven.

3. Canonical Account State — No Request Multiplication

There must be ONE canonical Storefront account/session state.

Inspect:

shared layout

desktop header

mobile header

StorefrontAccountMenu

auth hooks/providers

route transitions

React StrictMode/dev double effects

query/cache configuration

focus/reconnect refetch

duplicated component mounts

customer pages

Shipping/Payment layouts

Required:

shared consumers reuse canonical state

mounting desktop + mobile header must not independently create redundant auth/me traffic

route navigation must not create uncontrolled refetch multiplication

no interval/polling

no hidden per-component auth fetcher duplication

If current query library supports dedupe/cache, use its canonical mechanism.
Do NOT add timing hacks.

4. Anonymous Auth State Must Not Generate a 401 Storm

After canonical logout succeeds:

Expected anonymous Storefront state is known.

The UI must not repeatedly probe /api/auth/me in a way that creates repeated expected 401s.

Allowed:

one necessary session-resolution request when architecture genuinely requires it

explicit login/session transition validation

Forbidden:

repeated anonymous 401s caused by every header/menu/page mount

retry-on-401 loops

focus/reconnect refetch producing expected 401 noise

multiple simultaneous auth/me calls for the same state

treating expected unauthenticated state as an exceptional retry condition

Use the existing canonical unauthenticated representation.
Do NOT weaken API authorization.
Do NOT make /api/auth/me return fake authenticated data.
Do NOT suppress real unexpected 401s globally.

5. Merge Must Be Transition-Scoped and Idempotent

R3 reports merge 3.

Determine whether those 3 calls correspond to three distinct intentional login transitions in A–R.

If YES:

prove one merge max per relevant login transition

prove no repeated merge for one transition

prove no quantity multiplication

document why count=3 is correct

If NO:
repair so:

one logical login transition causes at most one canonical merge attempt

retries remain backend-idempotent

refresh/navigation does not re-merge

rehydration does not re-merge

same anonymous Cart cannot be consumed into account Cart repeatedly

Do NOT remove the legitimate anonymous→authenticated merge.

6. Request Budget / Deduplication Acceptance

Run one deterministic browser scenario and record counts.

Scenario:

A. fresh anonymous Home
B. navigate Home → PLP → PDP → Cart
C. login 09111111111 / 123456
D. navigate Cart → Shipping
E. save/select address
F. logout
G. navigate Home → PDP → Cart while anonymous
H. login same customer
I. restored/merged Cart
J. continue Shipping → successful COMMIT → Payment

Acceptance is behavioral rather than an arbitrary magic number, but MUST prove:

no interval auth requests

no continuously increasing auth/me count while idle

no duplicate simultaneous auth/me from desktop/mobile copies

no retry loop on anonymous 401

no 401 storm

no repeated merge for same auth transition

no duplicate logout

no duplicate checkout commit

Record request table and totals.

Evidence:
docs/evidence/TB-P10-T004/r24-r1-r4-network-budget.md

7. Idle Stability Test

On each state, remain idle at least long enough to detect periodic/refetch loops according to current frontend behavior:

authenticated Home

authenticated Shipping

anonymous Home after logout

Payment after successful commit

Record request counters at start/end.

PASS requires:

auth/me does not continuously grow while idle

cart/current does not continuously grow while idle

merge count does not grow while idle

no repeated 401s while idle

No polling workaround.

8. Preserve R3 Functional Behavior

Mandatory regression:

Header:

login -> profile name if available, otherwise mobile

current dev customer -> 09111111111

shared account dropdown

logout affordance

Logout:

account Cart remains DB Active with same lines/qty

anonymous cannot see account Cart

same-customer re-login restores

interim anonymous Cart merges canonically

Conversion:

Shipping/address leaves Cart Active

click does not pre-clear

before successful COMMIT badge/lines intact

successful COMMIT -> Cart Converted

Payment may then show 0/new active Cart

injected commit failure -> Shipping remains, badge/cart intact

Do NOT regress any of these to reduce network traffic.

9. Focused Tests

Required focused tests for:

Auth:

multiple account-state consumers dedupe canonical auth/me resolution

anonymous state does not retry expected unauthenticated response

route navigation does not create unbounded auth fetches

no polling timer

logout updates canonical auth state exactly once

Merge:
6. one login transition -> max one logical merge
7. refresh after login -> no second merge
8. route navigation -> no second merge
9. backend retry idempotency
10. quantity does not multiply

Cart:
11. logout persistence regression
12. re-login restore regression
13. anonymous interim merge regression
14. Shipping Active regression
15. commit success conversion regression
16. commit 409/failure regression

Also:

test relevant scope

test relevant scope

Host focused tests if backend touched

recovery guard

git diff --check

Do NOT run unrelated full suites.

10. Real Browser Runtime — REQUIRED

Use:
Host :5088
Frontend :3000

Repeat the deterministic A–J scenario.

Capture:

Network filtered auth/me

Network filtered merge

Network filtered logout

Network filtered checkout commit

console

authenticated header

anonymous post-logout

restored Cart

Shipping before COMMIT

Payment after COMMIT

Explicitly report:

auth/me total

auth/me while authenticated

auth/me while anonymous

anonymous 401 total

merge total

distinct login transitions requiring merge

duplicate merge count

logout request count

checkout commit count

idle counter growth

A non-zero count is not automatically failure.
The requirement is no multiplication/storm/retry/polling and one logical merge per relevant transition.

11. Anti-Pattern Gate

Scan for:

setInterval/timer auth polling

timer Cart polling

auth/me fetch in multiple header copies

retry-on-401 loop

global query invalidation/refetch storm on logout

auth provider remount per route

merge on render/mount

merge on every /me success

merge on every navigation

route-based Cart reset

optimistic Cart badge zero

localStorage Cart SoT

timing sleeps/debounce used to hide races

Any real occurrence => REPAIR.

Evidence:
docs/evidence/TB-P10-T004/r24-r1-r4-antipattern-scan.md

12. Evidence

Add:

r24-r1-r4-recovery-start.md

r24-r1-r4-network-root-cause.md

r24-r1-r4-auth-state-dedup.md

r24-r1-r4-anonymous-401.md

r24-r1-r4-merge-transition.md

r24-r1-r4-network-budget.md

r24-r1-r4-idle-stability.md

r24-r1-r4-focused-validation.md

r24-r1-r4-runtime.md

r24-r1-r4-r3-regression.md

r24-r1-r4-antipattern-scan.md

r24-r1-r4-recovery-sot.md

13. Recovery / Pipeline

At completion:

Phase P10

Last Architect-accepted before review: TB-P10-T004-R24

Last Implementation: TB-P10-T004-R24-R1-R4

Current Issued/Repair: none

USER_VISUAL_ACCEPTED=NO

Worker IDLE

branch main

ending HEAD

HEAD==origin/main

task-owned clean

no TB-P10-T005

Run recovery staleness guard.

Acceptance

PASS only if ALL:

exact reason for R3 auth/me=96 explained

exact reason for R3 merge=3 explained

exact reason for 14 anonymous auth/me 401 explained

no auth polling

no cart polling

no continuously increasing auth traffic while idle

no anonymous 401 storm

no retry-on-401 loop

one logical merge max per relevant auth transition

merge is backend-idempotent

no duplicate logout

no duplicate checkout submit

canonical shared account state retained

R3 header identity behavior preserved

R3 logout Cart preservation preserved

R3 re-login restore/merge preserved

R3 atomic conversion boundary preserved

failed commit still keeps Cart intact and blocks Payment navigation

focused tests green

runtime proof green

anti-pattern scan CLEAN

recovery exact

HEAD==origin/main

Worker IDLE

no TB-P10-T005

USER_VISUAL_ACCEPTED remains NO

Result Contract

Return ONLY canonical BRIDGE-WAKE-V1 Result with:

Summary
Applicable-Locks
Recovery-Start
Network-Root-Cause
Auth-State-Dedup
Anonymous-401
Merge-Transition
Network-Budget
Idle-Stability
R3-Regression
Focused-Validation
Runtime
AntiPattern-Scan
Recovery-SoT
Git
Architectural-Concerns
Visual-Concerns
Blockers

END_TOOBA_REPAIR_TASK
