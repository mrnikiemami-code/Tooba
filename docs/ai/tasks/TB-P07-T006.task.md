PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

TASK-ID: TB-P07-T006
PARENT-TASK: TB-P07-T005-R1
PROJECT: Tooba
PHASE: P07
CHANNEL: tooba-main
STATUS: ISSUED
TASK-TYPE: CATEGORY WORKSPACE — GENERAL + TRANSLATIONS EDITOR
WORKER-POLICY: ONE WORKER = ONE ACTIVE TASK

Architect Acceptance

TB-P07-T005-R1 is ACCEPTED.

Canonical baseline:

Current Category Tree is USER-APPROVED.

DO NOT redesign or visually change AppCategoryTree.

Category workspace has explicit VIEW and EDIT modes.

View/Edit access pattern is now a locked Admin UX principle.

Public category URLs use clean localized slug only:
/{locale}/category/{localizedSlug}

Never append visible CategoryId suffixes.

(Locale, Slug) uniqueness remains backend-enforced.

Slug history/redirect foundation remains canonical.

final accepted baseline commit:
d682d98639def86969c38dd48091abeec347e496

Locked UX Principle

The Admin must be understandable for an ordinary non-technical user without training.

For this task:

simple first

advanced later

no technical jargon in normal UI

no raw UUIDs

no backend enum keys

no infrastructure terminology

human-readable status and validation

clear difference between VIEW and EDIT

save/cancel behavior must be obvious

multi-language editing must feel simple rather than technical

Hard Visual Lock

The existing approved Category Tree MUST remain untouched.

Do NOT:

change Tree orientation

change node direction

alter drag/drop

alter search

alter icons

alter spacing

alter selected state

restyle AppCategoryTree

Only minimal compatibility changes are allowed if absolutely required.

Goal

Turn the Category Workspace from a shell into a usable, production-quality editor for:

General category settings

Multi-language translations

Keep future tabs visible but do NOT implement Attributes/Facets/MegaMenu/Products/SEO advanced flows yet.

The user must be able to:

open category in VIEW mode

see clean General information

explicitly enter EDIT mode

edit General fields

switch languages

create/update a translation

see translation completeness

edit localized Name/Slug/descriptions

preview canonical localized URL

understand missing translations

save/cancel safely

return to VIEW mode after save

1 — Workspace Navigation

Keep the existing Category Workspace composition.

Tabs:

عمومی

ترجمه‌ها

ویژگی‌ها

فیلترها

مگامنو

محصولات

SEO

تنظیمات

تاریخچه

Implement real functionality only for:

عمومی

ترجمه‌ها

Future tabs remain clean progressive placeholders.

Do not fake data/actions.

2 — General Tab — VIEW Mode

The default detail experience remains VIEW mode.

Present data as readable sections/cards, not disabled form controls.

Show at minimum:

نام دسته در زبان فعلی

دسته والد

وضعیت

ترتیب نمایش

قابل نمایش بودن

تصویر

آیکن

آدرس عمومی دسته

وضعیت ترجمه‌ها

تعداد زیرمجموعه‌ها if efficiently available

تعداد محصولات only if already efficiently available

Actions:

ویرایش only when canEdit

storefront/open-preview action when appropriate

No Save button in VIEW.

3 — General Tab — EDIT Mode

When authorized user selects ویرایش, convert only the editable fields to controls.

Support:

Parent category

Status

SortOrder

IsVisible

Image

Icon

active/default translation Name/Slug if this is already the established create/edit pattern

If Media module integration is not yet available for polished selector:

use existing Media selector if available

otherwise show current media and defer picker behind a clean explicit placeholder

do NOT invent a fake file-upload architecture

Parent selector:

ordinary user-friendly

searchable

displays category path/name

never raw ID

prevents invalid self/descendant parent

preserves current Tree

4 — Status UX

Do not expose:
Draft
Published
Archived

as raw English values in Persian Admin.

Use localized labels:

پیش‌نویس

منتشرشده / فعال according to established product wording

بایگانی‌شده

If status transitions have domain constraints:

explain disabled transitions in human language

do not expose implementation errors

5 — Translation Tab

Build a first-class translation editor over CatalogCategoryTranslation.

The user must understand that this is ONE category with multiple language versions.

Do NOT make translations look like separate category records.

Recommended composition:

Top:

locale switcher/list

translation status per locale

Then selected locale editor.

Example locale status:

فارسی — کامل

English — ناقص

العربية — ایجاد نشده

Do not use technical terms like BCP-47 in normal UI.

6 — Locale Switcher

Use existing supported locales from Tooba, not a hardcoded fake list if a canonical locale source exists.

For each locale show:

display name

locale code subtly if useful

completeness status

current selected state

Behavior:

switching locale loads that CategoryTranslation

unsaved edits must not silently disappear

if dirty, prompt/save/discard according to existing form-mode convention

7 — Translation Fields

For selected locale, support:

Required/core:

Name

Slug

Content:

ShortDescription

Description

SEO preview fields may be visible here only if already part of T004 translation entity:

SeoTitle

SeoDescription

MetaKeywords only if existing architecture actually uses it

IMPORTANT:
Full SEO workflow remains for later dedicated SEO task.
Do not overbuild advanced SEO in T006.

8 — Slug UX

User edits only human slug.

Example:

Name:
گوشی موبایل

Slug:
گوشی-موبایل

Canonical preview:
/fa/category/گوشی-موبایل

Never show or require:
01a03826

Never append CategoryId automatically.

Auto-suggest slug from Name:

on new/missing translation

do not overwrite a manually edited slug unexpectedly

Normalize according to existing backend rules.

9 — Duplicate Slug UX

Backend (Locale, Slug) uniqueness remains authoritative.

If duplicate:
show ordinary localized message near Slug:

این نامک برای یک دسته‌بندی دیگر استفاده شده است. یک نامک متفاوت انتخاب کنید.

Do not silently invent suffixes.
Do not use CategoryId as fallback.
Do not mutate slug behind user's back.

10 — Translation Completeness

Define a clear UI-level completeness calculation based on required fields.

At minimum:

ایجاد نشده = no translation row

ناقص = translation exists but required fields missing / not publish-ready

کامل = required localized fields present

Do not claim complete based only on row existence.

If publishability rules already exist in domain, align to them.

11 — Create Missing Translation

When locale is ایجاد نشده, the user should get an obvious action:

ایجاد ترجمه

Then show the locale form.

Reasonable convenience:

may prefill slug suggestion from localized name after user enters Name

may optionally copy selected safe non-SEO content only if user explicitly requests; do NOT auto-copy language content

12 — Save / Cancel

VIEW/EDIT behavior is mandatory.

For General:

VIEW -> Edit -> Save/Cancel -> VIEW

For Translation:

VIEW -> Edit selected locale -> Save/Cancel -> VIEW

On save:

use real API

show localized success

update translation status

update Tree label only if current UI locale Name changed

preserve Tree selection/expanded state

preserve route/deep link

On cancel:

discard unsaved changes

return to VIEW

preserve selected locale/category

13 — Dirty State

Protect unsaved changes when:

switching locale

switching tab

selecting another category

navigating browser away where practical

Use established Tooba confirmation pattern.

Do not over-prompt when form is clean.

14 — Authorization

Use existing SpiceDB-centered backend authorization.

Required:

view-only user can inspect General + Translations

edit action appears only when allowed

mutation remains backend-authoritative

no frontend-only security assumptions

If translation editing needs a separate permission in existing authorization model, respect it.
Do not invent broad admin bypass.

15 — Canonical AppDataGrid Lock

No grid is required for this task.

If any grid is introduced for an auxiliary view:
USE ONLY:
src/frontend/design-system/app-data-grid/AppDataGrid.tsx

Raw AgGridReact is forbidden.

16 — API Integration

Use real T004/T005 APIs.

Expected:

category workspace summary

update category core

get translation(s)

upsert translation

move parent if parent changed

route preview derived from canonical slug

If a narrow API gap exists:

add minimal module-owned endpoint/contract

no cross-module SQL JOIN

no generic dynamic repository

17 — Form Architecture

Use the approved reusable View/Edit form pattern.

Do not create separate duplicate forms for View and Edit if a clean shared field-definition/presentation model is possible.

But do NOT force a generic framework that makes Category code harder to understand.

Simple reusable primitives are preferred:

form mode

field presentation

validation message

locale status

save bar/actions

18 — Accessibility

Required:

form labels

keyboard navigation

visible focus

error association

icon-only actions have tooltip/aria-label

color is not the only status signal

RTL layout correct

mobile touch targets appropriate

19 — Mobile

On mobile:

Category Workspace remains full page

General and Translation sections are readable

locale switcher must not overflow badly

sticky Save/Cancel footer/header is allowed if it improves usability

no desktop side-by-side form compression

20 — Tests

Frontend tests at minimum:

General VIEW default

General EDIT

save/cancel

status localization

parent selector without IDs

invalid parent blocked

Translation tab loads

locale switching

missing translation

create translation

incomplete/complete status

Name/Slug editing

slug preview clean

no CategoryId suffix

duplicate slug message

dirty locale-switch protection

view-only user cannot edit

editor can edit

Tree selection/expanded state preserved after current-locale Name save

mobile rendering

Backend if touched:

full suite

translation upsert

duplicate slug

slug history

route resolution

parent move validation

21 — Validation

Frontend:

typecheck

lint

tests

production build

Backend if touched:

restore

build

full tests

Require:

0 errors

0 failed

0 skipped

no new warnings

Always:

git diff --check

22 — Runtime

Keep alive:

Backend :5088

Frontend :3000

Shopeiva :3001

Preview:
http://localhost:3000/fa/admin/catalog/categories

23 — Git

Commit + push.

Require:

HEAD == origin/main

tracked tree clean

unrelated user changes preserved

24 — Acceptance

PASS only if ALL:

approved Category Tree remains visually/behaviorally unchanged

General tab is real, not placeholder

Translation tab is real, not placeholder

explicit VIEW and EDIT modes preserved

General fields actually editable with permissions

status labels are localized

parent selector is user-friendly and ID-free

multi-language UI clearly represents ONE category

missing/incomplete/complete translation states are understandable

missing translation can be created

user edits only human slug

clean canonical URL preview shown

no visible CategoryId suffix

duplicate slug error is understandable

slug history preserved

dirty-state safe

save/cancel predictable

Tree context preserved after save

no raw AgGridReact

backend authorization remains authoritative

full validation green

runtime alive

Git synchronized

USER_VISUAL_ACCEPTED remains NO until manual user review.

Result Contract

Return ONLY:

PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_WORKER_RESULT

Task-ID:
TB-P07-T006
Parent-Task:
TB-P07-T005-R1
Channel:
tooba-main
Status:
PASS | FAIL | BLOCKED | RECOVERY_CONFLICT

Summary:
...

Tree-Lock:

AppCategoryTree touched:

visual changed:

behavior changed:

General:

VIEW:

EDIT:

fields:

parent selector:

status localization:

media:

save:

cancel:

dirty-state:

Translations:

locale source:

locale switcher:

statuses:

missing translation:

create translation:

fields:

VIEW:

EDIT:

save:

cancel:

dirty locale switch:

Slug:

human-only:

ID suffix:

auto-suggest:

canonical preview:

duplicate handling:

history preserved:

Authorization:

SpiceDB path:

view-only:

edit:

backend authoritative:

API:

existing APIs:

backend touched:

endpoints/contracts added:

gaps:

Canonical-AppDataGrid:

used:

raw AgGridReact:

new grid wrapper:

Validation:

frontend typecheck:

frontend lint:

frontend tests:

frontend build:

backend restore:

backend build:

backend full tests:

passed:

failed:

skipped:

warnings:

new warnings:

git diff --check:

Runtime:

Backend:

Frontend:

Shopeiva:

Category Admin:

kept alive:

Git:

branch:

commit:

push:

final HEAD:

origin/main:

synchronized:

tracked tree:

unrelated user changes:

Visual-Concerns:
...

Architectural-Concerns:
...

Blockers:
...

END_TOOBA_WORKER_RESULT

After Result:

Worker -> IDLE / Waiting

keep runtimes alive

do not invent T007

do not add extra prose

END_TASK