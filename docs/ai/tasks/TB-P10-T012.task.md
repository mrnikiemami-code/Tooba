PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_TASK

Task-ID:
TB-P10-T012

Parent-Task:
TB-P10-T011

Channel:
tooba-main

WorkerId:
tooba-worker-01

AgentType:
cursor

Status:
ISSUED

USER_VISUAL_ACCEPTED=NO

Title:
Landing Page Composer — Professional Admin UX, Section Editing, Preview, Publish, and Storefront Section Rendering

Architect Decision

TB-P10-T011 is Architect-ACCEPTED.

Accepted baseline now includes:

canonical StoreLandingPage

Draft / Published lifecycle

Store + Locale + Slug routing

Store HomePageId contract

canonical PageSection model

approved typed Section registry

controlled ProductCollection sources

Published-only enabled ordered public projection

LOCK-SF-001…178

This task implements the first real user-facing Landing Page Composer and the matching Storefront section renderer.

The result must be understandable to a normal Store Admin, visually professional, and usable without technical knowledge.

This task DOES implement:

Admin Landing Page list

create/edit Page

clear Page metadata/SEO form

visual Section Composer

add/remove/enable-disable/reorder sections

section-specific forms

controlled pickers for supported data sources

Preview

Publish / Unpublish or Draft/Published transition according to current lifecycle

select Page as Home

Storefront rendering of approved sections

Home rendering when HomePageId points to a Published Page

This task does NOT implement:

Menu builder

free-form HTML/CSS/JS

arbitrary page grid/layout editor

Elementor/Webflow-style designer

page-level custom CSS

custom query builder

template gallery beyond any minimal existing default

TB-P10-T013

UX CONTRACT — CRITICAL

The user explicitly requires:

professional UI

beautiful but not confusing

understandable to ordinary Store Admins

no technical leakage

visual previews where choice is visual

clear saved/unsaved state

clear Publish status

Persian-friendly language

minimal cognitive load

A technically functional but confusing Composer is NOT complete.

Do NOT expose:

Page IDs

SectionType enum keys

internal Product IDs

raw config JSON

SQL/query expressions

technical route internals

registry keys as normal-facing copy

Recovery State

Phase: P10
Last Architect-accepted: TB-P10-T011
Last Implementation: TB-P10-T011
Current Task: TB-P10-T012
Worker: tooba-worker-01
Expected branch: main
Expected previous HEAD: 2e1aa53969628bec0b0ae7a69f86b9c1ea34c20b
USER_VISUAL_ACCEPTED=NO

Preserve LOCK-SF-001…178.

1. Recovery Preflight

Record:

branch

HEAD

origin/main

git status --short

Recovery/SoT

Host :5088

FE :3000

current Store appearance state

unrelated local user-authored changes

Unexpected tracked divergence => RECOVERY_CONFLICT/BLOCK.

Evidence:
docs/evidence/TB-P10-T012/recovery-start.md

2. Mandatory Discovery — Admin UX + Renderer Reuse

Inspect targeted code only.

Determine:

canonical Admin navigation/settings/content workspace

existing DataGrid/list conventions

form, drawer, modal, toast, dirty-state patterns

searchable entity pickers

drag/reorder utilities if already present

preview patterns

current Home section components that can be reused

current Storefront section renderers

whether Hero/Product/Category/Brand/Article/Review components already exist and can consume PageSection projection

Do NOT build duplicate visual components when existing Storefront components can be parameterized safely.

Evidence:
docs/evidence/TB-P10-T012/composer-discovery.md

3. Admin Landing Page List

Add a professional Landing Pages workspace in canonical Store Admin navigation.

Required list fields:

عنوان

آدرس صفحه

زبان

وضعیت

صفحه اصلی؟

آخرین ویرایش

actions

Required actions:

ایجاد صفحه

ویرایش

پیش‌نمایش

انتشار / بازگرداندن به پیش‌نویس according to lifecycle

انتخاب به‌عنوان صفحه اصلی where eligible

UX:

empty state understandable

no raw enum/key copy

clear status badges

responsive

RTL correct

Use existing DataGrid only if it improves UX and matches current Admin standards; do not force an oversized grid for a simple list.

4. Create / Edit Page Metadata

Page editor header/details must support:

عنوان

slug with friendly label such as «آدرس صفحه»

locale/language

Draft/Published status

SEO title

SEO description

Required:

inline validation

reserved slug message understandable

duplicate slug message understandable

Save state clear

no raw backend error codes shown directly

Do NOT expose technical PageId.

5. Section Composer — Main UX

The Composer should feel like controlled Lego blocks, not a developer form.

Required layout:

page sections shown in current order

each section has human-readable name

concise summary of configured source/content

enable/disable

edit

duplicate only if safe and useful; otherwise omit

delete with confirmation

reorder

Reorder UX:

drag/drop if existing reliable library/pattern exists

also keyboard/buttons up/down or equivalent accessible fallback

immediate local ordering

save through canonical reorder API

stable Section IDs preserved

Avoid dense nested forms on one screen.

Prefer editing a section in:

side panel/drawer
or

focused modal
using existing Admin pattern.

6. Add Section Experience

“افزودن بخش” must open a visual, understandable chooser.

Show approved sections that are actually supported:

Hero

ProductCollection

CategoryGrid

BrandStrip

PromoBanner

ArticleList

Reviews

RichText

Each option must have:

Persian label

one-line explanation

simple icon/thumbnail/mini-preview if feasible

no SectionType key exposed

If a registered section cannot yet render reliably, do not show it as available.

7. Section-Specific Forms

Build simple typed forms, not a generic JSON editor.

Hero

Friendly inputs for current supported Hero content:

title

subtitle/description where supported

CTA label/link where supported

media selection/reference where current media architecture supports it

ProductCollection

User chooses source from human labels:

انتخاب دستی

دسته‌بندی

برند

جدیدترین‌ها

Then:

Manual -> searchable Product picker

Category -> Category picker

Brand -> Brand picker

Newest -> no extra technical fields

count/take -> friendly bounded selector

Do NOT show unsupported BestSelling/Featured/Discounted yet.

CategoryGrid

Use searchable Category picker and bounded display options only if renderer supports them.

BrandStrip

Use Brand picker.

ArticleList

Use only currently supported content source modes discovered in T011.

Reviews

Use existing approved review/testimonial source; no fake manual review authoring unless domain already supports it.

PromoBanner / RichText

Only controlled text/media/link fields.
No raw HTML editor.

8. Searchable Pickers

Product/Category/Brand/Article selection must be normal-user friendly.

Required:

search by human name/title

show meaningful identity context

no manual ID entry

Store-scoped results

loading / empty / error states

selected items visibly removable/reorderable where order matters

Reuse existing picker/search APIs if present.

No new general-purpose query language.

9. Storefront Section Renderer

Implement one canonical Section renderer/dispatcher for public Landing Pages.

Required:

approved SectionType -> canonical renderer

reuse accepted Storefront components where possible

one rendering contract

no arbitrary component name from DB

unknown/unavailable type fails safely

enabled ordered sections only

Rendering should respect:

current PaletteKey

ThemeMode

ProductCardSkin

RTL

responsive behavior

No page-local theme system.

10. HomePage Rendering

If Store.HomePageId points to an eligible Published Landing Page:

canonical Home route renders that Page composition

If unset:

existing accepted Home remains unchanged

If selected Home becomes invalid/unpublished:

safe fallback to accepted canonical Home or defined safe behavior

no blank homepage

no cross-Store resolution

This must be deterministic and documented.

11. Preview

Provide useful Preview before Publish.

Preferred:

preview route/token tied to authenticated Admin

Draft preview allowed only to authorized Store Admin

public users still cannot resolve Draft page

Preview must:

render actual Storefront section renderer

use current unsaved or saved draft according to safest architecture

clearly indicate Preview mode

not be indexable

not expose Draft to public

Avoid building a separate fake preview renderer.

12. Publish Flow

Required:

clear Draft vs Published status

publish action understandable

save before publish if needed

confirmation only where useful

publish invalidates relevant Page/Home cache

Page becomes publicly resolvable immediately

no rebuild/redeploy

unpublish returns public route to 404 unless it is selected Home, in which case apply safe Home fallback contract

No hidden automatic publish.

13. Select as Home

In Admin, allow selecting an eligible Published Page as Home.

UX:

clear action

indicate current Home

if Draft, explain that it must be Published first

no cross-Store choice

safe confirmation if replacing existing custom Home

Do NOT force every Store to use custom Home.

14. Seed / Demo Data — REQUIRED

The user explicitly wants to inspect the result at the end.

Seed a small professional demo set for the development Store using canonical seed mechanism, not ad-hoc runtime SQL.

Create at least:

1 published demo Landing Page

1 draft demo Landing Page

representative supported sections

one ProductCollection using real seeded products

one Category/Brand/content section where data exists

Seed must be:

development/demo-safe

idempotent

easy to identify

not destructive

not required in production unless existing environment seeding conventions say so

This is NOT the final visual-acceptance seed pack; it is enough to inspect Composer and renderer now.

15. Visual Quality Requirements

Admin Composer must visually feel production-grade.

Required:

clear hierarchy

Persian labels

consistent spacing

section cards easy to scan

selected/disabled state obvious

actions not cluttered

mobile/responsive reasonable

no technical identifiers

no empty placeholder previews

no raw backend messages

Storefront Landing:

visual sections should reuse current Shopeiva-aligned components

no broken spacing between sections

no duplicate headings/empty bands

no section rendering skeleton left unfinished

USER_VISUAL_ACCEPTED remains NO until user review.

16. Minimal Useful Tests Only

Do NOT create a large test matrix.

Essential tests only:

Admin can create Page and add one valid Section

ProductCollection controlled source saves/resolves

reorder preserves Section IDs/order

Draft preview authorized but Draft public 404

publish makes route public without rebuild

HomePage selection renders selected Published Page; unset still renders canonical Home

disabled Section not rendered

cross-Store picker/reference blocked

unknown/invalid Section config rejected

recovery guard + git diff --check

Reuse existing T010/T011 tests for routing/isolation instead of duplicating them.

Visual behavior should be proven mainly with runtime screenshots, not dozens of unit tests.

17. Runtime — REQUIRED

Use:

Host :5088

FE :3000

Run real browser flow:

A. open Landing Pages Admin
B. create Draft page
C. add Hero
D. add ProductCollection using a supported source through UI
E. add Category/Brand/Article or another supported section
F. reorder
G. disable one section
H. Preview Draft
I. verify public Draft URL 404
J. Publish
K. public URL renders actual sections
L. choose Page as Home
M. /fa renders selected Landing composition
N. switch palette and verify Landing follows palette
O. switch Dark/UserChoice and verify Landing follows theme
P. switch ProductCardSkin and verify ProductCollection follows skin
Q. unset/restore Home -> canonical Home returns
R. leave demo seeded Pages available for later user inspection if seed contract supports it

Capture persistent screenshots:

page-list.png

page-editor.png

add-section.png

product-section-editor.png

composer-reordered.png

draft-preview.png

published-landing.png

custom-home.png

landing-dark.png

landing-alt-palette.png

Screenshots must persist under:
docs/evidence/TB-P10-T012/screenshots/

18. Performance

Required:

no N+1 section load

no unbounded product collection

no Admin polling

no Storefront polling

no duplicate Page/Section fetch per component

Page projection/caches invalidated on relevant writes

section rendering should use bounded queries/projections

Do not over-engineer.

19. Anti-Pattern Gate

Scan for:

generic JSON editor

raw SectionType keys in visible UI

manual product/category/brand ID entry

arbitrary HTML/CSS/JS

SQL/query builder

separate fake Preview renderer

duplicate Storefront section components when canonical ones exist

drag/drop timing hacks

delete/recreate reorder

page-local theme state

polling

public Draft exposure

Home blank when selected Page invalid

physical page generation/redeploy

Any real occurrence => REPAIR.

Evidence:
docs/evidence/TB-P10-T012/antipattern-scan.md

20. Lock Registry

Add non-duplicate locks after LOCK-SF-178 equivalent to:

Store Admin Landing/Page Composer must be human-readable and must not expose technical IDs, enum keys, JSON, SQL, or route internals.

Section editing uses typed section-specific forms and Store-scoped searchable pickers, never raw config editing.

Draft preview is authorized/non-indexable and must not make Draft publicly resolvable.

Public Landing rendering uses one canonical approved Section renderer and inherits Store PaletteKey, ThemeMode, and ProductCardSkin.

Selecting a Published Landing Page as Home replaces Home composition only through canonical HomePageId; unset/invalid selection falls back safely to canonical Home.

Publishing/unpublishing and Page/Section writes invalidate only relevant Page/Home caches and never require rebuild/redeploy.

Composer reorder preserves stable PageSection identity and must remain accessible beyond pointer-only drag/drop.

Use next canonical LOCK-SF numbers.
Do not renumber existing locks.

21. Evidence

Create:
docs/evidence/TB-P10-T012/

Required:

recovery-start.md

composer-discovery.md

admin-page-list.md

composer-ux.md

section-forms-pickers.md

storefront-renderer.md

preview-publish.md

home-selection.md

seed-demo.md

focused-validation.md

runtime.md

performance.md

antipattern-scan.md

recovery-sot.md

screenshots/

Keep evidence concise and visual where appropriate.

22. Recovery / Pipeline

At completion:

Phase P10

Last Architect-accepted before review: TB-P10-T011

Last Implementation: TB-P10-T012

Current Issued/Repair: none

USER_VISUAL_ACCEPTED=NO

Worker IDLE

branch main

ending HEAD

HEAD==origin/main

task-owned clean

unrelated local user changes preserved

do NOT invent/start TB-P10-T013

Run recovery staleness guard.

Acceptance

PASS only if ALL:

professional Landing Page list exists

Page create/edit metadata UX is clear

visual Add Section chooser exists

section-specific forms exist

Product/Category/Brand/etc use searchable pickers, not IDs

no raw enum/JSON/technical leakage

reorder/enable-disable/delete work

one canonical Storefront Section renderer exists

Draft preview works while public Draft remains 404

Publish exposes Page immediately without rebuild

HomePage selection renders published Landing composition

unset/invalid Home safely restores canonical Home

Landing inherits Palette/Theme/ProductCardSkin

demo/seed Pages exist for inspection

persistent screenshots prove UI/runtime

only essential tests added

no polling/N+1/unbounded collection

anti-pattern scan CLEAN

Lock registry updated without duplicates

Recovery exact

HEAD==origin/main

Worker IDLE

no TB-P10-T013

USER_VISUAL_ACCEPTED remains NO

Result Contract

Return ONLY canonical BRIDGE-WAKE-V1 Result with:

Summary
Applicable-Locks
Recovery-Start
Composer-Discovery
Admin-Page-List
Composer-UX
Section-Forms-Pickers
Storefront-Renderer
Preview-Publish
Home-Selection
Seed-Demo
Focused-Validation
Runtime
Performance
AntiPattern-Scan
Lock-Registry
Recovery-SoT
Git
Architectural-Concerns
Visual-Concerns
Blockers

END_TOOBA_TASK