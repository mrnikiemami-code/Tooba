PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

TASK-ID: TB-P05-T026-R1
PROJECT: Tooba
PHASE: P05 — Operational Surface Integration
CHANNEL: tooba-main
STATUS: ISSUED
TASK-TYPE: GATE REPAIR + SIDE-BY-SIDE LIVE VISUAL REVIEW
PARENT-TASK: TB-P05-T026
WORKER-POLICY: ONE WORKER = ONE ACTIVE TASK

Title

P05 Gate Repair — Run Tooba + Original Shopeiva Side-by-Side First, Resolve NU1900, Prepare User Home/PDP Review

Architect Decision

TB-P05-T026 Worker Result is NOT Architect-accepted.

Reasons:

The user explicitly says the current Tooba Home is still visually wrong.
Therefore Worker-reported Home = MATCH is NOT sufficient visual acceptance.

TB-P05-T026 required:

warnings = 0
errors = 0
failed = 0
skipped = 0

but Worker Result still reported:

NU1900 NuGet advisory-fetch warning

Therefore:

TB-P05-T026 = REPAIR_REQUIRED
P05 = AWAITING_ARCHITECT_GATE
Critical User Instruction

THIS MUST HAPPEN FIRST, BEFORE ANY CODE CHANGE:

1. Start Tooba Backend
2. Start Tooba Frontend
3. Start ORIGINAL purchased Shopeiva storefront runtime
4. Keep all three running simultaneously
5. Verify all three are reachable
6. Record exact URLs

The user wants to personally compare:

Original Shopeiva Home
vs
Current Tooba Home

and:

Original Shopeiva PDP
vs
Current Tooba PDP

Do NOT make another guessed Home redesign before user feedback.

Objective

This Repair Task has two parallel-but-safe goals:

A. Make side-by-side live visual inspection possible for the user.

B. Resolve the remaining technical P05 gate failure:
NU1900 must be zero during required backend validation.

The project must continue moving forward while preserving the user's ability to inspect the real template.

Repository Recovery

Run:

git rev-parse --show-toplevel
git fetch origin
git branch --show-current
git rev-parse HEAD
git rev-parse origin/main
git status --short --branch

Expected predecessor:

9c14494eb2ee54cfae4f042ce25666295ba39f07

Require:

branch = main

HEAD == origin/main

safe/known tracked tree

If material conflict:

RECOVERY_CONFLICT

No force push.
No destructive reset.
No silent stash.

PART A — START ALL RUNTIMES FIRST

This is mandatory and must happen BEFORE changing any tracked file.

A1 — Tooba Infrastructure

Ensure required infrastructure is running:

Docker Desktop if needed

PostgreSQL

SpiceDB only if required for validation/runtime

A2 — Tooba Backend

Start repository-supported Host.

Verify health.

Expected current known URL may be:

http://127.0.0.1:5088

Do not assume; use actual runtime configuration.

A3 — Tooba Frontend

Start repository-supported Next dev runtime.

Expected current known URL may be:

http://127.0.0.1:3000

Do not assume; verify actual.

A4 — ORIGINAL SHOPEIVA STOREFRONT

Start the actual purchased Shopeiva source/runtime.

Use repository/reference discovery; do not rely on a single absolute machine path.

Known source may exist under a repository-relative/reference path such as:

reference/shopeiva

or the previously used purchased source location.

Discover from repository/docs if location differs.

Use the actual Shopeiva source, not screenshots, clones, or Tooba.

Run the original storefront on a non-conflicting port.

Prefer the already proven development port if available:

3017

but verify actual assigned port.

A5 — Simultaneous Reachability

All must remain available at the same time:

Tooba Backend
Tooba Frontend
Original Shopeiva Frontend

Verify HTTP response for each.

Create:

docs/evidence/TB-P05-T026-R1/01-three-runtime-start-proof.md

Record:

command

working directory

PID

port

URL

health/result

startup timestamp

PART B — IMMEDIATE SIDE-BY-SIDE USER PREVIEW PREPARATION

Before doing any repair work, determine exact working routes.

Required:

TOOBA:
Home
PDP

ORIGINAL SHOPEIVA:
Home
PDP

Use a real Tooba demo product and a real Shopeiva product route.

Create:

docs/evidence/TB-P05-T026-R1/02-side-by-side-preview-routes.md

Record exact URLs.

Capture proof only; DO NOT alter Home yet:

03-original-shopeiva-home-live.png
04-current-tooba-home-live.png
05-original-shopeiva-pdp-live.png
06-current-tooba-pdp-live.png
IMPORTANT HOME FREEZE

Until the user gives direct visual feedback:

DO NOT redesign Home
DO NOT reinterpret Home
DO NOT "improve" Home
DO NOT reorder Home sections
DO NOT change category layout
DO NOT change hero
DO NOT change product rails
DO NOT change spacing/density

Home is frozen for implementation in this Repair Task.

The purpose is to let the user identify exact visual differences.

PART C — SIDE-BY-SIDE DIFFERENCE MAP WITHOUT CHANGING HOME

Worker may inspect and document differences, but must not fix visual Home differences yet.

Create:

docs/evidence/TB-P05-T026-R1/07-home-side-by-side-observation-map.md

For each visible section:

Original Shopeiva structure
Current Tooba structure
Observed difference
Severity:
- NONE
- MINOR
- MATERIAL
User confirmation required:
YES/NO

Do not declare MATCH globally.

For Home specifically, final status in this Task must be:

AWAITING_USER_VISUAL_FEEDBACK

unless user feedback arrives through the active pipeline before Result and can be safely incorporated.

PART D — PDP SIDE-BY-SIDE REVIEW

PDP may be inspected but should also not receive speculative redesign.

Compare:

top block

gallery

variants

buy panel

sticky tabs

each distinct tab

other sellers

related products

mobile

Create:

docs/evidence/TB-P05-T026-R1/08-pdp-side-by-side-observation-map.md

If no user complaint is known for PDP, do not change visual structure unless there is an obvious runtime regression.

PART E — RESOLVE NU1900 FOR REAL

The previous Gate still had:

NU1900

This must be eliminated during required backend validation.

Use the accepted NuGet connectivity recovery pattern from earlier tasks.

Do NOT:

disable NuGetAudit

globally suppress NU1900

weaken TLS

hard-code secrets

mark warning as acceptable

Diagnose current environment:

DNS

proxy

NuGet feed connectivity

advisory endpoint

local CONNECT proxy helper if repository-supported

environment variables

transient GitHub/NuGet networking

Use the existing accepted helper/process if still valid.

Create:

docs/evidence/TB-P05-T026-R1/09-nuget-zero-warning-proof.md
PART F — BACKEND FULL VALIDATION

Run:

dotnet restore src/backend/Tooba.slnx
dotnet build src/backend/Tooba.slnx
dotnet test src/backend/Tooba.slnx

Required final result:

warnings = 0
errors = 0
failed = 0
skipped = 0

If NU1900 remains:
return BLOCKED.

Do not claim PASS with any warning.

PART G — FRONTEND REGRESSION VALIDATION

Do not alter Home/PDP visual structure.

Run:

cd src/frontend
npm run typecheck
npm run lint
npm run test:critical-storefront
npm run test:storefront
npm run test:customer
npm run test:seller

Run Admin/listing/cart/checkout suites where present.

Then:

npm run build
IMPORTANT NEXT DEV RECOVERY

Previous evidence shows:

next build while next dev is running
may invalidate/recreate .next
and cause runtime Home 500

Therefore after build:

stop/restart Tooba Frontend dev runtime if necessary

re-verify Home 200

re-verify PDP 200

keep it running

Also ensure original Shopeiva remains running.

PART H — KEEP ALL RUNTIMES RUNNING AT RESULT

Mandatory if technically possible.

At Result time, leave running:

Tooba Backend
Tooba Frontend
Original Shopeiva Frontend

Verify reachability again.

Create:

docs/evidence/TB-P05-T026-R1/10-final-three-runtime-proof.md
PART I — USER PREVIEW URL CONTRACT

Result MUST contain a dedicated section exactly named:

USER-SIDE-BY-SIDE-PREVIEW

It must contain actual working URLs:

Tooba Backend Health:
...

Tooba Home:
...

Tooba PDP:
...

Original Shopeiva Home:
...

Original Shopeiva PDP:
...

No placeholders.

The goal is for the user to open both stores immediately and tell Architect what is visually wrong.

PART J — DO NOT CLOSE P05 YET

Because Home visual acceptance is explicitly waiting for the user:

Expected SoT:

TB-P05-T026 = REPAIR_REQUIRED
TB-P05-T026-R1 = AWAITING_ARCHITECT_ACCEPT
HOME_VISUAL_ACCEPTANCE = AWAITING_USER_FEEDBACK
P05 = AWAITING_ARCHITECT_GATE

Worker must NOT mark P05 complete.

PART K — MAIN WORK CONTINUES WITHOUT HOME GUESSING

While Home is frozen, the Worker should complete all non-visual gate repair work in this Task:

NU1900 resolution

full backend zero-warning validation

frontend regression validation

runtime stability

side-by-side route preparation

evidence

SoT

Do not start P06 implementation.

This satisfies the user's instruction to keep work moving while they inspect Home.

Evidence

Create:

docs/evidence/TB-P05-T026-R1/

Required:

01-three-runtime-start-proof.md
02-side-by-side-preview-routes.md
03-original-shopeiva-home-live.png
04-current-tooba-home-live.png
05-original-shopeiva-pdp-live.png
06-current-tooba-pdp-live.png
07-home-side-by-side-observation-map.md
08-pdp-side-by-side-observation-map.md
09-nuget-zero-warning-proof.md
10-final-three-runtime-proof.md
11-full-validation-proof.md
12-user-feedback-handoff.md

12-user-feedback-handoff.md must state:

Home visual repair is intentionally waiting for direct user feedback.
No speculative Home redesign was performed in this Repair Task.
Git

If tracked docs/evidence/config changes exist:

git diff --check
git status --short --branch
git add ...
git commit -m "test prepare Shopeiva side-by-side review and repair P05 gate [TB-P05-T026-R1]"
git push origin main
git fetch origin
git rev-parse HEAD
git rev-parse origin/main
git status --short --branch

Require:

HEAD == origin/main

tracked tree clean

Local runtime logs may remain untracked if documented.

Acceptance Criteria

PASS requires ALL:

Tooba Backend started before work

Tooba Frontend started before work

Original Shopeiva started before work

all three reachable simultaneously

exact side-by-side URLs returned

original Shopeiva Home visible

current Tooba Home visible

original Shopeiva PDP visible

current Tooba PDP visible

Home NOT speculatively modified

Home status = AWAITING_USER_VISUAL_FEEDBACK

NU1900 = 0

backend warnings = 0

backend errors = 0

backend failed = 0

backend skipped = 0

frontend regressions green

runtimes restarted after build as needed

all three runtimes still available at Result

P05 NOT closed

repo synchronized

Result Contract

PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_WORKER_RESULT

Task-ID:
TB-P05-T026-R1

Channel:
tooba-main

Status:
PASS | FAIL | BLOCKED | RECOVERY_CONFLICT

Summary:
...

Runtime-Start-Before-Work:

Tooba Backend:

Tooba Frontend:

Original Shopeiva:

all three reachable:

Side-by-Side:

Tooba Home:

Original Shopeiva Home:

Tooba PDP:

Original Shopeiva PDP:

Home:

visual changes made:

global MATCH claimed:

observation map:

status:
AWAITING_USER_VISUAL_FEEDBACK

PDP:

visual changes made:

observation map:

runtime status:

NuGet:

NU1900:

advisory access:

audit disabled:

TLS weakened:

Backend-Validation:

restore:

build:

tests passed:

warnings:

errors:

failed:

skipped:

Frontend-Validation:

typecheck:

lint:

critical-storefront:

storefront:

customer:

seller:

admin/listing/cart/checkout:

build:

Runtime-After:

Tooba Backend running:

Tooba Frontend running:

Original Shopeiva running:

Tooba Backend PID:

Tooba Frontend PID:

Shopeiva PID:

USER-SIDE-BY-SIDE-PREVIEW:

Tooba Backend Health:

Tooba Home:

Tooba PDP:

Original Shopeiva Home:

Original Shopeiva PDP:

Evidence:

01:

02:

03:

04:

05:

06:

07:

08:

09:

10:

11:

12:

Source-of-Truth:

T026:

T026-R1:

HOME_VISUAL_ACCEPTANCE:

P05:

Git:

commit:

push:

final HEAD:

origin/main:

synchronized:

tracked tree:

local runtime logs:

Architectural-Concerns:
...

Visual-Concerns:
...

Blockers:
...

END_TOOBA_WORKER_RESULT

After Result:

return to IDLE

do not self-issue another Task

leave all three runtimes running if technically possible

END_TASK