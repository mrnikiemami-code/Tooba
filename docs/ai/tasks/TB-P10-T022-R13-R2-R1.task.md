PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_TASK

Task-ID:
TB-P10-T022-R13-R2-R1

Parent-Task:
TB-P10-T022-R13-R2

Channel:
tooba-main

WorkerId:
tooba-worker-01

AgentType:
cursor

Status:
ISSUED

Appearance-USER_VISUAL_ACCEPTED=YES
Builder-USER_VISUAL_ACCEPTED=NO

Title:
Product Showcase Variant Visual Closure — Prove Real Admin Integration and Preserve User Fixes

Architect Review

TB-P10-T022-R13-R2 is technically strong, but NOT fully accepted yet.

Reason:
Worker reported that Admin auth cookies were unavailable and the visual PNGs were captured through an evidence page mounting the production VariantLivePreview/Embla renderer, rather than through the actual Admin Appearance/Review flow.

That is not enough for final Builder visual acceptance because the user explicitly asked that the five new designs be added into the EXISTING:
ویرایش بخش → ظاهر
space without redesign.

A second critical concern is preservation of user work.

Worker reported:
Pre-task local hero/selector/grid work committed and pushed as 18ca10c9 (preserved).

This repair must explicitly prove that the user’s pre-existing bug fixes were preserved and are still present in current HEAD.

Do NOT redesign anything.
Do NOT add new variants.
Do NOT touch ProductCard.
Do NOT alter existing variants.
Do NOT enter P11/Admin roadmap.
Do NOT touch Canonical Home reset.
Do NOT invent TB-P10-T023.

Canonical Start

Last Architect-accepted:
TB-P10-T022-R13-R1-R1

Last Implementation:
TB-P10-T022-R13-R2

Expected previous HEAD:
a08092bd3c3d1984e149db96f9b39a161766524f

Important preserved user-work commit:
18ca10c9

Locks:
LOCK-SF-001…380

Builder:
USER_VISUAL_ACCEPTED=NO

Worker Protocol

No heartbeat.
No polling.
No IDLE loop.

After canonical Result through Bridge:
STOP completely.

1. Recovery / User-Work Preservation — MUST DO FIRST

Verify:

branch main

HEAD == origin/main

commit 18ca10c9 is an ancestor of current HEAD

identify exactly which files/change groups from the user’s pre-task work were included in 18ca10c9

verify those changes are still present and not overwritten by R13-R2

verify no user-owned pre-existing files were reset/reverted/deleted

unrelated .tmp-* / stashes remain untouched

Do NOT modify user work merely to make the task cleaner.

If user work was overwritten:

STOP

return RECOVERY_CONFLICT

do not attempt silent reconstruction.

Evidence:
docs/evidence/TB-P10-T022-R13-R2-R1/user-work-preservation.md

2. Real Admin Route Visual Proof — CRITICAL

Do NOT use a standalone evidence/demo page as substitute for Admin integration proof.

Capture from the actual Admin route and actual existing wizard:

ویرایش بخش → ظاهر

for Product Showcase.

If authentication is required:

use the project’s legitimate dev/test auth mechanism

do not bypass authorization in product code

do not disable auth

do not add fake auth cookies

do not hardcode a privileged session

If current test harness already has a supported authenticated state, use it.

If actual Admin capture is genuinely impossible due to environment-only auth tooling:

prove DOM/runtime integration via the real route using the supported dev auth fixture,

otherwise return BLOCKED rather than substituting a demo page.

3. Exact Picker Integration Proof

In the real Product Showcase Appearance step, prove:

Existing variants are still present and unchanged.

Five new variants are also present:

سانی

مانی

سینمایی

سینمایی پلاس

کاشف

The new cards must:

use the same existing picker layout

not redesign the picker

use current selection UX

show actual live production preview

use human names

preserve current descriptions/metadata style

Required screenshot:
admin-product-showcase-picker-real-route.png

4. Real Admin Selection Flow

Using actual Admin wizard:

A. open Product Showcase section
B. go to Appearance step
C. select سانی
D. advance
E. return to Appearance — selection persists
F. select سینمایی
G. Review step renders actual selected variant
H. save
I. reopen section — same variant selected

Repeat focused persistence proof for at least one more new variant.

5. Real Review-Step Proof

Capture actual Admin Review step, not evidence page.

Required screenshot:
admin-product-showcase-review-real-route.png

It must show:

selected Persian design name

real production Product Showcase preview

current source/settings summary

no geometric fallback

6. Existing Variant Preservation Proof

From real Admin picker verify existing variants such as:

آریا

زهره

ماهور

شگفت‌انگیز
and all current registered Product Showcase variants remain available.

Do not rely only on registry test.

Required screenshot:
admin-product-showcase-existing-variants-preserved.png

7. Storefront Proof for Selected New Variants

Use actual Page save + Storefront render flow.

At minimum prove:

sunny

cinematic

cinematic-plus

explorer

For each:

Admin selection persists

Storefront renders the matching variant

ProductCard actions remain intact

RTL correct

No need to recapture all five if previous storefront evidence is already valid and current hash/renderer unchanged; but at least 2 variants must be re-proven end-to-end Admin → Save → Storefront in this repair.

8. SSR / ProductCard Regression

Re-run focused guards:

initial HTML includes product title/price/link/image markup

ProductCard component remains unchanged in business behavior

no client-only product list

no duplicate fetch

no Product API/schema change

9. Embla Scope Regression

Verify:

Embla remains scoped to new Product Showcase rail implementation

Hero Slider still uses Swiper

existing Product Showcase Swiper variants remain unchanged

no second global carousel migration happened

10. No Redesign Gate

Compare current Admin picker layout with pre-R13-R2 structure.

Allowed:

five additional variant cards

their descriptions/previews

Not allowed:

new picker shell

changed wizard navigation

changed card layout system

changed ProductCard

reordered unrelated existing variants without reason

new settings unrelated to these variants

Document:
docs/evidence/TB-P10-T022-R13-R2-R1/no-redesign-proof.md

11. Required Visual Evidence

Capture from actual Admin/runtime:

admin-product-showcase-picker-real-route.png

admin-product-showcase-existing-variants-preserved.png

admin-product-showcase-sunny-selected.png

admin-product-showcase-cinematic-selected.png

admin-product-showcase-review-real-route.png

admin-product-showcase-reopen-persisted.png

storefront-product-showcase-sunny-real-flow.png

storefront-product-showcase-cinematic-real-flow.png

storefront-product-showcase-mobile-real-flow.png

Evidence root:
docs/evidence/TB-P10-T022-R13-R2-R1/

12. Focused Validation

Required:

commit 18ca10c9 is ancestor of HEAD

user pre-task changes still present

actual Admin route opens

real Appearance step contains five new variants

existing variants remain

picker layout unchanged except additive cards

real selection persists

real Review step uses production component

save/reopen persists variant

Admin → Storefront flow works for at least 2 new variants

ProductCard behavior unchanged

SSR content intact

Embla scoped only to new variants

Hero Swiper unchanged

critical-storefront PASS

recovery-staleness PASS

git diff --check PASS

13. Anti-Pattern Gate

Reject:

evidence page used as final Admin proof

auth bypass added to product

fake/hardcoded privileged cookie

redesign of picker

ProductCard modifications

existing variant changes

overwriting commit 18ca10c9 user changes

reset/restore/clean of user files

polling/timeouts

P11 implementation

unrelated cleanup

14. Locks

No new locks expected.

LOCK-SF-375…380 already define the feature.

Only add a lock if a genuinely new cross-cutting invariant is discovered.

15. Completion State

At completion:

Phase P10 / Builder Acceptance Repair

Last Architect-accepted before review: TB-P10-T022-R13-R1-R1

Last Implementation: TB-P10-T022-R13-R2-R1

TB-P11-T001 retained for later only

Appearance YES

Builder NO

no TB-P11-T002

no TB-P10-T023

main

HEAD==origin/main

user work preserved

Do NOT write Worker IDLE.
After Result through Bridge:
STOP completely.

Acceptance

PASS only if:

user changes are explicitly proven preserved,

actual Admin route proves additive five-variant integration,

existing variants remain untouched,

Review step is proven in actual Admin,

at least two end-to-end Admin→Storefront saves are proven,

no redesign,

no auth bypass,

Result sent through Bridge,

worker stops.

Result Contract

Return ONLY canonical BRIDGE-WAKE-V1 Result with:

Summary
Applicable-Locks
Recovery-Start
User-Work-Preservation
Admin-Route-Proof
Picker-Integration
Existing-Variant-Preservation
Selection-Persistence
Review-Step-Proof
Admin-To-Storefront
SSR-ProductCard-Regression
Embla-Scope
No-Redesign-Proof
Focused-Validation
Visual-Evidence
AntiPattern-Scan
Lock-Registry
Recovery-SoT
Git
Architectural-Concerns
Visual-Concerns
Blockers

Do not include Worker-IDLE.
After sending Result through Bridge, STOP.

END_TOOBA_TASK