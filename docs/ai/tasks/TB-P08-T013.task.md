PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_TASK

Task-ID:
TB-P08-T013

Parent-Task:
TB-P08-T012-R1

Channel:
tooba-main

Title:
Content Taxonomy & Tags — Two-Level Article Categories + Searchable Hierarchical Picker + Content Tags

Role:
Implement the next P08 Content foundation step after CKEditor acceptance.

This task is ONLY about:

Article Category taxonomy semantics and UX

Article Category picker UX

Content-owned Tags model and Article Tags UX

Do NOT start:

Publication Readiness engine

Preview workflow

History/audit redesign

Comments moderation

Help center

final Publish UX redesign

Preserve:

TB-P08-T011 Language tabs / Draft-first / Author picker / Article locale lock

TB-P08-T012 Media/SEO/Category-save/error repairs

TB-P08-T012-R1 CKEditor 5 Article editor + DAM integration

AppCategoryTree canonical wrapper

AppDataGrid canonical wrapper

Content-owned taxonomy boundaries

existing public locale routing/SEO semantics

user-authored committed changes

USER_VISUAL_ACCEPTED=NO.

Content Article Categories are NOT Catalog Categories.

Article Category hierarchy is locked to MAXIMUM 2 LEVELS:

Level 1:

Main Article Category

selectable directly by Article

Level 2:

Subcategory

child of Level 1

selectable directly by Article

Forbidden:

Level 3+

forcing every Article to use Level 2

treating Level 1 as navigation-only

An Article may select:

one Level 1 Category directly
OR

one Level 2 Category

Article and Category must use the same Language.

Admin management remains based on canonical AppCategoryTree.

External inspiration:
Digikala Mag category UX may be used as taxonomy inspiration only.
Do NOT copy Catalog architecture or storefront markup blindly.

Inspect existing:

content.categories schema/entity

parent relationship

create/update/move commands

tree APIs

article assignment rules

public category routes

existing Content seed/migrations

Enforce maximum depth = 2 in backend authoritative domain logic.

Required:

root/main category => depth 1

child/subcategory => depth 2

creating child below depth 2 rejected

moving a node must not produce depth > 2

moving a Level 1 with child under a Level 2 parent rejected

cycles remain impossible

parent and child must share same Language

Article assignment accepts Level 1 or Level 2

Article/Category Language mismatch rejected with stable machine code

Do NOT rely on frontend only.

Use stable error codes, e.g. repository convention equivalents for:

category.max_depth_exceeded

category.language_mismatch

category.invalid_parent

category.cycle

No raw exception text.

Before migration/enforcement:

inspect whether any current Content Category is deeper than Level 2

If none:

document evidence and enforce directly

If deeper data exists:

DO NOT silently delete/reparent

provide a safe migration/report strategy

task may normalize only deterministic demo/seed data if explicitly safe

real user data requires blocker/concern if no lossless automatic migration exists

Do not break public existing slugs unnecessarily.

Keep AppCategoryTree.

Make the two-level rule obvious and human:

Level 1 action:

add main category

add subcategory

Level 2 action:

edit

delete/archive according to existing semantics

MUST NOT show “add child”

Disable/hide impossible actions instead of allowing a save error later where practical.

Use Persian/English localized labels according to Admin language.

No technical “L1/L2” wording in normal UI.
Human wording:

دسته اصلی

زیردسته

If tree supports drag/drop/move:

prevent invalid depth before mutation when possible

backend still authoritative

Current flat HTML/basic select is not acceptable.

Replace Article Category selection with a professional:

hierarchical

searchable

human-readable
picker.

Requirements:

only Categories in Article Language

Level 1 and Level 2 visually distinguishable

search by translated/display name

show parent context for Level 2

Article may choose Level 1 directly

Article may choose Level 2 directly

no raw Category IDs

bounded/lazy query where practical

clear empty state

inactive/unavailable Category not assignable

historical existing assignment remains readable if later inactive/archived according to existing semantics

Suggested visual examples:
راهنمای خرید
راهنمای خرید › موبایل

Do not create a second Category tree component if AppCategoryTree/shared primitives can be reused.

TB-P08 discovery found no proper ContentTag entity; current TagsCsv / comma-separated UI is not acceptable.

Create a Content-owned Tag model if repository inspection confirms none exists.

Do NOT reuse Catalog Product Tags merely because the word “Tag” exists.

Recommended semantic shape:

ContentTag

Id (UUID v7 repo convention)

Language/Locale

Name

normalized name

slug if public/tag route semantics justify it

active/archive state according to existing Content conventions

audit fields

Article many-to-many assignment

At minimum:

unique normalized tag name per Language

Article/Tag same Language

no cross-module SQL join

no raw CSV persistence as the canonical relationship

If current Article table has TagsCsv:

migrate existing data safely into normalized Content Tags + ArticleTag assignments

trim/deduplicate

preserve readable existing tags

do not lose data

After migration:

TagsCsv must not remain the canonical editing model

compatibility projection may remain temporarily only if justified/documented

Article workspace Tags control must be:

searchable

chip/token based

human-readable

add existing

create a new Tag inline only if user has appropriate Content edit permission

remove assignment safely

no comma-separated text box

no raw IDs

Language scoped:

only tags for Article Language

locale change on pristine Draft must clear/revalidate incompatible Tag assignments

If no dedicated Tag management page is necessary yet, inline create/search is sufficient.
Do NOT invent a large Tags admin module unless required.

Backend authoritative:

non-empty name

reasonable max length based on repo conventions

normalized uniqueness per Language

duplicate assignment idempotent/rejected cleanly

Tag Language must equal Article Language

inactive/archived Tag cannot be newly assigned

Stable localized error mapping.

Do not break:

/fa|en/blogs/{slug}

category public routes

canonical/sitemap behavior

no cross-language fallback

If Tags are already rendered publicly through TagsCsv:

preserve equivalent Article rendering from normalized relationships

Do NOT add public Tag archive routes unless existing product requirements/code clearly already expect them.
If adding tag slugs for future use, they may remain non-routed in this task.

Do not regress T012-R1.

Article body must remain CKEditor 5.
DAM image insertion must remain Tooba Media Library only.
No TipTap return for Article.
No CKEditor cloud/upload path.
Media/SEO/Category save fixes remain.

No normal UI exposure of:

Bad Request

raw machine codes

raw JSON

category.max_depth_exceeded

tag.language_mismatch

duplicate DB exceptions

Map known cases in centralized error map.

Human examples:

«دسته‌بندی مقاله حداکثر می‌تواند دو سطح داشته باشد.»

«زبان دسته‌بندی باید با زبان مقاله یکسان باشد.»

«این برچسب قبلاً ثبت شده است.»

«زبان برچسب باید با زبان مقاله یکسان باشد.»

Category picker:

do not load all languages

query only Article Language

bounded/searchable list/tree

Tags:

server search

bounded result set

no full-tag preload when tag count grows

DB-native filtering

appropriate normalized index

No N+1.

No full repository suites.
No long browser campaign.

Backend focused:

create Level 1

create Level 2

reject Level 3

reject invalid move to Level 3

reject cross-language parent

assign Article to Level 1

assign Article to Level 2

reject Article/Category language mismatch

Tag create/search

duplicate normalized Tag rejection

ArticleTag assign/remove

Article/Tag language mismatch

TagsCsv migration preservation where applicable

Frontend focused:

AppCategoryTree invalid child action absent/disabled

hierarchical searchable Article Category picker

Level 1 selectable

Level 2 selectable

tag chips search/create/remove

no CSV Tag input

localized errors

CKEditor regression source assert

Also:

changed backend compile

relevant frontend typecheck/lint

recovery guard

git diff --check

Quick real runtime smoke required.

fa:

create/find main category

create subcategory

confirm no third-level action

create/open Draft Article

assign main category, save/reload

assign subcategory, save/reload

create/search Tag

assign multiple Tags

remove one Tag

reload and verify persistence

en:

verify Category/Tag picker language isolation

basic save/reload

Attempt invalid cross-language assignment through API or focused test and confirm human/error contract.

No screenshot pack unless user asks.

Update:

Phase: P08

Last Architect-accepted: TB-P08-T012-R1

Current Issued: TB-P08-T013

Current Repair: (none)

Last Implementation on completion: TB-P08-T013

USER_VISUAL_ACCEPTED=NO

Worker Next State: IDLE

do NOT invent/start TB-P08-T014

Run recovery guard.

Create:
docs/evidence/TB-P08-T013/

Required concise evidence:

category-current-state.md

category-two-level-rule.md

category-picker.md

tag-current-state.md

tag-migration.md

tag-picker.md

error-ux.md

runtime-smoke.md

recovery-sot.md

Preserve user-authored changes.

Required:

commit task changes

push origin/main

HEAD==origin/main

no task-owned uncommitted changes

PASS only if ALL:

Content Category maximum depth is backend-enforced at 2

Level 1 and Level 2 are both Article-selectable

no Level 3 create/move possible

Article/Category Language rule enforced

Category Admin tree communicates main/subcategory semantics

Article Category picker is hierarchical + searchable

no raw IDs/basic flat select

proper Content-owned normalized Tags exist

TagsCsv is no longer canonical editing storage

existing TagsCsv data preserved/migrated safely if present

Article Tags UX is searchable chips

inline Tag create works if allowed

Article/Tag Language rule enforced

localized error UX

CKEditor/DAM/T012 repairs preserved

focused validation green

quick runtime smoke completed or explicit blocker documented

recovery guard passes

HEAD==origin/main

Worker returns IDLE

no T014 invented

USER_VISUAL_ACCEPTED=NO.

Return ONLY canonical BRIDGE-WAKE-V1 result with:

Summary
Category-Current-State
Two-Level-Enforcement
Existing-Data-Safety
Category-Admin-UX
Article-Category-Picker
Tag-Current-State
Tag-Domain
Tag-Migration
Tag-Picker
Language-Rules
Public-Compatibility
Error-UX
Performance
Focused-Validation
Runtime-Smoke
CKEditor-Media-Regression
Recovery-SoT
Git
Architectural-Concerns
Visual-Concerns
Blockers

END_TOOBA_TASK
