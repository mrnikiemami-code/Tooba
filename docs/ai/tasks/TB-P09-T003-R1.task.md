PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_REPAIR_TASK

Task-ID:
TB-P09-T003-R1

Parent-Task:
TB-P09-T003

Channel:
tooba-main

WorkerId:
tooba-worker-01

AgentType:
cursor

Status:
REPAIR

USER_VISUAL_ACCEPTED=NO

Title:
Admin Orders Repair — Executable Operations Matrix, Broken Operations Menu, Grid Widths, Clean E2E Order Run

Why Repair Is Required

Manual review after TB-P09-T003 found real defects. Orders are NOT visually/operationally accepted yet.

User findings:

Orders Grid appearance is already approved; DO NOT redesign it.

Only column width behavior may be changed in the Grid.

Restrictive max-width behavior prevents users from enlarging columns enough.

Current «عملیات» control is functionally broken/useless on many rows; clicking it often exposes no executable submenu/action.

The previously designed lifecycle/action matrix must become real executable behavior.

A returned/refunded Order does not make its Return/Refund state clear enough in the list.

Raw/technical refund wording must be humanized to بازگشت وجه; return remains مرجوعی.

Current historical test orders are confusing; user wants a fresh deterministic Order driven step-by-step from registration through payment, fulfillment, delivery, return/refund and settlement behavior.

Current Order Detail already has operational history; DO NOT create another history page.

Internal note deletion rule is now clarified: author may delete their own note only until another distinct user/operator has viewed/opened that Order after note creation.

This is a defect-repair task. Keep UI change minimal.

UI LOCK — VERY IMPORTANT

Allowed UI changes ONLY:

fix Orders column width constraints/resizing

fix existing Operations menu behavior

render correct backend-projected operations

humanize operation/status wording such as بازگشت وجه

minimal delete affordance for eligible internal notes

defect-only loading/error feedback

NOT allowed:

Grid redesign

Operations button restyle/redesign

toolbar redesign

new page/navigation

broad typography/spacing changes

new permanent action buttons

unnecessary new columns

1. Mandatory Discovery Before Code

Inspect current implementation first:

Orders grid column definitions

AppDataGrid defaultColDef, persisted column state and width/maxWidth behavior

current operations cell/menu renderer

AdminOrderOperationsComposer and operations GET/POST contracts

Order lifecycle

Payment status and payment methods

Fulfillment/shipment status

ReturnEligibilityEvaluator

Return/Refund status

Settlement/payable state

current dev reset/seed utilities

internal notes and current order-view/read paths

Identity/operator context

Reuse existing architecture. No duplicate subsystem.

If shared AppDataGrid must change, prove why and add regression coverage. Do not globally remove useful minWidth rules. The target is only restrictive maximum width behavior that prevents user resizing.

2. Grid Width Repair

For Orders grid:

remove/override restrictive maxWidth constraints preventing manual expansion

inspect per-column AND shared defaultColDef

keep sensible minWidth

keep columns resizable

keep saved views/column-state restore

keep current column order and visual design

do not use auto-fit in a way that blocks later manual resize

Acceptance:

every normal Orders column can be dragged wider than its initial width

saved widths restore correctly

no Grid redesign

3. Broken Operations Menu — Root Cause Repair

Diagnose why rows often yield هیچ عملیاتی مجاز نیست or no useful submenu.

Check:

composer projection

stale status/data

incorrect combined-state logic

permission filtering

endpoint response

frontend menu state

click/portal/z-index/menu rendering

contract mismatch

Required:

existing Operations button/menu opens reliably

do NOT redesign its appearance

items come from backend-authoritative projection

clicking an item executes the operation

pending state prevents duplicate submit

success refreshes affected state

errors are localized/human

frontend must NOT hardcode domain transitions

4. Executable Operations Matrix — Backend SoT

Implement/reconcile one authoritative action matrix/composer using combined state:

Order lifecycle

Payment status

Fulfillment status

Return eligibility/status

Refund status

Settlement status

user capability/permission

Do NOT reduce these to one fake status.

The matrix must expose only operations that are actually executable.

A. Order Registered / Awaiting Payment

Allowed as applicable:

لغو سفارش

payment-specific admin action only when current payment method requires it

Not allowed:

processing/packing/shipping/delivery/return/refund/seller payout

B. Manual / Card-to-Card Payment Awaiting Confirmation

If current Payment architecture supports manual/card-to-card confirmation:

تأیید پرداخت / تأیید واریز

رد پرداخت / رد واریز if modeled

لغو سفارش according to policy

payment evidence/details if existing

If NOT currently modeled:

do not fake frontend behavior

inspect Payment boundary

implement the smallest canonical support only if architecture is clear

otherwise return PARTIAL/BLOCKED with exact missing capability

C. Paid

شروع پردازش / آماده‌سازی

cancellation only if canonical policy allows

D. Processing

بسته‌بندی

cancel only if domain policy allows

E. Packed / Ready

ایجاد مرسوله

tracking action when shipment exists

no shipment over-allocation

F. Shipment Created

ثبت/اصلاح کد رهگیری if allowed

ارسال مرسوله

G. Dispatched

ثبت تحویل

tracking/history read actions

H. Delivered

درخواست مرجوعی only if ReturnEligibility=true

settlement/payable actions only if current financial rules allow

no cancellation as substitute for Return

I. Return Requested

تأیید مرجوعی

رد مرجوعی
according to existing Return model/capabilities

J. Return Approved / Refund Pending

اجرای/پیگیری بازگشت وجه

retry only when valid

K. Refunded

read return/refund detail

no duplicate refund

accepted seller-adjustment path remains intact

L. Settlement Pending / Seller Payable

Settlement is independent from Order status.
If current Settlement/Payout architecture supports an Admin action:

واریز سهم فروشنده or existing approved equivalent

view settlement detail

hold/deny payout when active return/refund rules require it

M. Seller Settled

view settlement

Return may still be valid if ReturnEligibility=true

never undo/rewrite old settlement

Refund produces idempotent seller debit/adjustment

5. Human Action Labels

FA UI must use business language, not technical codes.

Required examples:

refund => بازگشت وجه

retry refund => تلاش مجدد برای بازگشت وجه

return => مرجوعی

mark packed => بسته‌بندی

create shipment => ایجاد مرسوله

dispatch => ارسال مرسوله

delivered => ثبت تحویل

settlement payout => existing correct Persian wording, preferably واریز سهم فروشنده if semantically accurate

Return and Refund MUST remain distinct concepts.

6. Controlled Clean Development Reset

User explicitly wants a clean Order test from the beginning.

Before any destructive action:

verify environment is LOCAL DEVELOPMENT

verify active DB name

never touch production/non-dev DB

record concise evidence of what is reset

Then use existing reset/seed/test utilities if available.

Goal:

clear current TEST Order/Checkout transactional data safely

preserve master/reference data needed for a valid Order: users, sellers, products/offers, payment config, shipping config, schema/migrations

If selective deletion is unsafe due to FKs/events/audit:

use a fresh local development database instead

do NOT use risky ad-hoc SQL deletion

7. Fresh Order — Real End-to-End Run

Create ONE fresh Order using real supported UI/API/application flows, not direct final-state SQL.

Drive it through as much of the real lifecycle as current architecture supports:

Order registered

Payment pending

if manual/card-to-card exists: awaiting confirmation -> Admin confirmation
otherwise use the real existing payment-success path and document the gap

Paid

Processing

Packed

Shipment created

Tracking assigned

Dispatched

Delivered

Return requested

Return approved

بازگشت وجه

Settlement/adjustment behavior as applicable

AT EACH STEP capture:

OrderStatus

PaymentStatus

FulfillmentStatus

ReturnStatus

SettlementStatus

backend-projected operation codes

Persian labels rendered in UI

PASS requires the next valid action to actually appear and execute.

8. Orders List State Visibility

User observed an Order with Return + Refund history but list did not make those states apparent enough.

Do NOT redesign or automatically add many columns.

Inspect existing وضعیت and پرداخت semantics.

Required:

if existing DTO/status fields already contain returned/refunded state, render them correctly and humanly

do not let a meaningful refunded/returned state be hidden by misleading generic wording

بازگشت وجه and مرجوعی must be distinguishable

If this truly requires a new column, DO NOT add it automatically in this repair. Document the exact gap for Architect decision.

9. History Decision — Locked

Current Order Detail already has operational history.

keep it there

no new history page

no page-inside-page navigation

If a Grid quick-history affordance ever becomes necessary, it must be modal/drawer using existing patterns, but DO NOT add it in this repair unless fixing an existing concrete defect requires it.

10. Internal Note Delete Rule

Implement the user's clarified rule backend-authoritatively.

A note may be deleted only when ALL are true:

current user is the note author

no OTHER distinct user/operator has opened/viewed this Order after the note was created

note is not already deleted

permission/capability allows it

Lock semantics:

author's own later views DO NOT lock their own note

the first later view/open by another distinct user permanently locks deletion for that note

Do not implement this as frontend timestamp guessing.

Prefer:

reuse existing Order view/read audit if available

otherwise add the smallest audit/view acknowledgement consistent with current architecture

If hard delete violates audit conventions:

use soft delete/tombstone

remove from normal notes UI

retain audit evidence of deletion

UI:

small delete action only on eligible own note

hidden when locked

localized confirmation

no note-card redesign

11. Permission Verification

Verify:

View-only user sees View/detail but no mutation operations

operational user sees only permitted lifecycle actions

finance/settlement action requires existing finance capability

Return/Refund actions require existing capabilities

unauthorized direct POST is rejected backend-side

Do not add redundant permission families.

12. Error UX

Normal UI must never expose:

raw operation code

raw refund

HTTP status text

JSON

stack/DB/provider exception

Use localized human FA/EN messages.

13. Focused Tests

Backend required:

matrix representative combinations

awaiting-payment operations

manual/card-to-card path if supported

paid -> processing

processing -> packed

packed -> shipment

shipment -> dispatch

dispatched -> delivered

delivered eligible -> Return

Return approved -> Refund

refunded -> no duplicate Refund

settlement pending/settled projections

unauthorized action rejection

invalid direct transition rejection

note own-delete before other-view allowed

note delete after other-view forbidden

author's own view does not lock note

Frontend required:

operations menu opens

menu populated from backend

actionable row does not falsely show empty operations

localized operation labels

بازگشت وجه instead of raw refund

Orders columns can expand beyond initial width

eligible own-note delete visible

locked delete hidden

existing Grid appearance regression green

existing Order detail regression green

Also:

relevant build/typecheck/lint

recovery guard

git diff --check

No unrelated full repository suite.

14. Required Runtime / Browser Smoke

Use:
Backend: http://127.0.0.1:5088
Frontend: http://127.0.0.1:3000

Must prove:

clean dev/fresh Order

each supported lifecycle step shows correct next operation

Operations menu opens reliably

action executes and refreshes state

next valid operation appears

columns can be manually enlarged beyond previous max

no Grid redesign

بازگشت وجه wording correct

note delete rule works for same-user vs another-user view

If any lifecycle step cannot be completed due to missing backend capability:
return PARTIAL/BLOCKED with exact reason.
Do NOT claim PASS from unit tests alone.

15. Recovery / Pipeline

Update:

Phase: P09

Last Architect-accepted: TB-P09-T003

TB-P09-T003 under repair due manual user findings

Current Repair: TB-P09-T003-R1

Last Implementation on completion: TB-P09-T003-R1

USER_VISUAL_ACCEPTED=NO

Worker Next State: IDLE

do NOT invent/start TB-P09-T004

Run recovery guard.

16. Evidence

Create/update:
docs/evidence/TB-P09-T003/

Required:

r1-root-causes.md

r1-grid-widths.md

r1-operations-menu.md

r1-action-matrix.md

r1-clean-dev-reset.md

r1-fresh-order-e2e.md

r1-list-state-visibility.md

r1-note-delete-rule.md

r1-permissions.md

r1-runtime-visual-smoke.md

r1-recovery-sot.md

17. Git

Required:

preserve user-authored changes

commit repair

push origin/main

HEAD==origin/main

no task-owned uncommitted changes

Acceptance

PASS only if ALL:

Orders Grid visual design remains unchanged

restrictive max-width behavior removed for Orders columns

user can manually expand columns

existing Operations control opens and works

Operations button appearance is not unnecessarily redesigned

actionable Orders no longer falsely show no operations

operations come from authoritative combined-state matrix

frontend does not hardcode business transitions

every supported lifecycle step exposes the correct executable next action

fresh Order is driven end-to-end in runtime

manual/card-to-card flow is genuinely supported/proven OR exact missing backend gap is reported

refund renders as بازگشت وجه

Return and Refund remain distinct

no new history page

internal note delete rule is backend-authoritative

another user's later Order view permanently locks note deletion

author's own view does not lock note

permissions remain backend-authoritative

no raw technical errors

architecture locks preserved

focused tests green

runtime/browser smoke complete

recovery guard passes

HEAD==origin/main

Worker returns IDLE

no TB-P09-T004 invented

USER_VISUAL_ACCEPTED=NO.

Result Contract

Return ONLY canonical BRIDGE-WAKE-V1 result with:

Summary
Root-Causes
Grid-Widths
Operations-Menu
Executable-Action-Matrix
Payment-Manual-Flow
Clean-Dev-Reset
Fresh-Order-E2E
Orders-List-State-Visibility
Return-Refund-Labels
History-Decision
Internal-Note-Delete-Rule
Permissions
Error-UX
Focused-Validation
Runtime-Visual-Smoke
Recovery-SoT
Git
Architectural-Concerns
Visual-Concerns
Blockers

END_TOOBA_REPAIR_TASK