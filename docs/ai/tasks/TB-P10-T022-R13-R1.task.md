PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_TASK

Task-ID: TB-P10-T022-R13-R1
Parent-Task: TB-P10-T022-R13
Channel: tooba-main
WorkerId: tooba-worker-01
AgentType: cursor
Status: ISSUED

Appearance-USER_VISUAL_ACCEPTED=YES
Builder-USER_VISUAL_ACCEPTED=NO

Title:
Hero Slider Builder Repair — Six Canonical Variants, Preset Heights, Structured CTA Targets, Semantic SEO/Accessibility, Visible Validation

ARCHITECT DECISION
This is a focused Builder visual/UX repair discovered during user visual acceptance.
Do NOT enter P11/Admin implementation.
Do NOT start TB-P11-T002.
Do NOT touch the already-resolved Canonical Home restore behavior.
Do NOT invent TB-P10-T023.

The user has confirmed the previous Home-reset issue is already solved outside this task. Preserve that fix exactly.

CURRENT KNOWN STATE

Last Architect-accepted Builder task: TB-P10-T022-R13

Admin audit TB-P11-T001 PASS is retained for later only; do not execute its roadmap now.

Last known repository tip before user-approved follow-up fixes: 3d694233047708434b020750be6729500a8d1a3a

If current HEAD is a clean descendant containing the approved Home-reset fix, preserve it and continue.

Appearance USER_VISUAL_ACCEPTED=YES

Builder USER_VISUAL_ACCEPTED=NO

Locks known through LOCK-SF-366

UPDATED WORKER PROTOCOL
There is NO periodic heartbeat protocol.
There is NO Worker-IDLE protocol after result.
For this task:

claim this exact task when prompted by the user,

execute it,

deliver the canonical Result through Bridge,

STOP COMPLETELY.

After Result:

do not poll Bridge,

do not send heartbeat,

do not check for another task,

do not enter/announce IDLE,

do not fetch next work automatically.
A future task is fetched only after an explicit user prompt.

SCOPE
Repair ONLY the Hero/Slider Builder UX and runtime contract:

exactly six HeroSlider variants,

exact Persian names,

one shared Swiper engine/API,

height presets instead of raw pixels,

image-dimension guidance,

slide content/SEO-accessibility cleanup,

structured per-slide destination picker,

validation visibility and live recovery,

runtime/storefront regression.

Do not redesign unrelated Section families.

RECOVERY PREFLIGHT
Record branch/HEAD/origin/main/git status, current HeroSlider and registry, wizard Appearance/Settings/Review steps, slide schema, renderer, Swiper modules, media picker, Product/Category selectors, validation framework, unrelated stashes and .tmp-* noise.

Preserve unrelated user work.
If HEAD differs from the old pin only because of the approved Home reset and/or accepted audit/docs commits, document and continue.
Return RECOVERY_CONFLICT only for unexplained conflicting source divergence.

Evidence: docs/evidence/TB-P10-T022-R13-R1/recovery-start.md

EXACTLY SIX CANONICAL SLIDER VARIANTS
The Slider family must expose exactly:

Persian UI Name	Persisted Variant Key
الماس	fullscreen
سیمین	shapes
کیمیا	diagonal
فاخته	cinematic
صبا	split
عقیق	editorial

Rules:

Admin shows Persian names.

Persist the stable English key only.

Persian name is presentation metadata only.

Any non-slider or obsolete Slider choices must not appear in the Slider picker.

Existing saved legacy Slider sections must remain renderable through safe compatibility mapping if necessary.

Do not silently delete/reset user data.

Evidence: docs/evidence/TB-P10-T022-R13-R1/slider-variant-map.md




<HeroSlider variant="shapes" /> <HeroSlider variant="diagonal" /> <HeroSlider variant="cinematic" /> <HeroSlider variant="split" /> <HeroSlider variant="editorial" />

Requirements:

one HeroSlider implementation/family,

Swiper.js is the single slider engine,

one autoplay contract,

one RTL contract,

one navigation/pagination contract,

variants differ by layout/effect metadata/config,

EffectCreative may be used where appropriate,

no second slider library,

no six copy-pasted implementations.

ACTUAL LIVE PREVIEW FOR ALL SIX
Appearance step must render the actual production HeroSlider for all six.

real production component,

real safe transition behavior,

PreviewFakeData only for preview content,

no geometric fallback,

no static screenshots,

no navigation/business mutations,

Persian design name clearly visible.
All six must be visually distinguishable.

HEIGHT UX — NO RAW PIXELS
Remove raw Admin pixel input such as اندازه نمایش (پیکسل) = 420.
Replace with exactly:

متوسط

بزرگ

خیلی بزرگ

Persist typed preset keys such as medium / large / xlarge.
Responsive pixel behavior is code-owned for Desktop/Tablet/Mobile.
User never configures breakpoints or arbitrary pixels.

If legacy numeric height exists, keep render compatibility and map it safely to nearest preset for editing where possible.

RECOMMENDED IMAGE DIMENSIONS
Next to height/media controls show code-owned guidance based on selected variant + selected height preset.
At minimum show:

recommended aspect ratio,

recommended minimum pixel dimensions,

short focal/crop guidance.

Example UX only:
پیشنهاد تصویر: نسبت 16:9 — حداقل 1600×900 — سوژه اصلی نزدیک مرکز تصویر

Do not use one blind recommendation for all six variants.
Derive sensible recommendations from production layout.
Guidance is advisory unless existing media validation already enforces a real minimum.
Responsive crop remains automatic and focal-point-compatible.

Evidence: docs/evidence/TB-P10-T022-R13-R1/slider-image-guidance.md

SLIDE COUNT / TABS
Keep the current slide-tab UX.

slide count remains explicitly configurable under existing safe min/max contract,

each slide has a tab/chip,

selected tab obvious,

tab carries an error indicator when that slide is invalid,

reducing count must not silently destroy valid content without confirmation if destructive.

SLIDE CONTENT + SEO/ACCESSIBILITY CORRECTION
Page-level SEO remains the actual SEO source of truth.
Per slide keep semantically correct content fields:

image/media,

image Alt,

visible title,

visible description/subtitle where supported,

CTA/button label when CTA is used,

CTA destination.

Do NOT keep misleading per-slide controls named SEO Title / SEO Description unless runtime truly emits a distinct valid semantic use.
Preferred repair:

existing «عنوان سئو اسلاید» becomes ordinary visible «عنوان» if it is the visible heading,

existing «توضیح سئو اسلاید» becomes ordinary visible «توضیح» if it is visible copy,

otherwise remove fake SEO-only fields safely.

Do not duplicate Page title/meta/canonical/OG/hreflang into slides.
Slide-level SEO/accessibility is primarily Alt + meaningful visible content + accessible CTA label.

REQUIRED VS OPTIONAL VALIDATION
Default contract:
Required:

image,

image Alt,

visible title.

Optional:

description,

CTA.

Conditional:

destination selected => CTA label required,

CTA label entered => destination required,

custom URL selected => valid URL/path required.

If a variant genuinely does not support a field, hide/disable it and do not force unused content.

Evidence: docs/evidence/TB-P10-T022-R13-R1/slider-validation-contract.md

STRUCTURED CTA DESTINATION PICKER
Replace the simple free-text /products field with a structured destination selector.
At minimum:

بدون پیوند

همه محصولات

محصول مشخص

دسته‌بندی مشخص

آدرس سفارشی

If an existing shared target contract cleanly supports Brand / Store Page / Article, those may be reused, but do not expand architecture unnecessarily.

All Products:

no entity picker,

resolve canonical products route.

Specific Product:

use existing/canonical Product selector,

human-readable data,

no GUID exposure,

canonical product route resolution.

Specific Category:

use existing/canonical Category selector,

understandable hierarchy/search,

no GUID exposure,

canonical category route resolution.

Custom URL:

validated path/URL according to current navigation policy.

Persist a typed target contract such as destinationType + target entity reference + custom URL where applicable.
Do not persist human labels as identity.
Preserve compatibility for legacy raw URL values.
No first-item shortcuts.

PRODUCT / CATEGORY PICKER UX
For large datasets:

searchable,

paginated,

Persian labels,

reuse canonical selector/AppDataGrid patterns where already available,

no full Catalog dropdown,

no raw IDs.

Product picker should show useful human identity such as name, SKU if present, and image if existing picker supports it.
Category picker should make hierarchy understandable.

VALIDATION VISIBILITY — CRITICAL
Bottom-only error text is insufficient.
On blocked Next / Save / Review:

A. Visible popup/modal summary
Example:
اطلاعات اسلاید ۱ ناقص است. لطفاً فیلدهای مشخص‌شده را تکمیل کنید.
Identify invalid slides in Persian; no internal field keys.

B. Inline invalid fields

red border,

red/error label/helper,

aria-invalid,

concise inline message.

C. Slide tab error state

invalid slide chip/tab gets icon/badge and visual error state,

not color-only.

D. Step error state

Settings/Review step indicator reflects blocked validation.

E. First-error navigation

automatically switch to first invalid slide,

scroll first invalid field into view inside modal,

focus it when appropriate.

LIVE ERROR RECOVERY
Once errors are shown, when a field becomes valid:

immediately remove red state,

remove inline error,

update slide-tab error state immediately,

update step/global validation state immediately.

Do not require pressing Next again just to clear styling.
No polling, interval, or timeout.

MODAL SCROLL / ACTIONS
Ensure long edit modal remains usable:

step/header understandable,

action footer visible/sticky as appropriate,

content scrolls internally,

validation focus/scroll works inside modal,

validation popup renders above modal correctly,

no nested scroll trap,

mobile remains usable.

RUNTIME RENDERING
For each six variants verify production storefront:

renders correctly,

RTL,

autoplay,

manual swipe,

navigation/pagination per variant contract,

height presets affect responsive height,

focal/crop works,

CTA target resolves.

Verify Desktop, Tablet, Mobile.

BACKWARD COMPATIBILITY
Audit saved Slider sections.
If legacy variant/settings differ:

preserve rendering,

use safe adapter/mapping,

do not mass-delete sections,

do not silently reset slide data,

do not break published pages.

Evidence: docs/evidence/TB-P10-T022-R13-R1/backward-compatibility.md

FOCUSED VALIDATION
Required:

Slider picker exposes exactly 6 variants

exact Persian/key mapping

one HeroSlider/Swiper engine contract

no second slider library

all 6 real previews render

no obsolete Slider choice exposed for new edits

only medium/large/xlarge height presets

no raw pixel input exposed

image guidance changes by variant/height

slide tabs edit the correct slide

fake slide SEO fields removed/renamed semantically

image/title/alt validation works

CTA pair conditional validation works

All Products target resolves

Product picker resolves selected product

Category picker resolves selected category

Custom URL validates

no raw GUID in picker UI

popup validation appears when blocked

first invalid slide auto-activates

first invalid field scroll/focus works

field error clears live

slide-tab error clears live

all six runtime variants render

RTL/autoplay/swipe regression

critical-storefront PASS

recovery-staleness PASS

git diff --check PASS

REQUIRED VISUAL EVIDENCE
Host :5088 + FE :3000.
Capture at minimum:

slider-six-variants.png

slider-almas-preview.png

slider-simin-preview.png

slider-kimia-preview.png

slider-fakhteh-preview.png

slider-saba-preview.png

slider-aghigh-preview.png

slider-height-presets.png

slider-image-guidance.png

slider-tabs.png

slider-destination-type.png

slider-product-picker.png

slider-category-picker.png

slider-validation-popup.png

slider-invalid-fields.png

slider-tab-error-state.png

slider-error-cleared-live.png

slider-review-final.png

slider-storefront-desktop.png

slider-storefront-mobile.png

Evidence: docs/evidence/TB-P10-T022-R13-R1/

ANTI-PATTERN GATE
Reject:




6 Slider variants,

non-slider variants inside Slider family,

six copy-pasted slider components,

second slider library,

raw pixel height input,

one-size image guidance,

fake per-slide meta SEO fields,

raw free-text-only internal target model,

loading entire Product/Category Catalog into dropdown,

raw GUID exposure,

first-item shortcuts,

bottom-only validation,

color-only errors,

errors remaining red after correction,

polling/timeouts for validation,

Storefront theme leaking into Admin chrome,

regression to Canonical Home reset,

unrelated P11/Admin implementation.

Evidence: docs/evidence/TB-P10-T022-R13-R1/antipattern-scan.md

LOCKS
Add non-duplicate locks after LOCK-SF-366:

LOCK-SF-367 Hero Slider exposes exactly six canonical variants: fullscreen/الماس, shapes/سیمین, diagonal/کیمیا, cinematic/فاخته, split/صبا, editorial/عقیق.
LOCK-SF-368 All six Hero Slider variants share one HeroSlider/Swiper engine, autoplay, RTL, navigation and persistence contract; parallel slider implementations/libraries are forbidden.
LOCK-SF-369 Hero Slider height is user-selected only through Medium/Large/Extra-Large presets; responsive pixel behavior remains code-owned.
LOCK-SF-370 Slider media UI provides code-owned variant/height-specific recommended aspect ratio and minimum image dimensions without making arbitrary pixels user-configurable.
LOCK-SF-371 Slide-level SEO/accessibility uses semantic visible content plus image Alt; page SEO remains page-level and fake per-slide meta-title/meta-description controls are forbidden.
LOCK-SF-372 Slide CTA destinations use a typed target model with canonical Product/Category/All-Products/Custom-URL resolution; raw GUID/internal identifiers are never exposed to ordinary users.
LOCK-SF-373 Wizard validation must surface blocked progression through visible summary + inline field errors + slide-tab state + first-error navigation, and error state clears live once corrected.
LOCK-SF-374 After Bridge Result delivery, Cursor stops completely; no heartbeat, polling, IDLE loop, or automatic next-task fetch is part of the Tooba worker protocol.

RECOVERY / COMPLETION STATE
At completion record:

Phase P10 / Builder Acceptance Repair

Last Architect-accepted before review: TB-P10-T022-R13

Last Implementation: TB-P10-T022-R13-R1

Admin audit TB-P11-T001 retained for later, not executed further

Current Issued/Repair: none after Result

Appearance USER_VISUAL_ACCEPTED=YES

Builder USER_VISUAL_ACCEPTED=NO

branch main

HEAD==origin/main

unrelated work preserved

no TB-P10-T023

no TB-P11-T002

Do NOT write Worker IDLE as an execution state.
After canonical Result is delivered through Bridge, STOP.

ACCEPTANCE
PASS only if all requirements above are met, previous Canonical Home reset remains preserved, no P11 work starts, anti-pattern scan is clean, Result is delivered through Bridge, and Cursor stops after Result.

RESULT CONTRACT
Return ONLY canonical BRIDGE-WAKE-V1 Result with:
Summary
Applicable-Locks
Recovery-Start
Slider-Variant-Map
Single-Engine-Architecture
Live-Variant-Previews
Height-Presets
Image-Guidance
Slide-Content-SEO
CTA-Destination-Model
Product-Category-Pickers
Validation-UX
Live-Error-Recovery
Modal-UX
Runtime-Rendering
Backward-Compatibility
Focused-Validation
Visual-Evidence
AntiPattern-Scan
Lock-Registry
Recovery-SoT
Git
Architectural-Concerns
Visual-Concerns
Blockers

Do not include Worker-IDLE claim.
After sending this Result through Bridge, STOP completely.

END_TOOBA_TASK
