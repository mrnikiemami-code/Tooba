PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

TASK-ID: TB-P06-T005
PROJECT: Tooba
PHASE: P06 — Core API Integration / Operational Hardening
CHANNEL: tooba-main
STATUS: ISSUED
TASK-TYPE: AUTHORIZATION / SPICEDB PRODUCTION HARDENING
WORKER-POLICY: ONE WORKER = ONE ACTIVE TASK

Title

SpiceDB Production Authorization Hardening — Topology, Consistency, Health, Fail-Closed Behavior and Operational Safety

Architect Context

Architect ACCEPTED:

TB-P06-T001 = ACCEPTED
TB-P06-T002 = ACCEPTED
TB-P06-T003 = SUPERSEDED_WRONG_TRANSPORT
TB-P06-T003-R1 = ACCEPTED
TB-P06-T004 = ACCEPTED
P06 = IN_PROGRESS

Locked authorization architecture:

SpiceDB = canonical authorization engine
Authorization = ReBAC
Authorization checks = use-case boundary
Identity/User != Party/Organization

Development may use InMemory mode where already supported.
Production authorization must be safe and explicit.

Objective

Establish a production-grade authorization baseline covering:

SpiceDB topology
configuration validation
credentials/secrets
TLS
schema/model loading
consistency semantics
fail-closed behavior
timeouts
retry policy
health/readiness
circuit/failure behavior
observability
tenant isolation
cache safety
authorization test coverage

Do NOT redesign domain permissions.
Do NOT replace SpiceDB.
Do NOT add UI scope.

Repository Recovery

Run first:

git rev-parse --show-toplevel
git fetch origin
git branch --show-current
git rev-parse HEAD
git rev-parse origin/main
git status --short --branch

Expected predecessor:
e1f0edf580f199d1fa227a136315e2aa167dc3b3
OR the exact accepted TB-P06-T004 commit if SoT advanced after T004.

If HEAD/origin do not match current accepted SoT:
RECOVERY_CONFLICT.

A — Start Runtime Before Work

Mandatory before tracked changes.

Start:

PostgreSQL

Backend

Frontend

SpiceDB in local/test mode if repository supports it

Verify:

health/live

health/ready

Home 200

Create:
docs/evidence/TB-P06-T005/01-runtime-before-authz-hardening.md

B — Current Authorization Inventory

Create:
docs/evidence/TB-P06-T005/02-authorization-inventory.md

Inventory:

authorization modes

SpiceDB client package/version

endpoint configuration

token/credential source

TLS behavior

schema source

permission check APIs

relationship write APIs

consistency mode

timeout/retry policy

caching

health/readiness integration

audit/logging hooks

all current use-case-boundary checks

C — Production Mode Validation

Production must fail fast if:

SpiceDB mode selected but endpoint missing

credential/token missing

TLS policy invalid

schema/config incompatible

insecure dev fallback enabled unintentionally

Development-only InMemory mode may remain.

No silent Production fallback from SpiceDB to allow-all or InMemory.

Create:
03-production-authz-validation.md

D — Fail-Closed Policy

Authorization engine failure must not become allow-by-default.

Classify operations:

read

write

privileged/admin

background processing

Default:
authorization infrastructure uncertainty => deny or fail safely.

Document exact exceptions only if architecture explicitly supports them.

Create:
04-fail-closed-policy.md

E — Timeout / Retry / Backoff

SpiceDB calls need bounded behavior.

Define:

connect timeout

request timeout

retry count

backoff

retryable vs non-retryable errors

No infinite retry.
No request thread blocking loops.
No retry on deterministic permission denial.

Create:
05-authz-timeout-retry-policy.md

F — Consistency Semantics

Audit SpiceDB consistency usage.

Document when to use:

minimize latency

at-least-as-fresh

fully consistent / exact token behavior where justified

Critical invariant:
relationship write followed by permission check must not produce unsafe stale authorization.

Use ZedToken/consistency token where architecture requires.

Do not force strongest consistency globally without evidence.

Create:
06-spicedb-consistency-model.md

G — Relationship Write Safety

Audit relationship mutation paths.

Verify:

durable business identifiers

deterministic subject/resource IDs

no DB connection data in relationships

no accidental cross-tenant relationship

deletes/revokes safe

retries idempotent

Create:
07-relationship-write-safety.md

H — Schema / Model Governance

SpiceDB schema must be versioned and reviewable.

Verify:

schema stored in repo

changes explicit

production apply process documented

no ad-hoc manual-only model mutation

backward compatibility considered

rollback limitation documented

If no schema migration mechanism exists:
implement smallest operational command/process.

Create:
08-spicedb-schema-governance.md

I — Tenant / Edition Isolation

Marketplace:

seller/customer/admin relationships scoped correctly

Single-Store:

tenant/store boundary explicit where needed

no relationship granting access across tenant databases

tenant identity independent from hostname

Add tests for cross-tenant denial.

Create:
09-authz-edition-isolation.md

J — Authorization Cache Safety

If authorization result caching exists:
audit:

cache key includes all required dimensions

permission

resource

subject

tenant/store context

consistency token where required

Revoke-sensitive checks must not be dangerously stale.

If no cache exists:
do not add one just for this task.

Create:
10-authz-cache-safety.md

K — Health / Readiness

Liveness must not depend on SpiceDB.

Readiness:
if Production mode requires SpiceDB, readiness should fail when critical authz dependency unavailable.

Avoid expensive full permission scans.

Use smallest safe probe.

Create:
11-spicedb-health-readiness.md

L — Observability

Add/verify:

latency metrics

request count

deny count

infrastructure failure count

timeout count

retry count

trace spans

resource/subject metadata sanitized

no token/secret logging

Differentiate:
DENIED
ERROR
TIMEOUT
UNAVAILABLE

Create:
12-authz-observability.md

M — Security Logging Boundary

Technical logs != security audit.

For sensitive operations, ensure security-relevant authorization outcome can be audited without leaking secrets.

Do not turn every permission check into noisy permanent audit if architecture separates these concerns.

Create:
13-security-audit-boundary.md

N — Integration Tests

Use real SpiceDB/Testcontainers when practical.

Required tests:

allowed relationship => allow

missing relationship => deny

revoke => subsequent protected operation denied

SpiceDB unavailable => fail closed

timeout => fail closed

invalid token/config => startup/readiness failure

consistency after relationship write

duplicate relationship write safe

cross-tenant relationship denied

admin privilege isolation

retry on transient infrastructure failure only

no secret leakage in error/log output

Create:
14-spicedb-integration-tests.md

O — Production Operations Guide

Create/update:
docs/operations/authorization-spicedb.md

Include:

topology

required env/config

TLS

credentials

schema deployment

health/readiness

timeout/retry

consistency

incident behavior

outage behavior

secret rotation

relationship repair

troubleshooting

rollback limitations

P — UI Regression

No UI changes expected.

Run:

cd src/frontend
npm run typecheck
npm run lint
npm run test
npm run build

Q — Backend Validation

Use zero-warning path:

dotnet restore src/backend/Tooba.slnx
dotnet build src/backend/Tooba.slnx
dotnet test src/backend/Tooba.slnx

Run SpiceDB integration tests.

Require:
warnings = 0
errors = 0
failed = 0
skipped = 0

Always:
git diff --check

R — Runtime After Work

After validation:

Backend running

Frontend running

SpiceDB running where local mode used

health/live 200

health/ready 200

Home 200

authorization smoke check succeeds

unauthorized smoke check denied

Keep runtimes running if technically possible.

S — Source of Truth

Update:
docs/PROJECT-STATE.md
docs/ROADMAP.md
docs/ai/TOOBA-RECOVERY-CONTEXT.md
docs/prompts/START-HERE-IF-CHATGPT-IS-LOST.md

Expected:

P06 = IN_PROGRESS
TB-P06-T001 = ACCEPTED
TB-P06-T002 = ACCEPTED
TB-P06-T003 = SUPERSEDED_WRONG_TRANSPORT
TB-P06-T003-R1 = ACCEPTED
TB-P06-T004 = ACCEPTED
TB-P06-T005 = AWAITING_ARCHITECT_ACCEPT

AUTHORIZATION = SPICEDB_REBAC
MESSAGING_TRANSPORT = MASSTRANSIT_POSTGRESQL_SQL_TRANSPORT

HOME_VISUAL_REVIEW = OPEN_FOR_USER_FEEDBACK
PDP_VISUAL_REVIEW = OPEN_FOR_USER_FEEDBACK

Evidence

Create:
docs/evidence/TB-P06-T005/

Required:
01-runtime-before-authz-hardening.md
02-authorization-inventory.md
03-production-authz-validation.md
04-fail-closed-policy.md
05-authz-timeout-retry-policy.md
06-spicedb-consistency-model.md
07-relationship-write-safety.md
08-spicedb-schema-governance.md
09-authz-edition-isolation.md
10-authz-cache-safety.md
11-spicedb-health-readiness.md
12-authz-observability.md
13-security-audit-boundary.md
14-spicedb-integration-tests.md
15-final-validation-proof.md
16-final-runtime-preview.md

Acceptance Criteria

PASS requires:

Production SpiceDB config fail-fast

no silent insecure fallback

fail-closed behavior

bounded timeout/retry

safe consistency semantics

relationship writes idempotent/safe

schema governance explicit

cross-tenant denial proven

cache safety proven if cache exists

readiness reflects SpiceDB accurately

observability distinguishes deny vs infrastructure error

security audit boundary documented

real SpiceDB integration tests green

backend 0/0/0/0

no UI regression

runtime healthy

repo synchronized

Git

git add ...
git commit -m "chore harden SpiceDB production authorization [TB-P06-T005]"
git push origin main
git fetch origin
git rev-parse HEAD
git rev-parse origin/main
git status --short --branch

Require HEAD == origin/main and tracked tree clean.

Result Contract

PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_WORKER_RESULT

Task-ID:
TB-P06-T005

Channel:
tooba-main

Status:
PASS | FAIL | BLOCKED | RECOVERY_CONFLICT

Summary:
...

Runtime-Before:

postgres:

backend:

frontend:

spicedb:

health:

Authorization:

production mode:

fallback:

package/version:

endpoint source:

credential source:

TLS:

Fail-Closed:

unavailable:

timeout:

invalid config:

permission denial:

Retry:

timeout:

retries:

backoff:

retryable errors:

Consistency:

default:

relationship-write-followup:

token strategy:

Relationships:

idempotent:

cross-tenant protection:

secret leakage:

Schema:

repo versioned:

deployment process:

rollback:

Cache:

exists:

dimensions:

revoke safety:

Health:

liveness:

readiness:

outage behavior:

Observability:

traces:

latency:

denies:

failures:

timeouts:

secret logging:

Integration-Tests:

allow:

deny:

revoke:

unavailable fail-closed:

timeout fail-closed:

invalid config:

consistency:

duplicate write:

cross-tenant:

admin isolation:

transient retry:

secret leakage:

Validation:

backend restore:

backend build:

backend tests:

SpiceDB tests:

warnings:

errors:

failed:

skipped:

typecheck:

lint:

critical-storefront:

frontend build:

git diff --check:

Runtime-After:

backend:

frontend:

spicedb:

health/live:

health/ready:

USER-PREVIEW:

Frontend:

Backend Health:

Home:

PDP:

Source-of-Truth:

P06:

T004:

T005:

authorization:

messaging transport:

Home visual review:

PDP visual review:

Git:

commit:

push:

final HEAD:

origin/main:

synchronized:

tracked tree:

Architectural-Concerns:
...

Visual-Concerns:
...

Blockers:
...

END_TOOBA_WORKER_RESULT

After Result:

return to IDLE

do not self-issue another Task

keep Backend + Frontend running if technically possible

END_TASK