PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_TASK

Task-ID:
TB-P10-T015

Parent-Task:
TB-P10-T014

Channel:
tooba-main

WorkerId:
tooba-worker-01

AgentType:
cursor

Status:
ISSUED

USER_VISUAL_ACCEPTED=NO

Title:
Storefront Background Tint Option — Optional Palette-Family Surface Tone for Storefront

Architect Decision

TB-P10-T014 is Architect-ACCEPTED.

Accepted baseline now includes:

7 curated palettes

LightOnly / DarkOnly / System / UserChoice ThemeMode

4 Product Card skins

professional Admin Appearance UX

Landing/Home/Page/Menu integration

seeded demo pages/menu

final visual acceptance pack

default restored to tooba-blue / LightOnly / classic / canonical Home

LOCK-SF-001…192

This task adds ONE optional visual enhancement:

BackgroundStyle

Neutral

PaletteTint

This is a controlled optional Appearance setting.

It must make the storefront feel more themed without damaging readability or looking amateurish.

This task does NOT implement:

arbitrary/custom background colors

free-form theme builder

per-page background styling

section-level custom backgrounds

TB-P10-T016

Product Decision — LOCKED

The user explicitly wants the storefront body to optionally carry a subtle tint from the selected palette family, because the current experience feels too white/gray even when the palette changes.

Required product direction:

completely optional

default remains current neutral storefront

tint must be subtle and professional

cards/surfaces must remain visually separated and readable

no layout change

no "paint everything with primary"

dark mode must also be coherent if tint is enabled

this is a Store-scoped Appearance setting

Canonical values:

Neutral

PaletteTint

Do not add extra styles unless genuinely needed.

Recovery State

Phase: P10
Last Architect-accepted: TB-P10-T014
Last Implementation: TB-P10-T014
Current Task: TB-P10-T015
Worker: tooba-worker-01
Expected branch: main
Expected previous HEAD: 9f9132fa243bf2197c3cd40b044942a3980857d9
USER_VISUAL_ACCEPTED=NO

Preserve LOCK-SF-001…192.

1. Recovery Preflight

Record:

branch

HEAD

origin/main

git status --short

Recovery/SoT

Host :5088

FE :3000

current Store appearance state

unrelated local user-authored changes

Unexpected tracked divergence => RECOVERY_CONFLICT/BLOCK.

Evidence:
docs/evidence/TB-P10-T015/recovery-start.md

2. Discovery — Current Surface/Background Tokens

Inspect targeted code only.

Determine:

current semantic background/surface tokens in light/dark

where storefront outer background / section background / page background are currently defined

how palettes currently define tokens

whether current palette registry can safely hold tint values

where Admin Appearance payload/settings can be extended

Document which surfaces should be tintable and which must remain neutral/elevated for readability.

Evidence:
docs/evidence/TB-P10-T015/background-discovery.md

3. Canonical BackgroundStyle Contract

Extend canonical StoreAppearanceSettings with a typed controlled key:

BackgroundStyle:

Neutral

PaletteTint

Required:

backend validated

invalid value rejected

persisted with other appearance settings

saved atomically with PaletteKey / ThemeMode / ProductCardSkin through the canonical Appearance API

default/legacy missing value resolves to Neutral

no behavior change for existing Stores

Do NOT expose raw enum keys in normal Admin-facing copy.

4. Canonical Tint Tokens

Implement tint through semantic tokens, not page hacks.

Recommended token intent:

store/page background

section background / alt section background where appropriate

Rules:

each palette defines subtle neutral-family tint values for light mode

each palette may define subtle dark-surface hue values for dark mode if needed

tint values must be curated; do NOT derive blindly from primary alpha

no status-color coupling

cards/surfaces/inputs remain readable and distinct

header/menu/product card chrome should keep their accepted hierarchy

Do NOT create page-local tint colors.

Evidence:
docs/evidence/TB-P10-T015/tint-token-mapping.md

5. Surface Scope

Apply BackgroundStyle only to appropriate Storefront surfaces.

Expected scope:

overall page/store background

large section wrapper background where the storefront currently feels too white/flat

Do NOT heavily recolor:

product cards

inputs/forms

modal surfaces

mini cart surfaces

elevated cards that need contrast

status/warning/error surfaces

The goal is ambience, not saturation.

Document exact surfaces changed.

6. Admin Appearance UX

Extend existing Appearance UI with a clear optional control.

Required:

Persian human-readable label

short explanation

clear preview difference between Neutral and PaletteTint

integrated with current dirty/save/cancel flow

no technical wording

default shown clearly

preview meaningful enough that normal user understands what changes

Do not clutter the page.

Suggested copy direction:

پس‌زمینه فروشگاه

خنثی

رنگی ملایم

Use best product wording.

7. Preview / UX Quality

Admin preview must visibly communicate:

Neutral = current white/gray storefront tone

PaletteTint = subtle family tone based on selected palette

The preview must use canonical appearance tokens/settings, not a fake one-off color box.

If a small storefront preview mock/panel is already available, reuse it.
Otherwise add the minimum meaningful preview.

8. Light / Dark Compatibility

BackgroundStyle must work with:

all 7 palettes

LightOnly

DarkOnly

System effective light/dark

UserChoice effective light/dark

Required:

subtle but visible difference

readable text

section separation preserved

product cards still pop from the background

no muddy / amateur color cast

If a palette needs minor tint tuning, do it centrally in the palette registry.

9. Storefront Runtime Behavior

Required:

Neutral: current accepted look remains effectively unchanged

PaletteTint: selected storefront areas adopt subtle palette-family background

Home, Landing pages, and custom Home all inherit BackgroundStyle

no page-local override

no rebuild/redeploy required

no polling

no duplicate appearance fetches

10. Demo / Seed State

Keep demo state inspectable.

Required:

Admin can switch BackgroundStyle

at least one screenshot/demo in default palette with tint

at least one screenshot/demo in a non-default palette with tint

restore ending runtime state to canonical default unless task evidence explicitly documents another safe ending state

Do not alter the seeded demo data structure beyond what is needed to inspect this feature.

11. Minimal Useful Tests Only

Per Architect/user instruction, keep tests risk-driven and minimal.

Essential tests only:

invalid BackgroundStyle rejected

legacy/missing BackgroundStyle resolves to Neutral

save/persist BackgroundStyle with appearance payload

Store isolation preserved

Home/Landing public appearance projection reflects BackgroundStyle

Neutral keeps current accepted fallback behavior

PaletteTint affects canonical tint tokens/projection where testable

recovery guard + git diff --check

Reuse existing appearance tests where possible.

Do NOT create a big matrix for every palette unless a single focused representative test cannot prove the contract.

12. Runtime — REQUIRED

Use:

Host :5088

FE :3000

Run focused browser flow:

A. open Appearance
B. verify default Neutral
C. select PaletteTint with tooba-blue
D. inspect Home
E. inspect one published Landing
F. switch palette to forest-green and keep PaletteTint
G. inspect Home/Landing
H. switch DarkOnly and inspect tinted dark feel
I. restore Neutral
J. restore canonical default appearance (tooba-blue / LightOnly / classic / canonical Home / Neutral)

Capture persistent screenshots:

appearance-background-style.png

home-neutral.png

home-tinted-blue.png

home-tinted-forest.png

home-dark-tinted.png

landing-tinted.png

Store under:
docs/evidence/TB-P10-T015/screenshots/

Record:

PaletteKey

ThemeMode/effective mode

ProductCardSkin

BackgroundStyle

no console/render-loop issues

no task-owned hydration mismatch

no broken contrast

13. Performance / Safety

Required:

no extra per-section appearance fetch

no per-component background logic

appearance cache invalidation remains Store-scoped

no client-side flash from BackgroundStyle

no heavy CSS hacks

no new layout instability

14. Anti-Pattern Gate

Scan for:

arbitrary background color picker

palette primary used directly as page wash without curation

page-local tint hacks

card/input surfaces over-tinted

raw enum values visible in Admin

duplicate appearance API path

polling

no-op preview

per-page override sneaking in

Any real occurrence => REPAIR.

Evidence:
docs/evidence/TB-P10-T015/antipattern-scan.md

15. Lock Registry

Add non-duplicate locks after LOCK-SF-192 equivalent to:

Storefront background tone is a controlled optional Store appearance setting; arbitrary background colors are forbidden.

BackgroundStyle supports only Neutral and PaletteTint, with Neutral as backward-compatible default.

PaletteTint uses curated semantic tint tokens per palette/theme and must not directly repurpose status semantics or heavily tint elevated UI surfaces.

Home/Landing/custom Home inherit one canonical Store BackgroundStyle; no page-local override.

Use next canonical LOCK-SF numbers.
Do not renumber existing locks.

16. Evidence

Create:
docs/evidence/TB-P10-T015/

Required:

recovery-start.md

background-discovery.md

tint-token-mapping.md

appearance-ux.md

runtime.md

focused-validation.md

antipattern-scan.md

recovery-sot.md

screenshots/

Keep evidence concise.

17. Recovery / Pipeline

At completion:

Phase P10

Last Architect-accepted before review: TB-P10-T014

Last Implementation: TB-P10-T015

Current Issued/Repair: none

USER_VISUAL_ACCEPTED=NO

Worker IDLE

branch main

ending HEAD

HEAD==origin/main

task-owned clean

unrelated local user changes preserved

do NOT invent/start TB-P10-T016

Run recovery staleness guard.

Acceptance

PASS only if ALL:

BackgroundStyle contract exists and is backend-validated

default remains Neutral for backward compatibility

Admin Appearance exposes a clear optional control

preview is meaningful

PaletteTint is subtle/professional

Home and Landing visibly inherit tint

cards/surfaces remain readable and distinct

light/dark both coherent

no layout change

no arbitrary color builder introduced

only essential tests added

persistent screenshots exist

runtime restored to canonical default

anti-pattern scan CLEAN

Lock registry updated without duplicates

Recovery exact

HEAD==origin/main

Worker IDLE

no TB-P10-T016

USER_VISUAL_ACCEPTED remains NO

Result Contract

Return ONLY canonical BRIDGE-WAKE-V1 Result with:

Summary
Applicable-Locks
Recovery-Start
Background-Discovery
Tint-Token-Mapping
Appearance-UX
Focused-Validation
Runtime
AntiPattern-Scan
Lock-Registry
Recovery-SoT
Git
Architectural-Concerns
Visual-Concerns
Blockers

END_TOOBA_TASK