PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_TASK

Task-ID:
TB-P10-T007

Parent-Task:
TB-P10-T006

Channel:
tooba-main

WorkerId:
tooba-worker-01

AgentType:
cursor

Status:
ISSUED

USER_VISUAL_ACCEPTED=NO

Title:
Storefront Token Completion — Full Palette Coverage Across Home, PDP, Shipping, and Remaining Locked Surfaces

Architect Decision

TB-P10-T006 is Architect-ACCEPTED.

Accepted baseline now includes:

canonical StoreAppearanceSettings

7 curated PaletteKey presets

Admin Appearance tab

shared palette registry for Admin preview + Storefront runtime

backend-authoritative persistence and isolation

immediate per-Store cache invalidation

SSR application without rebuild/redeploy

LOCK-SF-001…144

T006 also proved an important remaining gap:
Home, PDP, Shipping and some other Storefront surfaces still contain hard-coded brand colors, so alternate palettes do not yet apply consistently across the full Storefront.

This task closes that gap.

This task does NOT implement:

dark mode

custom color/theme builder

Product Card skin selection

Landing/Page builder

Menu builder

typography customization

layout redesign

TB-P10-T008

Recovery State

Phase: P10
Last Architect-accepted: TB-P10-T006
Last Implementation: TB-P10-T006
Current Task: TB-P10-T007
Worker: tooba-worker-01
Expected branch: main
Expected previous HEAD: 10d4b3450573c1667b447aff21fad61cfd177e54
USER_VISUAL_ACCEPTED=NO

Preserve LOCK-SF-001…144.

1. Recovery Preflight

Record:

branch

HEAD

origin/main

git status --short

Recovery/SoT

Host :5088

FE :3000

current selected palette for Store

unrelated local user-authored changes

Unexpected tracked divergence => RECOVERY_CONFLICT/BLOCK.

Evidence:
docs/evidence/TB-P10-T007/recovery-start.md

2. Mandatory Hard-Coded Color Inventory

Start from T006 deferred-token-inventory and re-scan relevant Storefront code.

Focus on:

Home

PDP

Shipping

Checkout shared chrome

PLP

Header/Mega Menu

Cart/Mini Cart

Customer/account Storefront surfaces

shared buttons/links/badges

related/featured/recommended product sections

article/content cards used on Home

Classify every remaining hard-coded color into:

A. Brand color -> must become semantic token in this task
B. Neutral/surface/text -> migrate only when safe and visually equivalent
C. Status semantic color -> keep semantically independent
D. Intentional content/media color -> do not tokenize
E. Shopeiva-specific visual detail -> migrate only if equivalent can be proven

Evidence:
docs/evidence/TB-P10-T007/color-inventory.md

Required table:
File/Component | Current Value | Role | Classification | Action | Visual Risk

3. Brand Color Completion

All remaining Storefront BRAND usages that should follow PaletteKey must move to semantic brand tokens.

At minimum inspect and repair:

#2563EB family

#1d4ed8 family

Shopeiva red #E53935 where it is incorrectly acting as Tooba brand CTA/accent rather than a true semantic/status/content color

hard-coded blue/red Tailwind classes serving as brand presentation

Important:
Do NOT mechanically replace every red/blue value.

For each #E53935 usage, decide whether it is:

legacy Shopeiva brand color -> migrate to primary token

danger/error/status -> preserve semantic status token

content-specific decorative color -> preserve/document

Evidence:
docs/evidence/TB-P10-T007/brand-token-migration.md

4. Home — Full Palette Coverage

Home is a locked visual surface.

Required:

Hero controls/CTA/accent if brand-bound

section titles/links where brand-bound

Best Sellers

Brand cards

Newest Products

Reviews

Latest Articles

category/menu accents

carousel navigation/active states

badges/actions where brand-bound

Rules:

no layout/spacing/DOM redesign

no section order change

no animation behavior change

no typography change

no Shopeiva structural drift

default tooba-blue must remain visually equivalent

Alternate palettes must visibly propagate to brand-bound Home elements.

Evidence:
docs/evidence/TB-P10-T007/home-token-completion.md

5. PDP — Full Palette Coverage

PDP is a locked visual surface.

Inspect and migrate brand-bound:

primary CTA

variant active state

tabs active/hover state

links

quantity/action affordances

related products accents

gallery controls if brand-bound

delivery/info UI where brand-bound

Preserve:

product data semantics

pricing behavior

stock/status semantics

review/rating semantic colors

layout/tabs/media structure

Default tooba-blue must remain visually equivalent.

Evidence:
docs/evidence/TB-P10-T007/pdp-token-completion.md

6. Shipping / Checkout — Remove Legacy Shopeiva Brand Red

Shipping currently contains pre-existing #E53935.

Determine exact role of every red usage.

Required:

if it is a brand CTA/accent inherited from Shopeiva, migrate to semantic primary

if it is danger/error/destructive, keep danger semantic

if it is validation/error, do NOT bind to PaletteKey

Primary checkout action should follow selected Store palette.

Do NOT change:

checkout state machine

Cart conversion boundary

payment/commit semantics

address behavior

error semantics

Evidence:
docs/evidence/TB-P10-T007/shipping-checkout-token-completion.md

7. Neutral Token Completion — Safe Only

Where obvious shared neutral colors still bypass semantic tokens, migrate only if:

visually equivalent under default theme

no layout change

no readability regression

no interaction-state regression

Examples may include:

background

surface

elevated surface

text

muted text

border

Do not force broad migration if current neutral usage is content-specific or risky.

Document deferred neutral values that remain.

8. Status Semantic Isolation

Prove that curated palette switching does NOT alter:

danger

success

warning

validation error

out-of-stock status

destructive action semantics

rating stars if intentionally non-brand

payment status semantics

Add focused tests.

Evidence:
docs/evidence/TB-P10-T007/status-color-isolation.md

9. Palette Cross-Surface Consistency

Use at least:

tooba-blue

forest-green

wine-burgundy

one additional palette with high contrast difference

Verify brand-bound elements across:

Header

Home

PLP

PDP

Cart

Shipping

Account header/menu

all resolve from the same canonical palette registry.

No local page-specific palette mapping.

Evidence:
docs/evidence/TB-P10-T007/cross-surface-consistency.md

10. Accessibility / Contrast

For each curated palette, validate:

primary vs on-primary

primary hover/strong

link text on common surfaces

selected/active controls

focus indicators

CTA text contrast

Use existing accessibility/contrast utilities if present.
Do not invent a separate design engine.

If a preset fails accepted contrast threshold, repair the code-owned palette tokens rather than special-casing a page.

Evidence:
docs/evidence/TB-P10-T007/contrast-validation.md

11. SSR / Runtime Behavior

Preserve T006 behavior:

palette applied on first HTML

no FOUC

no hydration mismatch

no rebuild/redeploy

no restart after palette save

no per-component appearance fetch

no palette polling

No new runtime theme state.

12. Shopeiva Comparison

Because Home and PDP are locked surfaces:

Run Shopeiva runtime side-by-side if current project contract still requires it.

For tooba-blue:

compare structure

spacing

layout

interactions

animation behavior

card density

visual hierarchy

Token substitution must not create layout drift.

Alternate palettes are allowed to change brand color only.

Evidence:
docs/evidence/TB-P10-T007/shopeiva-comparison.md

13. Focused Tests

Required:

Token mapping:

no remaining hard-coded Tooba primary hex in migrated Storefront brand roles

legacy Shopeiva red brand usage removed from Shipping/Checkout brand roles

status semantic red remains independent

default blue maps exactly to accepted default

all 7 palettes produce valid required tokens

Home:
6. brand-bound CTA/accent follows palette
7. section interactions preserve behavior
8. no visual contract DOM regression where tests exist

PDP:
9. CTA follows palette
10. active variant/tab follows palette where brand-bound
11. stock/status semantic colors unchanged

Checkout:
12. primary Shipping/Checkout CTA follows palette
13. validation/error remains danger
14. Cart Active until successful COMMIT
15. injected 409 remains on Shipping with Cart intact

Cross-surface:
16. Header/Home/PLP/PDP/Cart/Shipping use same PaletteKey
17. no page-local palette override
18. no appearance polling
19. no per-card appearance request

Accessibility:
20. curated palette contrast checks pass

Regression:
21. account identity
22. logout Cart persistence
23. re-login restore
24. anonymous merge
25. atomic conversion boundary

Also:

relevant lint/typecheck scoped to task

full tsc state reported truthfully

test

test

recovery guard

git diff --check

Do NOT repair unrelated pre-existing Admin/grid errors.

14. Real Runtime — REQUIRED

Use:

Host :5088

FE :3000

Run:

A. Admin select tooba-blue
B. inspect Home/PLP/PDP/Cart/Shipping
C. switch to forest-green
D. fresh navigate Home/PLP/PDP/Cart/Shipping
E. verify brand-bound surfaces become green
F. verify danger/error/status do NOT become green
G. switch to wine-burgundy
H. repeat representative Home/PDP/Shipping checks
I. switch to fourth high-contrast preset
J. repeat representative checks
K. restore tooba-blue
L. verify default visual equivalence restored

Capture screenshots:

Home default

Home alternate

PDP default

PDP alternate

Shipping default

Shipping alternate

one error/status state proving semantic isolation

Record:

PaletteKey

SSR marker

effective CSS variables

console

relevant network count

no hydration errors

15. Visual Acceptance Boundary

Worker must NOT set USER_VISUAL_ACCEPTED=YES.

Worker provides proof only.

Architect/user visual acceptance remains separate.

If any locked Home/PDP/Shopeiva geometry changes, report Visual-Concern and do not self-accept it.

16. Anti-Pattern Gate

Scan for:

mechanical replacement of every red/blue value

status colors mapped to brand primary

page-specific palette tables

duplicate palette registries

hard-coded alternate palette values in components

inline conditional palette branches

client-only recolor

polling

per-page/per-card appearance fetch

timing hacks

CSS/HTML/JS from DB

dark mode implementation sneaking in

Product Card skins sneaking in

Landing/Page implementation sneaking in

Any introduced real occurrence => REPAIR.

Evidence:
docs/evidence/TB-P10-T007/antipattern-scan.md

17. Lock Registry

Add non-duplicate locks after LOCK-SF-144 equivalent to:

Every Storefront brand-bound visual surface must resolve brand color from canonical semantic appearance tokens, not page-local hard-coded brand hexes.

Legacy Shopeiva brand red must not survive as a Storefront brand CTA/accent where canonical Store palette should apply.

Danger/success/warning/validation/payment-status semantics remain independent of Store brand PaletteKey.

Home and PDP palette migration may change brand color only; locked geometry/layout/interaction remains unchanged.

All curated palettes must satisfy canonical contrast requirements before being selectable.

Storefront pages must not define local palette registries or independent appearance fetch/state.

Use next canonical LOCK-SF numbers.
Do not renumber existing locks.

18. Evidence

Create:
docs/evidence/TB-P10-T007/

Required:

recovery-start.md

color-inventory.md

brand-token-migration.md

home-token-completion.md

pdp-token-completion.md

shipping-checkout-token-completion.md

status-color-isolation.md

cross-surface-consistency.md

contrast-validation.md

shopeiva-comparison.md

focused-validation.md

runtime.md

antipattern-scan.md

recovery-sot.md

19. Recovery / Pipeline

At completion:

Phase P10

Last Architect-accepted before review: TB-P10-T006

Last Implementation: TB-P10-T007

Current Issued/Repair: none

USER_VISUAL_ACCEPTED=NO

Worker IDLE

branch main

ending HEAD

HEAD==origin/main

task-owned clean

unrelated local user changes preserved

do NOT invent/start TB-P10-T008

Run recovery staleness guard.

Acceptance

PASS only if ALL:

T006 accepted baseline preserved

hard-coded Storefront BRAND colors inventoried

remaining brand-bound hard-coded colors migrated

Home follows PaletteKey on brand-bound surfaces

PDP follows PaletteKey on brand-bound surfaces

Shipping/Checkout primary brand CTA follows PaletteKey

legacy Shopeiva red no longer acts as brand color where primary should apply

danger/success/warning/validation/payment status remain independent

all 7 curated palettes remain valid

contrast validation green

one canonical registry/state only

no page-local palette logic

no FOUC/hydration mismatch

no rebuild/redeploy/restart for palette switching

tooba-blue remains visually equivalent

locked Home/PDP structure/interactions unchanged

R4 account/cart/checkout semantics preserved

focused tests green

runtime A–L green

anti-pattern scan CLEAN

Lock registry updated without duplicates

Recovery exact

HEAD==origin/main

Worker IDLE

no TB-P10-T008

USER_VISUAL_ACCEPTED remains NO

Result Contract

Return ONLY canonical BRIDGE-WAKE-V1 Result with:

Summary
Applicable-Locks
Recovery-Start
Color-Inventory
Brand-Token-Migration
Home-Token-Completion
PDP-Token-Completion
Shipping-Checkout-Token-Completion
Status-Color-Isolation
Cross-Surface-Consistency
Contrast-Validation
Shopeiva-Comparison
Focused-Validation
Runtime
AntiPattern-Scan
Lock-Registry
Recovery-SoT
Git
Architectural-Concerns
Visual-Concerns
Blockers

END_TOOBA_TASK
