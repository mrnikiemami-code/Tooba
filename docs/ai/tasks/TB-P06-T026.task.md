PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

TASK-ID: TB-P06-T026
PROJECT: Tooba
PHASE: P06
CHANNEL: tooba-main
STATUS: ISSUED
TASK-TYPE: CUSTOMER WALLET + GIFT CARD
WORKER-POLICY: ONE WORKER = ONE ACTIVE TASK

Title

Customer Wallet & Gift Card — Real Ledger-Based Store Credit, Gift Card Issuance/Redemption and Shopeiva-Locked UI

Architect Context

ACCEPTED:

TB-P06-T025 = ACCEPTED

P06 = IN_PROGRESS

Goal:
Close Wallet/Gift Card honestly, with no fake balance, fake deposits, fake cards or client-authoritative finance.

Financial Architecture — LOCKED

Wallet balance MUST be derived from immutable ledger entries.

FORBIDDEN:

mutable Customer.Balance authority

arbitrary balance overwrite

fake deposit/top-up

fake card

direct SQL mutation for proof

non-idempotent gift-card redemption

Concept:
WalletAccount -> WalletLedgerEntry -> derived balance.

Gift Card:
issued value -> remaining value -> redemption ledger -> expiration/status -> idempotent redemption.

ABSOLUTE SHOPEIVA LOCK

If Shopeiva has Wallet/Gift Card UI, reuse it exactly.
LOCKED:
CSS, Tailwind, JS behavior, animation, transition, hover/focus/active, spacing, typography,
responsive/mobile, icons, badges, cards, tabs, modal/drawer, list/form geometry, micro-interactions.

Do NOT port fake Shopeiva deposit/cards behavior.
Replace bindings with real Tooba data only.
Admin UI absent from template must be source-derived from accepted Shopeiva/Admin patterns.
Foreign UI = VISUAL REGRESSION.

A — Recovery

Run:
git rev-parse --show-toplevel
git fetch origin
git branch --show-current
git rev-parse HEAD
git rev-parse origin/main
git status --short --branch

Expected predecessor:
753ca815491fd589cffe784ed0d00d696c4c8a29

Require main, HEAD == origin/main, safe tracked tree.

B — Runtime

Start and keep running:

PostgreSQL

Backend

Tooba Frontend

Original Shopeiva

Verify health/live + health/ready 200.

C — Shopeiva Source Discovery FIRST

Inspect exact Wallet/Gift Card source:

balance card

history

redeem form

fake deposit/cards

feedback

mobile behavior

Document exact visual/interaction contract.

D — Backend Audit

Audit Payment, Order, Promotion, Refund, Settlement and any existing Wallet/StoreCredit/GiftCard capability.
Do not duplicate owner.

E — Wallet Domain

Create/extend canonical wallet owner.

WalletAccount:

Id

TenantId

Customer identity

Currency

Status

WalletLedgerEntry:

Id

WalletAccountId

Type

Amount

Currency

Direction

SourceType

SourceId

IdempotencyKey

CreatedAt

safe Metadata

Balance = ledger-derived only.

Allowed sources may include:

GiftCardCredit

RefundCredit if integrated

OrderPaymentDebit if checkout spend integrated

privileged audited Adjustment only if required

F — Gift Card Domain

GiftCard:

Id

TenantId

secure Code/token representation

Currency

InitialAmount

safe remaining-value semantics

Status

IssuedAt

ExpiresAt optional

RecipientCustomer optional

CreatedBy actor

GiftCardRedemption:

Id

GiftCardId

Customer/Wallet

Amount

OrderId optional

IdempotencyKey

CreatedAt

Rules:

secure code generation/storage

expiration

revoke/disable

no negative value

tenant/currency validation

duplicate redeem safe

G — Wallet Credit Sources

Only legitimate sources:

Gift Card

Refund-to-wallet if safely supported

audited Admin adjustment if implemented

No fake customer deposit/top-up.

H — Checkout Integration

Integrate wallet spend only if safely feasible.

If implemented:

never exceed balance

currency match

authoritative checkout snapshot

reservation/debit semantics

idempotency/double-spend protection

rollback/release when appropriate

Mixed wallet + PSP only if actually modeled/proven.
Otherwise explicitly defer mixed tender.

I — Refund Compatibility

If Refund-to-wallet is supported:

event/process credits exactly once

idempotent

linked to refund/order
Otherwise defer honestly.

J — Customer APIs

Implement:

wallet summary

derived balance

paginated ledger/history

redeem gift card

checkout apply if implemented

Own-only; foreign wallet denied.

K — Admin APIs

Gift Card:

issue

list/search/filter

inspect

revoke/disable

redemption history

Wallet:

inspect ledger

adjustment only with permission + reason + immutable entry

never set balance directly

Add canonical Access Control permissions as required.

L — Customer UI

Reuse exact Shopeiva wallet source.
Required:

real balance

real history

redeem Gift Card

real feedback

loading/empty/error

mobile

Hide unsupported fake deposit/card controls.

M — Admin UI

Use accepted Admin shell + Shopeiva-derived patterns.
Required:

Gift Card list/search/filter

issue

detail

revoke/status

redemption history

Wallet inspect if implemented

No foreign finance dashboard.

N — Navigation + Access Control

Once live:

Customer Wallet/Gift Card nav

Admin Gift Card/Wallet operational nav

Permission-projected.
Customer own-only.
Seller gets no Customer-wallet authority unless explicit future rule.
Tenant isolation required.

O — Notifications

Use real Notification module for meaningful:

Gift Card redeemed

refund wallet credit

customer-facing Admin adjustment

No fake realtime.

P — Development Seeding — MANDATORY

Development/Test only, idempotent.

Seed:

Customer wallet

multiple real ledger entries

valid unused Gift Card

redeemed/part-used card per adopted policy

expired/disabled card

Admin-visible redemption history

No Production pollution.

Q — E2E

No direct DB mutation.

Prove:
1 customer opens wallet
2 real balance/history
3 redeem valid Gift Card
4 ledger/balance changes
5 duplicate redeem no double credit
6 invalid/expired rejected
7 Admin sees redemption
8 foreign wallet denied
9 wallet checkout spend if implemented
10 refund-to-wallet if implemented

R — Browser Proof

Capture:
Customer wallet/history/redeem/success/error/mobile.
Admin Gift Card list/issue/detail/redemption.
Original Shopeiva side-by-side.

S — Visual Fidelity

Prove:
CSS/JS/animation/transition/hover/focus/responsive/source geometry preserved.
Fake source deposit/cards not ported as fake behavior.
Unauthorized deviation = NONE.

T — Tests

Minimum:
1 own wallet
2 foreign deny
3 ledger-derived balance
4 duplicate credit idempotent
5 valid gift redemption
6 invalid reject
7 expired reject
8 disabled reject
9 duplicate redemption safe
10 currency mismatch
11 admin issue
12 admin revoke
13 admin permission deny
14 tenant isolation
15 checkout debit if implemented
16 insufficient funds
17 refund credit if implemented
18 seed idempotency

U — Validation

Backend:
dotnet restore src/backend/Tooba.slnx
dotnet build src/backend/Tooba.slnx
dotnet test src/backend/Tooba.slnx

Require 0 warnings / 0 errors / 0 failed / 0 skipped.

Frontend:
cd src/frontend
npm run typecheck
npm run lint
npm run test
npm run test
npm run test
npm run test
npm run build

Also run wallet/gift/admin/access-control/navigation/i18n tests.
Always git diff --check.

V — Runtime MUST REMAIN ALIVE

After build/tests KEEP RUNNING:

Backend :5088

Tooba Frontend :3000

Original Shopeiva :3001

Verify health/live + health/ready and seeded Wallet/Gift Card pages.

W — USER-PREVIEW

Return exact concrete URLs, no placeholders:

Customer Wallet

Customer Gift Card/redeem

Admin Gift Cards

Admin seeded Gift Card

Admin Wallet inspect if live

Original Shopeiva comparison

exact dev preview identity/context

X — Readiness

Report Customer/Admin/Wallet/Gift Card/Product sale/Marketplace readiness,
remaining gaps, real PSP blocker, Settings, Advanced Variant deferred.

Y — Source of Truth

Update:
P06 = IN_PROGRESS
TB-P06-T025 = ACCEPTED
TB-P06-T026 = AWAITING_ARCHITECT_ACCEPT

WALLET_BACKEND = LIVE | PARTIAL
WALLET_LEDGER = LIVE
CUSTOMER_WALLET_UI = LIVE
GIFT_CARD_BACKEND = LIVE
GIFT_CARD_REDEMPTION = LIVE
ADMIN_GIFT_CARD_UI = LIVE
WALLET_CHECKOUT = LIVE | DEFERRED
REFUND_TO_WALLET = LIVE | DEFERRED
WALLET_DEMO_SEED = LIVE_DEV_ONLY
WALLET_USER_PREVIEW = READY
VISUAL_CONTRACT = SHOPEIVA_LOCKED

Update canonical recovery docs.

Acceptance Criteria

PASS requires:

immutable ledger

no mutable authoritative balance

real Gift Card lifecycle

secure/idempotent redemption

Customer own wallet

Admin operations

Access Control

no fake deposit/cards

real seeded preview

source-derived Shopeiva UI

browser proof

backend 0/0/0/0

frontend green

runtimes alive

exact preview URLs

repo synchronized

Worker may report:
WALLET_GIFT_CARD_LIVE
WALLET_USER_PREVIEW_READY

Worker must NOT report:
MIXED_TENDER_LIVE unless proven
REFUND_TO_WALLET_LIVE unless proven
USER_VISUAL_ACCEPTED
PRODUCT_FULLY_READY
PRODUCTION_GO_LIVE_READY

Git

git add ...
git commit -m "feat add wallet and gift card [TB-P06-T026]"
git push origin main
git fetch origin
git rev-parse HEAD
git rev-parse origin/main
git status --short --branch

Require HEAD == origin/main and tracked tree clean.

Result Contract

PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_WORKER_RESULT

Task-ID:
TB-P06-T026
Channel:
tooba-main
Status:
PASS | FAIL | BLOCKED | RECOVERY_CONFLICT

Summary:
...

Audit:

Wallet:

Gift Card:

Checkout:

Refund:

Wallet:

owner:

ledger:

authoritative balance:

currencies:

idempotency:

foreign access:

GiftCard:

secure code:

issue:

redeem:

remaining value:

expiration:

revoke:

duplicate redemption:

Checkout:

wallet spend:

partial wallet+PSP:

reservation/debit:

rollback:

deferred:

Refund:

refund-to-wallet:

idempotency:

deferred:

Customer:

API:

wallet:

history:

redeem:

UI:

fake deposit:

fake cards:

Admin:

issue:

list/search:

revoke:

redemption history:

wallet inspect:

arbitrary set balance:

Access Control:

UI:

Seed:

Development only:

idempotent:

wallet:

ledger entries:

unused card:

redeemed card:

expired/disabled:

E2E:

balance:

valid redeem:

duplicate:

invalid:

expired:

Admin inspect:

foreign deny:

checkout:

refund:

direct DB mutation:

Visual-Fidelity:

Shopeiva source:

CSS:

JS:

animation:

transition:

hover/focus:

responsive:

fake source actions removed:

unauthorized deviation:

Validation:

backend restore:

backend build:

backend tests:

warnings:

errors:

failed:

skipped:

typecheck:

lint:

frontend tests:

frontend build:

git diff --check:

Runtime:

Backend:

Frontend:

Shopeiva:

health/live:

health/ready:

kept alive after Result:

USER-PREVIEW:

Customer Wallet:

Customer Gift Card:

Admin Gift Cards:

Admin Gift Card:

Admin Wallet:

Original Shopeiva:

dev preview identity/context:

Readiness-After:

Customer %:

Admin %:

Wallet %:

Gift Card %:

Product sale readiness %:

Marketplace readiness %:

production blockers:

remaining gaps:

Source-of-Truth:

P06:

T025:

T026:

Wallet:

Gift Card:

checkout:

refund:

seed:

user preview:

visual contract:

Git:

commit:

push:

final HEAD:

origin/main:

synchronized:

tracked tree:

Architectural-Concerns:
...
Visual-Concerns:
...
Blockers:
...

END_TOOBA_WORKER_RESULT

After Result:

return to IDLE

DO NOT STOP Backend / Tooba Frontend / Original Shopeiva

do not self-issue another Task

END_TASK
