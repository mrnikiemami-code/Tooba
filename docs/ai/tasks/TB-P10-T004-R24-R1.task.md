PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_REPAIR_TASK

Task-ID:
TB-P10-T004-R24-R1

Parent-Task:
TB-P10-T004-R24

Channel:
tooba-main

WorkerId:
tooba-worker-01

AgentType:
cursor

Status:
REPAIR

USER_VISUAL_ACCEPTED=NO

Title:
Authenticated Storefront Continuity Repair — Preserve Cart Through Login/Shipping + Canonical Account Header + Address Name Split

Architect Reopen Decision

TB-P10-T004-R24 was previously ACCEPTED based on automated/runtime evidence, but the user has now manually found a real storefront defect.

Therefore the customer-facing identity/cart portion is REOPENED for repair.

Observed defects from user runtime:

After successful login, the real Cart is actually empty — not merely a badge rendering issue.

Shipping still has checkout/order summary data while the active Cart has already disappeared too early.

Header still shows ورود even though the customer is authenticated.

Authenticated storefront must expose a consistent account affordance on ALL storefront pages:

account/customer area

customer orders

logout

Before inventing new account UI, inspect the purchased Shopeiva template. If Shopeiva already contains customer/account/order/logout menu or panel patterns, REUSE them with minimal change.

Shipping address currently uses one combined full-name field. It must be changed to separate:

FirstName

LastName
in UI, API, persistence/snapshot as appropriate.

The user explicitly confirms:

Cart must remain populated after login and throughout Shipping.

Cart must only be converted/emptied AFTER the authoritative atomic checkout commit succeeds when moving into real Payment.

Authenticated account/header experience must be the same across all storefront pages.

Do NOT redesign the storefront.
Do NOT create a second account UI if Shopeiva already provides one.
Do NOT move Cart contents into auth Session.
Do NOT weaken LOCK-SF-103 atomic checkout.
Do NOT create TB-P10-T005.

Core Architecture Clarification

Session/Auth stores CUSTOMER IDENTITY.

Cart remains server-side persistent commerce state.

Required model:

Anonymous Cart
→ login
→ server-side adopt/merge into authenticated Active Cart
→ Cart remains Active with all lines
→ Shipping uses same authenticated Active Cart
→ Cart badge/count remains correct
→ user clicks final ادامه به پرداخت
→ backend atomic checkout:
limits → validation → reserve → Order → Cycle #1 → Cart Converted → COMMIT
→ ONLY AFTER successful COMMIT may old Cart become Converted/empty

If login, merge, shipping navigation, address save, or page refresh empties/converts the Cart, that is a defect.

Recovery State

Phase: P10
Last Architect-accepted: TB-P10-T004-R24
Last Implementation: TB-P10-T004-R24
Current Repair: TB-P10-T004-R24-R1
Worker: tooba-worker-01
Expected branch: main
Expected previous HEAD: 95b92d69e1e7bebfddb50ffd92956e8acb1ddfed
USER_VISUAL_ACCEPTED=NO

Applicable Locks

Read:

docs/architecture/TOOBA-LOCKS.md

docs/architecture/STOREFRONT-CHECKOUT-PLAN.md

Preserve LOCK-SF-001…116.

Especially:

LOCK-SF-103 atomic checkout

LOCK-SF-104 hide presentation-only

LOCK-SF-105 canonical customer cancel

LOCK-SF-106…111 identity/login/cart merge

backend-authoritative identity

Cart != Session

Cart != Reservation

Cart persists until checkout commit

no frontend optimistic Cart clear

Shopeiva structure is locked UI contract

FA RTL / EN LTR

1. Recovery Preflight

Record:

Phase

Last Architect-accepted

Last Implementation

Current Repair

Worker

branch

HEAD

origin/main

HEAD==origin/main

tracked dirty state

task-owned untracked state

Unexpected tracked divergence => BLOCK.

Evidence:
docs/evidence/TB-P10-T004/r24-r1-recovery-start.md

2. Mandatory Root-Cause Discovery — Cart Is Truly Empty

Trace the exact state changes for:

A. anonymous Cart before login
B. OTP login completes
C. anonymous Cart merge/adopt executes
D. authenticated Cart lookup
E. redirect to Shipping
F. Shipping page load
G. address create/update
H. final continue-to-payment submit
I. Cart Converted

Inspect:

anonymous cart ID/token

authenticated customer Cart association

merge/adopt implementation

active cart resolver

header cart resolver

Shipping cart/projection resolver

any cleanup/detach after login

any cart rotation triggered by login

any Converted write before final checkout submit

any frontend clearing after login/redirect

Create:
docs/evidence/TB-P10-T004/r24-r1-cart-continuity-discovery.md

Required table:

Stage | Anonymous Cart ID | Authenticated Cart ID | Cart Status | Lines | CustomerId | Mutation source | Expected?

Find the exact line/path causing the Cart to become empty.

Do not patch symptoms first.

3. Locked Cart Continuity Invariant

After successful login/merge:

one canonical authenticated Active Cart must contain the merged lines

lines remain persisted server-side

quantities remain exact

header cart count is read from that same authoritative cart

Shipping reads from that same cart

navigating between Cart / Shipping / Home / PDP must not clear it

refresh must not clear it

address save must not clear it

auth refresh/session refresh must not clear it

No Cart conversion before final checkout commit.

4. Anonymous -> Authenticated Cart Merge Repair

Prove/fix:

Case A — only anonymous Cart exists

After login:

adopt or merge into authenticated Active Cart

original lines preserved

authenticated active cart has same lines

no empty replacement cart may shadow it

Case B — authenticated customer already has Active Cart

merge anonymous lines into it

same Offer/Variant canonical line merges qty

distinct lines preserved

no duplicate cart

decimal qty preserved

unavailable line remains visible/actionable

no reservation

Case C — repeated login/session refresh

idempotent

does not merge twice

does not zero cart

does not duplicate quantity

5. Cart Must Survive Shipping

Critical proof:

After login and merge:

/fa/cart shows N lines

header badge = N

/fa/shipping shows same N lines/order summary

browser back to /fa/cart still shows N lines

reload Shipping still shows N lines

save/change address still shows N lines

no Order yet if final continue-to-payment has not been committed

no Reservation Cycle yet

Cart status remains Active

This directly covers the user-reported defect.

6. Exact Conversion Boundary

Only when user presses the final checkout action that commits shipping/order:

Before successful backend response:

Cart Active

lines present

header badge N

Backend transaction:

identity/limits

validate Cart/prices/shipping

reserve

Order aggregate

Cycle #1

source Cart Converted

COMMIT

After success:

old Cart Converted

header may show 0/new Active Cart

pending Order exists

Payment page may open

If transaction fails:

Cart remains Active with original lines

badge remains N

no partial Order/reservation/cycle

Re-run R21 atomic assertions focused on this boundary.

7. Canonical Authenticated Header / Account UX

The user is authenticated but header still shows ورود.

This must be repaired across ALL storefront pages.

First inspect purchased Shopeiva/reference template for existing:

authenticated customer menu

account/profile dropdown

customer dashboard/panel link

orders link

logout

user icon pattern

mobile equivalent

Evidence:
docs/evidence/TB-P10-T004/r24-r1-shopeiva-account-discovery.md

Mandatory rule

If Shopeiva has an account/customer menu/panel pattern:
REUSE it with minimal visual change.

Do NOT invent a new visual pattern.

Authenticated header must show consistently:

account/customer identity affordance

سفارش‌های من / Customer Orders

حساب کاربری / Customer Panel if existing route/surface exists

خروج از حساب کاربری

If an existing customer panel already exists in project/template:
wire to it.

If only Customer Orders exists:
use existing route and do not invent a large new panel in this repair.

8. One Header State Across All Storefront Routes

The same canonical StorefrontShell auth state must apply to:

Home

PLP

PDP

Cart

Shipping

Payment

customer order pages

Login route after authenticated redirect as applicable

Required:

logged out => ورود

logged in => account affordance, not ورود

logout => immediately returns to logged-out header state

no page-specific stale auth state

no separate header auth implementations

Desktop + mobile.

9. Logout

Authenticated customer must have a visible Logout action.

Logout must:

call canonical existing logout/session endpoint

clear auth session

not delete/cancel Orders

not expose authenticated Cart to next anonymous user

update header immediately/authoritatively

redirect safely to storefront or current safe route

After logout:

customer orders inaccessible until login

authenticated Cart remains account-owned and is not leaked

anonymous browsing still works

10. Customer Orders / Customer Panel

Inspect existing routes/template.

Required:

account menu exposes سفارش‌های من

route must use existing customer orders implementation if it exists

if customer panel exists, expose it

do not create a duplicate panel

Pending-payment Orders, paid Orders, cancelled Orders remain visible according to existing customer order semantics.

Hidden pending card does NOT hide Order from customer Orders/history.

11. Address Model — Split FirstName / LastName

Current UI uses combined:
نام و نام خانوادگی

Repair to separate:

نام

نام خانوادگی

Also retain:

Mobile

Province

City

Address

PostalCode

Do NOT implement this as two frontend inputs concatenated back into one opaque FullName field unless legacy compatibility requires a transitional adapter.

Preferred canonical model:

FirstName

LastName

Update:

frontend form

validation

API contracts

persistence

shipping/address projection

recipient/order snapshot used for committed Order

Admin/customer display mapping

Existing historical records with FullName:

must not crash

migrate/backfill only if safe/deterministic

otherwise compatibility fallback for old rows

do not guess split if ambiguous

For new addresses:
FirstName and LastName required separately.

Evidence:
docs/evidence/TB-P10-T004/r24-r1-address-name-model.md

12. Address Validation

Required:

FirstName required

LastName required

Mobile existing validation preserved

Province required

City required

Address required

PostalCode existing rules preserved

FA labels correct.
EN labels smoke.

No raw validation codes.

13. Header Cart Count Source

Header badge MUST derive from canonical current Active Cart.

It must not:

cache pre-login anonymous cart forever

switch to an empty newly-created cart while merged cart exists

use local-only count as source of truth

After login merge:

badge matches server active cart

all storefront pages use same count

After final checkout commit:

badge reflects new/empty active cart

14. Focused Tests

Cart merge/continuity:

anonymous Cart 2 lines

login

authenticated Active Cart still has 2 lines

header badge=2

Shipping has same 2 lines

reload Shipping -> still 2

back Cart -> still 2

address save -> still 2

no Order before final commit

no Reservation before final commit

repeated session refresh no duplication/loss

existing authenticated cart merge correct

decimal 1.25 exact

Conversion:
14. final commit success -> Cart Converted
15. only after success badge becomes 0/new active cart
16. final commit failure -> Cart remains populated
17. atomic rollback regression

Header/auth:
18. logged-out header shows Login
19. logged-in header no longer shows Login
20. Orders link visible
21. account/customer link visible if existing
22. Logout visible
23. logout works
24. all storefront routes share same auth state
25. mobile header same semantics

Address:
26. FirstName separate
27. LastName separate
28. both persisted independently
29. committed shipping/order snapshot retains both
30. historical old address does not crash
31. FA validation
32. EN smoke

Regression:
33. pending-payment section
34. cancel/hide
35. open-unpaid/churn
36. R21 atomic checkout
37. recovery guard
38. git diff --check

Do NOT run unrelated full suites.

15. Real Browser Runtime — REQUIRED

Use Host :5088 + FE :3000.

A. logout / fresh anonymous state
B. add 2 products
C. verify Cart count=2
D. continue -> Login
E. login 09111111111 / 123456
F. immediately verify header is authenticated
G. verify Cart count STILL=2
H. Shipping has same 2 lines
I. create Address with separate FirstName/LastName
J. save address; Cart STILL=2
K. navigate Home -> Cart -> Shipping; header/account/cart state consistent
L. open account menu
M. Customer Orders link works
N. Logout works
O. login again; authenticated cart restored correctly
P. final continue-to-payment succeeds
Q. ONLY NOW Cart converted/badge 0
R. Payment page Order summary correct
S. fault final commit -> Cart remains populated
T. mobile header/account/cart smoke

Capture screenshot evidence for:

authenticated header

Cart after login with non-zero badge

Shipping before final commit with non-zero badge

address modal separate FirstName/LastName

account dropdown/menu

after final commit badge zero

16. Security

Prove:

wrong user cannot see merged Cart

logout does not leak Cart

customer orders require correct auth

no cart ID takeover

no auth state inferred only from frontend

no stale session display

address ownership correct

17. Performance / Network

Required:

no auth polling

no cart polling

one/focused auth/session fetch pattern

no N+1 header/cart lookups

no repeated merge on every page navigation

no duplicate address save

Document request counts.

18. Anti-Pattern Gate

Scan for:

Cart stored in auth Session as line data

frontend-only cart persistence

localStorage as Cart SoT

duplicate Storefront headers

route-specific auth hacks

manually forcing badge count

creating empty authenticated cart that shadows merged cart

merge on every request/navigation

optimistic Cart clear

FirstName/LastName re-concatenated as sole new storage

raw FullName parsing guesses

duplicate customer panel

logout implemented as UI-only

polling/retry hacks

Any occurrence => REPAIR.

Evidence:
docs/evidence/TB-P10-T004/r24-r1-antipattern-scan.md

19. Lock Registry

Add non-duplicate locks equivalent to:

Active Cart is persistent server commerce state; auth Session carries identity, not Cart line state.

Login/cart merge must preserve the authenticated Active Cart until the final atomic checkout commit converts it.

Storefront authenticated header/account state is canonical and shared across all storefront routes.

Customer logout is always available when authenticated and uses canonical session logout.

New shipping addresses store FirstName and LastName separately; committed order recipient snapshots preserve both independently.

20. Evidence

Add under docs/evidence/TB-P10-T004/:

r24-r1-recovery-start.md

r24-r1-cart-continuity-discovery.md

r24-r1-shopeiva-account-discovery.md

r24-r1-cart-merge-repair.md

r24-r1-header-account.md

r24-r1-logout.md

r24-r1-customer-orders.md

r24-r1-address-name-model.md

r24-r1-atomic-boundary-regression.md

r24-r1-security.md

r24-r1-network-performance.md

r24-r1-focused-validation.md

r24-r1-runtime.md

r24-r1-antipattern-scan.md

r24-r1-recovery-sot.md

21. Recovery / Pipeline

At completion:

Phase P10

Last Architect-accepted before review: TB-P10-T004-R24

Last Implementation: TB-P10-T004-R24-R1

Current Issued/Repair: none

USER_VISUAL_ACCEPTED=NO

Worker: tooba-worker-01 — IDLE

branch main

ending HEAD recorded

HEAD==origin/main

task-owned worktree clean

no TB-P10-T005

Run recovery guard.

Acceptance

PASS only if ALL:

real Cart does not empty on login

Cart remains Active through Shipping/address work

header badge remains correct before final checkout commit

Cart converts only after successful atomic checkout commit

failure leaves Cart intact

authenticated header no longer displays ورود

same account/header semantics across all storefront pages

Shopeiva existing account/customer UI reused where available

customer Orders link works

logout is present and works

no cross-session/customer Cart leak

FirstName/LastName are separate in UI and canonical new data model

old address data does not crash

runtime A–T PASS

focused tests green

anti-pattern scan CLEAN

recovery exact

HEAD==origin/main

Worker IDLE

no TB-P10-T005

Result Contract

Return ONLY canonical BRIDGE-WAKE-V1 Result with:

Summary
Applicable-Locks
Recovery-Start
Cart-Continuity-Discovery
Shopeiva-Account-Discovery
Cart-Merge-Repair
Header-Account
Logout
Customer-Orders
Address-Name-Model
Atomic-Boundary-Regression
Security
Network-Performance
Focused-Validation
Runtime
AntiPattern-Scan
Lock-Registry
Recovery-SoT
Git
Architectural-Concerns
Visual-Concerns
Blockers

END_TOOBA_REPAIR_TASK