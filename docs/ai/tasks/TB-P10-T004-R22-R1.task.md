PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_REPAIR_TASK

Task-ID:
TB-P10-T004-R22-R1

Parent-Task:
TB-P10-T004-R22

Channel:
tooba-main

WorkerId:
tooba-worker-01

AgentType:
cursor

Status:
REPAIR

USER_VISUAL_ACCEPTED=YES

Title:
First Canonical Login Page + Checkout Auth Gate + Development OTP Fixture + Cart Merge

Architect Review

TB-P10-T004-R22 is correctly BLOCKED and is NOT accepted as implementation.

Discovery established:

there is no existing frontend Login page

no /login route exists

current auth is Host/BFF password login only

OTP exists only as identifier verification, not as a customer login UI

Cart has no merge API

checkout/shipping/payment are currently anonymous-capable

The user previously believed a Login page existed. Repository discovery is authoritative for implementation.

Architect now explicitly AUTHORIZES creation of the FIRST canonical customer Login page, with minimal Shopeiva-consistent UI, reusing existing auth/BFF infrastructure and existing Identity OTP mechanism where possible.

This repairs R22's blocker.

Do NOT create a second/parallel auth stack.
Do NOT redesign auth globally.
Do NOT implement R23 MaxOpenUnpaidOrders/churn quotas yet.
Do NOT create TB-P10-T005.

Product Decision

Default:
CheckoutIdentityPolicy = AuthenticatedOnly

Allowed anonymously:

Home / PLP / PDP

AddToCart

Cart

Authentication required:

Shipping

Checkout

Payment

Submit checkout

Future optional:
GuestAllowed

Default remains AuthenticatedOnly.

Development Fixture

Development/Test only:

Mobile:
09111111111

OTP:
123456

This must be implemented through an environment-gated development auth/OTP path.

Production must NOT accept this fixed OTP unless supplied by the real configured provider.

Recovery State

Phase: P10
Last Architect-accepted: TB-P10-T004-R21-R1
Last Implementation: TB-P10-T004-R21-R1
Current Repair: TB-P10-T004-R22-R1
Worker: tooba-worker-01
Expected branch: main
Expected current HEAD: dba67566abce8425cce41a70a6ac877ce5dd38d5
USER_VISUAL_ACCEPTED=YES

Applicable Locks

Read:

docs/architecture/TOOBA-LOCKS.md

docs/architecture/STOREFRONT-CHECKOUT-PLAN.md

Preserve LOCK-SF-001…105.

Especially:

atomic checkout LOCK-SF-103

hide presentation-only LOCK-SF-104

canonical customer cancel LOCK-SF-105

backend-authoritative auth/capabilities

Shopeiva locked storefront language

FA RTL / EN LTR

no guest secret leakage

1. Recovery Preflight

Record:

Phase

Last Architect-accepted

Last Implementation

Current Repair

Worker

branch

HEAD

origin/main

HEAD==origin/main

tracked dirty state

task-owned untracked state

Unexpected tracked divergence => BLOCK.

Evidence:
docs/evidence/TB-P10-T004/r22-r1-recovery-start.md

2. Reconfirm Auth Discovery

Reconfirm:

current Host/BFF login endpoint contracts

current auth/session cookie behavior

current Identity OTP verify mechanism

customer identity model

cart ownership model

logout flow

locale routing pattern

Document exact APIs reused.

Evidence:
docs/evidence/TB-P10-T004/r22-r1-auth-discovery.md

3. Create First Canonical Customer Login Route

Create canonical locale-aware route:

/fa/login

/en/login

Minimal UI only.

Use existing Shopeiva visual language:

storefront header/chrome if appropriate

mobile input

OTP input

submit

error state

loading state

Do NOT create:

modal login

alternate checkout-specific login form

duplicate BFF auth stack

social login

password reset

registration redesign

This route becomes the canonical customer login experience.

4. OTP Login Flow

Preferred flow:

customer enters mobile

backend sends/creates OTP through existing Identity OTP mechanism

customer enters OTP

OTP verified

customer session established using current auth/session architecture

returnTo honored safely

If current Identity verification cannot directly establish customer session:

add the minimum canonical bridge in backend/BFF

do not invent a parallel identity store

document identity binding

Development:

for 09111111111, OTP 123456 works only in Development/Test

production path uses configured provider/normal OTP generation

5. No Password UI Unless Required Internally

The existing BFF password login may remain for Admin/internal auth if already used.

Customer login UX in this task should be mobile + OTP.

Do NOT expose admin/internal username/password login to storefront customers.

If session issuance internally reuses an existing auth service, do so behind backend boundary.

6. Checkout Identity Policy

Add Store-level:

CheckoutIdentityPolicy

Values:

AuthenticatedOnly

GuestAllowed

Default:
AuthenticatedOnly

Backend returns effective policy.

Admin Settings:
هویت مشتری در فرایند خرید

Options:

ورود الزامی

خرید مهمان مجاز

Warning on GuestAllowed:
خرید مهمان می‌تواند کنترل سفارش‌های پرداخت‌نشده و محدودیت‌های رزرو موجودی را کاهش دهد.

Do not overstate; informational only.

7. Backend Enforcement

Under AuthenticatedOnly, backend must reject unauthenticated access/submit for:

Shipping projection/commit where customer identity is required

Checkout submission

Payment initiation/access

other checkout-sensitive endpoints discovered

Frontend redirect alone is insufficient.

Use stable machine code equivalent:
checkout.authentication_required

FA:
برای ادامه فرایند خرید وارد حساب خود شوید.

8. Route Gate

Anonymous:

Cart allowed

click continue -> redirect to canonical Login

safe returnTo

After login:

return to intended route

Preserve locale.

No open redirect.
No secrets in URL.

9. Anonymous Cart Merge API

Implement one canonical backend operation triggered after successful login.

Inputs derived from server-controlled anonymous cart/session ownership, not arbitrary user-supplied cart IDs alone.

Rules:

anonymous Cart survives login

authenticated Active Cart is not overwritten

same canonical line merges quantity

distinct lines remain distinct

decimal quantities preserved

price revalidated

availability revalidated

no Reservation

no Order

no Payment

If an authenticated cart does not exist:

adopt/associate or create canonical authenticated cart using existing domain semantics

10. Merge Conflict UX

If one line is now unavailable:

do not silently drop it

keep it visible with availability status or return an actionable merge result

user can fix Cart before checkout

If quantity exceeds current allowed availability:

return canonical business outcome

do not reserve stock during merge

11. Existing Pending Orders

Login/merge must not mutate:

existing pending Orders

reservation cycles

payment timers

hide preferences

cancel state

No anonymous historical Order ownership transfer unless a secure existing binding path exists.
Document current behavior.

12. Logout Isolation

Required:

account-owned pending Orders remain account-owned

authenticated Cart cannot leak to next anonymous/customer session

logout clears/rotates auth session correctly

fresh anonymous Cart context used after logout if needed

13. Security

Prove:

no open redirect

no session fixation

no cross-customer cart merge

no arbitrary anonymous cart takeover

no fixed OTP in Production

no OTP logging

no guest/payment secret in returnTo/query

no frontend-only checkout auth

no customer identity inferred solely from cart token

14. Focused Tests

Auth:

/fa/login exists

/en/login exists

mobile+OTP flow works

09111111111 / 123456 succeeds in Development

fixed OTP fails/not enabled in Production mode

session established

logout works

Policy:
8. default AuthenticatedOnly
9. Store setting can switch GuestAllowed
10. backend returns effective policy

Gate:
11. anonymous Cart allowed
12. anonymous Shipping gated
13. anonymous Checkout gated
14. anonymous Payment gated
15. backend submit rejects anonymous
16. authenticated customer passes

Return:
17. internal returnTo accepted
18. external returnTo rejected
19. locale preserved

Merge:
20. anonymous-only cart -> authenticated cart
21. authenticated cart + anonymous cart merge
22. same line quantity merge
23. distinct lines preserved
24. decimal 1.25 exact
25. unavailable line not silently lost
26. no Reservation
27. no Order
28. no Payment
29. no cross-user merge

Regression:
30. pending Orders unchanged
31. R21 atomic checkout preserved
32. cancel/hide preserved
33. recovery guard
34. git diff --check

Do NOT run unrelated full suites.

15. Real Browser Runtime — REQUIRED

Use Host :5088 + FE :3000.

A. anonymous user adds 2+ items
B. Cart opens without login
C. continue checkout
D. redirected to /fa/login
E. enter 09111111111
F. enter OTP 123456
G. session established
H. return to intended shipping/checkout route
I. original Cart lines still present
J. authenticated pre-existing Cart + anonymous Cart merge
K. no Reservation exists before final checkout submit
L. final checkout still honors R21 atomic commit
M. logout isolation
N. Admin Settings switch AuthenticatedOnly/GuestAllowed
O. restore AuthenticatedOnly

16. Visual Gate

Login page:

FA RTL

EN LTR

mobile

desktop

Shopeiva-consistent

no redesign

clear OTP progression

no technical codes

Do not claim USER_VISUAL_ACCEPTED changes; it remains YES for already accepted surfaces, but new Login page still requires smoke evidence.

17. Anti-Pattern Gate

Scan for:

duplicate auth stack

storefront password login

hardcoded OTP production bypass

frontend-only gate

open redirect

localStorage cart merge SoT

direct client-selected customer/cart ownership

merge creating Reservation/Order/Payment

silent line drop

cart overwrite

polling

retry hacks

Any occurrence => REPAIR.

Evidence:
docs/evidence/TB-P10-T004/r22-r1-antipattern-scan.md

18. Lock Registry

Add non-duplicate locks equivalent to:

canonical customer Login route is locale-aware mobile+OTP and reuses existing Identity/session architecture

default CheckoutIdentityPolicy is AuthenticatedOnly

Cart may be anonymous; Shipping/Checkout/Payment require auth under AuthenticatedOnly

auth gate is backend-enforced

anonymous cart merge is server-authoritative and creates no reservation/order/payment

fixed Development OTP is environment-gated only

19. Planned NEXT Task — DO NOT IMPLEMENT

After R22-R1 PASS:

TB-P10-T004-R23
Max Open Unpaid Orders + Reservation Churn Anti-Abuse + Admin Settings

Planned:

MaxOpenUnpaidOrdersPerCustomer

ReservationCommitWindowMinutes

MaxCheckoutCommitsPerCustomerInWindow

cancellation frees open-order slot, but does not refund churn quota

hide does not free open-order slot

CustomerId + Store scoped

concurrency-safe

pre-reservation/pre-Order enforcement

Admin Settings

optional Offer-level flash-sale quota only if discovery justifies

20. Evidence

Add:

r22-r1-recovery-start.md

r22-r1-auth-discovery.md

r22-r1-login-page.md

r22-r1-otp-login.md

r22-r1-identity-policy.md

r22-r1-login-gate.md

r22-r1-cart-merge.md

r22-r1-security.md

r22-r1-admin-settings.md

r22-r1-focused-validation.md

r22-r1-runtime.md

r22-r1-visual-smoke.md

r22-r1-antipattern-scan.md

r22-r1-recovery-sot.md

under:
docs/evidence/TB-P10-T004/

21. Recovery / Pipeline

At completion:

Phase P10

Last Architect-accepted before review: TB-P10-T004-R21-R1

Last Implementation: TB-P10-T004-R22-R1

Current Issued/Repair: none

USER_VISUAL_ACCEPTED=YES

Worker: tooba-worker-01 — IDLE

branch main

ending HEAD recorded

HEAD==origin/main

task-owned worktree clean

no TB-P10-T005

Run recovery guard.

Acceptance

PASS only if ALL:

first canonical Login page exists

mobile+OTP customer flow works

Development fixture works only in Development/Test

AuthenticatedOnly default

Cart anonymous-capable

Shipping/Checkout/Payment auth-gated

backend enforcement exists

safe returnTo

anonymous Cart merge works

authenticated existing Cart merge works

decimal exact

unavailable lines not lost

merge creates no Reservation/Order/Payment

pending Orders unchanged

logout isolation correct

Admin Setting works

security passes

runtime A–O PASS

visual smoke pass

anti-pattern scan CLEAN

focused tests green

recovery exact

HEAD==origin/main

Worker IDLE

no TB-P10-T005

Result Contract

Return ONLY canonical BRIDGE-WAKE-V1 Result with:

Summary
Applicable-Locks
Recovery-Start
Auth-Discovery
Login-Page
OTP-Login
Identity-Policy
Login-Gate
Cart-Merge
Security
Admin-Settings
Focused-Validation
Runtime
Visual-Smoke
AntiPattern-Scan
Lock-Registry
Recovery-SoT
Git
Architectural-Concerns
Visual-Concerns
Blockers

END_TOOBA_REPAIR_TASK