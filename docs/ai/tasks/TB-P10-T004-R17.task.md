PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_REPAIR_TASK

Task-ID:
TB-P10-T004-R17

Parent-Task:
TB-P10-T004-R16

Channel:
tooba-main

WorkerId:
tooba-worker-01

AgentType:
cursor

Status:
REPAIR

USER_VISUAL_ACCEPTED=NO

Title:
Admin Reservation Cycle Audit UX — Current Reservation State, Cycle History, Retry Limit and Supply Context

Architect Review

TB-P10-T004-R16 is ACCEPTED.

Accepted:

Cart has a dedicated در انتظار پرداخت section independent of Active Cart

pending payment projection is batched

countdown derives from server ExpiresAt

failed Payment Attempt does not reset hold

expired retry stays on same Order and uses backend reacquire

Manual AwaitingAdmin is informational, not pay-again

guest ownership uses committed proof, not Active Cart

runtime A–K PASS

anti-pattern scan CLEAN

HEAD==origin/main

Worker IDLE

This task now exposes the reservation-cycle model to Admin in a professional operational/audit view.

Do NOT change lifecycle rules from R15/R16.
Do NOT redesign Admin Order Detail.
Do NOT add reservation mutation shortcuts from UI.
Do NOT add polling.
Do NOT create TB-P10-T005.

Recovery State

Phase: P10
Last Architect-accepted: TB-P10-T004-R16
Last Implementation: TB-P10-T004-R16
Current Repair: TB-P10-T004-R17
Worker: tooba-worker-01
Expected branch: main
Expected previous HEAD: e3f5d5dbe43d841d035531f4fb3c294327dc1ff4
USER_VISUAL_ACCEPTED=NO

Applicable Locks

Read:

docs/architecture/TOOBA-LOCKS.md

docs/architecture/STOREFRONT-CHECKOUT-PLAN.md

Preserve:

LOCK-SF-001…090

Reservation Cycle != Payment Attempt

immutable cycle history

same Order across retries

retry inside Active cycle does not reset ExpiresAt

new cycle only after expiry/release + successful reacquire

max cycles enforced

server time authoritative

SupplyStatus separate from PaymentStatus and reservation-cycle status

backend-authoritative capabilities

AppDataGrid canonical

no raw technical IDs/errors in normal UI

no N+1

1. Recovery Preflight

Record:

Phase

Last Architect-accepted

Last Implementation

Current Repair

Worker

branch

HEAD

origin/main

HEAD==origin/main

tracked dirty state

task-owned untracked state

Unexpected tracked divergence => BLOCK.

Evidence:
docs/evidence/TB-P10-T004/r17-recovery-start.md

2. Mandatory Discovery

Inspect current Admin:

Orders grid

Order Detail

Payments grid/detail

SupplyStatus card from R8

operational history/timeline

reservation-cycle projection from R15

retry/recovery capabilities

current status badge design

current modal/dialog patterns

AppDataGrid column/filter capabilities

Create:
docs/evidence/TB-P10-T004/r17-admin-reservation-audit-discovery.md

Document exact insertion points and avoid parallel UI.

3. Admin Business Model

Admin must be able to answer, without reading technical logs:

Is inventory currently reserved for this Order?

Which reservation cycle is active/current?

When does it expire?

How many cycles have been used?

How many retries remain?

Why did previous cycles end?

Was stock reacquire attempted?

Did reacquire fail because of stock?

Is the Order currently supply-capable?

Has payment already succeeded?

These concepts remain distinct:

Order Status

Payment Status

SupplyStatus

Reservation Cycle Status

Do not merge them into one overloaded status.

4. Order Detail — Reservation Section

Add/extend one compact section:

رزرو موجودی

Show:

current business reservation state

current cycle number

total cycles used / maximum cycles

StartedAt

ExpiresAt

remaining time when Active

reason/phase in human language

SupplyStatus

retry remaining count

Human labels equivalent to:

Current status:

رزرو فعال

مهلت رزرو پایان یافته

رزرو با لغو سفارش آزاد شد

رزرو مجدد ناموفق

رزرو پس از پرداخت نهایی شد

Reasons:

رزرو اولیه پرداخت

رزرو مجدد پس از پایان مهلت

در انتظار ثبت اطلاعات پرداخت

در انتظار بررسی پرداخت

بازیابی سفارش

بازیابی تاریخی

بازیابی پرداخت دیرهنگام

Use centralized localization.
Do NOT render raw enums.

5. Reservation History Timeline

Add a compact history/timeline under the same section.

Each cycle row/event should show:

Cycle number

Start

End/expiry

final status

reason

policy snapshot used:

effective hold duration

effective max cycles

actor/source where useful

related payment attempt reference only as human-safe reference if current Admin conventions support it

Also show non-cycle audit events where relevant:

Reacquire requested

Reacquire failed

Retry limit reached

Do NOT expose reservation GUIDs by default.

Technical IDs may exist behind an existing admin diagnostics pattern only if already standard.

6. Active Countdown in Admin

If a cycle is Active:

show remaining time

local visual countdown is allowed

source is server ExpiresAt/server clock

no per-second API calls

countdown is presentation-only

at zero, perform one normal refresh/revalidation if needed

No lifecycle mutation from frontend.

7. Orders Grid

Add minimal reservation-cycle awareness to Orders grid only if it improves operations without clutter.

Preferred:

keep existing SupplyStatus column

add one compact column:
رزرو موجودی

Possible labels:

فعال #1

پایان‌یافته #1

فعال #2

نهایی‌شده

—

Requirements:

batched/server-side projection

no N+1

filterable if current backend/grid architecture supports it cleanly

saved views preserved

no layout regression

If adding a separate column causes excessive density, use a combined compact sublabel within existing SupplyStatus cell, but do not overload semantics. Choose based on discovery.

8. Payments Context

On Admin Payment detail/row context where relevant, expose concise reservation context:

active cycle number/status

whether retry is still possible

whether supply must be reacquired

Do NOT duplicate the full history there.
Full audit stays on Order Detail.

9. Capability-Driven Actions

Admin UI must not invent actions from status strings.

Use backend capabilities for:

canConfirmDeposit

canRecoverInventory

canRetryReservation

canCancelOrder

canRestoreOrder

Rules:

no “retry reservation” button if normal Confirm already auto-reacquires

no action if max cycles reached unless there is an explicit supported override policy

no manual resurrection action

no direct “extend timer” action unless product policy explicitly supports it (currently it does NOT)

10. No Manual Timer Extension

This is LOCKED for now.

Admin must NOT have:

تمدید رزرو

ریست تایمر

direct ExpiresAt editor

If future business need appears, it requires a separate explicit policy/task.

This prevents hidden bypass of max-cycle and Black Friday policy.

11. Retry Limit Visibility

When max cycles reached:

Admin should see:
حداکثر دفعات رزرو این سفارش استفاده شده است.

Show:

cycles used

max cycles

last failure/expiry reason

current SupplyStatus

Do not offer hidden retry unless backend policy allows a canonical exception.

12. Reacquire Failure Diagnostics

When a reacquire attempt failed:

show event in history

show affected line shortages if available

required qty

available qty

shortage qty

unit

Business copy:
رزرو مجدد به دلیل کمبود موجودی انجام نشد.

No raw inventory error.

13. Audit Integrity

Admin view must reflect immutable backend history.

Forbidden:

reconstructing cycle history from current Reservation rows in frontend

inferring cycle number from count of Payment Attempts

rewriting labels based on current Settings instead of stored policy snapshot

hiding failed reacquire events

collapsing multiple cycles into one generic “expired” row

14. Performance

Required:

Order Detail: one focused reservation-cycle/history projection request or included server projection

Orders grid: no per-row cycle/history endpoint calls

Payments grid/detail: no N+1

no polling loops

local countdown only

Evidence:
docs/evidence/TB-P10-T004/r17-performance.md

15. Anti-Pattern Gate

Scan for:

cycle == Payment Attempt inference

frontend reconstructing history

per-row N+1

raw enum rendering

raw GUID display

admin timer extension shortcut

duplicated retry logic

local clock authority

per-second API polling

first-cycle-only assumptions

first-line-only shortage diagnostics

Any new occurrence => REPAIR.

Evidence:
docs/evidence/TB-P10-T004/r17-antipattern-scan.md

16. Focused Tests

Order Detail:

Active Cycle #1

Expired Cycle #1

Active Cycle #2

CommittedPaid

ReleasedByCancel

ReacquireFailed

retry limit reached

immutable policy snapshot shown

line shortage diagnostics

Countdown:
10. uses server ExpiresAt
11. no API per second
12. zero triggers at most one refresh

Grid:
13. reservation state visible
14. no N+1
15. saved view/grid behavior preserved

Payments:
16. concise reservation context correct
17. no duplicated full history

Capabilities:
18. no manual extend timer
19. no invalid retry action
20. Confirm auto-reacquire remains existing path

Localization/layout:
21. FA RTL
22. EN LTR
23. mobile/desktop Order Detail smoke

Regression:
24. R8 SupplyStatus
25. R15 cycle projection
26. R16 pending payment customer UX
27. recovery guard
28. git diff --check

Do NOT run unrelated full suites.

17. Runtime Matrix — REQUIRED

A. Order with active Cycle #1
B. Expired Cycle #1
C. Order on Cycle #2 after retry
D. Failed reacquire with shortage
E. Max cycles reached
F. Manual review active
G. Paid/Committed cycle
H. Cancelled/released cycle
I. Orders grid no N+1
J. FA/EN layout smoke

For A–H capture Admin-visible state and backend projection equality.

18. Lock Registry

Add non-duplicate locks equivalent to:

Admin reservation-cycle audit is backed by immutable backend history

Reservation Cycle status remains distinct from PaymentStatus/OrderStatus/SupplyStatus

Admin cannot manually reset/extend reservation timer

grid reservation-cycle projection must not use N+1

retry-limit and reacquire-failure events are visible operationally

current Settings never rewrite historical cycle policy display

19. Evidence

Add under docs/evidence/TB-P10-T004/:

r17-recovery-start.md

r17-admin-reservation-audit-discovery.md

r17-order-detail.md

r17-cycle-history.md

r17-orders-grid.md

r17-payments-context.md

r17-capabilities.md

r17-performance.md

r17-focused-validation.md

r17-runtime.md

r17-antipattern-scan.md

r17-recovery-sot.md

20. Recovery / Pipeline

At completion:

Phase P10

Last Architect-accepted before review: TB-P10-T004-R16

Last Implementation: TB-P10-T004-R17

Current Issued/Repair: none

USER_VISUAL_ACCEPTED=NO

Worker: tooba-worker-01 — IDLE

branch main

ending HEAD recorded

HEAD==origin/main

task-owned worktree clean

no TB-P10-T005

Run recovery guard.

Acceptance

PASS only if:

Admin can see current reservation-cycle state

full immutable cycle history visible on Order Detail

retry limit/remaining cycles visible

reacquire failures and shortage diagnostics visible

active countdown uses server ExpiresAt

no per-second API polling

no manual timer extension/reset action

Orders grid has efficient cycle visibility

Payments context concise and correct

no raw enums/GUIDs/errors

no N+1

R8/R15/R16 regressions green

runtime A–J PASS

anti-pattern scan CLEAN

focused tests green

recovery exact

HEAD==origin/main

Worker IDLE

no TB-P10-T005

USER_VISUAL_ACCEPTED=NO.

Result Contract

Return ONLY canonical BRIDGE-WAKE-V1 Result with:

Summary
Applicable-Locks
Recovery-Start
Discovery
Order-Detail
Cycle-History
Orders-Grid
Payments-Context
Capabilities
Performance
Focused-Validation
Runtime
AntiPattern-Scan
Lock-Registry
Recovery-SoT
Git
Architectural-Concerns
Visual-Concerns
Blockers

END_TOOBA_REPAIR_TASK