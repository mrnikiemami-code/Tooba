PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

TASK-ID: TB-P07-T011
PARENT-TASK: TB-P07-T010
PROJECT: Tooba
PHASE: P07
CHANNEL: tooba-main
STATUS: ISSUED
TASK-TYPE: PRODUCT CATALOG ADMIN — LIST + PRODUCT WORKSPACE FOUNDATION
WORKER-POLICY: ONE WORKER = ONE ACTIVE TASK

Architect Acceptance

TB-P07-T010 is ACCEPTED technically.

Canonical Category foundation is now end-to-end:

Admin Category Tree

translations

category-specific attributes

category-specific facets

Mega Menu binding

canonical localized Category routes

real Category PLP

typed dynamic filters

subtree listing

SEO canonical / filtered noindex

Product != Offer preserved

Pricing / Inventory module boundaries preserved

HEAD == origin/main:
6103e91b9c1d96f1dd823a8af36ddf966af1bdad

USER_VISUAL_ACCEPTED for PLP remains NO, but user has explicitly allowed pipeline continuation for now.

New Goal

We now move into PRODUCT CATALOG ADMIN.

This task builds the professional Product Master management foundation in Admin.

This is NOT seller Offer management.

Canonical ownership:

Admin
→ creates/edits canonical Product Master
→ assigns Category
→ fills Category-driven attributes
→ defines standardized variants
→ publishes/archives Product

Seller
→ later selects Product / Variant
→ creates Offer
→ sets Price / Inventory
Locked UX Principle

UI/UX MUST be understandable for an ordinary non-technical user without training.

For Product Catalog:

clear list

clear create action

obvious VIEW vs EDIT

no raw IDs

no giant technical form

use progressive disclosure

category selection before category-driven attributes

ordinary Persian labels

mobile-intentional

product creation should feel like a guided product workflow, not database CRUD

Hard Grid Lock

The existing Tooba custom grid is canonical:

src/frontend/design-system/app-data-grid/AppDataGrid.tsx

This is NOT raw AG Grid.

MANDATORY:

reuse this exact AppDataGrid for Product list

preserve all existing customizations

Saved Views

filters

Advanced Filter

Jalali

Column Manager

exports

row actions

tooltips

RTL

current CSS/theme

FORBIDDEN:

raw AgGridReact

new grid wrapper

new table implementation

page-specific duplicate grid CSS

Existing Product List Baseline

There is already a Product list integration at Admin Products.

Do NOT rebuild it from scratch.

Audit and preserve current route, query API, column/filter behavior and canonical AppDataGrid composition.

Expected route family:

/{locale}/admin/products

or existing canonical Admin product route.

Goal of T011

Create a professional Product Catalog Admin structure consisting of:

Existing Product List using canonical AppDataGrid

Product create entry point

Product Workspace route

Explicit VIEW / EDIT modes

Product lifecycle shell

Category selection

General + Translation foundations

Tabs prepared for:

General

Translations

Attributes

Variants

Media

SEO

Status/Publishing

History

Do NOT fully implement Variants/Media/SEO advanced flows yet.

1 — Product Master Boundary

Product Master owns canonical descriptive identity.

Do NOT introduce:

Product.Price

Product.Stock

Product.SellerId as canonical product ownership

Do NOT let Seller create Product Master in this task.

Pricing / Offer / Inventory remain separate.

2 — Product List

Use existing Product list + canonical AppDataGrid.

Must support existing professional capabilities.

Ensure row actions are aligned to locked Admin form pattern:

مشاهده

ویرایش

حذف / بایگانی only if actually allowed and permission-aware

Clicking view opens Product Workspace in VIEW mode.

Clicking edit opens same Product Workspace in EDIT mode.

Do NOT create separate unrelated view/edit pages if same workspace can handle both modes cleanly.

3 — Product Workspace Routes

Use stable deep links.

Conceptually:

/{locale}/admin/products/{productId}

Optional mode/tab query or segment according to existing routing conventions.

Examples:

/{locale}/admin/products/{id}
/{locale}/admin/products/{id}/translations
/{locale}/admin/products/{id}/attributes
/{locale}/admin/products/{id}/variants

Do not expose ProductId to user as a visual field just because it appears in route.

Refresh/back/forward must preserve context.

4 — Product Create Flow

Primary CTA:

+ محصول جدید

Creation must be progressive.

Recommended first step:

Category

Product Name in active locale

Slug suggestion

Status = Draft

Then create Product Master and open workspace.

Do NOT ask for all attributes, variants, SEO, media, offer, price and stock in one giant initial modal.

5 — Category Selection

Category selection is critical.

Use a user-friendly Category picker.

Requirements:

searchable

displays human path

no raw IDs

supports deep tree

RTL

clear selected category path

Example:

کالای دیجیتال > موبایل > گوشی هوشمند

Do NOT expose technical category hierarchy fields.

After Category selection:

Product editor later consumes effective category schema

changing Category must be intentional because schema may change

6 — Category Change Safety

If Product already has category-driven attribute data/variants and user changes Category:

do NOT silently discard data

calculate/report affected fields

require explicit confirmation

preserve compatible values when safe

mark incompatible values for review

T011 may establish contract/shell; detailed migration behavior can be completed in later Product task.

7 — Product General Tab

VIEW mode:
show readable product summary.

At minimum:

localized Name

Category path

Status

Slug / canonical storefront route if product route exists

Brand if existing canonical model supports it

short summary

publication readiness summary

variant count if available

media readiness summary if available

No disabled grey form.

EDIT mode:
only authorized fields become inputs.

8 — Product Translation Model

Use existing translation architecture or create canonical ProductTranslation if missing.

Localized fields conceptually:

ProductTranslation
- ProductId
- Locale
- Name
- Slug
- ShortDescription
- Description
- SeoTitle
- SeoDescription

Do NOT add:

NameFa

NameEn

DescriptionFa

etc.

Slug must remain human and locale-specific.

9 — Product Slug

User edits only human slug.

No ProductId suffix visible.

Example:

آیفون-16-پرو

Public route strategy should remain clean and locale-aware.

If Product routing is not yet finalized:

establish resolver/contract

do not invent an SEO-hostile route

document exact route choice

Slug uniqueness must be system-enforced.

Do not silently append cryptic IDs.

10 — Product Status / Lifecycle

Use an explicit lifecycle aligned with current domain.

At minimum conceptually:

Draft

Published/Active

Archived

UI labels localized.

Publishing must eventually depend on readiness rules.

Do not expose raw enum names.

11 — Workspace Tabs

Prepare professional tabs:

عمومی
ترجمه‌ها
ویژگی‌ها
تنوع‌ها
رسانه
SEO
انتشار
تاریخچه

Implement real shell and at minimum:

عمومی

ترجمه‌ها foundation

category selection

Other tabs may be clean placeholders only if later tasks own them.

Do not fake functionality.

12 — VIEW / EDIT Mode

Locked global Admin rule.

VIEW:

readable values

no editable controls

no Save

Edit action only when authorized

EDIT:

explicit ویرایش

Save / Cancel

dirty-state protection

backend authoritative permissions

Return to VIEW after successful save.

13 — Authorization

Use SpiceDB-centered authorization.

Conceptually distinguish:

view Product Master

create Product Master

edit Product Master

publish/archive

delete if supported

Do not use frontend-only security.

14 — Product Attributes Handoff

Do NOT fully implement Product Attribute editor in T011.

But wire the workspace so next task can consume:

CategoryId
→ Effective Category Attribute Schema
→ Product Attribute Values

Required readiness:

category-selected state available to Attributes tab

schema loading contract defined

no Product-level arbitrary attributes outside category schema unless architecture explicitly supports global attributes

15 — Variants Handoff

Do NOT implement full variants in T011.

Prepare for:

Category effective schema
→ IsVariantAxis attributes
→ selected attribute options
→ standardized Product Variants

Use Persian UI term:

تنوع

Avoid گونه.

16 — Offer/Pricing/Inventory Boundary

Product Workspace must NOT show editable Seller Price/Stock as Product fields.

If later Admin needs commercial summary:

read-only composition through contracts

no ownership leakage

T011 must preserve:
Product Master != Offer

17 — Product List Query

Preserve existing server-side Product grid query architecture:

AppDataGrid → Tooba GridQuery → Product module/application query

Do not send raw AG Grid filters to backend.

Do not regress T003 canonicalization.

18 — Product List Columns

Do not redesign blindly.

Preserve approved current grid.

If minor column additions are needed for catalog usability, use canonical metadata/config.

Potential useful columns only if already backed safely:

image

product name

category

status

variant count

updated date

actions

No page-specific CSS.

19 — Search / Filtering

Existing Product AppDataGrid filters remain canonical.

Category filter should use human category labels/path where possible.

Do not expose CategoryId in normal filter UI.

20 — Delete vs Archive

Be careful with Product Master deletion.

Prefer archive/deactivate for products already referenced by offers/orders/history.

Do not hard-delete referenced catalog truth casually.

If hard-delete rules are not fully available:

expose Archive

defer destructive delete

Document policy.

21 — Mobile

Product list uses canonical responsive AppDataGrid behavior.

Product Workspace:

full-width mobile layout

sticky/obvious actions

tabs mobile-safe

no desktop split compression

create flow usable on mobile

22 — Accessibility

Required:

labels

focus states

errors associated to fields

keyboard navigation

status not color-only

icon-only actions have tooltip/aria-label

23 — API Contracts

Provide/use module-owned APIs for:

Product list query (existing)

Product detail/workspace

create Product Master

update Product core

Product translations

Category assignment

lifecycle actions as appropriate

No EF entities directly.

No cross-module joins.

24 — Tests

Frontend:

existing Product AppDataGrid regression tests

list route

View row action

Edit row action

create Product CTA

create first-step flow

category picker

no raw IDs

Workspace VIEW

Workspace EDIT

Save/Cancel

dirty-state

translation shell/foundation

localized status

mobile

permission-aware actions

Backend:

create Product Master

assign Category

update Product core

translation per locale

duplicate product slug policy

lifecycle

authorization where testable

Product list query regression

no Product.Price/Product.Stock introduced

no cross-module Pricing/Inventory joins

25 — Documentation

Create/update:

docs/catalog/PRODUCT-CATALOG-ADMIN.md

Document:

Product Master ownership

Product vs Offer

Product Workspace UX

VIEW/EDIT pattern

Category-first flow

translation model

slug policy

lifecycle

Attributes handoff

Variants handoff

delete/archive policy

canonical AppDataGrid reuse

Update recovery/project state docs.

26 — Validation

Backend:

restore

build

full tests

Frontend:

typecheck

lint

tests

production build

Require:

0 errors

0 failed

0 skipped

no new warnings

Always:

git diff --check

27 — Runtime

Keep alive:

Backend :5088

Frontend :3000

Shopeiva :3001

Product Admin:
http://localhost:3000/fa/admin/products

28 — Git

Commit + push.

Require:

HEAD == origin/main

tracked tree clean

unrelated user changes preserved

Acceptance

PASS only if ALL:

existing customized Tooba AppDataGrid is reused

no raw AgGridReact

Product List remains professional

Product Workspace exists

Product create flow is simple and progressive

Category selection is human-readable

VIEW / EDIT preserved

ProductTranslation architecture is locale-based

no NameFa/NameEn columns

clean human slug policy

ProductId not appended visibly

Product lifecycle clear

Product != Offer preserved

Product.Price/Product.Stock not introduced

Pricing/Inventory boundaries preserved

Attributes/Variants next-step contracts ready

UI understandable for ordinary user

full validation green

runtime alive

Git synchronized

USER_VISUAL_ACCEPTED remains NO until user manually reviews Product Catalog Admin runtime.

Result Contract

Return ONLY:

PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_WORKER_RESULT

Task-ID:
TB-P07-T011
Parent-Task:
TB-P07-T010
Channel:
tooba-main
Status:
PASS | FAIL | BLOCKED | RECOVERY_CONFLICT

Summary:
...

Canonical-AppDataGrid:

existing Tooba AppDataGrid reused:

raw AgGridReact:

new grid wrapper:

page-specific grid CSS:

current grid visual preserved:

Product-List:

route:

query API:

columns:

row actions:

filters:

saved views preserved:

responsive:

Product-Workspace:

route:

VIEW:

EDIT:

tabs:

deep link:

refresh/back-forward:

mobile:

Create-Flow:

CTA:

first-step fields:

progressive:

post-create navigation:

Category:

picker:

path labels:

raw IDs visible:

change safety:

effective schema handoff:

Translations:

model:

locales:

fields:

slug:

uniqueness:

visible ID suffix:

Lifecycle:

statuses:

publish/archive:

delete policy:

Architecture:

Product != Offer:

Product.Price introduced:

Product.Stock introduced:

Pricing join:

Inventory join:

Seller ownership leakage:

Authorization:

SpiceDB:

view:

create:

edit:

publish/archive:

backend authoritative:

Attributes-Handoff:

CategoryId:

effective schema contract:

next task readiness:

Variants-Handoff:

IsVariantAxis source:

terminology:

next task readiness:

API:

existing:

added:

backend touched:

frontend integration:

gaps:

Validation:

backend restore:

backend build:

backend full tests:

frontend typecheck:

frontend lint:

frontend tests:

frontend build:

passed:

failed:

skipped:

warnings:

new warnings:

git diff --check:

Documentation:

PRODUCT-CATALOG-ADMIN:

recovery/state:

Runtime:

Backend:

Frontend:

Shopeiva:

Product Admin:

kept alive:

Git:

branch:

commit:

push:

final HEAD:

origin/main:

synchronized:

tracked tree:

unrelated user changes:

Architectural-Concerns:
...

Visual-Concerns:
...

Blockers:
...

END_TOOBA_WORKER_RESULT

After Result:

Worker -> IDLE / Waiting

keep runtimes alive

do not invent next task

do not add extra prose

END_TASK
