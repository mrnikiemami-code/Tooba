# Tooba — TB-P00-T023 — Promotion & Discount Architecture

BEGIN_TOOBA_CURSOR_TASK_V1

Protocol-Version: 1
Task-ID: TB-P00-T023
Phase: P00 — Architecture / Discovery
Type: Architecture Design / Promotion & Discount
Repository: https://github.com/mrnikiemami-code/Tooba
Primary-Branch: main
Implementation-Agent: Cursor
Architect: ChatGPT
Execution-Mode: PIPELINE
Depends-On: TB-P00-T022
Architect-Decision-On-Dependency: ACCEPTED

## Objective

Design Tooba's Promotion / Discount architecture deeply enough to support:

- product/order/shipping promotions;
- coupon/promo codes;
- automatic promotions;
- seller-funded vs platform-funded promotions;
- Marketplace and Single-Store editions;
- market/currency/channel-aware eligibility;
- customer/organization/B2B-targeted promotions;
- quantity and threshold rules;
- validity windows;
- combinability/stacking;
- exclusivity;
- deterministic evaluation;
- quote/order snapshotting;
- auditability;
- future campaign/content integration;
- professional Admin/Seller UX;
- future service extraction.

This task is architecture documentation only.

Do not implement a promotion engine, coupon endpoints, rule DSL, database schemas, UI, or template integration.

---

## 1. Repository Recovery

Before edits:

```bash
git rev-parse --show-toplevel
git fetch origin
git branch --show-current
git rev-parse HEAD
git rev-parse origin/main
git status --short --branch
```

Require:

```text
branch = main
HEAD == origin/main
```

Repository must contain no unknown unsafe changes.

Expected predecessor:

```text
5f677b31e60faae66b74ae2e668c63d0c901fde6
```

If synchronized `main` legitimately advanced, continue from current safe synchronized `main`.

Unsafe or ambiguous repository state =>

```text
RECOVERY_CONFLICT
```

Never force push, rewrite history, destructively reset, silently stash, delete, overwrite, or auto-commit unknown work.

---

## 2. Source-of-Truth Transition

Update:

```text
TB-P00-T022 = ACCEPTED
TB-P00-T023 = ISSUED / AWAITING_ARCHITECT_ACCEPT
```

Roadmap:

```text
P00 Fulfillment = COMPLETE
P00 Promotion / Discount = IN_PROGRESS
```

Do not mark T023 accepted.

---

## 3. Create Architecture Document

Create:

```text
docs/architecture/22-promotion-discount.md
```

It must contain all sections below.

---

# A. Core Separation

Hard rules:

```text
Promotion != Pricing
Promotion != Coupon Code
Promotion != Campaign Content
Promotion != Order
Promotion != Payment
```

Pricing owns base/contextual commercial price formation.

Promotion owns conditional commercial incentives/adjustments.

Coupon is one trigger/mechanism, not the whole Promotion model.

Content/Marketing may describe a campaign but does not calculate discount truth.

---

# B. Promotion Scope

Define conceptual capabilities:

```text
Promotion
Promotion Rule / Eligibility
Promotion Benefit
Coupon / Code
Redemption / Usage
Funding Source
Combinability Policy
Validity
Targeting
Evaluation Result
```

Do not create schema.

---

# C. Benefit Types

Preserve extensibility for:

```text
percentage discount
fixed amount discount
fixed target price
buy X get Y
quantity discount
free shipping
shipping discount
bundle benefit
gift item future
seller/platform subsidy
```

Do not lock all types into one generic formula.

Exact first-release subset:

```text
NEEDS_LATER_P00_DETAIL
```

---

# D. Eligibility Dimensions

Promotion eligibility may depend on:

```text
Tenant / Deployment
Market
SalesChannel
Currency
Seller
Offer
Product
Variant
Category
Brand
Customer / Party
Organization / Commercial Account
Contract
Quantity
Cart subtotal
Order subtotal
Shipping method
Payment method where business policy permits
First purchase / segment future
Campaign source
Date/time
Coupon
```

Do not require every dimension for every promotion.

---

# E. Locale vs Market

Preserve:

```text
Locale != Market != Currency
```

Promotion eligibility is commercial context, usually Market/Channel/etc., not UI language.

Localized campaign copy is separate Content concern.

---

# F. Promotion Evaluation Boundary

Design conceptual internal contract, e.g.:

```text
IPromotionEvaluator
```

or equivalent.

Input should be normalized commerce context.

Output should be deterministic structured adjustments/evidence.

No engine-specific DSL leaks into Checkout/Order.

---

# G. Pricing Integration

Hard principle:

```text
Pricing establishes eligible/base price
Promotion applies conditional adjustments
```

Need clear sequencing and ownership.

Avoid Promotion directly mutating authored Price Books.

Promotion may consume Pricing quote lines and return adjustments.

Exact pipeline may be:

```text
Base/Contextual Price
→ Promotion Evaluation
→ Tax/Fee/Shipping composition as defined later
→ Final Quote
```

Exact tax ordering is out of scope unless already known.

Mark unresolved ordering explicitly.

---

# H. Quote Determinism

Given the same:

```text
PromotionVersion
PricingVersion
Context
Items
Time
```

evaluation should be reproducible.

Need explicit clock/time input, not hidden ambient time.

No random/unversioned behavior in final commercial quote.

---

# I. Promotion Versioning

Promotions may change after customer views them.

Preserve:

```text
PromotionId
PromotionVersion / Revision
Validity
Rule version
Benefit version
```

Order/Quote should snapshot applied promotion evidence.

Do not depend on current mutable promotion when rendering historical Order.

---

# J. Coupon Model

Coupon/code is a trigger/claim mechanism.

Need conceptual support for:

```text
single shared code
unique generated codes
customer-bound code
organization-bound code
seller-bound code
campaign-bound code
limited redemption
```

Coupon itself does not own pricing calculation.

---

# K. Coupon Security

Coupon handling must consider:

```text
brute-force guessing
enumeration
case normalization
rate limiting
usage race
customer binding
tenant isolation
expiration
revocation
```

Do not log full sensitive/private coupon codes unnecessarily.

Exact entropy policy later.

---

# L. Automatic Promotion

Promotions may apply without code.

Examples:

```text
category sale
seller campaign
cart threshold
quantity break
market campaign
free shipping threshold
```

Evaluation must explain which promotion applied and why.

---

# M. Manual / Admin Adjustment

Do not conflate ad-hoc admin price override with standard Promotion.

Manual commercial adjustments may require a separate controlled mechanism.

If allowed later:

```text
authorization
reason
audit
limits
```

must apply.

Classify exact manual-adjustment capability as later detail.

---

# N. Stacking / Combinability

This is critical.

Need explicit policies such as:

```text
stackable
exclusive
best-of
priority/order
group-exclusive
coupon + automatic compatibility
seller promotion + platform promotion compatibility
```

Do not let order of database rows determine discount outcome.

---

# O. Conflict Resolution

When promotions conflict, evaluation must be deterministic.

Possible concepts:

```text
priority
promotion group
exclusive flag
benefit comparison
policy strategy
```

Exact strategy later, but accidental nondeterminism is forbidden.

---

# P. Maximum Discount / Guardrails

Preserve future:

```text
max benefit
max quantity
max order discount
minimum price floor
seller subsidy limit
campaign budget
```

Do not let combined promotions create negative line totals.

Final payable money must respect Money invariants.

---

# Q. Funding Source

Marketplace requires funding attribution.

Potential funding sources:

```text
Platform
Seller
Shared
Manufacturer/Partner future
```

This is distinct from customer-facing benefit.

Need future settlement/accounting integration.

Promotion must preserve funding evidence.

---

# R. Marketplace

Marketplace promotions may target:

```text
one seller
many sellers
one offer
category
platform-wide
seller-funded offer campaign
platform-funded campaign
shared-funding campaign
```

Do not apply seller-specific promotion to another seller's Offer.

---

# S. Single-Store

Single-Store reuses same Promotion architecture.

Seller may be implicit.

No forked discount engine.

---

# T. B2B

Future B2B may require:

```text
organization-targeted promotion
contract exclusion
account-specific incentive
volume incentive
channel campaign
sales-rep code future
```

Do not let promotions override negotiated contract pricing unless explicit policy says so.

Pricing/Contract precedence needs later explicit rule.

---

# U. Quantity Pricing Boundary

Distinguish:

```text
authored quantity/contract price
```

from:

```text
promotion quantity discount
```

Both can change payable price, but have different semantics, ownership, audit, validity.

Do not collapse them.

---

# V. Shipping Promotion

Free/discounted shipping is a Promotion benefit.

Fulfillment provides shipping service/rate facts.

Checkout/Pricing composition applies customer-facing shipping charge and promotion.

Do not mutate carrier raw rate.

---

# W. Payment-Method Promotion

Some markets may allow payment-method-specific incentives.

Architecture may preserve this dimension, but Payment provider must not own promotion rules.

Payment method eligibility is input; Promotion evaluates benefit.

Legal/commercial constraints remain product-policy detail.

---

# X. Cart Boundary

Cart may show estimated promotion benefits.

Cart is not Promotion authority.

Changes to:

```text
items
quantity
seller
market
currency
coupon
```

trigger re-evaluation.

Long-lived cart promotion display may become stale.

Checkout revalidates.

---

# Y. Checkout Boundary

Checkout performs authoritative re-evaluation before Order placement.

If promotion expired/usage exhausted/eligibility changed, user must receive clear recoverable UX.

Do not silently preserve an invalid discount.

---

# Z. Order Snapshot

Order must snapshot:

```text
PromotionId
PromotionVersion
Benefit/Adjustment
Funding attribution
Coupon reference where safe
Eligibility/evaluation evidence where needed
```

Historical Order must not recompute current promotion rules.

---

# AA. Payment Boundary

Payment receives final payable amount from accepted commercial quote/order.

Payment must not independently apply coupon or promotion.

Refund handling may need proportional/policy-aware allocation later.

Promotion itself does not execute refunds.

---

# AB. Refund / Cancellation Readiness

Promotions complicate:

```text
partial cancellation
partial refund
return
```

Need preserved per-line/order adjustment allocation so later financial workflows can determine correct refundable amounts.

Exact refund allocation policy:

```text
NEEDS_LATER_P00_DETAIL
```

---

# AC. Redemption / Usage

Need first-class usage/redemption tracking for restricted promotions.

Potential constraints:

```text
total redemption limit
per customer
per organization
per seller
per coupon
per period
```

Usage must handle concurrency.

Do not rely on analytics counters for eligibility.

---

# AD. Reservation of Limited Promotion

For scarce promotions/coupons, race conditions may exist.

Analyze whether usage is:

```text
checked at checkout
reserved before order
consumed on order
released on cancellation/payment failure
```

Do not lock universal behavior.

Mark exact limited-redemption reservation strategy:

```text
NEEDS_LATER_P00_DETAIL
```

---

# AE. Time

Validity uses authoritative time abstraction.

Need:

```text
StartsAt
EndsAt
Timezone/business calendar semantics
```

Do not depend on client clock.

Use existing NodaTime/time architecture direction where applicable.

---

# AF. Scheduling

Promotion activation/deactivation may be scheduled.

Background jobs/events must carry tenant context.

Even without scheduler, evaluation must respect validity timestamps.

---

# AG. Content / Campaign Integration

Content/Page Composition may describe and surface a promotion.

Examples:

```text
campaign landing
banner
promo strip
coupon explanation
```

Content references Promotion by opaque ID/contract.

Content does not calculate discount.

---

# AH. SEO

Promotion/campaign routes may be indexable or noindex according to SEO policy.

Promotion engine does not decide canonical/indexation.

Expired campaign handling belongs to SEO/Content route policy.

---

# AI. Search

Search may consume promotion projection such as:

```text
has promotion
discount range
campaign badge
```

Search is stale projection only.

Checkout re-evaluates.

Do not use search badge as discount authority.

---

# AJ. Display Price / Badge

Storefront may display:

```text
original price
discounted price
discount percent
campaign badge
coupon-required label
```

UI must clearly distinguish:

```text
automatic discount
coupon-only benefit
starting-from offer discount
seller-specific discount
```

Avoid misleading global discount badges when only one seller/condition qualifies.

---

# AK. Promotion Explainability

Evaluation should produce explainable result.

Potential:

```text
applied promotions
rejected coupon reason
conflict reason
benefit amount
affected lines
funding source
```

Do not expose internal sensitive abuse-prevention details.

This is important for customer support and Admin UX.

---

# AL. Customer UX

Professional UX must cover:

```text
coupon apply/remove
success
invalid
expired
not eligible
usage exhausted
minimum threshold not reached
seller/offer mismatch
stacking conflict
promotion changed at checkout
```

Do not use generic "invalid code" for every case unless security requires ambiguity.

---

# AM. Admin UX

Do not design Promotions as raw CRUD.

Admin workflow should support:

```text
Promotion Library
Draft
Scheduled
Active
Paused/Disabled
Expired
Eligibility
Benefits
Coupon configuration
Combinability
Funding
Usage
Preview/Test
Audit
Performance link to Analytics
```

Need guardrails against dangerous configurations.

---

# AN. Seller UX

Marketplace Seller panel may support seller-scoped promotion creation where policy permits.

Need:

```text
seller-owned offers only
funding clarity
date window
budget/usage
approval workflow future
analytics
```

Do not let seller affect other sellers/platform-funded rules.

---

# AO. Preview / Simulation

Professional admin tooling should support future "simulate promotion" before activation.

Inputs:

```text
sample cart
market
seller
customer/account
coupon
time
```

Output explains eligibility/benefit.

No production implementation now.

---

# AP. Approval / Governance

High-impact promotions may require approval workflow future.

Examples:

```text
large discount
platform-funded campaign
seller subsidy above threshold
```

SpiceDB governs who can create/approve/activate.

Do not hardcode role names.

---

# AQ. Authorization

Potential permissions:

```text
view promotion
create
edit draft
approve
activate
pause
manage coupon
view usage
view funding
seller-create-own
```

SpiceDB remains authorization authority.

---

# AR. Audit

Durable audit for:

```text
rule change
benefit change
activation
pause
coupon creation/revocation
funding change
limit change
manual override
```

Technical logs are insufficient.

---

# AS. Analytics

Analytics may observe:

```text
promotion impression
coupon attempt
coupon success/failure category
promotion conversion
revenue
seller/platform funding impact
```

Analytics does not own eligibility/redemption truth.

---

# AT. Budget

Future campaign budget may be a business constraint.

Do not model analytics spend counters as budget authority.

If budget-limited promotion is supported later, consumption needs durable concurrency-safe authority.

Mark detailed budget subsystem:

```text
DEFERRED / NEEDS_LATER_DETAIL
```

depending on roadmap.

---

# AU. Multi-Currency

Benefits with monetary values must carry Currency.

Do not apply a fixed USD amount as if it were same numeric amount in EUR/IRR.

Promotion may be:

```text
market/currency-specific
percentage
currency-specific fixed amount
```

FX-derived promotion amounts require explicit policy; do not silently convert.

---

# AV. Rounding

Promotion allocation may require rounding.

Need deterministic rounding compatible with Pricing/Money rules.

No floating-point money.

Exact allocation/rounding algorithm:

```text
NEEDS_LATER_P00_DETAIL
```

---

# AW. Tax Interaction

Tax ordering can materially affect totals.

Do not guess jurisdictional rule.

Architecture must allow policy such as:

```text
discount before tax
discount allocation across taxable lines
shipping discount tax treatment
```

Detailed Tax architecture may be separate if product requires it.

Mark unresolved tax ordering clearly.

---

# AX. Rule Representation

Do not prematurely build arbitrary end-user scripting.

Potential approaches later:

```text
typed rule model
composable conditions/actions
limited DSL
policy objects
```

Hard rule:

```text
No arbitrary executable code from Admin/DB
```

Rules must be validated and versioned.

---

# AY. Engine Complexity

Avoid starting with a universal enterprise rule engine if first-release needs are simpler.

Architecture-ready does not mean implementing all theoretical promotion types now.

Recommend incremental typed extensibility.

---

# AZ. Promotion Events

Candidate events:

```text
PromotionActivated
PromotionChanged
PromotionPaused
PromotionExpired
CouponRedeemed
RedemptionReleased
```

Names not locked.

Consumers may include:

```text
Search projections
Cache invalidation
Analytics
Audit
Content/Campaign projections
```

Use outbox readiness.

---

# BA. Cache

Published/active promotion configuration may be cacheable.

Cache key dimensions may include:

```text
Tenant
Market
SalesChannel
Seller
PromotionVersion
```

Critical checkout evaluation must not trust stale cached eligibility when limits/redemptions are scarce.

Cache != promotion usage authority.

---

# BB. Observability

Need:

```text
evaluation latency
coupon failure categories
redemption conflicts
promotion application rate
engine errors
stale projection mismatch
budget/usage anomalies future
```

Integrate with OpenTelemetry.

---

# BC. Failure Matrix

Analyze:

```text
Coupon invalid
Coupon expired
Coupon exhausted
Promotion expired during checkout
Seller offer changed
Price changed
Usage race
Stacking conflict
Promotion engine unavailable
Invalid rule config
Currency mismatch
Cross-tenant promotion leak
Search shows stale badge
```

For each classify:

```text
fail closed?
retry?
re-evaluate?
remove benefit?
customer message?
admin alert?
```

Critical direction:

If Promotion authority is unavailable during authoritative checkout evaluation, do not silently invent/retain a discount.

---

# BD. Data Ownership Matrix

Create matrix across:

```text
Promotion
Pricing
Cart
Checkout
Order
Payment
Fulfillment
Seller
Content
Search
Analytics
Authorization
```

Against:

```text
base price
promotion rule
coupon
discount adjustment
shipping rate
shipping discount
redemption count
final payable
funding source
campaign copy
discount badge projection
conversion metric
activation permission
```

Mark:

```text
OWNER
SOURCE
REFERENCE
CONSUMER
PROJECTION
NOT_OWNER
```

---

# BE. Testing Strategy — Architecture Level

Future implementation must test:

```text
automatic promotion
coupon promotion
stacking
exclusive promotion
seller-scoped promotion
platform-funded promotion
multi-currency
quantity threshold
cart threshold
usage limit race
expiration at checkout
coupon normalization
tenant isolation
B2B target
shipping discount
order snapshot
partial refund allocation later
Admin simulation
RTL/mobile promotion UX
```

No tests now.

---

# BF. Decision Summary

Classify:

```text
RECOMMENDED_FOR_ADR
NEEDS_LATER_P00_DETAIL
DEFERRED
```

At minimum recommend:

1. Promotion is separate from Pricing, Coupon, Content, Order and Payment.
2. Coupon is a trigger/claim mechanism, not the Promotion domain itself.
3. Pricing provides contextual/base quote inputs; Promotion returns conditional adjustments.
4. Promotion evaluation is deterministic and versioned.
5. Order snapshots applied promotion evidence; historical orders do not re-evaluate current rules.
6. Locale/Market/Currency remain separate.
7. Marketplace seller/platform funding attribution is first-class.
8. Seller-scoped promotion cannot leak across seller Offers.
9. Authored quantity/contract price is distinct from promotional quantity discount.
10. Shipping promotion modifies customer shipping charge, not carrier raw rate.
11. Cart display is estimate; Checkout re-evaluates authoritatively.
12. Stacking/combinability is explicit and deterministic.
13. Scarce redemption/usage is concurrency-safe and not analytics-based.
14. Fixed monetary benefits are currency-aware.
15. Promotion rule configuration never executes arbitrary code.
16. Content/Search/SEO consume promotion references/projections only.
17. SpiceDB governs create/approve/activate/pause scopes.
18. Significant promotion changes are audited.
19. Admin/Seller Promotion UX is workflow-oriented with simulation/preview readiness, not CRUD.
20. Promotion failure at authoritative checkout must not silently retain an unverified benefit.

Do not create final ADR yet.

---

## 4. Gap Review Preparation

At the end of the document, add a concise subsection:

```text
Remaining P00 Capability Gaps After T023
```

Evaluate only whether the following still require dedicated architecture tasks before P00 Gate:

```text
Reviews / Ratings
Notifications
Support
Returns / RMA
Tax
Fraud / Risk
```

For each classify:

```text
DEDICATED_TASK_REQUIRED
BOUNDARY_SUFFICIENT_FOR_P00
DEFER_POST_P00
NEEDS_USER_PRODUCT_DECISION
```

Give architectural reasoning.

Do NOT implement or design those capabilities deeply in this task.

This subsection is input to Architect review only.

---

## 5. Consistency Updates

If required, minimally align:

```text
docs/architecture/01-capability-domain-map.md
docs/architecture/08-pricing-market-currency.md
docs/architecture/10-cart-checkout-order.md
docs/architecture/11-payment.md
docs/architecture/12-content-page-composition.md
docs/architecture/13-seo-architecture.md
docs/architecture/14-search-indexing.md
docs/architecture/21-fulfillment.md
docs/architecture/20-frontend-ux-template-adaptation.md
```

No unrelated redesign.

---

## 6. Update SoT

Update:

```text
docs/PROJECT-STATE.md
docs/ROADMAP.md
docs/ai/TOOBA-RECOVERY-CONTEXT.md
```

Expected:

```text
Last Architect Accepted Task: TB-P00-T022
Current Issued Task: TB-P00-T023
Task State: AWAITING_ARCHITECT_ACCEPT
Current Phase: P00 — Architecture / Discovery
Promotion / Discount: IN_PROGRESS
```

Do not issue or execute the P00 Gate.

---

## 7. Save Complete Envelope Verbatim

Save exact task to:

```text
docs/ai/tasks/TB-P00-T023.task.md
```

Verify exact markers:

```text
BEGIN_TOOBA_CURSOR_TASK_V1
END_TOOBA_CURSOR_TASK_V1
```

Do not save a shortened pointer.

---

## 8. Explicit Out of Scope

Do NOT:

- implement promotions;
- create schemas/migrations;
- build coupon endpoints;
- build rule engine/DSL;
- build Admin/Seller Promotion UI;
- implement Tax;
- implement Returns;
- implement Reviews;
- implement Notifications;
- implement Support;
- implement Fraud/Risk;
- import/modify Shopeiva;
- create final ADR;
- issue or execute any Gate.

---

## 9. Validation

Run:

```bash
git diff --check
git status --short --branch
```

Manual checks:

- T022 ACCEPTED;
- T023 awaiting Architect acceptance;
- Promotion != Pricing/Coupon/Content/Order/Payment;
- deterministic/versioned evaluation represented;
- Marketplace funding attribution represented;
- stacking/combinability explicit;
- usage/redemption concurrency represented;
- Cart estimate vs Checkout authority separated;
- Order snapshots promotion evidence;
- multi-currency safe;
- shipping discount boundary preserved;
- Admin/Seller UX workflow-oriented;
- gap-review subsection exists;
- no application code;
- no external template changes.

---

## 10. Git Commit & Push

Commit:

```text
docs design Tooba promotion architecture [TB-P00-T023]
```

Then:

```bash
git push origin main
git fetch origin
git rev-parse HEAD
git rev-parse origin/main
git status --short --branch
```

Require:

```text
HEAD == origin/main
```

No force push.

Do not delete, stash, move, or commit unrelated local/external artifacts.

---

## 11. Result Contract

Return exactly:

```text
BEGIN_TOOBA_CURSOR_RESULT_V1

Protocol-Version: 1
Task-ID: TB-P00-T023
Phase: P00 — Architecture / Discovery
Status: PASS | BLOCKED | RECOVERY_CONFLICT

Summary:
...

Repository-Recovery:
- Repo-Root:
- Branch:
- Starting-HEAD:
- Starting-Origin-Main:
- Starting-Status:

Changes:
- ...

Promotion-Boundary:
- ...

Pricing-Checkout-Order:
- ...

Coupons-Redemption:
- ...

Stacking-Funding:
- ...

Marketplace-B2B:
- ...

Content-Search-SEO:
- ...

Authorization-Audit:
- ...

UIUX:
- ...

Remaining-P00-Gaps:
- Reviews / Ratings:
- Notifications:
- Support:
- Returns / RMA:
- Tax:
- Fraud / Risk:

Decision-Summary:
- RECOMMENDED_FOR_ADR:
- NEEDS_LATER_P00_DETAIL:
- DEFERRED:

Validation:
- git diff --check:
- other checks:
- manual consistency review:

Git:
- Commit:
- Push:
- Final-HEAD:
- Final-Origin-Main:
- Final-Status:
- Head-Matches-Origin: YES | NO

Source-of-Truth:
- Last-Architect-Accepted-Task:
- Current-Issued-Task:
- Current-Phase:
- Task-State:
- Recovery-Ready: YES | NO

Architectural-Concerns:
- ...

Blockers:
- ...

END_TOOBA_CURSOR_RESULT_V1
```

---

## 12. Pipeline Continuity — MANDATORY

After RESULT:

```text
WAITING
```

Remain inside the Tooba Architect-controlled pipeline.

Do not leave PIPELINE mode.

Do not invent, infer, prepare, or execute unauthorized work.

Do not advance to:

```text
TB-P00-T024
TB-P00-GATE
implementation
template migration
UI implementation
```

or any other work without a new valid Architect envelope.

A valid task requires:

```text
BEGIN_TOOBA_CURSOR_TASK_V1
...
END_TOOBA_CURSOR_TASK_V1
```

A valid gate requires:

```text
BEGIN_TOOBA_CURSOR_GATE_V1
...
END_TOOBA_CURSOR_GATE_V1
```

Cursor PASS is not Architect ACCEPT.

After sending RESULT, stop execution and remain WAITING for Architect review.

---

## 13. UI / UX Protection — MANDATORY PROJECT RULE

Tooba is a commercial product.

Weak UI/UX is considered product failure.

Promotion UX must not become raw rule/coupon CRUD.

Hard rules:

```text
Backend/module boundary != UI boundary
Build PASS != UI ACCEPT
Functional PASS != Visual ACCEPT
Desktop PASS != Mobile PASS
LTR PASS != RTL PASS
```

Future Promotion UI should support coherent workflows such as:

```text
Promotion Library
Create/Edit Flow
Eligibility Builder
Benefit Builder
Coupon Configuration
Combinability
Funding
Schedule
Preview / Simulation
Approval
Usage
Analytics
Audit
```

Customer-facing promotion UX must clearly explain applied/failed/changed discounts without misleading pricing.

Future user-visible implementation requires applicable visual evidence for:

```text
Desktop
Mobile
RTL
LTR where supported
Loading
Empty
Error
Invalid coupon
Expired coupon
Stacking conflict
Promotion changed at checkout
Responsive behavior
Accessibility
```

Architect visual acceptance is mandatory.

END_TOOBA_CURSOR_TASK_V1
