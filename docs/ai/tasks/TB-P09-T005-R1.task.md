PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_REPAIR_TASK

Task-ID:
TB-P09-T005-R1

Parent-Task:
TB-P09-T005

Channel:
tooba-main

WorkerId:
tooba-worker-01

AgentType:
cursor

Status:
REPAIR

USER_VISUAL_ACCEPTED=NO

Title:
Seller-Scoped Fulfillment Final Proof — Complete Runtime Matrix Without UI Rework

Architect Review

TB-P09-T005 delivered the required architecture and focused tests, and correctly preserved the user-authored UI changes.

However, the Result's Runtime-E2E section proves only:

whole-group pack

selected pack

The issued task explicitly required runtime proof for the remaining operational matrix as well.

Therefore TB-P09-T005 is NOT architect-accepted yet.

This repair is verification-first and defect-only.

DO NOT redesign UI.
DO NOT change the user's width/horizontal-scroll/layout refinements.
DO NOT add Offer editor yet.
DO NOT start provider-specific shipping forms yet.

Scope

Complete the missing real runtime proof for the already-implemented T005 behavior.

Fix code ONLY if a runtime scenario reveals a real defect.

1. Preserve User UI — Locked

All current user-authored visual changes are authoritative:

horizontal scrolling

current width behavior

seller-group layout

financial section layout

current grid filters

current kebab actions

Do NOT revert or normalize them.

2. Required Runtime Dataset

Use LOCAL/Development only.

Create/use deterministic data with:

at least one seller with 2+ order lines

at least one line with quantity > 1

at least one returnable line

if feasible, one non-returnable line

No SQL insertion of final lifecycle states.

Use application/domain/API paths.

3. Runtime Scenario A — Partial Quantity

Prove:

ordered quantity > 1

pack only part of the quantity

UI/API reflects:

packed quantity

remaining eligible quantity

remaining quantity can be packed later

Record exact:

orderLineId

ordered qty

packed qty before/after

remaining qty

4. Runtime Scenario B — Reverse Packing Before Allocation

Prove:

pack selected quantity

while not allocated to shipment:

بازگشت از بسته‌بندی is projected

operation succeeds

packed quantity decreases correctly

quantity becomes eligible again

Then repack it for next scenario.

5. Runtime Scenario C — Shipment From Subset

Prove:

create shipment from selected subset only

explicit shipment allocations persisted:

shipmentId

orderLineId

quantity

another unselected line/quantity remains unallocated

No cross-seller allocation.

6. Runtime Scenario D — Reverse Blocked After Allocation

For a quantity already allocated to active Created shipment:

try بازگشت از بسته‌بندی

backend must reject

localized human error

no state corruption

7. Runtime Scenario E — Cancel Pre-Dispatch Shipment

Prove:

Created shipment can be cancelled/voided

its allocations are released

released quantities become eligible for a new shipment

shipment remains auditable, not silently erased

Then create a replacement shipment from the released quantity.

8. Runtime Scenario F — Tracking, Dispatch, Irreversibility

On replacement shipment:

assign/update tracking as current model supports

dispatch shipment

after dispatch:

shipment cancel/release is rejected

reverse-to-packed/unpack is rejected

localized human errors

Then:

mark delivered

verify delivered quantity/state

9. Runtime Scenario G — Return Deadline

For the delivered returnable line:

Verify actual UI/API data after delivery:

returnable = yes

ReturnWindowDays snapshot

return deadline calculated from canonical delivered time

remaining return time/day count shown

Persian UI has human label

If deterministic clock/test support exists, also prove expired state.
If runtime clock manipulation is not available, focused automated test evidence for expiry is acceptable, but live delivered deadline is REQUIRED.

For non-returnable line, if fixture exists:

UI says غیرقابل مرجوعی

10. Human Status Regression

Verify normal FA UI shows no raw:

ReadyToFulfill

Packed enum name

Dispatched enum name

Delivered enum name

Return/Refund technical codes

Use human Persian business labels.

11. Operations Projection Verification

At each runtime stage capture backend-projected actions.

Minimum:

Before pack:

valid forward action(s)

Packed/unallocated:

ایجاد مرسوله

بازگشت از بسته‌بندی

Created shipment:

tracking action

ابطال مرسوله

Dispatched:

ثبت تحویل

NO reverse/cancel

Delivered:

fulfillment reverse absent

Frontend must continue consuming backend projection.

12. Cancellation Regression

Quickly verify:

whole-order cancel policy still authoritative

after dispatch, normal Order cancel is rejected

line/shipment reverse actions cannot bypass Order cancel rules

Do not redesign whole-order Operations.

13. Focused Validation

Re-run focused T005 suites plus any new defect regression tests.

Required categories:

partial quantity

unpack before allocation

unpack after allocation reject

shipment subset allocation

shipment cancellation release

post-dispatch irreversibility

return deadline

non-returnable/expired automated coverage

user UI preservation

RTL labels

Also:

recovery guard

git diff --check

No unrelated full repository suite.

14. Runtime / Browser Smoke

Use:
Backend: http://127.0.0.1:5088
Frontend: http://127.0.0.1:3000

Browser smoke must verify:

current user width/horizontal-scroll changes remain

no heavy visual change

seller groups still render

quantity selector works

reverse actions appear/disappear correctly

shipment card updates

return deadline visible

human status labels visible

USER_VISUAL_ACCEPTED remains NO.

15. Evidence

Update:
docs/evidence/TB-P09-T005/

Add:

r1-runtime-partial-quantity.md

r1-runtime-reverse-pack.md

r1-runtime-shipment-allocation.md

r1-runtime-shipment-cancel-release.md

r1-runtime-dispatch-irreversibility.md

r1-runtime-return-deadline.md

r1-ui-preservation.md

r1-recovery-sot.md

16. Recovery / Pipeline

Update:

Phase: P09

Last Architect-accepted: TB-P09-T004

TB-P09-T005 under repair

Current Repair: TB-P09-T005-R1

Last Implementation on completion: TB-P09-T005-R1

USER_VISUAL_ACCEPTED=NO

Worker Next State: IDLE

do NOT invent/start TB-P09-T006

Run recovery guard.

17. Git

If no code defect is found:

evidence-only commit is acceptable

If defects are found:

fix narrowly

preserve user-authored UI changes

Required:

push origin/main

HEAD==origin/main

no task-owned uncommitted changes

Acceptance

PASS only if ALL:

partial quantity runtime proven

unpack/reverse before allocation runtime proven

shipment subset allocation runtime proven

reverse after allocation rejected

pre-dispatch shipment cancellation releases allocations

replacement shipment can be created

post-dispatch cancel/reverse rejected

delivered transition works

live delivered return deadline shown

human FA statuses shown

whole-order cancellation policy preserved

user visual refinements preserved

focused validation green

runtime/browser smoke complete

recovery guard passes

HEAD==origin/main

Worker IDLE

no TB-P09-T006 invented

USER_VISUAL_ACCEPTED=NO.

Result Contract

Return ONLY canonical BRIDGE-WAKE-V1 result with:

Summary
Runtime-Partial-Quantity
Runtime-Reverse-Packing
Runtime-Shipment-Subset
Runtime-Allocation-Guard
Runtime-Shipment-Cancel-Release
Runtime-Dispatch-Irreversibility
Runtime-Delivery
Runtime-Return-Deadline
Status-Humanization
Operations-Projection
Cancellation-Regression
User-UI-Preservation
Focused-Validation
Runtime-Visual-Smoke
Recovery-SoT
Git
Architectural-Concerns
Visual-Concerns
Blockers

END_TOOBA_REPAIR_TASK