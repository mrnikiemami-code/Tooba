PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_REPAIR_TASK

Task-ID:
TB-P09-T003-R3

Parent-Task:
TB-P09-T003-R2

Channel:
tooba-main

WorkerId:
tooba-worker-01

AgentType:
cursor

Status:
REPAIR

USER_VISUAL_ACCEPTED=NO

Title:
Admin Orders Repair R3 — Production-Safe Manual/Card-to-Card Payment Capability

Architect Review

TB-P09-T003-R2 fixed the operational matrix, fresh Order lifecycle, manual confirmation flow, and list Return/Refund visibility.

One product-critical gap remains in the Result:

Manual gateway is non-Prod only.

The user asked for a real sellable operational flow where a store can have card-to-card/manual payment enabled and Admin can confirm the deposit.

A Development-only fake/manual gateway is not sufficient for production readiness.

This repair is intentionally narrow:
make manual/card-to-card payment a production-safe, configurable payment method using the existing Payment architecture.

Do NOT redesign Orders.
Do NOT create a second Payment subsystem.

Goal

Provide a real Manual/Card-to-Card payment method that can be enabled per store/environment using existing Payment/provider configuration patterns.

Required business flow:

Store has manual/card-to-card payment method enabled

Customer places Order

Payment remains در انتظار تأیید واریز

Admin sees تأیید واریز / optional رد واریز

Admin confirmation goes through Payment domain/application path

canonical payment success effects run

Order becomes eligible for processing

action is idempotent and permission-protected

1. Mandatory Discovery

Inspect existing:

payment provider registry

provider configuration

store/payment-method enablement

Admin payment settings

provider secrets/config storage

PaymentIntent/PaymentAttempt/Verify flow

payment success integration event

storefront payment-method list

tenant/store boundaries

current manual gateway introduced in R2

Reuse existing conventions.

Do NOT:

special-case Production with ad-hoc code

hardcode card/account data

bypass Payment Verify/settlement events

mutate Order directly

create another payment aggregate

2. Production-Safe Manual Provider

Replace/remove the non-Prod only limitation.

Manual/Card-to-Card provider must be a normal supported provider that can be enabled/disabled by configuration.

Expected semantics:

provider code remains stable

provider can be disabled by default

Admin/store config controls availability

no secret or card/account number hardcoded in source

if store-specific card/account instructions are modeled, store them through existing secure/config patterns

storefront sees manual option only when enabled

Admin confirmation only for payments created with manual provider

If the current Payment provider model has no store-level enablement:

minimally extend existing provider configuration boundary

do not build a new settings subsystem

3. Customer-Facing Manual Payment State

When customer selects manual/card-to-card:

create payment attempt through Payment module

Order remains unpaid / not fulfillable

human status: در انتظار تأیید واریز

customer/order detail may show payment instructions only if existing UI already has a suitable place

keep UI minimal

Do NOT fake succeeded payment before Admin confirmation.

4. Admin Confirmation / Rejection

Operations matrix must continue to project:

تأیید واریز

رد واریز if supported cleanly

لغو سفارش if policy allows

Confirmation:

requires existing payment.reconcile or canonical capability

backend-authoritative

idempotent

duplicate confirmation must not duplicate:

payment success

Order transition

seller settlement accrual

outbox event

wrong provider/state rejected with human localized message

Rejection:

must not mark payment succeeded

must leave a clear state and allow valid next business action according to current Payment model

do not invent refund semantics for an unpaid manual payment

5. Store / Edition Behavior

Verify both:

SingleStore

Marketplace

Manual payment availability should respect current edition/store configuration.

Do not accidentally enable it globally for all stores.

If Marketplace requires per-seller behavior, do not confuse seller settlement with customer payment method.

6. Optional Payment Evidence

Audit whether payment proof/evidence upload already exists.

If it exists:

reuse it

If it does not:

do NOT build a new Media upload subsystem in this repair

document as optional future enhancement

The core acceptance is real configurable manual payment + Admin confirmation.

7. UI Lock

No Orders redesign.

Allowed minimal UI:

payment method label

در انتظار تأیید واریز

تأیید واریز

رد واریز if supported

existing config screen field/toggle if provider configuration UI already exists and needs exposure

Do NOT change:

Grid appearance

Order detail layout

action button styling

navigation

notes/history/invoice sections

8. Focused Tests

Backend:

manual provider enabled -> storefront method available

manual provider disabled -> unavailable

create manual payment -> pending confirmation

Admin confirm -> payment succeeds through canonical path

duplicate confirm idempotent

wrong provider confirm rejected

confirm after already succeeded rejected/idempotent correctly

rejection flow

SingleStore behavior

Marketplace behavior

no duplicate settlement accrual/event

current lifecycle E2E regression

Frontend:

human manual/card-to-card label

pending confirmation status

confirm action visibility

reject action visibility if applicable

no operations menu regression

no grid width regression

no Return/Refund label regression

Also:

relevant build/typecheck/lint

recovery guard

git diff --check

9. Runtime Smoke — REQUIRED

Use:
Backend: http://127.0.0.1:5088
Frontend: http://127.0.0.1:3000

Run with a normal Development configuration that mirrors production behavior, NOT a special fake-only branch.

Required:

enable manual/card-to-card provider through normal config

create fresh Order

select manual payment

observe در انتظار تأیید واریز

Admin sees تأیید واریز

confirm

payment succeeds

next Order operation becomes processing

duplicate confirm does not duplicate effects

disable provider and verify storefront no longer offers it

If production-safe enablement cannot be implemented within existing Payment architecture:
return BLOCKED/PARTIAL with exact architectural reason.

10. Recovery / Pipeline

Update:

Phase: P09

Last Architect-accepted: TB-P09-T003

TB-P09-T003 remains under repair

Current Repair: TB-P09-T003-R3

Last Implementation on completion: TB-P09-T003-R3

USER_VISUAL_ACCEPTED=NO

Worker Next State: IDLE

do NOT invent/start TB-P09-T004

Run recovery guard.

11. Evidence

Create/update:
docs/evidence/TB-P09-T003/

Required:

r3-payment-provider-discovery.md

r3-production-manual-provider.md

r3-store-enablement.md

r3-admin-confirmation.md

r3-idempotency.md

r3-runtime-smoke.md

r3-recovery-sot.md

12. Git

Required:

preserve user-authored changes

commit repair

push origin/main

HEAD==origin/main

no task-owned uncommitted changes

Acceptance

PASS only if ALL:

manual/card-to-card is not Development-only/fake-only

provider can be enabled/disabled through normal configuration

no account/card data hardcoded in source

customer manual payment remains pending until Admin confirmation

Admin تأیید واریز is backend-authoritative

canonical Payment success/outbox path used

duplicate confirmation is idempotent

no duplicate Order/Settlement effects

wrong provider/state rejected

SingleStore/Marketplace behavior verified

UI remains minimally changed

existing Operations matrix remains working

focused tests green

runtime smoke complete

recovery guard passes

HEAD==origin/main

Worker returns IDLE

no TB-P09-T004 invented

USER_VISUAL_ACCEPTED=NO.

Result Contract

Return ONLY canonical BRIDGE-WAKE-V1 result with:

Summary
Payment-Provider-Discovery
Production-Manual-Provider
Store-Enablement
Customer-Pending-State
Admin-Confirmation
Rejection-Flow
Idempotency
Edition-Behavior
Optional-Evidence
UI-Regression
Focused-Validation
Runtime-Smoke
Recovery-SoT
Git
Architectural-Concerns
Visual-Concerns
Blockers

END_TOOBA_REPAIR_TASK