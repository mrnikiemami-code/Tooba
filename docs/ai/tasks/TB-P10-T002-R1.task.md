PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_REPAIR_TASK

Task-ID:
TB-P10-T002-R1

Parent-Task:
TB-P10-T002

Channel:
tooba-main

WorkerId:
tooba-worker-01

AgentType:
cursor

Status:
REPAIR

USER_VISUAL_ACCEPTED=NO

Title:
Storefront Shipping Final Multi-Seller Runtime Gate

Architect Review

TB-P10-T002 is functionally strong and almost accepted.

Accepted from T002:

real /shipping projection

Store-enabled shipping methods only

backend-authoritative shipping pricing

saved/new address flow

recipient + notes persistence

earliest-delivery backend validation

later delivery allowed / earlier rejected

/payment handoff

focused tests 13/13

real runtime A/B/C/D/F

Shopeiva visual contract preserved

However, the Result explicitly states:

Runtime multi-seller limited to available fixture seller; MAX-prep covered by unit tests.

The T002 acceptance contract required Scenario E as a REAL runtime multi-seller checkout with 2+ Sellers.

Therefore T002 is NOT fully accepted yet.

This repair is ONLY the missing multi-seller runtime proof and any minimal defect exposed by it.

Do NOT redesign Shipping.
Do NOT change Payment.
Do NOT add new shipping concepts.
Do NOT start TB-P10-T003.

Applicable Locks

Read:
docs/architecture/TOOBA-LOCKS.md

Preserve:

LOCK-SF-001… shipping locks

Product != Offer

seller-specific fulfillment readiness

Store-enabled shipping methods only

shipping price backend-authoritative

earliest delivery never before all required seller readiness

no fake methods/rates/dates

Shopeiva UI locked

backend-authoritative validation

dynamic language registry

no raw IDs/codes in normal UI

1. Build/Locate Real Multi-Seller Runtime Fixture

Use normal supported application/runtime mechanisms.

Required cart:

one Store

one checkout/cart

at least 2 distinct Sellers

at least one sellable Offer from each Seller

valid inventory/reservations

both Offers visible/usable in current Store context

known seller preparation days that differ, e.g. Seller A faster than Seller B

at least one Store-enabled shipping method eligible for the whole customer checkout path

Do not fake SellerPartyId in frontend state.
Do not hand-edit final projection rows to manufacture success.

Document:
docs/evidence/TB-P10-T002/r1-multiseller-fixture.md

2. Real Multi-Seller Shipping Projection

Open the real Storefront /shipping route with that cart.

Verify the backend projection resolves BOTH sellers.

Evidence must prove:

Seller A present

Seller B present

cart/checkout remains one customer checkout

shipping-method eligibility is computed across both

no request-per-seller frontend waterfall

no seller internal IDs shown in customer UI

3. Earliest Delivery — Real Runtime Proof

Use intentionally different preparation times.

For example:

Seller A preparation = 1 day

Seller B preparation = 3 days

selected shipping method lead = N days

Expected:
minimum = applicable base date + max(1,3) + N
according to the exact T002 implemented formula/current domain semantics.

Required runtime proof:

UI does NOT offer an earlier date

forged backend request for earlier date is rejected

exact minimum accepted if current business rule permits

a later date is accepted

Do not satisfy this with unit tests only.

4. Real Multi-Seller Shipping Price

Verify with the same runtime fixture:

customer-visible shipping quote is authoritative

quote reflects current supported multi-seller shipping calculation

changing shipping method (when more than one eligible real method exists) changes quote correctly

if only one eligible method exists, document that and verify its exact amount

no static/free fallback

no Rial/Toman conversion error

Do not invent a new allocation model in this repair.

5. Seller-Specific Constraint Failure

Prove one realistic failure case in runtime:

Example:

disable/make selected method unavailable for Seller B or its route/destination through supported configuration

Expected:

method is no longer offered for the whole checkout when current architecture requires every seller to support it
OR

backend rejects invalid selection according to canonical shipping semantics

Restore fixture/config after test if needed.

No direct production-data corruption.

6. Persistence / Handoff

For the multi-seller cart:

select valid address

select valid shipping method

select valid delivery date/time

refresh /shipping

authoritative selection remains valid

click ادامه به پرداخت

/payment opens

checkout draft still contains correct multi-seller shipping selection

Do not implement payment methods.

7. Focused Regression

Re-run only relevant tests:

StorefrontShippingCalculatorTests

storefront-shipping-api tests

any multi-seller shipping tests added

recovery guard

git diff --check

If runtime exposes a real defect:

repair minimally

add focused regression

do not broaden scope

8. Evidence

Add under:
docs/evidence/TB-P10-T002/

Required:

r1-multiseller-fixture.md

r1-multiseller-runtime.md

r1-delivery-minimum-runtime.md

r1-shipping-price-runtime.md

r1-constraint-failure.md

r1-handoff.md

r1-focused-validation.md

r1-recovery-sot.md

Use human seller/product labels in screenshots/docs where possible.
Raw IDs may exist only in technical evidence where needed.

9. Recovery / Pipeline

Phase: P10 — Storefront Checkout Journey

Last Architect-accepted: TB-P10-T001

TB-P10-T002 under repair

Current Repair: TB-P10-T002-R1

USER_VISUAL_ACCEPTED=NO

Worker Next State after Result: IDLE

do NOT invent/start TB-P10-T003

Run recovery guard.

10. Git

Required:

preserve user-authored changes

commit + push if changes/evidence added

HEAD==origin/main

no task-owned uncommitted changes

Acceptance

PASS only if ALL:

real runtime cart has 2+ distinct Sellers

/shipping resolves both sellers

Store-enabled shipping eligibility works across both

runtime earliest-delivery uses slowest required seller readiness

earlier forged delivery rejected

exact/later valid date behavior proven

multi-seller shipping amount authoritative

seller-specific incompatibility handled correctly

refresh persistence works

/payment handoff preserves authoritative shipping state

no UI redesign

no Payment implementation

focused tests green

recovery guard passes

HEAD==origin/main

Worker IDLE

no TB-P10-T003 invented

USER_VISUAL_ACCEPTED=NO.

Result Contract

Return ONLY canonical BRIDGE-WAKE-V1 Result with:

Summary
Applicable-Locks
MultiSeller-Fixture
MultiSeller-Runtime
Delivery-Minimum-Runtime
Shipping-Price-Runtime
Constraint-Failure
Persistence-Handoff
Focused-Validation
Recovery-SoT
Git
Architectural-Concerns
Visual-Concerns
Blockers

END_TOOBA_REPAIR_TASK