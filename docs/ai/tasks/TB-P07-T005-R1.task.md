PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

TASK-ID: TB-P07-T005-R1
PARENT-TASK: TB-P07-T005
PROJECT: Tooba
PHASE: P07
CHANNEL: tooba-main
STATUS: ISSUED
TASK-TYPE: FOCUSED REPAIR — CATEGORY VIEW/EDIT MODES + CLEAN SLUG UX
WORKER-POLICY: ONE WORKER = ONE ACTIVE TASK

Architect Review

TB-P07-T005 functional result was PASS, but USER_VISUAL_ACCEPTED remained NO.

User manual review now establishes:

CURRENT CATEGORY TREE IS APPROVED.

DO NOT CHANGE the tree visual direction, orientation, interaction model, spacing, drag behavior, or hierarchy.

Repair only the Category workspace/form behavior and slug UX.

All applicable Admin forms must support explicit VIEW and EDIT modes according to access.

Public Category URLs must use clean localized human slugs such as:

/fa/category/گوشی-موبایل

DO NOT append visible CategoryId fragments such as:

گوشی-موبایل-01a03826

to the public slug/route.

Hard Scope Lock

DO NOT TOUCH AppCategoryTree except for an unavoidable compile/API compatibility fix.

Specifically forbidden unless a real blocker exists:

visual changes

RTL direction changes

node layout changes

drag/drop changes

search changes

icon changes

spacing changes

selected state changes

Tree redesign

The current Tree is USER-APPROVED.

Goal

Repair Category Admin workspace so that:

category details can be opened in explicit read-only VIEW mode

authorized users can intentionally switch to EDIT mode

users without edit access can still view data cleanly

editable controls are not exposed in VIEW mode

saving/canceling returns to predictable state

clean localized slug is used

uniqueness is enforced by system/backend

CategoryId remains internal and invisible in public URL

1 — View Mode

Default detail-opening behavior should be VIEW mode unless the existing product convention clearly requires another safe default.

In VIEW mode:

show category data as readable presentation

no editable input controls

no save button

no destructive mutation controls

show Edit action only if user has permission

do not visually imitate disabled forms full of grey inputs

use readable field/value presentation consistent with approved UI

Fields include as appropriate:

Name

Parent

Status

SortOrder

IsVisible

Slug

Storefront route

translation readiness

image/icon

other General fields already in T005 scope

2 — Edit Mode

Authorized user chooses explicit action:

ویرایش

Then workspace enters EDIT mode.

In EDIT mode:

fields become appropriate inputs

Save clearly primary

Cancel clearly secondary

destructive actions remain separate

validation errors appear next to relevant fields

do not lose unsaved data accidentally

if navigating away with dirty changes, use existing Tooba dirty-state convention or a minimal safe confirmation

On successful save:

update data

update tree label only if translated Name changed

return to VIEW mode

preserve same category route/deep link

show localized success feedback

On Cancel:

discard unsaved edits

return to VIEW mode

3 — Permission-Aware UI

Use existing SpiceDB-centered authorization architecture.

Do NOT implement fake frontend-only security.

Required behavior:

View permission only:

can open Category workspace

sees VIEW mode

does NOT see enabled Edit action

cannot mutate by directly invoking UI controls

Edit permission:

sees ویرایش

can enter EDIT mode

can save permitted fields

Other permissions such as publish/archive/delete remain separate and must not be inferred merely from edit permission if existing authorization distinguishes them.

Backend remains authoritative.

4 — Form Pattern as Reusable Admin Principle

Do not hardcode an ad hoc Category-only hack if a small reusable Admin form-mode primitive fits current architecture.

Preferred conceptual pattern:

ViewMode
EditMode
canView
canEdit
isDirty
onEdit
onSave
onCancel

Keep it lightweight.
Do NOT start a broad unrelated refactor of every Admin screen in this Repair.

Document the pattern so later Admin forms follow the same rule.

5 — Slug UX

User enters/edits ONLY the human localized slug, e.g.:

گوشی-موبایل

Do NOT ask user to know, type, copy, or preserve any CategoryId suffix.

Do NOT automatically append visible CategoryId to the slug.

Correct public canonical route example:

/fa/category/گوشی-موبایل

Incorrect:

/fa/category/گوشی-موبایل-01a03826

CategoryId remains internal database/entity identity.

6 — Slug Generation

When creating or editing a localized category translation:

auto-suggest slug from localized Name

user may edit the human slug

normalize using existing T004 slug rules

show a route preview

never expose ID suffix as a required concept

Example:

Name:
گوشی موبایل

Suggested slug:
گوشی-موبایل

Preview:
/fa/category/گوشی-موبایل

7 — Slug Uniqueness

T004 already established unique (Locale, Slug).

Preserve this architecture.

When user attempts a duplicate slug:

backend must reject safely

UI must explain in ordinary language

do NOT silently append CategoryId

do NOT silently mutate to a cryptic suffix

Preferred Persian message concept:

این نامک برای یک دسته‌بندی دیگر استفاده شده است. یک نامک متفاوت انتخاب کنید.

If the current backend contract does not return a sufficiently specific conflict, add the smallest safe error mapping necessary.

8 — Slug Change / SEO Safety

Preserve T004 slug history behavior.

On changing:

گوشی-موبایل

to:

موبایل

the previous slug must remain in slug history and route resolver must allow permanent redirect behavior to the new canonical route.

Do not remove or bypass slug history.

Do not create redirect loops.

9 — Route Preview

In VIEW mode:
show clean canonical public path, read-only.

Example:

/fa/category/گوشی-موبایل

In EDIT mode:
preview updates as slug is edited, but saved canonical route changes only after successful save.

Do not mix internal CategoryId into the displayed URL.

10 — Status / General Fields Editing

The user specifically observed that current statuses/details are not editable.

In EDIT mode, make T005 General fields actually editable according to T004 contracts and permissions.

At minimum inspect/support where backend permits:

Parent

Status

SortOrder

IsVisible

localized Name

localized Slug

image/icon if existing API supports it cleanly

Do not fake controls if an API is missing.
If a narrow T004 API gap exists, add the minimal module-owned endpoint/command needed.

11 — Translation Editing Boundary

Do not fully implement the rich T006 translations experience here.

R1 may edit the active locale Name + Slug where necessary for General form UX.

Full translation editor remains T006.

12 — Tree Preservation During Save

Because Tree is approved:

do not restyle it

after Name update, update/refresh only necessary node data

preserve expanded nodes

preserve selected category

preserve scroll position where practical

do not reset whole Tree UX unnecessarily

13 — Tests

Add/update frontend tests for:

workspace defaults to VIEW mode

view-only user sees no usable edit action

editor sees Edit action

entering EDIT mode

Save

Cancel

dirty-state protection if implemented

active locale Name edit

human slug edit

clean route preview

no CategoryId suffix in displayed/public route

duplicate slug error UX

successful slug change returns VIEW mode

Tree expanded/selected context preserved after save

Backend tests if touched:

duplicate locale+slug conflict remains enforced

slug change creates history

current route resolves

old route resolves redirect

no visible-ID suffix policy introduced

14 — Validation

Frontend:

typecheck

lint

full relevant tests

production build

Backend if touched:

restore

build

full tests

Require:

0 errors

0 failed

0 skipped

no new warnings

Always:

git diff --check

15 — Runtime

Keep alive:

Backend :5088

Frontend :3000

Shopeiva :3001

Preview:
http://localhost:3000/fa/admin/catalog/categories

16 — Git

Commit + push.

Require:

HEAD == origin/main

tracked tree clean

unrelated user changes preserved

Acceptance

PASS only if ALL:

approved Category Tree is unchanged

Category detail has explicit VIEW mode

Category detail has explicit EDIT mode

permission-aware View/Edit behavior works

view-only users are not presented with editable controls

edit-authorized users can intentionally edit

Save/Cancel behavior is predictable

editable General fields are actually wired

user edits only human slug

no CategoryId suffix is appended to public slug

public route follows /{locale}/category/{localizedSlug}

duplicate locale+slug is rejected clearly

slug history remains functional

route preview is clean and human-readable

tree context survives edits

full validation green

Git synchronized

USER_VISUAL_ACCEPTED remains NO until user manually reviews the repaired runtime.

Result Contract

Return ONLY:

PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_WORKER_RESULT

Task-ID:
TB-P07-T005-R1
Parent-Task:
TB-P07-T005
Channel:
tooba-main
Status:
PASS | FAIL | BLOCKED | RECOVERY_CONFLICT

Summary:
...

Scope:

Tree touched:

Tree visual changed:

Workspace repaired:

Slug UX repaired:

View-Mode:

default mode:

field presentation:

editable controls exposed:

Edit action:

view-only behavior:

Edit-Mode:

enter edit:

editable fields:

Save:

Cancel:

dirty-state:

post-save mode:

Authorization:

SpiceDB path:

canView:

canEdit:

backend authoritative:

Slug:

user-entered value:

ID suffix appended:

route preview:

uniqueness:

duplicate UX:

slug history:

redirect behavior:

Tree-Preservation:

expanded state:

selected node:

scroll/context:

label refresh:

API:

frontend-only:

backend touched:

endpoints/commands added:

gaps:

Validation:

frontend typecheck:

frontend lint:

frontend tests:

frontend build:

backend restore:

backend build:

backend full tests:

passed:

failed:

skipped:

warnings:

new warnings:

git diff --check:

Runtime:

Backend:

Frontend:

Shopeiva:

Category Admin:

kept alive:

Git:

branch:

commit:

push:

final HEAD:

origin/main:

synchronized:

tracked tree:

unrelated user changes:

Visual-Concerns:
...

Architectural-Concerns:
...

Blockers:
...

END_TOOBA_WORKER_RESULT

After Result:

Worker -> IDLE / Waiting

keep runtimes alive

do not invent T006

do not add extra prose

END_TASK