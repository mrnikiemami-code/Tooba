PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_REPAIR_TASK

Task-ID:
TB-P10-T004-R24-R1-R3

Parent-Task:
TB-P10-T004-R24-R1-R2

Channel:
tooba-main

WorkerId:
tooba-worker-01

AgentType:
cursor

Status:
REPAIR

USER_VISUAL_ACCEPTED=NO

Title:
Storefront Account + Cart Persistence Repair — Real Customer Identity, Logout Cart Preservation, Exact Cart Conversion Boundary

Architect Reopen Decision

The user manually found three remaining customer-facing problems after R24-R1/R2:

Authenticated header shows only generic حساب کاربری; customer identity/name is not shown.

Customer filled the Cart while authenticated, logged out, and the Cart appeared to disappear.

User reports the Cart is again becoming empty before the intended Payment transition.

R24-R1/R2 are therefore NOT considered visually complete.

This task must repair/prove all three with real browser evidence.

Do NOT redesign the storefront.
Do NOT store Cart line state inside auth Session.
Do NOT weaken account isolation.
Do NOT clear/delete authenticated Cart on logout.
Do NOT move the atomic conversion boundary earlier.
Do NOT create TB-P10-T005.

Product Behavior — LOCKED
A. Authenticated identity in header

Large-store behavior:

Logged out:

show ورود

Logged in:

show customer identity affordance

prefer human customer name when available

account dropdown includes:

حساب/پنل کاربری

سفارش‌های من

خروج از حساب کاربری

Name precedence for header:

customer profile DisplayName / FirstName+LastName when available

mobile number as safe fallback

only then generic حساب کاربری

Do NOT invent a name.
Do NOT derive a name from shipping address unless customer-profile architecture explicitly defines that as profile data.

Use ONE canonical Storefront account state across all pages.

B. Logout does NOT delete account Cart

Authenticated Cart is server-side account-owned commerce state.

On logout:

auth session ends

authenticated Cart MUST remain stored for that CustomerId

Orders remain stored

no Cart lines are deleted

no Cart is converted

no Cart is cancelled

For privacy:

anonymous post-logout session must NOT see the authenticated Cart contents

header/cart badge may show a separate anonymous Cart

this is not Cart loss

On re-login with same customer:

previous authenticated Active Cart MUST be restored

if anonymous post-logout Cart acquired items meanwhile, canonical merge applies

quantities/lines preserved according to normal merge rules

Thus:
logout hides account Cart from anonymous context; it never destroys it.

C. Exact Cart conversion boundary

Cart may become Converted ONLY after successful atomic checkout commit.

Before final Shipping → Payment commit:

Cart status Active

all lines present

authenticated header badge matches Active Cart

no Order

no Cycle #1

On click ادامه به پرداخت:

frontend must NOT pre-clear

backend performs atomic commit

only after COMMIT:

source Cart becomes Converted

Order exists

Cycle #1 exists

Payment page opens

active-cart badge may now become 0/new cart

Therefore:

badge=0 ON the Payment page is correct ONLY if atomic checkout commit has already succeeded.

badge/cart=0 on Shipping BEFORE the commit success response is a defect.

This task must prove exact timestamps/network ordering in browser.

Recovery State

Phase: P10
Last Architect-accepted: TB-P10-T004-R24
Last Implementation: TB-P10-T004-R24-R1-R2
Current Repair: TB-P10-T004-R24-R1-R3
Worker: tooba-worker-01
Expected branch: main
Expected previous HEAD: 6ca66a7271bc4ae3a0879dca04f134b6ba4a4647
USER_VISUAL_ACCEPTED=NO

Applicable Locks

Preserve LOCK-SF-001…122.

Especially:

LOCK-SF-103 atomic checkout

LOCK-SF-117 Cart != Session

LOCK-SF-118 Cart survives login until COMMIT

LOCK-SF-119 canonical shared account header

LOCK-SF-120 canonical logout

LOCK-SF-121/122 recipient-name rules

1. Recovery Preflight

Record exact pipeline/Git state.

Unexpected tracked divergence => BLOCK.

Evidence:
docs/evidence/TB-P10-T004/r24-r1-r3-recovery-start.md

2. Root-Cause Discovery — Header Identity

Trace:

/api/auth/me

Host session/customer projection

customer profile/entity

available FirstName / LastName / DisplayName / Mobile fields

StorefrontAccountMenu input/state

desktop header

mobile header

Create:
docs/evidence/TB-P10-T004/r24-r1-r3-header-identity-discovery.md

Required table:
Source | CustomerId | Name fields | Mobile | Current header use | Required use

Do NOT use shipping-recipient name as customer identity unless it is canonically the customer profile.

3. Canonical Account Display

Implement/reuse one canonical account-display projection.

Preferred:

displayName

mobile

authenticated flag

Display:

name when present

otherwise mobile

generic account label only last fallback

Dropdown must retain:

customer panel

orders

logout

Same component/state on:
Home, PLP, PDP, Cart, Shipping, Payment, Customer pages.

No page-specific auth header.

4. Logout Cart Persistence Discovery

Trace actual logout:

frontend clearCartSession

BFF logout

auth cookie removal

Active Cart database rows

CustomerId ownership

anonymous Cart/session creation after logout

Create:
docs/evidence/TB-P10-T004/r24-r1-r3-logout-cart-discovery.md

Prove whether any current path:

deletes Cart

marks Cart abandoned

clears CustomerId

converts Cart

loses discoverability on next login

If yes, minimally repair.

5. Correct Logout Semantics

Required runtime state:

Before logout:

Customer A authenticated

Active Cart A has 2+ lines

After logout:

DB Cart A still Active

Cart A still owned by Customer A

same lines/qty remain

anonymous session cannot access Cart A

After same customer re-login:

Cart A restored as authenticated Active Cart

badge/Cart lines restored

If anonymous Cart B was created after logout:

login merges B into A canonically

no duplicate quantities

no loss

no Reservation/Order/Payment during merge

6. Logout UI

On authenticated header:
خروج از حساب کاربری

After click:

canonical logout endpoint

header changes to logged-out state

no account Cart destruction

no Order mutation

no pending-order mutation

Do not show customer Cart to anonymous user after logout.

7. Exact Cart Conversion Root-Cause Discovery

Instrument/trace one real checkout:

T0 Cart page Active
T1 Shipping page loaded
T2 Address selected/saved
T3 user clicks ادامه به پرداخت
T4 checkout commit request sent
T5 backend transaction begins
T6 Order/Reserve/Cycle/Convert writes
T7 transaction COMMIT
T8 success response
T9 navigation to Payment
T10 header current-cart refresh

Evidence:
docs/evidence/TB-P10-T004/r24-r1-r3-conversion-timeline.md

Record:

CartId

Cart status

line count

Order existence

Cycle existence

browser route

header badge
at T0–T10.

8. Conversion Boundary Assertions

Before T7 successful COMMIT:

Cart Active

lines exist

badge not zero solely because user clicked

no UI optimistic clearing

After T7:

old Cart Converted

Order exists

Cycle #1 exists

At Payment page:

badge 0/new Cart is acceptable

If commit fails at T6/T7:

Cart remains Active

lines remain

badge remains original count

route must not proceed to Payment

9. Frontend Cart State Source

Header/Cart must use canonical current Active Cart API/state.

Forbidden:

zeroing badge on route transition

zeroing badge on click

clearing local cart before backend success

treating shipping draft exists as Cart converted

using stale empty post-login cart over real active cart

10. Customer Identity Persistence

If customer currently has no profile name but shipping has a name:
do NOT silently mutate customer profile unless existing product behavior explicitly asks for it.

For Development test user:

if profile already has name, display it

otherwise display mobile 09111111111

generic حساب کاربری alone is not preferred when mobile is known

Document current available identity.

11. Focused Tests

Header:

logged-out -> ورود

logged-in with profile name -> name

logged-in without name -> mobile

desktop dropdown panel/orders/logout

mobile same semantics

route navigation keeps same identity

Logout:
7. authenticated Cart has 2 lines
8. logout -> DB Cart remains Active with 2
9. anonymous cannot access it
10. re-login -> same Cart restored
11. anonymous interim Cart merge -> no loss/duplication
12. logout does not mutate Orders

Conversion:
13. Shipping before final click -> Cart Active
14. address save -> Cart Active
15. click -> frontend does not pre-clear
16. before COMMIT -> badge/lines intact
17. after COMMIT -> Converted
18. Payment page -> badge may be 0/new cart
19. fault before COMMIT -> Cart still Active
20. fault -> no Payment navigation
21. R21 atomic tests remain green

Regression:
22. OTP login
23. recipient First/Last
24. pending payment
25. cancel/hide
26. open-unpaid/churn
27. recovery guard
28. git diff --check

Do NOT run unrelated full suites.

12. Real Browser Runtime — REQUIRED

Use real Host :5088 + FE :3000.

A. Login with 09111111111 / 123456
B. header displays actual name if profile has one, otherwise mobile
C. add 2 products
D. Cart count=2
E. logout
F. verify anonymous cannot see account Cart
G. DB/API proof account Cart still Active with 2
H. login again
I. Cart count restored=2
J. navigate Home/PDP/Cart/Shipping — same identity + same Cart
K. add/save address — Cart remains 2
L. before final continue-to-payment — Cart remains 2
M. click continue and capture request timeline
N. only after successful backend COMMIT Cart converts
O. Payment page opens
P. badge 0/new Active Cart only now
Q. repeat with injected commit failure — Cart remains 2 and no Payment navigation
R. mobile account menu + logout smoke

Capture screenshots:

logged-in header identity

account dropdown

Cart before logout

anonymous after logout

restored Cart after re-login

Shipping immediately before final commit with badge 2

Payment after successful commit with badge 0

13. Network / Console

Required:

no auth polling

no cart polling

no repeated merge

no 401 storm

no console errors

no duplicate logout calls

no duplicate checkout submit

14. Anti-Pattern Gate

Scan for:

deleting account Cart on logout

Cart lines in Session

showing account Cart to anonymous after logout

generic account label despite available name/mobile

using shipping recipient as account identity without canonical profile relation

optimistic badge zero

route-based Cart clearing

duplicate header state

localStorage Cart SoT

polling

timing hacks

Any occurrence => REPAIR.

Evidence:
docs/evidence/TB-P10-T004/r24-r1-r3-antipattern-scan.md

15. Lock Registry

Add non-duplicate locks equivalent to:

Logout terminates authentication but never deletes/converts the customer-owned Active Cart; the Cart is hidden from anonymous context and restored/merged on re-login.

Authenticated Storefront header displays canonical customer identity (name when available, mobile fallback) and one shared account menu across all Storefront routes.

Cart badge/lines may not be optimistically cleared on final checkout action; conversion becomes visible only after authoritative atomic commit.

16. Evidence

Add:

r24-r1-r3-recovery-start.md

r24-r1-r3-header-identity-discovery.md

r24-r1-r3-logout-cart-discovery.md

r24-r1-r3-account-display.md

r24-r1-r3-logout-cart-persistence.md

r24-r1-r3-conversion-timeline.md

r24-r1-r3-network.md

r24-r1-r3-focused-validation.md

r24-r1-r3-runtime.md

r24-r1-r3-antipattern-scan.md

r24-r1-r3-recovery-sot.md

17. Recovery / Pipeline

At completion:

Phase P10

Last Architect-accepted before review: TB-P10-T004-R24

Last Implementation: TB-P10-T004-R24-R1-R3

Current Issued/Repair: none

USER_VISUAL_ACCEPTED=NO

Worker IDLE

branch main

ending HEAD

HEAD==origin/main

task-owned clean

no TB-P10-T005

Acceptance

PASS only if ALL:

logged-in header shows real customer name or mobile fallback, not generic-only account text

customer panel/orders/logout present consistently across storefront

logout does not delete/convert account Cart

anonymous post-logout cannot see account Cart

re-login restores account Cart

interim anonymous Cart merges safely

Cart remains Active through Shipping/address work

no optimistic badge/cart clearing

exact conversion timeline proves Cart converts only after backend COMMIT

failed commit leaves Cart intact and prevents Payment navigation

runtime A–R PASS

focused tests green

anti-pattern scan CLEAN

recovery exact

HEAD==origin/main

Worker IDLE

no TB-P10-T005

Result Contract

Return ONLY canonical BRIDGE-WAKE-V1 Result with:

Summary
Applicable-Locks
Recovery-Start
Header-Identity-Discovery
Logout-Cart-Discovery
Account-Display
Logout-Cart-Persistence
Conversion-Timeline
Network
Focused-Validation
Runtime
AntiPattern-Scan
Lock-Registry
Recovery-SoT
Git
Architectural-Concerns
Visual-Concerns
Blockers

END_TOOBA_REPAIR_TASK