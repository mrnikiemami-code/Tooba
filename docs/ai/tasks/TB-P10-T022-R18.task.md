PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_TASK

Task-ID:
TB-P10-T022-R18

Parent-Task:
TB-P10-T022-R17

Channel:
tooba-main

WorkerId:
tooba-worker-01

AgentType:
cursor

Status:
ISSUED

Appearance-USER_VISUAL_ACCEPTED=YES
Builder-USER_VISUAL_ACCEPTED=NO

Title:
Campaign Promotional Pricing + Lightweight Capability Map — Real Amazing Price Without Duplicating Pricing Truth

Architect Review

TB-P10-T022-R17 is ACCEPTED.

The vertical path is now proven:
Builder → PromotionCampaign source → real campaign resolver → Preview → Publish → SSR Storefront.

Remaining critical commercial gap:
campaign membership currently uses canonical normal AuthoredPrice only.
A real Amazing Offer still needs a real campaign-scoped promotional selling price.

This task must solve that gap correctly inside the EXISTING Pricing architecture.

Also, preserve the valuable R14–R17 audit findings in ONE lightweight architecture capability map.
This is documentation capture only — NO new broad audit.

IMPORTANT:
The capability map is NOT a mandatory file to read before every task.
It is consulted/updated only when a task introduces, changes, or depends on architectural capability ownership.

Canonical Start

Last Architect-accepted:
TB-P10-T022-R17

Last Implementation:
TB-P10-T022-R17

Expected previous HEAD:
368dba34bb0ad893354cb33005a88bf6c581bcec

User-work ancestor:
18ca10c9

Locks:
LOCK-SF-001…411

Appearance:
YES

Builder:
NO

Worker Protocol

No heartbeat.
No polling.
No IDLE loop.

After canonical Result through Bridge:
STOP completely.

1. Recovery Preflight

Verify:

branch main

HEAD==origin/main

R17 PromotionCampaign source present

18ca10c9 ancestor

user work preserved

unrelated stashes/.tmp untouched

Evidence:
docs/evidence/TB-P10-T022-R18/recovery-start.md

Unexpected tracked divergence => RECOVERY_CONFLICT.

2. Narrow Pricing Discovery ONLY

Do NOT re-audit the whole repository.

Inspect only the existing Pricing path necessary to answer:

exact AuthoredPrice entity/table

exact pricing dimensions currently supported

QualifierKind / qualifier representation

Market / Region / Channel / Currency semantics actually present

current price precedence/resolution algorithm

current uniqueness/index constraints

current bulk pricing resolver used by Product Showcase

how effective selling price is selected today

Goal:
find the smallest safe extension for campaign-scoped promotional price.

Evidence:
docs/evidence/TB-P10-T022-R18/pricing-discovery.md

3. Architectural Requirement — One Pricing Engine

There must remain ONE canonical pricing engine.

Forbidden:

CampaignOffer.PromoAmount scalar

second promotion-price resolver disconnected from Pricing

duplicated currency/market/channel logic

copied normal/list price into campaign tables

computing Storefront discount from unrelated fields

Preferred:
extend/reuse existing AuthoredPrice + qualifier model so a merchandising campaign can participate in normal price resolution.

Use actual project conventions, not invented schema.

4. Campaign Price Qualifier

If existing Pricing qualifier architecture safely supports extension:

Add a campaign-aware qualifier using repository conventions.

Semantics:

price record remains an AuthoredPrice / canonical pricing record

campaign identity is a qualifier/scope condition

existing Currency and other canonical dimensions remain authoritative

campaign-scoped price is only eligible while the campaign is runtime-active and the SellerOffer is a member

Do NOT make AMAZING itself the price qualifier if campaign instance identity is needed.
A concrete campaign price belongs to the concrete campaign context.

If existing qualifier system structurally cannot express this safely:
STOP implementation of promo pricing and return BLOCKER with exact minimal schema change proposal.
Do NOT fall back to scalar PromoAmount.

5. Price Resolution Precedence

Define deterministic precedence using current Pricing rules.

Required behavior:

applicable campaign price may override normal authored selling price only for the matching active campaign context

outside campaign context, normal price remains unchanged

future campaign price must not activate early

expired/archived campaign price must not apply

wrong Store/campaign must not apply

wrong Currency/Market/Channel must not apply

if no valid campaign price exists, use normal canonical price

Do not invent arbitrary precedence unrelated to current engine.

Evidence:
docs/evidence/TB-P10-T022-R18/price-precedence.md

6. Compare-At / Discount Semantics

Need a commercially correct Storefront projection.

For active campaign item:

promotional selling price = applicable campaign-scoped canonical price

compare-at/original price = canonical normal price only when it is strictly greater than promo price

discount percent = derived from those two prices

never store discount percent

If promo >= normal:

do not show fake discount

do not show misleading crossed-out price

Use existing Money/currency semantics.

Evidence:
docs/evidence/TB-P10-T022-R18/discount-semantics.md

7. Bulk Pricing — No N+1

PromotionCampaign source already resolves multiple members.

Campaign price resolution must support bulk evaluation.

Required:

no per-member DB roundtrip

reuse/extend existing batch pricing resolver

bound by existing Product Showcase MaxTake

one coherent pricing projection per item

Evidence:
docs/evidence/TB-P10-T022-R18/bulk-pricing.md

8. Real Development Seed Prices

Extend Development/Test campaign seed from R16 with real campaign-scoped prices.

At minimum:

3 active campaign members receive valid promo prices lower than their normal prices

at least 1 member has no promo override and therefore falls back to normal price

at least 1 future campaign member has a promo price that must NOT apply now

multi-currency or additional dimension case if existing dev data safely supports it

Seed must remain:

Development/Test only

idempotent

deterministic

no production pollution

Do not mutate normal AuthoredPrice truth merely to simulate discount.

Evidence:
docs/evidence/TB-P10-T022-R18/test-prices.md

9. Builder Preview Integration

Existing R17 source UI/persistence remains unchanged.

When پیشنهاد شگفت‌انگیز resolves:

ProductCard projection receives effective promo selling price when valid

normal price is available for compare-at display if existing ProductCard supports it

derived discount percent only when semantically valid

Do NOT redesign the source step.
Do NOT create Amazing-specific ProductCard.

10. Published Storefront Integration

Published PromotionCampaign source must show the same effective pricing semantics under SSR.

Required:

server-rendered HTML contains promo price for active seeded campaign

normal price remains unchanged outside campaign context

future campaign price does not leak

expired campaign price does not leak

no client-only price correction flash

No polling.

Evidence:
docs/evidence/TB-P10-T022-R18/storefront-pricing.md

11. Cart / Checkout Price Integrity

This task MUST inspect the existing add-to-cart / cart pricing boundary narrowly enough to avoid a dangerous mismatch.

Determine whether Cart/Checkout currently re-resolves price from canonical Pricing at command time.

Required commercial invariant:
If customer adds an active campaign item at promo price, server-side cart/checkout must validate/re-resolve the same applicable campaign price context.

Do NOT trust client price.

If current Cart/Checkout path cannot carry/resolve campaign context safely:

do not fake end-to-end purchase support

document exact gap

either minimally extend context if tightly scoped and safe

or return this as an explicit follow-up blocker while keeping Storefront display correct

Evidence:
docs/evidence/TB-P10-T022-R18/cart-price-integrity.md

12. Lightweight Capability Map — Capture Existing Audit Value

Create:
docs/architecture/TOOBA-CAPABILITY-MAP.md

DO NOT perform a new whole-repo audit.

Populate only verified facts from R14–R18, including:

Commerce

SellerOffer = canonical sellable listing identity

Pricing

AuthoredPrice = canonical pricing truth

actual verified dimensions/qualifier model

campaign pricing extension point after this task

bulk resolver identity

Inventory

StockPosition = canonical stock truth

Checkout Promotions

PromotionDefinition = checkout discount/promotion concern

explicitly NOT merchandising campaign rail ownership

Merchandising

MerchandisingPromotionType

MerchandisingCampaign

MerchandisingCampaignTranslation

MerchandisingCampaignOffer

active/future resolver contracts

source integration

Builder Product Showcase Sources

Manual

Category

Brand

Newest

PromotionCampaign

Localization

Page locale owns section runtime locale

campaign translation fallback path

Scope

Store/tenant scope behavior

Marketplace + Single-Store shared model

Deferred/Extension Points

any capability still intentionally deferred after R18

For each capability record compactly:

Canonical owner

Entity/Table or Service/Resolver

Module

Scope

Reuse rule

Last Verified Task

Keep it compact.
This is a lookup map, not a giant architecture book.

13. Capability Map Usage Rule

Add a short note to the file itself:

Do NOT read this file automatically for every task.

Consult it when a task introduces a new capability, changes ownership/schema, or needs to discover whether a capability already exists.

Update only the affected sections when architectural ownership/capability changes.

Evidence files remain the deep-detail source; this map is the quick index.

Do NOT add mandatory per-task startup reading.

14. Focused Tests

Required:

normal price resolution unchanged without campaign context

active matching campaign price applies

future campaign price does not apply

expired campaign price does not apply

archived campaign price does not apply

non-member SellerOffer does not receive campaign price

wrong campaign does not apply

wrong Store does not apply

wrong Currency does not apply where applicable

other current pricing dimensions remain respected

promo < normal -> compare-at + derived discount valid

promo >= normal -> no fake discount

member without promo -> normal price fallback

bulk resolution no N+1

Builder preview gets effective promo price

published SSR gets effective promo price

existing Manual/Category/Brand/Newest pricing unaffected

R17 PromotionCampaign source persistence unaffected

Development seed idempotent

Cart/checkout price authority proven or explicit guarded gap documented

recovery-staleness PASS

git diff --check PASS

18ca10c9 ancestor PASS

HEAD==origin/main PASS

Keep tests focused.

15. Real Runtime Proof

Use real Development runtime.

Prove:

A. Open page with Product Showcase source = پیشنهاد شگفت‌انگیز
B. active campaign products appear
C. at least 3 seeded members show real campaign promo selling prices
D. normal/original price shown only when valid
E. derived discount percent is mathematically correct if current card displays it
F. one campaign member without promo override uses normal price
G. no future price leakage
H. reload persists same behavior
I. published Storefront SSR contains promo prices
J. no console errors
K. no repeated price requests/polling

If Cart integration is safely completed:
L. add promo item to cart
M. server-side Cart price matches active campaign price
N. price changes/expiry are authoritatively handled according to existing checkout policy

Evidence:
docs/evidence/TB-P10-T022-R18/runtime-proof.md

16. Anti-Pattern Gate

Reject:

PromoAmount scalar on CampaignOffer

duplicate Pricing table/engine

client-authoritative promo price

stored discount percent

fake compare-at price

future/expired promo leakage

per-item pricing query

hardcoded AMAZING-specific ProductCard

ProductCard duplication

Builder redesign

campaign products copied into page JSON

inventory duplication

whole-repo audit

mandatory capability-map read on every task

P11 work

user-work overwrite

Evidence:
docs/evidence/TB-P10-T022-R18/antipattern-scan.md

17. Locks

Add after LOCK-SF-411:

LOCK-SF-412 Merchandising campaign promotional price participates in the canonical Pricing/AuthoredPrice engine through existing pricing dimensions/qualifiers; a parallel campaign price engine or scalar CampaignOffer PromoAmount is forbidden.
LOCK-SF-413 Campaign discount percentage is derived from applicable canonical normal price and active campaign selling price; it is never persisted as campaign truth.
LOCK-SF-414 Campaign price applicability is runtime-contextual: active campaign membership + Store + canonical pricing dimensions must all match; future/expired/archived campaign prices never apply.
LOCK-SF-415 PromotionCampaign pricing resolution is bulk/bounded and must not introduce per-item Pricing database queries.
LOCK-SF-416 TOOBA-CAPABILITY-MAP is a compact architectural ownership index updated when capability ownership/schema materially changes; it is not a mandatory per-task startup read.

Do not renumber existing locks.

18. Completion State

At completion:

Phase:
P10 / Builder Acceptance + Amazing Commercial Pricing

Last Architect-accepted before review:
TB-P10-T022-R17

Last Implementation:
TB-P10-T022-R18

Expected state:

PromotionCampaign Builder source remains working

real campaign-scoped promo pricing implemented through canonical Pricing OR explicit BLOCKER if unsafe

normal price unchanged outside campaign

compare-at/discount derived correctly

Development campaign promo-price seed exists

SSR pricing proven

Cart/Checkout authority proven or precise follow-up gap documented

lightweight TOOBA-CAPABILITY-MAP created from already-verified R14–R18 facts

no broad re-audit

no Admin Campaign UI yet

Appearance YES

Builder NO until user visually accepts

P11 retained for later

main

HEAD==origin/main

18ca10c9 ancestor

user work preserved

locks through LOCK-SF-416

Do NOT write Worker IDLE.

After canonical Result through Bridge:
STOP completely.

Acceptance

PASS only if:

canonical Pricing remains single source of truth

campaign price is dimension-safe

no scalar PromoAmount

active/future/expired applicability correct

derived discount semantics correct

bulk price resolution avoids N+1

Builder Preview + Storefront SSR show real campaign prices

existing Product Showcase sources regressions absent

capability map captures R14–R18 verified facts without new broad audit

user work preserved

canonical Result delivered

Worker stops

If correct canonical campaign pricing cannot be expressed safely:
Status must be BLOCKED, not a fake PASS.

Result Contract

Return ONLY canonical BRIDGE-WAKE-V1 Result with:

Summary
Applicable-Locks
Recovery-Start
Pricing-Discovery
Campaign-Price-Qualifier
Price-Precedence
Discount-Semantics
Bulk-Pricing
Test-Prices
Builder-Preview-Pricing
Storefront-Pricing
Cart-Price-Integrity
Capability-Map
Focused-Validation
Runtime-Proof
AntiPattern-Scan
Lock-Registry
Recovery-SoT
Git
Architectural-Concerns
Blockers

Do not include Worker-IDLE.

After sending Result through Bridge:
STOP completely.

END_TOOBA_TASK