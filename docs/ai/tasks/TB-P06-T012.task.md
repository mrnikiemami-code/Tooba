PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

TASK-ID: TB-P06-T012
PROJECT: Tooba
PHASE: P06 — Core API Integration / Operational Hardening
CHANNEL: tooba-main
STATUS: ISSUED
TASK-TYPE: SETTLEMENT / PAYOUT FOUNDATION
WORKER-POLICY: ONE WORKER = ONE ACTIVE TASK
PARENT-CONTEXT: TB-P06-T011 Returns/Refunds LIVE

Title

Marketplace Settlement & Payout Foundation — Accrual, Commission, Refund Adjustments, Seller Wallet + Admin Ops

Architect Context

TB-P06-T011 = Returns/Refunds end-to-end ACCEPTED or in flight
Payment `payment.succeeded.v1` and Returns `refund.succeeded.v1` events exist
P05 gate deferred settlement — this task delivers first live marketplace settlement
Locked: Order ≠ Payment ≠ Settlement; no cross-module SQL JOIN; SpiceDB ReBAC; MassTransit outbox; Shopeiva vendor wallet structure

Objective

Minimum production Settlement module: accrual on payment success, 10% commission snapshot, refund debit adjustments, payout request/process with Fake (dev) and FailClosed (production) gateways, seller HTTP APIs + Shopeiva-faithful vendor wallet UI, admin settlement balances and payout queue DataGrids.

Architecture Boundary

- New module `src/backend/Modules/Settlement/` with dedicated `settlement` schema
- Event handlers marketplace-gated only
- Bridges to Order/Payment/Returns via contract readers — no foreign schema joins
- Host HTTP in `src/backend/Host/Tooba.Host/Settlement/`
- Frontend: `settlement-api.ts`, `vendor-wallet-ui.tsx`, `/admin/settlement`, `/admin/payouts`
- Dev: `launchSettings.json` Marketplace edition; `MarketplaceDevelopmentBootstrap` migrates settlement; appsettings.Development stays SingleStore for tests

Out of Scope

- Real production payout provider integration
- Fake deposit / bank card UI
- Customer-facing wallet
- Settlement redesign of locked storefront surfaces

Commit message

feat add marketplace settlement and payout foundation [TB-P06-T012]

Evidence

docs/evidence/TB-P06-T012/ (01–25)

Tests

- Backend: SettlementFoundationTests.cs (3 cases)
- Frontend: settlement-api.test.ts
- Full backend suite target: 239 pass

Validation

```text
dotnet restore/build/test src/backend/Tooba.slnx
cd src/frontend && npm run typecheck && npm run lint && npm run test && npm run build
git diff --check
```

Shopeiva source

../SarvNewVerRequirment/reference/shopeiva/src/components/vendor/panel/wallet/wallet.jsx
