PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

TASK-ID: TB-P06-T011-R2
PROJECT: Tooba
PHASE: P06
CHANNEL: tooba-main
STATUS: ISSUED
TASK-TYPE: VISUAL ACCEPTANCE EVIDENCE REPAIR
PARENT-TASK: TB-P06-T011
PREVIOUS-REPAIR: TB-P06-T011-R1
WORKER-POLICY: ONE WORKER = ONE ACTIVE TASK

Title

Returns/Refunds Final Visual Acceptance Proof — Mandatory Live Shopeiva vs Tooba Browser Evidence

Architect Decision

TB-P06-T011-R1 is NOT accepted yet.

Backend repair is materially complete:

real Inventory restock implemented

idempotency dedup implemented

tests green

no cross-module DB violation reported

Remaining acceptance gap is VISUAL EVIDENCE.

The previous Task explicitly required:

actual Shopeiva runtime

browser side-by-side comparison

CSS/JS/motion/responsive proof

complete evidence set

Worker Result reported only evidence 01-03 and described PNG captures as optional.

That is not acceptable.

In this Task, browser evidence is MANDATORY.

Scope

Do NOT:

add product features

change Returns domain

change Refund domain

change Inventory behavior

redesign UI

refactor unrelated code

Only fix UI if a direct comparison reveals a real deviation.

Repository Recovery

Run:

git rev-parse --show-toplevel
git fetch origin
git branch --show-current
git rev-parse HEAD
git rev-parse origin/main
git status --short --branch

Expected predecessor:

92fa21e6068c89a889f1115b9859ffc65fb09eed

Require:

main

HEAD == origin/main

safe tracked tree

If conflict:
RECOVERY_CONFLICT

A — Start All Three Runtimes

Before any tracked changes, start:

Tooba Backend

Tooba Frontend

Original Shopeiva

Record:

command

PID

port

URL

HTTP status

Create:

docs/evidence/TB-P06-T011-R2/01-runtime-before-visual-proof.md

All three runtimes must be reachable.

If Original Shopeiva cannot run:
return BLOCKED.

Do NOT substitute static source-only proof.

B — Exact Source Confirmation

Reconfirm actual source files:

Customer:

returnFormModal.jsx

returnDetailModal.jsx

any order-detail integration component actually reused

Seller:

actual vendor order/detail/action source used as visual basis

Admin:

exact accepted Admin source-derived basis

Create:

docs/evidence/TB-P06-T011-R2/02-exact-source-confirmation.md

For each touched Tooba file include exact source mapping.

C — Customer Browser Side-by-Side

Capture Original Shopeiva and Tooba for:

return request form/modal

return detail/status

item selection / quantities

reason/note area

action buttons

loading/disabled/interactive state where applicable

Compare:

geometry

spacing

typography

radius

border

shadow

badge

icon

hover

focus

transition

mobile layout

Create:

docs/evidence/TB-P06-T011-R2/03-customer-side-by-side.md

PNG captures are REQUIRED.

D — Seller Browser Side-by-Side

Capture Original Shopeiva and Tooba for:

returns list

return detail

approve/reject workflow

pending/disabled action state

hover/focus interaction

mobile viewport

Create:

docs/evidence/TB-P06-T011-R2/04-seller-side-by-side.md

PNG captures are REQUIRED.

E — Admin Browser Proof

There may be no direct Shopeiva Admin.

Therefore compare Tooba Admin Returns against:

accepted T024 Admin shell

exact Shopeiva Vendor/Account patterns used as source basis

current existing Admin components

Capture:

Admin Returns list

Admin Return detail

Data Grid row/hover

action/detail state

mobile viewport

Create:

docs/evidence/TB-P06-T011-R2/05-admin-browser-proof.md

PNG captures are REQUIRED.

F — Motion / Interaction Proof

Static screenshots alone are NOT enough.

Using browser/CDP or equivalent, prove at least:

Customer:

modal open/close or interactive transition

Seller:

hover/focus/disabled/action transition

Admin:

row hover / detail interaction

Record:

initial computed state

interaction

changed computed state

transition/transform/opacity/class behavior

Create:

docs/evidence/TB-P06-T011-R2/06-motion-interaction-proof.md

G — CSS / JS / Responsive Parity Matrix

Create:

docs/evidence/TB-P06-T011-R2/07-css-js-responsive-parity.md

For each touched surface compare:

CSS:

display

gap

padding

margin

width/height

radius

border

shadow

typography

colors

Interaction:

hover

focus

active

disabled

reveal/hide

Motion:

transition-property

duration

easing

opacity

transform

modal behavior

Responsive:

breakpoints

stacking

action placement

overflow

Every material deviation must be:
REPAIRED
or
EXPLICITLY JUSTIFIED

H — Live Data Proof

Verify runtime UI contains no:

fake refund amount

fake status

fake timestamps

static demo return objects

fake successful actions

Create:

docs/evidence/TB-P06-T011-R2/08-live-data-runtime-proof.md

I — Inventory Restock Re-Verification

No new backend work expected.

Re-run tests proving:

restock exactly once

duplicate completion safe

partial restock correct

rejected/cancelled no restock

non-restockable no restock

Create:

docs/evidence/TB-P06-T011-R2/09-restock-regression-proof.md

J — Full Validation

Frontend:

cd src/frontend
npm run typecheck
npm run lint
npm run test
npm run test
npm run test
npm run test
npm run build

Run returns/admin tests if available.

Backend:

dotnet restore src/backend/Tooba.slnx
dotnet build src/backend/Tooba.slnx
dotnet test src/backend/Tooba.slnx

Require:
warnings = 0
errors = 0
failed = 0
skipped = 0

Always:

git diff --check

Create:

docs/evidence/TB-P06-T011-R2/10-final-validation.md

K — Final Runtime

After build/tests:
restart runtimes if needed.

Require:

Backend running

Tooba Frontend running

Original Shopeiva running

health/live = 200

health/ready = 200

Home = 200

PDP = 200

Customer Returns route reachable

Seller Returns route reachable

Admin Returns route reachable

Create:

docs/evidence/TB-P06-T011-R2/11-final-runtime.md

L — User Preview URLs

Return exact URLs:

Tooba:

Home

PDP

Customer Returns

Seller Returns

Admin Returns

Original Shopeiva:

comparable Customer route

comparable Seller route

If login required, state role/context only.
Do not expose secrets.

M — Source of Truth

Update:

P06 = IN_PROGRESS
TB-P06-T011 = REPAIR_REQUIRED
TB-P06-T011-R1 = REPAIR_REQUIRED
TB-P06-T011-R2 = AWAITING_ARCHITECT_ACCEPT

RETURNS_BACKEND = LIVE
REFUNDS_BACKEND = LIVE_FOUNDATION
INVENTORY_RESTOCK = LIVE
RETURNS_CUSTOMER_UI = LIVE
RETURNS_SELLER_UI = LIVE
RETURNS_ADMIN_UI = LIVE

VISUAL_CONTRACT = SHOPEIVA_LOCKED
UI_PRIORITY = BACKEND_TO_SHOPEIVA_LIVE_INTEGRATION

HOME_VISUAL_REVIEW = OPEN_FOR_USER_FEEDBACK
PDP_VISUAL_REVIEW = OPEN_FOR_USER_FEEDBACK

Update:

docs/PROJECT-STATE.md

docs/ROADMAP.md

docs/ai/TOOBA-RECOVERY-CONTEXT.md

docs/prompts/START-HERE-IF-CHATGPT-IS-LOST.md

Acceptance Criteria

PASS requires ALL:

Original Shopeiva runtime actually running

exact source files reconfirmed

Customer PNG side-by-side evidence

Seller PNG side-by-side evidence

Admin browser evidence

mobile evidence

interactive/motion evidence

CSS/JS/responsive parity matrix

no fake UI data

restock tests still green

frontend all green

backend 0/0/0/0

runtime healthy

preview URLs returned

repo synchronized

Worker may report:
RETURNS_REFUNDS_VISUAL_PARITY_PROVEN

Worker must NOT report:
USER_VISUAL_ACCEPTED

Git

git add ...
git commit -m "test prove returns Shopeiva visual parity [TB-P06-T011-R2]"
git push origin main
git fetch origin
git rev-parse HEAD
git rev-parse origin/main
git status --short --branch

Require:
HEAD == origin/main
tracked tree clean

Result Contract

PIPELINE-PROTOCOL: BRIDGE-WAKE-V1

BEGIN_TOOBA_WORKER_RESULT

Task-ID:
TB-P06-T011-R2

Channel:
tooba-main

Status:
PASS | FAIL | BLOCKED | RECOVERY_CONFLICT

Summary:
...

Runtime-Before:

Backend:

Tooba Frontend:

Shopeiva:

Source:

Customer files:

Seller files:

Admin basis:

Customer-SideBySide:

original captures:

Tooba captures:

desktop:

mobile:

repaired deviations:

Seller-SideBySide:

original captures:

Tooba captures:

desktop:

mobile:

repaired deviations:

Admin-Browser:

captures:

source basis:

responsive:

Motion:

Customer:

Seller:

Admin:

computed-state proof:

Parity:

CSS:

JS:

transition:

animation:

responsive:

unresolved material deviation:

Live-Data:

fake returns:

fake refunds:

fake actions:

Restock:

exactly once:

duplicate:

partial:

rejected/cancelled:

non-restockable:

Validation:

backend restore:

backend build:

backend tests:

warnings:

errors:

failed:

skipped:

typecheck:

lint:

critical-storefront:

storefront:

customer:

seller:

admin/returns:

frontend build:

git diff --check:

Runtime-After:

Backend:

Tooba Frontend:

Shopeiva:

health/live:

health/ready:

Home:

PDP:

USER-PREVIEW:

Home:

PDP:

Customer Returns:

Seller Returns:

Admin Returns:

Original Customer:

Original Seller:

Source-of-Truth:

P06:

T011:

T011-R1:

T011-R2:

returns backend:

refunds backend:

inventory restock:

customer UI:

seller UI:

admin UI:

visual contract:

Git:

commit:

push:

final HEAD:

origin/main:

synchronized:

tracked tree:

Architectural-Concerns:
...

Visual-Concerns:
...

Blockers:
...

END_TOOBA_WORKER_RESULT

After Result:

return to IDLE

do not self-issue another Task

keep Backend + Tooba Frontend + Original Shopeiva running where technically possible

END_TASK