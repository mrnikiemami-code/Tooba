import { ProductCreateScreen } from "../../product-create-screen";

/** مسیر اختصاصی ایجاد محصول — بدون فرم درون‌خطی روی فهرست. */
export default function AdminProductCreatePage() {
  return <ProductCreateScreen />;
}
